---
title: AGI Maze as a Benchmark Framework for World-Modeling Agents
title_zh: AGI迷宫：作为世界建模智能体的基准框架
authors: Alexey Potapov
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00627v1"
tags: ["query:world-models"]
score: 10.0
evidence: 面向智能体的世界建模基准
tldr: 大型语言模型在部分可观测、有状态的环境中难以维持世界表征。AGI Maze框架提供网格迷宫任务，用于评估智能体的世界建模能力。初步测试显示，普通LLM在推理时无法内部表示迷宫结构。即使允许使用消息历史作为工作记忆，基线智能体仍无法在人类够用的步数预算内可靠解决小迷宫。该工作强调了当前LLM在世界建模方面的局限性，并为未来研究提供了基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00627v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 433, \"height\": 507, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00627v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 614, \"height\": 735, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00627v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1532, \"height\": 872, \"label\": \"Table\"}]"
motivation: 当前LLM在需要持久、可操作的外部世界表征的任务中表现不佳，缺乏评估其世界建模能力的统一框架。
method: 提出AGI Maze框架，包含多种网格迷宫任务和清晰API，支持不同难度级别，用于测试智能体学习并使用世界状态表征的能力。
result: 普通LLM在推理时无法内部表示迷宫；使用消息历史作为工作记忆的基线智能体虽有所提升，但在步数预算内仍无法可靠求解小迷宫。
conclusion: AGI Maze揭示了LLM在简单世界建模任务上的不足，表明当前模型尚不具备有效的结构化世界表征能力，需进一步研究。
---

## 摘要
大型语言模型是强大的模式补全系统，但其默认操作模式——从静态上下文中预测下一个词元——并不能可靠地产生对外部世界的持久、可操控的表征。一旦环境部分可观测、具有状态、且需要对隐藏状态进行记忆和结构化假设，许多在文本中看似“推理”的任务会变得极其困难。AGI迷宫是一个轻量级框架，用于构建此类环境而无需高维感官输入。它提供了一系列基于网格的迷宫任务，具有清晰的API和多种难度模式。其目标是创建基准测试，要求智能体必须学习并使用世界状态表征，而不仅仅是根据现成的观测推断局部规则。我们初步评估了多个普通大型语言模型在简单迷宫上的表现，结果显示它们在推理时内部未能表征迷宫。我们还引入了一个基线智能体，允许其使用消息历史作为工作记忆，在智能体运行时构建观测的描述。尽管这可以提高性能，但对于一个大型语言模型智能体而言，即使在步数预算远超人类所需的情况下，仍然不足以可靠地解决即使是小型迷宫。

## Abstract
Large language models (LLMs) are powerful pattern-completion systems, but their default operating mode - predicting the next token from a static context - does not reliably produce persistent, manipulable representations of an external world. Many tasks that look like "reasoning" in text become substantially harder once the environment is partially observable, stateful, and requires memory and structured hypotheses about hidden state. AGI Maze is a lightweight framework for building such environments without requiring high-dimensional sensory inputs. It provides a family of grid-based maze tasks with a clean API and multiple difficulty regimes. The goal is to create benchmarks where agents must learn and use world state representations, not just infer a local rule over readily provided observations. We provide an initial evaluation of several vanilla LLMs on simple mazes showing that they fail to represent mazes internally at LLM inference time. We also introduce a baseline agent, which is allowed to use its message history as a working memory to construct descriptions of observations at agentic runtime. Although this can improve performance, it is still insufficient for an LLM agent to reliably solve even small mazes within a step budget that is more than enough for humans.

---

## 论文详细总结（自动生成）

# 论文总结：AGI Maze as a Benchmark Framework for World-Modeling Agents

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前大型语言模型（LLM）本质上是静态的模式补全系统，其默认操作（从静态上下文中预测下一个 token）无法可靠地产生对外部世界的持久、可操作的表征。在部分可观测、有状态、需要记忆和结构化假设的环境中，许多看似“推理”的任务变得极其困难。
- **研究动机**：现有 LLM 在需要持续世界状态跟踪的任务中（如部分可观测的迷宫导航）表现不佳，而现有基准（如 ARC-AGI-3）多是完全可观测、不要求跨时间维持可查询的世界状态，不能很好地测试智能体构建和使用世界表征的能力。
- **整体含义**：论文提出 AGI Maze 框架，旨在创建一个轻量级、低带宽（无需像素）、部分可观测的基准，用于评估智能体在语言领域的**世界建模**能力——包括表征潜在状态、维护不确定性信念、更新信念、基于表征进行推理、高效使用记忆等。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：设计一系列基于网格的迷宫任务，智能体只能通过文本观测（如“你尝试向右走，但被巨石挡住”）来感知环境，必须自行构建内部地图或状态表征，并进行推理以找到出口。环境包含多种机制增加部分可观测性和不确定性。
- **关键技术细节**：
  - **基本环境**：网格迷宫（典型大小 3×3 到 8×8），包含墙壁、不可破坏的边界（单一开口为出口）、宝藏和钥匙。智能体可执行上下左右动作，撞墙或边界消耗步数但不改变位置。需要先拿钥匙再拿宝藏，最后通过出口。
  - **动态部分可观测性增强机制**：
    - **河流**：踏入河流单元格会强制向下游移动（例如 2 格），但智能体不知道流向，需要推理。
    - **陷阱循环**：踏入陷阱会被传送到下一个陷阱（循环环绕），可能有多条循环。
  - **步数预算**：步数限制，迫使智能体高效导航和探索，不能盲目试错。
  - **扩展机制**：支持新物品（船、手榴弹、手电筒）、新单元格类型、新动作（爆破、照亮），要求智能体通过交互推断新规则，而非硬编码。
- **流程**：通过 HTTP API 提供环境，智能体从起始位置开始，逐次发送动作，接收文本观测，直至在预算内完成目标。未提供公式或算法伪代码。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：
  - 迷宫分为五组：**TUTORIAL**（教学，不计分）、**TRAINING**（小迷宫+宽松步数预算）、**CLASSIC**（较大迷宫，核心规则，对人类校准）、**EXTENDED**（新机制）、**HIDDEN**（私有保留，含未公开的新物品/单元格类型，用于测试泛化性）。
  - 具体测试了多个迷宫尺寸和难度：S0（仅有钥匙和宝藏）、S1（加河流）、S2（加陷阱）、S3（同时含河流和陷阱），尺寸从 3×3 到 5×4 不等。
- **基准**：
  - **随机行走智能体**（在每一步随机选择四个方向之一）作为基线。
  - **普通 LLM 智能体**：使用 GPT-4o Mini、GPT-5 Mini、Gemini 3.1 Flash Light、MiniMax M2.7 等，仅将游戏描述、历史动作-观测对拼接进提示，直接让 LLM 选择动作，无外部记忆或地图构建。
  - **规划智能体**（以 GPT-5.5、Gemini 3.5 Flash 为代表）：在动作前先写笔记到提示（作为工作记忆），记录位置、探索状态、计划等，然后执行动作。
- **对比方法**：无其他类型智能体（如强化学习、显式地图构建）参与对比。

## 4. 资源与算力

- **未明确说明**：论文未提及使用的 GPU 型号、数量、训练时长等算力信息。实验为推理级评估，不涉及模型训练。需要指出该信息缺失。

## 5. 实验数量与充分性

- **实验数量**：
  - 对每个迷宫条目（如 S0-01 等）包含 10 个不同迷宫，每个 LLM 在每个迷宫上执行 10 次试验，总计每个 LLM 每条目 100 次试验。
  - 提供了两个步数阈值（低预算和高预算，后者为前者的两倍）。
  - 共测试了 4 个轻量 LLM 加上随机基线，以及后续两个更强 LLM 在规划条件下的有限测试。
- **充分性评估**：
  - **优点**：多次重复试验统计成功率，覆盖不同迷宫尺寸和难度，区分有无动态不确定性。
  - **局限**：轻量 LLM 仅测试了 TRAINING 组，未在 CLASSIC/EXTENDED 上做全面测试；规划智能体仅针对 S3-03 一个迷宫做了对比；未与人类表现做系统对照（仅提及人类可轻松解小迷宫但未给出定量数据）。实验规模有限，尤其缺乏更复杂场景下的系统消融。

## 6. 论文的主要结论与发现

- 普通 LLM（vanilla LLM）在推理时无法在内部构建可靠的迷宫表征，即使是 3×3 的简单迷宫也无法在宽松步数预算内达到 100% 成功率。部分模型（如 GPT-4o Mini）的表现甚至低于随机行走基线。
- 允许 LLM 在提示中写笔记作为工作记忆（规划智能体）可以提升更强模型（GPT-5.5、Gemini 3.5 Flash）的表现（从 30-40% 提升到 60-70%），但对轻量模型无改善。
- 这些结果支持了论文的核心假设：默认情况下，LLM 不会在推理过程中自动构建可更新、可查询的显式世界状态表征；将记忆视为文本仍是脆弱且低效的。
- AGI Maze 框架能够有效暴露 LLM 在信念跟踪、地图构建、记忆使用、长程规划以及适应新机制方面的不足。

## 7. 优点

- **设计简洁**：无需像素或连续控制，仅用文本观测，隔离了感知问题，聚焦于表征和推理瓶颈。
- **逐步增强复杂性**：从简单迷宫到含河流、陷阱等动态元素，再到可扩展的新机制，支持系统评估泛化性。
- **提供标准 API 和公开网站**（agimaze.org），易于复现和扩展。
- **明确区分“规则发现”和“状态表征”**：强调世界建模需要处理潜在状态和信念，而非仅模式匹配。
- **训练/隐藏组设计**防止整体性能被单一策略破解，推动研究真正通用的世界建模能力。

## 8. 不足与局限

- **实验覆盖有限**：仅测试了少数轻量 LLM 和两个较强模型，未包含 GPT-4、Claude、开源大模型等更多代表性模型；未在 CLASSIC/EXTENDED/HIDDEN 组上做系统评估；规划智能体仅针对一个迷宫尺寸做了比较，缺少消融。
- **缺乏人类定量基线**：仅提及“人类可轻松解决小迷宫”，但未报告人类具体成功率或步数，影响公平对比。
- **未测试显式世界模型构建智能体**：论文虽提到未来方向是让智能体构建外部地图/模拟器，但本身未评估此类方法，结论局限于当前 LLM 的“零外部工具”能力。
- **步数预算设定较随意**：低预算为“软性合理”，但未详细说明如何校准，可能对某些模型不够公平。
- **随机基线表现异常**：在 5×4 大迷宫上，随机基线成功率仅为 3.2%，而 LLM 接近 0%，说明任务本身极难，可能部分归因于迷宫设计困难而非 LLM 特有缺陷。
- **未讨论 token 消耗问题**：将整个历史作为提示可能导致长上下文问题，但论文未评估 token 成本对性能的影响。

（完）
