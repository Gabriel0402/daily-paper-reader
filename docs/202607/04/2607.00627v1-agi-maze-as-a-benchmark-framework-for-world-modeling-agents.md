---
title: AGI Maze as a Benchmark Framework for World-Modeling Agents
title_zh: AGI迷宫：世界建模智能体的基准框架
authors: Alexey Potapov
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00627v1"
tags: ["query:world-models"]
score: 9.0
evidence: 世界模型智能体基准框架
tldr: 大型语言模型（LLM）在静态上下文预测中表现出色，但在部分可观测、有状态的环境中缺乏持久的世界表示能力。为此，提出AGI Maze框架，提供基于网格的迷宫任务，要求智能体学习并使用隐藏状态表示。初始评估显示，普通LLM无法在推理时内部表示迷宫；即使允许基线智能体使用消息历史作为工作记忆，仍无法在人类轻松的预算步数内可靠解决小迷宫。该框架为测试智能体的世界建模能力提供了基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00627v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 433, \"height\": 507}, {\"url\": \"assets/figures/arxiv/2607.00627v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 614, \"height\": 735}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00627v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1532, \"height\": 872}]"
motivation: LLM在部分可观测、需记忆和假设推理的环境中的世界建模能力不足，现有基准缺乏对隐藏状态结构假设的评估。
method: 提出AGI Maze框架，包含网格迷宫任务和干净API，支持多种难度；评估了普通LLM及使用历史消息记忆的基线智能体。
result: 普通LLM推理时无法内部表示迷宫；基线智能体利用工作记忆后性能提升，但仍无法在人类完成步数内可靠解决小迷宫。
conclusion: AGI Maze揭示了LLM在需要持久世界状态表示的任务上的显著不足，为未来研究方向提供基准。
---

## 摘要
大型语言模型（LLM）是强大的模式补全系统，但其默认操作模式——从静态上下文中预测下一个词元——并不能可靠地生成外部世界的持久、可操控的表示。一旦环境部分可观测、具有状态，并且需要记忆和关于隐藏状态的结构化假设，许多在文本中看似“推理”的任务会变得极其困难。AGI迷宫是一个轻量级框架，用于构建此类环境而无需高维感官输入。它提供了一系列基于网格的迷宫任务，具有简洁的API和多种难度级别。其目标是创建基准，其中智能体必须学习并使用世界状态表示，而不仅仅是根据现成观测推断局部规则。我们对几个通用LLM在简单迷宫上进行了初步评估，显示它们在LLM推理时无法在内部表示迷宫。我们还引入了一个基线智能体，允许其使用消息历史作为工作记忆，在智能体运行时构建观测描述。虽然这可以提高性能，但对于LLM智能体来说，即使在时间预算（对人类而言绰绰有余）之内，仍无法可靠地解决即使是小型的迷宫。

## Abstract
Large language models (LLMs) are powerful pattern-completion systems, but their default operating mode - predicting the next token from a static context - does not reliably produce persistent, manipulable representations of an external world. Many tasks that look like "reasoning" in text become substantially harder once the environment is partially observable, stateful, and requires memory and structured hypotheses about hidden state. AGI Maze is a lightweight framework for building such environments without requiring high-dimensional sensory inputs. It provides a family of grid-based maze tasks with a clean API and multiple difficulty regimes. The goal is to create benchmarks where agents must learn and use world state representations, not just infer a local rule over readily provided observations. We provide an initial evaluation of several vanilla LLMs on simple mazes showing that they fail to represent mazes internally at LLM inference time. We also introduce a baseline agent, which is allowed to use its message history as a working memory to construct descriptions of observations at agentic runtime. Although this can improve performance, it is still insufficient for an LLM agent to reliably solve even small mazes within a step budget that is more than enough for humans.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前大型语言模型（LLM）在静态文本预测任务中表现出色，但在需要持久、可操控的世界状态表示的任务中（尤其是部分可观测、有状态、需要记忆和结构化假设的环境），其“世界建模”能力严重不足。LLM 的默认操作模式（从静态上下文中预测下一个词元）并不主动构建外部世界的稳定表征，这限制了它们在智能体场景中的应用。
- **动机**：现有基准（如 ARC-AGI-3）多为全观测、无记忆需求的任务，无法有效测试智能体在部分可观测、需长期记忆和推断隐藏状态时的表现。论文提出 AGI Maze 框架，旨在填补这一空白，强调世界建模不仅仅是发现规则，更在于表示潜在状态、维护信念、更新证据并高效使用内存。
- **整体含义**：通过设计系列网格迷宫任务，揭示 LLM 在缺乏显式外部世界模型时的脆弱性，为研究更鲁棒的智能体世界建模能力提供标准化的测试床。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：构建一个轻量级、部分可观测的迷宫环境，智能体必须通过探索、记忆和推理来构建内部世界模型（如地图、状态估计），才能高效完成任务。环境不提供像素级输入，而是以文本形式提供观测和动作反馈。
- **关键技术细节**：
  - **基础环境**：网格迷宫（3×3～8×8），包含墙、巨石（单出口）、宝箱、钥匙。动作：上下左右。规则：撞墙/巨石不移动但消耗步骤；开宝箱需要钥匙；出口需要宝箱；部分迷宫额外需要出口钥匙。
  - **动态不确定性**：为增加部分可观测性，引入河流（触发强制向下游移动，流向未知）、陷阱循环（踩入后传送至下一个陷阱，循环可能多个）。这使智能体必须维护信念状态，无法仅靠局部探索确定位置。
  - **步数预算**：每个迷宫设有推荐步数上限，超限视为失败。困难迷宫需要精确探索和推理，否则无法在预算内完成。
  - **扩展性**：支持引入新物品（船、手榴弹、手电筒）、新细胞类型、新动作（爆破、照亮），迫使智能体适应新机制，避免过拟合。
  - **API**：基于 HTTP 的 REST API，提供游戏描述、启动、步行动作接口，返回文本观测和结构化字段。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：AGI Maze 框架内置 5 组迷宫群：
  - **TUTORIAL**：开放地图，用于学习基础机制，不评分。
  - **TRAINING**：小迷宫（3×3～5×4），宽松步数预算，用于基线对比和迭代。
  - **CLASSIC**：更大迷宫，核心规则，经人类校准。
  - **EXTENDED**：引入新机制（如河流、陷阱、新物品），测试泛化能力。
  - **HIDDEN**：私有保留集，含未知新机制，防止数据集拟合。
- **基准**：论文没有提供固定基准，而是用框架示例。实验中使用了随机游走智能体作为基础对比。
- **对比方法**：
  - **Vanilla LLM 智能体**：仅提供游戏描述、当前迷宫参数、历史行动-观测序列，无外部记忆/地图。测试的模型：GPT-4o Mini、GPT-5 Mini、Gemini 3.1 Flash Light、MiniMax M2.7。
  - **Planning 智能体（笔记型）**：允许智能体在行动前生成“笔记”作为工作记忆（记录坐标、探索状态、计划）。对比模型：GPT-5.5、Gemini 3.5 Flash（轻量模型也有尝试但效果差）。
- **实验场景**：共测试了 7 个迷宫条目（S0-01 至 S3-03），每个条目包含 10 个相同尺寸/规则的迷宫，每个 LLM 每个迷宫进行 10 次试验（共 100 次/条目）。两个步数阈值：下阈值（合理人类水平）和上阈值（两倍），但论文未揭示具体数值。

## 4. 资源与算力

- **文中未明确说明**使用的 GPU 型号、数量、训练时长等任何算力信息。只提到进行了多次 LLM 推理（每次试验调用 API），未提及训练过程。因此无法评估计算成本。

## 5. 实验数量与充分性

- **实验数量**：
  - Vanilla 智能体：每个 LLM 在 7 个迷宫条目下各 100 次试验，共约 7×4×100 = 2800 次运行（轻量模型）。加上随机游走基线。
  - Planning 智能体：仅针对 S3-03 条目，两个模型（GPT-5.5、Gemini 3.5 Flash），每个模型 100 次试验（共 200 次）。
  - 强模型（GPT-5.5、Gemini 3.5 Flash）在 S3-03 上有少量额外试验（未指明次数）。
- **充分性评估**：
  - **优点**：覆盖了不同尺寸、不同复杂度（无动态元素、有河流/陷阱混合）的迷宫，且对比了随机游走，提供了初步基线。
  - **不足**：
    - 实验仅局限于 TRAINING 组（小迷宫），未在 CLASSIC、EXTENDED、HIDDEN 组上测试（仅提及强模型在 S3-03 上的表现，但 S3-03 属于 TRAINING？从表1看 S3-03 为 5×4 含所有元素，属于 TRAINING 组？论文中未明确分组对应表1，但描述说 TRAINING 为小型，S3-03 大小为 5×4 可视为小迷宫）。
    - 论文明确表示“vanilla 智能体未能可靠解决简单迷宫”，因此未在更困难组测试。这合理，但限制了结论的广谱性。
    - 只有 10 次试验/迷宫，样本量偏小（共 100 次/条目），可能存在随机波动。
    - 未对提示工程（如工作记忆如何引导）进行系统性消融。仅对比了“有笔记” vs “无笔记”两种条件，但仅有两个强模型在单一迷宫上的数据。
    - 未进行人类对照实验（仅提及其对人类轻松，但未给出人类平均成功率或步数）。
    - 随机游走基线本身也仅有 200 次试验（每个迷宫 10 次？文中说 random walk 试验次数？表1中随机数据是 10 次迷宫×？实际上每个迷宫试验次数未明确，但表头显示随机列百分比，推测也是 100 次/条目？但文本说“10 trials per each maze were performed for each LLM”，对随机游走未说明）。
    - 缺乏对步数预算下阈值的详细说明，只提了“下阈值”和“上阈值”，但未公开具体值。

**总体评价**：实验设计作为初步基准演示是充分的，验证了核心假设，但作为完整 benchmark 还需要更多样本、更全面的迷宫组、更多模型、以及更细致的消融。

## 6. 论文的主要结论与发现

- Vanilla LLM 智能体（无外部记忆/地图）即使在小迷宫上也表现极差，部分模型（如 GPT-4o Mini）效果甚至低于随机游走。说明 LLM 在推理时并未自主构建持久的世界表示。
- Planning 智能体（使用提示笔记作为工作记忆）对于强模型（GPT-5.5、Gemini 3.5 Flash）有显著提升（在 S3-03 上从 30%~40% 提升至 60%~70%），但仍无法达到 100% 且对人类来说仍然简单。对于轻量模型，笔记无改善（零分）。
- 结论：当前 LLM 内在推理缺乏显式世界模型能力；即使可以通过文本工作记忆辅助，仍不足以高效解决需要状态跟踪的迷宫任务。这证明 AGI Maze 框架能够有效暴露 LLM 的世界建模缺陷。

## 7. 优点：方法或实验设计上的亮点

- **框架设计精良**：AGI Maze 提供简洁、可扩展的 API，便于集成各种智能体。环境通过文本观测避开了像素级感知，使瓶颈聚焦于表示和推理。
- **核心原则清晰**：强调“世界建模”不仅是规则发现，更是状态表示、信念更新和记忆管理。迷宫动态不确定性（河流、陷阱）设计巧妙，能有效测试智能体对隐藏状态的处理。
- **扩展性**：支持新物品/机制，可防止过拟合，促进通用智能体的研究。
- **初步基线具有启发性**：通过简单实验（随机游走、vanilla LLM、规划智能体）揭示了 LLM 在部分可观测任务中的系统性失败，为后续研究提供了明确起点。
- **基准设计考虑公平性**：提供人类主观校准步数预算、隐藏测试集等措施，有助于评估真正的泛化能力。

## 8. 不足与局限

- **实验覆盖不完整**：仅在 TRAINING 组小迷宫上系统测试，未在 CLASSIC/EXTENDED/HIDDEN 组进行。对大型迷宫或复杂机制（如河流+陷阱混合）仅有一个示例（S3-03）且只用了强模型。
- **样本量有限**：每个迷宫条目的试验次数仅为 10×10，统计功率较弱；随机游走基线次数未明确，可能存在噪声。
- **提示设计未深入分析**：规划智能体仅使用最简单的笔记指令，未对更有效的提示策略（如要求构建地图、使用结构化格式）进行探索。未比较不同 LLM 在相同提示下的差异。
- **未进行人类对照实验**：只提及“对人类容易”，但缺乏具体数据，无法量化 LLM 与人类的差距。
- **步数预算细节缺失**：未公布具体步数阈值（比如下阈值和上阈值数字），使得结果复现和比较困难。
- **未分析失败模式**：LLM 为什么失败（迷失方向、死循环、记忆溢出等）没有详细诊断。
- **计算资源未报告**：无法评估实验成本或可重复性。
- **应用限制**：AGI Maze 聚焦网格迷宫，虽然可扩展，但真实世界建模（如连续控制、物理交互）仍不在其范围内，不能直接推广至所有领域。

（完）
