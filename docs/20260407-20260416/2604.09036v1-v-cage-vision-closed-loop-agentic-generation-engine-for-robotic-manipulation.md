---
title: "V-CAGE: Vision-Closed-Loop Agentic Generation Engine for Robotic Manipulation"
title_zh: V-CAGE：用于机器人操作的视觉闭环智能生成引擎
authors: "Yaru Liu, Ao-bo Wang, Nanyang Ye"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09036v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 该论文专注于通过自主数据合成来扩展用于机器人操作的视觉-语言-动作（VLA）模型。
tldr: V-CAGE是一种用于机器人操作的自动数据合成引擎，通过代理化框架结合语义推理与物理交互，以场景修补指导生成上下文一致的可达布局，并利用视觉语言模型实施闭环验证和感知压缩，实现高质量、可扩展的视觉-语言-动作数据生成。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09036v1/fig-001.webp\", \"caption\": \"TABLE III ABLATION OF IGSC ON SUB-TASK GENERATION ACROSS SCENES\", \"page\": 7, \"index\": 1, \"width\": 512, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2604.09036v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the V-CAGE framework.This end-to-end engine automates robotic data synthesis by bridging high-level semantic reasoning with IGSC to generate context-aware, physically valid environments across diverse domains. The agentic architecture utilizes VLM-based closed-loop verification to autonomously refine trajectories and sever error propagation chains in long-horizon manipulation tasks. The final output is optimized via action-aware perceptual compression, achieving around 93% reduction in storage size without compromising visual fidelity for downstream VLA training.\", \"page\": 3, \"index\": 2, \"width\": 1072, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2604.09036v1/fig-003.webp\", \"caption\": \"Fig. 2. Examples of context-aware scenes generated by V-CAGE, including home, office, retail (supermarket), and kitchen settings.\", \"page\": 4, \"index\": 3, \"width\": 526, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2604.09036v1/fig-004.webp\", \"caption\": \"Fig. 3. Four complex, long-horizon tasks synthesized by our pipeline.\", \"page\": 5, \"index\": 4, \"width\": 529, \"height\": 294}]"
motivation: 现有场景生成方法语义一致性和物理可行性不足，导致机器人任务失败率高。
method: 采用具身的代理系统，结合场景修补指导构建、视觉语言模型闭环验证和感知驱动压缩，实现自主的高保真数据生成。
result: "系统生成的场景语义结构合理、运动可达性高，并在压缩后保持训练效果同时减少90%以上数据量。"
conclusion: V-CAGE实现了高质量、可扩展的机器人操作数据自动合成，有望推动视觉-语言-动作模型的发展。
---

## 摘要
扩展视觉-语言-动作（VLA）模型需要大规模的数据集，这些数据集既要具备语义一致性，又要在物理上可行。然而，现有的场景生成方法往往缺乏上下文感知能力，难以合成嵌入丰富语义信息的高保真环境，这常常导致目标位置不可达，从而使任务过早失败。我们提出了 V-CAGE（Vision-Closed-loop Agentic Generation Engine），一种用于自主机器人数据合成的智能代理框架。与传统的脚本化流程不同，V-CAGE 作为一个具身智能系统运行，利用基础模型将高层语义推理与低层物理交互相结合。具体而言，我们引入了 “Inpainting-Guided Scene Construction”（修补引导的场景构建）方法，以系统化地生成具备上下文感知的布局，确保生成的场景既有语义结构，又在运动学上可达。为保证轨迹的正确性，我们结合功能性元数据与基于视觉-语言模型的闭环验证机制，实现视觉评论者（visual critic）的作用，以严格过滤潜在的静默错误，并切断错误传播链。最后，为克服大规模视频数据集的存储瓶颈，我们实现了一种感知驱动的压缩算法，在不影响后续 VLA 训练效果的前提下，实现了超过 90% 的文件体积压缩。通过集中化的语义布局规划与视觉自验证机制，V-CAGE 实现了端到端流程的自动化，使多样化、高质量的机器人操作数据集的高扩展性合成成为可能。

## Abstract
Scaling Vision-Language-Action (VLA) models requires massive datasets that are both semantically coherent and physically feasible. However, existing scene generation methods often lack context-awareness, making it difficult to synthesize high-fidelity environments embedded with rich semantic information, frequently resulting in unreachable target positions that cause tasks to fail prematurely. We present V-CAGE (Vision-Closed-loop Agentic Generation Engine), an agentic framework for autonomous robotic data synthesis. Unlike traditional scripted pipelines, V-CAGE operates as an embodied agentic system, leveraging foundation models to bridge high-level semantic reasoning with low-level physical interaction. Specifically, we introduce Inpainting-Guided Scene Construction to systematically arrange context-aware layouts, ensuring that the generated scenes are both semantically structured and kinematically reachable. To ensure trajectory correctness, we integrate functional metadata with a Vision-Language Model based closed-loop verification mechanism, acting as a visual critic to rigorously filter out silent failures and sever the error propagation chain. Finally, to overcome the storage bottleneck of massive video datasets, we implement a perceptually-driven compression algorithm that achieves over 90\% filesize reduction without compromising downstream VLA training efficacy. By centralizing semantic layout planning and visual self-verification, V-CAGE automates the end-to-end pipeline, enabling the highly scalable synthesis of diverse, high-quality robotic manipulation datasets.

---

## 论文详细总结（自动生成）

# V-CAGE：用于机器人操作的视觉闭环智能生成引擎 — 论文总结

---

## 一、研究背景与核心问题

- **研究动机：**  
  视觉-语言-动作（VLA）模型在机器人操作中展现出巨大潜力，但其**泛化性和推理能力需依赖海量高质量专家轨迹数据**。  
  现实世界中采集多步互动数据成本极高，且难以覆盖长尾情境，因此**基于仿真的数据自动生成**成为关键路径。  

- **核心问题：**  
  当前仿真任务生成面临三大限制：
  1. **语义脱节：** 场景生成缺乏上下文感知，易出现语义混乱、布局不合理。  
  2. **物理不可行：** 目标位置不可达或几何冲突频发。  
  3. **开环执行：** 缺乏视觉验证，导致“静默失败”传播。  

  本文的目标是设计一个能**在语义一致性与物理可行性之间闭环协调的自动数据合成框架**。

---

## 二、方法论概述

### 1. 框架核心思想

- **V-CAGE（Vision-Closed-loop Agentic Generation Engine）**：  
  一种具身（embodied）代理化系统，结合**大模型推理**与**物理仿真闭环验证**，实现从任务描述到可执行交互轨迹的自动生成。  
  整个流程基于 **OpenClaw agentic 框架**组织，形成自主的技能调用与模块式场景合成。

### 2. 关键技术模块

#### (1) Inpainting-Guided Scene Construction（IGSC）
- 输入：自然语言任务指令 *I*。  
- 输出：上下文感知、运动学可达的场景 *S\**。  
- 由四个阶段组成：
  1. **对象选择与初始无碰撞放置：**  
     LLM 读取任务并从资产库筛选相关物体集 *A\**，通过 AABB（Axis-Aligned Bounding Box）检测避免相交。  
  2. **功能布局规划与修补式优化：**  
     生成布局描述 *L* → 送入图像修补模型（Nano Banana）重排列对象，得到视觉一致的目标布局图 *I_tgt*。  
  3. **视觉对应匹配与坐标恢复：**  
     使用 **Grounding DINO + DINOv2** 提取特征并匹配对象，从图像重建世界坐标。  
  4. **碰撞优化：**  
     构建最小化代价函数  
     \( J(x) = λ_cJ_{coll} + λ_dJ_{disp} + λ_bJ_{bnd} \)，  
     通过 L-BFGS-B 求解实现最优几何分布。

#### (2) 模板化子任务搜索
- 将复杂任务拆为**可执行原子子任务**（如 pick-and-place），由 LLM 依据功能点元数据匹配可执行代码模板。  
- 确保语义合理、动作可行，为闭环执行提供可靠输入。

#### (3) 基于视觉语言模型的闭环验证
- 利用 **Gemini 3 VLM** 作为“视觉评论者”（visual critic），执行结果图像与任务描述匹配。  
- 对长时序轨迹 \(T_{1:k}\)，验证条件为：
  \[
  \prod_{i=1}^{k} \varphi_{VLM}(I^{(i)}_{img}, T_i) = 1
  \]
  若任一步失败（取 0），则整条轨迹废弃并重采样。  
  → 形成**拒绝采样式闭环生成**，彻底切断错误传播。

#### (4) 行为感知压缩（Action-Aware Perceptual Compression）
- **动作用途：** 克服视频数据存储瓶颈。  
- **策略：**
  - 优化关键帧提取：只保留交互峰值帧（约每序列 8 帧）。
  - 使用 **ColorVideoVDP** 衡量 JOD 差异 < 0.1，确保“视觉无损”。
  - 采用 **HEVC 编码 + CRF 调优**，实现文件体积 **压缩 93%**。

---

## 三、实验设计

### 1. 任务与数据集
- 使用 V-CAGE 自合成数据训练 VLA 模型 π0.5。  
- 四个多步任务（均含复杂语义与物理约束）：
  - AutoCheckout（扫描与支付）  
  - PackBreads（装箱面包）  
  - PackStationery（收纳文具）  
  - SortToCabinet（分类入柜）  

### 2. 对比设定
- **Baseline：**
  - 随机布局生成。  
  - 无 VLM 的开环流程。  
- **验证指标：** 任务成功率（Success Rate, SR）、语义一致性、Sim2Real 成功率。

### 3. Sim-to-Real 验证
- 硬件：ALOHA-AgileX 平台。  
- 任务：抓取四块积木放入指定托盘。  
- 数据：10 条真实专家演示 + 250 条模拟轨迹。  
- 比较：Real-Only vs. Real + Sim 联合训练。

### 4. 视频压缩实验
- 对比“原始视频数据集”和“压缩数据集”对下游训练性能的影响。

### 5. 消融实验
- **IGSC 模块消融：** 对比随机布局与修补布局在不同场景类别（厨房、办公室、浴室、柜体等）下可执行任务比例。  
- **VLM 闭环验证消融：** 比较未加验证（开环）与加入验证的生成数据纯度，测量 Ground Truth 成功率。

---

## 四、算力与资源

- 文中**未披露具体硬件配置或 GPU 数量、训练时长**。  
- 使用的主要计算组件：
  - SAPIEN 物理仿真引擎  
  - LLM 与 VLM（Gemini 3）  
  - 视觉模型（Grounding DINO、DINOv2）  
- 暗示需要较高算力以支撑多阶段仿真与视觉推理，但无显式量化指标。

---

## 五、实验规模与充分性

- **实验数量：**
  - 4 个主要任务 × 各 100 条轨迹。  
  - Sim2Real 测试：20 次物理试验。  
  - 2 组主要消融试验（IGSC、VLM）。  
- **充分性：**
  - 任务设计覆盖多领域（厨房、超市、办公室等）。  
  - 消融实验验证模块必要性与有效性。  
  - 虽然实验数量中等，但体现了闭环生成在多维场景下的可扩展性与效果。
- **客观性：**
  - 定量指标明确（成功率、压缩比例、纯度）。  
  - 但未报告标准差、统计显著性等信息，略有不足。

---

## 六、主要结论与发现

1. **生成质量卓越：**  
   IGSC 模块显著降低几何冲突与语义错误，提升场景可执行性（有效任务比例提升至 80% 左右）。  
2. **闭环验证有效：**  
   使用 VLM 过滤后，所有纳入数据的语义正确率达到 **100%**，彻底去除“静默错误”。  
3. **数据压缩可行：**  
   93% 压缩后，训练性能几乎无损。  
4. **下游提升明显：**
   - π0.5 模型从 0% 提升至最高 100% 任务成功率。  
   - Sim2Real 成功率由 20% → 55%，验证了仿真数据对真实任务的增益。

---

## 七、方法优点

- **端到端闭环自动化：** 将语义理解、物理仿真、视觉验证和数据压缩串联为统一系统。  
- **上下文感知场景生成：** 利用修补模型维持语义结构一致。  
- **强鲁棒性：** 通过 VLM 评估保证生成轨迹的因果一致性。  
- **高扩展与高效压缩：** 数据体积减少 90% 以上，利于大规模训练。  
- **模块化设计：** 建立在 OpenClaw 框架上，易复用与扩展。

---

## 八、不足与局限

- **计算成本高：** 闭环验证基于拒绝采样，生成复杂任务时效率较低。  
- **任务范围有限：** 仅验证桌面级操作，未覆盖柔性物体、流体等动态交互。  
- **硬件资源未报告：** 缺乏算力透明度，重现难度较大。  
- **统计分析不足：** 缺乏方差与置信区间数据，实验稳健性仍需强化。  
- **未来方向：**  
  - 以 VLM 评论者提供**密集奖励信号**实现在线修正。  
  - 拓展至**非刚体交互任务与实时系统**。

---

**（完）**
