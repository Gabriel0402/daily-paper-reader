---
title: "Coalitional Zero-Sum Games for ${H_{\\infty}}$ Leader-Following Consensus Control"
title_zh: "用于 ${H_{\\infty}}$ 领航者-跟随者一致性控制的联盟零和博弈"
authors: "Yunxiao Ren, Dingguo Liang, Yuezu Lv, Zhisheng Duan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06089v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 利用微分博弈论将多智能体一致性问题建模为联盟零和博弈。
tldr: 本文针对多智能体系统在对抗性干扰下的领航-跟随一致性控制问题，运用微分博弈理论构建联盟化零和博弈模型，提出分布式分解和解耦算法以降低高维求解复杂度，并通过仿真验证算法能稳健实现H∞一致性控制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06089v1/fig-001.webp\", \"caption\": \"Fig. 6. The evelotion of the norm of the global tracking error.\", \"page\": 9, \"index\": 1, \"width\": 430, \"height\": 333}]"
motivation: 为应对多智能体系统在存在对抗性外部输入下的鲁棒一致性控制挑战。
method: 基于微分博弈理论，将鲁棒领航-跟随控制问题建模为全局联盟化的零和博弈，并提出分布式求解和解耦算法。
result: 提出的分布式策略成功将高维广义代数Riccati方程分解为低维形式，并实现鲁棒的H∞控制策略。
conclusion: 研究证明所提博弈理论控制与分布式算法可有效提升系统鲁棒性与可实施性。
---

## 摘要
本文研究了一类受到类似对抗攻击的外部输入影响的多智能体系统的领航者-跟随者一致性问题。为此，我们利用微分博弈理论将鲁棒领航者-跟随者控制问题表述为一个全局联盟最小-最大零和博弈。具体来说，智能体的控制输入形成一个联盟以最小化全局代价函数，而攻击则形成对立联盟以使其最大化。值得注意的是，当这些外部对抗攻击表现为扰动时，所设计的基于博弈论的控制策略系统性地导出了一个鲁棒的 $H_\infty$ 控制律。解决该问题本质上需要求解一个高维广义代数Ricatti方程（GARE），这对分布式计算和控制器实现带来了显著挑战。为克服这些困难，我们提出了两步解决方案。首先，设计了一种分散化计算策略，将高维的 GARE 分解为多个统一的低维 GARE。其次，开发了一种基于动态平均一致性的解耦算法，用于消除鲁棒控制律中的固有耦合结构，从而实现其分布式实现。最后，通过具有反馈线性化动力学的多车辆系统编队控制的数值仿真验证了所提算法的有效性。

## Abstract
This paper investigates the leader-following consensus problem for a class of multi-agent systems subject to adversarial attack-like external inputs. To address this, we formulate the robust leader-following control problem as a global coalitional min-max zero-sum game using differential game theory. Specifically, the agents' control inputs form a coalition to minimize a global cost function, while the attacks form an opposing coalition to maximize it. Notably, when these external adversarial attacks manifest as disturbances, the designed game-theoretic control policy systematically yields a robust $H_\infty$ control law. Addressing this problem inherently requires solving a high-dimensional generalized algebraic Riccati equation (GARE), which poses significant challenges for distributed computation and controller implementation. To overcome these challenges, we propose a two-fold approach. First, a decentralized computational strategy is devised to decompose the high-dimensional GARE into multiple uniform, lower-dimensional GAREs. Second, a dynamic average consensus-based decoupling algorithm is developed to resolve the inherent coupling structure of the robust control law, thereby facilitating its distributed implementation. Finally, numerical simulations on the formation control of multi-vehicle systems with feedback-linearized dynamics are conducted to validate the effectiveness of the proposed algorithms.

---

## 论文详细总结（自动生成）

## 一、研究背景与核心问题

- **研究动机**：  
  在多智能体系统（Multi-Agent Systems, MASs）中，实现**领航者-跟随者一致性控制（Leader-Following Consensus）**是分布式控制领域的核心问题之一。随着**网络化控制系统和网络物理系统（CPS）**的快速发展，系统越来越容易受到**对抗性攻击或外部有意扰动**。这些扰动不同于随机噪声，而是“智能化”的破坏性输入，会削弱系统性能乃至破坏稳定性。

- **核心问题**：  
  针对这种恶意外部输入，论文将控制问题视为一个**对抗博弈问题**，即控制器（防御方）与攻击者（对抗方）之间的**零和博弈**。目标是使系统在最不利条件下仍能保持收敛与稳定，实现鲁棒的 \( H_\infty \) 性能。

- **研究目标**：  
  实现**鲁棒的领航-跟随一致性控制策略**，在仅依赖局部邻居信息的情况下抵御对抗性扰动，同时解决高维系统求解复杂、分布式实现困难等瓶颈。

---

## 二、方法论与技术框架

### 1. 理论框架

- 作者基于**微分博弈理论（Differential Game Theory）**建立了一个**全局联盟化（Coalitional）零和博弈模型**。
- 控制器联盟（所有智能体的输入 \(u_i\)）共同最小化全局代价函数 \(J\)；
- 攻击者联盟（所有扰动 \(w_i\)）共同最大化该代价函数；
- 通过对该**min–max问题**求鞍点（saddle-point），获得满足鲁棒 \(H_\infty\) 性能的最优控制律。

### 2. 数学模型

- 系统动态方程：
  \[
  \dot{x}_i = A x_i + B u_i + D w_i
  \]
- 领导者状态：
  \[
  \dot{x}_0 = A x_0
  \]
- 构建邻居误差向量、通信拓扑矩阵 \(L+G\)，得到整体系统误差动力学；
- 定义性能指标：
  \[
  J = \frac{1}{2}\int_0^\infty (\delta^T Q \delta + u^T R u - w^T \Gamma w)\, dt
  \]
  其中 \(Q, R, \Gamma\) 为加权矩阵。

### 3. 解法结构

- 利用贝尔曼最优原理推导广义代数Riccati方程（GARE）：
  \[
  \bar{A}^T P + P\bar{A} + Q - P\bar{B}R^{-1}\bar{B}^T P + P\bar{D}\Gamma^{-1}\bar{D}^T P = 0
  \]
- 由此得到最优控制和最坏攻击：
  \[
  u^* = -R^{-1}\bar{B}^T P \delta, \quad w^* = \Gamma^{-1}\bar{D}^T P \delta
  \]

### 4. 分布式解耦与实现算法

- **问题**：直接求解上述高维GARE需要全局信息，代价复杂度 \(O(N^3 n^3)\)。
- **解决方案**：
  1. **分布式计算分解策略**：  
     合理设计矩阵结构，使GARE按智能体分块分解为多个独立、低维的局部GARE。
  2. **基于动态平均一致性（Dynamic Average Consensus）的解耦算法（Algorithm 1）**：  
     使用局部状态与邻居信息，通过有限时间动态一致算法实现控制律耦合项的分布式估计与解耦。  
     算法保证输出 \(\hat{u}_i, \hat{w}_i \to u^*_i, w^*_i\) 在有限时间内收敛。

- 该算法通过Lyapunov分析严格证明了**稳定性与有限时间收敛性**。

---

## 三、实验设计与验证

- **实验场景**：  
  多车辆系统（multi-vehicle systems）的协调编队控制。车辆经过**反馈线性化**简化为线性系统。

- **设置**：
  - 智能体数量：8 个车辆 + 1 个领航者；
  - 每辆车状态：二维位置与速度 [\(q_x, q_y, \dot{q}_x, \dot{q}_y\)];
  - 通信拓扑：无向连通图（见 Fig.2）；
  - 本地权重矩阵：\(Q_i = 10I_4, R_i = I_2, \gamma_i = 2\)；
  - 本地算法增益：\(\alpha_i = 2100, \beta_i = 34000\)。

- **对比基线**：  
  文中未与其他算法建立直接对比，而是通过：
  1. 验证明算法的快速收敛特性；
  2. 展示控制律与最坏攻击的时序变化；
  3. 显示系统在攻击下仍保持编队稳定性和轨迹跟踪性能。

- **评估指标**：
  - 误差收敛性；
  - 控制信号与攻击信号的时序响应；
  - 编队轨迹与全局跟踪误差标准。

---

## 四、算力条件

- 文中**未提及任何计算资源或硬件信息**，也未说明运行时间、处理器配置或仿真平台（例如 MATLAB / Python / GPU）。  
  从描述方式判断，实验为**仿真级别验证**，非深度学习训练，不涉及大规模算力。

---

## 五、实验丰富性与充分性

- **实验数量**：  
  一组主要实验（8辆车+1领航者），包含：
  - 动态一致算法收敛曲线（Fig.4）；
  - 编队轨迹（Fig.5）；
  - 全局误差演化（Fig.6）；
  - 控制输入（Fig.7）；
  - 最坏攻击输入（Fig.8）。
- **实验充分性**：
  - 优点：覆盖了算法收敛性、控制性能、扰动鲁棒性三个层面；
  - 不足：仅限单一场景、无对比实验，未展示网络规模增长下的扩展性结果。

---

## 六、主要结论与发现

- 通过将鲁棒一致性控制建模为**全局联盟min-max博弈**，作者导出了确保系统 \(H_\infty\) 性能的分布式控制律。
- 提出的**分散计算+动态一致解耦双策略**确保了：
  - 高维GARE可被等价分解为低维形式；
  - 控制律可分布式实现；
  - 算法在有限时间内收敛。
- 仿真验证显示，在存在攻击和编队切换的情况下，系统能迅速恢复一致性并保持稳定。

---

## 七、论文优点

- **理论创新**：首次将领航-跟随一致性问题系统化表述为**联盟型零和博弈**；
- **方法可扩展性强**：构造低维GARE后，各智能体计算复杂度与网络规模无关；
- **可实现性高**：动态平均一致算法实现真正的分布式控制；
- **权重设计简洁**：代价函数权重结构直观且用户友好；
- **严格的数学证明**：有限时间收敛、稳定性与鲁棒性均有理论支撑。

---

## 八、局限与未来展望

- **实验覆盖不足**：  
  无与现有 \(H_\infty\) 控制或图博弈方法的对比实验；
- **应用局限性**：  
  当前模型与算法建立在**线性动态系统**假设下，对非线性系统的适用性尚待验证；
- **可扩展性验证缺失**：  
  未展示当智能体数量增加时算法的计算性能和通信负载情况；
- **参数整定依赖度较高**：  
  局部增益 \(\alpha_i, \beta_i\) 的选取依赖特定理论条件，实际系统调参成本可能较高。

---

**（完）**
