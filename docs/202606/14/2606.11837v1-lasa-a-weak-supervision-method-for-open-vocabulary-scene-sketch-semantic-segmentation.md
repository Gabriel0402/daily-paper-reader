---
title: "LASA: A Weak Supervision Method for Open-Vocabulary Scene Sketch Semantic Segmentation"
title_zh: "LASA: 一种用于开放词汇场景素描语义分割的弱监督方法"
authors: "Liwen Yi, Xianlin Zhang, Yue Zhang, Yue Ming, Xueming Li"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11837v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 基于弱监督的草图开词汇语义分割
tldr: 开放词汇场景草图语义分割面临缺乏纹理和颜色、依赖笔触结构的挑战，单层视觉语言特征不稳定。本文观察到不同ViT层的注意力图编码互补空间线索：浅层捕获全局布局，深层聚焦局部细节。据此提出LASA方法，通过跨层聚合结构注意，在弱监督下指导层次语义对齐并优化推理。在FS-COCO、SFSD和FrISS数据集上，mIoU分别提升+3.43、+8.01和+15.74，显著提升了分割精度和空间连贯性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11837v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 824, \"height\": 593, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11837v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1726, \"height\": 1016, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11837v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 847, \"height\": 574, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11837v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1778, \"height\": 632, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11837v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 843, \"height\": 704, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11837v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1760, \"height\": 647, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11837v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 891, \"height\": 736, \"label\": \"Table\"}]"
motivation: 单层视觉语言特征无法稳定捕获草图的稀疏笔触结构，需利用多层注意力互补信息以提供更鲁棒的结构先验。
method: 提出LASA框架，聚合跨ViT层注意力图形成结构注意，在弱监督下进行层次语义对齐，并在推理时细化预测。
result: 在FS-COCO、SFSD和FrISS上mIoU分别提升+3.43、+8.01和+15.74，优于现有弱监督方法。
conclusion: LASA通过跨层结构注意有效提升了草图语义分割的性能和空间一致性，验证了多层注意力互补性的关键作用。
---

## 摘要
开放词汇场景素描语义分割旨在根据推理时指定的灵活类别词汇表，为稀疏线条画分配密集语义标签，而无需在训练期间依赖像素级标注。与自然图像不同，素描缺乏纹理和颜色线索，使得语义理解高度依赖于笔画布局和空间配置，这一挑战导致单层视觉-语言特征 inherently 不稳定。我们的关键观察是，来自不同Vision Transformer层的注意力图编码了互补的空间线索：浅层捕获全局结构布局，而深层聚焦于局部笔画交叉和物体部件。这表明跨层聚合比任何单个层提供了更强的结构先验。利用这一见解，我们提出了一个基于**层累积结构注意力（LASA）**的结构感知框架，该框架聚合多层注意力以在弱监督下指导分层语义对齐，并在推理过程中细化预测。在FS-COCO、SFSD和FrISS上的实验表明，与之前的弱监督基线相比，LASA在mIoU上分别提升了+3.43、+8.01和+15.74，在分割精度和空间一致性上均表现出持续改进。我们的源代码将公开提供。

## Abstract
Open-vocabulary scene sketch semantic segmentation aims to assign dense semantic labels to sparse line drawings based on flexible category vocabularies specified at inference time, without relying on pixel-level annotations during training. Unlike natural images, sketches lack texture and color cues, making semantic understanding heavily dependent on stroke layout and spatial configuration, a challenge that renders single-layer vision-language features inherently unstable. Our key observation is that attention maps from different Vision Transformer layers encode complementary spatial cues: shallow layers capture global structural layouts, while deeper layers focus on local stroke intersections and object parts. This suggests that cross-layer aggregation provides a more robust structural prior than any individual layer alone. Leveraging this insight, we propose a structure-aware framework built upon \textbf{L}ayer-wise \textbf{A}ccumulated \textbf{S}tructural \textbf{A}ttention (\textbf{LASA}), which aggregates multi-layer attention to guide hierarchical semantic alignment under weak supervision and refine predictions during inference. Experiments on FS-COCO, SFSD, and FrISS show that LASA improves mIoU by $+3.43$, $+8.01$, and $+15.74$ over the prior weakly supervised baselines, demonstrating consistent gains in both segmentation accuracy and spatial coherence. Our source code will be made publicly available.

---

## 论文详细总结（自动生成）

# LASA: 一种用于开放词汇场景素描语义分割的弱监督方法 - 详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **任务定义**：开放词汇场景素描语义分割，即在推理时根据灵活的自由文本类别，为稀疏的线条画（素描）预测每个像素的语义标签，训练时仅使用图像级标签或描述，无需像素级标注。
- **核心挑战**：
  - 素描缺乏纹理和颜色，语义完全依赖笔画结构与空间布局。
  - 现有视觉-语言模型（如CLIP）针对自然图像优化，对素描效果差：深层特征空间不连贯、碎片化。
  - 弱监督下，全局语义信号无法提供足够的空间约束，导致结构-语义不匹配。
- **关键观察**：ViT不同层的注意力图提供**互补结构信息**——浅层关注全局布局，深层关注局部笔画交叉和部件。跨层聚合能形成更稳定的结构先验。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过跨层累积结构注意力（LASA）聚合多层ViT注意力图，作为结构先验注入特征融合，指导弱监督对齐，并在推理时细化预测。
- **三大组件**：
  1. **Accumulated Attention Encoder**
     - 提取前L-1层的自注意力矩阵，先对每层头平均，再跨层求和，得到累积注意 `A_acc`。
     - 对称化、ℓ1归一化、ReLU得到 `\hat{A}_acc`。
     - 用 `\hat{A}_acc` 替换最终Transformer层的标准注意力，进行值聚合：`Y = \hat{A}_acc V`。
  2. **Hierarchical Semantic Alignment**（弱监督训练）
     - **场景级对齐**：基于CLS特征的批量三元组损失，用最难的批内负样本。
     - **区域感知对齐**：
       - 从LASA的CLS-to-patch响应生成空间显著性图W，硬阈值化得到 `\tilde{W}`。
       - 结合背景掩码B，生成加权重编码输入 `S_w`（强调结构显著区域）。
       - 对 `S_w` 重新编码，在选定的层（7/10/12）上再次应用三元组损失。
     - 总损失：`L = L_scene + L_region`。
  3. **LASA-based Attention Refinement**（推理阶段）
     - 平均选定层（默认最后6层）的类响应图得到 `F_fuse`。
     - 用最终层LASA注意力（去掉CLS）传播最后一层响应图得到 `\tilde{F}_last`。
     - 残差相加：`F' = \tilde{F}_last + F_fuse`，再reshape为像素级预测。

## 3. 实验设计

- **数据集**：
  - **FS-COCO**：开放词汇素描分割基准（源自COCO）。
  - **SFSD**、**FrISS**：经典素描语义分割数据集，有像素级标注，但训练时不使用，仅用于评估。
- **评估指标**：mIoU（主要）、Pixel Accuracy (Acc@P)、Stroke Accuracy (Acc@S)。
- **基线方法**：
  - **训练-free**：CLIP、ProxyCLIP、CLIPer、CorrCLIP。
  - **Fine-tuned**（同一弱监督协议，重训）：CLIP、ProxyCLIP、CLIPer、CorrCLIP。
  - **全监督参考**：SketchSeger（封闭集）。
  - **弱监督SOTA**：Bourouis [3]（最接近的同类方法）。

## 4. 资源与算力

- **明确配置**：单张RTX 3090 GPU，AdamW优化器（学习率1e-6），训练20个epoch，输入resize到224×224。
- **其他**：文本编码器冻结，视觉编码器微调。未报告单次训练总时长/显存占用。

## 5. 实验数量与充分性

- **全面对比**：在三个数据集上比较了4种训练-free + 4种微调 + 1个全监督 + 1个弱监督SOTA，总共10+个基线（Table 1）。
- **消融实验**（Table 2）：
  - 注意力公式：对比Query-Key、Query-Query、Key-Key、Value-Value与LASA。
  - 组件消融：w/o LASA、w/o loss、w/o global loss、w/o region-aware、w/o refinement、full。
  - 细化层数：无、全部12、最后3、最后6层。
- **定性可视化**：Fig 4展示分割结果对比，Fig 5展示注意力图对比。
- **公平性**：所有微调基线均在同一弱监督协议、同一backbone、相同训练超参下重训。
- **结论**：实验充分、客观、公平，覆盖了方法的主要设计维度，验证了各组件的贡献。

## 6. 论文的主要结论与发现

- LASA在三个数据集上均取得**新SOTA**：
  - FS-COCO：mIoU提升+3.43（vs Bourouis）；
  - SFSD：+8.01；
  - FrISS：+15.74。
- **跨层累积注意优于任何单层注意**（如Q-K、V-V等），验证了互补性。
- **层次化对齐的两个级别互补**：场景级提供全局锚点，区域级增强结构显著区域的一致性。
- **推理细化提升显著**：mIoU+3.81（最后6层最佳）。
- **定性结果**：LASA生成更完整、边界更清晰的分割掩码，优于CLIPer和Bourouis。

## 7. 优点

- **创新性**：首次利用**CLIP内部跨层注意力**作为结构先验，完全无需外部辅助模型（如DINO、SAM），设计简洁。
- **方法高效**：累积注意替换最终层计算，不引入额外参数；推理细化仅涉及矩阵乘法，无训练开销。
- **弱监督可行性**：仅需图像级标签+描述，适合实用场景。
- **实验验证充分**：跨多个数据集、多种基线、详细消融，泛化能力强。
- **结果一致且显著**，尤其在大数据集FrISS上提升幅度达15.74 mIoU。

## 8. 不足与局限

- **依赖ViT架构**：方法基于ViT注意力，可能不适用于CNN或其他Transformer变体（如Swin）。
- **阈值超参**：区域感知对齐中的阈值τ_c固定为0.7，论文未讨论敏感性或自适应策略。
- **数据集规模有限**：仅三个公开数据集，且SFSD/FrISS类别固定，开放词汇性质有限。
- **与全监督差距**：弱监督下性能仍明显低于全监督方法（如SketchSeger在封闭集上的表现）。
- **未见失败案例分析**：未分析在高度重叠、罕见类别或极端稀疏素描上的表现。
- **训练代价**：虽仅需单卡RTX 3090，但20 epoch可能仍需数小时，未对比更高效方案。

（完）
