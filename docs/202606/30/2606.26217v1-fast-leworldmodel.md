---
title: Fast LeWorldModel
title_zh: 快速LeWorldModel
authors: "Yuntian Gao, Xiangyu Xu"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.26217v1"
tags: ["query:world-models"]
score: 9.0
evidence: 快速潜在世界模型，使用并行动作前缀预测
tldr: 视觉世界模型LeWM在规划时需逐帧自回归展开，计算成本高且误差随规划步长累积。Fast-LeWM将动作前缀作为基本预测单元，并行编码前缀并直接预测对应未来潜变量，避免逐帧推算。实验表明，Fast-LeWM在多个任务中提升平均成功率，显著缩短规划时间，且开环潜损失增长更慢。该方法为高效、低误差的视觉规划提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26217v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1524, \"height\": 611, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26217v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1353, \"height\": 769, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26217v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1678, \"height\": 491, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26217v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1712, \"height\": 422, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26217v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1481, \"height\": 343, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26217v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 967, \"height\": 198, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26217v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 963, \"height\": 593, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26217v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1150, \"height\": 301, \"label\": \"Table\"}]"
motivation: LeWM的自回归规划计算昂贵且误差累积，需并行化预测以提升效率和精度。
method: Fast-LeWM以动作前缀为单元，并行编码前缀并预测到达的潜在状态，替代单步滚动。
result: 相比LeWM，Fast-LeWM提高任务成功率，减少规划时间，开环潜损失随步长增长更慢。
conclusion: 动作前缀预测是提升世界模型规划效率与准确性的有效方案。
---

## 摘要
联合嵌入预测架构（JEPAs），包括最近的LeWorldModel（LeWM），已成为无重建视觉世界模型的有前途的基础。然而，对于视觉规划，LeWM通过重复应用局部一步潜在转移模型来评估候选动作序列。这种自回归展开使得规划计算成本高昂，并且随着视野增长，预测轨迹会积累潜在误差。我们提出了快速LeWorldModel（Fast-LeWM），一种快速潜在世界模型，它用动作前缀预测替代重复的局部展开。给定当前潜在状态和一个候选动作序列，Fast-LeWM编码其前缀，并并行预测执行这些前缀后达到的未来潜在状态。通过将动作前缀作为基本预测单元，Fast-LeWM直接建模跨越多个视野的不同程度上累积的动作效果。这种前缀级别的监督迫使模型学习状态如何在不同动作前缀下连续演化，而不仅仅是拟合一步状态转移。在规划期间，预测器可以使用编码动作序列中的最后一个前缀标记来评估相应的未来潜在状态，而无需显式地遍历每个中间想象状态。在多个任务中，Fast-LeWM在提高平均成功率的同时，大幅减少了规划时间，实现了更低的开环潜在损失，且其增长随展开视野增加而显著减慢。

## Abstract
Joint-Embedding Predictive Architectures (JEPAs), including recent LeWorldModel (LeWM), have become a promising foundation for reconstruction-free visual world models. For visual planning, however, LeWM evaluates candidate action sequences by repeatedly applying a local one-step latent transition model. This autoregressive rollout makes planning computationally expensive and exposes the predicted trajectory to accumulated latent errors as the horizon grows. We propose Fast LeWorldModel (Fast-LeWM), a fast latent world model that replaces repeated local rollout with action-prefix prediction. Given the current latent and a candidate action sequence, Fast-LeWM encodes its prefixes and predicts the future latents reached after executing those prefixes in parallel. By making action prefixes the basic prediction unit, Fast-LeWM directly models action effects accumulated to different extents over multiple horizons. This prefix-level supervision forces the model to learn how states continuously evolve under different action prefixes, rather than only fitting one-step state transitions. During planning, the predictor can use the last prefix token from the encoded action sequence to evaluate the corresponding future latent without explicitly rolling through each intermediate imagined state. Across multiple tasks, Fast-LeWM improves average success over LeWM while substantially reducing planning time, achieving lower open-loop latent loss whose growth becomes significantly slower as the rollout horizon increases.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：以 LeWorldModel (LeWM) 为代表的免重建视觉世界模型在奖励无关的目标驱动规划中展现了潜力，但其规划过程依赖**单步潜在转移模型**：对任一候选动作序列，必须通过**自回归方式**逐时刻将预测的潜在状态反馈回模型，反复迭代以得到最终状态。
- **问题**：这种自回归展开导致两个关键瓶颈：
  - **计算效率低**：每个候选序列都需要多次动作编码和潜在预测，特别是在 Cross-Entropy Method (CEM) 采样大量候选时，规划时间急剧增加；
  - **误差累积**：早期或中间时刻的预测误差会向后传播，导致长时域规划愈发不可靠。
- **本文目标**：设计一种新型快速潜在世界模型 Fast-LeWM，通过**动作前缀预测**替代自回归展开，实现**并行化**的多步状态预测，同时降低误差累积。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将**动作前缀**（action prefix）作为基本预测单元。给定当前潜在状态 \( z_t \) 和动作序列 \( a_{t:t+H-1} \)，Fast-LeWM 直接预测执行每个前缀（即 \( a_{t:t+k-1} \), \( k=1,\dots,H \)）后到达的未来潜在状态 \( \hat{z}_{t+k} \)，且所有预测可**并行完成**。
- **关键技术**：
  - **动作前缀编码器**：使用因果 Transformer 对动作序列进行编码。将当前潜变量 \( z_t \) 通过 MLP 映射为状态 token 并作为第 0 个 token 拼接到动作 token 序列前。利用**因果掩码**使得位置 k 的输出 token \( p_{t,k} \) 只能关注前 k 个动作（即前缀 \( a_{t},\dots,a_{t+k-1} \)），从而代表该前缀的累积效果。
  - **并行潜在预测器**：基于当前潜变量 \( z_t \) 和所有前缀 token \( p_{t,1:H} \)，**一次性**预测所有未来潜变量 \( \hat{z}_{t+1:t+H} \)。预测器采用 6 层带 AdaLN-zero 调制的残差 MLP。
  - **训练目标**：对每个前缀预测均施加 L2 损失（密集前缀损失 \( L_{\text{prefix}} \)），并加入 SIGReg 正则化防止潜变量坍塌：
    \[
    L_{\text{prefix}} = \frac{1}{H} \sum_{k=1}^{H} \| \hat{z}_{t+k} - z_{t+k} \|_2^2,\quad L_{\text{AP}} = L_{\text{prefix}} + \lambda \text{SIGReg}(Z).
    \]
  - **规划接口**：在 CEM 中，Fast-LeWM 只需一次前缀编码+一次并行预测即可得到所有候选序列的终端潜变量。还可引入可选的**自洽性惩罚** \( \beta \| \hat{z}_{t+H} - \tilde{z}_{t+H} \|_2^2 \)，其中 \( \tilde{z}_{t+H} \) 是从中间前缀出发再次预测得到的终端潜变量，鼓励模型一致性。

### 3. 实验设计：数据集、基准、对比方法

- **环境和数据集**：采用与 LeWM 相同的四个任务，每个任务提供离线数据：
  - Two-Room（Sobal et al.）
  - PushT（Zhou et al.）
  - Reacher（Tassa et al.）
  - OGBench-Cube（Park et al.）
- **基准**：对比 PLDM、DINO-WM、LeWM。所有方法使用相同的目标规划协议和 CEM 预算。
- **对比方法**：
  - 直接对比：成功率、规划时间（动态模块耗时、CEM 总耗时）、开环预测损失、物理状态探测结果。
  - 消融实验：长动作 LeWM（将 LeWM 动作块扩至 25 步）、仅终端监督的 Fast-LeWM、不带状态 token 的 Fast-LeWM。

### 4. 资源与算力信息

- 文中未明确说明训练使用的 GPU 型号和数量，仅提及**测量时间使用单张 NVIDIA 4090**。训练细节：批量大小除 Cube 用 32 外，其余任务用 128，所有模型训练 10 个 epoch，与 LeWM 协议一致。整体参数量 17.9M（接近 LeWM 的 18.0M）。
- 未提供训练的总时长或具体 GPU 小时数。

### 5. 实验数量与充分性

- **实验数量**：四类任务（10 个环境组合）上的成功率对比；效率对比（动态模块时间、CEM 时间）；开环预测损失曲线（4 张图）；物理状态探测（3 个物理量 × 2 种探针）；消融实验（4 种变体对比）；以及可视化定性对比。
- **充分性与公平性**：实验设计较为充分，严格遵循 LeWM 的评估协议（相同环境、规划参数、观察预处理、CEM 参数）。消融实验验证了各组件（前缀监督、状态 token）的必要性。物理探测实验额外说明潜在空间保留了更多物理信息。评价指标全面（成功率、时间、损失、相关性）。

### 6. 论文的主要结论与发现

- **规划成功率提升**：Fast-LeWM 平均成功率从 85.8%（LeWM）提高到 90.5%，加入了自洽性惩罚后进一步提升至 92.0%。
- **规划效率大幅提升**：动态模块耗时从 31.4s 降至 8.0s（降低 3.9 倍），CEM 总耗时从 54.4s 降至 28.3s（降低 48%）。
- **开环预测更准确**：在四类任务中，Fast-LeWM 的开环潜损失不仅初始值更低，而且随着步长增长的斜率也更小，表明误差累积被有效抑制。
- **潜在表征更丰富**：物理状态探测中，MLP 探针在三个变量上均取得最低 MSE 和最高相关系数，说明前缀级监督促进了更细粒度的物理状态保留。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：将单步自回归预测改为前缀并行预测，直接建模多步累积效果，既加速又减少误差。动作前缀编码器使用因果 Transformer 和状态 token 注入上下文，设计简洁有效。
- **实验严谨**：
  - 与 LeWM 使用完全相同规划协议、观察预处理、CEM 超参数，确保公平比较。
  - 消融实验清晰揭示各组件贡献（长动作块不能直接提速，密集前缀监督和状态 token 均提升性能）。
  - 物理探测实验从另一角度验证表征质量，增强了结论可信度。
- **实用性**：显著缩短规划时间且提升成功率，对实际机器人部署（需实时规划）有直接价值。

### 8. 不足与局限

- **实验覆盖**：仅在模拟器环境（Two-Room、PushT、Reacher、Cube）上验证，未涉及真实机器人、杂乱的动态环境或高维连续动作空间。
- **偏差风险**：所有任务规划时均使用固定视野 H=5（动作跳数 5，共 25 环境步），未探索不同长度的规划效果，可能存在对特定视野的过拟合。
- **应用限制**：
  - 自洽性惩罚项系数 \( \beta \) 需手动调节（文中设为 1），可能依赖任务。
  - Fast-LeWM 的并行预测仍需要一次完整的前缀编码，当 H 极大时，编码器（3 层因果 Transformer）的计算开销与序列长度呈平方关系，在超长规划时可能仍有瓶颈。
- **资源信息不透明**：未提供完整训练成本和 GPU 数量/类型，难以直接复现其计算开销。

（完）
