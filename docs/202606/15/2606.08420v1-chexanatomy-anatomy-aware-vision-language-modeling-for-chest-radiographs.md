---
title: "CheXanatomy: Anatomy-Aware Vision-Language Modeling for Chest Radiographs"
title_zh: "CheXanatomy: 面向胸部X光片的解剖感知视觉语言建模"
authors: "Sergios Gatidis, Curtis Langlotz, Christian Bluethgen"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08420v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 面向胸部X光片的解剖感知语义分割的视觉语言模型
tldr: 现有视觉语言模型缺乏对胸部X光片中精细解剖结构的显式编码，难以支持分割等空间精确任务。提出CheXanatomy框架，通过自回归下一个token预测目标直接生成解剖分割掩码，将解剖知识嵌入预训练VLM，无需任务专用解码器。其关键创新在于从CT体积合成逼真X光片并前向投影CT标签获得大规模解剖标注，实现可扩展监督。实验表明，该方法在分布内与专用卷积模型性能相当，在域偏移下鲁棒性更强，且解剖预训练显著提升下游定位任务的样本效率，证明了结构化解剖知识融入生成目标的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08420v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1266, \"height\": 825, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1263, \"height\": 595, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1245, \"height\": 334, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1247, \"height\": 856, \"label\": \"Figure\"}]"
motivation: 现有VLMs过度优化全局对齐，无法编码精细解剖结构，限制空间精确任务性能。
method: 通过自回归下一个token预测目标，在预训练VLM中直接生成解剖分割掩码，利用CT合成X光片及前向投影获取解剖标签进行监督。
result: 在合成和真实数据上，该方法在分布内与U-Net性能相当，域偏移下鲁棒性更强，且预训练模型在新任务上样本效率显著提升。
conclusion: 将解剖结构直接嵌入生成目标可促进空间基础表示，支持解剖感知的医学视觉语言建模。
---

## 摘要
视觉语言模型（VLM）通过在大规模图像-文本对上进行预训练，展现出强大的图像级理解能力，但主要针对全局对齐进行优化，并未显式编码细粒度的解剖结构，限制了其在分割等空间精确任务中的适用性。我们提出CheXanatomy框架，通过自回归令牌空间监督将显式解剖知识整合到预训练的VLM中。该模型并非添加任务特定的解码器头，而是通过下一个令牌预测训练生成解剖分割掩码。为实现可扩展的监督，我们从CT体积合成逼真的胸部X光片，并前向投影CT分割标签以获得解剖一致的2D掩码。我们在合成和真实胸部X光片上，以U-Net为基线评估该方法，包括模型规模、输入分辨率和视觉编码器微调的消融实验。自回归解剖监督在分布内达到了与专用卷积模型相当的性能，并在域迁移至真实CXR数据时展现出更强的几何鲁棒性。此外，在有限监督下适应新定位任务时，解剖预训练模型表现出更高的样本效率。更大的模型和更高的输入图像分辨率可提升性能，而视觉编码器微调效果有限。这些结果表明，将解剖结构直接嵌入生成目标有助于促进空间基础的表征，并支持解剖感知的医学视觉语言建模。

## Abstract
Vision-language models (VLMs) pretrained on large-scale image-text pairs demonstrate strong image-level understanding, but are primarily optimized for global alignment and do not explicitly encode fine-grained anatomical structure, limiting their suitability for spatially precise tasks such as segmentation. We introduce CheXanatomy, a framework that integrates explicit anatomical knowledge into a pretrained VLM through autoregressive token-space supervision. Instead of adding task-specific decoder heads, the model is trained to generate anatomical segmentation masks via next-token prediction. To enable scalable supervision, we synthesize realistic chest radiographs from CT volumes and forward-project CT segmentation labels to obtain anatomically consistent 2D masks. We evaluate the approach on synthetic and real chest radiographs against a U-Net baseline, including ablations on model scale, input resolution, and vision encoder fine-tuning. Autoregressive anatomical supervision achieves performance comparable to specialized convolutional models in-distribution and demonstrates improved geometric robustness under domain shift to real CXR data. In addition, anatomy-pretrained models exhibit improved sample efficiency when adapting to novel localization tasks under limited supervision. Larger models and higher input image resolution improve performance, while vision encoder fine-tuning has limited effect. These results show that embedding anatomical structure directly into the generative objective promotes spatially grounded representations and supports anatomy-aware medical vision-language modeling.

---

## 论文详细总结（自动生成）

# 论文《CheXanatomy: Anatomy-Aware Vision-Language Modeling for Chest Radiographs》详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有视觉语言模型（VLM）在大规模图像-文本对预训练后，具备了图像级理解能力，但其训练目标主要优化全局对齐，**缺乏对精细解剖结构的显式编码**，限制了在分割等空间精确任务上的适用性。同时，传统的卷积分割模型（如 U-Net）虽然在分布内表现良好，但依赖大量像素级标注，且对域迁移（如从合成数据到真实 X 光片）的鲁棒性不足。
- **研究动机**：希望将显式解剖知识直接嵌入预训练 VLM 的生成目标中，使其能够在不添加任务特定解码器的情况下，通过自回归下一个 token 预测生成解剖分割掩码，从而获得空间基础的表征能力。
- **整体含义**：提出 CheXanatomy 框架，首次将结构化解剖先验通过 token 空间监督融入 VLM，实现了在分布内与卷积基线性能相当、在域迁移下几何鲁棒性更强，并显著提升下游定位任务的样本效率。这项工作表明，空间接地能力可以内嵌于多模态基础模型中。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 在预训练 VLM 中，**将解剖分割任务转化为结构化的 token 序列生成问题**，利用下一个 token 预测的交叉熵损失进行优化，无需额外解码器头或像素级损失。
- 为克服真实 CXR 标注稀缺的问题，**利用 CT 体积合成逼真的投影 X 光片**，同时将 CT 分割标签前向投影到 2D 空间获得解剖一致的掩码，实现大规模、可扩展的监督信号。

### 关键技术细节

1. **模型架构**：基于 PaliGemma 2 系列（SigLIP-So400m 视觉编码器 + Gemma 2 大语言模型）。边界框使用 4 个专有 token 表示；分割掩码使用 16 个掩码 token 表示，通过 VQ-VAE 将二值掩码编码为离散的 16 维潜变量，解码器在训练中不更新，仅优化 token 预测。

2. **合成 CXR 生成**：
   - 使用 CT-RATE 数据集中的非增强 CT 扫描（12,984 训练 + 683 测试）。
   - 使用 Diff-DRR 可微渲染器生成后前位（PA）和侧位（Lateral）投影，共 25,968 张合成训练图像和 1,366 张测试图像。
   - 使用 TotalSegmentator 从 CT 获得 80 个解剖结构的三维分割，正向投影为对齐的 2D 掩码。
   - 训练时采样多种文本同义词构造提示，并应用随机缩放增强。

3. **微调策略**：采用低秩适应（LoRA）进行参数高效微调，探索两种配置：视觉编码器冻结 vs 更新。实验使用 3B 和 10B 两种模型规模，输入分辨率 224×224 或 448×448。

4. **卷积基线**：标准 2D U-Net（4 级编码器，通道数 32→64→128→256→512），输入 448×448，输出 80 类分割。

## 3. 实验设计

### 使用的数据集/场景
- **合成 in-distribution 测试**：CT-RATE 测试集的 1,366 张合成 PA 和 Lateral 投影。
- **真实 out-of-distribution 测试**：
  - CheXmask：200 张真实 PA 片，提供心脏和肺部掩码。
  - VinDr-RibCXR：198 张真实 PA 片，提供双侧肋骨分割。
- **下游定位迁移任务**：RadVLM 视觉接地数据集（118,360 训练 / 22,972 测试），评估解剖预训练对样本效率的影响。

### Benchmark
- 主要对比**专用卷积模型 U-Net**（在相同合成数据上训练）的性能。
- 此外，在迁移实验中对比基线模型（无解剖预训练的 PaliGemma 同等配置）。

### 对比方法
- **PaliGemma 系列**：3B 与 10B 参数，224 和 448 输入分辨率，视觉编码器冻结或解冻，共 4 或 8 组配置。
- **U-Net**：一个基准。
- **消融实验**：模型规模、输入分辨率、视觉编码器微调与否；以及预训练 vs 非预训练在样本效率上的对比。

## 4. 资源与算力

论文中明确指出：
- 训练在 **8 块 H100 GPU** 上进行。
- 批量大小根据模型配置调整：10B 参数 + 448×448 分辨率为 batch size 8；3B 参数 + 224×224 分辨率为 batch size 24。
- 未明确给出具体训练时长（epoch 数或小时数）。论文提到“fine-tuned for one epoch”用于 RadVLM 迁移实验，但解剖预训练的具体迭代次数未说明。

## 5. 实验数量与充分性

- **实验数量**：较为充分。主要包括：
  - 合成 in-distribution 测试（1 个数据集，5 组模型对比）。
  - 真实 out-of-distribution 测试（2 个数据集合并，5 组模型 + U-Net 对比）。
  - 消融实验：模型规模（2 种）、分辨率（2 种）、视觉编码器冻结/解冻（2 种）共 8 种组合，均在两个主要测试场景中报告。
  - 数据效率迁移实验（RadVLM）：5 个数据分数（0%、1%、3%、10%、100%），对比预训练 vs 基线模型。
- **充分性**：实验设计覆盖了分布内、分布外、消融、迁移学习等关键维度，消融全面，对比公平（所有模型使用相同合成训练数据，U-Net 使用 448 输入，PaliGemma 使用不同配置）。但缺少对更多真实数据集（如多个医院来源）的泛化测试，且未在 CXR 上直接进行解剖预训练与下游全监督任务的联合评估。

## 6. 论文的主要结论与发现

1. **分布内性能**：最好的 PaliGemma 配置（10B+448）在 Dice 和 IoU 上接近 U-Net，且边界误差（Hausdorff、傅里叶描述子距离）更低。
2. **域迁移鲁棒性**：在真实 CXR 上，VLM 在保持重叠指标的同时，边界和形状误差显著优于 U-Net，表明解剖 token 监督带来更稳定的几何结构。
3. **消融发现**：模型规模增大和分辨率提升均能改善性能；**视觉编码器微调影响甚微**，说明解剖推理主要在语言 token 空间涌现。
4. **样本效率提升**：在 RadVLM 接地任务低数据区（0-3%训练数据），解剖预训练模型明显优于基线；随数据增多，差距收敛。
5. **方法优势**：无需解码器头、支持灵活语言提示、可集成新解剖结构，易于嵌入管线。

## 7. 优点

- **创新性**：首次将 CT 投影得到的解剖掩码通过 VQ-VAE 编码为离散 token，融入 VLM 的自回归生成目标，实现无需像素解码器的解剖感知模型。
- **可扩展性**：利用大量 CT 数据合成投影，绕过了真实 CXR 手工标注的瓶颈，可覆盖 80 个解剖结构。
- **实验设计全面**：包括 in-distribution、out-of-distribution、消融、迁移学习，对比了多种规模/分辨率/微调策略，且所有模型使用同源合成数据训练，确保公平。
- **实践价值**：解剖预训练显著提升下游任务的样本效率，对标注稀缺的医学场景具有实际意义。

## 8. 不足与局限

- **依赖 CT 数据**：合成 CXR 基于特定 CT 数据集（CT-RATE），其患者人口统计和扫描仪分布可能导致合成分布偏差，不一定覆盖所有真实病例变异。
- **未直接评估真实 CXR 上的分割完整度**：仅对心脏/肺和肋骨两个结构进行外部验证，80 个解剖目标在真实数据上的泛化未全面测试。
- **缺少与多任务 VLM 的横向比较**：仅与 U-Net 对比，未与 SAM、CLIP 引导方法或其他医学 VLM（如 RadVLM 本身的分割能力）进行比较。
- **计算开销**：10B 模型高分辨率下训练需要 8×H100，推理成本高，未报告推理速度或参数量。
- **视觉编码器冻结/解冻对比不充分**：可能因 LoRA 仅修改低秩权重，解冻编码器的收益未充分体现；更深入的微调策略（如全参数微调）未被探索。
- **缺乏不确定性或失败案例分析**：未讨论哪些解剖结构分割困难，或哪些真实图像导致性能下降。

（完）
