---
title: Learning a Semantic Calibration Network for Open-Vocabulary Semantic Segmentation
title_zh: 学习用于开放词汇语义分割的语义校准网络
authors: "Yang Sun, Tao Wang, Anastasia Ioannou, Ge Xu"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08001v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 基于语义校准网络的开放词汇语义分割
tldr: 开放词汇语义分割需识别新概念，但现有方法未充分建模类间语义关系。本文提出语义校准网络SCN，通过类消歧模块捕获隐式类间依赖，logits融合模块动态整合语义证据。在多个基准上显著优于现有方法，同时有效保持CLIP的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08001v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1568, \"height\": 743, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08001v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1408, \"height\": 731, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08001v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1415, \"height\": 994, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08001v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 682, \"height\": 685, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08001v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1869, \"height\": 469, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08001v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 882, \"height\": 194, \"label\": \"Table\"}]"
motivation: 现有方法聚焦特征聚合或微调，未能显式建模类间语义相关性，导致判别力与泛化能力难以平衡。
method: 提出SCN，包含类消歧模块利用残差结构捕获隐式类间依赖，以及logits融合模块动态整合多方面语义证据。
result: 在多个主流基准上，SCN相比最先进方法取得显著性能提升。
conclusion: 通过显式建模语义相关性，SCN在增强判别力同时保持CLIP泛化能力，为开放词汇语义分割提供有效范式。
---

## 摘要
语义图像分割为每个像素分配预定义的类别标签，最近取得了显著进展。开放词汇分割（OVS）将分割任务从固定集合扩展到开放集合，使得能够基于任意文本输入（如类别名称或描述）识别和分割新概念。在本文中，我们提出了一种新颖的语义校准网络（SCN）用于开放词汇语义分割。与先前专注于特征聚合或简单微调预训练模型的方法不同，SCN通过显式建模类别间的语义相关性来细化掩码分类过程，旨在增强模型的判别能力，同时有效保留预训练CLIP模型的泛化能力。具体来说，SCN包含两个核心组件：类别消歧（CD）和逻辑融合（LF）。首先，利用交叉注意力机制将文本嵌入转换为视觉感知的伪文本嵌入，以得出增强的相似度分数，补充原始的掩码-文本相似度分数。随后，类别消歧模块通过残差架构捕获隐式的类间依赖关系，以有效解决语义歧义。最后，逻辑融合模块动态整合多方面的语义证据，确保模型在保持CLIP固有泛化能力的同时达成稳健的语义共识。在主流基准上的全面实验结果表明，与最先进的算法相比，所提出的方法实现了显著的性能提升。

## Abstract
Semantic image segmentation assigns a predefined category label to each pixel, has achieved significant progress lately. Open-Vocabulary Segmentation (OVS) extends the segmentation task from a fixed set to an open set, enabling the identification and segmentation of novel concepts based on arbitrary text inputs, such as category names or descriptions. In this paper, we propose a novel Semantic Calibration Network (SCN) for open-vocabulary semantic segmentation. Different from prior approaches that focus on feature aggregation or simple fine-tuning of pre-trained models, SCN refines the mask classification process by explicitly modeling the semantic correlations between classes, aiming to enhance the model's discriminative power while effectively preserving the generalization abilities of the pre-trained CLIP model. Specifically, SCN comprises two core components: Class Disambiguation (CD) and Logits Fusion (LF). First, a cross-attention mechanism is utilized to transform the text embeddings into visually aware pseudo-text embeddings, in order to derive an enhanced similarity score that complements the original mask-text similarity score. Subsequently, the Class Disambiguation module captures implicit inter-class dependencies through a residual architecture to effectively resolve semantic ambiguities. Finally, the Logits Fusion module dynamically integrates multifaceted semantic evidence to ensure that the model achieves a robust semantic consensus while maintaining CLIP's inherent generalization capability. Comprehensive experimental results on mainstream benchmarks demonstrate that the proposed method achieves significant performance improvements compared to state-of-the-art algorithms.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究背景**：开放词汇语义分割（OVS）旨在将分割任务从固定类别扩展到开放集合，能够基于任意文本描述（如类别名称或描述）分割未见过的概念。主流方法采用“分割-后识别”范式，先生成类别无关的掩码提案，再通过预训练的视觉-语言模型（如CLIP）进行掩码级分类。
- **核心问题**：现有方法主要聚焦于特征聚合或简单微调预训练模型，但忽视了类别间的语义相关性建模。CLIP的图像-文本对齐能力偏向全局语义，与分割所需的细粒度像素级分类存在空间粒度差异。简单微调容易导致模型过拟合于训练集上的已见类别，削弱零样本泛化能力。
- **研究动机**：部分工作（如MaskCLIP++）指出性能瓶颈在于掩码分类过程中的语义混淆，而非掩码生成质量。因此，本文提出显式建模类别间语义关联，以提高判别力并保留CLIP的泛化能力。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：提出语义校准网络（SCN），通过显式建模类别间的语义相关性来细化掩码分类过程。SCN在相似度（logits）空间内操作，而非特征嵌入空间，以保持CLIP原始相似度排序（对泛化至关重要）。
- **关键技术细节**：
  - **总体架构**：输入图像和文本描述，分别经过CLIP图像编码器和文本编码器提取视觉特征 \(F_v\) 和文本嵌入 \(F_t\)。掩码生成器 \(M\) 产生 \(N\) 个二进制掩码，掩码池化聚合得到掩码级特征 \(F_m\)。SCN接收 \(F_m, F_v, F_t\)，输出精炼相似度矩阵 \(Q\) 用于分类。
  - **两个核心模块**：
    1. **类别消歧（CD）模块**：采用残差架构，通过适配器 \(A_1\) 和 \(A_2\) 分别从原始相似度 \(S\) 和掩码特征 \(F_m\) 中学习，再经适配器 \(A_3\) 联合建模，得到精炼相似度 \(R = S + A_3(A_1(S) + A_2(F_m))\)。直接在logit空间操作，避免破坏CLIP嵌入几何。
    2. **logits融合（LF）模块**：设计轻量级的融合权重生成器，根据 \(F_m\) 动态生成权重 \(\alpha\)，对精炼相似度 \(R\) 和增强相似度 \(T\) 进行加权融合：\(Q = \alpha T + (1-\alpha)R\)。
  - **增强相似度 \(T\) 的生成**：通过交叉注意力以文本为查询、视觉为键/值，将原始文本嵌入转换为视觉感知的伪文本嵌入 \(F'_t\)，再与 \(F_m\) 点积得到 \(T\)。残差结构保留原文本表示。
- **公式流程**（文字说明）：
  1. 计算标准相似度 \(S = F_m^\top F_t\)。
  2. 增强相似度：\(P_t = \text{Attention}(q(F_t), k(F_v), v(F_v))\)，\(F'_t = \text{Norm}(F_t + P_t)\)，\(T = F_m^\top F'_t\)。
  3. 类别消歧：\(R = S + A_3(A_1(S) + A_2(F_m))\)。
  4. logits融合：\(\alpha = \text{Generator}(F_m)\)，\(Q = \alpha T + (1-\alpha)R\)。

## 3. 实验设计：数据集、基准、对比方法
- **训练数据集**：COCO-Stuff（约118k图像，171个语义类别）。
- **测试基准**：
  - ADE20K：A-150（150个常见类别）和 A-847（847个细粒度类别）。
  - PASCAL VOC (PAS-20)：20个对象类别。
  - PASCAL-Context：PC-59（59个类别）和 PC-459（459个细粒度类别）。
- **评价指标**：平均交并比（mIoU）。
- **对比方法**：ZegFormer、DeOP、OvSeg、SAN、OpenSeg、EBSeg、SED、CAT-Seg、MAFTP、MaskAdapter（基线）。均为近年开放词汇分割的代表性方法，按VLM规模（ViT-B/16、ConvNeXt-B）分组对比。

## 4. 资源与算力
- **硬件**：2块 NVIDIA RTX A6000 GPU。
- **训练配置**：AdamW优化器，初始学习率1×10⁻⁴，权重衰减0.05，多步学习率衰减。总batch size=12，训练5个epoch。图像crop大小1024×1024，测试分辨率896×896。
- **未明确信息**：未说明总训练时长（如小时数）、模型参数量、推理速度等。

## 5. 实验数量与充分性
- **主要实验**：
  - 表2：在5个基准（A-150、A-847、PC-59、PC-459、PAS-20）上对比10种以上方法，其中SCN在A-150（36.2）、A-847（14.7）、PC-59（58.6）上取得最佳，PC-459和PAS-20上为次优。
  - 表3：消融实验，逐步添加CD和LF模块，在5个基准上显示一致性提升（A-150从35.6到36.2）。
  - 图3：定性结果对比，展示基线误分类“椅子”为“扶手椅”等情况，SCN能纠正。
- **充分性评价**：
  - **客观**：使用通用基准和标准mIoU，对比方法均为公开结果或作者重新评估（标注*）。
  - **公平**：与基线MaskAdapter使用相同骨干（ConvNeXt-B）和训练数据，确保改进来自SCN本身。
  - **局限**：仅覆盖5个数据集，未在更大规模或跨领域数据集（如Cityscapes、ADE20K全量）上验证；消融仅整体模块，未深入分析适配器内部结构、融合权重生成器等；缺乏对训练/推理速度、参数量统计。

## 6. 论文的主要结论与发现
- SCN通过显式建模语义相关性，在保持CLIP零样本泛化能力的同时，显著提升模型在语义模糊类别上的判别力。
- 类别消歧模块在logit空间操作能有效捕获类间隐式依赖，logits融合模块动态整合多源语义证据，两者共同提升性能。
- 在ADE20K、PASCAL-Context等基准上，SCN超越此前最先进方法，尤其是在细粒度类别（A-847、PC-459）上也有提升。

## 7. 优点
- **创新性**：首次在开放词汇分割中直接于相似度（logits）空间显式建模类间相关性，区别于以往的特征聚合或微调。
- **简洁有效**：SCN模块轻量（适配器+卷积），可端到端学习，无需改变CLIP主干，易于集成。
- **泛化保持**：通过残差结构保留原始相似度排序，动态融合避免过拟合，实验证明在未见类别上性能稳定。
- **实验验证充分**：跨多个基准进行全面对比，消融实验验证每个模块的必要性。

## 8. 不足与局限
- **实验覆盖有限**：训练仅使用COCO-Stuff单一数据集，未见在其他大规模或特定领域（如医学、遥感）的泛化验证。
- **消融深度不足**：仅进行了BL、+CD、+CD+LF的三级消融，未对增强相似度中交叉注意力、适配器结构、融合权重生成器等做细粒度分析。
- **部署细节缺失**：未报告推理速度、参数量、内存占用等实际应用关键指标。
- **可重复性**：未提供开源代码（文章显示为arxiv预印本，代码可能未公开），影响结果复现。
- **潜在偏差**：对比方法中部分调用了作者的重新评估（*），可能因实现差异导致小幅度波动；定性结果仅展示3个样例，代表性有限。

（完）
