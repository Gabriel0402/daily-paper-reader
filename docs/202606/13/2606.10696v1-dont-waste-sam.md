---
title: "Don't waste SAM"
title_zh: 不要浪费SAM
authors: "Nermeen Abou Baker, Uwe Handmann"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10696v1"
tags: ["query:mmseg"]
score: 9.0
evidence: SAM微调垃圾分割
tldr: SAM模型在通用分割中表现优异，但直接用于废物分割任务时精度不足。本研究对SAM进行微调，针对三个含有遮挡、透明等挑战的废物分割数据集。微调后的SAM-ViT-H在Zerowaste和TACO上IoU提升超过30点，接近TrashCan 1.0性能。验证了微调SAM作为基础模型可有效泛化到下游废物分割任务，避免浪费其潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10696v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1185, \"height\": 246, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10696v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1283, \"height\": 896, \"label\": \"Table\"}]"
motivation: SAM零样本性能在废物分割场景中有限，需通过微调提升泛化能力以应对遮挡、透明等挑战。
method: 使用SAM-ViT-H模型在三个废物分割数据集上进行全参数微调，不修改架构。
result: 微调模型在Zerowaste和TACO上IoU提升+30，在TrashCan上仅比SOTA低-1.44。
conclusion: 微调SAM作为基础模型是提升废物分割任务泛化性的关键步骤，SAM不应被浪费。
---

## 摘要
Meta AI 最近发布了 Segment Anything Model (SAM)，该模型在各种任务中表现出了卓越的零样本图像分割性能，且具有极高的准确性。尽管 SAM 无法在多个研究领域提供精确的分割，但它仍然是支持分割管线过程的有价值起点，特别是对于那些需要大量高级技能标注的任务。本研究旨在使用三个废物分割数据集评估 SAM 及微调 SAM 模型的泛化能力。尽管这些数据集是从真实场景中捕获的（如同 SAM 预训练时所使用的场景），但它们仍面临诸多挑战，包括遮挡、可变形物体、透明物体以及易与背景混淆的物体。在我们的发现中，微调后的 SAM-ViT-H 模型在 Zerowaste 和 TACO 数据集上优于现有最先进方法，IoU 显著提升 +30，并且在 TrashCan 1.0 数据集上逼近其性能水平，仅差 -1.44。在评估这些流行的废物数据集后，很明显，将 SAM 微调为基座模型是为下游废物分割任务提供更好泛化能力的关键一步。因此，不应忽视或浪费 SAM。

## Abstract
Meta AI has recently released the Segment Anything Model (SAM), which demonstrates exceptional zero-shot image segmentation performance across various tasks with remarkable accuracy. Despite its inability to provide accurate segmentation across multiple research fields, SAM still serves as a valuable starting point for supporting the segmentation pipeline process, particularly for tasks that require extensive and senior skills annotations. This study aims to evaluate the generalization of SAM and fine-tuning SAM models using three waste segmentation datasets. Although they are captured from real scenes as SAM was pretrained on, these datasets present several challenges, including occlusions, deformable objects, transparency, and objects easily confused with backgrounds. In our findings, the fine-tuned SAM-ViT-H model outperforms the state-ofthe-art Zerowaste, and TACO datasets with a significant increase of +30 in IoU, and it closely approaches performance levels of TrashCan 1.0, with only a -1.44 difference. After evaluating these popular waste datasets, it became evident that fine-tuning SAM as a foundational model is a crucial step for providing better generalization for downstream waste segmentation tasks. Therefore, SAM should not be disregarded or wasted.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：Meta AI 提出的 Segment Anything Model（SAM）在自然场景中展现出强大的零样本分割能力，但直接应用于特定领域（如废物分割）时，由于存在遮挡、透明、变形物体以及与背景易混淆等挑战，其性能有限。作者认为不应“浪费”SAM 作为基座模型的潜力，而是通过微调使其适配下游任务，提升泛化能力。
- **背景**：废物分割任务对于自动化回收和海洋垃圾清理有重要意义，但现有数据集（如 Zerowaste、TrashCan、TACO）具有高复杂性（不平衡的类别、可变背景、光照变化等）。已有方法（如 DeepLabv3+）在这些场景下泛化不足。SAM 凭借大规模预训练（SA-1B 数据集，1.1 亿掩码）提供了强有力的起点，但需要针对性调优。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在预训练 SAM 基础上，仅微调掩码解码器（mask decoder），保持图像编码器（ViT 骨干）和提示编码器（prompt encoder）冻结，以适配新任务。使用来自真实标注框（bbox）的提示，避免使用点或文本提示带来的额外复杂性。
- **关键技术细节**：
  - 模型选择：使用 SAM 的三个版本 ViT-B、ViT-L、ViT-H（参数数分别为 91M、308M、636M）。
  - 损失函数：`20 * focal loss + dice loss + MSE loss`，兼顾分类和掩码质量。
  - 优化器：Adam，学习率 `8 × 10⁻⁴`，批大小 8，早停（early stopping）防止过拟合。
  - 提示方式：仅使用由 Ground Truth 掩码生成的边界框（bbox）作为稀疏提示，降低任务难度并节省计算。
- **训练流程**：加载预训练 SAM 权重，初始化掩码解码器，仅在该解码器上进行梯度更新。输入包含图像和对应的边界框提示，输出为前景/背景分割掩码。

### 3. 实验设计：数据集、基准与对比方法

- **数据集**（三个废物分割数据集，均以 COCO 格式组织）：
  - **Zerowaste**：4661 张图像，来自传送带场景，包含可变形物体（纸板、软塑料、硬塑料、金属），背景高度杂乱。
  - **TrashCan 1.0**：7212 张水下图像，包含垃圾、植物和动物，用于海洋机器人自主垃圾分割。
  - **TACO**：1500 张图像，4784 个标注，来自森林、道路、海滩等多种户外环境，标注较粗糙。
- **基准方法**：DeepLabv3+（在 Waste 任务上的 SOTA 之一），以及未经微调的原始 SAM（ViT-B、ViT-L、ViT-H）。
- **对比设置**：每个数据集上分别运行原始 SAM 和微调后的 SAM（三种骨干大小），共 7 种方法对比（包括 DeepLabv3+）。
- **评估指标**：平均交并比（Mean IoU）、平均精确率（Mean Precision）、平均 F1 像素准确率（Mean F1 Pixel Accuracy）。

### 4. 资源与算力

- **硬件**：单张 NVIDIA Quadro RTX 8000 GPU，显存 48 GB。
- **训练轮次**：Zerowaste 和 TrashCan 1.0 各训练 5 个 epoch；TACO 训练 10 个 epoch（因数据更复杂）。
- **未详细说明**：论文未给出单次训练耗时或总 GPU 时长，也未说明是否进行超参数调优的细节（如学习率搜索）。

### 5. 实验数量与充分性

- **实验组数**：共 3 个数据集 × 7 种方法 = 21 个实验结果（每个组合给出 IoU、Precision、F1 三个指标），数据规模较大。
- **充分性**：
  - 正向：覆盖了两种 SOTA 基准（DeepLabv3+ 和原始 SAM），对比对象全面；包含了三种不同规模的 SAM，验证了规模对性能的影响。
  - 局限：未进行消融实验（如不同损失函数权重、不同提示策略、是否冻结编码器）；未在非废物领域验证泛化性；未分析模型对不同类别子集的分割表现（如透明 vs. 不透明物体）；仅依赖 bbox 提示，未探索点或文本提示。
- **客观性**：实验设置规范（固定训练/测试划分，无分割的按 80%/20% 随机划分），使用了公开数据集和标准指标，结论可复现。

### 6. 论文的主要结论与发现

- **微调大幅提升性能**：微调后的 SAM-ViT-H 在 Zerowaste 上 IoU 达 82.79，较 DeepLabv3+（52.13）提升 +30.66，较原始 SAM 提升 +21.67；在 TACO 上 IoU 达 61.06，较 DeepLabv3+（29.19）提升 +31.87，较原始 SAM 提升 +14.77。
- **逼近甚至超越 SOTA**：在 TrashCan 1.0 上微调后 IoU 达 78.88，仅比原有 SOTA（RITM）低 -1.44，但优于 DeepLabv3+ 和原始 SAM。
- **模型规模影响**：ViT-H（最大版本）在所有数据集上表现最好，说明更大参数规模有助于捕捉复杂模式；但计算资源需求也更高。
- **核心观点**：SAM 不应被浪费在零样本直接使用上，而应通过微调成为下游废物分割任务的强大基座模型；微调能有效应对遮挡、透明、变形等挑战。

### 7. 优点

- **问题选择具有挑战性**：废物分割任务天然包含多种 CV 困难（变形、透明、遮挡、小目标、光照变化），验证了 SAM 微调后的鲁棒性。
- **微调策略简洁高效**：仅微调掩码解码器，无需修改 SAM 骨干结构，计算成本较低；使用 bbox 提示简化了训练输入。
- **多模型对比**：涵盖 ViT-B/L/H 三种规模，提供了可扩展性参考。
- **实验重复性好**：使用公开数据集和标准分割指标，结果透明可复现。

### 8. 不足与局限

- **实验覆盖有限**：仅评估了废物分割场景，未在其他领域（如医学、遥感）验证泛化性；数据集数量较少（仅三个），且 TACO 标注粗糙可能影响评估公平性。
- **训练策略未深入优化**：学习率、批大小等超参数固定，未进行网格搜索；损失函数权重（20×focal）未说明选择依据；未尝试冻结全部编码器或适配器（如 SAM-Adapter）的对比。
- **提示依赖真实框**：实际应用中需先进行目标检测以获得边界框，增加了管线复杂度；若检测器性能不佳，分割上限将受限制。
- **计算资源不低**：ViT-H 有 636M 参数，即使仅训练 decoder 也需 48GB GPU 显存，对小型实验室或边缘设备不友好。
- **潜在偏差**：数据集中物体和背景分布差异大，微调后可能导致模型过于专业化，丧失部分零样本能力；论文未讨论过拟合风险（仅提及早停）。
- **缺乏定性分析**：文中仅展示一例可视化（图 1），未对所有数据集和失败案例进行详细分析。

（完）
