---
title: Neuro-Symbolic Injection of LTLf Constraints in Autoregressive Reinforcement Learning Policies
title_zh: 自回归强化学习策略中LTLf约束的神经符号注入
authors: "Ashkan Ansarifard, Matteo Mancanelli, Elena Umili, Fabio Patrizi"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08312v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 神经符号方法向自回归强化学习策略注入线性时态逻辑约束
tldr: 针对离线强化学习在时序逻辑约束下优化不足的问题，提出神经符号框架将LTLf约束注入Transformer策略。通过编译LTLf公式为自动机并设计可微满足信号作为正则化损失，在导航环境实验中表明该方法在满足时序约束的同时保持竞争性回报。核心贡献是架构无关的约束注入方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08312v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 829, \"height\": 1000, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1632, \"height\": 332, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1650, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1497, \"height\": 926, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1496, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1491, \"height\": 922, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08312v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1323, \"height\": 306, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08312v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1368, \"height\": 307, \"label\": \"Table\"}]"
motivation: 现有Transformer强化学习方法仅优化奖励，无法满足高层次时序逻辑约束。
method: 将LTLf公式编译为DFA，通过可微满足信号作为正则化项注入策略训练。
result: 在导航任务上，该方法显著提高LTLf约束满足率，且回报与基线相当。
conclusion: 神经符号约束注入可有效提升自回归策略对时序逻辑约束的遵守，且保持性能。
---

## 摘要
本文研究在有限踪迹线性时序逻辑（LTLf）表达的时延任务约束下的离线强化学习（RL）。近期，诸如轨迹变换器和决策变换器之类的基于transformer的方法被采用，将RL视为序列建模问题。然而，这些方法纯粹优化奖励，并未考虑高层时序需求。为此，我们引入一个神经符号框架，将LTLf背景知识注入到此类基于transformer的RL策略中。我们的方法将LTLf公式编译为确定性有限自动机（DFA），并通过可微表示和基于逻辑的损失函数将其整合到学习过程中。具体地，我们从DFA进展中推导出可微的满足信号，并将其作为训练期间的正则化项。所得方法跨不同模型具有架构无关性。我们在包含安全性和可达性时序属性组合的规范套件的导航环境中评估所提框架。实验结果表明，融入背景知识不仅提升了约束满足度，而且在奖励回报上与普通基线保持竞争力。

## Abstract
In this work we study offline reinforcement learning (RL) under temporally extended task constraints expressed in Linear Temporal Logic over finite traces (LTLf). Recently, transformer-based approaches such as Trajectory Transformers and Decision Transformers have been adopted to address RL as a sequence modeling problem. However, these methods optimize purely for reward and do not account for high-level temporal requirements. Here, we introduce a neurosymbolic framework that injects LTLf background knowledge into such transformer-based RL policies. Our approach compiles LTLf formulas into deterministic finite automata (DFAs) and integrates them into the learning process through a differentiable representation and a logic-based loss function. In particular, we derive differentiable satisfaction signals from DFA progression and use them as a regularization term during training. The resulting method is architecture-agnostic across different models. We evaluate the proposed framework on navigation environments with specification suites covering combinations of safety and reachability temporal properties. Experimental results show that incorporating background knowledge not only improves constraint satisfaction, but also maintains competitive return compared to vanilla baselines.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：离线强化学习（offline RL）在安全关键场景（如机器人、自动驾驶）中要求策略满足时序延展的约束（例如“最终到达目标且始终避免危险”），但现有Transformer序列建模方法（如Trajectory Transformer、Decision Transformer）仅优化奖励，无法保证高层时序逻辑约束的满足。
- **背景**：LTLf（有限迹线性时序逻辑）是一种规范语言，可精确表达时序任务要求。但将符号逻辑约束与自回归生成模型结合存在鸿沟。论文旨在弥合这一差距，通过神经符号方法将LTLf背景知识注入自回归强化学习策略，提升约束满足度而不牺牲性能。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将LTLf公式编译为确定性有限自动机（DFA），通过可微的满足信号作为正则化项，在训练过程中引导策略生成符合约束的轨迹。
- **关键技术细节**：
  - **框架架构**：包含一个“逻辑适配器”，定义统一的token模式、结束标记、token到符号映射、DFA rollout和逻辑损失评估，确保数据集序列化、符号映射和逻辑评估语义一致。
  - **DFA构建**：使用ltlf2DFA工具将LTLf公式转换为DFA，并扩展处理EOT（结束）符号。
  - **逻辑损失计算**：
    1. 从自回归模型预测的下一token概率分布中，通过Gumbel-Softmax采样生成可微的软符号序列。
    2. 使用DeepDFA（一种神经概率松弛DFA）计算采样轨迹满足约束的概率。
    3. 通过蒙特卡洛采样（N条轨迹）近似期望满足概率，取负对数作为逻辑损失 \( L_\phi = -\log(\hat{P}_{\theta \models \phi}) \)。
  - **总损失**：线性组合监督损失（交叉熵）与逻辑损失：\( L = \alpha L_\phi + (1-\alpha)L_{\mathcal{D}} \)，其中 \(\alpha \in [0,1]\) 控制正则化强度。
  - **架构无关性**：适用于任何自回归模型（TT和DT），只需改写逻辑适配器。

## 3. 实验设计：数据集/场景、基准、对比方法

- **环境**：ColourBomb，一个网格导航环境，包含起点、目标单元格（P/Y/U）、炸弹单元格（B）和墙壁（W）。每步有微小惩罚，到达目标得正奖励，进入炸弹得负奖励。
- **时序规范**：两类LTLf约束：
  - 安全（Safety）：\( G(\neg bomb) \)（始终避免炸弹）
  - 安全与可达（Reach-while-Safe）：\( G(\neg bomb) \land F(goal) \)（始终避免炸弹且最终到达目标）
- **对比方法**：普通Vanilla TT和DT（\(\alpha=0\)）作为基线，以及不同\(\alpha\)值的逻辑正则化变体（\(\alpha \in \{0.01,0.05,0.1,0.2,0.4,0.6,0.8\}\)）。
- **评估指标**：平均回报、约束满足率、目标到达率、炸弹触碰率。
- **推理策略**：所有模型使用贪心解码以隔离训练中逻辑正则化的效果。

## 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量或训练时长。仅在代码仓库（GitHub）开放了实现，但未提及计算资源细节。因此无法从文中获取具体算力信息。

## 5. 实验数量与充分性

- **实验数量**：针对DT和TT两个架构，每种架构在两种规格（Safety、Reach-while-Safe）下均进行了\(\alpha\)扫描（8个值+baseline），每个条件下3次运行取均值和标准差。总计约 \((2×2×9)×3 = 108\) 次实验单元（粗略估计）。
- **充分性评估**：
  - 优点：覆盖了不同正则化强度、不同架构、不同规格，结果一致展示规律；提供了定量表格和折线图。
  - 不足：实验仅在单一环境（ColourBomb）上进行，缺乏其他领域（如机器人、自动驾驶）的验证；未与安全RL经典方法（如约束策略优化、屏蔽）进行端到端比较；仅测试了贪心解码，未探索束搜索等解码策略。实验规模相对有限。

## 6. 论文的主要结论与发现

- 逻辑正则化能显著提升约束满足率，同时保持竞争力回报。
- DT在较高\(\alpha\)（0.8）下能达到完美满足率、零炸弹触碰、100%目标到达，而普通DT完全失败（满足率0，炸弹率100%）。
- TT在安全约束下，\(\alpha=0.8\)时满足率从0.353提升至0.499，炸弹率降低；在Reach-while-Safe约束下，\(\alpha=0.1\)最优，满足率从0.018提升至0.047，回报略有改善。
- TT的提升幅度比DT更温和，作者归因于未来需要更深入的研究。
- 参数\(\alpha\)的调优至关重要：过小无改善，过大可能过于保守（牺牲目标到达）。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：提出架构无关的神经符号框架，将符号约束以可微逻辑损失注入自回归策略训练，而非修改数据表示或奖励工程。
- **统一语义**：通过逻辑适配器保证数据集序列化、符号映射、逻辑评估的语义一致，避免不一致终止处理。
- **通用性**：可处理任意LTLf公式（不限于安全片段），超越大多数安全RL工作。
- **实验设计**：系统性地扫描\(\alpha\)，展示不同约束强度下的行为变化，清晰揭示权衡关系。

## 8. 不足与局限

- **实验覆盖不足**：仅在单一阵列环境（ColourBomb）上测试，缺乏多样化基准（如D4RL、Safety Gym等）；未与现有安全RL方法（如约束策略优化、屏蔽）进行标准化对比。
- **性能差距**：TT提升相对有限（满足率从0.018到0.047），实际应用中的增益可能较小。
- **计算开销**：蒙特卡洛采样和DeepDFA推理在训练时会增加额外计算成本，但未报告运行时开销数据。
- **推理策略局限**：仅使用贪心解码，未探索束搜索或基于自动机的约束解码（作者提及留作未来工作），可能限制了测试时的约束满足能力。
- **调参敏感性**：\(\alpha\)的选择对结果影响大，缺乏自适应调节机制，实际部署需要手动调参。
- **理论保证缺失**：逻辑损失仅提供软约束，不能保证100%满足（与屏蔽方法不同）。

（完）
