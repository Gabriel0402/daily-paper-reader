---
title: "OpenDPR: Open-Vocabulary Change Detection via Vision-Centric Diffusion-Guided Prototype Retrieval for Remote Sensing Imagery"
title_zh: OpenDPR：基于以视觉为中心的扩散引导原型检索的遥感影像开放词汇变化检测
authors: "Qi Guo, Jue Wang, Yinhe Liu, Yanfei Zhong"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27645v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用SAM和VLM进行遥感影像的开放词汇变化检测
tldr: 本文针对遥感影像的开放词汇变化检测（OVCD）提出OpenDPR框架。研究发现类别识别是OVCD的主要瓶颈，为此OpenDPR利用扩散模型离线构建视觉原型，通过视觉空间检索替代传统的图文匹配。同时，针对定位精度问题，设计了弱监督S2C模块以增强基础模型的空间建模能力。该方法在无需训练的情况下表现优异，且其弱监督版本OpenDPR-W在多个基准数据集上达到了SOTA性能。
selection_source: fresh_fetch
motivation: 现有的OVCD方法受限于视觉语言模型在细粒度地物识别上的不足，以及基础视觉模型在变化定位方面缺乏先验知识。
method: 提出一种以视觉为中心的扩散引导原型检索框架，并结合空间到变化的弱监督模块S2C来优化变化区域的定位与识别。
result: 在四个主流遥感变化检测数据集上的实验证明，该方法在无监督和弱监督模式下均显著优于现有技术。
conclusion: 通过引入扩散模型生成的视觉原型和弱监督定位增强，OpenDPR有效克服了开放词汇变化检测中的识别与定位瓶颈。
---

## 摘要
开放词汇变化检测（OVCD）旨在通过实现超越固定预定义类别集的泛化，来识别感兴趣的任意变化。我们将 OVCD 重新表述为一个两阶段流水线：首先利用 SAM 和 DINOv2 等视觉基础模型（VFM）生成与类别无关的变化提案，然后利用 CLIP 等视觉语言模型（VLM）进行类别识别。我们发现类别识别错误是 OVCD 的主要瓶颈，这主要是由于基于图像-文本匹配的 VLM 在表示细粒度地表覆盖类别方面的能力有限。为了解决这一问题，我们提出了 OpenDPR，这是一个无需训练的、以视觉为中心的扩散引导原型检索框架。OpenDPR 利用扩散模型离线构建目标类别的多样化原型，并在推理过程中在视觉空间内与变化提案进行相似性检索。次要瓶颈在于变化定位，这是由于 VFM 固有的变化先验缺失所致。为了弥补这一差距，我们设计了一个名为 S2C 的空间到变化（spatial-to-change）弱监督变化检测模块，将其强大的空间建模能力适配于变化定位。将预训练的 S2C 集成到 OpenDPR 中，产生了一个可选的弱监督变体 OpenDPR-W，它以极少的监督进一步提升了 OVCD 的性能。在四个基准数据集上的实验结果表明，所提方法在两种监督模式下均达到了最先进的性能。代码可在 https://github.com/guoqi2002/OpenDPR 获取。

## Abstract
Open-vocabulary change detection (OVCD) seeks to recognize arbitrary changes of interest by enabling generalization beyond a fixed set of predefined classes. We reformulate OVCD as a two-stage pipeline: first generate class-agnostic change proposals using visual foundation models (VFMs) such as SAM and DINOv2, and then perform category identification with vision-language models (VLMs) such as CLIP. We reveal that category identification errors are the primary bottleneck of OVCD, mainly due to the limited ability of VLMs based on image-text matching to represent fine-grained land-cover categories. To address this, we propose OpenDPR, a training-free vision-centric diffusion-guided prototype retrieval framework. OpenDPR leverages diffusion models to construct diverse prototypes for target categories offline, and to perform similarity retrieval with change proposals in the visual space during inference. The secondary bottleneck lies in change localization, due to the inherent lack of change priors in VFMs. To bridge this gap, we design a spatial-to-change weakly supervised change detection module named S2C to adapt their strong spatial modeling capabilities for change localization. Integrating the pretrained S2C into OpenDPR leads to an optional weakly supervised variant named OpenDPR-W, which further improves OVCD with minimal supervision. Experimental results on four benchmark datasets demonstrate that the proposed methods achieve state-of-the-art performance under both supervision modes. Code is available at https://github.com/guoqi2002/OpenDPR.

---

## 论文详细总结（自动生成）

### 论文总结：OpenDPR —— 基于以视觉为中心的扩散引导原型检索的遥感影像开放词汇变化检测

#### 1. 核心问题与整体含义（研究动机和背景）
传统的遥感变化检测（CD）通常基于“闭集”假设，即只能识别预定义的类别。**开放词汇变化检测（OVCD）**旨在识别任意感兴趣的变化类别。
*   **研究动机**：现有的 OVCD 框架（如基于 CLIP 的方法）主要依赖“图像-文本匹配”。然而，遥感地物类别极其精细且复杂，文本描述往往过于简略或模糊，导致视觉语言模型（VLM）在遥感领域的识别精度受限。
*   **核心发现**：作者通过“Oracle（真值）实验”证明，**类别识别错误**（而非变化定位）是制约 OVCD 性能的主要瓶颈。
*   **整体含义**：本文提出 OpenDPR 框架，试图通过“以视觉为中心”的检索机制替代传统的“图文匹配”，并引入弱监督模块解决基础模型缺乏变化先验的问题。

#### 2. 论文提出的方法论
论文提出了 **OpenDPR**（训练免除型）及其增强版 **OpenDPR-W**（弱监督型）：

*   **OpenDPR 核心流程**：
    1.  **多样化原型构建（离线）**：利用大语言模型（LLM）生成目标类别的详细描述，驱动扩散模型（Diffusion Model）合成多样化的遥感图像。通过 APE 模型进行地物定位，并利用 DINOv2 提取特征，最后通过 K-Means 聚类生成代表性的**视觉原型**。
    2.  **相似性驱动的视觉检索（推理）**：利用 SAM 和 DINOv2 生成类别无关的变化提案（Proposals）。提取提案区域的视觉特征，并在视觉空间内与离线构建的原型进行相似性匹配（采用 Category-mean 或 Global-max 策略），从而确定类别。
*   **S2C 弱监督增强模块**：
    *   针对变化定位不准的问题，设计了 **Spatial-to-Change (S2C)** 模块。
    *   **核心思想**：利用图像级标签，通过类激活图（CAM）生成初步伪标签，并利用视觉基础模型（VFM）的强空间建模能力对伪标签进行细化（Refinement），通过少量的弱监督预训练提升定位精度。

#### 3. 实验设计
*   **数据集**：
    *   **二值建筑变化检测**：LEVIR-CD、WHU-CD。
    *   **语义变化检测**：Hi-UCD mini、SECOND。
*   **Benchmark 与对比方法**：
    *   **无监督/训练免除类**：CVA, PCA-KM, AnyChange, M-C-I, I-M-C 等。
    *   **弱监督类**：FCD-GAN, MS-Former, TransWCD, ACWCD 等。
*   **评价指标**：(mean) IoU 和 (mean) F1 分数。

#### 4. 资源与算力
*   **模型组件**：使用了 GPT-4（生成描述）、Diffusion-Sat（合成图像）、DINOv2（特征提取）、SAM（掩码生成）、MiT-b1（S2C 编码器）。
*   **训练细节**：S2C 模块在预训练阶段使用了 Batch Size 8，迭代 10,000 次，输入图像尺寸为 256×256。
*   **算力说明**：**文中未明确指出具体的 GPU 型号和数量**，但从参数量（S2C 约 13.4M）和迭代次数来看，该方法对算力的需求相对适中。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流数据集上进行了测试，涵盖了二值和多分类语义变化场景。
*   **消融实验**：非常充分。分别针对原型构建方式（LLM vs. Naive）、视觉编码器选择（CLIP, DINO, DINOv2）、匹配策略（Mean vs. Max）以及 S2C 的各个组件进行了详细对比。
*   **客观性**：通过 Oracle 实验定位瓶颈，并对比了单标签和多标签推理场景，实验设计逻辑严密，对比基准公平。

#### 6. 论文的主要结论与发现
*   **识别是关键**：在 OVCD 任务中，提升类别识别能力比优化变化定位更能显著改善最终结果。
*   **视觉检索优于图文匹配**：在视觉空间内进行原型检索，能有效规避 VLM 在遥感细粒度分类上的语义模糊问题。
*   **扩散模型的价值**：扩散模型合成的图像可以作为高质量的视觉参考，弥补遥感标注数据的匮乏。
*   **弱监督的增益**：引入极少量的图像级标签（S2C 模块）能显著抑制基础模型产生的伪变化（False Positives）。

#### 7. 优点
*   **创新性**：首次将扩散模型生成的视觉原型引入 OVCD，摆脱了对 CLIP 文本编码器的过度依赖。
*   **性能卓越**：在无监督和弱监督设置下均刷新了 SOTA 记录，尤其在建筑检测任务中提升巨大（IoU 提升超过 8%）。
*   **灵活性**：OpenDPR 无需训练即可部署，而 S2C 模块作为插件可进一步提升性能。

#### 8. 不足与局限
*   **罕见类别失效**：对于极其复杂或罕见的类别（如 Hi-UCD 中的人行天桥），模型仍难以准确识别，这可能源于扩散模型生成的原型存在语义偏差。
*   **依赖性**：框架性能高度依赖于前端基础模型（如 DINOv2, SAM）和扩散模型的生成质量。
*   **离线开销**：虽然推理是高效的，但离线构建多样化原型库（调用 LLM 和扩散模型）需要一定的前期准备成本。

（完）
