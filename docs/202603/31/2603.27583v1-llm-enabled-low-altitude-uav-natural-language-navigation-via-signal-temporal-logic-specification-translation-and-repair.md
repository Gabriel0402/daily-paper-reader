---
title: LLM-Enabled Low-Altitude UAV Natural Language Navigation via Signal Temporal Logic Specification Translation and Repair
title_zh: 基于信号时序逻辑规范翻译与修复的大语言模型赋能低空无人机自然语言导航
authors: "Yuqi Ping, Huahao Ding, Tianhao Liang, Longyu Zhou, Guangyu Lei, Xinglin Chen, Junwei Wu, Jieyu Zhou, Tingting Zhang"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27583v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 将自然语言转换为用于无人机导航的信号时序逻辑 (STL)
tldr: 本研究针对低空无人机在城市环境中的自然语言导航挑战，提出了一种结合大语言模型（LLM）与信号时序逻辑（STL）的统一框架。该框架利用CoT和GRPO优化的LLM将自然语言指令精准翻译为STL规范，并通过混合整数线性规划（MILP）生成轨迹。针对指令冲突或不可行约束，引入了基于MILP诊断与LLM推理的修复机制，在确保安全的前提下实现任务的鲁棒执行，显著提升了导航的可靠性。
selection_source: fresh_fetch
motivation: 城市低空无人机导航需要将模糊的自然语言指令精准转化为满足严苛时空约束且安全可行的运动规划。
method: 采用推理增强的LLM将指令翻译为STL公式，结合MILP进行轨迹合成，并设计了规格修复模块以动态调整不可行约束。
result: 仿真和实机实验证明，该框架显著提升了NL-to-STL翻译的鲁棒性，并在复杂场景下实现了安全、可解释的导航。
conclusion: 该闭环框架为非专业人员提供了一种安全、可解释且具备高度自适应能力的无人机智能导航解决方案。
---

## 摘要
低空无人机（UAV）的自然语言（NL）导航通过为非专业操作员提供直观的界面，为低空航空服务提供了一种智能且便捷的解决方案。然而，在城市环境中部署这一功能，需要将描述不详的指令精确地映射为受时空约束限制的、安全关键且动力学可行的运动规划。为了应对这一挑战，我们提出了一个统一的框架，将自然语言指令翻译为信号时序逻辑（STL）规范，随后通过混合整数线性规划（MILP）合成轨迹。具体而言，为了从自由格式的自然语言生成可执行的 STL 公式，我们开发了一种推理增强的大语言模型（LLM），利用思维链（CoT）监督和群体相对策略优化（GRPO），确保了高度的语法有效性和语义一致性。此外，为了解决由严格的逻辑或空间要求引起的不可行性问题，我们引入了一种规范修复机制。该模块结合了基于 MILP 的诊断与 LLM 引导的语义推理，在严格执行安全保障的同时，选择性地放宽任务约束。大量的仿真和真实飞行实验表明，所提出的闭环框架显著提高了自然语言到 STL 翻译的鲁棒性，实现了在复杂场景下安全、可解释且具有适应性的无人机导航。

## Abstract
Natural language (NL) navigation for low-altitude unmanned aerial vehicles (UAVs) offers an intelligent and convenient solution for low-altitude aerial services by enabling an intuitive interface for non-expert operators. However, deploying this capability in urban environments necessitates the precise grounding of underspecified instructions into safety-critical, dynamically feasible motion plans subject to spatiotemporal constraints. To address this challenge, we propose a unified framework that translates NL instructions into Signal Temporal Logic (STL) specifications and subsequently synthesizes trajectories via mixed-integer linear programming (MILP). Specifically, to generate executable STL formulas from free-form NL, we develop a reasoning-enhanced large language model (LLM) leveraging chain-of-thought (CoT) supervision and group-relative policy optimization (GRPO), which ensures high syntactic validity and semantic consistency. Furthermore, to resolve infeasibilities induced by stringent logical or spatial requirements, we introduce a specification repair mechanism. This module combines MILP-based diagnosis with LLM-guided semantic reasoning to selectively relax task constraints while strictly enforcing safety guarantees. Extensive simulations and real-world flight experiments demonstrate that the proposed closed-loop framework significantly improves NL-to-STL translation robustness, enabling safe, interpretable, and adaptable UAV navigation in complex scenarios.

---

## 论文详细总结（自动生成）

### 论文总结：基于信号时序逻辑规范翻译与修复的大语言模型赋能低空无人机自然语言导航

#### 1. 核心问题与整体含义（研究动机和背景）
低空无人机（UAV）在城市环境中的应用（如物流、监控、应急救援）日益增多。非专业操作员通常使用自然语言（NL）下达指令，但 NL 具有模糊性和不确定性，而低空飞行对安全性、时空约束和动力学可行性有极高要求。
*   **核心挑战**：如何将模糊的 NL 指令精准转化为可验证、可执行的运动规划，并处理因指令冲突或环境限制导致的规划不可行问题。
*   **研究目标**：建立一个闭环框架，利用大语言模型（LLM）将 NL 翻译为信号时序逻辑（STL），并通过形式化方法确保飞行的安全性和任务的可完成性。

#### 2. 方法论：核心思想与关键技术
该论文提出了一个集成导航框架，包含三个核心模块：
*   **推理增强的 NL-to-STL 翻译器**：
    *   **核心思想**：利用思维链（CoT）和强化学习提升逻辑翻译的准确性。
    *   **流程**：首先使用 DeepSeek-V3 生成带有推理过程（CoT）的合成数据集；接着对 Qwen2.5-0.5B 模型进行冷启动监督微调（SFT）；最后采用**群体相对策略优化（GRPO）**算法，通过语法奖励、格式奖励和 BLEU 分数奖励进行强化学习，确保生成的 STL 公式语法正确且语义一致。
*   **基于 MILP 的轨迹规划器**：
    *   将 STL 公式编码为混合整数线性规划（MILP）约束。
    *   结合 UAV 动力学模型和环境约束，求解最优轨迹，最大化鲁棒性裕度（Robustness Margin）。
*   **规范修复机制（Specification Repair）**：
    *   **诊断**：当规划不可行时，提取不可约不可解子系统（IIS），定位冲突的 STL 子公式。
    *   **修复决策**：将冲突信息反馈给 LLM，由 LLM 根据原始语义意图决定是“放宽时间约束”还是“放宽谓词条件”（安全约束如避障则绝不放宽）。
    *   **重规划**：引入松弛变量重新求解 MILP，生成最终可行轨迹。

#### 3. 实验设计
*   **数据集**：使用 **NL2TL** 数据集（包含 20,000 组训练数据和 4,000 组测试数据）。
*   **基准模型（Benchmarks）**：
    *   Llama2-13B（微调版）
    *   T5-220M（微调版）
    *   Qwen2.5-0.5B（仅 Prompt 或仅 SFT 版）
*   **实验场景**：
    *   **仿真实验**：包括多目标选择避障、顺序授权通行、带时间限制的目标到达（触发修复机制）。
    *   **实机实验**：使用 DJI Matrice 300 RTK 无人机在真实场地执行搜救任务。

#### 4. 资源与算力
*   **模型规模**：主要使用 Qwen2.5-0.5B 作为基础模型。
*   **算力细节**：文中提到使用了 LoRA 进行参数高效微调，并利用 DeepSeek-V3 生成合成数据。
*   **明确说明**：论文**未明确列出**具体的 GPU 型号、数量及训练总时长。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4,000 组测试集上进行了定量翻译准确率对比。
    *   展示了 3 组典型的仿真任务（涵盖了逻辑析取、顺序约束和不可行修复）。
    *   完成了 1 组完整的实机飞行验证。
*   **充分性评价**：实验设计较为全面，涵盖了从纯文本翻译到闭环控制的全流程。通过消融实验（SFT vs. GRPO）验证了强化学习的有效性。对比了不同参数规模的模型，证明了小模型通过推理增强也能达到极高精度（98%），具有较好的客观性和公平性。

#### 6. 主要结论与发现
*   **翻译精度高**：提出的推理增强模型在 NL-to-STL 任务上达到了 **98.0%** 的准确率，显著优于规模更大的 Llama2-13B（94.8%）。
*   **小模型优势**：通过 CoT 和 GRPO 优化，0.5B 规模的模型即可胜任复杂的逻辑翻译任务，适合边缘端部署。
*   **修复机制有效**：闭环修复模块能智能识别任务优先级，在不牺牲安全性的前提下，通过微调任务参数（如延迟到达时间）使不可行任务变得可行。

#### 7. 优点（亮点）
*   **形式化与 AI 结合**：将 LLM 的语义理解能力与 STL/MILP 的严谨数学保证相结合，解决了神经网络直接输出控制指令缺乏可解释性和安全性的问题。
*   **GRPO 算法应用**：在逻辑翻译领域引入 GRPO 强化学习，有效解决了 STL 语法敏感、微小 token 错误导致公式失效的问题。
*   **语义感知的修复**：修复过程并非盲目数学松弛，而是由 LLM 引导，能够保留用户的核心意图。

#### 8. 不足与局限
*   **环境假设**：实验主要在已知静态障碍物的环境中进行，对于动态、未知环境的实时避障和在线逻辑重构讨论较少。
*   **算力细节缺失**：未提供具体的训练资源消耗，难以评估其训练成本。
*   **应用限制**：MILP 求解在大规模复杂约束下可能存在计算延迟，对于极高动态响应要求的场景可能存在瓶颈。
*   **幻觉风险**：尽管准确率高，但 LLM 在修复决策时仍存在产生“幻觉”或错误理解冲突原因的潜在风险。

（完）
