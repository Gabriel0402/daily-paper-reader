---
title: Conformalized Signal Temporal Logic Inference under Covariate Shift
title_zh: 协变量偏移下的符合预测化信号时序逻辑推断
authors: "Yixuan Wang, Danyang Li, Matthew Cleaveland, Roberto Tron, Mingyu Cai"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27062v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 动力系统中时间行为的信号时序逻辑推理
tldr: 针对动态系统中的信号时序逻辑（STL）推理，本文提出了一种处理协变量偏移的符合预测框架。传统方法假设数据独立同分布，而本研究通过结合无模板微分推理、分布对齐微调以及基于似然比估计的加权符合预测，确保了在部署环境分布变化时，所学逻辑规则的可靠性和统计有效性，显著提升了符号学习的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的STL推理方法在处理训练与测试数据分布不一致（协变量偏移）时，难以保证逻辑规则的统计有效性。
method: 采用无模板微分STL推理学习初始模型，利用少量部署数据进行对齐微调，并结合似然比估计实现加权符合预测。
result: 实验表明，该框架在保持STL公式可解释性的同时，显著提高了在分布偏移环境下的符号学习可靠性。
conclusion: 本研究成功将加权符合预测引入STL推理，为动态系统在非平稳环境下的逻辑规则学习提供了坚实的统计保障。
---

## 摘要
信号时序逻辑 (STL) 推断旨在学习动力系统中时序行为的可解释逻辑规则。为了确保所学 STL 公式的正确性，近期的方法已将符合预测 (conformal prediction) 作为一种不确定性量化的统计工具引入。然而，大多数现有方法依赖于校准数据和测试数据独立同分布且可交换的假设，而这一假设在现实场景中经常被违背。本文提出了一种符合预测化的 STL 推断框架，专门处理训练轨迹数据集与部署轨迹数据集之间的协变量偏移。从技术角度来看，该方法首先采用一种无模板、可微的 STL 推断方法来学习初始模型，随后利用有限的部署侧数据集对其进行微调，以促进分布对齐。为了在分布偏移下提供有效性保证，该框架估计了训练分布与部署分布之间的似然比，并将其整合到基于 STL 鲁棒性的加权符合预测方案中。在轨迹数据集上的实验结果表明，所提框架在保留 STL 公式可解释性的同时，显著提高了部署时的符号学习可靠性。

## Abstract
Signal Temporal Logic (STL) inference learns interpretable logical rules for temporal behaviors in dynamical systems. To ensure the correctness of learned STL formulas, recent approaches have incorporated conformal prediction as a statistical tool for uncertainty quantification. However, most existing methods rely on the assumption that calibration and testing data are identically distributed and exchangeable, an assumption that is frequently violated in real-world settings. This paper proposes a conformalized STL inference framework that explicitly addresses covariate shift between training and deployment trajectories dataset. From a technical standpoint, the approach first employs a template-free, differentiable STL inference method to learn an initial model, and subsequently refines it using a limited deployment side dataset to promote distribution alignment. To provide validity guarantees under distribution shift, the framework estimates the likelihood ratio between training and deployment distributions and integrates it into an STL-robustness-based weighted conformal prediction scheme. Experimental results on trajectory datasets demonstrate that the proposed framework preserves the interpretability of STL formulas while significantly improving symbolic learning reliability at deployment time.

---

## 论文详细总结（自动生成）

以下是对论文《Conformalized Signal Temporal Logic Inference under Covariate Shift》（协变量偏移下的符合预测化信号时序逻辑推断）的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在动力系统轨迹数据分布发生变化（即**协变量偏移**，Covariate Shift）的情况下，学习到既具有可解释性又具备统计可靠性保证的信号时序逻辑（STL）分类规则。
*   **研究动机**：
    *   STL 能够提供人类可读的逻辑公式，适用于安全关键型任务。
    *   现有的符合预测（Conformal Prediction, CP）方法通常假设训练和测试数据是“可交换的”（独立同分布），但在机器人和控制系统的实际部署中，环境变化常导致分布不一致。
    *   如果忽略这种偏移，学习到的 STL 公式在部署时可能会出现分类性能下降或不确定性量化失效（覆盖率不足）的问题。

### 2. 方法论
该框架整合了可微 STL 推断、分布对齐和加权符合预测技术，主要包含以下关键步骤：
*   **初始模型学习**：使用 **TLINet**（一种无模板的可微 STL 学习器）在原始训练集 $D_{train}$ 上通过最小化逻辑损失函数学习初始 STL 公式参数 $\theta_0$。
*   **分布对齐微调（Adaptation）**：
    *   **密度比估计**：利用 $k$ 最近邻（kNN）方法在嵌入空间中估计训练分布与部署分布之间的似然比（权重 $\omega(X)$），对部署侧数据进行加权。
    *   **鲁棒性分布正则化**：引入 **Jensen-Rényi 散度（JRD）** 作为正则项，强制要求训练集和部署集在 STL 鲁棒性值（Robustness Score）的分布上保持一致，从而稳定微调过程。
*   **加权符合预测（Weighted CP）**：
    *   将 STL 鲁棒性值作为非符合性得分（Nonconformity Score）。
    *   利用估计的权重 $\omega(X)$ 对校准集进行加权分位数计算，从而在分布偏移下为预测结果提供理论上的覆盖率保证（即预测集合包含真实标签的概率不低于 $1-\alpha$）。
*   **终止准则**：引入**有效样本量（ESS）**作为迭代停止的诊断指标，防止过度拟合权重波动。

### 3. 实验设计
*   **数据集/场景**：
    1.  **Naval Surveillance（海军监测）**：船舶推进系统的异常检测，涉及多变量时间序列。
    2.  **VIMA Dataset**：机器人操作任务，要求将方块放入篮子同时遵守安全约束。
*   **Benchmark 与对比方法**：
    *   **神经符号方法**：TLINet（基础模型）。
    *   **深度学习方法**：LSTM/RNN。
    *   **传统机器学习/树模型**：决策树（DT）、提升决策树（BCDT）、有向无环图分类器（DAG）。
*   **评估指标**：误分类率（MCR）、经验覆盖率（Coverage）、预测集大小（Inefficiency）。

### 4. 资源与算力
*   **硬件环境**：论文明确提到实验在一台 Linux 工作站上运行，配置为 **Intel Core i9-13900KF CPU**（24 核，32 线程）。
*   **GPU 使用**：文中**未明确提及**使用了 GPU，考虑到 TLINet 和 STL 鲁棒性计算的特性，该方法可能主要依赖 CPU 并行计算或轻量级加速。
*   **训练时长**：表格显示不同任务的训练/微调时间在 **15 到 16 秒**左右，具有较高的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：在两个代表性的动力系统数据集上进行了测试，涵盖了分类性能对比、符合预测覆盖率验证以及消融实验。
*   **充分性评价**：
    *   **客观性**：对比了从传统逻辑挖掘到现代深度学习的多种基准方法。
    *   **消融实验**：专门对比了“标准 CP”与“加权 CP”在偏移环境下的表现，验证了权重补偿的必要性。
    *   **局限性**：实验主要集中在二分类任务，对于多分类或更复杂的时空逻辑覆盖尚待深入探讨。

### 6. 主要结论与发现
*   **性能提升**：通过部署侧数据的微调，STL 公式在偏移分布下的误分类率显著降低，接近“先验已知部署分布”的理想模型。
*   **统计有效性**：加权 CP 能够成功将经验覆盖率维持在预设的置信水平（如 90%）以上，而忽略偏移的标准方法在某些情况下会出现覆盖失效。
*   **效率优化**：微调后的模型生成的预测集合更紧凑（Inefficiency 更低），这意味着模型在保持安全性的同时减少了决策的模糊性。

### 7. 优点
*   **理论与实践结合**：将形式化方法（STL）的严谨性与统计学习（CP）的鲁棒性结合，解决了实际部署中的分布偏移痛点。
*   **端到端可微**：利用可微 STL 框架，使得逻辑公式的结构和参数可以通过梯度下降直接针对部署环境进行优化。
*   **无需模板**：不需要预先定义逻辑公式的结构，增强了算法在复杂任务中的通用性。

### 8. 不足与局限
*   **权重估计依赖性**：kNN 密度比估计的准确性高度依赖于嵌入空间的选择，在极高维数据下可能面临“维度灾难”。
*   **部署数据需求**：虽然需求量较小，但仍需一定规模的无标签（或部分标签）部署侧数据来进行分布对齐。
*   **任务范围**：目前仅验证了轨迹分类任务，尚未扩展到在线监控或实时控制策略的合成。

（完）
