---
title: "TwinMixing: A Shuffle-Aware Feature Interaction Model for Multi-Task Segmentation"
title_zh: TwinMixing：一种用于多任务分割的混洗感知特征交互模型
authors: "Minh-Khoi Do, Huy Che, Dinh-Duy Phan, Duc-Khai Lam, Duc-Lung Vu"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28233v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 自动驾驶感知的多任务分割
tldr: 针对自动驾驶中可行驶区域和车道线分割任务，本文提出轻量化多任务模型TwinMixing。该模型采用共享编码器和任务特定解码器架构，通过高效金字塔混合（EPM）模块增强多尺度特征提取，并利用双分支上采样（DBU）块实现精细的特征重建。在BDD100K数据集上，该模型以极低的参数量和计算量实现了优异的分割精度，在低成本硬件上具有极高的实时部署潜力。
selection_source: fresh_fetch
motivation: 旨在解决自动驾驶分割任务中，如何在低成本硬件上平衡高精度分割与实时运行性能的挑战。
method: 提出包含高效金字塔混合模块（EPM）的共享编码器和具有双分支上采样结构（DBU）的任务特定解码器。
result: "在BDD100K数据集上，Base版本仅需0.43M参数即可达到92.0%的可行驶区域mIoU和32.3%的车道线IoU。"
conclusion: TwinMixing凭借其紧凑的模块化设计，在保持高性能的同时显著降低了计算开销，非常适合嵌入式感知系统。
---

## 摘要
准确且高效的感知对于自动驾驶至关重要，其中可行驶区域分割和车道线分割等分割任务为运动规划和控制提供了关键线索。然而，在低成本硬件上保持实时性能的同时实现高分割精度仍然是一个具有挑战性的问题。为了解决这一问题，我们提出了 TwinMixing，这是一种专门为可行驶区域和车道线分割设计的轻量级多任务分割模型。所提出的网络采用共享编码器和任务特定解码器的架构，实现了特征共享和任务专业化。在编码器内部，我们提出了一个高效金字塔混合（EPM）模块，该模块通过结合分组卷积、深度空洞卷积和通道混洗操作来增强多尺度特征提取，在最小化计算成本的同时有效地扩大了感受野。每个解码器采用双分支上采样（DBU）块，该块由基于可学习转置卷积的精细分支和基于无参数双线性插值的粗粒度分支组成，实现了细节丰富且空间一致的特征重建。在 BDD100K 数据集上的广泛实验验证了 TwinMixing 在 tiny、base 和 large 三种配置下的有效性。其中，base 配置在精度和计算效率之间取得了最佳平衡，仅凭 0.43M 参数和 3.95 GFLOPs，在可行驶区域分割上达到了 92.0% 的 mIoU，在车道线分割上达到了 32.3% 的 IoU。此外，TwinMixing 在相同任务上始终优于现有的分割模型。得益于其紧凑且模块化的设计，TwinMixing 在自动驾驶和嵌入式感知系统的实时部署中展现出强大的潜力。源代码：https://github.com/Jun0se7en/TwinMixing。

## Abstract
Accurate and efficient perception is essential for autonomous driving, where segmentation tasks such as drivable-area and lane segmentation provide critical cues for motion planning and control. However, achieving high segmentation accuracy while maintaining real-time performance on low-cost hardware remains a challenging problem. To address this issue, we introduce TwinMixing, a lightweight multi-task segmentation model designed explicitly for drivable-area and lane segmentation. The proposed network features a shared encoder and task-specific decoders, enabling both feature sharing and task specialization. Within the encoder, we propose an Efficient Pyramid Mixing (EPM) module that enhances multi-scale feature extraction through a combination of grouped convolutions, depthwise dilated convolutions and channel shuffle operations, effectively expanding the receptive field while minimizing computational cost. Each decoder adopts a Dual-Branch Upsampling (DBU) Block composed of a learnable transposed convolution-based Fine detailed branch and a parameter-free bilinear interpolation-based Coarse grained branch, achieving detailed yet spatially consistent feature reconstruction. Extensive experiments on the BDD100K dataset validate the effectiveness of TwinMixing across three configurations - tiny, base, and large. Among them, the base configuration achieves the best trade-off between accuracy and computational efficiency, reaching 92.0% mIoU for drivable-area segmentation and 32.3% IoU for lane segmentation with only 0.43M parameters and 3.95 GFLOPs. Moreover, TwinMixing consistently outperforms existing segmentation models on the same tasks, as illustrated in Fig. 1. Thanks to its compact and modular design, TwinMixing demonstrates strong potential for real-time deployment in autonomous driving and embedded perception systems. The source code: https://github.com/Jun0se7en/TwinMixing.

---

## 论文详细总结（自动生成）

### TwinMixing：一种用于多任务分割的混洗感知特征交互模型

#### 1. 核心问题与研究动机
在自动驾驶系统中，实时感知环境（特别是可行驶区域和车道线）对运动规划至关重要。然而，现有的语义分割模型往往面临以下挑战：
*   **计算开销大**：通用分割模型（如 SegFormer）在嵌入式低功耗硬件上难以达到实时性。
*   **特征交互不足**：现有的轻量化模型多依赖空洞卷积扩大感受野，但缺乏有效的通道间信息交换。
*   **重建质量不佳**：简单的上采样容易产生棋盘效应或丢失边缘细节。
**TwinMixing** 的研究动机在于设计一种极度轻量化、具备强特征交互能力且能稳定重建空间细节的多任务模型，以适配低成本硬件的实时部署。

#### 2. 方法论与关键技术
该模型采用“共享编码器 + 任务特定解码器”的架构，核心创新点包括：
*   **高效金字塔混合模块 (EPM)**：
    *   **核心思想**：遵循“压缩-拆分-变换-合并”原则。
    *   **技术细节**：首先通过 1x1 分组卷积降低维度，随后进入多个并行分支，每个分支使用不同扩张率（Dilation Rate）的 **3x3 深度空洞卷积 (DD-Conv)** 捕获多尺度上下文。
    *   **通道混洗 (Channel Shuffle)**：在分组卷积后引入混洗操作，打破组间信息孤岛，增强特征多样性。
*   **双分支上采样块 (DBU)**：
    *   **精细分支 (Fine Branch)**：利用可学习的转置卷积（Deconvolution）并结合编码器的跳跃连接（Skip Connection），负责恢复边缘和几何细节。
    *   **粗粒度分支 (Coarse Branch)**：采用无参数的双线性插值，提供空间平滑性和结构稳定性，避免转置卷积常见的棋盘伪影。
*   **损失函数**：结合了 **Focal Loss**（处理类别不平衡）和 **Tversky Loss**（优化细长结构如车道线的召回率）。

#### 3. 实验设计
*   **数据集**：主要使用 **BDD100K**（包含 10 万张图像，涵盖多种天气、场景和时间）。
*   **Benchmark**：在验证集（10,000 张）上评估可行驶区域的 mIoU 以及车道线的 Acc 和 IoU。
*   **对比方法**：包括通用模型（DeepLabV3+、SegFormer）、多任务感知模型（YOLOP、YOLOv8-seg）以及最新的轻量化模型（TwinLiteNet、TwinLiteNet+、DFFM、IALaneNet）。
*   **泛化性测试**：在 **Cityscapes** 和 **ACDC** 数据集上进行了跨域定性评估。

#### 4. 资源与算力
*   **训练环境**：使用单张 **NVIDIA RTX 4090 GPU**。
*   **训练参数**：Batch size 为 16，采用 AdamW 优化器，训练 100 个 epoch。
*   **训练时长**：Base 版本每轮（epoch）训练耗时约 **1070.2 秒**。论文指出，尽管其 FLOPs 较低，但由于计算图较复杂（多分支与混洗操作），训练时间比 TwinLiteNet 系列更长。
*   **推理部署**：在 NVIDIA Jetson 系列（AGX Orin, Xavier, Orin Nano, TX2）上测试了 TensorRT 加速后的延迟。

#### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **配置扩展**：提供了 Tiny (0.10M)、Base (0.43M)、Large (1.50M) 三种规模，证明了架构的可扩展性。
*   **消融实验**：针对 DBU 模块的两个分支、EPM 模块的扩张率设置、分组卷积的组数进行了详细对比。
*   **多任务对比**：对比了“单任务独立模型”与“多任务联合模型”的性能与效率差异。
*   **量化实验**：测试了 FP32、FP16 和 INT8 (QAT) 对精度的影响，验证了部署友好性。

#### 6. 主要结论与发现
*   **性能领先**：TwinMixing Large 在 BDD100K 上达到 92.8% mIoU（可行驶区域）和 34.2% IoU（车道线），优于参数量大得多的 IALaneNet。
*   **效率极高**：Base 版本在仅有 0.43M 参数和 3.95 GFLOPs 的情况下，实现了精度与速度的最优平衡。
*   **实时性强**：Tiny 版本在 Jetson AGX Orin 上延迟仅为 21.96ms，即使在老旧的 TX2 平台上也能保持约 16 FPS。

#### 7. 优点与亮点
*   **Shuffle-Aware 设计**：巧妙地将通道混洗引入轻量化分割，解决了深度卷积通道交互弱的问题。
*   **DBU 结构**：通过双分支融合，兼顾了边缘锐度（转置卷积）和空间平滑度（双线性插值），提升了分割图的视觉质量。
*   **部署导向**：不仅关注理论指标，还提供了完整的量化方案和嵌入式硬件实测数据。

#### 8. 不足与局限
*   **极端环境挑战**：在雨雪、强光对比或隧道等恶劣条件下，车道线分割精度仍有明显下降（如隧道场景 IoU 降至 28.9%）。
*   **训练开销**：由于采用了较多的小算子和分支结构，虽然推理 FLOPs 低，但训练时的内存访问开销（Memory Access Cost）较大，导致训练速度相对较慢。
*   **超参数依赖**：EPM 的扩张率和分组数仍需人工调优，未来可考虑引入神经架构搜索（NAS）自动优化。

（完）
