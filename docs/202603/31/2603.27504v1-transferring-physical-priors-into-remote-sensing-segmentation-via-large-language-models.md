---
title: Transferring Physical Priors into Remote Sensing Segmentation via Large Language Models
title_zh: 通过大语言模型将物理先验迁移至遥感分割中
authors: "Yuxi Lu, Kunqi Li, Zhidong Li, Xiaohan Su, Biao Wu, Chenya Huang, Bin Liang"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27504v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用大语言模型和物理先验进行遥感图像分割
tldr: 本研究针对遥感图像语义分割中物理先验（如DEM、SAR）整合难、重训成本高的问题，提出一种新范式。通过大语言模型从海量词汇中提取物理知识构建物理中心知识图谱（PCKG），并据此建立Phy-Sky-SA数据集。随后开发了PriorSeg模型，利用物理一致性损失进行残差细化。该方法无需重新训练基础模型即可显著提升分割精度与物理合理性，为遥感领域结合领域知识提供了新思路。
selection_source: fresh_fetch
motivation: 现有的遥感分割模型在整合物理变量时，面临空间对齐数据依赖性强和新传感器引入时重新训练成本高的问题。
method: 利用大语言模型构建物理中心知识图谱（PCKG），并提出一种带有物理一致性损失的物理感知残差细化模型PriorSeg。
result: 实验证明PriorSeg在不重新训练基础模型的情况下，有效提升了异构设置下的分割准确率和物理合理性。
conclusion: 该研究成功展示了如何通过大语言模型将领域特定的物理先验高效融入遥感分割模型，实现了性能与效率的双重提升。
---

## 摘要
遥感图像语义分割是地球观测的基础。实现准确的结果不仅需要整合光学图像，还需要整合物理变量，如数字高程模型 (DEM)、合成孔径雷达 (SAR) 和归一化植被指数 (NDVI)。最近的基础模型 (FMs) 利用预训练来利用这些变量，但在涉及新传感器时仍依赖于空间对齐的数据和昂贵的重新训练。为了克服这些局限性，我们引入了一种将特定领域物理先验整合到分割模型中的新范式。我们首先通过提示大语言模型从 1,763 个词汇中提取物理先验，构建了一个以物理为中心的知识图谱 (PCKG)，并利用它构建了一个异构的空间对齐数据集 Phy-Sky-SA。在此基础上，我们开发了 PriorSeg，这是一种物理感知的残差细化模型，采用结合了新型物理一致性损失的视觉-物理联合策略进行训练。在异构设置下的实验表明，PriorSeg 在不重新训练基础模型的情况下提高了分割精度和物理合理性。消融实验验证了 Phy-Sky-SA 数据集、PCKG 和物理一致性损失的有效性。

## Abstract
Semantic segmentation of remote sensing imagery is fundamental to Earth observation. Achieving accurate results requires integrating not only optical images but also physical variables such as the Digital Elevation Model (DEM), Synthetic Aperture Radar (SAR) and Normalized Difference Vegetation Index (NDVI). Recent foundation models (FMs) leverage pre-training to exploit these variables but still depend on spatially aligned data and costly retraining when involving new sensors. To overcome these limitations, we introduce a novel paradigm for integrating domain-specific physical priors into segmentation models. We first construct a Physical-Centric Knowledge Graph (PCKG) by prompting large language models to extract physical priors from 1,763 vocabularies, and use it to build a heterogeneous, spatial-aligned dataset, Phy-Sky-SA. Building on this foundation, we develop PriorSeg, a physics-aware residual refinement model trained with a joint visual-physical strategy that incorporates a novel physics-consistency loss. Experiments on heterogeneous settings demonstrate that PriorSeg improves segmentation accuracy and physical plausibility without retraining the FMs. Ablation studies verify the effectiveness of the Phy-Sky-SA dataset, the PCKG, and the physics-consistency loss.

---

## 论文详细总结（自动生成）

以下是对论文《Transferring Physical Priors into Remote Sensing Segmentation via Large Language Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：遥感图像语义分割不仅依赖视觉特征，还需要数字高程模型（DEM）、合成孔径雷达（SAR）和归一化植被指数（NDVI）等物理变量来消除视觉歧义（如区分阴影与水体）。
*   **研究动机**：
    *   **数据依赖性强**：现有基础模型（FMs）依赖大规模、人工对齐的多模态数据集进行预训练。
    *   **扩展性差**：引入新传感器或新领域时，通常需要对模型进行全量重训或架构修改，成本极高。
    *   **目标**：探索如何利用大语言模型（LLM）将物理先验知识高效注入现有的冻结基础模型中，而无需昂贵的重训或手动对齐数据。

### 2. 方法论
核心思想是利用 LLM 作为“知识桥梁”，将文本定义的语义类别转化为物理数值区间，并以此指导轻量化残差模型的训练。
*   **物理中心知识图谱 (PCKG) 构建**：
    *   利用 **GPT-4o** 对 1,763 个遥感类别词汇进行推理。
    *   提示词要求 LLM 分析语义结构、映射粗类，并推断 NDVI、DEM 和 SAR 的合理数值范围（闭区间），生成包含推理链的 JSON 结构化知识。
*   **Phy-Sky-SA 数据集合成**：
    *   基于 PCKG 中的数值区间，在原始 RGB 图像的掩码基础上合成模拟的物理变量图，解决缺乏对齐物理数据的问题。
*   **PriorSeg 模型架构**：
    *   **残差细化**：附加在冻结的基础模型（如 SkySense-O）之后，输入视觉特征、初始预测掩码和可选的物理变量。
    *   **公式**：最终预测 $Y^{(1)} = Y^{(0)} + \Delta Y$，其中 $\Delta Y$ 是模型学习的残差修正量。
*   **联合训练损失函数**：
    *   **像素级损失 ($L_{seg}$)**：交叉熵 + Dice 损失。
    *   **区域级损失 ($L_{region}$)**：增强类内特征紧凑性，减少噪点。
    *   **物理一致性损失 ($L_{phys}$)**：采用 **Hinge Loss**，当预测区域的物理均值超出 PCKG 定义的合法区间时进行惩罚。
*   **推理模式**：支持“仅视觉”（利用学习到的物理记忆修正）和“视觉-物理”（利用实时物理数据加权修正）两种模式。

### 3. 实验设计
*   **数据集**：
    *   **仅视觉测试**：OEM、iSAID、LoveDA（验证泛化能力）。
    *   **视觉-物理测试**：WHU-OPT-SAR、SEN12MS（包含真实物理变量）。
*   **基准模型 (Backbones)**：对比了 SegGPT、DINOv、SAN、CAT-Seg、SegEarth-OV 和 SkySense-O 六种主流基础模型。
*   **对比实验**：评估了各基础模型在加入 PriorSeg 细化模块前后的 mIoU 提升情况。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA A100 (80 GB) GPU** 进行训练。
*   **LLM 资源**：调用了 **GPT-4o** 的 API 用于构建 PCKG。
*   **训练细节**：文中未明确给出具体的训练总时长，但提到残差设计使得模型收敛更快且模块轻量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 个主流数据集和 6 种不同的模型架构，具有较强的普适性。
    *   **消融实验**：分别验证了 Phy-Sky-SA 数据集、PCKG 知识图谱和物理一致性损失对性能的贡献（分别提升约 1.72% 到 2.78%）。
    *   **可靠性分析**：对比了模拟物理变量与真实物理变量的分布，证明了 PCKG 引导合成的数据具有高度的真实性。
*   **评价**：实验设计较为全面，通过跨模型、跨数据集的零样本（Zero-shot）细化评估，客观地证明了该范式的有效性。

### 6. 主要结论与发现
*   **性能提升**：PriorSeg 在不同基座模型上实现了 **1.14% 至 6.7% 的 mIoU 提升**。
*   **物理合理性**：引入物理先验能显著减少视觉误判（如将建筑物阴影误认为水体）。
*   **灵活性**：该模型作为“插件”，无需修改或重训原始基础模型即可提升效果，且在缺乏物理输入时仍能通过学习到的先验知识改善结果。

### 7. 优点
*   **创新范式**：首次提出利用 LLM 提取物理先验并将其转化为空间约束，解决了遥感领域多模态数据对齐难的问题。
*   **轻量高效**：残差细化设计避免了对大模型的重训，具有极高的工程实用价值。
*   **可解释性**：PCKG 提供的推理链（Reasoning trace）为分割结果的修正提供了物理依据。

### 8. 不足与局限
*   **物理变量覆盖有限**：目前仅整合了 NDVI、DEM 和 SAR 三种变量，对于更复杂的物理特性（如多光谱细分波段）尚未涉及。
*   **LLM 幻觉风险**：尽管经过验证，但 LLM 推断的数值区间可能在极端地理环境下存在偏差（如高海拔地区的植被指数异常）。
*   **合成与真实的差距**：虽然分布接近，但模拟数据仍无法完全替代复杂的真实物理传感器噪声特性。

（完）
