---
title: "IP-SAM: Prompt-Space Conditioning for Prompt-Absent Camouflaged Object Detection"
title_zh: IP-SAM：针对无提示伪装目标检测的提示空间条件化
authors: "Huiyao Zhang, Jin Bai, Rui Guo, JianWen Tan, HongFei Wang, Ye Li"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27250v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 针对自动伪装目标检测改进SAM模型
tldr: 针对伪装物体检测（COD）中缺乏显式提示的问题，本文提出IP-SAM框架。该方法通过自提示生成器（SPG）提取图像上下文作为内在提示，并利用提示空间门控（PSG）抑制背景干扰，实现了在无外部提示下的全自动分割。IP-SAM在多个COD基准测试中达到SOTA性能，且在医疗息肉分割任务中展现出极强的零样本迁移能力。
selection_source: fresh_fetch
motivation: 现有的基础分割模型高度依赖显式提示，而在全自动部署场景下，提示的缺失导致模型解码能力受限且与原生接口存在结构性失配。
method: 提出IP-SAM，利用自提示生成器（SPG）产生内在区域锚点，并通过提示空间门控（PSG）作为非对称抑制约束来减少背景引起的误报。
result: 在四个伪装物体检测基准上达到SOTA性能（如COD10K上MAE为0.017），且在医疗影像任务中表现出优秀的零样本迁移能力。
conclusion: 该研究证明了通过提示空间调节可以有效弥补基础模型在无提示场景下的结构失配，为自动分割任务提供了一种高效且泛化性强的适配方案。
---

## 摘要
提示词驱动的基础分割模型已成为图像分割的主流范式，其中显式的空间提示（如点、框、掩码）引导掩码解码。然而，许多实际部署场景需要全自动分割，这导致了结构性不匹配：解码器期望在推理阶段无法获得的提示。现有的适配方法通常修改中间特征，无意中绕过了模型的原生提示接口，从而削弱了提示驱动的解码能力。我们提出了 IP-SAM，通过提示空间条件化从提示空间的角度重新审视适配过程。具体而言，自提示生成器（SPG）将图像上下文蒸馏为互补的内在提示，作为粗略的区域锚点。这些线索通过 SAM2 冻结的提示编码器进行投影，在无需外部干预的情况下恢复了提示引导的解码。为了抑制背景引起的误报，提示空间门控（PSG）在解码前利用内在背景提示作为非对称抑制约束。在确定的无外部提示协议下，IP-SAM 在四个伪装目标检测基准测试中实现了最先进的性能（例如，在 COD10K 上 MAE 为 0.017），且仅包含 21.26M 个可训练参数（优化了 SPG、PSG 和一个从头训练的任务特定掩码解码器，以及图像编码器的 LoRA，同时保持提示编码器冻结）。此外，所提出的条件化策略在伪装目标检测之外也具有泛化性，可应用于医学息肉分割，仅在 Kvasir-SEG 上训练的模型在 CVC-ClinicDB 和 ETIS 上均表现出强大的零样本迁移能力。

## Abstract
Prompt-conditioned foundation segmenters have emerged as a dominant paradigm for image segmentation, where explicit spatial prompts (e.g., points, boxes, masks) guide mask decoding. However, many real-world deployments require fully automatic segmentation, creating a structural mismatch: the decoder expects prompts that are unavailable at inference. Existing adaptations typically modify intermediate features, inadvertently bypassing the model's native prompt interface and weakening prompt-conditioned decoding. We propose IP-SAM, which revisits adaptation from a prompt-space perspective through prompt-space conditioning. Specifically, a Self-Prompt Generator (SPG) distills image context into complementary intrinsic prompts that serve as coarse regional anchors. These cues are projected through SAM2's frozen prompt encoder, restoring prompt-guided decoding without external intervention. To suppress background-induced false positives, Prompt-Space Gating (PSG) leverages the intrinsic background prompt as an asymmetric suppressive constraint prior to decoding. Under a deterministic no-external-prompt protocol, IP-SAM achieves state-of-the-art performance across four camouflaged object detection benchmarks (e.g., MAE 0.017 on COD10K) with only 21.26M trainable parameters (optimizing SPG, PSG, and a task-specific mask decoder trained from scratch, alongside image-encoder LoRA while keeping the prompt encoder frozen). Furthermore, the proposed conditioning strategy generalizes beyond COD to medical polyp segmentation, where a model trained solely on Kvasir-SEG exhibits strong zero-shot transfer to both CVC-ClinicDB and ETIS.

---

## 论文详细总结（自动生成）

以下是对论文《IP-SAM: Prompt-Space Conditioning for Prompt-Absent Camouflaged Object Detection》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：基础分割模型（如 SAM/SAM2）依赖显式空间提示（点、框、掩码）来引导解码，但在实际部署（如伪装目标检测 COD）中，通常处于“无提示”（Prompt-Absent）状态。
*   **研究动机**：现有的适配方法多采用特征空间修改（如 Adapter 或侧边融合），这绕过了模型原生的提示接口，导致提示驱动的解码路径未被充分激活。论文旨在解决这种“结构性失配”，通过在提示空间（Prompt-Space）进行条件化，恢复 SAM2 原生的解码能力。

### 2. 论文提出的方法论
*   **核心思想**：提出 **IP-SAM** 框架，通过合成“内在提示”（Intrinsic Prompts）来激活 SAM2 的原生提示接口，而非仅仅修改中间特征。
*   **关键技术细节**：
    *   **自提示生成器 (SPG)**：利用可学习的任务查询（Task Queries）从图像特征中提取上下文，生成互补的密集掩码提示（前景 $P^+$ 和背景 $P^-$）以及稀疏标记。
    *   **提示空间门控 (PSG)**：针对伪装场景中的背景干扰，利用背景提示生成的嵌入向量作为非对称抑制约束，对前景提示进行过滤，消除解码前的虚警。
    *   **原生接口对齐**：将合成的提示输入到 SAM2 冻结的提示编码器中，将其投影到原生的提示流形空间，随后引导任务特定的掩码解码器。
    *   **高效微调**：保持提示编码器严格冻结，仅对图像编码器使用 LoRA，并从头训练轻量级的 SPG、PSG 和掩码解码器。

### 3. 实验设计
*   **数据集/场景**：
    *   **COD 基准**：CAMO、CHAMELEON、COD10K、NC4K。
    *   **泛化性测试**：医学息肉分割（Kvasir-SEG 训练，CVC-ClinicDB 和 ETIS 零样本迁移）。
*   **Benchmark 与对比方法**：
    *   **专用 COD 模型**：FSPNet, ZoomNet, DGNet, CamoFormer, ZoomNeXt 等。
    *   **SAM 适配模型**：SAM-Adapter, MedSAM, MDSAM, SAM2-Adapter 等。
    *   **基准协议**：严格遵循“无外部提示”的自动化分割协议。

### 4. 资源与算力
*   **算力设备**：单张 **NVIDIA RTX 3090 GPU**。
*   **训练细节**：使用 AdamW 优化器，学习率 $1 \times 10^{-4}$，Batch Size 为 1，训练 100 个 Epoch。
*   **参数量**：可训练参数仅为 **21.26M**（基于 SAM2-L 骨干）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个 COD 数据集上进行了全面测试，并跨领域迁移至 2 个医学影像数据集。
*   **消融实验**：非常充分。涵盖了 SPG、PSG、侧边连接、细化头等模块的增量实验，以及针对提示空间 vs 特征空间、不同 LoRA 策略、不同 PSG 算子的深入对比。
*   **公平性**：所有基线模型均在相同的分辨率（512x512）和数据分割下重新训练，并取 3 次随机种子的平均值以确保客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：IP-SAM 在所有 COD 指标上均达到 SOTA，例如在 COD10K 上 MAE 达到 0.017，优于重型专用模型和现有的 SAM 适配器。
*   **提示空间的重要性**：实验证明，通过原生提示接口注入任务信息比直接在特征空间相加更有效，能更好地利用基础模型的几何先验。
*   **背景抑制能力**：PSG 模块能显著减少伪装环境下的误报，其生成的“隔离环”能精准锁定目标边界。
*   **零样本迁移**：在未见过的医学影像数据集上表现出极强的鲁棒性，证明了该范式的通用性。

### 7. 优点
*   **范式创新**：从“特征适配”转向“提示空间条件化”，遵循了基础模型的设计初衷。
*   **非对称门控**：PSG 巧妙地利用背景信息作为约束，解决了 COD 任务中最核心的背景泄露问题。
*   **参数高效**：以极小的训练代价（约 21M 参数）实现了超越全量微调的效果。
*   **高解释性**：通过可视化门控决策图，直观展示了模型如何识别并抑制干扰区域。

### 8. 不足与局限
*   **骨干依赖性**：性能受限于底层基础模型的能力，当缩减至 SAM2-T/B 等小模型时，性能下降较明显。
*   **极端场景风险**：在极低对比度场景下，如果 SPG 无法区分前景和背景，PSG 可能会过度抑制目标信号（虽然残差连接缓解了此问题）。
*   **推理开销**：虽然参数量小，但相比原生 SAM2 增加了 SPG 和 PSG 的计算步骤（速度从 35 FPS 降至 29 FPS）。

（完）
