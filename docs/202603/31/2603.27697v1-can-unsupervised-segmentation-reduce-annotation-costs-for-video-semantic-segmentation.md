---
title: Can Unsupervised Segmentation Reduce Annotation Costs for Video Semantic Segmentation?
title_zh: 无监督分割能否降低视频语义分割的标注成本？
authors: "Samik Some, Vinay P. Namboodiri"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27697v1"
tags: ["query:mmseg"]
score: 10.0
evidence: 利用SAM和SAM 2基础模型降低视频语义分割的标注成本
tldr: 本研究针对视频语义分割中精细像素级标注成本高昂的问题，提出利用分割基础模型（SAM和SAM 2）来降低标注负担。通过结合无标注视频帧和低成本的粗略标注，研究实现了一种自动化的掩码生成流程。实验证明，该方法在保持模型性能基本不变的情况下，能有效减少约三分之一的人工标注工作量。此外，研究还揭示了数据集中帧的多样性比单纯增加帧数对提升分割性能更为重要。
selection_source: fresh_fetch
motivation: 针对视频语义分割中精细像素级标注成本极高的问题，探索利用无标注数据和廉价粗略标注降低成本的方法。
method: 采用SAM和SAM 2等分割基础模型，通过自动化掩码生成技术充分利用无标注视频帧和粗略标注。
result: 实验表明，在维持相似分割性能的情况下，该方法能够减少约三分之一的人工标注需求。
conclusion: 研究强调了数据集中帧的多样性在提升视频语义分割性能方面比增加帧的数量更具决定性作用。
---

## 摘要
现有的用于视频语义分割的深度神经网络需要大量细粒度的像素级标注才能达到最佳效果。然而，获取此类标注的成本非常高昂。另一方面，原始的、未标注的视频帧几乎可以免费获取。同样，不需要精确边界的粗略标注也便宜得多。本文研究了利用这些资源来降低视频分割数据集所需标注成本的方法。我们展示了利用最先进的分割基础模型，即 Segment Anything Model (SAM) 和 Segment Anything Model 2 (SAM 2)，我们可以利用未标注的帧以及粗略标注，通过自动化掩码生成来减轻视频分割数据集人工标注的工作量。我们的研究表明，如果使用得当，我们可以在保持视频语义分割性能相近的情况下，将标注需求减少三分之一。更重要的是，我们的分析表明，为了获得最佳性能，数据集中帧的多样性比帧的数量更为重要。

## Abstract
Present-day deep neural networks for video semantic segmentation require a large number of fine-grained pixel-level annotations to achieve the best possible results. Obtaining such annotations, however, is very expensive. On the other hand, raw, unannotated video frames are practically free to obtain. Similarly, coarse annotations, which do not require precise boundaries, are also much cheaper. This paper investigates approaches to reduce the annotation cost required for video segmentation datasets by utilising such resources. We show that using state-of-the-art segmentation foundation models, Segment Anything Model (SAM) and Segment Anything Model 2 (SAM 2), we can utilise both unannotated frames as well as coarse annotations to alleviate the effort required for manual annotation of video segmentation datasets by automating mask generation. Our investigation suggests that if used appropriately, we can reduce the need for annotation by a third with similar performance for video semantic segmentation. More significantly, our analysis suggests that the variety of frames in the dataset is more important than the number of frames for obtaining the best performance.

---

## 论文详细总结（自动生成）

以下是对论文《Can Unsupervised Segmentation Reduce Annotation Costs for Video Semantic Segmentation?》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：视频语义分割（VSS）需要大量高精度的像素级标注，但人工标注成本极高（如 Cityscapes 数据集精细标注一帧需 90 分钟）。相比之下，原始视频帧和粗略标注（Coarse Annotation，仅需 7 分钟）获取成本极低。
*   **核心问题**：能否利用视觉基础模型（如 SAM 和 SAM 2）自动生成伪标签或优化粗略标注，从而在不显著降低模型性能的前提下，大幅减少人工标注的工作量？

### 2. 方法论
论文提出了两种主要策略来利用基础模型降低成本：
*   **利用 SAM 2 处理无标注帧**：
    *   **核心思想**：以少量的手动精细标注帧为种子，利用 SAM 2 的视频追踪能力向前后帧传播掩码。
    *   **流程**：1. 选取第 20 帧的手动标注作为初始掩码；2. 使用 SAM 2 向后追踪至第 30 帧生成掩码；3. 将帧序列反转，向前追踪至第 10 帧生成掩码。最终获得第 10-30 帧的伪标签用于训练。
*   **利用 SAM 优化粗略标注**：
    *   **核心思想**：针对 Cityscapes 等自带的廉价粗略标注，利用 SAM 细化其模糊的边界。
    *   **流程**：1. 针对实例级类别（如人、车、电线杆等 8 类），在粗略掩码内随机采样两个点作为提示（Prompts）；2. 调用 SAM 生成分割掩码；3. 进行两次迭代优化。
    *   **限制**：仅针对具有明确边界的实例类进行优化，不对天空、建筑物或植被等边界模糊的类别进行处理。

### 3. 实验设计
*   **数据集**：
    *   **Cityscapes**：包含 2,975 帧精细标注和 20,000 帧粗略标注。
    *   **IDD (Indian Driving Dataset)**：选取了 4,032 帧精细标注及其对应的无标注视频序列。
*   **基准模型 (Benchmark)**：
    *   **TMANet**：利用时间记忆注意力的视频分割网络。
    *   **TDNet**：时间分布式特征提取网络。
*   **对比实验**：
    *   **N1 (Baseline)**：使用 100% 手动精细标注训练。
    *   **P2/F2/B3 等**：不同比例的手动标注与 SAM 2 生成的伪标签混合（如 B3 代表 1/3 手动 + 2/3 自动生成）。
    *   **Refined vs. Non-refined**：对比直接使用粗略标注与经过 SAM 优化后的标注效果。

### 4. 资源与算力
*   **算力说明**：文中**未明确列出具体的 GPU 型号和数量**。
*   **训练细节**：所有实验均统一训练 40,000 个迭代（Iterations）。作者提到由于“资源限制”，在 TMANet 中仅使用 1 帧历史信息，在 TDNet 中使用 2 帧。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对无标注帧，测试了从 1/2 到 1/11 等多种手动标注占比的组合。
    *   针对粗略标注，以 10% 为步长测试了从 0% 到 100% 粗略占比的性能曲线。
    *   进行了跨模型（TMANet, TDNet）和跨数据集（Cityscapes, IDD）的验证。
*   **充分性评价**：实验设计较为全面，涵盖了消融实验（如顺序帧 vs. 间隔帧的对比），能够客观反映基础模型在不同标注压力下的表现。

### 6. 主要结论与发现
*   **标注减负**：通过 SAM 2 生成伪标签，可以在**仅使用 1/3 手动标注**的情况下，达到与全手动标注接近的性能（mIoU 降幅极小）。
*   **多样性胜过数量**：实验发现，训练集中**帧的多样性（Temporal Diversity）比帧的总数更重要**。例如，使用间隔较大的帧（第 10, 20, 30 帧）训练效果远好于使用连续帧（第 19, 20, 21 帧）。
*   **SAM 优化的局限性**：SAM 细化粗略标注对实例类有效，但对非实例类（如建筑、植被）或新出现的物体（SAM 2 无法追踪未出现在初始帧的物体）效果不佳。
*   **实践建议**：对于开发者，与其费力优化粗略标注，不如直接利用 SAM 2 在无标注帧上生成伪标签，这种方式效率更高。

### 7. 优点
*   **实用性强**：直接针对工业界痛点（标注贵），提供了一套无需微调基础模型即可使用的低成本方案。
*   **洞察深刻**：揭示了视频分割训练中“时间多样性”对性能的关键影响，为后续数据采样策略提供了依据。
*   **方法简洁**：利用 SAM/SAM 2 作为“黑盒”标注器，易于集成到现有的训练管线中。

### 8. 不足与局限
*   **新物体缺失**：SAM 2 只能追踪初始帧存在的物体，对于视频中途新出现的物体无法自动标注，导致伪标签存在系统性缺失。
*   **类别覆盖有限**：SAM 优化策略在处理背景类（Stuff classes）时表现不佳，无法实现全类别的自动精细化。
*   **迭代训练无效**：作者尝试过“自我训练（Dog-fooding）”迭代优化，但未能取得更好效果，说明伪标签中的噪声仍是瓶颈。
*   **硬件信息缺失**：缺乏具体的算力消耗报告，难以评估该方案在生成伪标签阶段的时间成本。

（完）
