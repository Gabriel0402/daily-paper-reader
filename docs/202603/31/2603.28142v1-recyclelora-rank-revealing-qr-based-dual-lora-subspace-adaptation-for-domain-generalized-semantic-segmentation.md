---
title: "RecycleLoRA: Rank-Revealing QR-Based Dual-LoRA Subspace Adaptation for Domain Generalized Semantic Segmentation"
title_zh: RecycleLoRA：基于秩揭示 QR 分解的双 LoRA 子空间自适应领域泛化语义分割
authors: "Chanseul Cho, Seokju Yun, Jeaseong Jeon, Seungjae Moon, Youngmin Ro"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28142v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用视觉基础模型进行领域泛化语义分割
tldr: 针对领域泛化语义分割（DGSS）中预训练模型子空间利用不足及LoRA表示多样性受限的问题，本文提出RecycleLoRA。该方法利用秩揭示QR分解（RRQR）系统地挖掘视觉基础模型的子空间结构，通过双LoRA适配器架构——主适配器学习次要子空间方向的独立特征，子适配器微调主要方向，实现了高效的特征互补。实验表明，RecycleLoRA在合成到真实及真实到真实的泛化任务中均达到SOTA水平，且不增加推理延迟。
selection_source: fresh_fetch
motivation: 现有的LoRA方法在利用视觉基础模型子空间结构方面存在不足，且面临表示多样性受限和参数利用率低的问题。
method: 提出基于RRQR分解的双LoRA适配器架构，分别利用次要和主要子空间方向来学习多样化且互补的特征表示。
result: 在多个合成到真实和真实到真实的泛化基准测试中取得了最先进的性能，且无需复杂的架构或额外的推理开销。
conclusion: 通过系统地利用预训练子空间结构，RecycleLoRA显著提升了语义分割模型的领域泛化能力。
---

## 摘要
领域泛化语义分割 (DGSS) 旨在在未见的领域中保持鲁棒的性能。视觉基础模型 (VFMs) 提供了丰富的多领域知识，可以增强泛化能力。然而，主动利用 VFM 内部丰富子空间结构的策略仍未得到充分探索，许多现有方法主要集中在保留预训练知识上。此外，它们的 LoRA 组件通常面临表示多样性有限和参数利用效率低的问题。我们提出了 RecycleLoRA，通过采用秩揭示 QR 分解 (RRQR) 系统地利用 VFM 的子空间结构并增强 LoRA 的表示丰富度，从而解决了这两个挑战。我们的主适配器利用 RRQR 识别出的次要子空间方向来学习多样且独立的特征，即使单独使用也能达到具有竞争力的性能。我们进一步引入了一个子适配器，通过极小的调整精细地优化主要方向，为主要适配器的强大基准性能提供补充改进。这种设计使双适配器能够学习不同的表示，而无需额外的正则化损失。我们通过基于 RRQR 的初始化对预训练子空间结构进行系统利用，从而实现了卓越的领域泛化性能。RecycleLoRA 在合成到真实泛化和真实到真实泛化任务上均达到了最先进的性能，且无需复杂的架构或额外的推理延迟。

## Abstract
Domain Generalized Semantic Segmentation (DGSS) aims to maintain robust performance across unseen target domains. Vision Foundation Models (VFMs) offer rich multi-domain knowledge that can enhance generalization. However, strategies for actively exploiting the rich subspace structures within VFMs remain under-explored, with many existing methods focusing primarily on preserving pre-trained knowledge. Furthermore, their LoRA components often suffer from limited representational diversity and inefficient parameter utilization. We propose RecycleLoRA, which addresses both challenges by employing Rank-Revealing QR Decomposition (RRQR) to systematically exploit VFM's subspace structures and enhance LoRA's representational richness. Our main adapter leverages minor subspace directions identified by RRQR to learn diverse and independent features, achieving competitive performance even when used alone. We further introduce a sub adapter that carefully refines major directions with minimal adjustments, providing complementary improvements to the main adapter's strong baseline performance. This design enables the dual adapters to learn distinct representations without requiring additional regularization losses. Our systematic exploitation of pre-trained subspace structures through RRQR-based initialization leads to superior domain generalization performance. RecycleLoRA achieves state-of-the-art performance on both synthetic-to-real generalization and real-to-real generalization tasks without complex architectures or additional inference latency.

---

## 论文详细总结（自动生成）

以下是对论文《RecycleLoRA: Rank-Revealing QR-Based Dual-LoRA Subspace Adaptation for Domain Generalized Semantic Segmentation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不访问目标域数据的情况下，使语义分割模型在未见的领域（Domain Generalization, DG）保持鲁棒性。
*   **研究背景**：现有的视觉基础模型（VFM，如 DINOv2）拥有丰富的多领域知识。然而，现有的微调方法（如 LoRA）在利用 VFM 内部子空间结构方面存在不足，且往往面临**表示多样性受限**和**参数利用率低**（秩亏损）的问题。
*   **动机**：现有的 SVD（奇异值分解）方法侧重于方差保留，但不一定能提供最适合下游任务的自适应方向。作者希望通过一种新的分解方式，系统地“回收”并利用预训练权重中的子空间信息。

### 2. 核心方法论
RecycleLoRA 提出了一种基于**秩揭示 QR 分解（RRQR）**的双适配器架构：
*   **RRQR 分解**：不同于 SVD，RRQR 通过贪婪列主元选择，直接从原始权重矩阵中识别出最具代表性的列。这能更好地保留局部结构信息，并确保选定方向之间的独立性，从而减少 LoRA 的表示冗余。
*   **双适配器设计（Dual-LoRA）**：
    *   **主适配器（Main Adapter）**：利用 RRQR 识别出的**次要方向（Minor directions）**进行初始化。这些方向对原始知识贡献较小，适合以较高的学习率学习任务特定的多样化特征。
    *   **子适配器（Sub Adapter）**：利用 RRQR 识别出的**主要方向（Major directions）**进行初始化。这些方向承载核心知识，因此使用极小的秩和较低的学习率进行微调，以实现精细的补充改进。
*   **稀疏初始化**：LoRA 的 $B$ 矩阵由 RRQR 的正交基初始化，$A$ 矩阵则根据 RRQR 选出的列索引进行稀疏初始化（设为 1），以维持初始的结构偏差。

### 3. 实验设计
*   **数据集与场景**：
    *   **合成到真实（Synthetic-to-Real）**：在 GTAV 上训练，在 Cityscapes、BDD100K、Mapillary 上测试。
    *   **多源合成泛化**：结合 GTAV、Synthia 和 UrbanSyn 进行训练。
    *   **真实到真实（Real-to-Real）**：在 Cityscapes 上训练，在 BDD100K、Mapillary 上测试。
*   **基准模型（Backbone）**：主要使用 DINOv2-Large，辅助验证使用 EVA02-L。分割头采用 Mask2Former。
*   **对比方法**：包括传统的 DG 方法、基于 VFM 的方法（如 Rein, SoMA, Mfuser, tqdm）以及标准的 LoRA 和 DoRA。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA A6000 GPU**。
*   **训练细节**：所有模型训练 40,000 次迭代（iterations），batch size 为 4。
*   **推理开销**：由于 LoRA 权重可以在推理前合并到主权重中，该方法**不增加任何额外的推理延迟**。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了单源合成、多源合成、真实域迁移等多个主流 DG 场景。
*   **消融实验**：
    *   验证了主/子适配器的各自贡献。
    *   分析了不同初始化策略（Kaiming, SVD, RRQR）的对比。
    *   探讨了学习率敏感性与初始化方向的关系。
    *   进行了秩（Rank）大小的超参数搜索。
*   **客观性**：通过可视化余弦相似度热图和有效秩（Effective Rank）分析，客观证明了该方法在减少表示冗余方面的有效性。实验对比涵盖了 2024-2025 年最新的 SOTA 方法，具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：在 GTAV → Cityscapes 任务中达到 73.01 mIoU，显著超过了之前的 SOTA 方法 SoMA（71.82）。
*   **表示多样性**：RRQR 初始化生成的 LoRA 权重之间相似度更低，有效秩更高，证明了参数利用效率的提升。
*   **互补性**：主适配器负责捕获前景物体的显著修改，子适配器负责背景的广泛偏移，两者在子空间上近乎正交，形成了良好的特征互补。

### 7. 优点与亮点
*   **创新的初始化策略**：首次将 RRQR 引入 LoRA 初始化，解决了 LoRA 常见的表示坍缩问题。
*   **无需额外损失函数**：通过结构设计（主/子适配器分工）自然实现特征互补，无需复杂的正则化或对抗损失。
*   **高效性**：在提升性能的同时，保持了参数高效微调（PEFT）的低开销特性，且推理零延迟。

### 8. 不足与局限
*   **超参数敏感性**：主/子适配器的秩分配和学习率比例需要根据不同任务进行手动调优（如合成到真实与真实到真实的配置不同）。
*   **中间方向的缺失**：目前仅利用了“主要”和“次要”两端方向，中间部分的子空间信息被忽略，可能仍有提升空间。
*   **应用范围**：虽然在语义分割上表现优异，但尚未在目标检测、视频分析等其他视觉任务或多模态任务中广泛验证。

（完）
