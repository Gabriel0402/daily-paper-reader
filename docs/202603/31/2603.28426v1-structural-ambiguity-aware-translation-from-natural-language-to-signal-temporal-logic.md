---
title: Structural-Ambiguity-Aware Translation from Natural Language to Signal Temporal Logic
title_zh: 结构歧义感知的自然语言到信号时序逻辑翻译
authors: "Kosei Fushimi, Kazunobu Serizawa, Junya Ikemoto, Kazumune Hashimoto"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28426v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 从自然语言到信号时序逻辑（STL）的翻译
tldr: 本研究针对非专家用户编写信号时序逻辑（STL）公式困难的问题，提出了一种感知结构歧义的自然语言到STL翻译方法。该方法基于组合范畴语法（CCG），通过保留多个语法分析结果而非单一解释，构建了一个包含n-best解析、语义组合及规范化聚合的三阶段流水线。其核心贡献在于能够显式表示歧义指令的多种合理解释，有效处理了自然语言中的附件和范围歧义，提高了翻译的可靠性。
selection_source: fresh_fetch
motivation: 针对自然语言描述任务时固有的结构歧义，解决传统方法强制单一解释导致翻译不可靠的问题。
method: 开发了基于CCG的三阶段流水线，通过n-best解析和模板语义组合生成并聚合带得分的STL候选集。
result: 案例研究表明，该方法能为歧义输入生成多个候选公式，同时将无歧义或等价的推导合并为单一公式。
conclusion: 该方法通过显式保留和量化歧义，为从自然语言到形式化逻辑的可靠翻译提供了一种有效的新途径。
---

## 摘要
信号时序逻辑 (STL) 被广泛用于规定信息物理系统中的定时和安全关键任务，但直接编写 STL 公式对于非专家用户而言具有挑战性。自然语言 (NL) 提供了一个便捷的接口，但其固有的结构歧义使得一对一地翻译为 STL 变得不可靠。在本文中，我们提出了一种“保留歧义”的方法，用于将自然语言任务描述翻译为 STL 候选公式。其核心思想是在解析阶段保留多个合理的句法分析，而不是强制采用单一解释。为此，我们开发了一个基于组合范畴语法 (CCG) 的三阶段流水线：保留歧义的 $n$-best 解析、面向 STL 的基于模板的语义组合，以及带有得分聚合的规范化。所提出的方法输出一组去重后的 STL 候选集及其合理性得分，从而显式地表示歧义指令的多种可能的形式化解释。与现有的最优（one-best）自然语言到逻辑翻译方法相比，该方法旨在保留附着歧义和作用域歧义。对代表性任务描述的案例研究表明，该方法能为真正具有歧义的输入生成多个 STL 候选，同时将无歧义或规范等价的推导合并为单个 STL 公式。

## Abstract
Signal Temporal Logic (STL) is widely used to specify timed and safety-critical tasks for cyber-physical systems, but writing STL formulas directly is difficult for non-expert users. Natural language (NL) provides a convenient interface, yet its inherent structural ambiguity makes one-to-one translation into STL unreliable. In this paper, we propose an \textit{ambiguity-preserving} method for translating NL task descriptions into STL candidate formulas. The key idea is to retain multiple plausible syntactic analyses instead of forcing a single interpretation at the parsing stage. To this end, we develop a three-stage pipeline based on Combinatory Categorial Grammar (CCG): ambiguity-preserving $n$-best parsing, STL-oriented template-based semantic composition, and canonicalization with score aggregation. The proposed method outputs a deduplicated set of STL candidates with plausibility scores, thereby explicitly representing multiple possible formal interpretations of an ambiguous instruction. In contrast to existing one-best NL-to-logic translation methods, the proposed approach is designed to preserve attachment and scope ambiguity. Case studies on representative task descriptions demonstrate that the method generates multiple STL candidates for genuinely ambiguous inputs while collapsing unambiguous or canonically equivalent derivations to a single STL formula.

---

## 论文详细总结（自动生成）

### 论文总结：结构歧义感知的自然语言到信号时序逻辑翻译

#### 1. 核心问题与整体含义（研究动机和背景）
信号时序逻辑（STL）是描述机器人和无人机等网络物理系统（CPS）定时与安全任务的标准语言。然而，STL 公式语法严苛，非专家用户难以直接编写。虽然自然语言（NL）提供了便捷接口，但其固有的**结构歧义**（如介词短语附着位置、逻辑作用域等）使得传统“一对一”的翻译方法极不可靠。例如，“在10秒内到达B或C，同时避开A”，避开A的要求是仅针对C还是针对整个任务？如果系统强制选择一种解释，可能会导致违反用户真实意图的危险行为。本文旨在开发一种能够**保留并显式表示歧义**的翻译框架，输出多个带得分的 STL 候选公式。

#### 2. 方法论：核心思想与技术细节
论文提出了一个基于组合范畴语法（CCG）的三阶段符号化流水线：
*   **阶段1：保留歧义的 $n$-best CCG 解析**：使用概率解析器（如 `depccg`）对输入句子进行分析。与传统只取最高分结果不同，该方法保留前 $N$ 个得分最高的语法树，从而捕捉不同的句法结构（如不同的修饰语附着点）。
*   **阶段2：面向 STL 的模板化语义组合**：利用 `ccg2lambda` 工具，为每个词汇分配特定的 STL 语义模板（如 "reach X" 映射为 $\lambda I. F(I, X)$）。通过 Lambda 演算，根据语法树的结构将这些模板组合成中间语义表示。
*   **阶段3：规范化与得分聚合**：
    *   将中间表示转换为标准 STL 语法。
    *   **规范化**：识别并合并语义等价但推导路径不同的公式。
    *   **得分聚合**：将合并后的公式得分进行累加并归一化，生成最终的候选集 $C(d) = \{(\varphi_j, s_j)\}$，其中 $s_j$ 代表该解释的合理性概率。

#### 3. 实验设计
*   **实验场景**：导航与路径规划任务描述。
*   **数据集/句子集**：设计了 12 个具有代表性的测试句子（S1-S12），涵盖简单到达、带时间限制的任务、析取（Or）、顺序执行（And then）以及复杂的结构歧义模式。
*   **Benchmark（基准）**：人工标注的参考解释集（Reference Interpretations）。
*   **对比方法**：与基于提示词（Prompting）的 **ChatGPT (GPT-4o/Thinking)** 模型进行对比，评估其在枚举所有合法结构解释方面的能力。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。
*   由于该方法主要基于符号化的 CCG 解析和 Lambda 演算，而非深度神经网络的训练，其计算开销主要集中在推理阶段的语法解析上，通常对算力要求较低，可在普通 CPU 环境下运行。

#### 5. 实验数量与充分性
*   **实验规模**：共测试了 12 组典型句子，重点分析了 5 组具有高度歧义的复杂案例（S8-S12）。
*   **充分性评价**：虽然句子绝对数量较少，但实验设计**针对性极强**，覆盖了逻辑语义中关键的歧义模式（如作用域冲突）。通过与 LLM 的对比，客观地展示了符号化方法在处理结构逻辑时的严谨性。然而，缺乏大规模真实世界语料库的测试，属于原理验证型实验。

#### 6. 主要结论与发现
*   **歧义保留能力**：该方法能成功识别并生成所有人工标注的合法 STL 解释，而不会像传统方法那样丢失潜在意图。
*   **去重效果**：规范化步骤能有效合并冗余推导，确保输出集简洁且具有可解释性。
*   **对比 LLM**：ChatGPT 虽然能生成部分正确解释，但在处理复杂的“时间锚定”和“作用域嵌套”时容易出错（出现 E1 作用域不匹配或 E2 结构不支持的错误），且无法保证穷尽所有合法的结构解释。

#### 7. 优点
*   **透明性与可解释性**：基于符号逻辑，每一步推导都有据可查，避免了深度学习的“黑箱”问题。
*   **安全性**：通过提供候选集，系统可以与用户交互（如展示不同公式对应的轨迹），让用户在执行前确认意图，降低了误解风险。
*   **鲁棒性**：能够处理 LLM 难以精确处理的复杂逻辑嵌套和时间约束范围。

#### 8. 不足与局限
*   **语言覆盖范围**：目前仅限于预定义的语义模板（如 reach, avoid），对于更丰富、非规范的自然语言表达处理能力有限。
*   **时间约束依赖**：当前模型假设输入中包含显式的时间信息，无法处理隐含时间约束或需要上下文推理的情况。
*   **解析器依赖**：翻译质量高度依赖于底层 CCG 解析器的准确性，如果解析器无法生成正确的语法树，后续步骤将失效。
*   **扩展性**：随着句子复杂度和歧义点增加，候选集的规模可能会迅速膨胀，需要更高效的排序或交互式筛选机制。

（完）
