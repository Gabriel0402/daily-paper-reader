---
title: Domain-Guided YOLO26 with Composite BCE-Dice-Lovász Loss for Multi-Class Fetal Head Ultrasound Segmentation
title_zh: 采用复合 BCE-Dice-Lovász 损失的领域引导 YOLO26 用于多类别胎儿头部超声分割
authors: M. Fazri Nizar
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.26755v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 将Segment Anything Model (SAM) 应用于医学超声分割
tldr: 本研究针对产前超声胎儿头部结构分割的挑战，提出了一种基于 YOLO26-Seg 的无需提示词（prompt-free）的自动分割流水线。通过引入 BCE-Dice-Lovász 复合损失函数、逆频率类别加权以及遵循解剖结构的领域引导复制粘贴增强技术，该方法能同时检测并分割大脑、透明隔腔（CSP）和侧脑室（LV）。实验表明，该方法在多类别分割精度上显著优于现有的基于 SAM 的基准模型，为临床产科影像提供了更高效的自动化工具。
selection_source: fresh_fetch
motivation: 现有的胎儿超声分割方法通常依赖于测试时的手动边界框提示，且在处理类别不平衡和复杂解剖结构时表现不足。
method: 基于 YOLO26-Seg 构建了集成复合损失函数和领域引导增强技术的端到端框架，并采用跨患者分层划分以防止数据泄露。
result: 在 575 张测试图像上，该方法的平均 Dice 系数达到 0.9253，在仅计算前景类别的情况下仍比基准模型高出 2.68 个百分点。
conclusion: 结合领域知识的增强策略和优化的复合损失函数能有效提升医学超声图像中细微结构的分割性能，实现了优于 SOTA 的全自动分割。
---

## 摘要
从产前超声中分割胎儿头部结构仍然是产科成像中的一个实际瓶颈。目前最先进的基准模型（随已发布数据集一同提出）采用了带有逐类别 Dice 和 Lovász 损失的 Segment Anything Model，但在测试时仍依赖于边界框提示。我们在 YOLO26-Seg 的基础上构建了一个无需提示的流水线，可在单次前向传播中联合检测和分割三种结构：大脑、透明隔腔 (CSP) 和侧脑室 (LV)。我们方法的核心在于三项改进：(i) 具有逆频率类别加权的复合 BCE-Dice-Lovász 分割损失，通过运行时猴子补丁 (monkey-patching) 注入 YOLO26 训练循环；(ii) 领域引导的复制粘贴增强，在移植少数类结构的同时遵循其相对于大脑边界的解剖位置；(iii) 患者间分层划分，以防止数据泄露。在 575 张留出测试图像上，复合损失变体达到了 0.9253 的平均 Dice 系数，比基准模型 (0.9012) 高出 2.68 个百分点，尽管我们仅报告了三个前景类别的结果，而基准模型的报告均值包含了简单的背景类。我们进一步对各组件进行了消融实验，并讨论了标注质量和类别不平衡对 CSP 和 LV 性能的影响。

## Abstract
Segmenting fetal head structures from prenatal ultrasound remains a practical bottleneck in obstetric imaging. The current state-of-the-art baseline, proposed alongside the published dataset, adapts the Segment Anything Model with per-class Dice and Lovász losses but still depends on bounding-box prompts at test time. We build a prompt-free pipeline on top of YOLO26-Seg that jointly detects and segments three structures, Brain, Cavum Septi Pellucidi (CSP), and Lateral Ventricles (LV), in a single forward pass. Three modifications are central to our approach: (i) a composite BCE-Dice-Lovász segmentation loss with inverse-frequency class weighting, injected into the YOLO26 training loop via runtime monkey-patching; (ii) domain-guided copy-paste augmentation that transplants minority-class structures while respecting their anatomical location relative to the brain boundary; and (iii) inter-patient stratified splitting to prevent data leakage. On 575 held-out test images, the composite loss variant reaches a mean Dice coefficient of 0.9253, exceeding the baseline (0.9012) by 2.68 percentage points, despite reporting over three foreground classes only, whereas the baseline's reported mean includes the easy background class. We further ablate each component and discuss annotation-quality and class-imbalance effects on CSP and LV performance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Domain-Guided YOLO26** 的全自动胎儿头部超声图像分割方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在产前超声检查中，精确分割胎儿头部结构（大脑、透明隔腔 CSP、侧脑室 LV）对于评估胎儿发育至关重要。
*   **研究背景**：
    *   **人工瓶颈**：手动标注耗时、主观且存在观察者间差异。
    *   **现有技术局限**：目前的 SOTA 模型（如 FetSAM）虽然精度高，但在推理时需要手动提供边界框提示（Prompt），无法实现完全自动化。
    *   **数据挑战**：医学影像中存在严重的类别不平衡（大脑大，CSP/LV 小）以及数据泄露风险（同一患者的多张图像被分入训练和测试集）。

### 2. 方法论
论文基于 **YOLO26-Seg** 架构，提出了三项核心改进：
*   **复合 BCE-Dice-Lovász 损失函数**：
    *   结合了三种损失：**BCE**（提供早期训练稳定性）、**Dice**（优化 F1 分数）和 **Lovász Hinge**（直接优化 IoU 指标，使边界更锐利）。
    *   引入**逆频率类别权重**，给予少数类（CSP、LV）更强的梯度信号。
    *   通过“运行时猴子补丁（Monkey-patching）”技术无缝集成到 YOLO 框架中。
*   **领域引导的复制粘贴增强（Domain-Guided Copy-Paste）**：
    *   不同于随机增强，该方法遵循解剖学约束：将供体图像中的 CSP/LV 结构提取，并根据其相对于大脑质心的相对偏移量，粘贴到受体图像的解剖合理位置。
*   **跨患者分层划分（Inter-Patient Splitting）**：
    *   按患者 ID 而非图像张数进行划分（70/15/15），确保同一患者的图像不会同时出现在训练集和测试集中，从而保证评估的客观性。

### 3. 实验设计
*   **数据集**：使用 HC18 大赛数据集及补充的胎儿头部超声集合，共 3,832 张图像。
*   **对比基准（Benchmark）**：
    *   **FetSAM**：基于 SAM 的提示式分割模型。
    *   **语义分割模型**：包括 U-Net、UNet++、SegFormer、DeepLabV3+ 等 12 种主流架构。
*   **实验配置**：对比了三种配置：Baseline（标准 YOLO26N）、FetSAM Loss（仅加入复合损失）、Domain-Guided（大模型 YOLO26L + 复合损失 + 领域增强）。

### 4. 资源与算力
*   **硬件**：使用了 **2 张 NVIDIA T4 GPU**。
*   **训练时长**：
    *   Baseline：约 2.45 小时。
    *   FetSAM Loss 变体：约 4.04 小时。
    *   Domain-Guided 变体（YOLO26L）：约 30.88 小时（分三个 session 完成）。
*   **推理速度**：YOLO26N 在 T4 上经 TensorRT 优化后可达 476 FPS，YOLO26L 约为 120 FPS。

### 5. 实验数量与充分性
*   **实验规模**：在 575 张独立的测试图像上进行了验证。
*   **消融实验**：详细对比了损失函数改进、模型容量提升以及数据增强对性能的贡献。
*   **充分性评价**：实验设计较为充分，特别是通过跨患者划分解决了医疗影像中常见的“性能虚高”问题。对比了 13 种不同架构，证明了所提方法在实例分割范式下的优越性。

### 6. 主要结论与发现
*   **性能超越 SOTA**：复合损失变体达到了 **0.9253 的平均 Dice 系数 (mDSC)**，优于 FetSAM 的 0.9012（且 FetSAM 的均值包含了简单的背景类，而本研究仅计算前景）。
*   **检测能力提升**：领域引导增强显著提升了少数类的召回率，使 CSP 的假阴性减少了 56%，LV 减少了 35%。
*   **损失函数作用**：Lovász 损失对小结构（LV）的分割精度提升尤为明显。

### 7. 优点与亮点
*   **无需提示（Prompt-free）**：实现了端到端的全自动检测与分割，更符合临床实际部署需求。
*   **解剖学感知**：数据增强策略考虑了医学图像的先验知识，避免生成解剖学上不可能的样本。
*   **工程实现优雅**：通过猴子补丁修改损失函数，无需重构复杂的 YOLO 源码，具有良好的可移植性。

### 8. 不足与局限
*   **标注质量瓶颈**：论文指出 CSP 和 LV 的原始标注非常粗糙（有时仅 4-8 个顶点），这限制了模型学习精细解剖边界的能力，也导致评估指标可能无法完全反映真实精度。
*   **类别不平衡仍存**：尽管有改进，但大脑（0.97 DSC）与小结构（0.82-0.84 DSC）之间仍存在约 15 个百分点的性能差距。
*   **应用限制**：目前仅针对 2D 超声切面，未涉及 3D 超声或多切面联合诊断。

（完）
