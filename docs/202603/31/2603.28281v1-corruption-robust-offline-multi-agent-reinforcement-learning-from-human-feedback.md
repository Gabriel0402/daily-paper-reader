---
title: Corruption-robust Offline Multi-agent Reinforcement Learning From Human Feedback
title_zh: 针对数据污染具有鲁棒性的离线多智能体人类反馈强化学习
authors: "Andi Nika, Debmalya Mandal, Parameswaran Kamalaruban, Adish Singla, Goran Radanović"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28281v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 基于线性马尔可夫博弈的鲁棒离线多智能体强化学习
tldr: 本文研究了离线多智能体人类反馈强化学习（MARLHF）在数据受损时的鲁棒性。在强污染模型下，针对线性马尔可夫博弈，研究了均匀覆盖和单边覆盖两种场景，分别提出了具有理论界限的鲁棒算法。为解决计算复杂性，进一步提出了针对粗相关均衡（CCE）的准多项式时间算法，这是首个系统性处理离线MARLHF对抗性数据污染的工作。
selection_source: fresh_fetch
motivation: 解决离线多智能体强化学习中人类反馈数据可能存在的恶意污染问题，以提高算法的鲁棒性。
method: 在线性马尔可夫博弈框架下，设计了针对不同覆盖条件的鲁棒估计器，并利用CCE松弛来降低计算复杂度。
result: 证明了在不同覆盖条件下纳什均衡差距的收敛界限，并给出了计算高效的准多项式时间算法。
conclusion: 该研究为离线MARLHF在对抗性环境下的应用提供了坚实的理论基础和实用的算法框架。
---

## 摘要
我们研究了在强污染模型下，离线多智能体人类反馈强化学习（MARLHF）对数据污染的鲁棒性：给定一个由轨迹-偏好元组组成的数据集 $D$（每个偏好是一个 $n$ 维二进制标签向量，代表 $n$ 个智能体中每一个的偏好），其中 $ε$ 比例的样本可能被任意污染。我们使用线性马尔可夫博弈框架对该问题进行建模。首先，在均匀覆盖假设下——即所有感兴趣的

## Abstract
We consider robustness against data corruption in offline multi-agent reinforcement learning from human feedback (MARLHF) under a strong-contamination model: given a dataset $D$ of trajectory-preference tuples (each preference being an $n$-dimensional binary label vector representing each of the $n$ agents' preferences), an $ε$-fraction of the samples may be arbitrarily corrupted. We model the problem using the framework of linear Markov games. First, under a uniform coverage assumption - where every policy of interest is sufficiently represented in the clean (prior to corruption) data - we introduce a robust estimator that guarantees an $O(ε^{1 - o(1)})$ bound on the Nash equilibrium gap. Next, we move to the more challenging unilateral coverage setting, in which only a Nash equilibrium and its single-player deviations are covered. In this case, our proposed algorithm achieves an $O(\sqrtε)$ bound on the Nash gap. Both of these procedures, however, suffer from intractable computation. To address this, we relax our solution concept to coarse correlated equilibria (CCE). Under the same unilateral coverage regime, we derive a quasi-polynomial-time algorithm whose CCE gap scales as $O(\sqrtε)$. To the best of our knowledge, this is the first systematic treatment of adversarial data corruption in offline MARLHF.

---

## 论文详细总结（自动生成）

这是一份关于论文《Corruption-robust Offline Multi-agent Reinforcement Learning From Human Feedback》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：研究在**离线多智能体人类反馈强化学习（MARLHF）**中，当训练数据受到恶意篡改（数据投毒/污染）时，如何保证算法的鲁棒性。
*   **研究动机**：
    *   RLHF 已成为微调大语言模型（LLM）的主流方法，但在自动驾驶、分布式系统等现实场景中，往往涉及多个决策者的交互（即多智能体）。
    *   现有的 MARLHF 研究极少，且所有学习算法都容易受到数据投毒攻击（如注入恶意偏好标签）。
    *   多智能体环境下的战略复杂性会放大攻击的影响，单智能体鲁棒方法难以直接迁移。

### 2. 方法论：核心思想与算法流程
该研究基于**线性马尔可夫博弈（Linear Markov Games）**框架，采用了“鲁棒奖励估计 $\rightarrow$ 鲁棒 Q 函数估计 $\rightarrow$ 估计差距最小化”的流水线：

*   **核心思想**：
    *   **奖励估计**：使用 `TrimmedMLE`（修剪极大似然估计）从受损的偏好数据中恢复各智能体的奖励参数。
    *   **覆盖度分类处理**：
        *   **均匀覆盖（Uniform Coverage）**：假设数据覆盖了所有特征方向，通过鲁棒值迭代实现 $O(\epsilon^{1-o(1)})$ 的纳什均衡差距（Nash Gap）。
        *   **单边覆盖（Unilateral Coverage）**：仅假设覆盖了纳什均衡及其单方面偏离。这是更现实但也更难的设定。
    *   **偏置梯度代理（Biased Gradient Surrogate）**：在无法获取真实奖励函数的情况下，利用行为策略的特征差异作为梯度代理，通过投影梯度上升（PGA）优化纳什差距。
*   **算法流程**：
    1.  **奖励参数估计**：利用 `RewardEst` 过程，在置信集内通过 PGA 寻找最坏情况下的奖励参数。
    2.  **鲁棒 Q 函数计算**：引入惩罚项（Bonus）来应对数据污染和覆盖不足，使用 `Rob-Q` 过程估计动作价值。
    3.  **均衡求解**：
        *   对于纳什均衡（NE），在策略空间搜索最小化估计差距的策略。
        *   对于计算效率，引入**粗相关均衡（CCE）**松弛，在每个阶段博弈中使用 `Optimistic Hedge`（乐观对冲）算法。

### 3. 实验设计
*   **研究性质**：这是一篇**纯理论证明论文**。
*   **Benchmark 与对比**：
    *   论文没有进行实证实验（Empirical Experiments），而是通过**数学证明**给出了算法的收敛界限（Upper Bounds）。
    *   对比对象是理论上的最优界限。论文证明了其在均匀覆盖下的界限接近线性依赖于污染率 $\epsilon$，在单边覆盖下达到 $O(\sqrt{\epsilon})$，这与单智能体鲁棒 RL 的最优理论速率一致。

### 4. 资源与算力
*   **说明**：由于本文侧重于算法复杂度和收敛性的理论推导，文中**未提及**任何关于 GPU 型号、数量或具体训练时长的实验数据。
*   **计算复杂度分析**：作者指出 CCE 版本的算法具有**准多项式时间复杂度**，在数据集大小和特征维度上是计算可行的。

### 5. 实验数量与充分性
*   **充分性评价**：从理论研究的角度看，该研究非常充分。它涵盖了从强假设（均匀覆盖）到弱假设（单边覆盖）的过渡，并解决了从不可计算（NE）到可计算（CCE）的转化。
*   **客观性**：论文通过严谨的附录证明（A-E 节，共 20 多页）支撑其结论，逻辑链条完整。但由于缺乏模拟环境或真实数据集的验证，其在复杂非线性环境（如深度神经网络）中的表现尚待观察。

### 6. 主要结论与发现
*   **首次系统性处理**：这是首个针对离线 MARLHF 对抗性数据污染进行的系统性理论研究。
*   **鲁棒性保证**：证明了即使在 $ε$ 比例的数据被任意破坏的情况下，依然可以学习到接近纳什均衡或粗相关均衡的策略。
*   **性能界限**：
    *   均匀覆盖下：纳什差距为 $\tilde{O}(n\epsilon^{1-o(1)} + n/\sqrt{m})$。
    *   单边覆盖下：纳什/CCE 差距为 $\tilde{O}(n\sqrt{\epsilon} + n/\sqrt{m})$。
*   **计算可行性**：通过 CCE 松弛，证明了在多智能体设定下实现鲁棒性并不一定需要指数级的计算开销。

### 7. 优点
*   **理论前瞻性**：填补了多智能体 RLHF 鲁棒性领域的空白。
*   **技术创新**：提出的“偏置梯度代理”方法为在没有真实奖励信息的情况下优化博弈均衡提供了新思路。
*   **严谨性**：对不同数据覆盖情况进行了细致分类，给出了匹配单智能体设定下的最优理论速率。

### 8. 不足与局限
*   **缺乏实证验证**：完全依赖理论推导，没有在实际的多智能体环境（如星际争霸、自动驾驶模拟器）中进行测试。
*   **线性假设限制**：模型基于线性马尔可夫博弈，虽然在理论上易于处理，但难以直接应用于具有高度非线性特征的深度强化学习任务。
*   **计算瓶颈**：尽管 CCE 算法是准多项式的，但在智能体数量极多或动作空间巨大时，计算开销依然可能非常显著。
*   **应用限制**：主要针对离线设定，未讨论在线交互中的动态污染问题。

（完）
