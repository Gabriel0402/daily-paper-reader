---
title: "SegRGB-X: General RGB-X Semantic Segmentation Model"
title_zh: SegRGB-X：通用 RGB-X 语义分割模型
authors: "Jiong Liu, Yingjie Xu, Xingcheng Zhou, Rui Song, Walter Zimmer, Alois Knoll, Hu Cao"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28023v1"
tags: ["query:mmseg"]
score: 10.0
evidence: 使用MA-CLIP的通用任意模态语义分割框架
tldr: "针对多模态语义分割中传感器特性差异大及开发冗余的问题，本文提出了通用RGB-X语义分割框架SegRGB-X。该框架通过MA-CLIP提供模态感知引导，利用模态对齐嵌入捕获细粒度特征，并结合领域特定细化模块（DSRM）动态调整特征。在事件、热成像、深度等五种模态数据集上均达到SOTA性能，mIoU达65.03%，实现了跨模态的统一高效分割。"
selection_source: fresh_fetch
motivation: 旨在解决不同传感器模态特性差异导致的开发冗余问题，构建一个能够处理任意模态的通用语义分割框架。
method: 引入了基于LoRA微调的模态感知CLIP、模态对齐嵌入以及用于动态特征调整的领域特定细化模块（DSRM）。
result: "在事件、热成像、深度、偏振和光场五种模态的数据集上均超越了现有专门模型，取得了65.03%的mIoU。"
conclusion: SegRGB-X通过统一的架构实现了卓越的跨模态分割性能，为多传感器融合感知提供了高效且通用的新范式。
---

## 摘要
跨任意传感器模态的语义分割由于传感器特性的多样性而面临重大挑战，且该任务的传统配置导致了冗余的开发工作。我们通过引入一个通用的任意模态语义分割框架来应对这些挑战，该框架统一了跨多种模态的分割。我们的方法具有三项关键创新：(1) 模态感知 CLIP (MA-CLIP)，通过 LoRA 微调提供特定模态的场景理解引导；(2) 模态对齐嵌入 (Modality-aligned Embeddings)，用于捕获细粒度特征；(3) 领域特定细化模块 (DSRM)，用于动态特征调整。在包含不同互补模态（事件、热成像、深度、偏振和光场）的五个多样化数据集上进行的评估表明，我们的模型超越了专门的多模态方法，并以 65.03% 的 mIoU 达到了最先进的性能。代码将在论文被接收后发布。

## Abstract
Semantic segmentation across arbitrary sensor modalities faces significant challenges due to diverse sensor characteristics, and the traditional configurations for this task result in redundant development efforts. We address these challenges by introducing a universal arbitrary-modal semantic segmentation framework that unifies segmentation across multiple modalities. Our approach features three key innovations: (1) the Modality-aware CLIP (MA-CLIP), which provides modality-specific scene understanding guidance through LoRA fine-tuning; (2) Modality-aligned Embeddings for capturing fine-grained features; and (3) the Domain-specific Refinement Module (DSRM) for dynamic feature adjustment. Evaluated on five diverse datasets with different complementary modalities (event, thermal, depth, polarization, and light field), our model surpasses specialized multi-modal methods and achieves state-of-the-art performance with a mIoU of 65.03%. The codes will be released upon acceptance.

---

## 论文详细总结（自动生成）

这是一份关于论文《SegRGB-X: General RGB-X Semantic Segmentation Model》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的多模态语义分割（RGB-X）研究通常采用“专家模型”策略，即针对每种特定的模态组合（如 RGB+深度、RGB+热成像）设计并训练独立模型。这导致了严重的**开发冗余**，且由于单一模态数据集规模有限，模型容易陷入**过拟合**，缺乏泛化到多种传感器的能力。
*   **研究动机**：旨在构建一个**通用的（Generalist）**语义分割框架，能够使用统一的架构和参数同时处理包括事件（Event）、热成像（Thermal）、深度（Depth）、偏振（Polarization）和光场（Light Field）在内的多种模态，实现跨模态的特征共享与高效融合。

### 2. 方法论：核心思想与关键技术
SegRGB-X 框架由骨干网络（带模态对齐嵌入）、MA-CLIP 模块、领域特定细化模块（DSRM）和分割头组成。
*   **MA-CLIP (Modality-aware CLIP)**：
    *   **核心思想**：利用预训练视觉语言模型（VLM）的强大先验。
    *   **技术细节**：通过 **LoRA** 轻量化微调 CLIP 的图像编码器，使其适应非 RGB 模态。使用 **LLaMA-Adapter** 为 RGB 图像生成高质量文本描述，通过对比学习（Contrastive Loss）将多种模态的特征映射到统一的语义空间。
*   **模态对齐嵌入 (Modality-aligned Embedding)**：
    *   在 Transformer 块的输入端，除了图像 Patch 嵌入外，引入了**可学习的模态对齐 Prompt** 和由 MA-CLIP 生成的**阶段特定控制 Prompt**。
    *   **作用**：弥补输入嵌入与 MA-CLIP 语义先验之间的特征间隙，引导模型识别特定模态的特性。
*   **DSRM (Domain-specific Refinement Module)**：
    *   **算法流程**：在 Backbone 的最后一个阶段，采用基于查询（Query-based）的 Prompt 架构。
    *   **细节**：首先通过全局平均池化（GAP）和 MLP 学习模态指示器，结合**可学习的通用 Prompt（Universal Prompt）**生成查询向量，依次进行**通道注意力**和**空间注意力**计算，动态地细化和增强模态特定特征。

### 3. 实验设计
*   **数据集与场景**：涵盖了 5 种不同的互补模态：
    1.  **DeLiVER**：合成自动驾驶场景（RGB + 事件）。
    2.  **MFNet**：城市街道场景（RGB + 热成像）。
    3.  **NYUDepthV2**：室内场景（RGB + 深度）。
    4.  **ZJU RGB-P**：城市驾驶场景（RGB + 偏振）。
    5.  **UrbanLF**：城市建筑场景（RGB + 光场）。
*   **Benchmark 与对比方法**：
    *   对比了当前最先进的（SOTA）多模态分割模型，包括 **CMX**、**CMNeXt**、**Gemini Fusion** 和 **Stitch Fusion**。
    *   对比基准分为“专家模型”（在单一数据集训练）和 SegRGB-X 的“通用模型”（联合训练）。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 张 NVIDIA A5000 GPU**。
*   **训练细节**：
    *   Batch size 为 8。
    *   初始学习率 $1 \times 10^{-4}$，采用 poly 策略。
    *   训练时长为 200 个 epoch。
    *   输入分辨率统一裁剪/缩放为 $512 \times 512$。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的联合训练实验，并在 5 个主流数据集上进行了详尽的定量对比。
*   **消融实验**：非常充分，涵盖了：
    *   关键组件（MA-CLIP、Prompt、DSRM）的有效性分析。
    *   DSRM 内部 9 种不同结构（注意力顺序、Query/Key 组合）的对比。
    *   不同文本生成模型（BLIP vs LLaMA-Adapter）对性能的影响。
    *   不同模态对齐策略（Aligned, Cross-modal, RGB-dominant）的探讨。
*   **客观性**：通过 t-SNE 可视化证明了 MA-CLIP 提取特征的区分度，并展示了在极端边缘情况（如过曝、运动模糊）下的鲁棒性，实验设计严谨、客观。

### 6. 主要结论与发现
*   **性能领先**：SegRGB-X 在 5 个数据集上的平均 mIoU 达到 **65.03%**，显著优于现有的专家模型。
*   **联合训练优势**：相比于单模态训练，联合训练能提升约 0.65% 的 mIoU，证明了跨模态共享表示的有效性。
*   **RGB 的主导作用**：实验发现，在模态对齐嵌入中使用“RGB 主导”策略效果最好，说明 RGB 提供的语义先验对其他模态具有强力的引导作用。
*   **微调潜力**：在通用模型基础上进行特定数据集的微调，性能可进一步提升至 65.72%。

### 7. 优点（亮点）
*   **通用性**：打破了“一模态一模型”的限制，用一个模型处理五种甚至更多模态，极大地降低了部署成本。
*   **VLM 的成功应用**：巧妙地通过 LoRA 和文本引导将 CLIP 的知识迁移到了非可见光模态（如事件相机、偏振）。
*   **鲁棒性强**：在传感器退化（模糊、低分辨率）和恶劣天气（夜间、雨雾）下表现稳定。
*   **效率平衡**：在提升精度的同时，推理延迟（31.51ms）保持在可接受范围内，具有实际应用潜力。

### 8. 不足与局限
*   **场景偏差**：在室内数据集（NYUDepthV2）上的表现相对一般（47.77% mIoU）。作者分析这是由于数据分布不均导致的（80% 的训练数据是室外场景），模型存在明显的**领域偏好**。
*   **模态覆盖**：虽然涵盖了 5 种模态，但尚未包含自动驾驶中极重要的 **LiDAR（点云）** 模态。
*   **计算开销**：虽然避免了多模型部署，但相比于最轻量级的专家模型（如 CMNeXt），其推理延迟仍高出约一倍。

（完）
