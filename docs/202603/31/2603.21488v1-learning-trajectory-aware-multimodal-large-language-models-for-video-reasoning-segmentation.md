---
title: Learning Trajectory-Aware Multimodal Large Language Models for Video Reasoning Segmentation
title_zh: 学习轨迹感知的多模态大语言模型用于视频推理分割
authors: "Jingnan Luo, Mingqi Gao, Jun Liu, Bin-Bin Gao, Feng Zheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21488v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 用于视频推理分割的多模态大语言模型
tldr: 本研究针对视频推理分割中动态场景下轨迹感知不足的问题，提出了TrajSeg框架。该框架基于多模态大模型，引入了双向文本-轨迹对齐机制（涵盖定位与描述指令），显著增强了模型对物体运动轨迹的理解。通过帧级内容集成模块和统一掩码解码器，TrajSeg实现了从轨迹到掩码的高效转换，并支持端到端训练。实验证明，该方法在多个视频分割基准测试中均达到领先水平。
selection_source: fresh_fetch
motivation: 现有视频推理分割方法依赖单向隐式对齐，在处理剧烈动态视频时难以准确感知物体轨迹。
method: 提出TrajSeg框架，通过双向文本-轨迹对齐增强感知，并利用帧级内容集成模块与统一掩码解码器实现端到端分割。
result: 在引用和推理视频分割数据集上的实验表明，TrajSeg在所有指标上均优于现有的视频推理分割方法。
conclusion: TrajSeg通过强化轨迹感知和简化架构，为视频推理分割任务提供了一个高效且性能卓越的统一框架。
---

## 摘要
多模态大语言模型（MLLMs）的繁荣激发了对视频推理分割的需求，该任务旨在根据人类指令对视频对象进行分割。以往的研究依赖于单向且隐式的文本-轨迹对齐，在面对剧烈的视频动态时难以进行轨迹感知。在这项工作中，我们提出了 TrajSeg，这是一个基于 MLLMs 构建的简单且统一的框架。具体而言，我们引入了双向文本-轨迹对齐，其中 MLLMs 接受定位意图（文本到轨迹）和描述意图（轨迹到文本）的指令。通过这种方式，MLLMs 可以从增强的对应关系中获益，并更好地感知视频中的对象轨迹。从轨迹生成掩码是通过帧级内容整合（FCI）模块和统一掩码解码器实现的。前者将 MLLM 解析的轨迹级标记（token）适配到特定帧的信息。后者将所有帧的分割统一到一个单一结构中，使所提出的框架得以简化并实现端到端训练。在指代和推理视频分割数据集上的广泛实验证明了 TrajSeg 的有效性，它在所有指标上都优于现有的视频推理分割方法。代码将在 https://github.com/haodi19/TrajSeg 公开。

## Abstract
The prosperity of Multimodal Large Language Models (MLLMs) has stimulated the demand for video reasoning segmentation, which aims to segment video objects based on human instructions. Previous studies rely on unidirectional and implicit text-trajectory alignment, which struggles with trajectory perception when faced with severe video dynamics. In this work, we propose TrajSeg, a simple and unified framework built upon MLLMs. Concretely, we introduce bidirectional text-trajectory alignment, where MLLMs accept grounding-intended (text-to-trajectory) and captioning-intended (trajectory-to-text) instructions. This way, MLLMs can benefit from enhanced correspondence and better perceive object trajectories in videos. The mask generation from trajectories is achieved via a frame-level content integration (FCI) module and a unified mask decoder. The former adapts the MLLM-parsed trajectory-level token to frame-specific information. The latter unifies segmentation for all frames into a single structure, enabling the proposed framework to be simplified and end-to-end trainable. Extensive experiments on referring and reasoning video segmentation datasets demonstrate the effectiveness of TrajSeg, which outperforms all video reasoning segmentation methods on all metrics. The code will be publicly available at https://github.com/haodi19/TrajSeg.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Trajectory-Aware Multimodal Large Language Models for Video Reasoning Segmentation》的结构化深度分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视频推理分割（Video Reasoning Segmentation）方法大多是图像推理分割（如 LISA）的直接扩展，存在两个主要缺陷：
    1.  **轨迹感知能力弱**：依赖单向的“文本到轨迹”对齐，在处理剧烈运动或复杂动态场景时，模型难以通过隐式监督准确捕捉物体的时空轨迹。
    2.  **架构复杂且非端到端**：通常采用“分割+跟踪”的二阶段流水线，关键帧和非关键帧由不同的模型处理，导致训练和部署复杂，且缺乏帧间的深度交互。
*   **整体含义**：本文提出了 **TrajSeg** 框架，旨在通过双向对齐增强模型对动态轨迹的理解，并利用统一的掩码生成器实现端到端的视频推理分割。

### 2. 方法论
TrajSeg 由四个核心组件构成：
*   **双向文本-轨迹对齐（Bidirectional Alignment）**：
    *   除了常规的**定位任务**（根据文本预测轨迹 Token `<TRJ>`），引入了**描述任务**（根据物体轨迹特征预测文本描述）。
    *   通过轨迹编码器（Trajectory Encoder）将跨帧的物体特征聚合，训练 MLLM 生成描述性文本，从而显式增强模型对轨迹动态和时空一致性的理解。
*   **帧级内容集成模块（FCI）**：
    *   MLLM 输出的轨迹级 Token 虽富含语义，但缺乏帧内空间细节。
    *   FCI 模块利用交叉注意力机制，将轨迹 Token 与特定帧的视觉特征融合，生成具有空间感知力的帧级目标 Token。
*   **统一掩码生成器（Unified Mask Generator）**：
    *   **统一结构**：将关键帧（基于指令分割）和非关键帧（基于记忆跟踪）的分割逻辑整合进同一个解码器。
    *   **灵活提示**：关键帧使用帧级目标 Token 作为提示；非关键帧则利用包含历史信息的记忆库（Memory Bank）进行查询，确保时空一致性。
*   **算法流程**：输入视频和指令 -> 采样关键帧 -> MLLM 提取轨迹 Token -> FCI 细化特征 -> 统一解码器逐帧生成掩码（关键帧初始化/更新记忆，非关键帧查询记忆）。

### 3. 实验设计
*   **数据集**：
    *   **指代视频分割（RVOS）**：Ref-YouTube-VOS, Ref-DAVIS, MeViS（侧重复杂运动描述）。
    *   **推理视频分割**：ReVOS（包含指代和推理子集）。
*   **Benchmark 与对比方法**：
    *   对比了传统的 RVOS 方法（如 ReferFormer, MTTR, ReferDINO）。
    *   对比了基于 MLLM 的推理分割方法（如 TrackGPT, VISA, VideoLISA）。
*   **评价指标**：Jaccard 指数 (J)、F-measure (F) 及其平均值 (J&F)。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 GPU**（具体型号未明确说明，但根据 LLaVA-7B 的规模推测为 A100 或同级别显卡）。
*   **训练配置**：
    *   使用 DeepSpeed 优化引擎。
    *   **预训练阶段**：在图像数据集上训练 10 个 epoch。
    *   **主训练阶段**：在视频数据集上混合训练 14 个 epoch。
    *   Batch Size 为 80，学习率为 0.0003。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流基准数据集上进行了测试，涵盖了从简单指代到复杂推理的多种场景。
*   **消融实验**：
    *   验证了双向对齐（Bi-Align）对推理能力的提升。
    *   验证了 FCI 模块对空间精度的贡献。
    *   探讨了关键帧数量（1, 5, 10）对性能和时空鲁棒性的影响。
*   **充分性评价**：实验设计较为全面，不仅有定量指标对比，还提供了可视化分析（如注意力图、长视频表现、失败案例），客观地展示了模型的优劣。

### 6. 主要结论与发现
*   **性能领先**：TrajSeg 在所有视频推理分割指标上均优于现有方法，尤其在 MeViS 等长视频和复杂动态数据集中表现突出。
*   **双向学习的价值**：引入“轨迹到文本”的描述任务能显著提升模型在复杂上下文中的推理能力。
*   **架构简化**：统一的掩码生成器不仅减少了参数量（相比二阶段模型），还通过记忆机制增强了预测的稳定性。
*   **长视频鲁棒性**：相比 VISA 等方法，TrajSeg 在视频长度增加时性能下降更缓慢，证明了其轨迹感知能力的有效性。

### 7. 优点
*   **端到端统一性**：打破了“分割+独立跟踪器”的传统模式，实现了训练与推理的一致性。
*   **创新的双向对齐**：通过多任务学习（定位+描述）解决了 MLLM 在视频领域“看不懂动态”的问题。
*   **时空一致性强**：FCI 模块与统一解码器的结合，兼顾了高层语义和底层空间细节。

### 8. 不足与局限
*   **采样限制**：模型性能仍受限于输入 MLLM 的采样帧数，在极长视频中可能存在信息丢失。
*   **复杂指令歧义**：在处理涉及多个相似物体且指令极度模糊的场景时（如失败案例所示），模型仍可能出现定位错误。
*   **算力需求**：虽然架构简化了，但基于 LLaVA-7B 的微调仍需要较高的计算资源，且推理速度可能受限于大模型的自回归生成过程。

（完）
