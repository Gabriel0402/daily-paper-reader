---
title: Learning Partial Action Replacement in Offline MARL
title_zh: 在离线多智能体强化学习中学习部分动作替换
authors: "Yue Jin, Giovanni Montana"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28573v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 离线多智能体强化学习
tldr: 针对离线多智能体强化学习中联合动作空间庞大且分布外动作难以处理的问题，本文提出PLCQL框架。该框架将部分动作替换（PAR）子集选择建模为上下文老虎机问题，利用PPO算法学习状态相关的自适应策略，动态决定替换智能体的数量。PLCQL在保证策略改进的同时，将Q函数评估次数从n次降至1次，显著提升了计算效率，并在多个基准测试中取得了领先性能。
selection_source: fresh_fetch
motivation: 现有的部分动作替换方法在处理离线多智能体学习时，存在计算开销大且无法根据状态动态调整替换策略的问题。
method: 提出PLCQL框架，通过上下文老虎机建模和带不确定性权重的PPO算法，学习状态相关的自适应动作替换策略。
result: "在MPE、MaMuJoCo和SMAC等基准测试中，PLCQL在66%的任务中获得最高分，且计算效率远超现有方法。"
conclusion: PLCQL通过自适应的动作替换机制，在降低计算复杂度的同时，有效平衡了离线多智能体学习中的策略改进与保守估计。
---

## 摘要
离线多智能体强化学习（MARL）面临着一个严峻挑战：联合动作空间随智能体数量呈指数级增长，导致数据集覆盖范围呈指数级稀疏，且不可避免地会出现分布外（OOD）联合动作。部分动作替换（PAR）通过将一部分智能体锚定在数据集动作上来缓解这一问题，但现有方法依赖于枚举多个子集配置，计算成本高昂，且无法适应不同的状态。我们提出了 PLCQL 框架，该框架将 PAR 子集选择建模为上下文老虎机（contextual bandit）问题，并使用带有不确定性加权奖励的近端策略优化（PPO）来学习状态相关的 PAR 策略。这种自适应策略在每个更新步骤中动态确定要替换的智能体数量，从而在策略改进与保守价值估计之间取得平衡。我们证明了一个价值误差界限，表明估计误差与偏离智能体的期望数量呈线性比例关系。与之前基于 PAR 的方法 SPaCQL 相比，PLCQL 将每次迭代的 Q 函数评估次数从 n 减少到 1，显著提高了计算效率。实验表明，PLCQL 在 MPE、MaMuJoCo 和 SMAC 基准测试的 66% 任务中获得了最高的归一化分数，在 84% 的任务中优于 SPaCQL，同时大幅降低了计算成本。

## Abstract
Offline multi-agent reinforcement learning (MARL) faces a critical challenge: the joint action space grows exponentially with the number of agents, making dataset coverage exponentially sparse and out-of-distribution (OOD) joint actions unavoidable. Partial Action Replacement (PAR) mitigates this by anchoring a subset of agents to dataset actions, but existing approach relies on enumerating multiple subset configurations at high computational cost and cannot adapt to varying states. We introduce PLCQL, a framework that formulates PAR subset selection as a contextual bandit problem and learns a state-dependent PAR policy using Proximal Policy Optimisation with an uncertainty-weighted reward. This adaptive policy dynamically determines how many agents to replace at each update step, balancing policy improvement against conservative value estimation. We prove a value-error bound showing that the estimation error scales linearly with the expected number of deviating agents. Compared with the previous PAR-based method SPaCQL, PLCQL reduces the number of per-iteration Q-function evaluations from n to 1, significantly improving computational efficiency. Empirically, PLCQL achieves the highest normalised scores on 66% of tasks across MPE, MaMuJoCo, and SMAC benchmarks, outperforming SPaCQL on 84% of tasks while substantially reducing computational cost.

---

## 论文详细总结（自动生成）

### 论文总结：在离线多智能体强化学习中学习部分动作替换 (PLCQL)

#### 1. 核心问题与整体含义
离线多智能体强化学习（Offline MARL）的核心挑战在于**联合动作空间的组合爆炸**。随着智能体数量增加，数据集对联合动作的覆盖变得极其稀疏，导致在训练中不可避免地遇到分布外（OOD）的联合动作。这会引发 Q 值的严重高估，进而导致策略失效。

虽然之前有研究提出“部分动作替换”（PAR）策略（即只修改部分智能体的动作，其余保持数据集中已有的动作），但现有方法（如 SPaCQL）存在两个痛点：
*   **计算开销大**：需要枚举所有可能的替换子集规模，计算成本随智能体数量线性增长。
*   **缺乏灵活性**：无法根据当前环境状态动态调整替换策略。

#### 2. 方法论
论文提出了 **PLCQL (PAR Learning-based Conservative Q-Learning)** 框架，其核心思想是将 PAR 子集选择建模为一个**上下文老虎机（Contextual Bandit）**问题。

*   **核心机制**：
    *   **上下文（Context）**：当前的引导状态（Bootstrap State）$s'$。
    *   **动作（Action）**：选择替换智能体的数量 $k \in \{1, \dots, n\}$。
    *   **奖励（Reward）**：定义为**不确定性加权的 Q 值**。公式结合了 Q 函数集成（Ensemble）的方差和估计值，鼓励选择既能提升价值又具有高可靠性（低方差）的替换规模。
*   **关键技术**：
    *   使用 **PPO (Proximal Policy Optimization)** 算法来训练这个状态相关的 PAR 策略 $\pi_{par}$。
    *   在每次 Q 函数更新时，不再枚举所有 $k$，而是根据 $\pi_{par}$ 采样一个 $k$，并随机选择 $k$ 个智能体进行动作替换。
    *   **理论保证**：论文证明了价值估计误差界限与偏离智能体的期望数量呈线性关系，证明了自适应调整 $k$ 的理论合理性。

#### 3. 实验设计
*   **基准测试（Benchmarks）**：
    *   **MPE**（多智能体粒子环境）：包括协作导航、捕食者-猎物等任务。
    *   **MaMuJoCo**：多智能体连续控制任务（如 Half-Cheetah）。
    *   **SMAC**（星际争霸 II 微操）：包括 2s3z, 3s_vs_5z 等不同难度的地图。
*   **数据集类型**：涵盖 Expert（专家）、Medium（中等）、Medium-Replay（中等回放）和 Random（随机）等多种质量的数据。
*   **对比方法**：
    *   策略约束类：OMAR, IQL, MA-TD3+BC, AWAC, MAICQ。
    *   价值约束类：MACQL, CFCQL, SPaCQL。
    *   扩散模型类：DoF。

#### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA Tesla V100 GPU** 上运行。
*   **模型细节**：使用 10 个 Q 网络组成的集成模型；PAR 策略网络为 2 层隐藏层（每层 64 单元）的 MLP。
*   **算力表现**：论文明确提到 PLCQL 将每轮迭代的 Q 函数评估次数从 $n$ 次（智能体数量）降至 **1 次**，在 V100 上观察到了显著的墙钟时间（Wall-clock）加速。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个基准测试的 **32 个不同任务/数据集组合**上进行了测试。
*   **可靠性**：所有实验均采用 **5 个随机种子**运行，并报告了均值和标准差。
*   **消融实验**：针对“自适应学习”与“固定 $k$ 值”、以及“不确定性权重”的作用进行了专门的对比实验。
*   **充分性**：实验覆盖了连续和离散动作空间、不同规模的智能体群体以及不同质量的离线数据，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先**：PLCQL 在 66% 的任务中取得了最高分，并在 84% 的任务中优于之前的 PAR 标杆方法 SPaCQL。
*   **效率提升**：在保持甚至提升性能的同时，大幅降低了计算开销，解决了 PAR 方法在大规模智能体系统中的扩展性问题。
*   **自适应性**：实验证明，没有一个固定的替换规模 $k$ 能在所有状态下最优，PLCQL 学习到的状态相关策略能有效平衡“保守估计”与“策略改进”。

#### 7. 优点
*   **高效性**：通过采样而非枚举，极大地优化了离线 MARL 的训练速度。
*   **自适应性**：首次引入上下文老虎机来动态调节离线学习的保守程度。
*   **理论与实践结合**：不仅有严谨的误差界限证明，在复杂基准测试中也表现强劲。

#### 8. 不足与局限
*   **子集采样随机性**：虽然学习了替换的数量 $k$，但在确定 $k$ 后，具体选择哪几个智能体进行替换仍是**均匀随机采样**，未考虑智能体间的依赖关系。
*   **集成模型依赖**：算法仍依赖于 Q 函数集成来估计不确定性，当智能体数量极多时，维护 10 个 Q 网络的内存和计算开销依然存在。
*   **实现复杂度**：相比基础的离线 RL 算法，增加了一个额外的 PPO 元学习层，增加了系统实现的复杂性。

（完）
