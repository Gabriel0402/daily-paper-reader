---
title: Live Interactive Training for Video Segmentation
title_zh: 视频分割的实时交互式训练
authors: "Xinyu Yang, Haozheng Yu, Yihong Sun, Bharath Hariharan, Jennifer J. Sun"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26929v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用SAM2从人类修正中学习的交互式视频分割
tldr: "针对交互式视频分割中用户反馈利用率低、导致重复劳动的问题，本文提出了一种名为LIT（Live Interactive Training）的新型框架。该框架允许模型在推理过程中通过在线学习人类的修正反馈来实时优化。具体实现LIT-LoRA通过在用户提供修正时快速更新轻量级LoRA模块，显著减少了后续帧的错误。实验表明，该方法在多个基准测试中减少了18-34%的修正次数，且每步训练开销极低，展现了实时自适应技术在减少人工负担方面的巨大潜力。"
selection_source: fresh_fetch
motivation: 现有的交互式分割模型仅将用户修正用于即时修复，而无法从中学习，导致在处理复杂场景时需要大量重复的人工干预。
method: 提出了LIT框架及其具体实现LIT-LoRA，通过在推理阶段实时、快速地微调轻量级LoRA模块来吸收用户反馈。
result: "在挑战性的视频分割基准上，该方法将总修正次数降低了18-34%，且每次修正仅增加约0.5秒的训练开销。"
conclusion: 实时在线学习能有效提升交互式视觉工具的效率，显著减少复杂视觉任务中的冗余人工劳动。
---

## 摘要
交互式视频分割在处理挑战性场景（如遮挡、物体分离、伪装等）时，通常需要大量的用户干预以保证鲁棒性。然而，即使是像 SAM2 这样最先进的模型，也仅将修正用于即时修复，而无法从这些反馈中学习，导致用户工作效率低下且重复。为解决这一问题，我们提出了实时交互式训练（Live Interactive Training, LIT），这是一种针对基于提示（prompt-based）的视觉系统的新型框架，模型可以在推理阶段从人类修正中进行在线学习。我们的主要实例化方案 LIT-LoRA 通过实时持续更新轻量级 LoRA 模块来实现这一目标。当用户提供修正时，该模块会根据反馈进行快速训练，从而使视觉系统能够提升在同一视频后续帧上的表现。利用 LIT 的核心原理，我们的 LIT-LoRA 实现方案在挑战性的视频分割基准测试中，平均减少了 18-34% 的总修正次数，且每次修正的训练开销仅为约 0.5 秒，几乎可以忽略不计。我们通过将其成功适配到其他分割模型，并扩展到基于 CLIP 的细粒度图像分类任务，进一步证明了该方法的通用性。我们的工作突显了实时自适应在变革交互式工具以及显著减少复杂视觉任务中冗余人力投入方面的巨大潜力。项目地址：https://youngxinyu1802.github.io/projects/LIT/。

## Abstract
Interactive video segmentation often requires many user interventions for robust performance in challenging scenarios (e.g., occlusions, object separations, camouflage, etc.). Yet, even state-of-the-art models like SAM2 use corrections only for immediate fixes without learning from this feedback, leading to inefficient, repetitive user effort. To address this, we introduce Live Interactive Training (LIT), a novel framework for prompt-based visual systems where models also learn online from human corrections at inference time. Our primary instantiation, LIT-LoRA, implements this by continually updating a lightweight LoRA module on-the-fly. When a user provides a correction, this module is rapidly trained on that feedback, allowing the vision system to improve performance on subsequent frames of the same video. Leveraging the core principles of LIT, our LIT-LoRA implementation achieves an average 18-34% reduction in total corrections on challenging video segmentation benchmarks, with a negligible training overhead of ~0.5s per correction. We further demonstrate its generality by successfully adapting it to other segmentation models and extending it to CLIP-based fine-grained image classification. Our work highlights the promise of live adaptation to transform interactive tools and significantly reduce redundant human effort in complex visual tasks. Project: https://youngxinyu1802.github.io/projects/LIT/.

---

## 论文详细总结（自动生成）

### 论文总结：视频分割的实时交互式训练 (LIT)

#### 1. 核心问题与研究动机
*   **核心问题**：现有的交互式视频分割系统（如 SAM2）虽然允许用户通过点击或框选进行修正，但模型仅将这些反馈视为临时输入或缓存，而**不具备学习能力**。
*   **研究动机**：在处理遮挡、物体分离或伪装等复杂场景时，模型往往会在后续帧中重复犯同样的错误，迫使用户进行大量重复的修正劳动。
*   **整体含义**：本文旨在打破“修正-遗忘-再修正”的低效循环，提出一种让模型在推理阶段通过人类反馈进行实时在线学习的框架。

#### 2. 方法论：LIT 与 LIT-LoRA
*   **核心思想**：将交互式修正视为在线监督信号，通过参数高效微调（PEFT）技术，在推理过程中动态更新模型。
*   **关键技术细节**：
    *   **LIT 框架**：一种用户反馈驱动的在线学习变体。它将视频流视为相干组，在处理每一帧时，若用户发现错误并提供修正（如掩码或点击），该修正即作为标签。
    *   **LIT-LoRA 实现**：在冻结的分割模型（如 SAM2）的掩码解码器（Mask Decoder）中插入极轻量级的 LoRA 模块（仅约 3.5 万个参数）。
    *   **算法流程**：
        1.  **错误触发**：用户识别分割失败并提供修正掩码。
        2.  **实时更新**：利用该修正掩码，通过 Focal Loss 和 Dice Loss 对 LoRA 模块进行快速训练（约 40 个 epoch）。
        3.  **预测与验证**：更新后的 LoRA 模块应用于后续帧。如果预测满足质量阈值则接受；否则，用户再次修正，模型继续增量更新。
    *   **即插即用**：该模块不改变原模型架构，在切换新视频时重置 LoRA。

#### 3. 实验设计
*   **数据集**：在四个极具挑战性的视频目标分割（VOS）基准上进行评估：**VOST**（物体破碎/变形）、**LVOSv2**（长视频）、**MOSEv2**（复杂场景/拥挤）和 **SA-V**（大规模通用场景）。
*   **Benchmark 与对比方法**：
    *   以 **SAM2 (Large)** 作为主要基准模型。
    *   对比了原始不带学习能力的系统。
    *   验证了在其他模型（如 **DAM4SAM**, **SAMURAI**）上的适配性。
    *   扩展实验：在 **CLIP** 模型上进行细粒度图像分类任务（使用 CUB-200, Stanford Cars, SUN397 数据集）。
*   **评估协议**：采用标准的“合成用户修正”协议，通过设定 IoU 阈值（0.5 或 0.75）自动触发修正，以确保实验的可重复性。

#### 4. 资源与算力
*   **硬件环境**：实验使用了 **NVIDIA RTX Ada 6000 GPU**。
*   **训练开销**：
    *   **参数量**：LoRA 模块仅包含 **35K** 个可训练参数（约占全模型参数的 0.01%）。
    *   **耗时**：每次用户修正触发的在线训练耗时仅约 **0.5 ± 0.2 秒**。
    *   **对比**：相比于人类标注掩码所需的数十秒时间，该计算开销几乎可以忽略不计，满足实时交互需求。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个 VOS 数据集和 3 个图像分类数据集，进行了大量的定量分析。
*   **消融实验**：针对训练轮数（Epochs）、LoRA 插入位置、不同的适配策略（如全量微调 vs LoRA）以及多个 LoRA 模块的效果进行了详细对比。
*   **客观性**：除了合成用户实验，还进行了包含 **6 名志愿者**的小规模真实用户研究，验证了在真实场景下修正次数减少了 41.92%，修正时间减少了 23.04%。实验设计全面，对比基准清晰，具有较高的说服力。

#### 6. 主要结论与发现
*   **显著减负**：LIT-LoRA 在挑战性 VOS 任务中减少了 **18-34%** 的用户修正次数，并节省了约 **22%** 的总标注时间。
*   **性能提升**：在相同的修正预算下，LIT-LoRA 引导的模型在后续帧的传播精度（J&F 分数）显著高于原始模型。
*   **通用性强**：该框架不仅适用于多种视频分割模型，还能有效应用于基于文本反馈的图像分类任务（修正减少 35-43%）。

#### 7. 优点与亮点
*   **高效性**：通过极小的参数更新实现了显著的性能增益，且训练延迟极低。
*   **泛化能力**：模型能够学习到特定视频中的错误模式（如特定物体的遮挡特性），并将其推广到后续帧。
*   **非侵入性**：作为插件式模块，无需重新训练庞大的基础模型，易于集成到现有的交互式工具中。

#### 8. 不足与局限
*   **依赖用户监控**：系统目前仍需要用户主动发现错误。虽然文中探讨了利用模型预测的 IoU 作为自动触发信号，但发现其可靠性不足，存在误报或漏报。
*   **合成用户偏差**：虽然是行业标准，但合成用户修正与真实人类的交互逻辑（如点击位置的选择）可能存在差异。
*   **基础模型依赖**：该方法的有效性在一定程度上依赖于基础模型（如 SAM2 或 CLIP）本身已具备的强大特征表示能力。
*   **极端场景失效**：在物体极小、背景极度复杂或发生剧烈突发变形时，轻量级 LoRA 可能仍难以捕捉到足够的修正信息。

（完）
