---
title: "Swim2Real: VLM-Guided System Identification for Sim-to-Real Transfer"
title_zh: Swim2Real：用于仿真到现实迁移的 VLM 引导系统辨识
authors: "Kevin Qiu, Kyle Walker, Mike Y. Michelis, Marek Cygan, Josie Hughes"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20827v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: VLM引导的机器人仿真到现实迁移系统辨识
tldr: 针对软体水下机器人Sim-to-Real迁移中参数标定困难的问题，本文提出Swim2Real框架。该方法利用视觉语言模型（VLM）对比真实与仿真视频，自动优化16个物理参数，无需人工设计搜索阶段。通过回溯线搜索显著提升了优化效率，实现了高精度的系统辨识，并成功支持强化学习策略在物理机器人上的零样本迁移。
selection_source: fresh_fetch
motivation: 软体水下机器人受非线性流固耦合影响，传统手动标定方法复杂且难以处理混沌的参数空间。
method: 利用VLM对比视频差异并提出参数更新建议，结合回溯线搜索验证步长，实现16个参数的同步自动化标定。
result: "该方法将速度误差降低了43%，且训练出的强化学习策略在物理机器人上的游泳距离显著优于BayesOpt和CMA-ES。"
conclusion: VLM引导的标定能有效弥合水下机器人的仿真与现实差距，为自动化、通用的模拟器调优提供了新路径。
---

## 摘要
我们提出了 Swim2Real，这是一个利用视觉语言模型（VLM）反馈，直接从游泳视频中校准具有 16 个参数的仿生鱼模拟器的流水线，无需人工设计的搜索阶段。校准软体水下机器人具有极大的挑战性，因为非线性流固耦合使得参数空间变得极其复杂，简化的流体模型会引入持续存在的仿真与现实差距（sim-to-real gap），且受控的水下实验难以复现。该平台之前的研究需要三个手动定制的阶段来处理这种复杂性。VLM 通过对比仿真视频和真实视频来提出参数更新建议。随后，回溯线搜索（backtracking line search）验证每个步长，通过恢复那些方向正确但幅度过大的提议，将接受率从 14% 提高到 42%，提升了两倍。Swim2Real 同时校准所有 16 个参数，在所有电机频率下最紧密地匹配了真实鱼的速度（平均绝对误差 MAE = 7.4 mm/s，比次优方法低 43%），且在五次运行中均无离群种子点。训练策略生成的电机指令以 50 Hz 的频率迁移至实体鱼，完成了从游泳视频到现实世界部署的完整流程。下游强化学习（RL）策略的游泳距离比基于贝叶斯优化（BayesOpt）校准的模拟器高出 12%，比 CMA-ES 高出 90%。这些结果表明，VLM 引导的校准可以直接从视频中缩小水下机器人的仿真与现实差距，实现强化学习向物理游泳机器人的零样本（zero-shot）迁移，而无需手动进行系统辨识，这是迈向水下机器人自动化、通用模拟器调优的重要一步。

## Abstract
We present Swim2Real, a pipeline that calibrates a 16-parameter robotic fish simulator from swimming videos using vision-language model (VLM) feedback, requiring no hand-designed search stages. Calibrating soft aquatic robots is particularly challenging because nonlinear fluid-structure coupling makes the parameter landscape chaotic, simplified fluid models introduce a persistent sim-to-real gap, and controlled aquatic experiments are difficult to reproduce. Prior work on this platform required three manually tailored stages to handle this complexity. The VLM compares simulated and real videos and proposes parameter updates. A backtracking line search then validates each step size, tripling the accept rate from 14% to 42% by recovering proposals where the direction is correct but the magnitude is too large. Swim2Real calibrates all 16 parameters simultaneously, most closely matching real fish velocities across all motor frequencies (MAE = 7.4 mm/s, 43% lower than the next-best method), with zero outlier seeds across five runs. Motor commands from the trained policy transfer to the physical fish at 50 Hz, completing the pipeline from swimming video to real-world deployment. Downstream RL policies swim 12% farther than those from BayesOpt-calibrated simulators and 90% farther than CMA-ES. These results demonstrate that VLM-guided calibration can close the sim-to-real gap for aquatic robots directly from video, enabling zero-shot RL transfer to physical swimmers without manual system identification, a step toward automated, general-purpose simulator tuning for underwater robotics.

---

## 论文详细总结（自动生成）

### Swim2Real: VLM 引导的机器人鱼仿真到现实迁移系统辨识总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何自动化地校准复杂的软体水下机器人模拟器，以实现高精度的仿真到现实（Sim-to-Real）策略迁移。
*   **研究动机**：
    *   **物理复杂性**：软体水下机器人涉及非线性材料响应和复杂的流固耦合，参数空间极其混沌，传统手动调优或简化物理模型难以弥合仿真与现实的差距。
    *   **现有方法局限**：传统的系统辨识（System ID）方法（如贝叶斯优化或进化策略）通常将模拟器视为黑盒，仅依赖标量误差信号，忽略了视频中丰富的视觉和物理结构信息。
    *   **人工成本**：先前的研究通常需要多阶段的人工干预（如分步校准刚度、几何和流体参数），缺乏通用性和自动化能力。

#### 2. 方法论
Swim2Real 提出了一个结合视觉语言模型（VLM）物理推理能力与数值优化技术的两阶段流水线：
*   **核心思想**：利用 VLM（如 Gemini 1.5 Pro）作为“物理诊断师”，通过对比真实与仿生的视频差异来指导参数搜索，并引入回溯线搜索（Backtracking Line Search）来精细化调整步长。
*   **关键技术细节**：
    1.  **VLM 引导校准**：向 VLM 提供侧视图视频（包含仿真与真实的骨架叠加图）、数值数据（各频率下的误差和速度）以及参数语义说明。VLM 识别物理差异（如“尾部摆动过大”），并提出 16 个参数（流体系数、电机几何、各关节刚度与阻尼）的更新建议。
    2.  **回溯线搜索**：VLM 往往能指出正确的优化方向但难以确定精确幅度。系统通过几何递减的步长（$\beta=0.5$）验证 VLM 的提议。如果全步长失败，则尝试半步长，直到找到能降低目标函数 $L(\theta)$ 的参数。
    3.  **参数空间**：同时优化 16 维连续空间，包括 5 个流体动力学系数、1 个电机臂长度、5 个关节刚度和 5 个关节阻尼。

#### 3. 实验设计
*   **硬件平台**：全长 0.6 米、重 1.5 kg 的腱驱动仿生鱼，具有 5 个关节的柔性尾部。
*   **实验场景**：在 2m x 3m 的水池中，通过顶置摄像头追踪 9 个身体标记点，涵盖 0.5Hz 至 2.25Hz 的 8 种驱动频率。
*   **Benchmark（对比方法）**：
    *   **Bayesian Optimization (BayesOpt)**：基于高斯过程的黑盒优化。
    *   **CMA-ES**：协方差矩阵自适应进化策略。
    *   **Random Search**：随机搜索作为基准。
    *   **消融实验**：包括无回溯线搜索版本、更换 VLM 模型（Gemini 1.5 vs 1.0）以及单次 VLM 提议（Warm start）。
*   **评估指标**：L2 标记点误差、前向游泳速度 MAE、下游强化学习（RL）任务（前向游泳和目标到达）的迁移表现。

#### 4. 资源与算力
*   **计算硬件**：模拟器运行在 AMD Ryzen 7 PRO 7840U CPU（单核）上。
*   **耗时**：
    *   **Swim2Real**：平均运行时间 19 分钟（8 分钟模拟 + 11 分钟 VLM API 调用）。
    *   **对比方法**：BayesOpt 和 CMA-ES 约为 6 分钟。
*   **VLM 成本**：每个运行周期约调用 15.6 次 VLM API。文中未提及具体 GPU 型号，因为主要计算压力在于 CPU 模拟和云端 VLM 推理。

#### 5. 实验数量与充分性
*   **实验规模**：所有方法均在 5 个随机种子下运行，每个种子限制为 40 次模拟评估。
*   **充分性**：
    *   实验涵盖了从底层参数误差到高层 RL 策略迁移的全过程。
    *   通过 5 组种子的统计分布展示了方法的稳定性（Swim2Real 无离群点）。
    *   消融实验验证了回溯线搜索将提议接受率从 14% 提升至 42% 的关键作用。
    *   **客观性**：使用了“前向速度”作为非目标函数的独立测试指标，确保了校准的真实有效性。

#### 6. 主要结论与发现
*   **精度领先**：Swim2Real 的速度匹配误差（MAE = 7.4 mm/s）比次优方法（BayesOpt）低 43%。
*   **可靠性高**：在 16 维的高维搜索中，Swim2Real 在所有种子实验中均表现稳定，而 CMA-ES 在有限预算下经常崩溃。
*   **策略迁移成功**：在校准后的模拟器中训练的 RL 策略，在物理机器人上的游泳距离比 BayesOpt 提升了 12%，比 CMA-ES 提升了 90%。
*   **VLM 优势**：VLM 能够提供可解释的物理推理（如识别出高频下的“锁定”现象是由阻尼过大引起的），这是标量优化器无法做到的。

#### 7. 优点
*   **全自动化**：消除了人工设计多阶段校准流程的需要，实现了真正的端到端系统辨识。
*   **物理可解释性**：VLM 的反馈不仅是数值，还包含对机器人运动特性的定性分析。
*   **高效性**：通过回溯线搜索解决了 VLM 在定量估计上的不精确问题，显著提高了样本效率。

#### 8. 不足与局限
*   **误差下限**：由于模拟器将连续尾部离散化为 5 个刚性段，存在约 50mm 的固有建模误差下限。
*   **硬件偏差未完全消除**：在现实部署中，机器人存在由于摩擦导致的左偏航（steering bias），由于校准是在局部坐标系下进行的，这种全局运动偏差未能通过标记点匹配完全修正。
*   **依赖闭源模型**：目前主要依赖 Gemini 等商业 VLM，虽然测试了不同版本，但在开源模型上的泛化能力尚待验证。
*   **实时性限制**：VLM API 的调用增加了整体校准的时间开销。

（完）
