---
title: "VLGOR: Visual-Language Knowledge Guided Offline Reinforcement Learning for Generalizable Agents"
title_zh: VLGOR：面向可泛化智能体的视觉-语言知识引导离线强化学习
authors: "Pengsen Liu, Maosen Zeng, Nan Tang, Kaiyuan Li, Jing-Cheng Pang, Yunan Liu, Yang Yu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22892v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉语言模型引导的离线强化学习用于通用智能体
tldr: "针对大语言模型在强化学习中缺乏物理环境感知、泛化能力弱的问题，本文提出VLGOR框架。该框架通过微调视觉语言模型（VLM），结合初始视觉观察和指令生成具有时空一致性的虚拟交互数据。利用反事实提示增强数据多样性，使智能体在离线强化学习中更好地将语言指令与视觉环境对齐。实验证明，VLGOR在机器人操作任务中显著提升了对未知任务的泛化性能，成功率比基准方法高出24%以上。"
selection_source: fresh_fetch
motivation: 现有的结合LLM的强化学习智能体因缺乏对物理环境的直接感知，难以理解环境动力学并泛化到新任务。
method: 提出VLGOR框架，通过微调视觉语言模型生成基于反事实提示的虚拟轨迹，以丰富离线强化学习的训练数据。
result: "在机器人操作基准测试中，VLGOR在需要新策略的未知任务上表现优异，成功率提升超过24%。"
conclusion: 整合视觉与语言知识生成的虚拟数据能有效增强智能体的环境感知与指令遵循能力，显著提升离线强化学习的泛化性。
---

## 摘要
将大语言模型（LLMs）与强化学习（RL）相结合，使智能体能够更有效地理解语言指令以执行任务。然而，LLMs 通常缺乏对物理环境的直接感知，这限制了它们对环境动力学的理解以及泛化到未见任务的能力。为了解决这一局限性，我们提出了视觉-语言知识引导的离线强化学习（VLGOR），该框架整合了视觉和语言知识来生成想象轨迹（imaginary rollouts），从而丰富交互数据。VLGOR 的核心前提是微调一个视觉-语言模型，以初始视觉观测和高层指令为条件预测未来状态和动作，确保生成的轨迹在时间上连贯且在空间上合理。此外，我们采用反事实提示来生成更多样化的轨迹用于离线强化学习训练，使智能体能够获取有助于遵循语言指令的知识，同时基于视觉线索实现环境具身。在机器人操控基准测试上的实验表明，VLGOR 显著提升了在需要全新最优策略的未见任务上的性能，成功率比基准方法高出 24% 以上。

## Abstract
Combining Large Language Models (LLMs) with Reinforcement Learning (RL) enables agents to interpret language instructions more effectively for task execution. However, LLMs typically lack direct perception of the physical environment, which limits their understanding of environmental dynamics and their ability to generalize to unseen tasks. To address this limitation, we propose Visual-Language Knowledge-Guided Offline Reinforcement Learning (VLGOR), a framework that integrates visual and language knowledge to generate imaginary rollouts, thereby enriching the interaction data. The core premise of VLGOR is to fine-tune a vision-language model to predict future states and actions conditioned on an initial visual observation and high-level instructions, ensuring that the generated rollouts remain temporally coherent and spatially plausible. Furthermore, we employ counterfactual prompts to produce more diverse rollouts for offline RL training, enabling the agent to acquire knowledge that facilitates following language instructions while grounding in environments based on visual cues. Experiments on robotic manipulation benchmarks demonstrate that VLGOR significantly improves performance on unseen tasks requiring novel optimal policies, achieving a success rate over 24% higher than the baseline methods.

---

## 论文详细总结（自动生成）

以下是对论文《VLGOR: Visual-Language Knowledge Guided Offline Reinforcement Learning for Generalizable Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机与背景）
开发能够执行多样化任务的通用智能体是人工智能的核心目标。虽然大语言模型（LLM）具备丰富的语义知识，但将其应用于强化学习（RL）时存在显著局限：
*   **缺乏物理具身（Grounding）：** LLM 仅处理离散文本，难以理解连续的物理动力学（如位置、速度）。
*   **物理一致性缺失：** 仅依赖语言生成的虚拟轨迹往往违反物理约束（如物体穿透、运动不合理）。
*   **泛化瓶颈：** 离线强化学习受限于训练数据的覆盖范围，难以应对未见过的指令或任务变体。
**VLGOR 的核心动机**是利用视觉-语言模型（VLM）的感知能力，生成具有时空连贯性和物理合理性的虚拟交互数据，从而增强智能体在离线 RL 中的泛化能力。

### 2. 方法论：核心思想与技术细节
VLGOR 框架分为两个主要阶段：

*   **阶段一：基于反事实提示的轨迹生成（Rollout Generation）**
    *   **环境对齐微调：** 使用视觉-语言模型 **BAGEL** 作为骨干，通过 LoRA 适配器进行微调。模型输入初始视觉状态 $s_0$ 和文本指令 $G$，预测未来的视觉状态 $\hat{s}_h$ 和动作 $\hat{a}_h$。
    *   **反事实提示（Counterfactual Prompts）：** 为了增加数据多样性，通过修改原始指令的“操作目标（Target）”或“行为模式（Behavior）”生成反事实指令。例如，将“向右移动绿球”改为“向右移动蓝球”或“向左移动绿球”。
    *   **物理一致性：** 相比仅依赖语言的模型，VLM 通过视觉编码器直接感知空间结构，确保生成的轨迹符合物理规律。

*   **阶段二：技能获取（Skill Acquisition）**
    *   **数据增强：** 将原始离线数据集与 VLM 生成的虚拟轨迹合并，构建增强数据集 $D_t$。
    *   **离线 RL 训练：** 在增强数据上训练目标条件策略 $\pi(a|s, G)$。该框架具有通用性，支持多种离线 RL 算法，如行为克隆（BC）和保守 Q 学习（CQL）。

### 3. 实验设计
*   **实验场景/数据集：**
    *   **CLEVR-Robot：** 机器人臂操纵 5 个移动球体达到目标配置。
    *   **Meta-World：** 使用 Sawyer 机械臂执行开关门、按按钮、拿起物体等复杂任务。
*   **任务难度分级（测试泛化性）：**
    1.  **指令改写（Rephrasing）：** 语义相同但表达不同的指令。
    2.  **反事实任务（Counterfactual）：** 修改了目标或行为的新任务。
    3.  **困难任务（Hard）：** 与训练数据差异巨大的复杂组合任务。
*   **对比基准（Benchmarks）：**
    *   基础算法：BC、CQL、TD3、PRDC。
    *   增强方法：**KALM**（仅依赖语言模型生成轨迹的 SOTA 方法）、**COMBO**（基于模型的离线 RL）。

### 4. 资源与算力
*   **硬件设备：** 实验在一台 **NVIDIA H800 80GB PCIe GPU** 上完成。
*   **训练细节：** 
    *   VLM 微调：20 个 Epoch，Batch Size 为 16。
    *   离线 RL 训练：50 个 Epoch，使用 Adam 优化器。
    *   数据集规模：原始离线数据包含 100,000 个轨迹对，微调 VLM 使用了其中 10,000 条。

### 5. 实验数量与充分性
*   **实验规模：** 针对两个基准环境，分别在三种难度的未见任务上进行了测试。在 CLEVR-Robot 上测试了 3,200 个新目标。
*   **消融实验：** 论文详细分析了微调样本数量对轨迹质量（PSNR、SSIM、MED 指标）的影响，以及不同类型的反事实提示对成功率的贡献。
*   **客观性：** 所有实验均采用 **3 个随机种子**重复运行，并提供了标准差（误差棒），确保了结果的统计可靠性。对比实验涵盖了从纯监督学习到价值函数的多种主流离线 RL 算法。

### 6. 主要结论与发现
*   **性能大幅提升：** VLGOR 在未见任务上的成功率比基准方法高出 **24% 以上**。
*   **物理真实性：** 定性分析显示，VLGOR 生成的轨迹在物体交互和空间定位上远优于仅依赖语言的 KALM（见论文图 1 和图 5）。
*   **泛化能力：** 随着任务难度增加（从改写到困难任务），VLGOR 的优势愈发明显，证明了视觉感知对处理复杂、长程任务的关键作用。
*   **数据效率：** 即使在微调样本有限的情况下，VLM 也能通过反事实提示生成高质量的补充数据。

### 7. 优点与亮点
*   **视觉-语言深度融合：** 解决了 LLM 在物理世界中“幻觉”运动的问题，实现了更好的环境具身。
*   **反事实增强策略：** 提出了一种系统化的方法来扩展离线数据集，无需昂贵的在线交互即可覆盖更广的任务空间。
*   **算法兼容性：** VLGOR 作为一个插件式的框架，可以无缝集成到现有的各种离线 RL 算法中。

### 8. 不足与局限
*   **环境复杂度有限：** 实验主要集中在模拟环境（CLEVR, Meta-World），尚未在真实世界的复杂机器人场景中验证。
*   **计算负担：** 同时学习环境动力学（生成状态）和模仿策略（生成动作）对 VLM 的建模能力要求较高。
*   **任务范围：** 目前主要针对物体操控类任务，对于涉及更复杂逻辑推理或极长时程的任务，其生成质量仍有待观察。

（完）
