---
title: Gaze-Regularized Vision-Language-Action Models for Robotic Manipulation
title_zh: 机器人操作中基于注视正则化的视觉-语言-动作模型
authors: "Anupam Pani, Yanchao Yang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23202v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于机器人操作的注视正则化视觉-语言-动作模型
tldr: 针对视觉-语言-动作（VLA）模型在精细操作任务中缺乏主动视觉注意力的问题，本文提出一种注视正则化训练框架。该方法通过KL散度将人类注视热图转化为补丁级分布，引导模型内部注意力对齐人类视觉模式。在不改变架构和推理开销的前提下，该方法显著提升了模型性能、收敛速度和鲁棒性，并增强了决策的可解释性。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理精细操作时缺乏有效的视觉注意力分配机制，而人类注视数据蕴含了丰富的任务意图和规划信息。
method: 利用KL散度将时间聚合的人类注视热图转化为补丁级分布，以此正则化Transformer的注意力权重，使模型学习到人类的感知偏置。
result: "在多个机器人操作基准测试中实现了4-12%的性能提升，且在光照变化和传感器噪声下表现出更强的鲁棒性。"
conclusion: 引入人类感知先验能有效加速机器人学习并提升任务表现，同时通过生成与人类策略一致的注意力模式增强了系统的可解释性。
---

## 摘要
尽管视觉-语言-动作（VLA）模型取得了显著进展，但由于当前模型缺乏主动视觉注意力分配机制，机器人在处理细粒度操作任务时仍面临挑战。人类的注视自然地编码了意图、规划和执行模式，为引导机器人感知提供了强大的监督信号。我们提出了一种注视正则化训练框架，在不改变模型架构或增加推理开销的情况下，将 VLA 模型的内部注意力与人类视觉模式对齐。该方法将时间聚合的注视热图转换为补丁级（patch-level）分布，并通过 KL 散度对 Transformer 的注意力进行正则化，在保持部署效率的同时，为任务相关特征建立了归纳偏置。实验表明，将该方法集成到现有的 VLA 架构中后，在各项操作基准测试中实现了 4-12% 的性能提升。注视正则化模型能以更少的训练步数达到同等性能，并在光照变化和传感器噪声下保持鲁棒性。除了性能指标外，学习到的注意力模式还产生了反映人类策略的可解释可视化结果，增强了用户对机器人系统的信任。此外，该框架无需眼动追踪设备，可直接应用于现有数据集。这些结果表明，人类感知先验可以显著加速机器人学习，同时提升任务性能和系统可解释性。

## Abstract
Despite advances in Vision-Language-Action (VLA) models, robotic manipulation struggles with fine-grained tasks because current models lack mechanisms for active visual attention allocation. Human gaze naturally encodes intent, planning, and execution patterns -- offering a powerful supervisory signal for guiding robot perception. We introduce a gaze-regularized training framework that aligns VLA models' internal attention with human visual patterns without architectural modifications or inference-time overhead. Our method transforms temporally aggregated gaze heatmaps into patch-level distributions and regularizes the transformer's attention through KL divergence, creating an inductive bias toward task-relevant features while preserving deployment efficiency. When integrated into existing VLA architectures, our approach yields 4-12% improvements across manipulation benchmarks. The gaze-regularized models reach equivalent performance with fewer training steps and maintain robustness under lighting variations and sensor noise. Beyond performance metrics, the learned attention patterns produce interpretable visualizations that mirror human strategies, enhancing trust in robotic systems. Moreover, our framework requires no eye-tracking equipment and applies directly to existing datasets. These results demonstrate that human perceptual priors can significantly accelerate robot learning while improving both task performance and system interpretability.

---

## 论文详细总结（自动生成）

以下是对论文《Gaze-Regularized Vision-Language-Action Models for Robotic Manipulation》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在处理精细机器人操作时表现不佳，主要原因是它们属于“被动感知”。模型必须在没有引导的情况下，同时学习“看哪里”和“如何做”，这导致训练效率低、收敛慢，且在复杂环境下缺乏鲁棒性。
*   **研究动机**：人类在操作物体时，注视点（Gaze）自然地编码了任务意图、规划和执行模式。通过引入人类注视模式作为监督信号，可以将 VLA 模型从被动观察者转变为主动感知者，从而提高其对任务相关特征的关注度。

### 2. 论文提出的方法论
*   **核心思想**：提出一种**注视正则化（Gaze-Regularized）训练框架**。在训练阶段，利用人类注视先验来约束 Transformer 内部的注意力分布，使其向任务相关区域对齐，而在推理阶段则不需要任何注视输入。
*   **关键技术细节**：
    1.  **注视先验生成**：由于机器人数据集通常缺乏眼动数据，作者使用预训练的注视预测模型（如 GLC 网络）在视频剪辑上生成合成注视热图。
    2.  **时间聚合**：为了捕捉人类的预测性注视（即在动作发生前看向目标），对连续帧的热图进行加权平均聚合，生成更稳定的注视分布。
    3.  **补丁级分布转换**：将连续的热图投影到与 Transformer 视觉 Token 相同的网格上，转化为离散的概率分布。
    4.  **注意力正则化**：在训练损失函数中引入 **KL 散度（Kullback-Leibler Divergence）**。计算模型内部视觉-语言交叉注意力分布与合成注视分布之间的差异，并将其作为正则化项。
*   **公式简述**：总损失 $L_{total} = L_{action} + \lambda \cdot D_{KL}(Gaze \parallel Attention)$。其中 $\lambda$ 是调节正则化强度的超参数。

### 3. 实验设计
*   **数据集与基准（Benchmark）**：
    *   **LIBERO Suite**：包括 LIBERO-Spatial（空间位置）、LIBERO-Object（物体识别）、LIBERO-Goal（顺序推理）和 LIBERO-10（多任务泛化）。
    *   **ALOHA-Sim**：用于验证跨域泛化能力。
    *   **真实世界实验**：在 Aloha 机器人上进行抓取和放置任务。
*   **对比方法**：
    *   **Baseline**：标准的 VLA 模型（如 Pi-0 和 OpenVLA），不含注视正则化。
    *   **消融变体**：包括不同正则化强度（$\lambda$）、使用不同的注视预测模型（如 DeepGaze）、以及均匀分布的注意力。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了训练步数（如 10k, 20k, 30k 步），并指出注视正则化模型在 20k 步时即可达到基准模型 30k 步以上的性能，强调了样本效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 个 LIBERO 子集和 ALOHA 仿真环境，共计几十种不同的操作任务。
    *   进行了跨架构验证（Pi-0 和 OpenVLA），证明了方法的通用性。
    *   进行了鲁棒性测试（光照变化、传感器噪声、视角缺失、语言指令扰动）。
    *   包含真实机器人部署实验。
*   **充分性评价**：实验设计非常充分且客观。作者不仅关注成功率，还通过 Top-k 重叠度量化了注意力对齐程度，并进行了详尽的消融实验来寻找最佳的正则化系数 $\lambda$。

### 6. 论文的主要结论与发现
*   **性能提升**：注视正则化使模型在 LIBERO 各项任务中实现了 **4-12%** 的成功率提升（例如在 LIBERO-Spatial 上从 85.9% 提升至 95.5%）。
*   **收敛加速**：模型能以更少的训练步数达到更高的性能，显著提高了学习效率。
*   **鲁棒性增强**：在光照变化和相机噪声下，正则化模型表现出更强的稳定性，因为它学会了关注语义上稳定的特征（如物体边界）。
*   **可解释性**：可视化结果显示，正则化后的模型注意力更加集中在任务关键区域，这增强了人类对机器人决策的信任。

### 7. 优点
*   **无推理开销**：该方法仅在训练时使用注视数据，推理时完全“注视无关”，不增加实时部署的计算负担。
*   **架构无关（Modular）**：可以作为插件集成到任何基于 Transformer 的 VLA 模型中，无需修改模型结构。
*   **无需额外设备**：通过合成注视模型解决了缺乏真实眼动数据的问题，使其能直接应用于现有的大规模机器人数据集。

### 8. 不足与局限
*   **合成注视的偏差**：虽然验证了合成注视的有效性，但它仍是人类真实行为的近似，可能无法完全捕捉到专家操作时的微妙感知策略。
*   **超参数敏感性**：实验显示 $\lambda$ 的选择至关重要。过强的正则化（如 $\lambda=10$）会导致性能大幅下降，说明注视应作为“软引导”而非“硬约束”。
*   **任务局限性**：目前的实验主要集中在短时或中等长度的操作任务上，对于极长程、需要复杂逻辑推理的任务，注视信号的引导作用是否依然如此显著仍需进一步验证。

（完）
