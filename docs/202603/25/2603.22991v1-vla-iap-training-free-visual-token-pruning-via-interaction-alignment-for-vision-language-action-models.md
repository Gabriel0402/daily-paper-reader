---
title: "VLA-IAP: Training-Free Visual Token Pruning via Interaction Alignment for Vision-Language-Action Models"
title_zh: VLA-IAP：面向视觉-语言-动作模型的基于交互对齐的免训练视觉 Token 剪枝
authors: "Jintao Cheng, Haozhe Wang, Weibin Li, Gang Wang, Yipu Zhang, Xiaoyu Tang, Jin Wu, Xieyuanli Chen, Yunhui Liu, Wei Zhang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22991v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 视觉语言动作（VLA）模型的视觉令牌剪枝
tldr: 针对视觉-语言-动作（VLA）模型推理成本高的问题，本文提出VLA-IAP，一种无需训练的视觉Token剪枝方法。该方法通过引入几何先验保留关键物理交互区域，并结合语义-动作对齐的动态调度策略，实现了从保守到激进的剪枝过渡。在保持与原始模型相当性能的同时，显著提升了推理速度，并在多种仿真和真实机器人场景中验证了其泛化性。
selection_source: fresh_fetch
motivation: 现有的视觉剪枝方法忽略了VLA任务中连续物理交互的重要性，容易误删对操作至关重要的结构化区域。
method: 提出一种无需训练的交互对齐剪枝方法，利用几何先验保留结构锚点，并根据语义-动作一致性动态调整剪枝强度。
result: "在LIBERO基准测试中达到97.8%的成功率，实现高达1.54倍的加速，且性能与未剪枝模型相当。"
conclusion: VLA-IAP在多种架构和真实机器人平台上展现了强大的泛化能力和实用性，有效平衡了VLA模型的推理效率与任务成功率。
---

## 摘要
视觉-语言-动作（VLA）模型迅速推动了具身智能的发展，使机器人能够执行复杂的指令驱动型任务。然而，随着模型容量和视觉上下文长度的增加，VLA 系统的推理成本成为在资源受限平台上进行实际部署的主要瓶颈。现有的视觉 Token 剪枝方法主要依赖于语义显著性或简单的时序线索，忽略了连续的物理交互，而这是 VLA 任务的一个基本属性。因此，当前的方法往往会剪掉视觉上稀疏但在结构上对操作至关重要的区域，导致任务早期阶段的行为不稳定。为了克服这一问题，我们提出转向一种显式的“交互优先”范式。我们提出的免训练方法 VLA-IAP（交互对齐剪枝）引入了一种几何先验机制来保留结构锚点，并采用一种根据语义-动作对齐情况调整剪枝强度的动态调度策略。这实现了从保守到激进的过渡，确保了早期不确定阶段的鲁棒性以及交互锁定后的效率。大量实验表明，VLA-IAP 在 LIBERO 基准测试中实现了 97.8% 的成功率和 1.25 倍的加速，在保持与未剪枝骨干网络相当的性能的同时，加速比最高可达 1.54 倍。此外，该方法在多种模型架构、三个不同的仿真环境以及真实机器人平台上均表现出优越且一致的性能，验证了其强大的泛化能力和实际应用价值。项目网站：https://chengjt1999.github.io/VLA-IAP.github.io/。

## Abstract
Vision-Language-Action (VLA) models have rapidly advanced embodied intelligence, enabling robots to execute complex, instruction-driven tasks. However, as model capacity and visual context length grow, the inference cost of VLA systems becomes a major bottleneck for real-world deployment on resource-constrained platforms. Existing visual token pruning methods mainly rely on semantic saliency or simple temporal cues, overlooking the continuous physical interaction, a fundamental property of VLA tasks. Consequently, current approaches often prune visually sparse yet structurally critical regions that support manipulation, leading to unstable behavior during early task phases. To overcome this, we propose a shift toward an explicit Interaction-First paradigm. Our proposed \textbf{training-free} method, VLA-IAP (Interaction-Aligned Pruning), introduces a geometric prior mechanism to preserve structural anchors and a dynamic scheduling strategy that adapts pruning intensity based on semantic-motion alignment. This enables a conservative-to-aggressive transition, ensuring robustness during early uncertainty and efficiency once interaction is locked. Extensive experiments show that VLA-IAP achieves a \textbf{97.8\% success rate} with a \textbf{$1.25\times$ speedup} on the LIBERO benchmark, and up to \textbf{$1.54\times$ speedup} while maintaining performance \textbf{comparable to the unpruned backbone}. Moreover, the method demonstrates superior and consistent performance across multiple model architectures and three different simulation environments, as well as a real robot platform, validating its strong generalization capability and practical applicability. Our project website is: \href{https://chengjt1999.github.io/VLA-IAP.github.io/}{VLA-IAP.com}.

---

## 论文详细总结（自动生成）

### VLA-IAP：面向视觉-语言-动作模型的基于交互对齐的免训练视觉 Token 剪枝

#### 1. 论文的核心问题与整体含义
*   **研究动机**：视觉-语言-动作（VLA）模型虽然在具身智能领域表现出色，但其巨大的计算开销导致推理频率通常低于 5Hz，难以满足实时闭环控制的需求。
*   **核心问题**：现有的视觉 Token 剪枝方法多遵循“感知优先”范式，依赖语义显著性或简单时序线索。这导致模型容易误删视觉上不显著但在物理交互中至关重要的结构化区域（如透明边缘、平滑手柄），从而在任务早期阶段引发行为不稳定。
*   **整体含义**：本文提出从“感知优先”转向“交互优先”范式，通过引入物理几何先验和动态对齐策略，在不损失性能的前提下显著提升 VLA 模型的推理效率。

#### 2. 论文提出的方法论：VLA-IAP
VLA-IAP 是一种**免训练（Training-free）**的剪枝框架，核心包含三个模块：
*   **几何先验机制（Geometric Prior）**：利用 Sobel 算子提取图像的边缘梯度，将其转化为“物理感知图”。这确保了即使某些区域语义权重较低，只要具备关键的物理轮廓（结构锚点），就会被保留，从而修正了预训练模型对语义背景的偏好。
*   **语义-动作对齐模块（Semantic-Motion Alignment）**：
    *   **语义先验**：计算视觉 Token 与语言指令的互相关联。
    *   **动作先验**：通过视觉特征的**二阶时空差分**（近似加速度）来识别局部运动区域，并利用高斯平滑和形态学操作增强连通性。
*   **交互对齐动态策略**：计算语义掩码与动作掩码的交并比（IoU）。
    *   **保守模式（低 IoU）**：任务早期，仅剔除纯背景，保留大部分非背景信息以应对不确定性。
    *   **激进模式（高 IoU）**：当语义意图与物理运动对齐（交互锁定）时，大幅度剪枝，仅保留核心交互区域。

#### 3. 实验设计
*   **数据集/基准测试**：
    *   **LIBERO**：评估终身学习和泛化能力（空间、对象、目标、长程任务）。
    *   **VLABench**：包含 100 个复杂推理任务（如加调料、选扑克）。
    *   **CALVIN**：评估长程语言条件下的策略学习。
*   **对比方法**：包括 FastV、SparseVLM、DivPrune、VLA-Cache、SpecPrune-VLA、EfficientVLA 等主流剪枝方法。
*   **骨干网络**：验证了 OpenVLA-OFT、DreamVLA、$\pi_0$ 和 $\pi_{0.5}$ 多种架构。
*   **真实场景**：在单臂和双臂机器人平台上进行了简单、长程及协作任务的测试。

#### 4. 资源与算力
*   **硬件环境**：实验运行在 Linux 系统上，使用 **NVIDIA A100 GPU**。
*   **算力说明**：由于该方法是**免训练**的，因此不需要额外的训练时长。论文重点分析了推理延迟（Latency），例如在 $\pi_0$ 模型上，30% Token 保留率下推理延迟从 98.03ms 降至 65.79ms。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个大型仿真基准测试和真实机器人实验。在 VLABench 上详细测试了 10 个子任务，并进行了组件级消融实验。
*   **充分性与客观性**：
    *   对比了不同 Token 保留率（70%、50%、30%）下的表现。
    *   消融实验涵盖了时空剪枝、IoU 调度和边缘增强权重的对比，甚至对比了不同边缘检测算子（Canny, HED, BDCN, Sobel）的影响。
    *   实验跨越了多种模型架构和环境，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能与速度平衡**：在 LIBERO 上实现了 97.8% 的成功率和 1.25 倍加速；在 30% 保留率下达到 1.54 倍加速且性能不减。
*   **鲁棒性**：在极端压缩（仅保留 30% Token）时，传统方法在 VLABench 上几乎崩溃（成功率接近 0%），而 VLA-IAP 仍能保持 17.1% - 33.3% 的成功率，证明了“交互优先”范式的优越性。
*   **真实部署**：在真实机器人上实现了约 1.48 倍的加速，并略微提升了平均成功率。

#### 7. 优点
*   **无需训练**：即插即用，适配多种现有的 VLA 模型，部署成本极低。
*   **物理 grounding 强**：通过几何先验解决了纯语义剪枝导致的“目标丢失”问题。
*   **动态自适应**：IoU 门控机制允许模型在探索阶段谨慎、在执行阶段高效，符合机器人操作的逻辑。

#### 8. 不足与局限
*   **超参数依赖**：IoU 阈值、边缘权重等超参数可能需要针对不同环境进行微调。
*   **运动检测延迟**：基于二阶差分的运动先验依赖于前几帧的积累，在物体突然高速运动或相机剧烈抖动时，可能会产生瞬时偏差。
*   **应用限制**：主要针对机械臂操纵任务，对于需要全局高频视觉感知的复杂导航任务，其剪枝策略的适用性尚待验证。

（完）
