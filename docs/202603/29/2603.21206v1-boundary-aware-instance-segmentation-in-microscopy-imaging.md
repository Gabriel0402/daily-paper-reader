---
title: Boundary-Aware Instance Segmentation in Microscopy Imaging
title_zh: 显微成像中的边界感知实例分割
authors: "Thomas Mendelson, Joshua Francois, Galit Lahav, Tammy Riklin-Raviv"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21206v1"
tags: ["query:mmseg"]
score: 9.0
evidence: SAM 在显微镜实例分割中的应用
tldr: 针对显微成像中密集、重叠细胞分割的难题，本文提出了一种无需提示词的边界感知实例分割框架。该方法通过预测有符号距离函数（SDF）而非传统的二进制掩码，实现了平滑且几何一致的轮廓建模。结合学习到的 Sigmoid 映射和统一的修正豪斯多夫距离（MHD）损失函数，该框架在处理相邻实例时表现出更强的边界定位能力和鲁棒性，在多个数据集上显著提升了边界精度，超越了基于 SAM 的基础模型。
selection_source: fresh_fetch
motivation: 现有的分割基础模型在处理显微图像中密集接触或重叠的细胞实例时，往往需要大量提示词且难以准确分离边界。
method: 提出一种预测有符号距离函数（SDF）的框架，利用学习到的映射函数将 SDF 转换为概率图，并采用 MHD 损失函数进行联合优化。
result: 在公开和私有高通量显微数据集上的评估显示，该方法在边界精度和实例级分割性能上均优于最新的 SAM 相关模型。
conclusion: 该研究证明了引入边界感知几何建模能有效提升密集场景下的细胞实例分割质量，为生物医学影像分析提供了更可靠的工具。
---

## 摘要
在显微视频中准确描绘单个细胞对于研究细胞动力学至关重要，但分离接触或重叠的实例仍然是一个持续的挑战。尽管像 SAM 这样的分割基础模型提高了图像分割的可及性，但在没有大量提示的情况下，它们在密集显微场景中仍然难以分离相邻的细胞实例。我们提出了一种无需提示的边界感知实例分割框架，该框架预测有符号距离函数（SDF）而非二值掩码，从而实现了平滑且几何一致的细胞轮廓建模。通过学习到的 Sigmoid 映射将 SDF 转换为概率图，实现了清晰的边界定位和对相邻实例的鲁棒分离。训练过程由统一的修正豪斯多夫距离（MHD）损失引导，该损失集成了基于区域和基于边界的项。在公共和私有高通量显微数据集上的评估表明，与最近基于 SAM 和基础模型的方法相比，该方法在边界准确性和实例级性能方面均有所提升。源代码可在以下网址获取：https://github.com/ThomasMendelson/BAISeg.git

## Abstract
Accurate delineation of individual cells in microscopy videos is essential for studying cellular dynamics, yet separating touching or overlapping instances remains a persistent challenge. Although foundation-model for segmentation such as SAM have broadened the accessibility of image segmentation, they still struggle to separate nearby cell instances in dense microscopy scenes without extensive prompting.   We propose a prompt-free, boundary-aware instance segmentation framework that predicts signed distance functions (SDFs) instead of binary masks, enabling smooth and geometry-consistent modeling of cell contours. A learned sigmoid mapping converts SDFs into probability maps, yielding sharp boundary localization and robust separation of adjacent instances. Training is guided by a unified Modified Hausdorff Distance (MHD) loss that integrates region- and boundary-based terms.   Evaluations on both public and private high-throughput microscopy datasets demonstrate improved boundary accuracy and instance-level performance compared to recent SAM-based and foundation-model approaches. Source code is available at: https://github.com/ThomasMendelson/BAISeg.git

---

## 论文详细总结（自动生成）

这篇论文介绍了一种针对显微成像中密集细胞实例分割的新框架，重点解决了相邻或重叠细胞难以分离的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在显微镜图像中，细胞往往密集排列、相互接触甚至重叠。传统的语义分割（如 U-Net 配合交叉熵损失）难以准确区分相邻实例的边界。
*   **研究背景**：虽然 Segment Anything Model (SAM) 等基础模型在通用分割领域表现优异，但在显微场景下，它们通常需要大量的交互式提示（Prompts），且在处理极度密集的细胞群时，边界精度依然不足。
*   **研究目标**：开发一种无需提示（Prompt-free）、能够感知边界且能稳健分离接触实例的自动分割框架。

### 2. 方法论
*   **核心思想**：放弃传统的二进制掩码（Binary Mask）预测，转而预测**连续的有符号距离函数（Signed Distance Function, SDF）**。SDF 能够建模像素到边界的距离，从而提供更丰富的几何信息。
*   **关键技术细节**：
    *   **SDF 建模**：网络输出 SDF 图，内部为正值，外部为负值，边界处为零。
    *   **可学习映射**：使用带参数的 Sigmoid 函数将 SDF 转换为概率图，从而实现端到端的训练。
    *   **软边界提取**：通过公式 $B = \sigma(SDF) \cdot \sigma(-SDF)$ 提取微分化的“软边界”图。
    *   **统一损失函数 ($L_{Total}$)**：
        1.  **修正豪斯多夫距离（MHD）损失**：包含两个项（$L_{LMHD}$ 和 $L_{RMHD}$），分别从预测边界到真值边界、真值边界到预测边界进行距离优化，强制轮廓对齐。
        2.  **最小二乘误差（LSE）损失**：直接约束预测 SDF 与真值 SDF 的数值一致性。
        3.  **交叉熵（CE）损失**：确保区域级别的分类准确性。
*   **算法流程**：输入图像经过 U-Net 骨干网络，输出 SDF，通过上述复合损失函数指导训练，推理时通过 SDF 的零水平集提取实例。

### 3. 实验设计
*   **数据集**：
    *   **公开数据集**：细胞分割基准（CSB）中的四个序列（SIM+、HeLa、GOWT1、MSC），涵盖荧光、明场及模拟数据。
    *   **私有数据集**：来自 Lahav 实验室的 3D 辐照乳腺癌球状体（MCF7）切片，具有极高的细胞密度和形态复杂性。
*   **Benchmark（评价指标）**：采用 CSB 官方的 **SEG 指标**（即匹配实例间的平均 Jaccard 指数，要求交并比 IoU > 0.5）。
*   **对比方法**：
    *   标准 U-Net。
    *   SAM (ViT B) 及其变体（使用不同密度的网格提示）。
    *   $\mu$SAM（针对显微镜优化的 SAM）。
    *   Cellpose-SAM。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA V100 GPU**。
*   **训练时长**：每个数据集的训练时间约为 **45 分钟**（包含数据增强）。
*   **推理效率**：平均每张图像推理耗时 **0.77 秒**。
*   **模型规模**：基于 U-Net 骨干，拥有约 **49.7M** 个可学习参数。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同模态的数据集上进行了完整测试。
*   **消融实验**：专门针对 MHD 损失的各个组件（$L_{CE}, L_{LSE}, L_{LMHD}, L_{RMHD}$）进行了组合对比，验证了每一项对提升 SEG 分数的贡献。
*   **充分性评价**：实验设计较为充分，对比了当前最先进的基础模型（SAM 系列）和领域专用模型（Cellpose），涵盖了从模拟到真实、从 2D 到 3D 切片的多种场景，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：在所有测试数据集上，该方法均取得了**第一或第二**的最佳表现，尤其在处理密集接触的细胞（如 MCF7 和 MSC 数据集）时，显著优于 SAM 和 Cellpose。
*   **边界精度**：引入 MHD 损失和 SDF 建模能有效消除拓扑错误，使相邻细胞的边界更加清晰、分离更加彻底。
*   **无需提示的优势**：在不依赖人工提示的情况下，性能超越了需要复杂提示的基础模型。

### 7. 优点
*   **几何一致性**：通过 SDF 建模，比像素级分类更符合生物结构的物理特性。
*   **端到端优化**：将豪斯多夫距离转化为可微形式并引入损失函数，直接优化边界形状。
*   **鲁棒性**：在低对比度和高噪声的显微图像中表现稳健。

### 8. 不足与局限
*   **维度限制**：目前主要针对 2D 图像或 3D 堆栈的 2D 切片，尚未完全扩展到原生的 3D 体素分割。
*   **预处理依赖**：计算真值 SDF 需要对原始标签进行去边框等预处理，以确保实例间的清晰分离。
*   **时间序列缺失**：尚未利用显微视频中的时间相干性（Temporal Consistency），这对于细胞追踪任务至关重要。

（完）
