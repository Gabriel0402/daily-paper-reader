---
title: "SEVerA: Verified Synthesis of Self-Evolving Agents"
title_zh: SEVerA：自我演化智能体的验证合成
authors: "Debangshu Banerjee, Changming Xu, Gagandeep Singh"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25111v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 将硬性形式化规范与大语言模型代理程序合成相结合
tldr: 针对自演化LLM智能体在自主执行中缺乏安全与正确性保证的问题，本文提出SEVerA框架。该框架引入形式化保护生成模型（FGGM），通过一阶逻辑定义输出契约并结合拒绝采样与验证回退机制。SEVerA通过搜索、验证和学习三个阶段，在保证硬性形式化约束的同时，利用梯度优化提升任务效用。实验证明，SEVerA在保证零约束违反的前提下，性能优于现有基准。
selection_source: fresh_fetch
motivation: 现有的自演化智能体框架在处理未知输入时缺乏形式化的安全和正确性保障，存在可靠性与安全隐患。
method: 提出SEVerA框架，利用FGGM为LLM调用添加形式化契约，并通过搜索、形式化验证和基于梯度的学习优化智能体程序。
result: 在Dafny验证、符号数学和工具使用等任务中，SEVerA实现了零约束违反，且性能超越了无约束及最先进的基准模型。
conclusion: 形式化行为约束不仅能确保智能体的正确性，还能有效引导合成过程，从而产生更高质量的自演化智能体。
---

## 摘要
最近的进展展示了自我演化 LLM 智能体在程序修复和科学发现等任务中的有效性。在这种范式中，规划器 LLM 合成一个调用参数化模型（包括 LLM）的智能体程序，这些模型随后针对每个任务进行调整以提升性能。然而，现有的自我演化智能体框架无法提供安全或正确性的形式化保证。由于此类程序通常在未见过的输入上自主执行，缺乏保证引发了可靠性和安全性方面的担忧。我们将智能体代码生成建模为一个受约束的学习问题，将硬性形式化规范与捕捉任务效用的软性目标相结合。我们引入了形式化保护生成模型（FGGM），允许规划器 LLM 使用一阶逻辑为每个生成模型调用指定形式化的输出契约。每个 FGGM 调用都将底层模型封装在一个带有经过验证的回退机制的拒绝采样器中，确保对于任何输入和参数设置，返回的输出均满足契约。基于 FGGM，我们提出了 SEVerA（自我演化验证智能体），这是一个三阶段框架：搜索阶段合成包含 FGGM 调用的候选参数化程序；验证阶段证明所有参数值在硬约束下的正确性，从而将问题转化为无约束学习；学习阶段应用可扩展的基于梯度的优化（包括 GRPO 风格的微调），在保持正确性的同时优化软性目标。我们在 Dafny 程序验证、符号数学合成和符合策略的智能体工具使用（$τ^2$-bench）上对 SEVerA 进行了评估。在各项任务中，SEVerA 实现了零约束违规，且性能优于无约束和 SOTA 基准，表明形式化行为约束不仅能保证正确性，还能引导合成更高质量的智能体。

## Abstract
Recent advances have shown the effectiveness of self-evolving LLM agents on tasks such as program repair and scientific discovery. In this paradigm, a planner LLM synthesizes an agent program that invokes parametric models, including LLMs, which are then tuned per task to improve performance. However, existing self-evolving agent frameworks provide no formal guarantees of safety or correctness. Because such programs are often executed autonomously on unseen inputs, this lack of guarantees raises reliability and security concerns. We formulate agentic code generation as a constrained learning problem, combining hard formal specifications with soft objectives capturing task utility. We introduce Formally Guarded Generative Models (FGGM), which allow the planner LLM to specify a formal output contract for each generative model call using first-order logic. Each FGGM call wraps the underlying model in a rejection sampler with a verified fallback, ensuring every returned output satisfies the contract for any input and parameter setting. Building on FGGM, we present SEVerA (Self-Evolving Verified Agents), a three-stage framework: Search synthesizes candidate parametric programs containing FGGM calls; Verification proves correctness with respect to hard constraints for all parameter values, reducing the problem to unconstrained learning; and Learning applies scalable gradient-based optimization, including GRPO-style fine-tuning, to improve the soft objective while preserving correctness. We evaluate SEVerA on Dafny program verification, symbolic math synthesis, and policy-compliant agentic tool use ($τ^2$-bench). Across tasks, SEVerA achieves zero constraint violations while improving performance over unconstrained and SOTA baselines, showing that formal behavioral constraints not only guarantee correctness but also steer synthesis toward higher-quality agents.

---

## 论文详细总结（自动生成）

### SEVerA：自我演化智能体的验证合成——论文深度总结

#### 1. 核心问题与研究动机
随着大语言模型（LLM）在程序修复、科学发现等任务中的广泛应用，**自我演化智能体（Self-Evolving Agents）**成为一种新范式。在这种模式下，规划器 LLM 合成智能体程序，并根据任务反馈不断微调其内部参数。
*   **核心矛盾**：现有的自我演化框架仅关注“软性”性能指标（如准确率），缺乏对“硬性”安全性和正确性的形式化保证。
*   **安全隐患**：由于智能体在未见过的输入上自主执行，可能产生严重后果（如在代码修复中删除测试用例而非修复 Bug，或在工具调用中违反业务策略）。
*   **研究目标**：将智能体代码生成建模为**受约束的学习问题**，既保留形式化验证的严谨性，又利用梯度优化的灵活性。

#### 2. 方法论：SEVerA 框架
论文提出了 **SEVerA**（Self-Evolving Ver ified Agents），其核心是**形式化保护生成模型（FGGM）**。

*   **关键技术：FGGM**
    *   **局部契约**：规划器 LLM 为每个生成模型调用指定一阶逻辑（FOL）定义的输入-输出契约（$\Phi_l, \Psi_l$）。
    *   **拒绝采样与回退**：将模型输出视为提案分布，通过拒绝采样确保输出符合契约。若多次采样均失败，则触发一个**经过验证的非参数化回退程序（Fallback）**。
    *   **参数无关性**：无论底层模型参数如何变化，FGGM 始终保证输出满足契约。

*   **SEVerA 三阶段流程**
    1.  **搜索（Search）**：规划器 LLM 在验证感知语言（如 Dafny）中合成包含 FGGM 调用的候选程序。
    2.  **验证（Verify）**：利用 Dafny 内置验证器证明程序在所有可能的参数值下均满足全局行为规范。
    3.  **学习（Learn）**：在保证正确性的前提下，对验证通过的程序进行梯度优化。使用 **GRPO（群体相对策略优化）** 风格的微调，结合全局任务损失和局部一致性损失（Conformance Loss），提升模型生成符合契约输出的概率。

#### 3. 实验设计
论文在四个具有代表性的领域进行了评估：
*   **Dafny 程序验证**：合成循环不变式和断言。Benchmark 使用了 HumanEvalDafny 和 DafnyBench。
*   **符号数学合成**：从自然语言生成符合特定语法的数学表达式。使用 GSM-Symbolic 数据集。
*   **智能体工具使用**：在客户服务场景中调用 API 并遵守策略。使用 $\tau^2$-bench（零售和航空领域）。
*   **受约束符号回归**：从噪声数据中恢复符合物理/数学先验约束的公式。

**对比方法**：
*   原生 LLM（如 Claude 4.5, Qwen3-8B）。
*   SOTA 约束生成方法（如 CRANE 约束解码）。
*   特定领域的基准（如 Agent-C 用于工具调用，PySR 用于符号回归）。
*   不带约束的自我演化基准。

#### 4. 资源与算力
*   **硬件**：实验主要在 **NVIDIA A100 (40GB) GPU** 上运行。
*   **模型**：底层模型包括闭源的 Claude Sonnet 4.5（仅搜索/验证）和开源的 Qwen3-8B（进行 GRPO 微调）。
*   **训练细节**：在 GSM-Symbolic 任务中，使用了 LoRA 适配器（Rank=16），GRPO 训练 5 个 Epoch，每组 Prompt 生成 8 个样本。
*   **时长**：论文未明确给出总训练时长，但提供了推理/验证的单例耗时（约 10-25 秒/例）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 35 组符号回归任务、数百个 Dafny 验证题目以及复杂的多轮对话工具调用场景。
*   **消融实验**：专门针对“局部契约微调”与“全局损失微调”的贡献进行了消融分析（见表 7），验证了各组件的互补性。
*   **客观性**：实验对比了多种不同类型的基准（从遗传算法到最新的强化学习微调），并使用了持有测试集（Held-out test set）评估约束违反率，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **零违规保证**：SEVerA 在所有任务中均实现了 **0% 的约束违反率**，而基准方法在某些任务中违规率高达 76%。
*   **性能提升**：形式化约束不仅保证了安全，还通过缩小搜索空间引导 LLM 生成更高质量的代码。例如，在 HumanEvalDafny 上验证率达到 97.0%（基准为 86.9%）。
*   **学习有效性**：通过 GRPO 微调，模型在满足硬约束的同时，任务准确率显著提升（如 GSM-Symbolic 从 53.2% 提升至 66.0%）。

#### 7. 优点与亮点
*   **理论完备性**：提供了 SEVerA 的**健全性（Soundness）证明**，确保合成的智能体在任何输入和参数下都满足规范。
*   **架构灵活性**：FGGM 机制不依赖于模型内部概率，因此同时适用于开源和闭源模型。
*   **神经符号结合**：成功将演绎程序合成（Deductive Synthesis）与现代强化学习（RL）微调无缝集成。

#### 8. 不足与局限
*   **资源感知缺失**：目前的规范仅约束功能正确性，未考虑计算资源（如 Token 消耗、API 调用次数或运行时间）的硬性限制。
*   **验证器依赖**：高度依赖 Dafny 和底层 SMT 求解器（如 Z3）。对于某些不可判定（Undecidable）的一阶逻辑公式，验证器可能会超时或返回未知。
*   **回退程序的质量**：系统的安全性底线取决于人工或规划器提供的 Fallback 程序，如果 Fallback 过于简单，虽然保证了安全，但可能导致任务效用大幅下降。

（完）
