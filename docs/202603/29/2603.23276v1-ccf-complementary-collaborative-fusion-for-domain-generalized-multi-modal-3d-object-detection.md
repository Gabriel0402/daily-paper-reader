---
title: "CCF: Complementary Collaborative Fusion for Domain Generalized Multi-Modal 3D Object Detection"
title_zh: CCF：面向领域泛化多模态三维目标检测的互补协作融合
authors: "Yuchen Wu, Kun Wang, Yining Pan, Na Zhao"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23276v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 自动驾驶中的多模态融合3D目标检测
tldr: 针对多模态3D目标检测在跨域场景（如雨天、夜晚）性能下降及LiDAR主导导致视觉信息利用不足的问题，本文提出互补协作融合（CCF）框架。该框架通过查询解耦损失平衡模态梯度，利用LiDAR引导的深度先验增强2D查询的空间初始化，并采用互补跨模态掩码促进模态间的自适应融合。实验证明，CCF在保持源域性能的同时，显著提升了跨域泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决多模态3D检测中因环境导致的模态退化以及LiDAR分支主导导致的视觉信息利用不足和跨域鲁棒性差的问题。
method: 提出了查询解耦损失以平衡梯度、LiDAR引导的深度先验以优化空间初始化，以及互补跨模态掩码以促进模态间的竞争与协作。
result: 在广泛的实验中，该方法在多个跨域检测任务上均取得了优于现有最先进基准的性能提升。
conclusion: CCF框架通过重新平衡模态贡献和增强几何先验，有效提升了多模态3D目标检测在复杂多变环境下的泛化性能。
---

## 摘要
多模态融合已成为实现精确三维目标检测的一种极具前景的范式。然而，当部署在与训练集不同的目标领域时，性能会大幅下降。在这项工作中，我们专注于双分支候选区域级检测器，并确定了限制鲁棒跨领域泛化的两个因素：1) 在雨天或夜间等具有挑战性的领域，某种模态可能会发生严重退化；2) 激光雷达（LiDAR）分支通常在检测过程中占据主导地位，导致视觉线索被系统性地低估，且在点云受损时表现脆弱。为了应对这些挑战，我们提出了三个组件。首先，查询解耦损失（Query-Decoupled Loss）为纯二维、纯三维和融合查询提供独立监督，从而重新平衡跨模态的梯度流。其次，激光雷达引导的深度先验（LiDAR-Guided Depth Prior）通过对图像预测和激光雷达衍生的深度分布进行概率融合，为二维查询增强了实例感知的几何先验，从而改善了它们的空间初始化。第三，互补跨模态掩码（Complementary Cross-Modal Masking）对图像和点云应用互补的空间掩码，鼓励来自两种模态的查询在融合解码器中进行竞争，从而促进自适应融合。大量实验表明，该方法在保持源域性能的同时，相比现有最先进的基准模型取得了显著提升。代码和模型已在 https://github.com/IMPL-Lab/CCF 公开。

## Abstract
Multi-modal fusion has emerged as a promising paradigm for accurate 3D object detection. However, performance degrades substantially when deployed in target domains different from training. In this work, focusing on dual-branch proposal-level detectors, we identify two factors that limit robust cross-domain generalization: 1) in challenging domains such as rain or nighttime, one modality may undergo severe degradation; 2) the LiDAR branch often dominates the detection process, leading to systematic underutilization of visual cues and vulnerability when point clouds are compromised. To address these challenges, we propose three components. First, Query-Decoupled Loss provides independent supervision for 2D-only, 3D-only, and fused queries, rebalancing gradient flow across modalities. Second, LiDAR-Guided Depth Prior augments 2D queries with instance-aware geometric priors through probabilistic fusion of image-predicted and LiDAR-derived depth distributions, improving their spatial initialization. Third, Complementary Cross-Modal Masking applies complementary spatial masks to the image and point cloud, encouraging queries from both modalities to compete within the fused decoder and thereby promoting adaptive fusion. Extensive experiments demonstrate substantial gains over state-of-the-art baselines while preserving source-domain performance. Code and models are publicly available at https://github.com/IMPL-Lab/CCF.

---

## 论文详细总结（自动生成）

### 论文总结：CCF（互补协作融合）

#### 1. 核心问题与研究动机
*   **核心问题**：多模态（LiDAR + 图像）3D目标检测在跨领域场景（如雨天、夜晚、不同城市）中性能显著下降。
*   **研究背景**：
    *   **模态退化**：恶劣天气（雨天）导致LiDAR点云稀疏，低光照（夜晚）导致图像质量下降。
    *   **模态不平衡**：在现有的双分支检测器中，LiDAR分支占据主导地位。作者通过实验发现，训练过程中3D查询（Query）获得匹配监督的机会是2D查询的37.5倍，导致视觉信息被严重低估，且模型在LiDAR受损时表现脆弱。
*   **整体含义**：本文旨在通过重新平衡模态间的监督、初始化和融合过程，提升多模态3D检测器的跨域鲁棒性。

#### 2. 方法论
提出了**互补协作融合（CCF）**框架，包含三个核心组件：
*   **查询解耦损失（Query-Decoupled Loss, QDL）**：
    *   **核心思想**：解决监督不平衡问题。
    *   **技术细节**：在训练时执行三次并行的解码器传递（权重共享）：纯2D查询路径、纯3D查询路径和融合查询路径。每个路径独立进行匈牙利匹配并计算损失。这确保了2D查询能获得独立的梯度流，而不被更精确的3D查询“掩盖”。
*   **LiDAR引导的深度先验（LiDAR-Guided Depth Prior, LGDP）**：
    *   **核心思想**：利用LiDAR的几何信息辅助2D查询的深度初始化。
    *   **技术细节**：将图像预测的深度分布与LiDAR点云生成的深度直方图进行概率融合。引入一个轻量化置信度网络预测融合权重 $\lambda$，通过对数空间的“专家乘积”（Product-of-Experts）方式生成最终深度分布，从而提高2D查询在3D空间中的初始定位精度。
*   **互补跨模态掩码（Complementary Cross-Modal Masking, CCMM）**：
    *   **核心思想**：模拟现实中的非对称模态退化，强制模型进行自适应融合。
    *   **技术细节**：对图像和点云应用空间互补的掩码（如GridMask）。若图像某区域被遮蔽，则保留对应的LiDAR点；反之亦然。这种设计迫使模型在部分观测下让两种模态的查询在解码器中竞争，减少对单一模态的过度依赖。

#### 3. 实验设计
*   **数据集**：基于 **nuScenes** 构建跨域基准。
    *   **源域（Source）**：新加坡晴天场景（226个场景）。
    *   **目标域（Target）**：雨天（Rain）、夜晚（Night）、波士顿城市（Boston）。
*   **Benchmark与对比方法**：
    *   对比了多种最先进（SOTA）方法，包括 CMT、ISFusion、MoME、BEVFusion、TransFusion 等。
    *   包含 LiDAR-only 方法（如 FSDv2）作为参考。
*   **评价指标**：mAP（平均精度）和 NDS（nuScenes检测得分）。

#### 4. 资源与算力
*   **训练参数**：使用 AdamW 优化器，初始学习率 $4 \times 10^{-4}$，权重衰减 0.01，采用余弦退火策略。
*   **训练规模**：Batch size 为 16，训练 24 个 epoch。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号和数量，但根据其模型规模（基于 MV2DFusion 和 ISFusion），通常需要 4-8 张 A100 或同级别 GPU。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在三个目标域（雨天、夜晚、波士顿）上进行了全面的性能对比。
    *   **Oracle 实验**：对比了在全量数据训练下的性能上限。
    *   **消融实验**：详细验证了 QDL、LGDP、CCMM 三个组件的有效性，并探讨了不同掩码策略（如随机掩码、模态丢弃等）的影响。
    *   **定性分析**：提供了可视化检测结果对比。
*   **充分性评价**：实验设计合理，通过“源域训练-目标域测试”的设置严格遵循了领域泛化（DG）的标准协议。消融实验深入到了组件设计的细节，逻辑严密，结果具有说服力。

#### 6. 主要结论与发现
*   **2D查询潜力巨大**：即使在跨域场景下，2D候选框的质量依然很高，但由于深度估计不准和监督不足，其潜力未被挖掘。
*   **CCF 显著提升鲁棒性**：CCF 在 Rain (+2.8% mAP)、Night (+1.3% mAP) 和 Boston (+3.2% mAP) 领域均取得了显著提升，且未牺牲源域性能。
*   **互补掩码优于模态丢弃**：相比于直接丢弃整个模态，空间上的互补掩码更能有效训练模型在复杂环境下的自适应融合能力。

#### 7. 优点
*   **洞察深刻**：定量分析了训练过程中查询匹配的不平衡率（37.5:1），为多模态融合研究提供了新的视角。
*   **即插即用**：提出的组件（如 QDL 和 CCMM）具有较强的通用性，可扩展至其他双分支检测框架。
*   **推理友好**：QDL 仅在训练阶段增加计算开销，推理时只需执行融合路径，不增加额外延迟。

#### 8. 不足与局限
*   **数据集单一**：实验主要集中在 nuScenes 内部的域偏移，未在 Waymo 或 Argoverse 等其他大型数据集上验证跨数据集的泛化性。
*   **极端天气模拟**：虽然考虑了雨天和夜晚，但对于大雾、降雪等更极端的传感器失效场景（如镜头遮挡）探讨较少。
*   **计算开销**：虽然推理不增加负担，但训练时三次解码器传递会显著增加显存占用和训练时间。

（完）
