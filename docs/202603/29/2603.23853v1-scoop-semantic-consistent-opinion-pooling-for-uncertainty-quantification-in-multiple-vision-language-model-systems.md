---
title: "SCoOP: Semantic Consistent Opinion Pooling for Uncertainty Quantification in Multiple Vision-Language Model Systems"
title_zh: SCoOP：多视觉语言模型系统中不确定性量化的语义一致性意见池化
authors: "Chung-En Johnny Yu, Brian Jalaian, Nathaniel D. Bastian"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23853v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多视觉语言模型系统中的不确定性量化
tldr: 针对多视觉语言模型（VLM）集成中存在的不确定性和幻觉风险，本文提出SCoOP框架。该框架采用无需训练的语义一致性意见池化方法，通过不确定性加权线性组合来量化系统级不确定性。实验表明，SCoOP在ScienceQA数据集上的幻觉检测和拒绝回答性能显著优于现有基准，且计算开销极低，有效提升了多模态AI系统的可靠性。
selection_source: fresh_fetch
motivation: 多个异构VLM的集成虽然增强了推理能力，但也放大了输出的不确定性并增加了幻觉风险。
method: 提出一种无需训练的SCoOP框架，利用不确定性加权的线性意见池化来衡量多模型系统层面的语义一致性。
result: 在ScienceQA任务中，SCoOP的幻觉检测AUROC达到0.866，拒绝回答AURAC达到0.907，显著超越了现有基准方法。
conclusion: SCoOP为多VLM系统提供了一种高效且原则性的不确定性感知聚合机制，在几乎不增加推理延迟的情况下显著提升了系统可靠性。
---

## 摘要
结合多个视觉语言模型（VLM）可以增强多模态推理能力和鲁棒性，但聚合异构模型的输出会放大不确定性并增加幻觉风险。我们提出了 SCoOP（语义一致性意见池化），这是一种通过不确定性加权线性意见池化实现的多 VLM 系统免训练不确定性量化（UQ）框架。与以往针对单一模型设计的不确定性量化方法不同，SCoOP 显式地衡量了跨多个 VLM 的集体、系统级不确定性，从而能够对高度不确定的样本进行有效的幻觉检测和弃权。在 ScienceQA 数据集上，SCoOP 在幻觉检测方面实现了 0.866 的 AUROC，比基准方法（0.732-0.757）高出约 10-13%。在弃权方面，其 AURAC 达到 0.907，超过基准方法（0.818-0.840）7-9%。尽管取得了这些提升，SCoOP 相对于基准方法仅引入了微秒级的聚合开销，这与典型的 VLM 推理时间（秒级）相比微不足道。这些结果表明，SCoOP 为不确定性感知聚合提供了一种高效且有原则的机制，提升了多模态人工智能系统的可靠性。

## Abstract
Combining multiple Vision-Language Models (VLMs) can enhance multimodal reasoning and robustness, but aggregating heterogeneous models' outputs amplifies uncertainty and increases the risk of hallucinations. We propose SCoOP (Semantic-Consistent Opinion Pooling), a training-free uncertainty quantification (UQ) framework multi-VLM systems through uncertainty-weighted linear opinion pooling. Unlike prior UQ methods designed for single models, SCoOP explicitly measures collective, system-level uncertainty across multiple VLMs, enabling effective hallucination detection and abstention for highly uncertain samples. On ScienceQA, SCoOP achieves an AUROC of 0.866 for hallucination detection, outperforming baselines (0.732-0.757) by approximately 10-13%. For abstention, it attains an AURAC of 0.907, exceeding baselines (0.818-0.840) by 7-9%. Despite these gains, SCoOP introduces only microsecond-level aggregation overhead relative to the baselines, which is trivial compared to typical VLM inference time (on the order of seconds). These results demonstrate that SCoOP provides an efficient and principled mechanism for uncertainty-aware aggregation, advancing the reliability of multimodal AI systems.

---

## 论文详细总结（自动生成）

### 论文总结：SCoOP —— 多视觉语言模型系统中的语义一致性意见池化

#### 1. 核心问题与研究动机
*   **研究背景**：视觉语言模型（VLM）在多模态推理中表现出色，但“幻觉”（生成与图像不符的信息）仍是其在医疗、机器人等安全敏感领域应用的主要障碍。
*   **核心问题**：虽然集成多个异构 VLM 可以增强鲁棒性，但简单的聚合方法会放大单个模型的不确定性，甚至传播幻觉。目前缺乏一种能够显式衡量“系统级”不确定性且无需额外训练的方法。
*   **研究动机**：开发一种轻量级、无需训练的框架，通过量化多模型系统的不确定性来检测幻觉，并允许系统在不确定性过高时“弃权”（Abstention），从而提升 AI 系统的可靠性。

#### 2. 方法论：SCoOP 框架
SCoOP（Semantic-Consistent Opinion Pooling）的核心思想是将多个 VLM 视为提供概率意见的专家，通过不确定性加权的线性意见池化进行决策。
*   **单模型意见提取**：
    1.  对每个模型 $M_k$ 进行 $N$ 次采样（在一定温度 $T$ 下）。
    2.  使用正则表达式和语义聚类将原始文本输出映射到统一的选择题选项空间 $\Theta$。
    3.  计算每个模型选择各选项的经验概率分布 $p_k$。
*   **不确定性加权**：
    1.  计算每个模型的香农熵（Shannon Entropy）$H_k$ 作为其不确定性度量。
    2.  定义模型权重 $w_k$ 与其熵成反比（即置信度越高，权重越大），并进行归一化。
*   **系统级聚合**：
    1.  **线性意见池化（LOP）**：通过加权平均得到系统级的概率分布 $p_{agg} = \sum w_k p_k$。
    2.  **最终决策**：选择 $p_{agg}$ 中概率最高的选项作为系统输出。
    3.  **系统不确定性**：计算 $p_{agg}$ 的熵 $H_{agg}$，作为衡量整个系统是否产生幻觉的指标。

#### 3. 实验设计
*   **数据集**：
    *   **ScienceQA**：科学多模态推理。
    *   **MMMU**：大学水平的多学科多模态理解。
    *   **MMBench**：真实世界的视觉感知与推理。
*   **评估指标**：
    *   **AUROC**：衡量不确定性分数区分幻觉（错误回答）与正确回答的能力。
    *   **AURAC**：衡量通过拒绝高不确定性样本来提升系统准确率的效果。
    *   **E2E-Latency**：端到端延迟。
*   **对比基准（Baselines）**：
    *   **Naive Selection**：简单选择系统中不确定性最低的单个模型的输出。
    *   **Majority Voting**：多数投票法，通过模型间的一致性来估计不确定性。

#### 4. 资源与算力
*   **硬件环境**：使用了 **1 台 NVIDIA Blackwell B200 GPU (180 GB 显存)** 和 4 台 Intel Xeon Platinum 8570 CPU。
*   **模型规模**：涵盖了从 2B 到 78B 参数不等的 16 个开源 VLM（包括 LLaVA-v1.6, Gemma-3, InternVL3, DeepSeek-VL2, Qwen2.5-VL）。
*   **训练时长**：由于该方法是 **Training-free（无需训练）** 的，因此不存在训练时长，主要开销在于推理采样。

#### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 **52 种独特的多 VLM 系统组合**，系统规模 $K$ 从 2 到 5 不等。
*   **充分性**：
    *   进行了跨模型参数规模（Small 到 Extra Large）的对比。
    *   进行了消融实验，探讨了系统大小（模型数量）对性能的影响。
    *   在低准确率（<50%）的极端挑战条件下测试了鲁棒性。
*   **客观性**：实验覆盖了多种主流模型架构和多个权威榜单，对比基准设置合理，结果具有统计意义。

#### 6. 主要结论与发现
*   **性能显著提升**：在 ScienceQA 上，SCoOP 的幻觉检测 AUROC 达到 0.866，比基准高出 **10-13%**；弃权性能 AURAC 达到 0.907，高出 **7-9%**。
*   **效率极高**：SCoOP 引入的聚合开销仅为 **微秒级（$\mu s$）**，相对于 VLM 秒级的推理时间几乎可以忽略不计。
*   **规模效应**：SCoOP 在不同参数规模和不同数量的模型组合下均表现稳定，且优于单一模型的平均水平。
*   **鲁棒性**：即使在所有个体模型表现都很差的挑战性任务中，SCoOP 依然能提供有效的不确定性信号。

#### 7. 优点
*   **无需训练（Training-free）**：不需要标注数据或微调，易于部署到现有的多模型系统中。
*   **即插即用**：支持异构模型的集成，通过统一选项空间解决了不同模型输出格式不一的问题。
*   **高性能与低开销的平衡**：在显著提升幻觉检测能力的同时，保持了极高的计算效率。
*   **理论严谨**：基于经典的线性意见池化理论，并引入不确定性加权，逻辑清晰。

#### 8. 不足与局限
*   **任务限制**：目前主要针对 **选择题（MCQ）** 形式的任务，对于自由格式的开放式问答（Free-form VQA）支持尚不明确。
*   **采样开销**：虽然聚合过程很快，但为了估计不确定性，每个模型需要多次采样（如 $N=10$），这会成倍增加总体的推理延迟。
*   **依赖选项空间**：方法依赖于将输出映射到预定义的选项集，对于没有固定选项的任务需要额外的语义聚类步骤。

（完）
