---
title: "DriveTok: 3D Driving Scene Tokenization for Unified Multi-View Reconstruction and Understanding"
title_zh: DriveTok：面向统一多视图重建与理解的 3D 驾驶场景 Token 化
authors: "Dong Zhuo, Wenzhao Zheng, Sicheng Zuo, Siming Yan, Lu Hou, Jie Zhou, Jiwen Lu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19219v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 用于视觉-语言-动作模型的3D驾驶场景标记化
tldr: DriveTok 是一种针对自动驾驶场景的高效 3D 场景分词器，旨在解决现有 2D 分词器在处理高分辨率多视图场景时效率低且视图不一致的问题。它通过 3D 可变形交叉注意力机制将视觉特征转化为统一的场景 Token，并支持多视图重建（RGB、深度、语义）及 3D 语义占用预测。在 nuScenes 数据集上的实验证明，该方法在重建和理解任务中均表现优异，为自动驾驶视觉模态提供了高效的接口。
selection_source: fresh_fetch
motivation: 现有的图像分词器多针对单目 2D 场景，在处理多视图自动驾驶场景时存在效率低下和视图不一致的问题。
method: 利用视觉基础模型提取特征，通过 3D 可变形交叉注意力生成场景 Token，并结合多视图 Transformer 和 3D 预测头实现多任务重建与理解。
result: 在 nuScenes 数据集上，DriveTok 在图像重建、语义分割、深度预测和 3D 占用预测等多个任务中均取得了良好的性能。
conclusion: DriveTok 成功实现了集语义、几何和纹理信息于一体的统一 3D 场景分词，为自动驾驶中的多视图重建与理解提供了高效方案。
---

## 摘要
随着视觉-语言-动作模型和世界模型在自动驾驶系统中的日益普及，可扩展的图像 Token 化作为视觉模态的接口变得至关重要。然而，大多数现有的 Token 器是针对单目和 2D 场景设计的，在应用于高分辨率多视图驾驶场景时，会导致效率低下和视图间的不一致性。为解决这一问题，我们提出了 DriveTok，这是一种用于统一多视图重建与理解的高效 3D 驾驶场景 Token 器。DriveTok 首先从视觉基础模型中获取语义丰富的视觉特征，然后通过 3D 可变形交叉注意力机制将其转换为场景 Token。在解码方面，我们采用多视图 Transformer 从场景 Token 中重建多视图特征，并使用多个头部来获取 RGB、深度和语义重建结果。我们还在场景 Token 上直接添加了一个 3D 头部，用于 3D 语义占据预测，以获得更好的空间感知能力。通过多个训练目标，DriveTok 学习到了整合语义、几何和纹理信息的统一场景 Token，从而实现高效的多视图 Token 化。在广泛使用的 nuScenes 数据集上进行的大量实验表明，DriveTok 生成的场景 Token 在图像重建、语义分割、深度预测和 3D 占据预测任务中表现出色。

## Abstract
With the growing adoption of vision-language-action models and world models in autonomous driving systems, scalable image tokenization becomes crucial as the interface for the visual modality. However, most existing tokenizers are designed for monocular and 2D scenes, leading to inefficiency and inter-view inconsistency when applied to high-resolution multi-view driving scenes. To address this, we propose DriveTok, an efficient 3D driving scene tokenizer for unified multi-view reconstruction and understanding. DriveTok first obtains semantically rich visual features from vision foundation models and then transforms them into the scene tokens with 3D deformable cross-attention. For decoding, we employ a multi-view transformer to reconstruct multi-view features from the scene tokens and use multiple heads to obtain RGB, depth, and semantic reconstructions. We also add a 3D head directly on the scene tokens for 3D semantic occupancy prediction for better spatial awareness. With the multiple training objectives, DriveTok learns unified scene tokens that integrate semantic, geometric, and textural information for efficient multi-view tokenization. Extensive experiments on the widely used nuScenes dataset demonstrate that the scene tokens from DriveTok perform well on image reconstruction, semantic segmentation, depth prediction, and 3D occupancy prediction tasks.

---

## 论文详细总结（自动生成）

这篇论文提出了 **DriveTok**，一种专为自动驾驶设计的 3D 场景分词器（Tokenizer），旨在为视觉-语言-动作模型（VLA）和世界模型提供一个高效、统一且具备空间感知能力的视觉接口。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：自动驾驶正从单纯的感知转向基于推理的架构（如 VLA 和世界模型）。这些模型需要将传感器输入转化为既包含低级纹理（用于重建）又包含高级语义（用于理解）的表征。
*   **核心问题**：现有的图像分词器（如 VQ-VAE/GAN）多为 2D 单目设计。在处理自动驾驶的多视图高分辨率输入时，存在以下缺陷：
    1.  **效率低下**：逐图像分词会导致 Token 数量随摄像头数量和分辨率线性增长。
    2.  **视图不一致**：缺乏 3D 空间约束，导致不同视角间的表征无法对齐。
    3.  **缺乏空间感知**：难以捕捉驾驶场景中至关重要的 3D 几何结构。

### 2. 方法论
DriveTok 的核心思想是将多视图图像压缩为一组固定的、位于 3D 空间中的**统一场景 Token**。
*   **3D 场景编码器**：
    *   利用预训练的视觉基础模型（如 DINOv3）提取语义特征。
    *   使用 **3D 可变形交叉注意力（3D Deformable Cross-Attention）** 机制，根据相机内外参将图像特征采样并投影到统一的 3D 场景网格（如 BEV 网格）中，生成与分辨率和摄像头数量无关的场景 Token。
*   **空间感知多视图解码器**：
    *   采用 **可见性引导的注意力（Visibility-Guided Attention）**：通过预计算的可见性掩码，确保场景 Token 仅与能看到该区域的摄像头视图 Token 进行交互，避免无效计算和纹理过拟合。
    *   引入 **Plücker 射线嵌入**：为视图 Token 加入几何先验，增强跨视图消歧能力。
*   **统一重建与理解任务**：
    *   通过联合训练多个任务来丰富 Token 的信息量：**2D 任务**（RGB 重建、深度预测、语义预测）和 **3D 任务**（语义占用预测/Occupancy Prediction）。
    *   **语义正则化**：在潜空间直接对场景 Token 进行语义监督，防止结构损坏。

### 3. 实验设计
*   **数据集**：使用主流的 **nuScenes** 自动驾驶数据集（包含 6 路环视摄像头）。
*   **对比基准（Benchmarks）**：
    *   **图像重建**：对比 VQGAN、ViT-VQGAN、FlowMo、BEV-VAE。
    *   **深度预测**：对比单目模型（UniDepthV2、Metric3D-V2）和多视图模型（SurroundDepth、SelfOcc）。
    *   **3D 占用预测**：对比 BEVFormer、TPVFormer、OccFormer、SurroundOcc、GaussianFormer 等。
*   **评估指标**：PSNR、SSIM（重建）；AbsRel、$\delta < 1.25$（深度）；mIoU（占用预测）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A800 (80GB) GPU**。
*   **训练细节**：训练时长约为 **400k 次迭代**。
*   **优化技术**：启用了 FlashAttention-2 和 BFloat16 精度以降低显存消耗并加速训练。
*   **参数量**：模型总参数量约为 **280M**。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了四大类任务（重建、深度、语义、占用预测）的定量对比，并提供了大量的定性可视化结果（包括夜间等挑战性场景）。
*   **消融实验**：针对“可见性引导注意力”的作用、不同任务组合对 Token 质量的影响、以及不同骨干网络（DINOv3 vs ResNet-101）的性能差异进行了详细实验。
*   **充分性评价**：实验设计较为全面，不仅验证了性能，还分析了推理延迟和显存占用，证明了该方法在保持高性能的同时具备较好的效率。

### 6. 主要结论与发现
*   **统一表征的有效性**：DriveTok 能够将多视图输入压缩为紧凑的场景 Token，同时保留精细的纹理、准确的几何结构和丰富的语义。
*   **多任务协同**：加入 3D 占用预测和深度预测任务虽然会轻微降低图像重建的 PSNR，但能显著提升 Token 的空间推理能力。
*   **性能领先**：在 nuScenes 上，DriveTok 在深度预测和 3D 语义占用预测方面达到了 SOTA 或同等水平，且在图像重建上优于专门的 2D 分词器。

### 7. 优点与亮点
*   **架构解耦**：Token 数量固定，不随摄像头增减或分辨率变化而改变，非常适合作为大模型的视觉前端。
*   **几何一致性**：通过 3D 空间投影和可见性掩码，从底层架构上保证了多视图的几何一致性。
*   **语义丰富**：利用视觉基础模型（DINOv3）作为编码器，使 Token 具备极强的语义泛化能力。

### 8. 不足与局限
*   **计算开销**：虽然 Token 数量固定，但解码阶段涉及多视图 Transformer 和多个任务头，总推理延迟（约 671ms）对于实时闭环控制可能仍有优化空间。
*   **伪标签依赖**：深度预测和部分语义监督依赖于预训练模型（如 MoGe-2）生成的伪标签，其上限受限于这些基础模型的质量。
*   **空间范围限制**：目前的场景 Token 局限于固定的 BEV 范围（如 51.2m），对于超远距离的感知能力尚未深入探讨。

（完）
