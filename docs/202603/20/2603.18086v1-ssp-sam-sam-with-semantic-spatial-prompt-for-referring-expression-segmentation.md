---
title: "SSP-SAM: SAM with Semantic-Spatial Prompt for Referring Expression Segmentation"
title_zh: SSP-SAM：结合语义-空间提示的 SAM 用于指代性表达分割
authors: "Wei Tang, Xuejing Liu, Yanpeng Sun, Zechao Li"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18086v1"
tags: ["query:mmseg"]
score: 10.0
evidence: 将 SAM 与语义空间提示集成，用于语言引导的分割
tldr: 针对Segment Anything Model (SAM)在理解自然语言方面的局限性，本文提出SSP-SAM框架。该框架通过引入语义-空间提示（SSP）编码器，结合视觉和语言注意力适配器来提取关键特征，为SAM生成高质量的引导提示。SSP-SAM不仅在指代分割（RES）任务中表现出色，还能自然支持广义指代分割（GRES），在多个基准测试中实现了高精度分割和优秀的开集词汇泛化能力。
selection_source: fresh_fetch
motivation: SAM虽然具备强大的通用分割能力，但由于缺乏对自然语言的深度理解，难以直接应用于指代分割任务。
method: 提出SSP-SAM框架，利用语义-空间提示编码器中的双重注意力适配器来增强视觉和语言特征的融合，生成精确的引导提示。
result: "在RES和GRES基准测试中均取得领先性能，特别是在高精度阈值Pr@0.9下表现优异，并展现了强大的开集词汇处理能力。"
conclusion: SSP-SAM通过有效的多模态提示机制充分释放了SAM的潜力，为指代分割任务提供了一个高效且灵活的解决方案。
---

## 摘要
Segment Anything Model (SAM) 在通用图像分割方面表现卓越，但在理解自然语言方面的能力有限，这限制了其在指代性表达分割 (RES) 中的直接应用。为此，我们提出了 SSP-SAM，这是一个通过集成语义-空间提示 (SSP) 编码器来充分利用 SAM 分割能力的框架。具体而言，我们在 SSP 编码器中融入了视觉和语言注意力适配器，用以突出视觉特征中的显著对象和语言特征中的判别性短语。这种设计增强了提示生成器的指代目标表示，从而产生高质量的 SSP，使 SAM 能够在语言引导下生成精确的掩码。尽管 SSP-SAM 并非专门为广义指代性表达分割 (GRES) 设计（在 GRES 中，指代目标可能对应零个、一个或多个物体），但它无需额外修改即可自然地支持这种更灵活的设置。在广泛使用的 RES 和 GRES 基准测试上的大量实验证实了我们方法的优越性。值得注意的是，我们的方法生成了高质量的分割掩码，即使在 Pr@0.9 等严格阈值下也能达到极高的精度。在 PhraseCut 数据集上的进一步评估表明，与现有的最先进 RES 方法相比，该方法在开放词汇场景中表现出更优的性能。代码和检查点可在以下网址获取：https://github.com/WayneTomas/SSP-SAM。

## Abstract
The Segment Anything Model (SAM) excels at general image segmentation but has limited ability to understand natural language, which restricts its direct application in Referring Expression Segmentation (RES). Toward this end, we propose SSP-SAM, a framework that fully utilizes SAM's segmentation capabilities by integrating a Semantic-Spatial Prompt (SSP) encoder. Specifically, we incorporate both visual and linguistic attention adapters into the SSP encoder, which highlight salient objects within the visual features and discriminative phrases within the linguistic features. This design enhances the referent representation for the prompt generator, resulting in high-quality SSPs that enable SAM to generate precise masks guided by language. Although not specifically designed for Generalized RES (GRES), where the referent may correspond to zero, one, or multiple objects, SSP-SAM naturally supports this more flexible setting without additional modifications. Extensive experiments on widely used RES and GRES benchmarks confirm the superiority of our method. Notably, our approach generates segmentation masks of high quality, achieving strong precision even at strict thresholds such as Pr@0.9. Further evaluation on the PhraseCut dataset demonstrates improved performance in open-vocabulary scenarios compared to existing state-of-the-art RES methods. The code and checkpoints are available at: https://github.com/WayneTomas/SSP-SAM.

---

## 论文详细总结（自动生成）

### 论文结构化总结：SSP-SAM

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：Segment Anything Model (SAM) 虽然在通用图像分割上表现卓越，但其设计初衷是基于几何提示（点、框、掩码），缺乏对自然语言（尤其是自由格式文本）的深度理解能力。
*   **研究背景**：指代性表达分割 (RES) 要求模型根据文本描述定位并分割特定目标。现有结合 SAM 的方法多为两阶段（先检测后分割或先生成候选再重排），存在语言理解与掩码预测解耦、计算开销大或依赖复杂多模态大模型 (MLLM) 等问题。
*   **研究动机**：开发一种轻量级、单阶段的框架，通过将语言特征转化为 SAM 可理解的“语义-空间提示 (SSP)”，充分释放 SAM 的分割潜力，并使其能自然扩展到广义指代分割 (GRES) 场景。

#### 2. 方法论：核心思想与关键技术
SSP-SAM 的核心是 **语义-空间提示 (SSP) 编码器**，其流程如下：
*   **注意力适配器 (Attention Adapter)**：
    *   **视觉注意力适配器**：利用 CLIP 图像特征，先进行句子级过滤，再通过词级交叉注意力生成显著性图，并应用高斯映射增强目标物体特征，抑制背景。
    *   **语言注意力适配器**：使用 Bi-GRU 处理文本，动态调整单词权重，突出具有判别性的短语（如“左边”、“更大的”等属性或关系词）。
*   **特征融合**：通过门控融合机制（Gated Fusion），将增强后的视觉和语言特征结合，生成兼具语义和空间信息的特征表示。
*   **提示生成器 (Prompt Generator)**：引入一组可学习的特殊 Token（RES Tokens），通过 Transformer 编码器提取指代目标的特征，最终作为 SSP 输入 SAM 的掩码解码器。
*   **辅助任务 (Auxiliary REC)**：在训练中引入指代性表达理解 (REC) 任务（预测边界框），利用现有的框标注数据进行预训练，提升模型对目标位置的感知能力。

#### 3. 实验设计
*   **数据集**：
    *   **经典 RES**：RefCOCO, RefCOCO+, RefCOCOg, ReferIt。
    *   **广义 RES (GRES)**：gRefCOCO（包含单目标、多目标及无目标场景）。
    *   **开集词汇 (Open-vocabulary)**：PhraseCut。
*   **Benchmark 与指标**：使用 gIoU（平均交并比）、cIoU（累积交并比）、Precision@X (Pr@0.5/0.7/0.9) 以及无目标准确率 (N-Acc.)。
*   **对比方法**：
    *   **非 SAM 方法**：LAVT, CGFormer, PolyFormer 等。
    *   **SAM 相关方法**：RISAM, Prompt-RIS, LISA (基于 MLLM), GSVA 等。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA Tesla V100 GPU**。
*   **训练时长与参数**：
    *   **预训练**：在合并的 REC 数据集上预训练 20 个 epoch（Batch size 128）。
    *   **微调**：在目标数据集上微调 80 个 epoch（Batch size 64）。
    *   **模型规模**：SSP-SAM-224 的可训练参数量仅为 **18.37M**，远小于基于大语言模型的方法。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个主流数据集上进行了详尽测试，并针对 SSP 编码器的各个组件（视觉/语言适配器、提示生成器类型、Token 数量、高斯映射等）进行了 **7 组消融实验**。
*   **充分性与客观性**：实验不仅对比了分割精度，还对比了推理延迟（Inference Time）和参数量。通过在 PhraseCut 上的零样本测试验证了泛化性，并在 Pr@0.9 这一严苛指标下展示了优势，实验设计全面且客观。

#### 6. 主要结论与发现
*   **性能领先**：SSP-SAM 在 RefCOCO 系列和 ReferIt 数据集上刷新了 SOTA 记录，尤其在 gIoU 指标上表现突出。
*   **高精度分割**：在 Pr@0.9 指标下，SSP-SAM 显著优于现有方法（如比 CGFormer 高出约 9%），说明其生成的掩码边界更精准、更贴合目标。
*   **GRES 适应性**：无需特殊修改，模型在处理多目标和无目标场景时表现稳健，N-Acc. 指标领先。
*   **高效性**：推理速度比基于 MLLM 的方法（如 LISA）快 50 倍以上，且参数量极低。

#### 7. 优点（亮点）
*   **轻量且高效**：通过轻量级的适配器设计，在保持极低参数量的同时实现了高性能。
*   **高质量掩码**：充分利用了 SAM 的细粒度分割能力，生成的掩码质量往往超过了数据集本身的粗糙标注（Ground Truth）。
*   **通用性强**：一个架构同时兼容经典 RES、GRES 和开集词汇分割任务。
*   **即插即用**：SSP 编码器可以适配不同的视觉-语言模型（如 CLIP, EVA-CLIP, SigLIP2）。

#### 8. 不足与局限
*   **空间词依赖**：在缺乏空间描述词的数据集（如 RefCOCO+）上，语言适配器的增益相对有限。
*   **复杂逻辑理解**：对于包含否定逻辑或极度复杂的长句描述，模型仍存在误判风险（如 Failure Cases 所示）。
*   **开集场景挑战**：虽然在 PhraseCut 上表现良好，但在面对极大规模多目标（如“许多自行车”）或局部描述时，分割精度仍有提升空间。

（完）
