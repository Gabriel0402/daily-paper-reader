---
title: Formalizing and validating properties in Asmeta with Large Language Models (Extended Abstract)
title_zh: 在 Asmeta 中利用大语言模型进行属性的形式化与验证（扩展摘要）
authors: "Andrea Bombarda, Silvia Bonfanti, Angelo Gargantini, Nico Pellegrinelli"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15375v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 利用大语言模型进行时态逻辑属性的规范化与验证
tldr: 本研究针对基于模型的开发框架中编写时序逻辑属性的难题，提出将大语言模型（LLM）集成到 Asmeta 框架中。通过构建基于 LLM 的代理工作流，辅助用户进行属性的定义、形式化、解释和验证，并利用模型检测器的反馈进行迭代优化。该工作作为概念验证，展示了 LLM 在简化形式化规格说明编写和提高验证效率方面的潜力。
selection_source: fresh_fetch
motivation: 用户在将非正式需求转化为形式化的时序逻辑属性时面临巨大挑战。
method: 提出一种集成 LLM 的 Asmeta 框架工作流，利用 LLM 代理结合模型检测器反馈来辅助属性的形式化与验证。
result: 通过代表性示例证明了 LLM 能够有效支持时序属性的定义、解释及验证过程。
conclusion: 该集成方案具有可行性，能显著降低形式化方法的应用门槛并提升开发效率。
---

## 摘要
对于基于模型的开发框架的用户而言，编写时序逻辑属性通常是一项具有挑战性的任务，尤其是在将非形式化需求转化为形式化规范时。在本文中，我们探讨了将大语言模型（LLM）集成到 Asmeta 框架中的构想，以协助用户进行时序属性的定义、形式化、解释和验证。我们提出了一种工作流，其中基于 LLM 的智能体通过利用 Asmeta 规范和模型检测器生成的反馈来支持这些活动。这项工作作为一个概念验证，通过代表性示例展示了这种集成的可行性和潜在收益。

## Abstract
Writing temporal logic properties is often a challenging task for users of model-based development frameworks, particularly when translating informal requirements into formal specifications. In this paper, we explore the idea of integrating Large Language Models (LLMs) into the Asmeta framework to assist users during the definition, formalization, explanation, and validation of temporal properties. We present a workflow in which an LLM-based agent supports these activities by leveraging the Asmeta specification and the feedback produced by the model checker. This work serves as a proof of concept that illustrates the feasibility and potential benefits of such an integration through representative examples.

---

## 论文详细总结（自动生成）

### 论文总结：在 Asmeta 中利用大语言模型进行属性的形式化与验证

#### 1. 核心问题与研究动机
*   **核心问题**：在基于模型的开发（MBD）中，将非形式化的自然语言需求准确转化为形式化的时序逻辑属性（如 LTL 或 CTL）对开发者（尤其是初学者）来说具有极高的门槛和挑战。
*   **研究动机**：
    *   形式化验证（如模型检测）对于安全关键型系统至关重要，但编写正确的时序逻辑公式非常困难。
    *   现有的 Asmeta 框架虽然支持抽象状态机（ASM）建模和验证，但用户在定义验证属性时经常遇到语法和逻辑障碍。
    *   探索大语言模型（LLM）是否能作为“桥梁”，辅助用户完成从需求定义到验证反馈的全流程。

#### 2. 方法论与核心思想
论文提出了一种集成 LLM 代理的 Asmeta 框架工作流，核心思想是利用 LLM 的自然语言处理能力结合模型检测器的形式化反馈。
*   **核心流程**：
    1.  **输入**：用户提供 Asmeta 规范（ASM 模型）和以 EARS 语法编写的非形式化需求。
    2.  **处理**：LLM 代理提取模型签名（函数、域等），将需求翻译为嵌入在 AsmetaL 中的 LTL/CTL 公式。
    3.  **反馈循环**：生成的公式提交给 **AsmetaSMV**（集成 NuSMV）进行检测。如果存在语法错误，将错误信息反馈给 LLM 进行修正。
*   **四大功能目标**：
    *   **O1（定义辅助）**：根据模型上下文自动推导并建议潜在的自然语言属性。
    *   **O2（形式化翻译）**：将自然语言需求翻译为符合 Asmeta 语法的时序逻辑公式。
    *   **O3（属性解释）**：将复杂的时序逻辑公式反向翻译为易懂的自然语言。
    *   **O4（验证结果分析）**：将模型检测器生成的反例或见证场景（Avalla 场景）解释为自然语言，帮助用户理解失败原因。

#### 3. 实验设计
*   **实验场景**：使用了一个简单的“时钟（Clock）”模型作为示例。该模型包含秒、分、时的递增逻辑及信号控制。
*   **Benchmark**：本文属于初步研究（Extended Abstract），未设置标准的大规模基准测试集。
*   **对比方法**：主要通过人工与 LLM（具体为 ChatGPT 5.2 Instant 模型）的直接交互来展示可行性，未进行多模型横向对比。

#### 4. 资源与算力
*   **算力说明**：文中未提及具体的硬件资源（如 GPU 型号、数量）或训练时长。
*   **模型使用**：实验基于 **ChatGPT 5.2 Instant** 模型进行（注：论文发表时间标注为 2026 年，此处可能指代未来版本的模型或特定接口）。

#### 5. 实验数量与充分性
*   **实验数量**：仅展示了一个代表性的“时钟”模型案例，涵盖了上述四个功能目标的初步验证。
*   **充分性评价**：作为概念验证（Proof of Concept），实验展示了流程的可行性，但从学术严谨性角度看，实验数量较少，缺乏大规模工业案例的验证，也未进行消融实验或针对不同 LLM 性能的量化评估。

#### 6. 主要结论与发现
*   **可行性确认**：LLM 能够理解 Asmeta 规范的上下文，并生成语法基本正确、逻辑一致的时序逻辑属性。
*   **反馈的重要性**：集成模型检测器的反馈回路是必要的，因为 LLM 偶尔会生成语法错误的公式，而工具链的自动反馈可以有效闭环修正。
*   **降低门槛**：LLM 显著降低了非专家用户使用形式化方法的难度，特别是在解释反例和生成初始属性方面。

#### 7. 优点与亮点
*   **闭环集成**：不仅利用 LLM 生成代码，还将其与成熟的形式化验证工具（AsmetaSMV）结合，形成了“生成-校验-修正”的闭环。
*   **全生命周期支持**：覆盖了从需求 elicitation 到验证结果解释的多个阶段，而不仅仅是简单的代码翻译。
*   **上下文感知**：强调了 LLM 必须感知 ASM 模型的签名和类型系统，以确保生成的公式在语义上是有效的。

#### 8. 不足与局限
*   **实验规模有限**：仅有单一简单案例，无法证明其在处理复杂工业级模型时的扩展性和准确性。
*   **幻觉风险**：LLM 仍可能产生逻辑幻觉，虽然有语法校验，但深层的语义错误（即公式虽然合法但不符合用户真实意图）仍难以完全避免。
*   **依赖性**：高度依赖特定 LLM 的能力，且未讨论模型更新或不同模型之间的表现差异。
*   **缺乏量化指标**：未提供准确率、召回率或用户效率提升的具体量化数据。

（完）
