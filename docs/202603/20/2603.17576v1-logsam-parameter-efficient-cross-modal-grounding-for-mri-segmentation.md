---
title: "LoGSAM: Parameter-Efficient Cross-Modal Grounding for MRI Segmentation"
title_zh: LoGSAM：用于 MRI 分割的参数高效跨模态定位
authors: "Mohammad Robaitul Islam Bhuiyan, Sheethal Bhat, Melika Qahqaie, Tri-Thien Nguyen, Paula Andrea Pérez Toro, Tomas Arias Vergara, Andreas Maier"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17576v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 基于SAM的医学分割基础模型
tldr: "LoGSAM是一个高效的跨模态MRI脑肿瘤分割框架，旨在解决标注数据稀缺问题。它通过Whisper模型将放射科医生的语音转化为文本，利用NLP提取关键信息，并结合LoRA微调的Grounding DINO进行目标定位，最后由MedSAM生成分割掩码。该方法仅需更新极少量参数，在BRISC 2025数据集上达到了80.32%的Dice得分，展示了利用预训练大模型构建模块化医疗影像处理流程的潜力。"
selection_source: fresh_fetch
motivation: 针对现有MRI分割模型依赖大量标注数据且缺乏灵活性的问题，提出利用放射科医生口述信息驱动的自动化分割方案。
method: 结合Whisper语音识别、临床NLP提取、LoRA微调的Grounding DINO定位以及冻结的MedSAM分割，构建了一个参数高效的端到端流水线。
result: "在BRISC 2025挑战赛中取得80.32%的Dice得分，并在真实放射科医生语音测试中达到91.7%的病例级准确率。"
conclusion: 该研究证明了通过智能整合预训练基础模型并进行极少量参数微调，可以实现高效且准确的跨模态医疗影像分割。
---

## 摘要
使用磁共振成像 (MRI) 对脑肿瘤进行精确的定位和勾画，对于制定治疗计划和指导手术决策至关重要。然而，大多数现有方法依赖于特定任务的有监督模型，并受限于标注数据的稀缺性。为此，我们提出了 LoGSAM，这是一个参数高效、检测驱动的框架，可将放射科医生的口述转换为文本提示，用于基于基础模型的定位和分割。放射科医生的语音首先使用预训练的 Whisper ASR 模型进行转录和翻译，随后通过否定感知的临床 NLP 提取肿瘤特定的文本提示。这些提示通过经过 LoRA 适配的视觉语言检测模型 Grounding DINO (GDINO) 引导以文本为条件的肿瘤定位。LoRA 适配仅更新 5% 的模型参数，从而在保留预训练跨模态知识的同时，实现了计算高效的领域自适应。预测的边界框被用作 MedSAM 的提示，在无需任何额外微调的情况下生成像素级肿瘤掩码。在冻结的 MedSAM 上以 LoGSAM 衍生的先验为条件，在 BRISC 2025 数据集上实现了 80.32% 的最先进 Dice 分数。此外，我们使用一名认证放射科医生的德语口述在 12 个未见过的 MRI 扫描上评估了整个流程，实现了 91.7% 的病例级准确率。这些结果突显了通过智能利用预训练基础模型并进行极少参数更新，构建模块化“语音到分割”流程的可行性。

## Abstract
Precise localization and delineation of brain tumors using Magnetic Resonance Imaging (MRI) are essential for planning therapy and guiding surgical decisions. However, most existing approaches rely on task-specific supervised models and are constrained by the limited availability of annotated data. To address this, we propose LoGSAM, a parameter-efficient, detection-driven framework that transforms radiologist dictation into text prompts for foundation-model-based localization and segmentation. Radiologist speech is first transcribed and translated using a pretrained Whisper ASR model, followed by negation-aware clinical NLP to extract tumor-specific textual prompts. These prompts guide text-conditioned tumor localization via a LoRA-adapted vision-language detection model, Grounding DINO (GDINO). The LoRA adaptation updates using 5% of the model parameters, thereby enabling computationally efficient domain adaptation while preserving pretrained cross-modal knowledge. The predicted bounding boxes are used as prompts for MedSAM to generate pixel-level tumor masks without any additional fine-tuning. Conditioning the frozen MedSAM on LoGSAM-derived priors yields a state-of-the-art dice score of 80.32% on BRISC 2025. In addition, we evaluate the full pipeline using German dictations from a board-certified radiologist on 12 unseen MRI scans, achieving 91.7% case-level accuracy. These results highlight the feasibility of constructing a modular, speech-to-segmentation pipeline by intelligently leveraging pretrained foundation models with minimal parameter updates.

---

## 论文详细总结（自动生成）

### LoGSAM：用于 MRI 分割的参数高效跨模态定位论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：传统的脑肿瘤 MRI 分割方法高度依赖于特定任务的有监督学习和大量昂贵的像素级标注数据。此外，放射科医生在日常工作流中产生的口述报告（语音信息）未能被现有的自动化分割工具充分利用。
*   **研究动机**：旨在开发一种“语音到分割”（Speech-to-Segmentation）的框架，通过整合预训练的基础模型（Foundation Models），利用放射科医生的自然语言描述作为弱监督信号，实现参数高效且鲁棒的肿瘤定位与分割，减少对密集标注的依赖。

#### 2. 提出的方法论（LoGSAM 框架）
LoGSAM 采用模块化设计，主要包含三个核心阶段：
*   **语音到提示词生成（Speech-to-Prompt）**：
    *   使用 **Whisper (large-v3)** 模型将放射科医生的语音（如德语）转录并翻译为英文文本。
    *   利用 **spaCy** 和 **negspaCy** 进行临床自然语言处理（NLP），提取肿瘤类别（如神经胶质瘤、脑膜瘤等）并识别否定词（如“未见肿瘤”），最终生成结构化的文本提示词。
*   **参数高效的肿瘤定位（LoRA-augmented GDINO）**：
    *   核心模型为 **Grounding DINO (GDINO)**，它结合了 Swin-T 视觉骨干网络和 BERT 文本编码器。
    *   引入 **LoRA (Low-Rank Adaptation)** 技术，仅在注意力层、前馈网络和文本编码器末层等关键位置注入可训练的低 rank 矩阵。
    *   该阶段仅更新不到 5% 的参数，使模型能够适应 MRI 领域的特定视觉特征，同时保留大规模预训练的跨模态对齐知识。
*   **提示驱动的分割（MedSAM Segmentation）**：
    *   将 GDINO 预测的边界框（Bounding Box）作为空间提示，输入到冻结权重的 **MedSAM** 模型中。
    *   MedSAM 利用其预训练的医学影像知识，在无需进一步微调的情况下生成最终的像素级分割掩码。

#### 3. 实验设计
*   **数据集**：
    *   **BRISC 2025**：包含 6,000 张对比增强 T1 加权 MRI 切片，涵盖四种类别（胶质瘤、脑膜瘤、垂体瘤、健康）。
    *   **Kaggle MRI Dataset**：包含 5,249 张图像，作为分布外（OOD）测试集评估泛化能力。
    *   **真实场景测试**：由认证放射科医生对 12 个病例提供的德语口述录音。
*   **Benchmark 与对比方法**：
    *   对比了 **Trust-Refined U-Net Ensemble**（BRISC 2025 的先前 SOTA）。
    *   对比了全参数微调的 GDINO + MedSAM 基准。
    *   在检测任务上参考了 YOLOv11 的表现。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 GPU (40GB RAM)**。
*   **训练细节**：训练时长为 125 个 epoch，使用 AdamW 优化器，学习率为 $2 \times 10^{-4}$，采用 OneCycle 学习率调度策略。
*   **参数量**：LoRA 适配仅涉及约 4.96% 的可训练参数。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的性能评估（BRISC 测试集 1000 张图）。
    *   **消融实验**：系统分析了 LoRA 的 Rank 大小（32, 64, 128）以及 LoRA 注入位置（仅视觉层 vs. 全局注入）对性能的影响。
    *   **泛化性测试**：在 Kaggle OOD 数据集上验证了模型的鲁棒性。
    *   **定性分析**：展示了不同肿瘤类型的可视化分割结果。
*   **充分性评价**：实验设计较为全面，涵盖了从算法性能、参数效率到跨语言/跨数据集泛化能力的多个维度，结果具有较高的客观性。

#### 6. 主要结论与发现
*   **性能优异**：LoGSAM 在 BRISC 2025 上达到了 **80.32% 的 Dice 分数**，优于之前的集成 U-Net 方法（79.80%）。
*   **参数高效**：仅微调 <5% 的参数，即可达到全参数微调模型 98.6% 的性能水平。
*   **鲁棒性强**：在 OOD 数据集上表现稳定；在真实放射科医生语音测试中，实现了 **91.7% 的病例级准确率**。
*   **否定感知**：通过 NLP 模块有效降低了因临床描述中的否定词（如“无肿瘤”）导致的误报。

#### 7. 优点
*   **跨模态融合**：成功打通了“语音-文本-视觉定位-像素分割”的全链路，符合临床实际工作流。
*   **低标注依赖**：利用检测框驱动分割，避免了对大规模像素级标注的强依赖。
*   **模块化与灵活性**：各组件（Whisper, GDINO, MedSAM）均可独立升级，且 LoRA 使得在医疗垂直领域的适配成本极低。

#### 8. 不足与局限
*   **维度限制**：目前仅针对 **2D MRI 切片** 进行处理，尚未扩展到 3D 体素分割，这在临床 3D 容积评估中存在局限。
*   **语音识别风险**：实验中发现 ASR 可能会误读专业医学术语（如将 glioblastoma 误认为 glioplaston），导致下游任务失败。
*   **样本量限制**：虽然在公开数据集上实验充分，但真实的放射科医生语音测试样本较小（仅 12 例），需更大规模的临床验证。
*   **多序列缺失**：目前主要依赖 T1 加权序列，未整合 T2、FLAIR 等其他关键 MRI 序列信息。

（完）
