---
title: In-Field 3D Wheat Head Instance Segmentation From TLS Point Clouds Using Deep Learning Without Manual Labels
title_zh: 基于深度学习且无需人工标注的 TLS 点云田间三维麦穗实例分割
authors: "Tomislav Medic, Liangliang Nan"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14309v1"
tags: ["query:mmseg"]
score: 8.0
evidence: 遥感中基于TLS点云的3D实例分割
tldr: 本研究针对农田环境下小麦穗的3D实例分割难题，提出了一种无需人工标注的深度学习框架。该方法利用地面激光扫描（TLS）点云，通过3D-2D多视图投影结合Grounded SAM生成伪标签，随后训练3D全景分割网络。实验证明该方法在复杂场景下表现优异，优于基于高斯泼溅的现有方案，为植物表型分析提供了高效、低成本的自动化解决方案。
selection_source: fresh_fetch
motivation: 针对农田复杂场景下小麦穗3D点云人工标注极其困难且耗时的问题，探索无需手动标签的自动化分割方法。
method: 提出两阶段流程：首先利用多视图投影和Grounded SAM生成初始伪标签，随后以此训练监督式3D全景分割神经网络。
result: 实验结果显示该方法在小麦穗分割任务上表现出色，性能超越了基于多视图RGB和3D高斯泼溅的Wheat3DGS方案。
conclusion: 该研究证明了利用TLS点云进行无监督3D实例分割的可行性，并展示了其在其他类似点云分割任务中的迁移潜力。
---

## 摘要
激光扫描（LiDAR）点云的三维实例分割在许多遥感相关领域仍是一项挑战。成功的解决方案通常依赖于监督式深度学习和人工标注，因此主要关注那些可以通过视觉检查和点云人工标注进行清晰界定的对象。然而，对于场景更为复杂且拥挤的任务，如农业中的田间植物表型分析，此类方法往往并不可行。在本研究中，我们直接利用地面激光扫描（TLS）点云来解决田间麦穗实例分割任务。为了解决这一问题并规避对人工标注的需求，我们提出了一种新型的两阶段流水线。为了获取初始的三维实例建议（proposals），第一阶段采用了三维到二维的多视图投影、用于零样本二维以对象为中心分割的 Grounded SAM 流水线以及多视图标签融合。第二阶段将这些初始建议作为带噪声的伪标签，用于训练一个监督式三维全景式分割神经网络。我们的结果证明了所提方法的可行性，并显示出相对于 Wheat3DGS 的性能提升。Wheat3DGS 是近期一种基于多视图 RGB 图像和三维高斯泼溅（3D Gaussian Splatting）、无需人工三维标注的田间麦穗实例分割替代方案。这表明 TLS 是一种具有竞争力的传感替代方案。此外，结果表明所提流水线的两个阶段均能在无需人工标注的情况下提供可用的三维实例分割，这预示着该方法在其他类似的基于 TLS 的点云分割任务中具有良好的、低成本的可迁移性。

## Abstract
3D instance segmentation for laser scanning (LiDAR) point clouds remains a challenge in many remote sensing-related domains. Successful solutions typically rely on supervised deep learning and manual annotations, and consequently focus on objects that can be well delineated through visual inspection and manual labeling of point clouds. However, for tasks with more complex and cluttered scenes, such as in-field plant phenotyping in agriculture, such approaches are often infeasible. In this study, we tackle the task of in-field wheat head instance segmentation directly from terrestrial laser scanning (TLS) point clouds. To address the problem and circumvent the need for manual annotations, we propose a novel two-stage pipeline. To obtain the initial 3D instance proposals, the first stage uses 3D-to-2D multi-view projections, the Grounded SAM pipeline for zero-shot 2D object-centric segmentation, and multi-view label fusion. The second stage uses these initial proposals as noisy pseudo-labels to train a supervised 3D panoptic-style segmentation neural network. Our results demonstrate the feasibility of the proposed approach and show performance improvementsrelative to Wheat3DGS, a recent alternative solution for in-field wheat head instance segmentation without manual 3D annotations based on multi-view RGB images and 3D Gaussian Splatting, showcasing TLS as a competitive sensing alternative. Moreover, the results show that both stages of the proposed pipeline can deliver usable 3D instance segmentation without manual annotations, indicating promising, low-effort transferability to other comparable TLS-based point cloud segmentation tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《In-Field 3D Wheat Head Instance Segmentation From TLS Point Clouds Using Deep Learning Without Manual Labels》的结构化技术总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在无需人工标注 3D 点云的情况下，实现田间复杂环境下小麦穗（Wheat Head）的高精度三维实例分割。
*   **研究背景**：
    *   **标注困境**：在密集的农田中，小麦穗结构复杂、遮挡严重，人工对 3D 点云进行逐点标注几乎不可行且成本极高。
    *   **技术瓶颈**：现有的植物表型分析多依赖室内环境或简单的植株结构（如玉米），而针对田间小麦穗的 LiDAR（特别是 TLS 地面激光扫描）分割仍是空白。
    *   **研究动机**：利用 2D 视觉大模型的零样本（Zero-shot）能力来引导 3D 点云分割，摆脱对人工标签的依赖，提升高通量植物表型分析（HTFP）的效率。

### 2. 方法论
论文提出了一种**两阶段（Two-stage）**的自动化处理流程：

*   **第一阶段：基于多视图投影的 3D 实例生成 (Stage-I)**
    1.  **3D 到 2D 投影**：将 TLS 扫描仪获取的原始点云投影为球形强度图（Intensity）和距离图（Range）。
    2.  **2D 零样本分割**：利用 **Grounded SAM** 流水线（结合 Grounding DINO 检测和 SAM2 分割），根据文本提示（如 "wheat"）在 2D 图像上生成实例掩码。
    3.  **2D 到 3D 回投**：将 2D 掩码映射回 3D 空间，形成初步的 3D 实例片段。
    4.  **多视图标签融合**：构建稀疏无向加权图，利用 **HCS（高度连接子图）算法**进行聚类，将来自不同扫描站点的同一物体片段融合成完整的 3D 实例。

*   **第二阶段：基于全景分割网络的知识蒸馏 (Stage-II)**
    1.  **伪标签生成**：将第一阶段生成的 3D 实例作为“带噪声的伪标签”。
    2.  **3D 网络训练**：使用这些伪标签训练一个基于 **Minkowski Engine** 的 3D 全景分割神经网络（采用 U-Net 骨干网络和 PointGroup 聚类头）。
    3.  **推理与优化**：训练后的网络能够学习 3D 几何特征，从而识别出第一阶段因遮挡或多视图一致性不足而漏掉的实例。

### 3. 实验设计
*   **数据集/场景**：使用了一个公开的田间小麦表型数据集，包含 7 个样方（Plots），涵盖 42 个遗传品种以确保多样性。
*   **基准方法 (Benchmark)**：**Wheat3DGS**（目前该领域的 SOTA 方案，基于多视图 RGB 图像和 3D 高斯泼溅技术）。
*   **对比实验**：
    *   Wheat3DGS（基准）。
    *   Stage-I（仅投影融合）。
    *   Stage-II（伪标签训练后的网络预测）。
    *   Stage-I + Stage-II（两者结果合并）。
*   **评估指标**：F1 分数、精确率（Precision）、召回率（Recall）以及计数误差（CE）。

### 4. 资源与算力
*   **硬件**：使用了一台配备 **NVIDIA GeForce RTX 3090 Ti** (24 GB RAM) 的工作站。
*   **软件环境**：CUDA 11.5，基于 Minkowski Engine 构建。
*   **训练时长**：第二阶段的网络训练耗时约 **12 小时**。

### 5. 实验数量与充分性
*   **实验规模**：样方被划分为训练集（5 个）、验证集（1 个）和测试集（1 个）。虽然样方数量看似不多，但每个样方包含数百个麦穗，总计处理了数千个实例。
*   **充分性评价**：
    *   **客观性**：通过空间划分（Spatial Split）严格区分训练和测试区域，避免了数据泄露。
    *   **对比维度**：不仅对比了外部基准，还进行了内部阶段性的消融对比（Stage-I vs Stage-II），证明了引入第二阶段的必要性。
    *   **局限性**：由于 3D 密集标注不可行，评估使用的是“稀疏参考标签”（即每个麦穗手动标注一个中心点），这在一定程度上限制了对分割形状精确度的评估。

### 6. 主要结论与发现
*   **性能超越**：所提方法在 F1 分数上显著优于基于图像的 Wheat3DGS（F1 从 0.22 提升至 0.69-0.72），证明了 TLS 点云在复杂农田场景下的竞争优势。
*   **互补性**：Stage-I 依赖多视图一致性，容易受遮挡影响；Stage-II 通过学习几何特征能找回漏检实例。两者结合（I+II）达到了最高的召回率（0.76）和 F1 分数（0.72）。
*   **零样本可行性**：证明了通过 2D 基础模型（Foundation Models）向 3D 领域转移知识，可以有效解决无标注数据的分割难题。

### 7. 优点
*   **无需人工标注**：完全自动化生成伪标签并训练，极大地降低了农业表型分析的门槛。
*   **鲁棒性强**：相比纯图像方法，TLS 点云受光照影响小，且该流水线能处理极度拥挤的麦穗簇。
*   **通用性**：该流程不包含特定于小麦的先验知识，理论上可迁移至其他作物或类似的 TLS 点云分割任务。

### 8. 不足与局限
*   **欠分割问题**：在极度密集的区域（5 个以上麦穗紧挨），网络有时会将多个麦穗识别为一个实例（欠分割率约 7.5%-9.5%）。
*   **异常样本敏感**：对于形状异常（如极长、弯曲或水平生长的麦穗），Stage-II 的泛化能力仍有提升空间。
*   **合并策略简单**：Stage-I 和 Stage-II 的结果合并采用了较为简单的 NMS 策略，导致在某些情况下出现重复检测（False Positives 增加）。
*   **评估基准受限**：由于缺乏密集的真值（Ground Truth），无法评估单个麦穗的体积、表面积等精细形态学指标的准确性。

（完）
