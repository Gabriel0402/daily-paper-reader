---
title: Scaling Sim-to-Real Reinforcement Learning for Robot VLAs with Generative 3D Worlds
title_zh: 利用生成式 3D 世界扩展机器人 VLA 的 Sim-to-Real 强化学习
authors: "Andrew Choi, Xinjie Wang, Zhizhong Su, Wei Xu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18532v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 结合生成式3D世界的机器人VLA强化学习
tldr: 本研究针对机器人视觉-语言-动作（VLA）模型在真实世界强化学习中泛化性受限的问题，提出利用生成式3D世界模型进行大规模仿真训练。通过结合语言驱动的场景设计器，自动生成数百个包含多样化物体和背景的交互式场景，实现了高效并行的策略学习。该方法不仅显著提升了仿真和真实环境下的任务成功率与执行速度，还通过增加场景多样性增强了模型的零样本泛化能力，为解决Sim-to-Real鸿沟提供了新思路。
selection_source: fresh_fetch
motivation: 真实世界强化学习难以扩展场景多样性，导致预训练模型容易过拟合到特定场景，而手动设计仿真场景成本过高。
method: 利用3D生成模型和语言驱动的场景设计器自动构建大规模多样化仿真环境，并在此基础上对预训练VLA模型进行强化学习微调。
result: "仿真成功率从9.7%提升至79.8%，真实世界成功率从21.7%提升至75%，且任务完成速度显著加快。"
conclusion: 借助生成式3D世界模型提供的大规模多样化数据，可以有效提升VLA模型的性能和零样本泛化能力，实现高效的虚实迁移。
---

## 摘要
通过强化学习（RL）训练的大型视觉语言模型（VLM）的强劲表现，激发了在机器人领域微调视觉-语言-动作（VLA）模型的类似方法。许多近期工作直接在现实世界中微调 VLA，以避免处理仿真到现实（sim-to-real）的差距。虽然现实世界中的 RL 规避了 sim-to-real 问题，但它本质上限制了所得 VLA 的泛化性，因为在物理世界中扩展场景和物体的多样性极其困难。这导致了一个矛盾的结果：将一个广泛预训练的模型转变为一个过拟合的、特定于场景的策略。相比之下，在仿真中训练可以提供多样化的场景，但设计这些场景的成本也很高。在这项工作中，我们展示了通过利用 3D 世界生成模型，可以在不牺牲泛化性且减少人工劳动的情况下对 VLA 进行 RL 微调。利用这些模型结合语言驱动的场景设计器，我们生成了数百个包含独特物体和背景的多样化交互式场景，从而实现了可扩展且高度并行的策略学习。从预训练的模仿基线出发，我们的方法将仿真成功率从 9.7% 提高到 79.8%，同时在任务完成时间上实现了 1.25 倍的加速。我们进一步证明了由生成的数字孪生质量结合领域随机化所实现的成功 sim-to-real 迁移，将现实世界的成功率从 21.7% 提高到 75%，并实现了 1.13 倍的加速。最后，我们通过消融实验进一步强调了利用 3D 世界生成模型提供的几乎无限数据的优势，结果表明增加场景多样性能直接提高零样本泛化能力。

## Abstract
The strong performance of large vision-language models (VLMs) trained with reinforcement learning (RL) has motivated similar approaches for fine-tuning vision-language-action (VLA) models in robotics. Many recent works fine-tune VLAs directly in the real world to avoid addressing the sim-to-real gap. While real-world RL circumvents sim-to-real issues, it inherently limits the generality of the resulting VLA, as scaling scene and object diversity in the physical world is prohibitively difficult. This leads to the paradoxical outcome of transforming a broadly pretrained model into an overfitted, scene-specific policy. Training in simulation can instead provide access to diverse scenes, but designing those scenes is also costly. In this work, we show that VLAs can be RL fine-tuned without sacrificing generality and with reduced labor by leveraging 3D world generative models. Using these models together with a language-driven scene designer, we generate hundreds of diverse interactive scenes containing unique objects and backgrounds, enabling scalable and highly parallel policy learning. Starting from a pretrained imitation baseline, our approach increases simulation success from 9.7% to 79.8% while achieving a 1.25$\times$ speedup in task completion time. We further demonstrate successful sim-to-real transfer enabled by the quality of the generated digital twins together with domain randomization, improving real-world success from 21.7% to 75% and achieving a 1.13$\times$ speedup. Finally, we further highlight the benefits of leveraging the effectively unlimited data from 3D world generative models through an ablation study showing that increasing scene diversity directly improves zero-shot generalization.

---

## 论文详细总结（自动生成）

### 论文总结：利用生成式 3D 世界扩展机器人 VLA 的 Sim-to-Real 强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉-语言-动作（VLA）模型在机器人领域表现强劲，但其微调面临两难境地：在**现实世界**中进行强化学习（RL）虽能规避“虚实鸿沟”，但受限于物理环境的多样性，容易导致模型过拟合到特定场景；而在**仿真环境**中训练虽可扩展，但手动设计多样化、交互式的 3D 场景成本极高。
*   **研究动机**：探索是否能利用生成式 AI 自动构建大规模、多样化的 3D 交互环境，从而在不牺牲泛化性的前提下，通过仿真强化学习提升 VLA 模型的性能，并成功迁移至现实世界。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：构建一个自动化的“生成式仿真”流水线，将自然语言任务描述转化为结构化的 3D 交互场景，并利用大规模并行强化学习对预训练 VLA 进行微调。
*   **关键技术细节**：
    *   **语言驱动场景设计器**：利用 GPT-4o 将任务指令（如“把笔放进笔筒”）解析为包含背景、目标物、干扰项及其空间关系（如 ON, IN）的结构化场景图。
    *   **3D 世界生成模型**：基于 EmbodiedGen 引擎，根据场景图生成具有物理交互属性的 3D 资产和布局，并通过自动质量检查（QA）过滤物理上不合理的配置。
    *   **PPOFlow 算法**：针对流匹配（Flow Matching）架构的 VLA 模型（如 $\pi_0$），提出了一种基于 PPO 的变体。它通过在数值积分过程中注入可学习噪声，将多步去噪过程转化为单步高斯策略，从而支持标准的强化学习更新，并显著提升推理速度（约 2.36 倍）。
    *   **虚实迁移技术**：结合高保真数字孪生资产、领域随机化（DR）以及带重力补偿的 PD 控制，减小视觉和动力学差距。

#### 3. 实验设计
*   **数据集与场景**：
    *   **仿真**：利用生成流水线构建了 100 个独特的交互场景（W 集合），涵盖多种物体和背景。
    *   **基准测试（Benchmark）**：使用 SimplerEnv 中的 Bridge 桌面场景作为对比和评估。
*   **对比方法**：
    *   **$\pi_{pre}$**：仅在 BridgeV2 数据集上进行模仿学习预训练的基线模型。
    *   **手动设计场景 RL**：在 3 个手动设计的 SimplerEnv 场景中进行 RL 微调的模型。
    *   **不同规模 $N$ 的模型**：分别在 $N=1, 3, 10, 25, 50, 100$ 个生成场景中训练的模型，以研究多样性的影响。
*   **现实评估**：在 12 个真实场景中进行 240 次实验，包含 OOD（分布外）物体和属性。

#### 4. 资源与算力
*   **训练算力**：使用了 **8 台 NVIDIA RTX 6000 Ada GPU**。
*   **训练时长**：LoRA 微调 VLM 主干及全量微调动作头，耗时约 **5 天**。
*   **生成效率**：在单张 RTX 4090 上，生成一个完整场景平均耗时约 46.8 分钟（若使用预建资产库可缩短至 2 分钟）。

#### 5. 实验数量与充分性
*   **实验规模**：仿真实验涵盖 100 个场景，每个场景评估 100 次；现实实验包含 12 个场景，总计 240 次测试。
*   **充分性与客观性**：
    *   **消融实验**：详细探讨了训练场景数量 $N$ 对零样本泛化能力的影响，证明了性能随多样性单调增长。
    *   **多维度评估**：不仅测试了成功率（SR），还分析了任务完成时间（TF）、动力学失败率（DFR）和语义失败率（SFR）。
    *   **公平性**：对比了手动设计场景与自动生成场景的泛化差异，实验设计较为严谨。

#### 6. 主要结论与发现
*   **性能大幅提升**：RL 微调将仿真成功率从 9.7% 提升至 79.8%，现实世界成功率从 21.7% 提升至 75%。
*   **多样性即泛化力**：增加训练场景的数量能显著缩小分布内（ID）与分布外（OOD）的性能差距。例如，从 1 个场景增加到 50 个场景，OOD 成功率提升了 24.8 个百分点。
*   **多模态并非必须**：研究发现，将流匹配模型简化为单步高斯策略进行 RL 微调，并不会损失性能，反而能极大提高计算效率。
*   **Sim-to-Real 可行性**：证明了生成式 3D 模型产生的“数字孪生”质量足以支持复杂的机器人操纵任务迁移。

#### 7. 优点（亮点）
*   **全自动化流水线**：实现了从“自然语言描述”到“交互式仿真训练”再到“现实部署”的闭环，极大降低了人工设计成本。
*   **扩展性强**：利用生成式模型解决了机器人学习中长期存在的“数据多样性”瓶颈。
*   **算法优化**：PPOFlow 成功解决了大规模流匹配 VLA 模型难以进行强化学习微调的问题，并兼顾了推理效率。

#### 8. 不足与局限
*   **任务类型受限**：目前主要集中在“抓取与放置”（Pick-and-Place）任务，尚未涵盖更复杂的长程任务或涉及关节类物体（如开抽屉）的操作。
*   **生成模型限制**：受限于 EmbodiedGen 的能力，部分生成的物体可能存在比例失调或初始位置不稳的问题（约 15% 的场景仍需少量人工干预）。
*   **硬件依赖**：虽然减少了数据采集人力，但大规模 VLA 的 RL 训练对计算资源（如多卡 GPU 环境）仍有较高要求。

（完）
