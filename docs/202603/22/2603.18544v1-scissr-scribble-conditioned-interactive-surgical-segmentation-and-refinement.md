---
title: "SCISSR: Scribble-Conditioned Interactive Surgical Segmentation and Refinement"
title_zh: SCISSR：基于涂鸦条件的交互式手术分割与精细化
authors: "Haonan Ping, Jian Jiang, Cheng Yuan, Qizhen Sun, Lv Wu, Yutong Ban"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18544v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 调整SAM模型用于交互式手术场景分割
tldr: 针对手术场景中组织和器械分割难的问题，本文提出SCISSR框架，利用涂鸦（Scribble）作为交互提示。该框架引入轻量级涂鸦编码器和LoRA适配器，将手绘涂鸦转化为密集嵌入以进行迭代细化。基于SAM 2构建且具有通用性，在EndoVis 2018和CholecSeg8k数据集上表现优异，显著提升了复杂手术目标的分割精度和鲁棒性。
selection_source: fresh_fetch
motivation: 手术场景中目标形状不规则且存在遮挡，传统的点或框提示难以精确捕捉细微结构。
method: 开发了轻量级涂鸦编码器和空间门控融合模块，通过LoRA微调使SAM 2支持涂鸦提示和迭代细化。
result: "在EndoVis 2018和CholecSeg8k数据集上分别达到95.41%和96.30%的Dice系数，优于迭代点提示方法。"
conclusion: 涂鸦提示为手术图像分割提供了一种比点或框更高效、更鲁棒的交互式细化方案。
---

## 摘要
由于形状不规则、结构细长、镜面反射以及频繁的遮挡，手术场景中组织和器械的准确分割具有极高的标注强度。虽然 SAM 模型支持点、框和掩码提示，但点往往过于稀疏，而框对于定位此类具有挑战性的目标又过于粗糙。我们提出了 SCISSR，这是一个用于交互式手术场景分割的涂鸦提示框架。它引入了一个轻量级的涂鸦编码器（Scribble Encoder），将手绘涂鸦转换为与掩码解码器兼容的稠密提示嵌入，通过在错误区域绘制修正笔触，实现对目标对象的迭代精细化。由于所有新增模块（涂鸦编码器、空间门控融合和 LoRA 适配器）仅通过标准嵌入接口与主干网络交互，该框架并不局限于单一模型：本研究基于 SAM 2 构建，但相同的组件无需结构修改即可迁移到其他提示驱动的分割架构（如 SAM 3）。为了保留预训练能力，我们仅训练这些轻量级附加组件，同时保持其余主干网络冻结。在 EndoVis 2018 上的实验展示了强大的域内性能，而在分布外数据集 CholecSeg8k 上的评估进一步证实了跨手术领域的鲁棒性。SCISSR 在 EndoVis 2018 上经过五轮交互达到 95.41% 的 Dice 系数，在 CholecSeg8k 上经过三轮交互达到 96.30% 的 Dice 系数，在两个基准测试中均优于迭代点提示。

## Abstract
Accurate segmentation of tissues and instruments in surgical scenes is annotation-intensive due to irregular shapes, thin structures, specularities, and frequent occlusions. While SAM models support point, box, and mask prompts, points are often too sparse and boxes too coarse to localize such challenging targets. We present SCISSR, a scribble-promptable framework for interactive surgical scene segmentation. It introduces a lightweight Scribble Encoder that converts freehand scribbles into dense prompt embeddings compatible with the mask decoder, enabling iterative refinement for a target object by drawing corrective strokes on error regions. Because all added modules (the Scribble Encoder, Spatial Gated Fusion, and LoRA adapters) interact with the backbone only through its standard embedding interfaces, the framework is not tied to a single model: we build on SAM 2 in this work, yet the same components transfer to other prompt-driven segmentation architectures such as SAM 3 without structural modification. To preserve pre-trained capabilities, we train only these lightweight additions while keeping the remaining backbone frozen. Experiments on EndoVis 2018 demonstrate strong in-domain performance, while evaluation on the out-of-distribution CholecSeg8k further confirms robustness across surgical domains. SCISSR achieves 95.41% Dice on EndoVis 2018 with five interaction rounds and 96.30% Dice on CholecSeg8k with three interaction rounds, outperforming iterative point prompting on both benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《SCISSR: Scribble-Conditioned Interactive Surgical Segmentation and Refinement》** 的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：手术场景中的组织和器械分割极具挑战性，原因在于目标形状不规则、结构细长、存在镜面反射及频繁遮挡。
*   **研究动机**：现有的交互式分割模型（如 SAM 系列）主要依赖点（Point）或框（Box）提示。然而，点提示过于稀疏，难以捕捉复杂形态；框提示过于粗糙，会引入过多背景噪声。
*   **解决方案**：提出 **SCISSR** 框架，引入**涂鸦（Scribble）**作为交互媒介。涂鸦在提供密集空间覆盖的同时，标注成本仅略高于点击，是手术场景下更自然的交互方式。

### 2. 方法论
SCISSR 建立在 SAM 2 之上，通过轻量级插件实现涂鸦驱动的迭代细化，其核心组件包括：
*   **双路径涂鸦处理（Dual-track Pathway）**：
    *   **路径 1（累积涂鸦 → 稠密提示）**：将所有轮次的涂鸦并集编码为稠密嵌入，输入掩码解码器，确保模型记住用户的完整意图。
    *   **路径 2（最新涂鸦 → 存储查询）**：仅将当前轮次的修正涂鸦通过**空间门控融合（SGF）**模块注入存储注意力（Memory Attention）的查询特征中，引导模型关注新修正的区域。
*   **关键技术细节**：
    *   **Scribble Encoder**：轻量级卷积网络，将 256×256 的涂鸦图下采样并投影至 256 维嵌入空间。
    *   **空间门控融合（SGF）**：使用深度可分离卷积进行空间混合，并通过一个初始化为零的自适应缩放因子 $\alpha$ 控制融合强度，确保预训练特征在初始阶段不受干扰。
    *   **可切换 LoRA 适配器**：在掩码解码器和存储注意力层插入 LoRA，仅微调这些轻量级参数，冻结图像编码器。
    *   **迭代细化算法**：复用 SAM 2 的时间存储机制，将单张图像的多轮交互模拟为视频序列处理，前一轮的预测作为下一轮的先验。

### 3. 实验设计
*   **数据集**：
    *   **EndoVis 2018**：机器人肾切除术序列，作为域内（In-distribution）基准。
    *   **CholecSeg8k**：腹腔镜胆囊切除术数据，作为分布外（Out-of-distribution, OOD）基准，用于测试泛化性。
*   **对比方法（Baselines）**：
    *   **点提示**：SAM 2 Tiny、SAM 3（分别采用每连通分量 1 点和每类别 10 点的协议）。
    *   **框提示**：SAM 2 Tiny、SAM 3、MedSAM2。
    *   **全监督模型**：U-Net、SegFormer、HRNet、TAFPNet 等。
*   **评估协议**：使用自动化的“自适应涂鸦生成器”模拟人类交互，根据预测误差自动生成修正涂鸦。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 (24GB)** GPU。
*   **训练细节**：
    *   采用两阶段训练：第一阶段训练编码器和解码器 LoRA；第二阶段联合训练所有组件。
    *   使用 AdamW 优化器，Batch Size 为 2，训练 10 个 Epoch。
    *   主干网络使用 SAM 2 Tiny。

### 5. 实验数量与充分性
*   **实验规模**：在 EndoVis 2018 上测试了 4,616 个样本，在 CholecSeg8k 上测试了 8,800 个样本。
*   **消融实验**：
    *   验证了不同涂鸦策略（轮廓、中心线、波浪线、自适应）的影响。
    *   验证了 SGF 模块和存储机制（Memory）各自的增益。
    *   分析了点提示密度增加时的性能退化现象。
*   **评价**：实验设计较为充分，涵盖了从性能对比到泛化性验证，再到组件有效性的全方位分析。通过 OOD 测试证明了模型不依赖于特定手术类型的鲁棒性，对比实验在相同自动化协议下进行，具有较好的公平性。

### 6. 主要结论与发现
*   **性能领先**：SCISSR 在 EndoVis 2018（5 轮交互）达到 **95.41% Dice**，在 CholecSeg8k（3 轮交互）达到 **96.30% Dice**。
*   **收敛速度快**：相比点提示，SCISSR 能以更少的交互轮次达到更高的精度阈值（如 Dice ≥ 0.90）。
*   **点提示的局限**：实验发现，单纯增加点提示的数量（如从 10 点增至 50 点）反而会导致 SAM 性能剧烈下降，而涂鸦提示能稳定利用密集的空间信号。
*   **泛化能力**：仅在 EndoVis 2018 上训练的模型，在完全未见的 CholecSeg8k 数据集上表现依然优于专门针对医疗优化的 MedSAM2。

### 7. 优点与亮点
*   **架构无关性**：设计具有通用性，所有组件通过标准接口连接，可轻松迁移至 SAM 3 等新架构。
*   **交互效率高**：涂鸦提示比点提示更符合手术器械（长条状、弯曲状）的形态特征。
*   **轻量化**：通过 LoRA 和轻量级编码器实现，无需重新训练庞大的基础模型，计算开销低。

### 8. 不足与局限
*   **模拟偏差**：实验中的涂鸦是由算法基于 Ground Truth 自动生成的，虽然加入了扰动，但与真实人类医生的手绘涂鸦在随机性和不确定性上可能仍有差异。
*   **应用范围**：目前主要验证了单帧图像的交互细化，虽然提及了视频传播，但未在文中展示大规模视频序列的连续交互性能。
*   **实时性未详述**：虽然使用了 Tiny 模型，但未具体给出单次交互的推理延迟（Latency）数据，这对于术中实时引导至关重要。

（完）
