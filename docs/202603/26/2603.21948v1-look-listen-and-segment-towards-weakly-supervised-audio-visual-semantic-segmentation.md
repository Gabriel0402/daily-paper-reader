---
title: "Look, Listen and Segment: Towards Weakly Supervised Audio-visual Semantic Segmentation"
title_zh: 边看、边听、边分割：迈向弱监督音视频语义分割
authors: "Chengzhi Li, Heyan Huang, Ping Jian, Yanghao Zhou"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21948v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 使用跨模态对齐的弱监督音视频语义分割
tldr: 针对音视频语义分割（AVSS）中逐帧标注成本高昂的问题，本文提出了弱监督音视频语义分割（WSAVSS）任务，仅利用视频级标签生成发声对象的像素级掩码。作者提出了渐进式跨模态语义对齐（PCAS）框架，包含“先看后听”和“先听后分割”两个模块，通过视觉语义提示增强音频理解，并利用对比学习实现音频类别与图像区域的对齐。实验证明该方法在弱监督设置下达到领先水平，且能与全监督方法竞争。
selection_source: fresh_fetch
motivation: 旨在解决音视频语义分割中逐帧像素级标注成本过高的问题，探索仅使用视频级标签实现像素级分割的可能性。
method: 提出了PCAS框架，通过视觉语义提示增强音频特征，并利用渐进式对比对齐将音频类别映射到对应的图像区域。
result: 在AVS数据集上取得了弱监督方法的最佳性能，并在AVSS任务中展现出与全监督基准相当的竞争力。
conclusion: 该研究验证了通过弱监督跨模态对齐实现高精度音视频语义分割的可行性，为降低标注成本提供了有效方案。
---

## 摘要
音视频语义分割（AVSS）在像素级对齐音频和视频，但需要昂贵的逐帧标注。我们引入了弱监督音视频语义分割（WSAVSS），它仅使用视频级标签来生成发声物体的逐帧语义掩码。我们将 WSAVSS 分解为看、听和分割，并提出了用于语义的渐进式跨模态对齐（PCAS），包含两个模块：“先看后听”和“先听后分割”。PCAS 构建了一个分类任务，利用视频标签训练音视频编码器，注入视觉语义提示以增强帧级音频理解，然后应用渐进式对比对齐，在没有掩码标注的情况下将音频类别映射到图像区域。实验表明，PCAS 在 AVS 弱监督方法中达到了最先进的性能，并且在 AVSS 上与全监督基准相比仍具竞争力，验证了其有效性。

## Abstract
Audio-Visual Semantic Segmentation (AVSS) aligns audio and video at the pixel level but requires costly per-frame annotations. We introduce Weakly Supervised Audio-Visual Semantic Segmentation (WSAVSS), which uses only video-level labels to generate per-frame semantic masks of sounding objects. We decompose WSAVSS into looking, listening, and segmentation, and propose Progressive Cross-modal Alignment for Semantics (PCAS) with two modules: *Looking-before-Listening* and *Listening-before-Segmentation*. PCAS builds a classification task to train the audio-visual encoder using video labels, injects visual semantic prompts to enhance frame-level audio understanding, and then applies progressive contrastive alignment to map audio categories to image regions without mask annotations. Experiments show PCAS achieves state-of-the-art performance among weakly supervised methods on AVS and remains competitive with fully supervised baselines on AVSS, validating its effectiveness.

---

## 论文详细总结（自动生成）

以下是对论文《Look, Listen and Segment: Towards Weakly Supervised Audio-visual Semantic Segmentation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：音视频语义分割（AVSS）旨在实现像素级的发声对象识别与分割，但现有的全监督方法极度依赖昂贵的**逐帧像素级标注**。
*   **研究动机**：为了降低标注成本，本文首次提出了**弱监督音视频语义分割（WSAVSS）**任务，即仅利用**视频级类别标签**（Video-level labels）来实现像素级的语义分割。
*   **整体含义**：该研究探索了在缺乏密集标注的情况下，如何通过跨模态对齐技术，让模型学会“看”图像、“听”声音并最终完成“分割”。

### 2. 论文提出的方法论：PCAS 框架
作者提出了**渐进式跨模态语义对齐（PCAS）**框架，将其分解为三个逻辑步骤：

*   **先看后听（Looking-before-Listening）**：
    *   **核心思想**：利用视觉先验引导音频理解。
    *   **关键技术：时序视觉提示（TVP）**。将视觉编码器提取的帧级语义特征作为“提示标记（Prompt Tokens）”插入音频编码器的序列中。这使得模型在没有帧级标注的情况下，也能实现精确的帧级音频事件定位。
*   **先听后分割（Listening-before-Segmentation）**：
    *   **核心思想**：在充分理解音频语义的基础上，通过对比学习实现细粒度的空间对齐。
    *   **关键技术：渐进式对比学习**：
        1.  **CMC（跨模态对比）**：实现全局音视频语义的粗粒度对齐。
        2.  **CMPC（跨模态补丁对比）**：计算音频特征与视觉补丁（Patch）之间的相似度，通过设定阈值生成伪标签，强化发声区域与背景的区分度。
        3.  **CMCC（跨模态类别对比）**：通过局部裁剪（Crop）与全局特征的对比，迫使模型关注发声对象并抑制静默区域。
*   **分割头**：利用类激活图（CAM）生成伪掩码，并结合密集条件随机场（Dense CRF）进行精细化处理。

### 3. 实验设计
*   **数据集**：
    *   **AVS-S4**（单声源）和 **AVS-MS3**（多声源）：用于与现有的弱监督音视频分割（WSAVS）方法对比。
    *   **AVS-Semantic**：用于与全监督音视频语义分割（AVSS）方法对比，验证弱监督性能的上限。
*   **Benchmark 与对比方法**：
    *   **弱监督基准**：CAM, EZ-VSL, C2AM, WS-AVS 等。
    *   **全监督基准**：AVS, CATR, AVSegFormer, AVSAC, AVS-Mamba, COMBO 等。
*   **评价指标**：使用平均交并比（mIoU）和 F-score。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量及总训练时长。
*   **训练细节**：提到使用 Adam 优化器，训练 9 个 epoch（含 2 个 warm-up epoch），输入视频帧尺寸为 448x448 或 224x224，音频长度为 10 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流子数据集上进行了详尽的对比实验。
    *   **消融实验**：针对 TVP 模块、CMC、CMPC、CMCC 模块分别进行了组合验证，证明了每个组件的必要性。
    *   **可视化分析**：提供了 t-SNE 特征聚类可视化和定性的分割结果对比图。
*   **充分性评价**：实验设计较为充分，涵盖了从单源到多源、从弱监督到全监督的横向与纵向对比，客观地展示了该方法在边界处理和语义映射上的优势。

### 6. 论文的主要结论与发现
*   **性能领先**：PCAS 在弱监督设置下显著优于现有的 WSAVS 方法（如在 AVS-S4 上 mIoU 提升了约 22%）。
*   **逼近全监督**：在最具挑战性的 AVS-Semantic 任务中，PCAS（使用 ViT-base）的性能甚至超过了部分早期的全监督模型（如 AVS, CATR），证明了弱监督路径的巨大潜力。
*   **视觉提示的重要性**：实验证明，引入视觉提示（TVP）能极大增强模型对音频事件的帧级定位能力。

### 7. 优点
*   **任务创新**：首次定义并解决了弱监督音视频语义分割（WSAVSS）这一更具实际价值的任务。
*   **逻辑严密**：提出的“先看、再听、后分割”符合人类感知逻辑，渐进式对齐策略有效缓解了弱监督下的噪声干扰。
*   **即插即用**：对 Backbone（ResNet, ViT, PVT）具有良好的鲁棒性和适配性。

### 8. 不足与局限
*   **计算开销**：虽然是弱监督，但引入了多轮对比学习和视觉提示，推理或训练时的计算复杂度可能高于简单的分类网络。
*   **伪标签依赖**：性能仍受限于 CAM 生成的初始伪标签质量，对于极小物体或复杂背景下的发声源，可能存在漏检或误检。
*   **算力信息缺失**：缺乏硬件资源消耗的详细数据，不利于评估其在边缘设备上的部署可行性。

（完）
