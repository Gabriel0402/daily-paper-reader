---
title: "DriveVLM-RL: Neuroscience-Inspired Reinforcement Learning with Vision-Language Models for Safe and Deployable Autonomous Driving"
title_zh: DriveVLM-RL：受神经科学启发的视觉语言模型强化学习，用于安全且可部署的自动驾驶
authors: "Zilin Huang, Zihao Sheng, Zhengyang Wan, Yansong Qu, Junwei You, Sicong Jiang, Sikai Chen"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18315v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于安全自动驾驶决策的VLM-RL
tldr: DriveVLM-RL是一种受神经科学启发的强化学习框架，旨在解决自动驾驶中传统RL奖励设计难和视觉语言模型（VLM）推理延迟高的问题。该框架通过双路径架构将VLM的语义理解能力转化为奖励信号，并采用异步训练机制，使VLM仅在训练阶段发挥作用。实验证明，该方法在提升驾驶安全性和泛化能力的同时，确保了部署时的实时性。
selection_source: fresh_fetch
motivation: 传统强化学习缺乏语义理解且探索风险高，而视觉语言模型虽具备语义能力但推理延迟大，难以直接用于实时控制。
method: 提出双路径架构，结合CLIP空间评估与VLM动态风险推理生成语义奖励，并通过异步训练和部署时移除VLM保证实时性。
result: 在CARLA模拟器中，该方法在避障、任务成功率和泛化性方面表现优异，且在无显式碰撞惩罚的情况下仍具强鲁棒性。
conclusion: DriveVLM-RL为将大模型集成到自动驾驶系统提供了一种兼顾语义安全理解与实时部署可行性的新范式。
---

## 摘要
尽管端到端学习方法取得了快速进展，但确保自动驾驶车辆的安全决策仍然是一个根本性挑战。传统的强化学习（RL）方法依赖于人工设计的奖励或稀疏的碰撞信号，这无法捕捉安全驾驶所需的丰富上下文理解，并导致在现实场景中不可避免地出现不安全探索。最近的视觉语言模型（VLM）提供了极具前景的语义理解能力；然而，它们的高推理延迟和易产生幻觉的特性阻碍了其在实时车辆控制中的直接应用。为了解决这些局限性，本文提出了 DriveVLM-RL，这是一个受神经科学启发的框架，通过双通路架构将 VLM 集成到强化学习中，以实现安全且可部署的自动驾驶。该框架将语义奖励学习分解为：一个用于连续空间安全评估的静态通路（Static Pathway），使用基于 CLIP 的对比语言目标；以及一个用于注意力门控多帧语义风险推理的动态通路（Dynamic Pathway），使用轻量级检测器和大型 VLM。一种分层奖励综合机制将语义信号与车辆状态融合，而异步训练流水线则将昂贵的 VLM 推理与环境交互解耦。所有 VLM 组件仅在离线训练期间使用，并在部署时移除，从而确保了实时可行性。在 CARLA 模拟器中的实验表明，该方法在碰撞避免、任务成功率以及跨多种交通场景的泛化能力方面均有显著提升，包括在没有明确碰撞惩罚的设置下表现出强大的鲁棒性。这些结果表明，DriveVLM-RL 为将基础模型集成到自动驾驶中提供了一种实用的范式，且不损害实时可行性。演示视频和代码可在以下网址获取：https://zilin-huang.github.io/DriveVLM-RL-website/

## Abstract
Ensuring safe decision-making in autonomous vehicles remains a fundamental challenge despite rapid advances in end-to-end learning approaches. Traditional reinforcement learning (RL) methods rely on manually engineered rewards or sparse collision signals, which fail to capture the rich contextual understanding required for safe driving and make unsafe exploration unavoidable in real-world settings. Recent vision-language models (VLMs) offer promising semantic understanding capabilities; however, their high inference latency and susceptibility to hallucination hinder direct application to real-time vehicle control. To address these limitations, this paper proposes DriveVLM-RL, a neuroscience-inspired framework that integrates VLMs into RL through a dual-pathway architecture for safe and deployable autonomous driving. The framework decomposes semantic reward learning into a Static Pathway for continuous spatial safety assessment using CLIP-based contrasting language goals, and a Dynamic Pathway for attention-gated multi-frame semantic risk reasoning using a lightweight detector and a large VLM. A hierarchical reward synthesis mechanism fuses semantic signals with vehicle states, while an asynchronous training pipeline decouples expensive VLM inference from environment interaction. All VLM components are used only during offline training and are removed at deployment, ensuring real-time feasibility. Experiments in the CARLA simulator show significant improvements in collision avoidance, task success, and generalization across diverse traffic scenarios, including strong robustness under settings without explicit collision penalties. These results demonstrate that DriveVLM-RL provides a practical paradigm for integrating foundation models into autonomous driving without compromising real-time feasibility. Demo video and code are available at: https://zilin-huang.github.io/DriveVLM-RL-website/

---

## 论文详细总结（自动生成）

### DriveVLM-RL：受神经科学启发的视觉语言模型强化学习总结

#### 1. 核心问题与整体含义（研究动机和背景）
自动驾驶（AV）在复杂城市环境中的安全决策仍面临巨大挑战。
*   **传统强化学习（RL）的局限：** 依赖人工设计的奖励函数或稀疏的碰撞信号，缺乏对交通语义（如行人意图、上下文风险）的理解，且在训练中必须经历实际碰撞才能学习安全，这在现实部署中是不可接受的。
*   **视觉语言模型（VLM）的局限：** 虽然具备强大的语义推理能力，但推理延迟高（500-2000ms），且存在幻觉风险，难以直接用于实时车辆控制。
*   **研究动机：** 受人类大脑“习惯性”与“审慎性”双通路视觉处理机制的启发，论文旨在将 VLM 的语义知识集成到 RL 的奖励设计中，既能提供丰富的语义监督，又能通过异步架构确保部署时的实时性。

#### 2. 方法论：核心思想与关键技术
DriveVLM-RL 提出了一个双通路认知架构，将 VLM 作为“语义老师”而非直接控制器：
*   **静态通路（Static Pathway）：** 模拟大脑顶叶皮层。利用预训练的 CLIP 模型，计算鸟瞰图（BEV）与固定“对比语言目标”（如“道路畅通” vs “发生车祸”）之间的语义相似度，提供连续的空间安全评估。
*   **动态通路（Dynamic Pathway）：** 模拟大脑的注意力-前额叶回路。
    *   **注意力门控：** 使用轻量级检测模型（YOLOv8）识别关键对象（行人、骑行者等）。仅在检测到风险对象时触发昂贵的大型 VLM（LVLM）推理，过滤掉 70%-80% 的常规驾驶帧。
    *   **多帧语义推理：** 被触发后，LVLM（如 Qwen3-VL）分析多帧序列，生成动态风险描述（如“行人正准备横穿马路”），并将其转化为动态奖励信号。
*   **分层奖励综合（Hierarchical Reward Synthesis）：** 将静态/动态语义奖励与车辆状态（速度跟踪、车道中心保持、航向角、稳定性）进行**乘法融合**。这种设计确保了只有在满足语义安全的前提下，追求速度才有意义。
*   **异步训练流水线：** 采用多线程架构，将环境交互、VLM 奖励标注和策略更新解耦。**关键点：** 部署时完全移除 VLM 和检测器，仅运行训练好的策略网络，实现零推理延迟。

#### 3. 实验设计
*   **实验平台：** 高保真城市驾驶模拟器 CARLA。
*   **场景设置：** 包含车辆、行人、摩托车、自行车等异构交通流。训练在 Town 2 进行，测试跨越 Town 1-5。
*   **Benchmark 与对比方法：** 共对比了 **13 种** 方法，涵盖：
    *   **专家设计奖励：** TIRL-SAC, Chen-SAC, ASAP-RL, ChatScene。
    *   **LLM 自动生成奖励：** Revolve, Revolve-auto。
    *   **机器人领域 VLM 奖励：** VLM-SR, RoboCLIP, VLM-RM。
    *   **驾驶领域 VLM 奖励：** LORD, VLM-RL（作者前期工作）。

#### 4. 资源与算力
*   **硬件配置：** 工作站配备了 **3 台 NVIDIA RTX A6000 GPU**（每台 48GB 显存）、AMD Ryzen Threadripper Pro 7985WX 处理器（64 核）以及 512GB 内存。
*   **模型细节：** 使用了 OpenCLIP (ViT-bigG-14)、YOLOv8-medium 和 Qwen3-VL-4B-Instruct。
*   **训练时长：** 论文未明确给出总训练小时数，但提到异步流水线在 1M（一百万）步交互中实现了高效的奖励标注。

#### 5. 实验数量与充分性
实验设计非常详尽且具有说服力：
*   **基础性能实验：** 在 Town 2 进行了多随机种子的训练与测试对比。
*   **极端消融实验：** 进行了“无碰撞奖励”测试（R_penalty = 0），验证模型是否仅靠语义理解就能学会避障。
*   **泛化性实验：** 跨 5 个不同城镇（Town 1-5）测试，以及不同交通密度（空载、常规、拥堵）下的表现。
*   **算法无关性实验：** 将奖励机制从离策（SAC）迁移到在策（PPO）算法上进行验证。
*   **组件消融：** 针对视角选择、门控机制、奖励融合方式、LVLM 模型大小等进行了多组消融。
*   **客观性：** 实验涵盖了效率（速度、路程）与安全（碰撞率、碰撞速度）的双重指标，对比基准广泛。

#### 6. 主要结论与发现
*   **安全性显著提升：** 相比于最强的基准方法 ChatScene-SAC，DriveVLM-RL 的碰撞率降低了 **84%**。
*   **语义预判能力：** 在完全取消碰撞惩罚的极端情况下，DriveVLM-RL 的碰撞次数比基准方法减少了 **67%**，证明其内化了预测性安全准则。
*   **实时部署可行：** 成功解决了 VLM 推理慢的问题，部署时无需调用大模型。
*   **泛化能力强：** 在未见过的城镇和高密度交通中，其成功率和碰撞严重程度均优于现有方法。

#### 7. 优点与亮点
*   **神经科学启发：** 模仿人类大脑的快慢路径切换，设计优雅且符合直觉。
*   **注意力门控机制：** 极大地降低了训练时的计算开销，同时避免了无关帧对奖励信号的噪声干扰。
*   **对比语言目标（CLG）：** 解决了单一语言目标语义模糊的问题，使奖励信号更具判别力。
*   **异步架构：** 为大模型落地实时控制系统提供了一个可行的工程范式。

#### 8. 不足与局限
*   **特定场景性能下降：** 在结构差异巨大的环境（如高速公路 Town 4 或多层道路 Town 5）中，性能虽优于基准但仍有提升空间，说明语义词汇表需进一步扩充。
*   **Sim-to-Real 差距：** 实验目前仅限于仿真环境，现实世界中的传感器噪声、天气变化和视觉域偏移仍是挑战。
*   **固定词汇表限制：** 动态通路的风险描述目前依赖于预定义的参考词汇表，可能限制了对极少数长尾场景的描述灵活性。

（完）
