---
title: Adaptive Robust Estimator for Multi-Agent Reinforcement Learning
title_zh: 面向多智能体强化学习的自适应鲁棒估计器
authors: "Zhongyi Li, Wan Tian, Jingyu Chen, Kangyao Huang, Huiming Zhang, Hui Yang, Tao Ren, Jinyang Jiang, Yijie Peng, Yikun Ban, Fuzhen Zhuang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21574v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 大语言模型协作推理的多智能体强化学习
tldr: 该研究针对多智能体强化学习在协作推理中面临的信用分配困难和奖励信号噪声导致训练不稳定的问题，提出了一个鲁棒的框架。该框架包含DACR结构化协作流程，将推理分解为回答、评论和重写三个阶段以明确贡献归属；同时引入ARE自适应鲁棒估计器，通过优化批次经验均值估计来抵御重尾和噪声奖励。实验证明该方法在数学推理和具身智能任务中表现优异，显著提升了训练的稳定性和鲁棒性。
selection_source: fresh_fetch
motivation: 解决多智能体协作推理中交互模糊导致的信用分配难题，以及噪声奖励引发的策略优化不稳定问题。
method: 提出了由DACR（回答-评论-重写）三阶段协作流程和ARE（自适应鲁棒估计器）组成的强化学习框架。
result: 在数学推理和具身智能基准测试中，该方法在同质和异质设定下均优于基线，且对奖励噪声具有极强的抵抗力。
conclusion: 通过结构化协作和鲁棒均值估计，该框架有效解决了多智能体强化学习中的优化失败问题，实现了更稳定的策略提升。
---

## 摘要
多智能体协作已成为增强大语言模型推理能力的强大范式，但它面临着交互层面的模糊性，这种模糊性模糊了生成、批判和修订之间的界限，使得跨智能体的信用分配（credit assignment）变得困难。此外，在这种环境下的策略优化容易受到重尾（heavy-tailed）和噪声奖励的影响，这可能会使优势估计产生偏差，并引发不稳定甚至发散的训练。为了解决这两个问题，我们提出了一种用于协作推理的鲁棒多智能体强化学习框架，由两个组件组成：双智能体“回答-批判-重写”（DACR）和自适应鲁棒估计器（ARE）。DACR 将推理分解为一个结构化的三阶段流水线：回答、批判和重写，同时能够显式地将每个智能体的边际贡献归因于其合作伙伴的性能。ARE 在多智能体策略优化过程中提供批次经验均值的鲁棒估计。在数学推理和具身智能基准测试中，即使在存在噪声奖励的情况下，我们的方法在同构和异构设置下均始终优于基准方法。这些结果表明，该方法对奖励噪声具有更强的鲁棒性，且训练动态更稳定，有效防止了由噪声奖励信号引起的优化失败。

## Abstract
Multi-agent collaboration has emerged as a powerful paradigm for enhancing the reasoning capabilities of large language models, yet it suffers from interaction-level ambiguity that blurs generation, critique, and revision, making credit assignment across agents difficult. Moreover, policy optimization in this setting is vulnerable to heavy-tailed and noisy rewards, which can bias advantage estimation and trigger unstable or even divergent training. To address both issues, we propose a robust multi-agent reinforcement learning framework for collaborative reasoning, consisting of two components: Dual-Agent Answer-Critique-Rewrite (DACR) and an Adaptive Robust Estimator (ARE). DACR decomposes reasoning into a structured three-stage pipeline: answer, critique, and rewrite, while enabling explicit attribution of each agent's marginal contribution to its partner's performance. ARE provides robust estimation of batch experience means during multi-agent policy optimization. Across mathematical reasoning and embodied intelligence benchmarks, even under noisy rewards, our method consistently outperforms the baseline in both homogeneous and heterogeneous settings. These results indicate stronger robustness to reward noise and more stable training dynamics, effectively preventing optimization failures caused by noisy reward signals.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Robust Estimator for Multi-Agent Reinforcement Learning》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：大语言模型（LLM）通过多智能体协作（如解决者与评估者配对）能显著提升推理能力。强化学习（RL）已成为微调此类协作系统的常用手段。
*   **核心问题**：
    1.  **信用分配难题**：在多轮对话中，难以量化每个智能体对最终正确答案的边际贡献。
    2.  **奖励信号不稳定性**：推理任务的奖励通常由验证器或奖励模型生成，往往具有**噪声、偏斜且重尾（Heavy-tailed）**的特征。
    3.  **优化算法脆弱性**：主流算法（如 GRPO）依赖批次经验均值进行归一化，极少数极端离群值（Outliers）就会扭曲优势估计，导致训练震荡甚至发散。

### 2. 方法论
论文提出了一个结合结构化交互与鲁棒优化的框架，包含两个核心组件：

*   **DACR（双智能体回答-批判-重写）协议**：
    *   **三阶段流程**：1. 独立回答（Answer）；2. 交叉批判（Critique，交换答案并指出错误）；3. 结合反馈重写（Rewrite）。
    *   **交叉评价奖励机制**：智能体 $i$ 的总奖励由其最终答案质量和“协作效用”组成。公式表达为：$R_{total, i} = R_{rw, i} + \gamma (R_{rw, j} - R_{ans, j})$。这种设计通过奖励合作伙伴的进步来显式解决信用分配问题。
*   **ARE（自适应鲁棒估计器）**：
    *   **核心思想**：改进了经典的“均值的中位数”（Median-of-Means, MoM）估计器。
    *   **技术细节**：将样本分成若干块，在块内不直接求均值，而是通过最小化**自适应鲁棒损失函数（Barron Loss）**来估计位置参数。该损失函数通过形状参数 $\alpha$ 和尺度参数 $c$ 自动调节对离群值的敏感度。
    *   **优化流程**：采用**渐进非凸性（GNC）**和**迭代重权最小二乘法（IRLS）**来解决非凸优化问题，确保在存在重尾噪声时仍能获得稳定的优势函数估计。

### 3. 实验设计
*   **数学推理任务**：
    *   **数据集**：在 MATH 7.5k 上训练；在 MATH500（分布内）、AIME24/25、AMC23、Gaokao2023en、MinervaMath（分布外）上测试。
    *   **模型**：Qwen2.5 (1.5B/7B)、Llama3.1 (8B) 的同构与异构组合。
    *   **对比方法**：基础模型、基于标准 GRPO 的多智能体协作基线。
*   **具身智能任务（无人机导航）**：
    *   **场景**：视觉语言导航（VLN）。
    *   **基准**：FlightGPT。
    *   **模型**：Qwen2-VL-2B。
*   **噪声模拟**：在奖励中注入重尾的柯西噪声（Cauchy noise），测试算法在极端干扰下的表现。

### 4. 资源与算力
*   **硬件环境**：使用了 **1 台拥有 8 张 NVIDIA A800 GPU 的节点**。
*   **软件框架**：使用 Ray 进行分布式训练编排，vLLM 进行推理加速（4 个推理引擎，张量并行度为 2）。
*   **训练细节**：启用了梯度检查点（Gradient Checkpointing）和 Adam 参数卸载（Offloading）以优化显存。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 8B 不同规模的模型，测试了同构（相同模型协作）和异构（不同模型协作）多种组合。
*   **消融实验**：针对“三阶段交互协议”和“ARE 估计器”分别做了消融，验证了各组件的独立贡献。
*   **鲁棒性测试**：通过改变噪声组比例（20%-80%）和组内污染率，系统地评估了抗干扰能力。
*   **充分性评价**：实验设计较为全面，不仅在纯文本推理上验证，还跨领域扩展到了多模态具身智能任务，证明了方法的通用性。

### 6. 主要结论
*   **性能提升**：在数学推理任务中，ARE 显著优于 GRPO 基线，例如在 AMC23 基准上，Qwen2.5-1.5B 的准确率从 22.5% 提升至 32.5%。
*   **训练稳定性**：ARE 有效抑制了由噪声奖励引起的梯度尖峰，训练曲线更加平滑，收敛更稳定。
*   **协作价值**：DACR 协议通过结构化反馈显著提升了推理质量，证明了“批判-重写”机制比单纯的独立尝试更有效。
*   **泛化能力**：在无人机导航任务中，ARE 提升了成功率（SR）并降低了导航误差（NE），尤其在未见过的环境中表现更优。

### 7. 优点与亮点
*   **理论支撑扎实**：论文不仅提出了算法，还提供了关于 ARE 在有限方差和重尾分布下的渐近正态性及偏差上界的理论证明。
*   **解决痛点**：直接针对 RL 中最棘手的“奖励噪声”问题，ARE 相比传统的启发式裁剪（Clipping）更具统计学依据。
*   **架构通用**：DACR 协议和 ARE 估计器均具有模块化特征，可以轻松集成到现有的 RL 框架（如 PPO, GRPO）中。

### 8. 不足与局限
*   **计算开销**：ARE 涉及内部的迭代优化（GNC-IRLS），虽然论文称其开销可控，但在超大规模批次训练时，相比简单的均值计算仍会增加一定的计算延迟。
*   **参数敏感性**：尽管 ARE 具有自适应性，但块的数量 $k$ 和初始超参数的选择可能仍会对特定任务的收敛速度产生影响。
*   **多智能体规模**：实验主要集中在双智能体协作，尚未探讨在更多智能体（如 5 个以上）复杂交互网络中的表现。

（完）
