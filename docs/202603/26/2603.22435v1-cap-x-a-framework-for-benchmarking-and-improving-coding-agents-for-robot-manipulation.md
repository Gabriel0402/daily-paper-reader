---
title: "CaP-X: A Framework for Benchmarking and Improving Coding Agents for Robot Manipulation"
title_zh: CaP-X：一个用于基准测试和改进机器人操纵编程智能体的框架
authors: "Max Fu, Justin Yu, Karim El-Refai, Ethan Kou, Haoru Xue, Huang Huang, Wenli Xiao, Guanzhi Wang, Fei-Fei Li, Guanya Shi, Jiajun Wu, Shankar Sastry, Yuke Zhu, Ken Goldberg, Linxi \"Jim\" Fan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22435v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于机器人操作的视觉-语言-动作（VLA）方法
tldr: 本研究提出了CaP-X框架，旨在系统地评估和提升机器人操作中的“代码即策略”（Code-as-Policy）智能体。通过CaP-Gym环境和CaP-Bench基准测试，研究发现模型性能高度依赖人工抽象，但通过增加测试时计算（如多轮交互、反馈和自动技能合成）可显著提升鲁棒性。基于此，作者开发了CaP-Agent0和CaP-RL，在仿真和真实机器人上均实现了人类水平的可靠性，为具身智能提供了新的研究范式。
selection_source: fresh_fetch
motivation: 尽管“代码即策略”方法在机器人控制中展现出潜力，但其作为自主控制器的有效性及对人工抽象的依赖性仍缺乏系统性研究。
method: 开发了包含交互式环境CaP-Gym、多维度评估基准CaP-Bench、免训练框架CaP-Agent0以及结合强化学习的CaP-RL的综合性开源框架。
result: 实验揭示了模型性能随抽象层级降低而下降的趋势，但通过扩展测试时计算和结构化反馈，智能体在低级原语操作上的鲁棒性得到了显著增强。
conclusion: CaP-X为具身代码智能体的发展提供了标准化的平台，证明了通过推理增强和可验证奖励的强化学习可以实现跨仿真与现实的高可靠机器人操作。
---

## 摘要
“代码即策略”（Code-as-Policy）探讨了可执行代码如何补充数据密集型视觉-语言-动作（VLA）方法，但其作为具身操纵自主控制器的有效性仍有待深入研究。我们提出了 CaP-X，这是一个用于系统研究机器人操纵中“代码即策略”智能体的开源框架。其核心是 CaP-Gym，这是一个交互式环境，智能体通过合成和执行组合了感知与控制原语的程序来控制机器人。在此基础上，CaP-Bench 在不同的抽象级别、交互程度和感知落地方面对前沿语言和视觉-语言模型进行了评估。通过对 12 个模型的测试，CaP-Bench 揭示了一个一致的趋势：性能随人工设计的抽象而提升，但随着这些先验知识的移除而下降，暴露了对设计者脚手架的依赖。同时，我们观察到这种差距可以通过扩展智能体测试时计算来弥补——通过多轮交互、结构化执行反馈、视觉差异化、自动技能合成和集成推理——即使智能体在底层原语上运行，也能显著提高鲁棒性。这些发现使我们推导出 CaP-Agent0，这是一个无需训练的框架，在仿真和真实实体上的多项操纵任务中恢复了人类水平的可靠性。我们进一步引入了 CaP-RL，表明具有可验证奖励的强化学习可以提高成功率，并以极小的差距实现从仿真到现实（sim2real）的迁移。总之，CaP-X 为推进具身编程智能体提供了一个有原则的开源平台。

## Abstract
"Code-as-Policy" considers how executable code can complement data-intensive Vision-Language-Action (VLA) methods, yet their effectiveness as autonomous controllers for embodied manipulation remains underexplored. We present CaP-X, an open-access framework for systematically studying Code-as-Policy agents in robot manipulation. At its core is CaP-Gym, an interactive environment in which agents control robots by synthesizing and executing programs that compose perception and control primitives. Building on this foundation, CaP-Bench evaluates frontier language and vision-language models across varying levels of abstraction, interaction, and perceptual grounding. Across 12 models, CaP-Bench reveals a consistent trend: performance improves with human-crafted abstractions but degrades as these priors are removed, exposing a dependence on designer scaffolding. At the same time, we observe that this gap can be mitigated through scaling agentic test-time computation--through multi-turn interaction, structured execution feedback, visual differencing, automatic skill synthesis, and ensembled reasoning--substantially improves robustness even when agents operate over low-level primitives. These findings allow us to derive CaP-Agent0, a training-free framework that recovers human-level reliability on several manipulation tasks in simulation and on real embodiments. We further introduce CaP-RL, showing reinforcement learning with verifiable rewards improves success rates and transfers from sim2real with minimal gap. Together, CaP-X provides a principled, open-access platform for advancing embodied coding agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《CaP-X: A Framework for Benchmarking and Improving Coding Agents for Robot Manipulation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了**“代码即策略”（Code-as-Policy, CaP）**在机器人操纵中的应用。
*   **研究动机**：传统的机器人控制依赖专家编写复杂程序（缺乏灵活性），而现代的视觉-语言-动作（VLA）模型依赖海量数据（缺乏解释性和泛化性）。CaP 试图利用大语言模型（LLM）自动生成控制代码来结合两者的优点。
*   **核心问题**：现有的 CaP 研究大多依赖人类预设的高级 API（如 `stack_objects()`），这掩盖了模型在底层逻辑上的无能。论文旨在研究：当移除这些“人工脚手架”时，智能体的表现如何？以及如何通过增加“测试时计算”（推理增强）来提升其在底层原语上的鲁棒性。

### 2. 方法论：核心思想与关键技术
论文提出了一个名为 **CaP-X** 的综合框架，包含以下四个核心组件：
*   **CaP-Gym**：一个基于 Gymnasium 的交互式环境，支持 REPL（读取-求值-打印循环）模式。它集成了 RoboSuite、LIBERO-PRO 和 BEHAVIOR 等模拟器，将感知（如 SAM3, Molmo）和控制（如 IK 求解器）封装为可调用的原语。
*   **CaP-Bench**：一个多维度的基准测试，从**抽象级别**（高级 vs 底层）、**时间交互**（单轮 vs 多轮）和**感知落地**（文本反馈 vs 视觉差异）三个维度评估模型。
*   **CaP-Agent0（免训练框架）**：
    *   **视觉差异模块（VDM）**：将视觉观察转化为结构化自然语言，描述前后两帧的变化。
    *   **自动合成技能库**：从成功的底层执行记录中自动提取并推广通用的工具函数。
    *   **并行推理与集成**：同时采样多个模型的代码方案，由中心智能体进行合成。
*   **CaP-RL（强化学习增强）**：使用 **GRPO（群体相对策略优化）** 算法，通过环境提供的可验证奖励（如任务是否成功）对代码生成模型进行后训练（Post-training）。

### 3. 实验设计
*   **评估对象**：12 个前沿模型，包括 GPT-5.2、Gemini-3-Pro、Claude 4.5、DeepSeek-V3.1、Qwen-2.5-Coder 等。
*   **任务场景**：
    *   **核心任务**：7 个基础操纵任务（抬起、堆叠、擦拭、插拔等）。
    *   **扩展任务**：LIBERO-PRO（30 个长程任务）和 BEHAVIOR（移动操纵任务）。
    *   **真实世界**：在 Franka Panda 和 AgiBot G1 机器人上进行零样本（Zero-shot）测试。
*   **对比基准**：将模型生成的代码与人类专家编写的代码在相同原语下进行对比。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量。
*   **训练细节**：在 CaP-RL 部分，提到对 Qwen2.5-Coder-7B 模型进行了 50 次迭代的强化学习训练。由于感知和控制原语被实现为无状态服务，框架支持高吞吐量的并行评估。

### 5. 实验数量与充分性
*   **实验规模**：每个任务/层级进行 **100 次试验**，总计评估了 12 个模型在 8 个不同评估层级（S1-S4, M1-M4）的表现。
*   **消融实验**：针对 CaP-Agent0 的各个组件（VDM、技能库、并行查询）做了详细的消融研究。
*   **充分性评价**：实验设计非常充分且客观。它不仅涵盖了主流的闭源和开源模型，还通过跨模拟器（3 个不同平台）和跨实体（2 种机器人）的测试验证了通用性。通过引入“特权信息层级（S1）”成功排除了感知噪声对推理能力的干扰。

### 6. 主要结论与发现
*   **抽象依赖性**：模型性能高度依赖人类设计的 API。当 API 变得底层时，所有模型的成功率均大幅下降。
*   **测试时计算的价值**：多轮交互（纠错）和视觉差异反馈（VDM）能显著弥补底层原语带来的性能损失。
*   **免训练达到专家水平**：CaP-Agent0 在多个任务上达到了接近甚至超过人类专家的可靠性，且优于经过微调的 VLA 模型。
*   **Sim2Real 鸿沟极小**：由于代码操作的是抽象 API 而非原始像素，CaP-RL 训练的模型可以几乎无损地从仿真迁移到真实机器人。

### 7. 优点（亮点）
*   **系统性强**：首次系统性地解构了代码智能体对 API 抽象层级的依赖。
*   **闭环反馈**：引入 VDM 解决了视觉信息与代码生成之间的对齐问题，比直接输入图像更有效。
*   **自动进化**：技能库的自动合成展示了智能体自我提升的潜力。
*   **开源贡献**：提供了一个统一的、支持多种模拟器的交互式编程控制接口。

### 8. 不足与局限
*   **接触密集型任务挑战**：对于需要高频连续反馈的任务（如精细插入、倒水），纯代码生成方式仍显笨拙，不如端到端 VLA 平滑。
*   **感知模块限制**：性能上限仍受限于底层感知原语（如 SAM3 或 Molmo）的准确性，若感知报错，代码逻辑再强也无法完成任务。
*   **推理延迟**：CaP-Agent0 涉及多模型并行查询和多轮交互，在实时性要求极高的场景下可能存在延迟。

（完）
