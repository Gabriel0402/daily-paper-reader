---
title: "CATNAV: Cached Vision-Language Traversability for Efficient Zero-Shot Robot Navigation"
title_zh: CATNAV：用于高效零样本机器人导航的缓存式视觉-语言可通行性
authors: "Aditya Potnis, Francisco Affonso, Shreya Gummadi, Naveen Kumar Uppalapati, Girish Chowdhary"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22800v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 基于 VLM 的零样本机器人导航与轨迹选择
tldr: "CATNAV是一个针对非结构化环境的零样本机器人导航框架。它利用多模态大模型（VLM）根据机器人自身能力生成代价地图，无需特定任务训练。通过引入视觉语义缓存机制，该框架能识别场景新颖性并复用评估结果，将在线查询量降低了85.7%。此外，它还包含基于VLM的轨迹选择模块，在四足机器人实验中显著提升了导航成功率并减少了违规行为。"
selection_source: fresh_fetch
motivation: 传统导航难以在非结构化环境中根据机器人具体物理能力评估通行风险，且大模型在线推理成本高昂。
method: 提出一种结合视觉语义缓存机制和VLM轨迹选择的导航框架，实现高效的具身感知代价地图生成。
result: "在四足机器人实验中，CATNAV将VLM查询量减少了85.7%，目标到达率提升了10%，行为约束违规减少了33%。"
conclusion: CATNAV证明了通过缓存机制和视觉推理，可以实现高效、安全且具备具身感知能力的零样本机器人导航。
---

## 摘要
在非结构化环境中导航需要根据机器人的物理能力评估通行风险，这一挑战因机器人形态而异。我们提出了 CATNAV，这是一个代价感知的可通行性导航框架，它利用多模态大语言模型（LLM）在无需特定任务训练的情况下，生成零样本、形态感知的代价地图（costmap）。我们引入了一种视觉语义缓存机制，用于检测场景的新颖性，并对语义相似的帧重用先前的风险评估，从而减少了 85.7% 的在线视觉语言模型（VLM）查询。此外，我们引入了一个基于 VLM 的轨迹选择模块，该模块通过视觉推理评估候选方案，以便在给定行为约束的情况下选择最安全的路径。我们在室内外非结构化环境中的四足机器人上评估了 CATNAV，并与最先进的视觉-语言-动作（VLA）基准模型进行了比较。在五项导航任务中，CATNAV 的平均目标到达率提高了 10 个百分点，行为约束违规减少了 33%。

## Abstract
Navigating unstructured environments requires assessing traversal risk relative to a robot's physical capabilities, a challenge that varies across embodiments. We present CATNAV, a cost-aware traversability navigation framework that leverages multimodal LLMs for zero-shot, embodiment-aware costmap generation without task-specific training. We introduce a visuosemantic caching mechanism that detects scene novelty and reuses prior risk assessments for semantically similar frames, reducing online VLM queries by 85.7%. Furthermore, we introduce a VLM-based trajectory selection module that evaluates proposals through visual reasoning to choose the safest path given behavioral constraints. We evaluate CATNAV on a quadruped robot across indoor and outdoor unstructured environments, comparing against state-of-the-art vision-language-action baselines. Across five navigation tasks, CATNAV achieves 10 percentage point higher average goal-reaching rate and 33% fewer behavioral constraint violations.

---

## 论文详细总结（自动生成）

以下是对论文《CATNAV: Cached Vision-Language Traversability for Efficient Zero-Shot Robot Navigation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在非结构化环境（如农场、建筑工地或杂乱的室内）中，机器人导航的核心挑战在于**语义后果建模**：即根据机器人自身的物理能力（如底盘高度、运动方式）评估地形或物体的通行风险。
*   **研究动机**：传统学习方法（如强化学习）需要海量数据且难以泛化；现有的视觉语言模型（VLM）虽具备强大的零样本推理能力，但直接用于在线导航面临**高延迟**和**高计算成本**的问题。
*   **核心目标**：开发一个既能利用 VLM 的常识推理进行“具身感知”风险评估，又能通过缓存机制实现高效实时运行的零样本导航框架。

### 2. 方法论：核心思想与关键技术
CATNAV 框架主要由三个核心模块组成：
*   **具身感知的风险推理**：利用多模态 LLM（如 Gemini 3.0 Flash）识别场景物体，并根据输入的机器人物理描述（如“四足机器人，0.3m 离地间隙”）为每个物体分配 0-1 的风险评分。
*   **视觉语义缓存机制（核心创新）**：
    *   使用 CLIP 编码器将当前图像转换为向量，并存储在向量数据库中。
    *   **新颖性检测**：计算当前帧与缓存中最近邻（k-NN）的欧氏距离。若距离小于阈值 $\gamma$，则直接复用或聚合缓存的风险评分；若大于阈值，则判定为新场景，触发 VLM 查询。
    *   该机制通过减少冗余的 API 调用，显著降低了系统延迟。
*   **代价地图构建与轨迹推理**：
    *   **代价投影**：利用 CLIPSeg 进行开放词汇分割，将风险评分映射到像素级，再结合深度信息生成 3D 点云和 2D 占用代价图。
    *   **多方案轨迹生成**：使用 TRRT 算法生成四条候选路径（左、中、右、以及放宽成本限制的“冒险”路径）。
    *   **VLM 轨迹选择**：将四条彩色路径叠加在原始图像上，再次请求 VLM 根据特定的行为指令（如“靠右行驶”、“避开草坪”）选出最佳路径。

### 3. 实验设计
*   **实验平台**：Unitree Go1 四足机器人，搭载 NVIDIA Jetson Orin 边缘计算节点、ZED 2i 深度相机和 GNSS 接收器。
*   **实验场景**：涵盖 5 个真实任务，包括室外人行道导航（带行为约束）、动态行人避障、室内特定物体（如地上的纸张）避让。
*   **基准模型（Benchmark）**：对比了最先进的视觉-语言-动作（VLA）模型 **OmniVLA**。
*   **评估指标**：目标到达率（%）、到目标点的距离（m）、碰撞率（%）以及行为约束违规率（%）。

### 4. 资源与算力
*   **边缘端**：使用 **NVIDIA Jetson Orin** 处理实时感知、点云投影和局部规划。
*   **云端**：通过 4G/LTE 路由器连接互联网，调用 **Gemini 3.0 Flash** API 进行高层语义推理。
*   **训练时长**：论文未提及训练时长，因为该方法是**零样本（Zero-shot）**的，直接利用预训练的 VLM 和 CLIP 模型，无需针对特定任务进行微调。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同的任务场景中各进行了 10 次试验，总计 50 次真实机器人运行记录。
*   **消融实验**：针对缓存机制进行了详细的参数消融（改变最近邻数量 $k$ 和新颖性阈值 $\gamma$），验证了不同配置对查询频率和延迟的影响。
*   **充分性评价**：实验设计较为充分，涵盖了室内外、动静态环境以及不同的行为指令。对比 SOTA VLA 模型证明了其在复杂约束下的优越性，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **效率提升**：视觉语义缓存机制成功将在线 VLM 查询量减少了 **85.7%**，使机器人能够以高达 0.75 m/s 的速度平滑行驶。
*   **性能增强**：相比 OmniVLA，CATNAV 的平均目标到达率提升了 **10 个百分点**。
*   **行为合规**：在遵循特定自然语言指令（如“避开地上的纸”）方面，CATNAV 的违规率降低了 **33%**。
*   **具身感知**：系统能够根据机器人形态（如四足 vs 轮式）动态调整对同一物体的风险评估。

### 7. 优点与亮点
*   **高效性**：通过缓存机制解决了 VLM 导航中普遍存在的延迟和成本痛点。
*   **零样本泛化**：无需任何环境特定的训练数据，仅通过提示词（Prompt）即可适配新环境和新机器人形态。
*   **可解释性**：VLM 轨迹推理模块能够给出选择某条路径的具体理由（Reasoning），增加了系统决策的透明度。
*   **鲁棒性**：k-NN 聚合机制平滑了 VLM 可能产生的单帧误判，提高了风险评估的稳定性。

### 8. 不足与局限
*   **网络依赖**：系统高度依赖 4G/LTE 互联网连接以调用云端 API，在通信受限的环境（如地下或偏远山区）中无法工作。
*   **感知精度限制**：实验发现代价地图在远距离下可能会低估窄小障碍物（如长椅腿）的风险，导致碰撞率在某些任务中略高。
*   **实时性瓶颈**：尽管有缓存，但在遇到全新场景时仍需等待 VLM 响应（约 3-4 秒），可能导致机器人出现短暂顿挫。
*   **长程规划**：目前主要集中在局部避障和短期行为，缺乏对超长距离、复杂逻辑任务的深度规划能力。

（完）
