---
title: "Describe-Then-Act: Proactive Agent Steering via Distilled Language-Action World Models"
title_zh: 先描述后行动：通过蒸馏语言-动作世界模型实现主动式智能体引导
authors: "Massimiliano Pappa, Luca Romani, Valentino Sacco, Alessio Palma, Stéphane Lathuilière, Fabio Galasso, Xavier Alameda-Pineda, Indro Spinelli"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23149v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于主动智能体引导和故障预防的语言-动作世界模型
tldr: 针对安全关键型智能体在执行动作前需预判后果的需求，本文提出DILLO模型。传统视觉模拟世界模型延迟高，DILLO通过跨模态蒸馏，将视觉语言模型（VLM）的知识迁移到轻量级大语言模型（LLM）中，实现从“模拟后行动”到“描述后行动”的范式转变。该方法仅需策略潜状态和计划动作即可预测语义结果，在保持高保真度的同时，推理速度提升14倍，并在MetaWorld和LIBERO任务中显著提升了成功率。
selection_source: fresh_fetch
motivation: 现有的基于视觉模拟的世界模型推理延迟过高，难以满足实时安全预防的需求。
method: 提出DILLO框架，利用VLM作为教师标注轨迹，训练LLM学生模型根据策略潜状态预测动作的语义后果。
result: 在MetaWorld和LIBERO基准测试中，DILLO实现了14倍的推理加速，并将任务平均成功率提升了9.3个百分点。
conclusion: 证明了无需昂贵的视觉生成，仅通过语义预测即可有效引导智能体行为并预防失败。
---

## 摘要
部署安全关键型智能体需要在执行动作之前预判其后果。虽然世界模型为这种主动预见提供了一种范式，但目前依赖视觉模拟的方法会产生极高的延迟，每步通常超过数秒。在这项工作中，我们挑战了视觉处理对于预防失败是必要的这一假设。我们证明，训练好的策略的潜状态结合其计划动作，已经编码了足够的信息来预判动作结果，从而使视觉模拟在预防失败方面变得冗余。为此，我们引入了 DILLO（蒸馏语言-动作世界模型），这是一个快速引导层，将范式从“先模拟后行动”转变为“先描述后行动”。DILLO 通过跨模态蒸馏进行训练，其中特权视觉语言模型教师对离线轨迹进行标注，而以潜状态为条件的大语言模型学生则学习预测语义结果。这创建了一条纯文本推理路径，完全绕过了沉重的视觉生成，比基准方法实现了 14 倍的加速。在 MetaWorld 和 LIBERO 上的实验表明，DILLO 能够生成下一状态的高保真描述，并能引导策略，使各任务的平均回合成功率提高了 9.3 个百分点，最高提升达 15 个百分点。

## Abstract
Deploying safety-critical agents requires anticipating the consequences of actions before they are executed. While world models offer a paradigm for this proactive foresight, current approaches relying on visual simulation incur prohibitive latencies, often exceeding several seconds per step. In this work, we challenge the assumption that visual processing is necessary for failure prevention. We show that a trained policy's latent state, combined with its planned actions, already encodes sufficient information to anticipate action outcomes, making visual simulation redundant for failure prevention. To this end, we introduce DILLO (DIstiLLed Language-ActiOn World Model), a fast steering layer that shifts the paradigm from "simulate-then-act" to "describe-then-act." DILLO is trained via cross-modal distillation, where a privileged Vision Language Model teacher annotates offline trajectories and a latent-conditioned Large Language Model student learns to predict semantic outcomes. This creates a text-only inference path, bypassing heavy visual generation entirely, achieving a 14x speedup over baselines. Experiments on MetaWorld and LIBERO demonstrate that DILLO produces high-fidelity descriptions of the next state and is able to steer the policy, improving episode success rate by up to 15 pp and 9.3 pp on average across tasks.

---

## 论文详细总结（自动生成）

### 论文总结：Describe-Then-Act (DILLO)

#### 1. 核心问题与整体含义（研究动机和背景）
在部署安全关键型智能体（如机器人）时，预判动作后果至关重要。传统的**世界模型（World Models）**通常依赖**视觉模拟**（生成未来图像或视频）来预测后果，但这带来了极高的计算延迟（每步通常需数秒），难以满足实时控制需求。
本文提出了**“潜状态充分性假设”（Latent Sufficiency Hypothesis）**：认为策略网络的内部潜状态（Latent State）已经编码了足够的任务关键信息（如物体几何、距离、接触动力学），无需重新渲染像素即可预测动作结果。基于此，作者提出了 **DILLO**，将范式从“模拟后行动”转变为**“描述后行动”**，旨在实现极低延迟的主动式故障预防。

#### 2. 论文提出的方法论
DILLO 是一个**蒸馏语言-动作世界模型**，其核心流程如下：
*   **跨模态蒸馏**：
    *   **教师模型（VLM）**：在离线阶段，利用具有“特权信息”（如模拟器中的 6DoF 位姿、视觉历史）的视觉语言模型对轨迹进行标注，生成自然语言描述和二元裁决（Positive/Negative）。
    *   **学生模型（DILLO）**：一个轻量级 LLM（如 Gemma-1B/4B），学习仅根据策略的**潜状态 ($z_t$)** 和**计划动作块 ($a_{t:t+k}$)** 来预测教师生成的语义结果。
*   **关键技术细节**：
    *   **投影器（Projectors）**：设计了专门的线性投影层，将连续的策略潜状态和动作序列映射到 LLM 的嵌入空间。
    *   **三阶段训练方案**：1) 投影器对齐；2) 描述蒸馏（使用 LoRA 微调 LLM 生成行为预览）；3) 裁决优化（引入二元分类损失）。
*   **主动引导（Proactive Steering）**：在推理时，智能体采样 $N$ 个候选动作块，DILLO 并行评估这些动作并给出裁决，拒绝负面动作，仅执行被验证为“Positive”的动作。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **MetaWorld**：单任务强化学习场景（Soccer, Sweep-Into, Drawer-Open）。
    *   **LIBERO**：多任务模仿学习场景（包含 Goal, Object, Spatial 等 5 个任务套件，最高涉及 90 个不同任务）。
*   **对比基准（Benchmarks）**：
    *   **Zero-Shot (ZS) / Few-Shot (FS)**：直接使用 VLM 根据当前图像预测结果。
    *   **Few-Shot Captioning (CAP)**：作为“先知”基准，允许模型看到未来的真实图像进行描述。
    *   **Forewarn**：现有的基于视觉模拟的策略引导模型。
*   **评估指标**：文本-观测保真度（T2O）、基于 LLM（Qwen2.5-32B）的语义评分、回合成功率（SR）以及推理延迟。

#### 4. 资源与算力
*   **推理算力**：实验在消费级显卡 **NVIDIA RTX 3090** 上完成。
*   **对比算力**：基准模型 Forewarn 使用的是企业级显卡 RTX A6000（48GB VRAM）。
*   **训练算力**：文中提到使用了 NVIDIA 捐赠的 **DGX Spark** 集群进行大规模数据采集和模拟阶段的计算，但未详细说明具体的训练时长和 GPU 数量。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单单任务到复杂多任务（LIBERO-90）的广泛场景。
*   **验证维度**：
    *   **定量分析**：对比了不同参数规模（1B vs 4B）的模型表现。
    *   **消融/对比实验**：通过 T2O 指标验证了潜状态预测甚至优于直接看未来图像的 Captioning 模型。
    *   **人类研究**：组织了 10 名机器人专家对 120 个回合进行盲测，验证了 DILLO 裁决与人类判断的高度一致性（MetaWorld 达 84.1%）。
*   **客观性**：实验设计考虑了感知噪声注入和不同熟练度的策略检查点，确保了故障样本的多样性，实验结果较为客观。

#### 6. 论文的主要结论与发现
*   **潜状态确实充分**：实验证明策略潜状态足以支持高保真的语义预测，无需昂贵的视觉生成。
*   **显著加速**：DILLO 实现了约 **14 倍** 的推理加速（从 ~4s 降至 ~0.26s），使得实时闭环控制成为可能。
*   **性能提升**：在 MetaWorld 和 LIBERO 上，DILLO 引导的策略成功率平均提升了 **9.3 个百分点**，最高提升 **15 个百分点**。
*   **高准确度**：在不接触任何视觉观测的情况下，DILLO 的故障裁决准确率达到了 **91.4%**。

#### 7. 优点
*   **范式创新**：打破了“必须模拟视觉才能预测未来”的思维定式，极大地降低了世界模型的部署门槛。
*   **实时性强**：推理延迟极低，适合部署在计算资源受限的具身智能体上。
*   **可解释性**：除了二元裁决，还能生成人类可读的自然语言描述，便于人类监控和调试。
*   **通用性**：作为插件式的安全层，可以适配不同的底层策略架构（RL 或 IL）。

#### 8. 不足与局限
*   **架构依赖性**：由于潜状态是特定于策略架构的，DILLO 的投影器需要针对不同的策略模型重新对齐。
*   **极端多样性下的挑战**：在 LIBERO-90（90 个任务）中，单一模型的表现略有下降，说明在极高任务多样性下，模型容量或训练数据规模仍是瓶颈。
*   **空间泛化限制**：在 LIBERO-Spatial 任务中，由于缺乏显式的目标位置调节，模型对复杂空间配置的泛化能力仍有提升空间。
*   **教师依赖**：训练质量高度依赖于特权 VLM 教师的标注质量，若模拟器信息不准，蒸馏效果会受限。

（完）
