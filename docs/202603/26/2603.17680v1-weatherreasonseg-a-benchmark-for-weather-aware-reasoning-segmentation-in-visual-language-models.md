---
title: "WeatherReasonSeg: A Benchmark for Weather-Aware Reasoning Segmentation in Visual Language Models"
title_zh: WeatherReasonSeg：视觉语言模型中天气感知推理分割的基准
authors: "Wanjun Du, Zifeng Yuan, Tingting Chen, Fucai Ke, Beibei Lin, Shunli Zhang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17680v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 视觉语言模型推理分割基准
tldr: 本研究针对视觉语言模型（VLM）在恶劣天气下推理分割能力不足的问题，提出了WeatherReasonSeg基准。该基准由合成天气数据集和真实世界恶劣天气数据集组成，涵盖功能、应用场景等五个推理维度。通过对多种VLM的广泛评估，揭示了模型性能随天气严重程度增加而单调下降的趋势，为提升模型在复杂环境下的鲁棒性提供了重要基础。
selection_source: fresh_fetch
motivation: 现有推理分割基准主要基于理想拍摄条件，缺乏对雨、雪、雾等恶劣天气下模型鲁棒性的评估。
method: 通过在现有数据集上应用合成天气以及利用掩码引导的LLM提示构建真实世界数据集，建立了多维度的推理分割基准。
result: 实验发现VLM的性能随天气严重程度增加而单调下降，且不同天气类型诱发了模型不同的脆弱性模式。
conclusion: WeatherReasonSeg基准为推动具备天气感知能力的鲁棒推理分割技术的发展奠定了基础。
---

## 摘要
现有的视觉语言模型（VLM）在基于推理的分割任务中表现出了卓越的性能。然而，目前的基准测试主要由理想条件下拍摄的高质量图像构建。这提出了一个关键问题：当视觉线索因雨、雪或雾等恶劣天气条件而严重退化时，VLM 能否维持可靠的推理分割能力？针对这一挑战，我们推出了 WeatherReasonSeg，这是一个旨在评估 VLM 在恶劣天气条件下基于推理的分割性能的基准。它由两个互补的部分组成：首先，我们通过对现有分割数据集应用不同严重程度的合成天气，构建了一个可控的推理数据集，从而实现细粒度的鲁棒性分析；其次，为了捕捉现实世界的复杂性，我们通过掩码引导的大语言模型（LLM）提示生成语义一致的查询，构建了一个真实世界的恶劣天气推理分割数据集。我们进一步将评估范围扩展到五个推理维度，包括功能、应用场景、结构属性、交互和需求匹配。对多种 VLM 进行的广泛实验揭示了两个关键发现：（1）VLM 的性能随着天气严重程度的增加而单调下降；（2）不同的天气类型会导致截然不同的脆弱性模式。我们希望 WeatherReasonSeg 能为推进鲁棒的天气感知推理奠定基础。

## Abstract
Existing vision-language models (VLMs) have demonstrated impressive performance in reasoning-based segmentation. However, current benchmarks are primarily constructed from high-quality images captured under idealized conditions. This raises a critical question: when visual cues are severely degraded by adverse weather conditions such as rain, snow, or fog, can VLMs sustain reliable reasoning segmentation capabilities? In response to this challenge, we introduce WeatherReasonSeg, a benchmark designed to evaluate VLM performance in reasoning-based segmentation under adverse weather conditions. It consists of two complementary components. First, we construct a controllable reasoning dataset by applying synthetic weather with varying severity levels to existing segmentation datasets, enabling fine-grained robustness analysis. Second, to capture real-world complexity, we curate a real-world adverse-weather reasoning segmentation dataset with semantically consistent queries generated via mask-guided LLM prompting. We further broaden the evaluation scope across five reasoning dimensions, including functionality, application scenarios, structural attributes, interactions, and requirement matching. Extensive experiments across diverse VLMs reveal two key findings: (1) VLM performance degrades monotonically with increasing weather severity, and (2) different weather types induce distinct vulnerability patterns. We hope WeatherReasonSeg will serve as a foundation for advancing robust, weather-aware reasoning.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：WeatherReasonSeg 基准测试

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉语言模型（VLM）在基于推理的分割任务（Reasoning Segmentation）中表现出色，但其评估基准（如 ReasonSeg）大多基于理想天气下的高质量图像。在现实世界中，雨、雪、雾等恶劣天气会严重退化视觉线索，干扰视觉与语言的对齐。
*   **研究动机**：论文旨在探究当视觉输入受损时，VLM 是否还能维持可靠的推理分割能力，并填补目前缺乏针对恶劣天气鲁棒性评估基准的空白。

#### 2. 方法论：核心思想与关键技术
WeatherReasonSeg 基准由两个互补部分组成：
*   **可控合成天气推理数据集**：
    *   **核心思想**：在现有 ReasonSeg 数据集基础上，利用基于物理规律的生成器合成不同类型和严重程度的天气。
    *   **技术细节**：采用物理可解释的模型（如大气散射模型模拟雾、区域依赖模型模拟雨滴累积、粒子遮挡模型模拟雪），通过调节物理参数（如散射系数 $\beta$、雨滴密度 $s$、透射因子 $\alpha$）生成“轻度、中度、重度”三个等级的退化图像。
*   **真实世界恶劣天气推理数据集**：
    *   **核心思想**：基于 ACDC 真实恶劣天气数据集，构建语义一致且复杂的推理查询。
    *   **关键流程**：采用**掩码引导的 LLM 提示（Mask-guided LLM Prompting）**策略。将原始图像、目标掩码和类别标签输入 Qwen 模型，引导其从五个维度生成自然语言查询：**功能（Function）、应用场景（Application）、结构属性（Structure）、交互关系（Relationship）及需求匹配（Requirement）**。随后通过人工与模型协同校验确保查询质量。

#### 3. 实验设计
*   **数据集规模**：总计 44,721 个图像-查询对。其中合成部分 8,811 对（雨、雪、雾），真实世界部分 35,910 对（涵盖雨、雪、雾及夜间场景）。
*   **对比方法（Baselines）**：
    *   **基于基准感知的模型**：Grounded-SAM。
    *   **基于推理的模型**：LISA-7B、Seg-R1、Seg-Zero-7B。
    *   **性能上限参考**：SAM2（直接给定真值框/点提示）。
*   **评估指标**：采用分割任务通用的 **gIoU**（全局交并比）和 **cIoU**（累积交并比）。

#### 4. 资源与算力
*   **硬件配置**：实验使用了 **8 台 NVIDIA 24G 3090 GPU** 进行推理评估。
*   **训练/推理情况**：文中主要侧重于基准测试的评估，使用了各模型的官方实现和默认推理配置。论文未明确提及针对该基准进行大规模重新训练的时长，重点在于对现有预训练模型的零样本（Zero-shot）或推理能力测试。

#### 5. 实验数量与充分性
*   **实验维度**：实验涵盖了 3 种合成天气类型 × 3 种严重程度，以及 4 种真实恶劣环境（含夜间）。
*   **充分性分析**：
    *   **多维度对比**：不仅对比了不同模型，还深入分析了五个推理维度下的性能差异。
    *   **鲁棒性压力测试**：通过单调增加天气严重程度，清晰地展示了模型性能的衰减曲线。
    *   **客观性**：引入了 SAM2 作为感知能力的上限，证明了性能下降的主要瓶颈在于“推理阶段”而非单纯的“分割阶段”，实验设计逻辑严密、对比公平。

#### 6. 主要结论与发现
*   **性能单调下降**：VLM 的推理分割性能随天气严重程度的增加而显著下降。在重度合成天气下，平均准确率下降约 15%。
*   **推理是主要瓶颈**：在真实世界场景中，推理分割方法的准确率仅为纯感知方法（如 SAM2）上限的一半，表明恶劣天气严重干扰了视觉-语义的对齐推理。
*   **脆弱性模式差异**：雪天对性能的影响最大，其次是雾和雨。
*   **语义维度敏感度**：涉及“结构”和“功能”的查询相对稳健，而涉及“应用场景”和“需求匹配”的高级上下文推理在恶劣天气下最易出错。

#### 7. 优点（亮点）
*   **首创性**：填补了视觉语言模型在恶劣天气下推理分割评估的空白。
*   **物理一致性**：合成数据并非简单加噪，而是基于物理成像模型，具有更高的真实感和可解释性。
*   **多维度语义设计**：通过 LLM 生成五个维度的查询，使评估从简单的物体识别上升到复杂的逻辑推理。

#### 8. 不足与局限
*   **合成与真实的差距**：尽管使用了物理模型，合成天气仍无法完全模拟真实世界中复杂的光影交互和传感器噪声。
*   **类别多样性限制**：受限于基础数据集（ACDC 和 ReasonSeg），目标物体的类别多样性仍有提升空间。
*   **物理线索缺失**：目前未考虑偏振（Polarization）等能增强恶劣天气鲁棒性的物理成像线索。
*   **应用限制**：该基准目前主要针对静态图像，未扩展到视频序列中的动态推理分割。

（完）
