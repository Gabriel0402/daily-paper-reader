---
title: Grounding Vision and Language to 3D Masks for Long-Horizon Box Rearrangement
title_zh: 将视觉与语言关联至 3D 掩码以实现长程箱体重排
authors: "Ashish Malik, Caleb Lowe, Aayam Shrestha, Stefan Lee, Fuxin Li, Alan Fern"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23676v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于长程规划和重排的3D VLM
tldr: "针对长程3D物体重新排列任务，现有方法在处理复杂几何和多目标时存在局限。本文提出RAMP-3D，通过将视觉语言模型扩展到3D掩码预测，将规划任务转化为序列化的“抓取物体”和“放置区域”掩码预测。该方法在包含多达30个物体的仓库环境中表现优异，成功率达79.5%，为长程规划提供了一种有效的掩码式反应策略。"
selection_source: fresh_fetch
motivation: 传统的符号规划器和2D视觉语言模型在处理具有复杂3D几何约束和大量物体的长程任务时表现不佳。
method: 提出RAMP-3D模型，通过预测成对的3D掩码（抓取目标和放置区域）来反应式地生成多步拾取-放置动作。
result: "在11种仓库任务变体中，该方法实现了79.5%的成功率，显著优于基于2D视觉语言模型的基准方法。"
conclusion: 基于3D掩码的反应式策略是替代传统符号规划流水线、实现长程3D任务规划的一种极具潜力的方法。
---

## 摘要
我们研究了在仅使用视觉观测的情况下，如何根据描述不详的自然语言目标在 3D 环境中进行长程规划，重点关注多步 3D 箱体重排任务。现有方法通常依赖于符号规划器（其状态与目标的关联性较弱且脆弱），或者依赖于 2D 视觉语言模型（VLM）直接生成动作序列。这两种方法在处理大量物体、丰富的 3D 几何形状以及隐式语义约束的推理时都面临困难。3D VLM 的最新进展展示了自然语言指代物与 3D 分割掩码之间的强关联性，这表明其具有实现更通用规划能力的潜力。我们扩展了现有的 3D 关联模型，并提出了反应式动作掩码规划器（RAMP-3D），该规划器将长程规划建模为成对 3D 掩码的序列化反应式预测：一个“物体”掩码指示抓取对象，一个“目标区域”掩码指定放置位置。该系统处理 RGB-D 观测数据和自然语言任务规范，以反应式地生成用于 3D 箱体重排的多步取放动作。我们在包含 1 到 30 个箱体及多种自然语言约束的仓库式环境中，针对 11 种任务变体进行了实验。RAMP-3D 在长程重排任务中实现了 79.5% 的成功率，显著优于基于 2D VLM 的基准模型，证明了基于掩码的反应式策略是长程规划中替代符号流水线的一种极具前景的方案。

## Abstract
We study long-horizon planning in 3D environments from under-specified natural-language goals using only visual observations, focusing on multi-step 3D box rearrangement tasks. Existing approaches typically rely on symbolic planners with brittle relational grounding of states and goals, or on direct action-sequence generation from 2D vision-language models (VLMs). Both approaches struggle with reasoning over many objects, rich 3D geometry, and implicit semantic constraints. Recent advances in 3D VLMs demonstrate strong grounding of natural-language referents to 3D segmentation masks, suggesting the potential for more general planning capabilities. We extend existing 3D grounding models and propose Reactive Action Mask Planner (RAMP-3D), which formulates long-horizon planning as sequential reactive prediction of paired 3D masks: a "which-object" mask indicating what to pick and a "which-target-region" mask specifying where to place it. The resulting system processes RGB-D observations and natural-language task specifications to reactively generate multi-step pick-and-place actions for 3D box rearrangement. We conduct experiments across 11 task variants in warehouse-style environments with 1-30 boxes and diverse natural-language constraints. RAMP-3D achieves 79.5% success rate on long-horizon rearrangement tasks and significantly outperforms 2D VLM-based baselines, establishing mask-based reactive policies as a promising alternative to symbolic pipelines for long-horizon planning.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：RAMP-3D

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**长程（Long-Horizon）3D 物体（箱体）重排任务**。机器人需要根据模糊的自然语言指令（如“按颜色分组，黄色优先，堆叠高度不超过2层”），在复杂的 3D 仓库环境中通过视觉观测自主完成多步取放动作。
*   **研究动机**：传统的符号规划器依赖于脆弱的谓词逻辑和人工设计的场景图，难以处理复杂的 3D 几何约束；而现有的 2D 视觉语言模型（VLM）缺乏对 3D 空间、遮挡和体积关系的深度理解。
*   **核心目标**：探索是否可以直接利用 3D 视觉语言关联模型（Grounding VLMs）的能力，将规划任务转化为 3D 空间中的掩码预测，从而避开显式的符号层。

#### 2. 方法论：RAMP-3D
RAMP-3D（Reactive Action Mask Planner）将长程规划重新表述为**序列化的反应式 3D 掩码预测**。
*   **核心思想**：在每个步骤，模型输入多视角 RGB-D 图像和自然语言指令，直接输出两个 3D 分割掩码：一个指示“抓取哪个物体”（Which-object），另一个指示“放置到哪个目标区域”（Which-target-region）。
*   **关键技术细节**：
    *   **基础架构**：基于 UniVLG（一种 3D 关联模型），使用 Swin Transformer 作为视觉骨干，RoBERTa 作为文本编码器，通过体素化 3D 融合生成几何感知的 3D 视觉 Token。
    *   **配对一致性模块**：引入学习到的线性投影生成“抓取嵌入”和“放置嵌入”，通过计算两者的兼容性得分（Pair-consistency loss），确保预测的抓取物体与放置位置在逻辑上是匹配的。
    *   **终止预测头**：增加一个轻量级的“Done”分支，用于判断任务何时完成。
    *   **掩码空间动作表示**：动作直接在 3D 点云掩码空间定义，通过将掩码投影回物体 ID 或几何坐标来执行动作，天然地编码了几何可行性和占用约束。

#### 3. 实验设计
*   **数据集与场景**：在 IsaacSim 仿真环境中构建了一个 12m x 12m 的仓库，包含 1-30 个箱体、托盘、货架和干扰物。
*   **任务变体**：设计了 **11 种任务类型**，涵盖高度限制、颜色优先级、货架/托盘优先、空间排序、避免堆叠、箱体隔离等复杂约束。
*   **基准模型（Benchmark）**：
    *   **2D-pointer**：一个组合流水线，使用 GPT-5.1 作为高层规划器生成描述，再利用 Molmo-72B（最先进的 2D 指向模型）在俯视图中定位目标。
*   **评估模式**：分为“对齐目标模式”（Snap-to-target，理想化放置）和“自由形式模式”（Free-form，根据预测掩码中心点放置）。

#### 4. 资源与算力
*   **算力说明**：文中**未明确说明**训练 RAMP-3D 所使用的具体 GPU 型号、数量或训练总时长。
*   **辅助模型**：提到使用了 `gpt-oss-120b` 模型进行自然语言语料的增强（改写指令）。
*   **支持背景**：该研究得到了 NSF、DARPA 和 NVIDIA 学术资助计划的支持。

#### 5. 实验数量与充分性
*   **实验规模**：生成了约 9.5k 个独特场景样本，其中 2.2k 用于测试。
*   **充分性评价**：
    *   **多维度评估**：实验涵盖了单步预测准确率、掩码质量（IoU）、长程规划成功率以及语言泛化性测试。
    *   **消融实验**：对辅助预测任务（如识别空闲位置、已放置箱体等）进行了消融，证明了多任务学习对性能的提升。
    *   **客观性**：对比了强大的 2D VLM 基准，并在不同物体数量（1-10, 11-20, 21-30）下分析了性能退化情况，实验设计较为全面且客观。

#### 6. 主要结论与发现
*   **性能优异**：RAMP-3D 在长程重排任务中达到了 **79.5% 的成功率**，远超 2D 基准模型（单步有效性仅 41.4%）。
*   **3D 优势**：直接在 3D 空间操作使模型能更好地处理遮挡和复杂的堆叠逻辑。
*   **泛化能力**：通过 LLM 增强的指令训练，模型对未见过的自然语言模板表现出较强的鲁棒性。
*   **局限性体现**：随着物体数量增加和任务复杂度提升（如“保持箱体可达性”），成功率有所下降，反映了纯反应式策略在缺乏前瞻搜索时的短板。

#### 7. 优点（亮点）
*   **范式创新**：首次将 3D 关联模型（Grounding VLM）直接扩展为长程规划器，将动作空间定义为 3D 掩码。
*   **端到端几何推理**：无需手动设计符号谓词，模型能直接从原始视觉数据中学习空间关系和物理约束。
*   **处理复杂约束**：能够理解并执行包含多种隐式常识约束（如堆叠顺序、空间紧凑性）的复杂指令。

#### 8. 不足与局限
*   **缺乏前瞻性**：模型是纯反应式的（Reactive），没有多步前瞻搜索（Lookahead Search），在处理需要全局最优或具有回溯性质的任务时容易失败。
*   **视觉多样性有限**：实验主要集中在箱体（Box）重排，虽然几何关系复杂，但物体的语义类别较为单一。
*   **累积误差**：在“自由形式”执行模式下，微小的放置偏差会随步骤累积，导致长程任务失败。
*   **环境假设**：假设低层控制器能够完美执行取放动作，未考虑真实机器人的可达性、碰撞避让等运动学约束。

（完）
