---
title: "3D-Mix for VLA: A Plug-and-Play Module for Integrating VGGT-based 3D Information into Vision-Language-Action Models"
title_zh: 3D-Mix for VLA：一种用于将基于 VGGT 的 3D 信息集成到视觉-语言-动作模型中的即插即用模块
authors: "Bin Yu, Shijie Lian, Xiaopeng Lin, Zhaolong Shen, Yuliang Wei, Haishan Liu, Changti Wu, Hang Yuan, Bailing Wang, Cong Huang, Kai Chen"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24393v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 将3D信息集成到视觉-语言-动作 (VLA) 模型中
tldr: 针对视觉-语言-动作（VLA）模型在3D空间感知能力的不足，本文提出了一种名为3D-Mix的即插即用模块。通过系统对比九种集成方案，研究发现语义调节的门控融合能最有效地结合2D语义与3D几何特征。3D-Mix无需修改现有模型核心，在多种VLA架构和MLLM变体上均实现了显著的性能提升，特别是在跨域测试中表现优异，为增强机器人操作的空间智能提供了标准化方案。
selection_source: fresh_fetch
motivation: 现有的VLA模型主要基于2D数据训练，导致其在机器人操作任务中缺乏必要的3D空间感知能力。
method: 提出3D-Mix模块，采用语义调节的门控融合机制，将VGGT提供的3D几何信息自适应地集成到多种VLA架构中。
result: "在SIMPLER和LIBERO基准测试中，3D-Mix在9种模型变体上均取得一致增益，其中SIMPLER跨域测试平均提升达7.0%。"
conclusion: 3D-Mix作为一种通用的即插即用模块，通过优化3D信息融合策略，显著提升了VLA系统的空间智能和泛化能力。
---

## 摘要
视觉-语言-动作（VLA）模型利用多模态大语言模型（MLLM）进行机器人控制，但近期研究表明，由于主要在 2D 数据上进行训练，MLLM 表现出有限的空间智能，导致其在操作任务中的 3D 感知能力不足。虽然最近的方法引入了如 VGGT 等专门的 3D 视觉模型来增强空间理解，但它们采用了多种集成机制且缺乏系统性研究，使得最优融合策略尚不明确。我们在标准化基准上进行了一项全面的初步研究，对比了九种 VGGT 集成方案，并发现语义条件门控融合在所有评估的九种融合方案中表现最强，该方案能根据任务上下文自适应地平衡 2D 语义特征和 3D 几何特征。我们提出了 3D-Mix，这是一个即插即用模块，可以集成到多种 VLA 架构（如 GR00T 风格和 π 风格）中，而无需修改现有的 MLLM 或动作专家组件。在 SIMPLER 和 LIBERO 基准上针对六个 MLLM 系列（九个模型变体，参数量为 2B-8B）进行的实验表明，3D-Mix 带来了持续的性能提升，在所有九个 GR00T 风格变体中，其在分布外（OOD）SIMPLER 基准上的平均提升达 +7.0%，为增强 VLA 系统的空间智能建立了一种原则性方法。

## Abstract
Vision-Language-Action (VLA) models leverage Multimodal Large Language Models (MLLMs) for robotic control, but recent studies reveal that MLLMs exhibit limited spatial intelligence due to training predominantly on 2D data, resulting in inadequate 3D perception for manipulation tasks. While recent approaches incorporate specialized 3D vision models such as VGGT to enhance spatial understanding, they employ diverse integration mechanisms without systematic investigation, leaving the optimal fusion strategy unclear. We conduct a comprehensive pilot study comparing nine VGGT integration schemes on standardized benchmarks and find that semantic-conditioned gated fusion, which adaptively balances 2D semantic and 3D geometric features based on task context, achieved the strongest performance among all nine evaluated fusion schemes in our pilot study. We present 3D-Mix, a plug-and-play module that integrates into diverse VLA architectures (GR00T-style and $π$-style) without modifying existing MLLM or action expert components. Experiments across six MLLM series (nine model variants, 2B--8B parameters) on SIMPLER and LIBERO show that 3D-Mix delivers consistent performance gains, averaging +7.0% on the out-of-domain (OOD) SIMPLER benchmark across all nine GR00T-style variants, establishing a principled approach for enhancing spatial intelligence in VLA systems.

---

## 论文详细总结（自动生成）

以下是对论文《3D-Mix for VLA: A Plug-and-Play Module for Integrating VGGT-based 3D Information into Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型主要依赖于在 2D 图像-文本对上预训练的多模态大语言模型（MLLM）。由于缺乏显式的 3D 几何监督，这些模型在处理需要精确空间推理、深度感知和物体抓取姿态估计的机器人操作任务时表现欠佳。
*   **研究背景**：虽然已有研究尝试引入专门的 3D 视觉模型（如 VGGT），但集成方式多种多样且缺乏系统性对比，导致社区并不清楚哪种融合策略最为有效。

### 2. 方法论
*   **核心思想**：提出 **3D-Mix**，一个即插即用的模块，通过**语义调节的自适应门控机制（Semantic-conditioned Adaptive Gating）**，动态平衡来自 MLLM 的 2D 语义特征和来自 VGGT 的 3D 几何特征。
*   **关键技术细节**：
    *   **特征提取与投影**：使用预训练且冻结的 VGGT 提取几何特征，并通过线性层投影到 MLLM 的隐藏维度。
    *   **语义调节门控**：对 MLLM 的隐藏状态进行平均池化以获取全局语义上下文，将其与几何特征拼接后通过一个 Sigmoid 激活的门控网络，计算出每个 Token 位置的权重。
    *   **自适应融合**：根据门控权重对语义和几何特征进行加权融合，生成富含空间信息的 Token 序列，并将其拼接到原始 MLLM 序列中。
*   **架构集成**：
    *   **GR00T 风格**：在 MLLM 最后一层输出后插入 3D-Mix，作为动作专家（DiT）的输入。
    *   **$\pi$ 风格**：采用层级融合，在 MLLM 的多个中间层独立应用 3D-Mix，与动作专家的对应层进行交叉注意力关联。

### 3. 实验设计
*   **数据集与场景**：
    *   **SIMPLER**：一个 Real-to-Sim 的分布外（OOD）基准测试，用于衡量模型在视觉变化和物体配置改变下的泛化能力。
    *   **LIBERO**：包含四个任务套件（空间、物体、目标、长程），用于评估域内多任务学习能力。
*   **对比方法**：
    *   **初步研究（Pilot Study）**：对比了 9 种融合方案，包括 AE-Fusion（动作专家融合）、Early Fusion（早期融合）、Spatial Forcing（空间强迫对齐）等。
    *   **主实验**：在 6 个 MLLM 系列（Qwen2.5-VL, Qwen3-VL, RoboBrain, MimoEmbodied, RynnBrain 等）共 9 个变体上验证 3D-Mix 的有效性。

### 4. 资源与算力
*   **算力设备**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练配置**：采用 DeepSpeed ZeRO-2 优化器，训练步数为 60,000 步。
*   **学习率**：MLLM 主干为 $10^{-5}$，动作专家和融合模块为 $10^{-4}$。
*   **显存优化**：在 $\pi$ 风格模型训练中，由于层级交叉注意力的显存压力，单卡 Batch Size 从 16 降至 10。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **初步研究**：详尽对比了 9 种不同的架构设计方案。
    *   **广泛验证**：覆盖了从 2B 到 8B 参数量的 9 种主流 MLLM 变体。
    *   **消融实验**：包括 VGGT 冻结 vs. 微调、3D 信息敏感性测试（替换为噪声或零向量）、以及 $\pi$ 风格中的稀疏层融合深度测试。
*   **评价**：实验设计非常充分且具有高度的客观性。通过在不同架构（GR00T vs. $\pi$）和多个不同来源的 MLLM 上进行测试，证明了该模块的通用性和鲁棒性。

### 6. 主要结论与发现
*   **最优策略**：语义调节的门控融合（Gated Fusion）在所有 9 种方案中表现最佳，因为它能根据任务上下文自适应地决定对几何信息的依赖程度。
*   **性能提升**：3D-Mix 在所有测试的模型上均实现了性能增益。在 SIMPLER 跨域基准上，GR00T 风格变体的平均成功率提升了 **7.0%**，部分模型提升超过 10%。
*   **3D 信息的真实性**：消融实验证明，性能提升确实源于 VGGT 提供的 3D 几何信息，而非仅仅因为增加了参数量或特征维度。

### 7. 优点
*   **即插即用**：无需修改 MLLM 或动作专家的内部结构，易于集成到现有的 VLA 流程中。
*   **系统性强**：论文对融合位置和机制进行了详尽的实证研究，填补了该领域的理论空白。
*   **泛化性好**：在 OOD 任务（SIMPLER）上的显著提升证明了 3D 信息对于提高机器人鲁棒性的关键作用。

### 8. 不足与局限
*   **推理开销**：虽然 3D-Mix 模块本身很轻量，但引入额外的 VGGT 编码器会增加推理时的计算延迟和显存占用。
*   **VGGT 依赖**：实验主要基于冻结的 VGGT，虽然效果良好，但可能限制了模型在极特定任务下进一步优化几何特征的能力。
*   **仿真局限**：主要实验在仿真环境（SimplerEnv）中完成，虽然包含 Real-to-Sim 评估，但仍需更多真实机器人的部署数据来进一步验证。

（完）
