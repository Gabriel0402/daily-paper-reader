---
title: "LATS: Large Language Model Assisted Teacher-Student Framework for Multi-Agent Reinforcement Learning in Traffic Signal Control"
title_zh: LATS：用于交通信号控制中多智能体强化学习的大语言模型辅助师生框架
authors: "Yifeng Zhang, Peizhuo Li, Tingguang Zhou, Mingfeng Fan, Guillaume Sartoretti"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24361v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 大语言模型辅助的多智能体强化学习教与学框架
tldr: 针对多智能体强化学习（MARL）在交通信号控制中表征能力不足和泛化性差的问题，本文提出LATS框架。该框架利用大语言模型（LLM）作为“老师”提取交通场景的深层语义特征，通过知识蒸馏引导轻量级“学生”网络学习。这种方法结合了LLM的推理能力与强化学习的决策效率，在推理阶段无需调用LLM，显著提升了模型在复杂交通环境下的性能和泛化能力。
selection_source: fresh_fetch
motivation: 传统强化学习在复杂交通场景中表征能力有限，而大语言模型虽具备强推理能力但推理速度慢且存在幻觉，难以直接用于实时决策。
method: 提出一种老师-学生学习模块，利用LLM生成交通拓扑与动态的语义特征，并通过知识蒸馏训练轻量级学生网络以辅助强化学习决策。
result: 在多个交通数据集上的实验表明，该方法在性能和泛化性上均优于传统强化学习和纯大语言模型方法。
conclusion: LATS框架通过将LLM的先验知识转化为强化学习的表征优势，实现了高效、实时且泛化性强的交通信号控制。
---

## 摘要
自适应交通信号控制（ATSC）旨在通过实时调整交通信号灯来优化交通流并最大限度地减少延迟。多智能体强化学习（MARL）的最新进展在 ATSC 领域展现出巨大潜力，但现有方法仍受限于表征能力不足，在复杂多变的交通环境中往往导致次优性能和较差的泛化能力。另一方面，大语言模型（LLMs）在语义表征、推理和分析方面表现卓越，但其幻觉倾向和缓慢的推理速度往往阻碍了它们在决策任务中的直接应用。为了应对这些挑战，我们提出了一种名为 LATS 的新型学习范式，该范式整合了 LLMs 和 MARL，利用前者的强大先验知识和归纳能力来增强后者的决策过程。具体而言，我们引入了一个即插即用的师生学习模块，其中经过训练的嵌入式 LLM 作为教师，生成捕捉每个路口拓扑结构和交通动态的丰富语义特征。随后，一个更简单的（学生）神经网络通过潜在空间中的知识蒸馏来学习模拟这些特征，使最终模型能够独立于 LLM 运行，用于下游的强化学习决策过程。这种整合显著增强了整体模型在不同交通场景下的表征能力，从而实现了更高效且更具泛化性的控制策略。在多种交通数据集上的广泛实验表明，我们的方法增强了强化学习模型的表征学习能力，从而在整体性能和泛化性上均优于传统的强化学习和仅使用 LLM 的方法。

## Abstract
Adaptive Traffic Signal Control (ATSC) aims to optimize traffic flow and minimize delays by adjusting traffic lights in real time. Recent advances in Multi-agent Reinforcement Learning (MARL) have shown promise for ATSC, yet existing approaches still suffer from limited representational capacity, often leading to suboptimal performance and poor generalization in complex and dynamic traffic environments. On the other hand, Large Language Models (LLMs) excel at semantic representation, reasoning, and analysis, yet their propensity for hallucination and slow inference speeds often hinder their direct application to decision-making tasks. To address these challenges, we propose a novel learning paradigm named LATS that integrates LLMs and MARL, leveraging the former's strong prior knowledge and inductive abilities to enhance the latter's decision-making process. Specifically, we introduce a plug-and-play teacher-student learning module, where a trained embedding LLM serves as a teacher to generate rich semantic features that capture each intersection's topology structures and traffic dynamics. A much simpler (student) neural network then learns to emulate these features through knowledge distillation in the latent space, enabling the final model to operate independently from the LLM for downstream use in the RL decision-making process. This integration significantly enhances the overall model's representational capacity across diverse traffic scenarios, thus leading to more efficient and generalizable control strategies. Extensive experiments across diverse traffic datasets empirically demonstrate that our method enhances the representation learning capability of RL models, thereby leading to improved overall performance and generalization over both traditional RL and LLM-only approaches. [...]

---

## 论文详细总结（自动生成）

### 论文总结：LATS —— 大语言模型辅助的交通信号控制师生强化学习框架

#### 1. 核心问题与研究动机
*   **核心问题**：传统的自适应交通信号控制（ATSC）方法中，多智能体强化学习（MARL）模型通常规模较小，**表征能力有限**，难以应对复杂多变的城市交通拓扑和动态需求。而大语言模型（LLM）虽具备极强的语义理解和推理能力，但存在**推理速度慢（无法实时决策）**和**幻觉风险**，难以直接部署。
*   **研究动机**：探索如何将 LLM 的深层语义表征能力“注入”到轻量级的强化学习模型中，既保留 LLM 对交通环境的深刻理解，又维持强化学习的高效实时决策能力。

#### 2. 方法论
LATS 框架采用了一种**师生学习（Teacher-Student）架构**，核心流程如下：
*   **提示词构建（Prompt Construction）**：将路口的静态拓扑（车道数、限速、道路连接）与动态交通状况（过去几步的车辆数、排队情况）转化为结构化的文本提示词。
*   **教师模型（Teacher）**：使用预训练的嵌入式 LLM（如 `jina-embeddings-v2-small-en`）作为教师，将文本提示词编码为高维语义嵌入向量。嵌入式 LLM 专注于语义表征而非文本生成，有效规避了幻觉问题。
*   **学生模型与知识蒸馏（Student & KD）**：
    *   引入两个**变分自编码器（VAE）**，分别对教师的语义特征和学生的原始状态向量进行编码。
    *   在**潜在空间（Latent Space）**中通过最小化 KL 散度进行对齐，使学生网络能够模拟教师生成的语义特征。
*   **强化学习决策**：将学生网络学到的潜在语义特征与原始状态特征融合，输入到基于 **PPO（近端策略优化）** 的决策模块中。
*   **即插即用与推理脱离**：LLM 仅在训练阶段参与特征生成。在部署阶段，仅需运行轻量级的学生网络和 RL 策略，实现了 **LLM-free（无 LLM 依赖）** 的实时推理。

#### 3. 实验设计
*   **数据集与场景**：
    *   **Grid 5×5**：包含 25 个相同路口的同构网格网络。
    *   **Monaco**：基于摩纳哥真实地图的异构网络，包含 28 个具有不同拓扑结构的路口。
*   **Benchmark（基准指标）**：排队长度、平均车速、路口延迟、完成率、行程时间（Trip Time）和行程延迟。
*   **对比方法**：
    *   **传统方法**：Fixed-Time, Greedy, Max-Pressure。
    *   **纯 LLM 方法**：基于 Llama-2-7B 和 Llama-3-8B 的直接决策。
    *   **纯 RL 方法**：独立学习（MA2C, IQL 等）和参数共享方法（IPPO, AttendLight, CoLight, HeteroLight）。

#### 4. 资源与算力
*   **硬件环境**：Ubuntu 服务器，32GB RAM，Intel Core i9-13900KF CPU，**NVIDIA RTX 4090 GPU**。
*   **训练时长**：LATS 完整训练 1000 个 episode 约需 **24 小时**（主要耗时在 LLM 嵌入查询）。相比之下，普通 RL 基准方法（如 HeteroLight）在相同环境下仅需约 4 小时。
*   **推理速度**：LATS 推理时间小于 $10^{-4}$ 秒/步/智能体，而纯 LLM 决策需约 6.5 秒。

#### 5. 实验数量与充分性
*   **实验规模**：在同构和异构两个大型网络上进行了广泛测试，每个方法运行 10 个随机种子以确保统计显著性。
*   **消融实验**：
    *   **组件消融**：验证了教师模块、学生模块对性能的贡献。
    *   **提示词消融**：对比了仅包含拓扑、仅包含动态信息以及全信息的表现。
    *   **模型消融**：对比了不同 LLM 嵌入模型（Jina vs. BGE）的影响。
*   **泛化性测试**：进行了 **Zero-shot（零样本）迁移实验**（如在 Monaco 训练，在 Grid 上测试），验证了模型在未见场景下的鲁棒性。
*   **充分性评价**：实验设计全面，涵盖了性能、效率、泛化性和表征可视化（t-SNE），结果客观公平。

#### 6. 主要结论与发现
*   **性能领先**：LATS 在所有关键指标上均显著优于传统 RL 和纯 LLM 方法，尤其在减少行程时间和延迟方面表现突出。
*   **语义增强**：通过 t-SNE 可视化发现，LATS 学习到的特征空间比普通 RL 更具辨别力，能更好地区分不同路口的独特性。
*   **泛化能力强**：得益于 LLM 的先验知识，LATS 在跨场景迁移任务中表现出比其他参数共享 RL 模型更强的适应性。
*   **高效推理**：通过师生蒸馏，成功将 LLM 的知识压缩到小模型中，推理速度提升了数万倍。

#### 7. 优点
*   **架构创新**：首次将 LLM 的语义表征能力通过师生蒸馏引入到 RL 交通控制中，而非简单的提示词工程。
*   **即插即用**：该模块可以轻松集成到现有的各种 RL 交通控制框架中（如文中展示了集成到 AttendLight 的效果）。
*   **实用性高**：解决了 LLM 无法实时响应的痛点，使其具备了工业化部署的潜力。

#### 8. 不足与局限
*   **训练成本高**：尽管推理很快，但训练阶段频繁调用 LLM 嵌入接口导致时间成本显著增加。
*   **提示词依赖**：目前仍依赖于人工设计的固定提示词模板，可能无法捕捉到所有潜在的交通细微特征。
*   **缺乏智能体间协作语义**：目前主要增强的是单个路口的表征，尚未显式地利用 LLM 来建模路口间的通信和协同语义。
*   **环境局限**：实验主要在仿真器（SUMO）中进行，尚未在包含极端天气或交通事故等极端现实情况的场景中验证。

（完）
