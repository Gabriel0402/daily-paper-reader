---
title: "FCL-COD: Weakly Supervised Camouflaged Object Detection with Frequency-aware and Contrastive Learning"
title_zh: FCL-COD：基于频率感知和对比学习的弱监督伪装目标检测
authors: "Jingchen Ni, Quan Zhang, Dan Jiang, Keyu Lv, Ke Zhang, Chun Yuan"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22969v1"
tags: ["query:mmseg"]
score: 9.0
evidence: SAM模型在伪装目标检测中的挑战与改进
tldr: 针对弱监督伪装目标检测（WSCOD）中掩码标注成本高及现有方法（如SAM）在非伪装响应、局部响应和边界模糊等方面的不足，本文提出FCL-COD框架。该框架通过频率感知低秩自适应（FoRA）引入场景知识，结合梯度感知对比学习和多尺度频率表示学习，显著提升了边界精度和检测性能，在多个基准数据集上超越了现有弱监督甚至全监督方法。
selection_source: fresh_fetch
motivation: 现有的弱监督伪装目标检测方法性能受限，且通用大模型SAM在处理伪装场景时面临非目标响应和边界感知不足等挑战。
method: 提出FCL-COD框架，通过频率感知低秩自适应、梯度感知对比学习及多尺度频率表示学习来增强模型对伪装目标的识别与边界细化能力。
result: 实验结果显示，该方法在三个主流COD基准上均优于现有的弱监督技术，甚至在性能上超越了部分全监督方法。
conclusion: 本研究证明了结合频率信息与对比学习能有效解决弱监督伪装目标检测中的关键难题，显著提升了复杂场景下的分割精度。
---

## 摘要
现有的伪装目标检测（COD）方法通常依赖于掩码标注引导的全监督学习。然而，获取掩码标注既费时又费力。与全监督方法相比，现有的弱监督 COD 方法表现明显较差。即使是对于 Segment Anything Model (SAM)，在处理弱监督伪装目标检测（WSCOD）时仍面临挑战，例如：a. 非伪装目标响应，b. 局部响应，c. 极端响应，以及 d. 缺乏精细的边界感知，这导致在伪装场景中的结果不尽如人意。为了缓解这些问题，本文提出了一种基于频率感知和对比学习的 WSCOD 框架，命名为 FCL-COD。为了减轻非伪装目标响应的问题，我们提出了频率感知低秩自适应（FoRA）方法，将频率感知的伪装场景知识融入 SAM。为了克服局部和极端响应的挑战，我们引入了一种梯度感知对比学习方法，有效地描绘了精确的前景-背景边界。此外，为了解决缺乏精细边界感知的问题，我们提出了一种多尺度频率感知表示学习策略，有助于建模更精细的边界。我们通过在三个广泛认可的 COD 基准测试上进行的大量实证实验验证了我们方法的有效性。结果证实，我们的方法超越了最先进的弱监督技术，甚至超越了全监督技术。

## Abstract
Existing camouflage object detection (COD) methods typically rely on fully-supervised learning guided by mask annotations. However, obtaining mask annotations is time-consuming and labor-intensive. Compared to fully-supervised methods, existing weakly-supervised COD methods exhibit significantly poorer performance. Even for the Segment Anything Model (SAM), there are still challenges in handling weakly-supervised camouflage object detection (WSCOD), such as: a. non-camouflage target responses, b. local responses, c. extreme responses, and d. lack of refined boundary awareness, which leads to unsatisfactory results in camouflage scenes. To alleviate these issues, we propose a frequency-aware and contrastive learning-based WSCOD framework in this paper, named FCL-COD. To mitigate the problem of non-camouflaged object responses, we propose the Frequency-aware Low-rank Adaptation (FoRA) method, which incorporates frequency-aware camouflage scene knowledge into SAM. To overcome the challenges of local and extreme responses, we introduce a gradient-aware contrastive learning approach that effectively delineates precise foreground-background boundaries. Additionally, to address the lack of refined boundary perception, we present a multi-scale frequency-aware representation learning strategy that facilitates the modeling of more refined boundaries. We validate the effectiveness of our approach through extensive empirical experiments on three widely recognized COD benchmarks. The results confirm that our method surpasses both state-of-the-art weakly supervised and even fully supervised techniques.

---

## 论文详细总结（自动生成）

### FCL-COD：基于频率感知和对比学习的弱监督伪装目标检测论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
伪装目标检测（COD）旨在识别与背景高度相似的物体。传统全监督方法依赖昂贵的像素级掩码（Mask）标注，而现有的弱监督方法（WSCOD）在性能上远逊于全监督。即使是强大的通用分割模型（如 SAM），在伪装场景下也面临四大挑战：
*   **非伪装目标响应**：容易误触发背景中的显著非伪装物体。
*   **局部响应**：仅能识别物体的部分区域。
*   **极端响应**：出现严重的漏检或过度分割。
*   **边界感知不足**：无法精确勾勒伪装目标的细微边缘。
本文的研究动机在于利用频率域信息和对比学习增强 SAM 的适配能力，在仅使用边界框（Bounding Box）作为弱监督信号的前提下，提升伪装目标的检测精度。

#### 2. 方法论：核心思想与关键技术
论文提出了 **FCL-COD** 框架，采用“伪装知识注入+轻量化检测器训练”的两阶段策略：

*   **第一阶段：三元教师-学生自训练（Triadic Teacher-Student Self-training）**
    *   **FoRA（频率感知低秩自适应）**：在 LoRA 的基础上，引入空间增强算子（多尺度卷积）和频率调制算子（傅里叶变换），将频率域的伪装场景知识注入 SAM 的冻结骨干网络中。
    *   **GCL（梯度感知对比学习）**：利用 Grad-CAM 生成梯度激活图来识别易混淆的背景区域（难负样本），通过对比损失拉大高维空间中前景与背景特征的距离，解决局部和极端响应问题。
*   **第二阶段：轻量级检测器训练**
    *   **MSFA（多尺度频率感知注意机制）**：在轻量级编码器-解码器之间插入 MSFA 模块。该模块通过双分支设计（空间分支增强局部上下文，频率分支建模光谱线索），并利用“三通道注意力”实现跨域交互，从而捕获精细的边界特征。

#### 3. 实验设计
*   **数据集**：在四个主流 COD 基准上进行评估：**CAMO**、**CHAMELEON**、**COD10K** 和 **NC4K**。
*   **Benchmark 与对比方法**：
    *   **全监督方法**：如 ZoomNet, CamoFormer, FSPNet 等。
    *   **弱监督/零样本方法**：如 SCSOD, CRNet, WS-SAM, 以及基于 SAM 的最新方法 SAM-COD。
*   **评估指标**：MAE（平均绝对误差）、$S_m$（结构相似度）、$E_m$（增强对齐度）、$F_w^\beta$（加权 F 值）。

#### 4. 资源与算力
*   **硬件环境**：使用了 **2 张 NVIDIA H20 GPU**。
*   **训练细节**：采用 PyTorch 实现，使用带动量的 SGD 优化器（0.9），权重衰减 $5 \times 10^{-4}$，初始学习率 $1 \times 10^{-3}$ 并配合余弦退火策略。
*   **训练时长**：训练进行了 60 个 epoch，batch size 为 8。文中未明确给出具体的总训练小时数。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个数据集上进行了详尽的定量对比，并提供了大量的定性可视化结果。
*   **消融实验**：针对 FoRA、GCL、MSFA 三个核心组件分别进行了消融测试，验证了各模块对性能提升的贡献。此外，还对损失函数权重、FoRA 的内部结构、MSFA 的分支设计进行了参数敏感性分析。
*   **充分性与公平性**：实验涵盖了从弱监督到全监督的广泛对比，并进行了跨任务（泛化至显著目标检测 SOD）的验证，实验设计客观且逻辑严密。

#### 6. 主要结论与发现
*   **性能超越**：FCL-COD 在所有四个数据集上均显著优于现有的弱监督方法（如 SAM-COD），甚至在多个指标上超越了部分全监督方法（如 ZoomNet）。
*   **频率域的重要性**：实验证明，显式建模频率信息能有效抑制低频背景干扰，保留中高频的判别性细节，这对于解决伪装目标的边界模糊问题至关重要。
*   **对比学习的效用**：通过梯度感知的对比学习，模型能更精准地分离前景和易混淆背景，显著减少了误报和漏检。

#### 7. 优点（亮点）
*   **创新的频率感知**：将频率域分析引入 LoRA 和注意力机制，为处理极高相似度场景提供了新视角。
*   **难样本挖掘**：利用梯度信息引导对比学习，无需额外标注即可自动聚焦于“难背景”区域。
*   **高效的两阶段设计**：第一阶段利用大模型生成高质量伪标，第二阶段训练轻量化模型，兼顾了精度与推理效率。

#### 8. 不足与局限
*   **提示依赖**：虽然是弱监督，但第一阶段仍依赖于 Bounding Box 提示，对于完全无提示（如仅有类别标签）的场景未做深入探讨。
*   **算力门槛**：使用了 H20 这种高性能 GPU，对于算力受限的环境，其训练成本可能较高。
*   **应用限制**：主要针对静态图像，未探讨在视频伪装目标检测（VCOD）中的表现，视频中的运动信息可能与频率信息产生复杂的交互。

（完）
