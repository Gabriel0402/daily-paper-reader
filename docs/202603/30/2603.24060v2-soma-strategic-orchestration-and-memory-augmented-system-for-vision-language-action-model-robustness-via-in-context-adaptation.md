---
title: "SOMA: Strategic Orchestration and Memory-Augmented System for Vision-Language-Action Model Robustness via In-Context Adaptation"
title_zh: SOMA：通过上下文自适应提升视觉-语言-动作模型鲁棒性的策略编排与记忆增强系统
authors: "Zhuoran Li, Zhiyang Li, Kaijun Zhou, Jinyu Gu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24060v2"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 机器人控制器中视觉-语言-动作（VLA）模型的鲁棒性
tldr: 针对视觉-语言-动作（VLA）模型在分布外任务中鲁棒性不足的问题，本文提出SOMA系统。该系统无需微调参数，通过双存储检索增强生成（RAG）、LLM编排器及模型上下文协议（MCP）干预，为冻结的VLA策略提供上下文自适应能力。实验表明，SOMA在多个基准测试中显著提升了任务成功率，尤其在长程任务链中表现优异。
selection_source: fresh_fetch
motivation: 现有的VLA模型因缺乏长期记忆、因果失败归因和动态干预能力，在面对感知噪声和环境变化时鲁棒性受限。
method: 提出一种结合双存储RAG、LLM编排器和MCP干预的在线框架，并辅以离线存储巩固模块来提炼执行轨迹。
result: "在LIBERO-PRO等基准上，SOMA使平均成功率提升了56.6%，在长程任务链中更是实现了89.1%的显著增长。"
conclusion: SOMA证明了通过外部记忆增强和策略编排，可以在不改变模型参数的情况下显著增强VLA模型的鲁棒性和自适应能力。
---

## 摘要
尽管视觉-语言-动作（VLA）模型作为通用机器人控制器展现出巨大潜力，但由于缺乏长期记忆、因果故障归因和动态干预能力，它们在分布外（OOD）任务中应对感知噪声和环境变化的鲁棒性仍然受到根本性限制。为此，我们提出了 SOMA，一种策略编排与记忆增强系统，可在无需参数微调的情况下升级冻结的 VLA 策略，实现鲁棒的上下文自适应。具体而言，SOMA 通过由对比式双记忆检索增强生成（RAG）、归因驱动的大语言模型（LLM）编排器以及可扩展的模型上下文协议（MCP）干预组成的在线流水线运行，同时利用离线记忆整合模块将执行轨迹持续提炼为可靠的先验知识。在 LIBERO-PRO 和我们提出的 LIBERO-SOMA 基准测试中，对三种骨干模型（pi0、pi0.5 和 SmolVLA）进行的实验评估表明，SOMA 实现了 56.6% 的平均绝对成功率提升。这包括在长程任务链中 89.1% 的显著绝对提升。项目页面和源代码可在以下网址获取：https://github.com/LZY-1021/SOMA。

## Abstract
Despite the promise of Vision-Language-Action (VLA) models as generalist robotic controllers, their robustness against perceptual noise and environmental variations in out-of-distribution (OOD) tasks remains fundamentally limited by the absence of long-term memory, causal failure attribution, and dynamic intervention capability. To address this, we propose SOMA, a Strategic Orchestration and Memory-Augmented System that upgrades frozen VLA policies for robust in-context adaptation without parameter fine-tuning. Specifically, SOMA operates through an online pipeline of contrastive Dual-Memory Retrieval-Augmented Generation (RAG), an Attribution-Driven Large-Language-Model (LLM) Orchestrator, and extensible Model Context Protocol (MCP) interventions, while an offline Memory Consolidation module continuously distills the execution traces into reliable priors. Experimental evaluations across three backbone models (pi0, pi0.5, and SmolVLA) on LIBERO-PRO and our proposed LIBERO-SOMA benchmarks demonstrate that SOMA achieves an average absolute success rate gain of 56.6%. This includes a significant absolute improvement of 89.1% in long-horizon task chaining. Project page and source code are available at: https://github.com/LZY-1021/SOMA.

---

## 论文详细总结（自动生成）

### 论文总结：SOMA 系统

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型虽然在通用机器人控制方面表现出色，但在面对分布外（OOD）任务时鲁棒性极差。
*   **研究动机**：VLA 模型通常是“无状态”的控制器，缺乏长期记忆、因果故障归因能力以及动态干预机制。当环境发生感知噪声（如光照、遮挡）、语言歧义或任务链过长时，模型容易失效。传统的微调方法成本高且难以覆盖所有 OOD 情况，因此需要一种无需微调参数、能实现“上下文自适应”的增强系统。

#### 2. 方法论：核心思想与技术细节
SOMA 提出了一个解耦的架构，包含**在线执行**和**离线记忆巩固**两个流水线：
*   **双存储检索增强生成 (Dual-Memory RAG)**：
    *   构建了一个包含“成功经验”和“失败教训”的双分区存储库。
    *   利用 CLIP 编码器将当前任务与存储库中的历史轨迹进行相似度匹配，为后续决策提供对比参考。
*   **归因驱动的 LLM 编排器 (LLM Orchestrator)**：
    *   使用 Qwen3-VL-32B 作为推理引擎，诊断当前状态与历史成功/失败案例之间的差异。
    *   根据诊断结果，从工具库中选择并参数化相应的干预策略。
*   **可扩展的 MCP 干预工具库**：
    *   **Paint-to-Action**：通过视觉覆盖解决领域偏移。
    *   **Eraser**：擦除无关干扰物以减轻因果混淆。
    *   **Prompt-Refiner**：规范化模糊或带有噪声的指令。
    *   **Chaining-Step**：将长程任务分解为短程子任务。
    *   **Encore**：在执行停滞或失败时进行关键步骤的回滚与重试。
*   **双阶段记忆巩固 (Offline Consolidation)**：
    *   离线对新轨迹进行初步诊断并存入库。
    *   通过跨任务差异分析，不断修正和优化存储库中的故障归因和干预计划。

#### 3. 实验设计
*   **数据集/场景**：
    *   **LIBERO-PRO**：测试位置交换（Pos）和任务语义偏移（Task）。
    *   **LIBERO-SOMA**（本文提出）：包含视觉聚焦、杂乱移除、噪声指令、执行脆弱性和长程任务链五个维度。
*   **基准模型 (Backbones)**：$\pi_0$、$\pi_{0.5}$ 和 SmolVLA。
*   **对比方法**：主要对比了各 VLA 模型的原始版本（Base），并进行了针对 RAG 配置（无 RAG、仅成功、仅失败、双存储）的消融实验。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **模型细节**：提到使用了 Qwen3-VL-32B 作为 LLM 编排器的底层引擎，SAM3 用于图像分割，OpenCV 用于图像修复。由于 SOMA 是参数冻结的框架，其主要开销在于推理时的 LLM 调用和图像处理。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 25 个多样化任务，每个任务进行 199 次随机初始化轮次（Rollouts），以减少随机偏差。
*   **充分性**：实验覆盖了主流的 VLA 骨干模型，并针对不同类型的 OOD 干扰（视觉、语言、时序）进行了详尽测试。
*   **客观性**：通过消融实验验证了双存储机制和双阶段巩固的必要性，使用了“平均成功转向数”等量化指标，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **性能提升显著**：SOMA 在 LIBERO-PRO 和 LIBERO-SOMA 上平均绝对成功率提升了 **56.6%**。
*   **长程任务突破**：在长程任务链中，SOMA 实现了 **89.1%** 的惊人提升，证明了任务分解与重试机制的有效性。
*   **鲁棒性增强**：在原始模型几乎无法处理的语义偏移和位置交换场景下，SOMA 成功恢复了大部分性能（从接近 0% 提升至 50% 以上）。

#### 7. 优点
*   **即插即用 (Plug-and-play)**：无需对底层 VLA 模型进行任何参数微调，具有极强的通用性。
*   **闭环自进化**：通过离线记忆巩固机制，系统能够从失败中学习，实现经验的持续提炼。
*   **多模态干预**：结合了视觉处理、语言重写和时序控制，能够全方位应对机器人执行中的各类干扰。

#### 8. 不足与局限
*   **推理延迟**：引入 LLM 编排、RAG 检索和图像处理工具（如 SAM3、Inpainting）会显著增加单步动作的推理延迟，可能影响实时性要求极高的任务。
*   **依赖强 LLM**：系统的诊断和编排高度依赖于底层 LLM（如 Qwen3-VL）的推理能力，若 LLM 发生误判，干预措施可能适得其反。
*   **工具库覆盖面**：虽然 MCP 具有扩展性，但目前预设的五种工具可能仍无法覆盖所有极端的物理交互故障。

（完）
