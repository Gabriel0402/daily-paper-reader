---
title: "DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion"
title_zh: DiffusionAnything：用于统一导航与抓取前运动的端到端上下文扩散学习
authors: "Iana Zhura, Yara Mahmoud, Jeffrin Sam, Hung Khang Nguyen, Didar Seyidov, Miguel Altamirano Cabrera, Dzmitry Tsetserukou"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26322v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于机器人导航和操作的VLA模型
tldr: 针对机器人视觉运动规划中目标指定复杂及模型资源消耗大的问题，本文提出 DiffusionAnything。这是一个统一的图像空间扩散策略，通过多尺度特征调制，仅需 5 分钟自监督数据即可实现米级导航与厘米级抓取前运动。该模型仅依赖 RGB 输入，具备低显存占用和高推理频率，在未知场景中展现出强大的零样本泛化能力。
selection_source: fresh_fetch
motivation: 现有的视觉语言动作模型计算资源需求高且在陌生场景中泛化能力差，难以实现高效且通用的端到端运动规划。
method: 提出一种基于多尺度 FiLM 调制、轨迹对齐深度预测和自监督注意力机制的统一图像空间扩散策略框架。
result: 该模型仅需 2.0 GB 显存即可实现 10 Hz 的实时推理，并在新场景中表现出卓越的零样本泛化性能。
conclusion: 研究证明了轻量级扩散模型结合自监督学习，能够有效统一跨尺度的机器人导航与操作任务，适合机载部署。
---

## 摘要
直接从视觉高效预测运动规划仍然是机器人领域的一个基本挑战，通常规划需要明确的目标说明和特定任务的设计。最近的视觉-语言-动作（VLA）模型直接从视觉输入推断动作，但需要巨大的计算资源和大量的训练数据，且在面对新场景时无法实现零样本泛化。我们提出了一种统一的图像空间扩散策略，通过多尺度特征调制，仅需每个任务 5 分钟的自监督数据，即可处理米级导航和厘米级操作。该框架由三项关键创新驱动：(1) 基于任务模式、深度尺度和空间注意力的多尺度 FiLM 条件化，使单一模型能够实现适应任务的行为；(2) 轨迹对齐的深度预测将度量 3D 推理集中在生成的路径点上；(3) 来自 AnyTraverse 的自监督注意力实现了无需视觉语言模型和深度传感器的目标导向推理。该模型纯粹基于 RGB 输入运行（2.0 GB 内存，10 Hz），在实现对新场景的鲁棒零样本泛化的同时，仍适用于机载部署。

## Abstract
Efficiently predicting motion plans directly from vision remains a fundamental challenge in robotics, where planning typically requires explicit goal specification and task-specific design. Recent vision-language-action (VLA) models infer actions directly from visual input but demand massive computational resources, extensive training data, and fail zero-shot in novel scenes. We present a unified image-space diffusion policy handling both meter-scale navigation and centimeter-scale manipulation via multi-scale feature modulation, with only 5 minutes of self-supervised data per task. Three key innovations drive the framework: (1) Multi-scale FiLM conditioning on task mode, depth scale, and spatial attention enables task-appropriate behavior in a single model; (2) trajectory-aligned depth prediction focuses metric 3D reasoning along generated waypoints; (3) self-supervised attention from AnyTraverse enables goal-directed inference without vision-language models and depth sensors. Operating purely from RGB input (2.0 GB memory, 10 Hz), the model achieves robust zero-shot generalization to novel scenes while remaining suitable for onboard deployment.

---

## 论文详细总结（自动生成）

以下是对论文《DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在单一模型中统一处理机器人**米级（meter-scale）的导航**与**厘米级（centimeter-scale）的精细操作（抓取前运动规划）**。
*   **研究动机**：
    *   **传统架构缺陷**：导航与操作通常被视为独立领域，采用级联架构，容易产生复合误差、高延迟且切换僵硬。
    *   **现有VLA模型局限**：如RT-2、OpenVLA等视觉-语言-动作模型虽然泛化性强，但计算成本极高（需大算力GPU）、推理延迟高，且在未知场景的零样本（Zero-shot）表现往往不佳。
    *   **目标**：开发一种轻量级、纯RGB输入、具备上下文感知能力且易于机载部署的统一扩散策略框架。

### 2. 方法论
核心思想是利用**上下文感知的扩散策略（Context-Aware Diffusion Policy）**，通过多尺度特征调制实现跨任务学习。
*   **多尺度 FiLM 调制**：在UNet结构的编码器中引入特征线性调制（FiLM）。上下文向量 $c$ 包含：任务模式（导航/抓取）、深度尺度（米/厘米）和空间注意力图。通过在不同尺度（64x64到8x8）应用调制，使模型在导航时关注宏观自由空间，在抓取时关注微观物体细节。
*   **轨迹对齐的深度推理**：模型不预测全图深度，而是仅沿生成的轨迹路径点预测深度。这种设计降低了计算开销，同时保证了规划所需的度量 3D 精度。
*   **自监督注意力与目标学习**：利用 AnyTraverse 框架生成自监督的注意力图作为监督信号。模型支持两种模式：根据场景上下文自动选择目标，或通过参考图像特征匹配指定目标。
*   **损失函数**：总损失 $L_{total}$ 由扩散去噪损失、深度预测损失、可通行性（Traversability）重建损失和注意力图重建损失加权组成。

### 3. 实验设计
*   **实验场景**：包括室内走廊（导航）、带有各种物体的桌面（抓取前规划）。
*   **机器人平台**：Unitree G1 人形机器人，配备单目 RGB 摄像头。
*   **Benchmark 与对比方法**：
    *   **导航**：对比了 **NoMaD**（基于扩散的视觉导航基准）。
    *   **操作/移动操作**：对比了 NVIDIA 的 **GR00T (n1.5/n1.6)** 模型。
*   **评估指标**：成功率（SR）、避障间距（Clearance）、目标距离误差（Dist）。

### 4. 资源与算力
*   **训练资源**：使用单张 **NVIDIA RTX 4090 GPU**。
    *   **预训练**：使用 20 分钟视频数据，训练 50 个 epoch，耗时约 2 小时。
    *   **任务适配**：针对新任务仅需 5 分钟新数据，微调 10 个 epoch，耗时 15-20 分钟。
*   **推理性能**：显存占用仅 **2.0 GB**，推理频率达 **10 Hz**，非常适合机载实时部署。

### 5. 实验数量与充分性
*   **实验组别**：涵盖了自主探索导航、目标导向导航、抓取前运动规划三大类任务。
*   **充分性评价**：
    *   实验对比了当前最先进的导航模型（NoMaD）和人形机器人基础模型（GR00T），具有较强的说服力。
    *   进行了跨尺度的精度验证（导航误差 vs 抓取误差）。
    *   **局限性**：虽然在人形机器人上进行了实测，但样本量（如某些任务 n=8 或 10 次试验）相对较小，且主要集中在室内静态环境。

### 6. 主要结论与发现
*   **统一性验证**：单一模型通过上下文切换，能同时实现米级避障（100%成功率）和厘米级精细接近（4.71 cm 平均误差）。
*   **零样本泛化**：在完全未见过的场景中，该模型表现优于重型 VLA 模型。GR00T 在新场景中出现了系统性失败，而本方法通过显式几何推理（可通行性图和空间注意力）保持了鲁棒性。
*   **高效性**：仅需极少量的自监督数据（5分钟）即可完成新任务适配，无需人工标注或遥操作演示。

### 7. 优点
*   **轻量高效**：极低的显存占用和高推理频率，打破了 VLA 模型对大算力的依赖。
*   **自监督驱动**：利用 AnyTraverse 自动生成标签，极大降低了数据采集成本。
*   **跨尺度精度**：通过深度尺度调制，成功在同一个网络中解决了精度需求差异巨大的两类问题。
*   **部署友好**：纯 RGB 输入，无需昂贵的深度传感器或复杂的视觉语言编码器。

### 8. 不足与局限
*   **监督依赖**：高度依赖 AnyTraverse 生成的伪标签质量，若底层视觉模型出错，会影响策略性能。
*   **触觉缺失**：目前仅限于“抓取前”的运动规划，缺乏接触后的力反馈和闭环操作逻辑。
*   **环境复杂度**：实验场景相对受控，在极度拥挤或动态变化剧烈的环境中的表现尚待验证。
*   **任务范围**：目前主要针对导航和接近物体，尚未扩展到更复杂的长程逻辑任务（如“去厨房洗杯子”）。

（完）
