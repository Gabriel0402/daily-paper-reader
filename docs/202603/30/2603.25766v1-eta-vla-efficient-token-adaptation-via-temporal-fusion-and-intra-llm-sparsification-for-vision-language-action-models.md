---
title: "ETA-VLA: Efficient Token Adaptation via Temporal Fusion and Intra-LLM Sparsification for Vision-Language-Action Models"
title_zh: ETA-VLA：通过时间融合与 LLM 内部稀疏化实现的视觉-语言-动作模型高效 Token 适配
authors: "Yiru Wang, Anqing Jiang, Shuo Wang, Yuwen Heng, Zichong Gu, Hao Sun"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25766v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 具有时序融合功能的自动驾驶视觉-语言-动作模型
tldr: 针对自动驾驶中视觉-语言-动作（VLA）模型处理多视图历史帧带来的巨大计算开销，本文提出ETA-VLA框架。该框架引入了LLM内部稀疏聚合器（ILSA），模仿人类驾驶员的注意力分配，通过文本引导和时间一致性动态剪枝冗余视觉标记。实验证明，该方法在保持高性能的同时，显著降低了计算量和推理开销，实现了效率与精度的平衡。
selection_source: fresh_fetch
motivation: 自动驾驶VLA模型在处理多视图历史帧时，受限于LLM自注意力机制的平方复杂度，面临严重的计算瓶颈。
method: 提出ETA-VLA框架，利用文本引导的评分机制和多样性保持稀疏化策略，在LLM内部动态识别并剪枝冗余视觉标记。
result: "在NAVSIM v2基准上，该方法在减少32%至61%计算量的同时，仍能保持与最先进模型相当的驾驶性能。"
conclusion: ETA-VLA通过高效的标记适配技术，成功在保证自动驾驶精度的前提下大幅提升了VLA模型的推理效率。
---

## 摘要
将视觉-语言-动作（VLA）模型集成到自动驾驶系统中，为解释复杂场景和执行控制指令提供了一个统一的框架。然而，为了实现准确的时间推理，必须纳入历史多视图帧，这带来了沉重的计算负担，主要源于大语言模型（LLM）中自注意力机制的二次方复杂度。为了缓解这一瓶颈，我们提出了 ETA-VLA，一种用于 VLA 模型的高效 Token 适配框架。ETA-VLA 处理过去 $n$ 帧的多视图图像，并引入了一种新颖的 LLM 内部稀疏聚合器（ILSA）。受人类驾驶员注意力分配的启发，ILSA 在文本查询和时间一致性的引导下，动态地识别并剪枝冗余的视觉 Token。具体而言，我们利用文本引导的评分机制以及保持多样性的稀疏化策略来选择关键 Token 的稀疏子集，从而确保对驾驶场景的全面感知。在 NAVSIM v2 上的广泛实验表明，ETA-VLA 实现了与最先进基准模型相当的驾驶性能，同时将计算 FLOPs 降低了约 32%。值得注意的是，我们的方法剪枝了 85% 的视觉 Token，并将推理 FLOPs 降低了 61%，但在 NAVSIM v2 基准测试中仍保留了 94% 的原始准确率。

## Abstract
The integration of Vision-Language-Action (VLA) models into autonomous driving systems offers a unified framework for interpreting complex scenes and executing control commands. However, the necessity to incorporate historical multi-view frames for accurate temporal reasoning imposes a severe computational burden, primarily driven by the quadratic complexity of self-attention mechanisms in Large Language Models (LLMs). To alleviate this bottleneck, we propose ETA-VLA, an Efficient Token Adaptation framework for VLA models. ETA-VLA processes the past $n$ frames of multi-view images and introduces a novel Intra-LLM Sparse Aggregator (ILSA). Drawing inspiration from human driver attention allocation, ILSA dynamically identifies and prunes redundant visual tokens guided by textual queries and temporal consistency. Specifically, we utilize a text-guided scoring mechanism alongside a diversity-preserving sparsification strategy to select a sparse subset of critical tokens, ensuring comprehensive awareness of the driving scene. Extensive experiments on the NAVSIM v2 demonstrate that ETA-VLA achieves driving performance comparable to state-of-the-art baselines while reducing computational FLOPs by approximately 32\%. Notably, our method prunes 85% of visual tokens and reduces inference FLOPs by 61\%, but still retaining 94% of the original accuracy on the NAVSIM v2 benchmark.

---

## 论文详细总结（自动生成）

以下是对论文《ETA-VLA: Efficient Token Adaptation via Temporal Fusion and Intra-LLM Sparsification for Vision-Language-Action Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在自动驾驶领域，视觉-语言-动作（VLA）模型需要处理多视图（通常6个摄像头）和长历史帧（多帧时间序列）来提取时空特征。这种高维输入导致 Token 数量爆炸，由于大语言模型（LLM）自注意力机制具有二次方复杂度，计算开销（FLOPs）变得极其沉重，难以在车载嵌入式硬件上实时运行。
*   **研究动机**：模仿人类驾驶员的注意力分配机制——即在复杂场景中动态关注关键区域（如前方车辆、红绿灯），同时保持对周边环境（如盲区）的基本感知，从而在保证安全的前提下降低认知负担。

### 2. 方法论：核心思想与关键技术
ETA-VLA 框架主要由两个互补模块组成：
*   **时间融合模块 (TFM)**：
    *   在进入 LLM 之前，将过去 $n$ 帧的多视图特征进行压缩。
    *   利用可学习的时间嵌入（Time Embedding）和时间加权聚合机制，将多帧特征融合为单一的紧凑特征图，有效缓解了 Token 数量随时间线性增长的问题。
*   **LLM 内部稀疏聚合器 (ILSA)**：
    *   **动态文本锚点选择**：根据当前驾驶指令（如“左转”），动态识别 LLM 中与之相关的文本 Token 作为评分参考。
    *   **无 RoPE 语义评分**：在计算视觉 Token 重要性时，移除旋转位置嵌入（RoPE）的影响，消除因 Token 距离导致的偏置，确保评分纯粹基于语义相关性。
    *   **多样性保持稀疏化策略**：采用两阶段选择。首先全局选择 Top-K 个语义最相关的 Token；其次通过计算特征多样性得分，回收被剪枝但具有高信息独特性的 Token（如侧后方视图中的关键目标），防止丢失盲区信息。
*   **动作解码**：使用轻量级 Transformer 解码器将 LLM 输出的稀疏特征映射为连续的未来轨迹。

### 3. 实验设计
*   **数据集与场景**：主要在 **NAVSIM v2** 基准上进行评估，包含 **Navtest**（开路评估）和 **Navhard**（具有扰动的伪闭环评估）两个子集。
*   **评估指标**：使用 **EPDMS**（扩展预测驾驶员模型得分），综合考量碰撞风险（NC）、车道保持（LK）、舒适度（HC/EC）等 9 个维度。
*   **对比方法**：包括 Transfuser、VADv2、HydraMDP++、GTRS、DiffusionDrive 等当前最先进的（SOTA）自动驾驶规划模型。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A800 GPU**。
*   **训练细节**：
    *   第一阶段：在 trainval 集合上预训练投影层和适配器（1 个 epoch，batch size 32）。
    *   第二阶段：在 navtrain 集合上进行全模型微调（1 个 epoch，batch size 8）。
*   **模型基础**：基于 LLaVA-v1.5-7B（ViT-L/14 视觉编码器 + Vicuna-7B LLM）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多项定量对比实验和消融实验。
*   **消融实验**：
    *   验证了 TFM 和 ILSA 模块的必要性。
    *   测试了不同 LLM 层（Layer 2, 4, 6, 8）进行剪枝的效果。
    *   对比了不同的剪枝策略（随机剪枝、HoloV、SparseVLM）。
*   **充分性评价**：实验设计较为全面，涵盖了从开路到闭环的多种复杂场景，且通过对比不同剪枝率（35% 到 85%）展示了性能与效率的权衡曲线，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：ETA-VLA 在 Navtest 上取得了 **85.0** 的 EPDMS 得分，刷新了 SOTA 纪录，非常接近人类专家水平（90.3）。
*   **高效节能**：在剪枝 35% Token 的情况下，计算量减少了 **32%**，且性能反而略优于全量 Token 的基准模型（说明 ILSA 起到了去噪作用）。
*   **极限压缩**：在剪枝 85% Token 时，推理 FLOPs 降低了 **61%**，仍能保留 94% 的原始精度。
*   **鲁棒性**：在更难的 Navhard 任务中，ETA-VLA 同样表现出优于其他模型的鲁棒性。

### 7. 优点（亮点）
*   **人类启发式设计**：将人类驾驶员的注意力分配逻辑转化为算法（语义关注+多样性回收），解决了 VLA 模型在处理多视图数据时的冗余问题。
*   **无 RoPE 评分机制**：敏锐地察觉到 LLM 原有的位置编码会干扰视觉 Token 的语义评分，并提出了有效的解决方案。
*   **效率与精度双赢**：不仅降低了计算成本，还通过过滤无关背景噪声提升了规划的准确性。

### 8. 不足与局限
*   **数据集单一性**：实验主要集中在 NAVSIM v2 模拟器上，尚未在真实的物理道路测试中验证其泛化能力。
*   **层级敏感性**：消融实验显示，剪枝发生的 LLM 层级对结果影响极大（深层剪枝会导致性能剧降），这意味着模型对超参数设置较为敏感。
*   **实时性定义**：虽然 FLOPs 大幅下降，但论文未详细给出在特定嵌入式硬件（如 NVIDIA Orin）上的实际推理延迟（Latency）数据。

（完）
