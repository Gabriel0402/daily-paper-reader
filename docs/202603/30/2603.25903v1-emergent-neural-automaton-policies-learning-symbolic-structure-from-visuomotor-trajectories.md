---
title: "Emergent Neural Automaton Policies: Learning Symbolic Structure from Visuomotor Trajectories"
title_zh: 涌现神经自动机策略：从视觉运动轨迹中学习符号结构
authors: "Yiyuan Pan, Xusheng Luo, Hanjiang Hu, Peiqi Yu, Changliu Liu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25903v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 从视觉运动轨迹中学习符号结构和 Mealy 状态机
tldr: "针对长程机器人学习中端到端模型缺乏结构先验及神经符号方法依赖手工设计的问题，本文提出ENAP框架。该框架通过自适应聚类和扩展L*算法从视觉运动演示中自动推断Mealy状态机作为高层规划器，并结合底层残差网络实现连续控制。ENAP在无需任务标签的情况下，显著提升了样本效率和可解释性，在复杂任务中性能优于现有SOTA模型。"
selection_source: fresh_fetch
motivation: 旨在解决端到端策略在长程任务中缺乏结构推理能力，以及传统神经符号方法过度依赖手工符号先验的局限性。
method: "提出一种双层神经符号策略，利用L*算法从数据中自动学习离散状态机作为高层规划，并配合底层残差网络进行行为克隆。"
result: "在复杂操作和长程任务中，ENAP在低数据量情况下的表现比现有SOTA视觉语言动作策略高出多达27%。"
conclusion: ENAP证明了通过自动学习离散任务结构与连续控制相结合，可以有效提升机器人学习的效率、泛化能力和可解释性。
---

## 摘要
将机器人学习扩展到长程任务仍然是一个巨大的挑战。虽然端到端策略通常缺乏有效长期推理所需的结构先验，但传统的神经符号方法又过度依赖手工设计的符号先验。为了解决这一问题，我们提出了 ENAP（涌现神经自动机策略），这是一个允许双层神经符号策略从视觉运动演示中自适应涌现的框架。具体而言，我们首先采用自适应聚类和 L* 算法的扩展版本，从视觉运动数据中推断出 Mealy 状态机，作为捕获潜在任务模式的可解释高层规划器。然后，这种离散结构引导低层反应式残差网络通过行为克隆（BC）学习精确的连续控制。通过利用离散转移和连续残差显式建模任务结构，ENAP 在不需要任务特定标签的情况下实现了高样本效率和可解释性。在复杂操作和长程任务上的广泛实验表明，ENAP 在低数据量情况下的性能优于最先进的（SoTA）端到端 VLA 策略高达 27%，同时提供了机器人意图的结构化表示。

## Abstract
Scaling robot learning to long-horizon tasks remains a formidable challenge. While end-to-end policies often lack the structural priors needed for effective long-term reasoning, traditional neuro-symbolic methods rely heavily on hand-crafted symbolic priors. To address the issue, we introduce ENAP (Emergent Neural Automaton Policy), a framework that allows a bi-level neuro-symbolic policy adaptively emerge from visuomotor demonstrations. Specifically, we first employ adaptive clustering and an extension of the L* algorithm to infer a Mealy state machine from visuomotor data, which serves as an interpretable high-level planner capturing latent task modes. Then, this discrete structure guides a low-level reactive residual network to learn precise continuous control via behavior cloning (BC). By explicitly modeling the task structure with discrete transitions and continuous residuals, ENAP achieves high sample efficiency and interpretability without requiring task-specific labels. Extensive experiments on complex manipulation and long-horizon tasks demonstrate that ENAP outperforms state-of-the-art (SoTA) end-to-end VLA policies by up to 27% in low-data regimes, while offering a structured representation of robotic intent (Fig. 1).

---

## 论文详细总结（自动生成）

以下是对论文《Emergent Neural Automaton Policies: Learning Symbolic Structure from Visuomotor Trajectories》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何让机器人从原始的视觉运动轨迹中自动学习出逻辑结构（System 2 推理），而不需要依赖手工设计的符号先验（如 PDDL 谓词）？
*   **研究背景**：
    *   **端到端策略（如 VLA 模型）**：在短程任务中表现良好，但在长程任务中缺乏结构先验，且属于“黑盒”模型，数据需求量极大。
    *   **传统神经符号方法**：虽然具有可解释性，但通常需要专家预定义符号逻辑，难以自适应地从数据中提取任务本质。
*   **动机**：结合人类认知的双系统理论（System 1 反应式控制 + System 2 符号化推理），提出一种“神经自动机策略”，实现离散逻辑规划与连续残差控制的统一。

### 2. 方法论
ENAP 框架由三个核心阶段组成，采用 **EM（期望最大化）风格** 的迭代优化流程：

*   **自适应符号抽象（Symbol Abstraction）**：
    *   利用视觉编码器（如 DINOv2）提取特征，通过 **HDBSCAN 聚类算法** 将连续观测空间离散化为符号字母表 $\Sigma$。
    *   训练一个 **RNN 历史编码器**，将变长的历史轨迹映射为固定维度的嵌入 $h_t$，并引入“相位感知对比损失”来区分不同的任务阶段。
*   **扩展 L* 算法的结构提取（Structure Extraction）**：
    *   **改进 L* 算法**：将经典的离散自动机学习算法扩展到连续机器人数据。
    *   **成员查询（MQ）**：通过检索演示数据库，检查当前历史嵌入是否属于已知状态（闭合性检查）。
    *   **等价查询（EQ）**：通过非确定性路径搜索验证假设模型，若无法解释测试轨迹，则将其作为反例引入以细化状态机。
    *   **结果**：推断出一个 **概率 Mealy 状态机（PMM）**，节点代表任务阶段（如“抓取”、“对齐”），边代表动作先验和转移逻辑。
*   **双层控制管线（Bi-level Control）**：
    *   **高层**：PMM 根据当前状态和观测符号提供粗略的动作先验 $a_{base}$。
    *   **底层**：一个轻量级的**残差网络** $\psi$ 根据实时观测对 $a_{base}$ 进行微调，生成最终精确动作。

### 3. 实验设计
*   **实验场景**：
    1.  **复杂操作任务（ManiSkill 模拟器）**：PegInsert（插拔）、DualStackCube（双块堆叠）、MultiGoalPushT（多目标推 T 字块）。
    2.  **长程 TAMP 任务（CALVIN 基准）**：包含 5 个子任务的顺序执行和具有依赖关系的层次化任务。
    3.  **真实世界操作**：使用 Kinova Gen3 机械臂完成 StackLego（乐高堆叠）、MultiPickPlace（颜色匹配分类）、Hanger（挂衣架）。
*   **对比基准（Baselines）**：
    *   **生成式/VLA 策略**：OpenVLA (7.6B 参数)、$\pi_0$、Diffusion Policy。
    *   **传统模仿学习**：Transformer、GMM、BC。
    *   **长程基准**：HULC、MDT、FLOWER。

### 4. 资源与算力
*   **参数量对比**：ENAP 显著更轻量。例如在复杂操作中，ENAP 仅需约 23M 参数，而 OpenVLA 超过 7.6B 参数。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到在推理速度上，ENAP 在真实机器人上可达 281ms/步，远快于 $\pi_{0.5}$ 的 6841ms/步。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了模拟环境与真实世界，任务类型从简单的拾取到复杂的长程逻辑推理。
*   **数据效率实验**：专门测试了在 25%、50%、100% 数据量下的表现，证明了 ENAP 在低数据量下的鲁棒性。
*   **消融实验**：针对相似度阈值 $\tau_{sim}$、RNN 损失函数等关键超参数进行了消融分析。
*   **评价指标**：除了成功率，还引入了节点效率（SRN）、动作先验保真度（APF）、语义分离度（CSS）等 6 个结构化指标。
*   **结论**：实验设计较为全面，对比了当前最先进的大模型，结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **性能卓越**：在低数据量情况下，ENAP 的成功率比 SOTA VLA 模型高出多达 **27%**。
*   **高效轻量**：在性能提升的同时，参数量减少了约 **39%**（相比同类蒸馏模型），推理速度大幅提升。
*   **涌现结构**：ENAP 能够自动发现任务中的循环依赖（用于错误恢复）和逻辑分支（用于多模态目标）。
*   **错误恢复能力**：通过学习到的状态机自循环（Self-loop），机器人能在对齐失败时自动尝试重新调整，而非直接崩溃。

### 7. 优点与亮点
*   **无需标签（Label-free）**：完全从无标注的视觉运动演示中学习符号结构，消除了对专家谓词的依赖。
*   **高可解释性**：学习到的 Mealy 机器可以可视化为人类可理解的状态转移图，方便调试和验证。
*   **双层架构的优雅性**：将离散推理的确定性与连续控制的灵活性结合，解决了端到端模型在长程任务中的“遗忘”和“迷失”问题。

### 8. 不足与局限
*   **超参数敏感性**：结构提取的质量高度依赖于聚类阈值 $\tau_{sim}$，设置不当可能导致状态爆炸或过于简化。
*   **泛化限制**：目前主要针对特定任务的结构提取，如何跨任务、跨机器人（Cross-embodiment）迁移学习到的抽象符号仍是开放性挑战。
*   **演示质量依赖**：作为模仿学习框架，其性能上限仍受限于专家演示的覆盖范围和质量。

（完）
