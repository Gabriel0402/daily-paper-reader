---
title: Memory-Augmented Vision-Language Agents for Persistent and Semantically Consistent Object Captioning
title_zh: 记忆增强型视觉语言智能体：实现持久且语义一致的物体描述
authors: "Tommaso Galliena, Stefano Rosa, Tommaso Apicella, Pietro Morerio, Alessio Del Bue, Lorenzo Natale"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24257v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于具身语义一致性的视觉语言智能体
tldr: 针对视觉语言模型在不同视角下对同一物体描述不一致的问题，本文提出了一种增强记忆的视觉语言智能体。该模型将数据关联、物体描述和探索策略集成在统一的自回归框架中，通过物体级情节记忆确保跨序列的语义一致性。通过自监督学习和多视图伪标签训练，该方法在物体描述准确性和自相似性上显著优于现有基准，实现了可扩展且一致的场景表征。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在多视角下对同一物体的描述往往缺乏一致性，且现有方法多采用离线或多阶段流程，难以实现实时的语义持久性。
method: 提出一种统一的自回归框架，利用当前观测、俯视图和序列化的物体级情节记忆令牌，同步处理数据关联、物体描述和探索策略。
result: "在手动标注的测试集上，该模型在标准描述指标上提升了11.86%，在描述自相似性上提升了7.39%。"
conclusion: 该研究证明了通过集成物体级记忆和自监督训练，可以有效提升具身智能体在复杂环境中的语义一致性和描述性能。
---

## 摘要
视觉语言模型（VLM）在不同视角下对同一物体的描述往往不一致，这阻碍了具身智能体随时间构建一致语义表示的能力。以往的方法通过离线多视图聚合或多阶段流水线来解决不一致性，这些流水线将探索、数据关联和描述学习解耦，对先前观察到的物体的推理能力有限。在本文中，我们提出了一种统一的记忆增强型视觉语言智能体，它在单个自回归框架内同时处理数据关联、物体描述和探索策略。该模型处理当前的 RGB 观测、自顶向下的探索地图以及序列化为物体级标记（tokens）的物体级情景记忆，从而确保在长序列中保持持久的物体身份和语义一致性。为了以自监督方式训练模型，我们利用基于分歧的策略和伪描述模型在逼真的 3D 环境中收集了一个数据集，该模型强制要求多视图描述历史之间的一致性。在手动标注的物体级测试集上的广泛评估表明，与基准模型相比，标准描述评分提高了高达 11.86%，描述自相似性提高了 7.39%，同时通过紧凑的场景表示实现了可扩展的性能。代码、模型权重和数据可在 https://github.com/hsp-iit/epos-vlm 获取。

## Abstract
Vision-Language Models (VLMs) often yield inconsistent descriptions of the same object across viewpoints, hindering the ability of embodied agents to construct consistent semantic representations over time. Previous methods resolved inconsistencies using offline multi-view aggregation or multi-stage pipelines that decouple exploration, data association, and caption learning, with limited capacity to reason over previously observed objects. In this paper, we introduce a unified, memory-augmented Vision-Language agent that simultaneously handles data association, object captioning, and exploration policy within a single autoregressive framework. The model processes the current RGB observation, a top-down explored map, and an object-level episodic memory serialized into object-level tokens, ensuring persistent object identity and semantic consistency across extended sequences. To train the model in a self-supervised manner, we collect a dataset in photorealistic 3D environments using a disagreement-based policy and a pseudo-captioning model that enforces consistency across multi-view caption histories. Extensive evaluation on a manually annotated object-level test set, demonstrate improvements of up to +11.86% in standard captioning scores and +7.39% in caption self-similarity over baseline models, while enabling scalable performance through a compact scene representation. Code, model weights, and data are available at https://github.com/hsp-iit/epos-vlm

---

## 论文详细总结（自动生成）

### 论文总结：记忆增强型视觉语言智能体（EPOS-VLM）

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉语言模型（VLM）在具身导航任务中，对同一个物理物体在不同视角、距离或遮挡情况下，往往会给出不一致甚至矛盾的描述（语义漂移）。
*   **研究背景**：
    *   **缺乏持久性**：静态图像训练的 VLM 缺乏物体恒常性认知。
    *   **流程解耦**：以往解决一致性的方法通常将探索、数据关联和描述生成分为多个独立阶段，导致智能体无法根据当前的语义不确定性主动调整观察视角。
    *   **研究目标**：开发一个统一的框架，使智能体能够通过记忆维持物体身份，并在导航过程中主动消除语义歧义，生成一致的物体描述。

#### 2. 方法论
*   **核心思想**：提出 **EPOS-VLM**，这是一个统一的、记忆增强的视觉语言-动作（VLA）模型。它将**数据关联**、**物体描述**和**导航动作**集成在单个自回归 Transformer 框架中。
*   **关键技术细节**：
    *   **物体级情节记忆（Episodic Memory）**：建立一个结构化数据库，存储物体的持久 ID、3D 位置以及历史描述频率。
    *   **记忆令牌化（Tokenization）**：将上述记忆序列化为特定格式的文本令牌（如 `[OBJ-ID]`, `[POS]`, `[CAP-HISTORY]`），直接作为 VLM 的上下文输入，无需修改模型架构。
    *   **统一输出序列**：模型在每个时间步输出一个结构化序列：`[MATCH]`（判定当前物体是旧的还是新的）、`[CAPTION]`（生成一致性描述）和 `[ACTION]`（决定下一步动作）。
    *   **3D-CPS 伪标签生成器**：在训练阶段，利用 3D 感知的伪描述方法，通过聚合多视图历史并过滤视角相关的噪声，为自监督学习提供高质量的“地面真值”。

#### 3. 实验设计
*   **数据集与场景**：
    *   使用 **Habitat** 仿真平台，基于 **HM3D**（128个训练场景）和 **Gibson**（用于跨领域泛化测试）数据集。
    *   构建了一个手动标注的物体级描述测试集作为 Benchmark。
*   **对比方法**：
    *   **基础 VLM**：Qwen3-VL-2B, BLIP-2, InternVL-3B, CoCa, Florence 等。
    *   **一致性流水线**：IC3, ECO-score, LD-CPS 等多阶段方法。
    *   **探索策略**：随机目标（Random Goal）、前沿探索（Frontier Exploration）。
*   **评估指标**：
    *   **准确性**：BLEU-4, METEOR, ROUGE-L, CIDEr, SPICE。
    *   **一致性**：SBERT 嵌入的余弦相似度（Mean CS）及四分位距（IQR）。
    *   **数据关联**：跟踪准确率（Acc）、身份切换（IDSW）等。

#### 4. 资源与算力
*   **硬件设备**：使用了 **2 张 NVIDIA A100 GPU**。
*   **训练细节**：
    *   采用 **LoRA**（低秩自适应）进行高效微调。
    *   训练轮数为 3 个 Epoch。
    *   优化器为 AdamW，学习率 $2 \times 10^{-4}$，Batch Size 为 16（梯度累积 4 步）。
*   **推理效率**：模型在推理时保持约 0.7s/步的恒定时间，内存占用低于 10KB（远优于基于点云的密集表示方法）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了伪描述质量评估、物体描述准确性、跨视图一致性、数据关联性能、策略消融实验以及跨数据集泛化实验。
*   **充分性评价**：实验设计较为全面。通过在 Gibson 数据集上的测试验证了模型的泛化能力；通过“无记忆”版本的消融实验证明了情节记忆的关键作用；对比了多种 SOTA 模型，确保了评估的客观性。

#### 6. 主要结论与发现
*   **性能提升**：EPOS-VLM 在 SPICE 指标上比最强基准提升了 **11.86%**，在描述一致性（Mean CS）上提升了 **7.39%**。
*   **记忆的重要性**：显式的物体级记忆比单纯依靠视觉特征更能有效维持物体身份和语义一致性。
*   **主动探索的价值**：相比于纯几何覆盖的探索策略，EPOS-VLM 学习到的策略会主动重新访问语义不稳定的物体，从而通过更多视角消除歧义。
*   **可扩展性**：基于文本令牌的稀疏记忆表示在长序列任务中比点云等密集表示更具计算优势。

#### 7. 优点与亮点
*   **高度集成**：将感知、推理、记忆和行动整合进一个自回归序列，避免了多阶段模型中的误差累积。
*   **创新的记忆表征**：通过将 3D 空间信息和语义历史转化为文本令牌，巧妙地利用了预训练 VLM 的上下文推理能力。
*   **自监督闭环**：通过基于分歧的探索和 3D-CPS 伪标签，实现了无需人工标注的大规模具身数据训练。

#### 8. 不足与局限
*   **依赖外部分割模型**：模型仍需依赖预训练的实例分割模型（如 Mask2Former），分割错误会直接导致数据关联和描述失败。
*   **环境假设**：实验主要在静态、光照理想的仿真环境中进行，未充分考虑动态物体、传感器噪声及真实世界的实时性约束。
*   **计算延迟**：虽然内存占用低，但 0.7s 的推理延迟对于高速移动的机器人实时控制可能仍显不足。

（完）
