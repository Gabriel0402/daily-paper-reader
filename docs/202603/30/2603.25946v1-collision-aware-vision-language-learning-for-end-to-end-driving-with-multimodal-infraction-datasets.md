---
title: Collision-Aware Vision-Language Learning for End-to-End Driving with Multimodal Infraction Datasets
title_zh: 基于多模态违规数据集的端到端驾驶碰撞感知视觉-语言学习
authors: "Alex Koran, Dimitrios Sinodinos, Hadi Hojjati, Takuya Nanri, Fangge Chen, Narges Armanfard"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25946v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 端到端自动驾驶的视觉语言学习
tldr: 针对端到端自动驾驶中高碰撞违规率的问题，本文提出了视频语言增强异常检测器（VLAAD），利用多实例学习实现主动碰撞预测。通过构建大规模多模态数据集CARLA-Collide和Real-Collide，VLAAD作为插件集成到现有模型中，显著提升了闭环仿真驾驶分数和开环真实场景下的碰撞检测性能。
selection_source: fresh_fetch
motivation: 碰撞相关违规是端到端自动驾驶的主要瓶颈，但现有的表征学习对碰撞感知关注不足且缺乏高质量多模态数据集。
method: 提出基于多实例学习的VLAAD模型，并构建了包含仿真和真实场景的大规模多模态碰撞数据集CARLA-Collide与Real-Collide。
result: "将VLAAD集成至TransFuser++使驾驶分数提升14.12%，且在真实世界基准测试中AUC优于参数量更大的视觉语言模型。"
conclusion: 引入碰撞感知表征和多模态数据集能有效降低自动驾驶违规率，并展现出强大的跨场景泛化能力。
---

## 摘要
高违规率仍然是端到端（E2E）自动驾驶的主要瓶颈，CARLA 排行榜上的低驾驶得分证明了这一点。尽管与碰撞相关的违规是闭环评估中的主要失败模式，但碰撞感知的表示学习受到的关注却很有限。为了填补这一空白，我们首先开发了一种视频-语言增强异常检测器（VLAAD），利用多实例学习（MIL）公式来获取稳定的、时间局部化的碰撞信号，以进行主动预测。为了将这些能力转化为闭环仿真，我们必须克服现有仿真器数据集的局限性，这些数据集缺乏多模态性，且通常局限于简单的交叉路口场景。因此，我们推出了 CARLA-Collide，这是一个大规模多模态数据集，捕捉了高度多样化路网中的真实碰撞事件。在这些多样化的仿真数据上训练后，VLAAD 作为一个碰撞感知插件模块，可以无缝集成到现有的 E2E 驾驶模型中。通过将我们的模块集成到预训练的 TransFuser++ 智能体中，我们证明了在极少微调的情况下，驾驶得分相对提升了 14.12%。除了闭环评估外，我们还使用真实世界的驾驶数据进一步评估了 VLAAD 在开环设置下的泛化能力。为了支持这一分析，我们推出了 Real-Collide，这是一个由多种行车记录仪视频组成的多模态数据集，并配有用于碰撞检测和预测的语义丰富标注。在该基准测试中，尽管 VLAAD 仅包含 0.6B 参数，但其表现优于拥有数十亿参数的视觉-语言模型，AUC 提升了 23.3%。

## Abstract
High infraction rates remain the primary bottleneck for end-to-end (E2E) autonomous driving, as evidenced by the low driving scores on the CARLA Leaderboard. Despite collision-related infractions being the dominant failure mode in closed-loop evaluations, collision-aware representation learning has received limited attention. To address this gap, we first develop a Video-Language-Augmented Anomaly Detector (VLAAD), leveraging a Multiple Instance Learning (MIL) formulation to obtain stable, temporally localized collision signals for proactive prediction. To transition these capabilities into closed-loop simulations, we must overcome the limitations of existing simulator datasets, which lack multimodality and are frequently restricted to simple intersection scenarios. Therefore, we introduce CARLA-Collide, a large-scale multimodal dataset capturing realistic collision events across highly diverse road networks. Trained on this diverse simulator data, VLAAD serves as a collision-aware plug-in module that can be seamlessly integrated into existing E2E driving models. By integrating our module into a pretrained TransFuser++ agent, we demonstrate a 14.12% relative increase in driving score with minimal fine-tuning. Beyond closed-loop evaluation, we further assess the generalization capability of VLAAD in an open-loop setting using real-world driving data. To support this analysis, we introduce Real-Collide, a multimodal dataset of diverse dashcam videos paired with semantically rich annotations for collision detection and prediction. On this benchmark, despite containing only 0.6B parameters, VLAAD outperforms a multi-billion-parameter vision-language model, achieving a 23.3% improvement in AUC.

---

## 论文详细总结（自动生成）

### 论文总结：基于多模态违规数据集的端到端驾驶碰撞感知视觉-语言学习

#### 1. 核心问题与整体含义（研究动机和背景）
端到端（E2E）自动驾驶模型虽然在开环测试中表现良好，但在闭环仿真（如 CARLA 排行榜）中仍面临极高的违规率，其中**碰撞**是导致失败的最主要原因。现有的研究多关注路径规划和一般感知，而忽视了专门的“碰撞感知表征学习”。此外，现有的自动驾驶数据集（如 DeepAccident）大多局限于简单的交叉路口场景，且缺乏丰富的多模态（语言）描述，难以支持现代视觉-语言模型（VLM）的训练。本文旨在通过引入碰撞感知的多模态学习和针对性的数据集，解决 E2E 驾驶中的安全瓶颈。

#### 2. 方法论：核心思想与关键技术
论文提出了 **VLAAD (Video-Language-Augmented Anomaly Detector)**，其核心思想是将碰撞检测视为一种视觉-语言对齐的异常检测任务。
*   **核心架构**：基于冻结的 **XCLIP** 视觉-语言骨干网络，添加了轻量级的**适配器（Adapter）**和**检测器（Detector）**。
*   **多实例学习 (MIL)**：为了在仅有视频级标签的情况下实现碰撞的时间局部化，将视频视为“包（Bag）”，视频片段视为“实例（Instance）”。采用温度控制的 **Log-Sum-Exp (LSE) 池化**函数，使模型能够集中关注碰撞发生的关键帧，产生更尖锐、稳定的风险信号。
*   **损失函数**：结合了**余弦嵌入损失（Cosine Embedding Loss）**用于视觉-语言语义对齐，以及**二进制交叉熵损失（BCE）**用于碰撞分类。利用同方差不确定性加权（Uncertainty Weighting）动态平衡两项损失。
*   **集成策略**：VLAAD 作为一个插件模块，将其输出的碰撞风险分数作为辅助 Token 输入到预训练的驾驶模型（如 TransFuser++）中，通过极少量的微调提升决策安全性。

#### 3. 实验设计
*   **数据集**：
    *   **CARLA-Collide**：自研的大规模仿真数据集，涵盖多种天气、城镇和复杂的碰撞场景（侧碰、追尾、遮挡等），并利用 LLM 生成了自动标注的文本描述。
    *   **Real-Collide**：自研的真实世界基准测试集，包含 1000 个行车记录仪剪辑，涵盖碰撞和正常驾驶的平衡分布。
    *   **其他**：训练还使用了 MM-AU 和 BDDX 等公开真实数据集。
*   **Benchmark 与对比方法**：
    *   **闭环评估**：在 CARLA Town 13 路由上进行，对比基准为 **TransFuser++ (TF++)**。
    *   **开环评估**：在 Real-Collide 上对比了 **XCLIP Baseline** 和经过微调的数十亿参数模型 **LLaVA-Next**。

#### 4. 资源与算力
*   论文**未明确列出**具体的 GPU 型号和数量。
*   **训练细节**：VLAAD 训练 50 个 Epoch；驾驶模型集成微调仅需 5 个 Epoch。
*   **效率分析**：提到 VLAAD 模块在同步模式下每 tick 增加约 50ms 延迟，通过优化（如缓存视频嵌入）可降至 10ms 以下，证明了其轻量化和实时性。

#### 5. 实验数量与充分性
*   **实验规模**：闭环评估在 Town 13 的 20 条长路径上进行了 3 次重复实验并取平均值；开环评估进行了 5 次重复实验。
*   **消融实验**：非常充分，涵盖了“无文本”、“无适配器”、“无 MIL”等多种变体，验证了各组件的有效性。
*   **客观性**：使用了 CARLA Leaderboard 2.1 最新的违规评分公式（修正了早期版本鼓励提前停车的漏洞），并进行了 Wilcoxon 符号秩检验以验证统计显著性，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **性能提升**：将 VLAAD 集成到 TF++ 后，驾驶得分（DS）提升了 **14.12%**，违规分数（IS）提升了 **19.23%**。
*   **MIL 的价值**：MIL 不仅提高了检测准确率（AUC），更重要的是提供了时间对齐的风险信号，有效减少了误报。
*   **小模型胜大模型**：仅有 0.6B 参数的 VLAAD 在碰撞检测任务上比微调后的 LLaVA-Next（多模态大模型）AUC 高出 **23.3%**，说明针对性适配比单纯增加参数量更有效。

#### 7. 优点（亮点）
*   **时间局部化能力**：通过 MIL 解决了碰撞信号在时间轴上模糊的问题，使模型能“预见”碰撞。
*   **数据集贡献**：填补了自动驾驶领域缺乏高质量、多模态碰撞数据集的空白（CARLA-Collide 和 Real-Collide）。
*   **即插即用**：作为轻量级插件，无需从头训练复杂的驾驶模型即可显著提升安全性。

#### 8. 不足与局限
*   **领域差异（Domain Gap）**：实验显示在仿真环境训练的模型直接迁移到真实环境仍存在挑战，需要针对仿真数据进行骨干网络微调。
*   **因果混淆风险**：在“从头训练”实验中发现，虽然碰撞减少了，但非碰撞类违规有所增加，暗示模型可能产生了某种程度的因果混淆。
*   **架构依赖**：目前主要在类 TransFuser 架构上验证，对于其他非 Transformer 架构的适配性有待进一步研究。

（完）
