---
title: "PVI: Plug-in Visual Injection for Vision-Language-Action Models"
title_zh: PVI：视觉-语言-动作模型的插件式视觉注入
authors: "Zezhou Zhang, Songxin Zhang, Xiao Xiong, Junjie Zhang, Zejian Xie, Jingyi Xi, Zunyao Mao, Zan Mao, Zhixin Mai, Zhuoyang Song, Jiaxing Zhang"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12772v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 为机器人操作的VLA模型增强时间证据
tldr: 本研究针对视觉语言动作模型（VLA）在精细几何感知和时间信息利用上的不足，提出了插件式视觉注入（PVI）方案。PVI 是一种轻量级且与编码器无关的模块，通过零初始化残差路径将辅助视觉特征注入预训练的动作专家中，仅需单阶段微调即可保持原有行为。实验证明，PVI 配合时间视频特征在复杂多阶段任务中表现优异，并在真实机器人双臂折叠衣服任务中验证了其实用性。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型往往会削弱精细的几何线索，且缺乏对动作专家至关重要的显式时间证据。
method: 提出 PVI 模块，利用零初始化残差路径将辅助视觉表示注入预训练的动作专家，实现轻量化增强。
result: PVI 在多项任务中优于基础策略，特别是时间视频特征在需要状态跟踪的协调任务中比静态图像特征表现更佳。
conclusion: PVI 为增强 VLA 模型的视觉表征提供了一种高效、非侵入性的手段，有效提升了复杂长程操作任务的成功率。
---

## 摘要
将预训练的视觉语言模型（VLM）与流匹配动作专家相结合的视觉-语言-动作（VLA）架构已成为语言条件操控的一种强大范式。然而，针对语义抽象进行优化且通常以静态视觉观测为条件的 VLM，往往会削弱细粒度的几何线索，并且通常缺乏为动作专家提供的显式时间证据。先前的工作通过注入辅助视觉特征来缓解这一问题，但现有方法要么侧重于静态空间表示，要么需要对架构进行重大修改以适应时间输入，导致时间信息未得到充分探索。我们提出了插件式视觉注入（PVI），这是一种轻量级、与编码器无关的模块，它附加到预训练的动作专家上，并通过零初始化的残差路径注入辅助视觉表示，仅需单阶段微调即可保留预训练行为。使用 PVI，我们在基础策略和一系列具有竞争力的替代注入策略上获得了一致的提升。我们的对照研究表明，时间视频特征（V-JEPA2）优于强大的静态图像特征（DINOv2），在需要状态跟踪和协调的多阶段任务中提升最为显著。在长时程双臂折叠衣服上的真实机器人实验进一步证明了 PVI 在模拟环境之外的实用性。

## Abstract
VLA architectures that pair a pretrained VLM with a flow-matching action expert have emerged as a strong paradigm for language-conditioned manipulation. Yet the VLM, optimized for semantic abstraction and typically conditioned on static visual observations, tends to attenuate fine-grained geometric cues and often lacks explicit temporal evidence for the action expert. Prior work mitigates this by injecting auxiliary visual features, but existing approaches either focus on static spatial representations or require substantial architectural modifications to accommodate temporal inputs, leaving temporal information underexplored. We propose Plug-in Visual Injection (PVI), a lightweight, encoder-agnostic module that attaches to a pretrained action expert and injects auxiliary visual representations via zero-initialized residual pathways, preserving pretrained behavior with only single-stage fine-tuning. Using PVI, we obtain consistent gains over the base policy and a range of competitive alternative injection strategies, and our controlled study shows that temporal video features (V-JEPA2) outperform strong static image features (DINOv2), with the largest gains on multi-phase tasks requiring state tracking and coordination. Real-robot experiments on long-horizon bimanual cloth folding further demonstrate the practicality of PVI beyond simulation.

---

## 论文详细总结（自动生成）

这是一份关于论文《PVI: Plug-in Visual Injection for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型通常将预训练的 VLM（负责语义理解）与动作专家（如扩散模型或流匹配 Transformer，负责控制）结合。然而，VLM 往往为了语义抽象而牺牲了**细粒度的几何线索**（如边缘、对齐、抓取点），且通常只处理静态图像，缺乏连续动作所需的**时间动态信息**。
*   **研究动机**：现有的增强方法要么需要对 VLM 架构进行大幅修改，要么仅限于注入静态特征。研究者希望找到一种**轻量级、非侵入性且与编码器无关**的方法，在不破坏预训练模型能力的前提下，为动作专家直接补充缺失的视觉和时间证据。

### 2. 方法论（PVI：插件式视觉注入）
*   **核心思想**：在冻结的预训练 VLA 模型侧面增加一个可训练的“副本分支”，通过零初始化的残差路径将辅助视觉特征注入到主动作专家网络中。
*   **关键技术细节**：
    *   **双路径设计**：保留一个冻结的主 DiT（扩散 Transformer）分支处理 VLM 语义特征；同时创建一个可训练的副本分支（PVI），其架构与主 DiT 相同并由其初始化。
    *   **编码器无关接口**：PVI 可以接入任何辅助编码器（如视频编码器 V-JEPA2 或图像编码器 DINOv2）。特征通过一个线性投影层映射到动作专家的嵌入空间。
    *   **层级残差注入**：副本分支的每一层输出通过一个**零初始化（Zero-initialized）**的线性层，加到主分支对应层的隐藏状态上。
    *   **单阶段微调**：训练时冻结 VLM 和主 DiT，仅训练 PVI 分支、投影层及必要的适配器。由于采用零初始化，训练开始时模型行为与原始预训练模型完全一致，保证了训练的稳定性。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **仿真环境**：使用 **RoboTwin 2.0** 仿真器，涵盖 20 到 50 种不同的双臂操作任务（如调整瓶子、敲击、开关、堆叠等）。
    *   **真实机器人**：使用 Airbot 双臂机器人进行长时程的**布料折叠**任务，涉及 8 个连续子步骤。
*   **Benchmark 与对比方法**：
    *   **基准模型**：GR00T N1.5（3B 参数规模）。
    *   **对比策略**：Concat（输入层拼接）、ControlNet 风格、ReferenceNet 风格、ControlVLA 风格（双重注意力注入）。
    *   **表征研究**：对比了注入静态特征（DINOv2）、时间特征（V-JEPA2）以及两者结合的效果。

### 4. 资源与算力
*   **硬件设备**：使用了 **NVIDIA H20Z GPU**。
*   **训练配置**：
    *   单任务微调：4 台 H20Z，Batch Size 32，训练 20k - 60k 步。
    *   多任务/真实机器人：8 台 H20Z，Batch Size 64，训练约 60 个 Epoch。
*   **参数量**：PVI 引入了约 0.9B 的可训练参数（在 3B 基座上），但由于冻结了主干网络，实际训练开销与标准微调相当。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 20 个仿真任务上对比了 5 种不同的注入策略。
    *   在 10 个任务上进行了详细的编码器消融实验。
    *   进行了 20 任务和 50 任务规模的大规模多任务学习实验。
    *   每个任务进行 100 次随机初始化的 Rollout 测试。
*   **充分性评价**：实验设计非常系统且客观。通过控制变量法（固定编码器换架构、固定架构换编码器）清晰地证明了 PVI 架构的优越性以及时间信息的重要性。

### 6. 主要结论与发现
*   **PVI 架构更优**：相比于简单的特征拼接或类似 ControlNet 的设计，PVI 的层级残差注入在双臂操作任务中提升最显著（成功率从 35.7% 提升至 59.7%）。
*   **时间信息至关重要**：注入时间视频特征（V-JEPA2）的效果显著优于静态图像特征（DINOv2），尤其是在需要状态跟踪和双臂协调的任务中。
*   **保持预训练能力**：零初始化确保了模型在引入新特征时不会丢失原有的语义理解能力。
*   **现实可行性**：PVI 成功驱动了复杂的真实双臂折叠任务，证明了其在处理可变形物体和长时程任务中的潜力。

### 7. 优点
*   **非侵入性**：无需修改预训练 VLM 的权重或架构，易于适配不同的 VLA 基座。
*   **灵活性高**：编码器无关的设计允许根据任务需求灵活更换视觉前端（如 3D、深度图或视频）。
*   **训练稳定**：零初始化残差路径避免了微调初期的性能剧烈波动。
*   **性能提升显著**：在复杂、精细的操作任务中表现出极强的鲁棒性。

### 8. 不足与局限
*   **推理开销增加**：虽然训练是轻量级的，但推理时需要额外运行辅助编码器和副本分支，增加了计算延迟和显存占用。
*   **参数冗余**：PVI 需要复制一份动作专家的 DiT 块，导致总参数量增加（尽管大部分是冻结的）。
*   **真实世界定量评估较少**：虽然有长时程任务演示，但真实世界的对比实验（如不同策略在真机上的成功率对比）相对仿真环境较少。

（完）
