---
title: "AnoleVLA: Lightweight Vision-Language-Action Model with Deep State Space Models for Mobile Manipulation"
title_zh: AnoleVLA：基于深度状态空间模型的轻量级视觉-语言-动作模型，用于移动操作
authors: "Yusuke Takagi, Motonari Kambara, Daichi Yashima, Koki Seno, Kento Tokura, Komei Sugiura"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15046v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于机器人操作的轻量级视觉-语言-动作模型
tldr: "针对语言引导的机器人操作任务，传统基于 Transformer 的视觉-语言-动作模型（VLA）因计算成本高而难以在资源受限环境下部署。本文提出 AnoleVLA，一种利用深度状态空间模型（SSM）处理多模态序列的轻量化模型。该模型通过高效的序列建模生成动作轨迹，在保持低延迟的同时提升了泛化能力。实验表明，AnoleVLA 在真实场景中的成功率比大型 VLA 高出 21%，且推理速度提升了 3 倍，为移动机器人实时操作提供了高效方案。"
selection_source: fresh_fetch
motivation: 现有的 VLA 模型通常依赖计算密集型的 Transformer 架构，导致其在资源受限的移动机器人平台上难以实现高效部署。
method: 提出 AnoleVLA 架构，采用深度状态空间模型（SSM）替代 Transformer 来高效处理视觉和文本输入并生成机器人动作序列。
result: 在真实世界测试中，AnoleVLA 的推理速度比主流大型 VLA 快约 3 倍，且任务成功率显著提升了 21 个百分点。
conclusion: 该研究证明了深度状态空间模型在构建轻量化、高性能机器人动作模型方面的巨大潜力，有效平衡了计算效率与任务泛化性。
---

## 摘要
在本研究中，我们解决了语言引导的机器人操作问题，即机器人需要根据视觉观察和自然语言指令操作各种物体。这一任务对于在人类环境中运行的服务机器人至关重要，且要求具备安全性、效率和任务级的通用性。尽管视觉-语言-动作模型（VLA）在该任务中表现出强大的性能，但由于标准 Transformer 骨干网络的计算成本，它们在资源受限环境中的部署仍然具有挑战性。为了克服这一限制，我们提出了 AnoleVLA，这是一种轻量级 VLA，它使用深度状态空间模型来高效处理多模态序列。该模型利用其轻量且快速的序列状态建模来处理视觉和文本输入，从而使机器人能够高效地生成轨迹。我们在仿真和物理实验中评估了所提方法。值得注意的是，在真实世界的评估中，AnoleVLA 在任务成功率上比代表性的大规模 VLA 高出 21 个百分点，同时推理速度快了约三倍。

## Abstract
In this study, we address the problem of language-guided robotic manipulation, where a robot is required to manipulate a wide range of objects based on visual observations and natural language instructions. This task is essential for service robots that operate in human environments, and requires safety, efficiency, and task-level generality. Although Vision-Language-Action models (VLAs) have demonstrated strong performance for this task, their deployment in resource-constrained environments remains challenging because of the computational cost of standard transformer backbones. To overcome this limitation, we propose AnoleVLA, a lightweight VLA that uses a deep state space model to process multimodal sequences efficiently. The model leverages its lightweight and fast sequential state modeling to process visual and textual inputs, which allows the robot to generate trajectories efficiently. We evaluated the proposed method in both simulation and physical experiments. Notably, in real-world evaluations, AnoleVLA outperformed a representative large-scale VLA by 21 points for the task success rate while achieving an inference speed approximately three times faster.

---

## 论文详细总结（自动生成）

### AnoleVLA：基于深度状态空间模型的轻量级视觉-语言-动作模型总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作模型（VLA）虽然在机器人操作任务中表现出色，但大多基于 Transformer 架构。Transformer 的自注意力机制随序列长度呈平方级计算复杂度增长，导致模型在资源受限的移动机器人平台上推理延迟高、显存占用大，难以实现实时、高效的部署。
*   **研究背景**：服务机器人需要在人类环境中根据自然语言指令执行复杂的物体操作（如“把苹果放进红碗里”）。这要求模型不仅要具备多模态理解能力，还要能快速生成平滑的动作轨迹。

#### 2. 方法论
*   **核心思想**：采用**深度状态空间模型（Deep SSM，具体为 Mamba）**替代传统的 Transformer 作为骨干网络，利用其线性计算复杂度（$O(L)$）实现高效的多模态序列建模。
*   **关键技术细节**：
    *   **输入嵌入模块**：将机器人当前状态（$s_t$）、状态变化量（$\Delta s_t$）、RGB 图像（通过预训练的 SigLIP 2 编码）和自然语言指令拼接成序列。
    *   **Mamba 骨干网络**：由 $K$ 个 Mamba 块堆叠而成，通过循环状态更新机制处理输入序列，在保持长上下文关联的同时显著降低计算开销。
    *   **动作头（Action Head）**：直接从骨干网络的最后一个隐藏状态通过线性投影生成连续的动作块（Action Chunk），无需进行动作离散化或自回归解码。
    *   **两阶段训练策略**：
        *   **第一阶段**：使用速度损失（Velocity Loss，$L_1$ 范数）监督预测动作与真实轨迹的差异。
        *   **第二阶段**：引入加速度损失（Acceleration Loss），监督动作的时间差分，以提升机器人执行动作的平滑度和稳定性。

#### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：使用 Meta-World 基准测试，包含 50 种不同的物体操作任务（如移动物体、开门、开抽屉等）。
    *   **物理实验**：使用丰田 HSR 移动机器人，在模拟家庭环境（WRS2020RS 标准）中执行移动、抓取、打开/关闭抽屉、按压泵头等任务。
*   **对比方法（Baselines）**：
    *   大规模模型：$\pi_{0.5}$ (3.0B 参数)。
    *   同规模轻量级模型：SmolVLA (0.45B)、VLA-Adapter (0.5B)、TinyVLA (0.42B)。
*   **评估指标**：任务成功率（Success Rate）和推理速度（Inference Speed）。

#### 4. 资源与算力
*   **硬件配置**：使用单张 **NVIDIA GeForce RTX 4090 (24GB)** GPU 和 Intel Core i9-14900F 处理器。
*   **训练时长**：总训练步数为 400k 步（两个阶段各 20k 步），总耗时约 **20 小时**。
*   **推理性能**：推理速度为 **216 ms/chunk**，显存占用较低，适合在移动平台上运行。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验涵盖了 Meta-World 的全部 50 个任务，分为 Easy 到 Very Hard 四个难度等级。
    *   物理实验针对 5 类典型任务，每类进行 20 次测试，共计 100 次真实场景评估。
    *   **消融实验**：专门验证了“加速度损失”对成功率的影响（提升了约 4.73%）。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到现实的迁移，并对比了多种主流轻量级 VLA 架构，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能卓越**：AnoleVLA（0.47B）在 Meta-World 上的平均成功率（67.85%）显著超过了 3B 规模的 $\pi_{0.5}$（43.80%）以及其他轻量级模型。
*   **速度优势**：在真实世界测试中，AnoleVLA 的推理速度比 $\pi_{0.5}$ 快约 **3 倍**，且成功率高出 21 个百分点。
*   **平滑性**：两阶段训练策略有效解决了传统 VLA 常见的动作“抖动”问题，使物理机器人的操作更加精准平滑。
*   **样本效率**：在每类任务仅有 50 个演示样本的小数据集下，AnoleVLA 表现出比大规模模型更强的鲁棒性和适应性。

#### 7. 优点
*   **架构创新**：首次在 VLA 领域深入探索了深度 SSM（Mamba）的潜力，证明了其在机器人实时控制中的效率优势。
*   **部署友好**：极低的计算资源需求使其能够直接部署在移动机器人车载计算单元上。
*   **控制质量**：通过加速度损失的设计，兼顾了多模态理解与底层控制的平滑性。

#### 8. 不足与局限
*   **空间感知局限**：错误分析显示，最主要的失败原因是“位置识别错误”（占 50%），表明模型在精确提取物体空间坐标方面仍有提升空间。
*   **任务复杂度**：虽然在短时动作块生成上表现良好，但对于需要极长时序推理或极高精度的微操任务，其表现尚未完全验证。
*   **泛化边界**：实验主要集中在桌面和简单的家庭操作，对于更复杂、动态变化剧烈的环境泛化能力有待进一步观察。

（完）
