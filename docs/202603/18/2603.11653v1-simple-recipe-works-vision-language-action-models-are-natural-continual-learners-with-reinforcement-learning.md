---
title: "Simple Recipe Works: Vision-Language-Action Models are Natural Continual Learners with Reinforcement Learning"
title_zh: 简单方案行之有效：视觉-语言-动作模型是结合强化学习的天然持续学习者
authors: "Jiaheng Hu, Jay Shim, Chen Tang, Yoonchang Sung, Bo Liu, Peter Stone, Roberto Martin-Martin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11653v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 视觉-语言-动作模型的持续强化学习
tldr: 本研究探讨了视觉-语言-动作（VLA）模型在持续强化学习（CRL）中的表现。传统观点认为顺序微调会导致灾难性遗忘，但本文通过对三种模型和五个基准测试的系统研究发现，简单的顺序微调结合低秩自适应（LoRA）在保持高可塑性的同时，几乎没有遗忘现象，且保留了强大的零样本泛化能力。研究揭示了预训练大模型、参数高效微调与同策略强化学习之间的协同作用，为构建可自我改进的具身智能体提供了简单高效的新范式。
selection_source: fresh_fetch
motivation: 挑战传统持续学习中“顺序微调必然导致灾难性遗忘”的观点，探索大模型时代具身智能体在动态环境中的持续适应能力。
method: 在三个VLA模型和五个挑战性基准上，系统评估了结合低秩自适应（LoRA）的简单顺序微调策略在持续强化学习中的效果。
result: 实验证明简单的顺序微调表现出极强的鲁棒性，在保持高学习效率的同时几乎不产生遗忘，且性能优于许多复杂的持续学习算法。
conclusion: 预训练大模型、参数高效适配与同策略强化学习的协同作用重塑了稳定性与可塑性的权衡，使顺序微调成为VLA持续学习的有效方案。
---

## 摘要
针对视觉-语言-动作（VLA）模型的持续强化学习（CRL）是实现能够在开放式、演化环境中进行适应的自我改进具身智能体的一个极具前景的方向。然而，持续学习的传统观点认为，朴素的顺序微调（Seq. FT）会导致灾难性遗忘，因此需要复杂的 CRL 策略。在这项工作中，我们退后一步，对三种模型和五个具有挑战性的终身强化学习基准上的大型预训练 VLA 的 CRL 进行了系统研究。我们发现，与既有观点相反，结合低秩自适应（LoRA）的简单顺序微调表现异常强劲：它实现了高可塑性，几乎没有表现出遗忘，并保持了强大的零样本泛化能力，经常优于更复杂的 CRL 方法。通过详细分析，我们表明这种鲁棒性源于大型预训练模型、参数高效自适应和同策略（on-policy）强化学习之间的协同作用。这些组件共同重塑了稳定性-可塑性权衡，使持续适应既稳定又具可扩展性。我们的结果将顺序微调定位为 VLA 持续强化学习的一种强大方法，并为大模型时代的终身学习提供了新的见解。代码可在 github.com/UT-Austin-RobIn/continual-vla-rl 获取。

## Abstract
Continual Reinforcement Learning (CRL) for Vision-Language-Action (VLA) models is a promising direction toward self-improving embodied agents that can adapt in openended, evolving environments. However, conventional wisdom from continual learning suggests that naive Sequential Fine-Tuning (Seq. FT) leads to catastrophic forgetting, necessitating complex CRL strategies. In this work, we take a step back and conduct a systematic study of CRL for large pretrained VLAs across three models and five challenging lifelong RL benchmarks. We find that, contrary to established belief, simple Seq. FT with low-rank adaptation (LoRA) is remarkably strong: it achieves high plasticity, exhibits little to no forgetting, and retains strong zero-shot generalization, frequently outperforming more sophisticated CRL methods. Through detailed analysis, we show that this robustness arises from a synergy between the large pretrained model, parameter-efficient adaptation, and on-policy RL. Together, these components reshape the stability-plasticity trade-off, making continual adaptation both stable and scalable. Our results position Sequential Fine-Tuning as a powerful method for continual RL with VLAs and provide new insights into lifelong learning in the large model era. Code is available at github.com/UT-Austin-RobIn/continual-vla-rl.

---

## 论文详细总结（自动生成）

以下是对论文《Simple Recipe Works: Vision-Language-Action Models are Natural Continual Learners with Reinforcement Learning》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：具身智能体（Embodied Agents）需要在不断演化的环境中持续学习新技能。传统的持续学习（Continual Learning, CL）观点认为，简单的“顺序微调”（Sequential Fine-Tuning, Seq. FT）会导致严重的“灾难性遗忘”。
*   **核心问题**：在大模型时代，拥有数十亿参数的预训练视觉-语言-动作（VLA）模型在结合强化学习（RL）进行持续任务学习时，是否依然遵循这一传统规律？
*   **整体含义**：本文挑战了传统认知，证明了在大规模预训练 VLA 模型上，简单的顺序微调结合低秩自适应（LoRA）和同策略（On-policy）强化学习，就能表现出极强的稳定性、高可塑性和卓越的零样本泛化能力，无需复杂的持续学习算法。

### 2. 方法论
*   **核心思想**：提出一个简单的“配方”：**预训练 VLA + LoRA + 同策略 RL (GRPO)**。
*   **关键技术细节**：
    *   **VLA 模型**：使用大规模预训练模型作为起点，具备强大的初始表征和零样本能力。
    *   **参数高效微调 (LoRA)**：仅更新低秩矩阵，保持原始预训练权重冻结，限制了参数更新的自由度。
    *   **同策略强化学习 (GRPO)**：采用组相对策略优化（Group Relative Policy Optimization），通过在当前策略生成的样本上进行更新，产生隐式的 KL 散度正则化效果。
*   **协同效应分析**：
    *   **大模型**：高维空间的“零空间”（Null Space）特性使得新任务梯度很少干扰旧知识。
    *   **LoRA**：通过限制更新秩，防止单层权重发生剧烈结构变化。
    *   **同策略 RL**：相比监督微调（SFT），RL 仅在当前策略支持的范围内调整概率分布，避免了强制改变预训练分布导致的遗忘。

### 3. 实验设计
*   **数据集与场景**：涵盖 5 个挑战性基准：
    *   **LIBERO** (Spatial, Object, Long-horizon)：机器人操纵任务。
    *   **RoboCasa**：复杂的厨房环境任务。
    *   **ManiSkill**：基于 SAPIEN 物理引擎的物体放置任务。
*   **对比方法**：
    *   **基准**：顺序微调 (Seq. FT)、多任务学习 (Multi-Task Oracle)。
    *   **正则化类**：EWC (弹性权重巩固)。
    *   **回放类**：Expert Replay (专家回放)、Dark Experience Replay (DER)。
    *   **隔离类**：Dynamic Weight Expansion (动态权重扩展/多适配器)。
    *   **大模型适配类**：SLCA (分层学习率)、RETAIN (权重合并)。
*   **评估指标**：平均成功率 (AVG)、负向后向迁移 (NBT，即遗忘率)、前向迁移 (FWT)、零样本成功率 (ZS)。

### 4. 资源与算力
*   **模型规模**：主要使用 7B 参数量的 OpenVLA-OFT，以及 Pi-0 (基于 PaliGemma) 和原始 OpenVLA。
*   **算力说明**：文中未详细列出具体的 GPU 型号和数量，但提到使用了大规模强化学习基础设施（RLinf），且训练过程涉及数万次环境交互（如每个任务 10240 个 episode）。考虑到 7B 模型和 RL 训练的性质，通常需要多节点 A100/H100 集群支持。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种不同的 VLA 架构、5 个基准测试上进行了测试，每组实验运行 3 个随机种子。
*   **消融实验**：非常充分。分别验证了移除 RL（改用 SFT）、移除大模型（改用 12M 小模型）、移除 LoRA（全参数微调）的影响。
*   **鲁棒性测试**：针对相机角度、光照条件、机器人初始位置的扰动以及任务顺序的改变进行了额外验证。
*   **客观性**：实验设计严谨，对比了多种主流 CRL 范式，且在 Seq. FT 上未进行任何超参数调优，证明了其天然的鲁棒性。

### 6. 主要结论与发现
*   **Seq. FT 表现惊人**：在几乎所有基准上，简单的顺序微调在 AVG 和 ZS 指标上均接近或超过复杂的 CRL 方法。
*   **极低遗忘率**：NBT 指标通常低于 2%，甚至出现负值（即学习新任务促进了旧任务）。
*   **可塑性与泛化**：Seq. FT 保持了极高的学习新任务的能力，且其零样本泛化性能往往优于多任务训练（Oracle）。
*   **复杂方法适得其反**：传统的 CRL 约束（如 EWC 或回放）往往会损害模型的灵活性（可塑性），导致在新任务上表现不佳。

### 7. 优点
*   **简洁高效**：证明了无需复杂的存储缓冲区或复杂的正则化项即可实现持续学习。
*   **理论支撑**：通过 Fisher 信息矩阵分析和权重秩分析，从数学和几何角度解释了为何大模型 + LoRA + RL 能抑制遗忘。
*   **实用性强**：为构建能够自我改进的具身智能体提供了一个可扩展的工业级方案。

### 8. 不足与局限
*   **任务定义**：实验假设任务指令（语言）是明确提供的，未探讨任务边界模糊或指令缺失的情况。
*   **样本效率**：虽然 Seq. FT 最终能达到多任务 Oracle 的水平，但在某些任务上可能需要更多的训练步数。
*   **环境限制**：主要在仿真环境中验证，虽然做了多种扰动测试，但在真实物理世界中的表现（如硬件磨损、传感器噪声）仍待进一步验证。
*   **任务序列长度**：实验主要集中在 4-5 个任务的序列，更长序列（如数十个任务）下的累积效应尚不明确。

（完）
