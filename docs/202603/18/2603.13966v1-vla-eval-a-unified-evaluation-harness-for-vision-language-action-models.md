---
title: "vla-eval: A Unified Evaluation Harness for Vision-Language-Action Models"
title_zh: vla-eval：视觉-语言-动作模型的统一评估框架
authors: "Suhwan Choi, Yunsung Lee, Yubeen Park, Chris Dongjoo Kim, Ranjay Krishna, Dieter Fox, Youngjae Yu"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13966v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉-语言-动作模型的统一评估工具
tldr: 针对视觉-语言-动作（VLA）模型评估中存在的脚本碎片化、依赖冲突和协议不明确等问题，本文推出了开源评估框架 vla-eval。该框架通过 WebSocket 协议和 Docker 隔离技术，实现了模型推理与基准测试环境的解耦。它支持 13 个基准测试和 6 种模型，通过并行化技术将评估效率提升了 47 倍，并提供了包含 657 条结果的 VLA 排行榜，显著提升了 VLA 模型评估的标准化与效率。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型评估缺乏统一标准，各模型库独立维护脚本导致代码冗余、依赖冲突且实验协议不明确。
method: 提出一种基于 WebSocket 协议和 Docker 环境隔离的解耦架构，通过统一接口实现模型与基准测试的自动交叉评估。
result: 框架支持 13 个基准测试，通过并行化实现 47 倍吞吐量提升，并成功复现了已发表模型性能，同时揭示了影响结果的隐藏因素。
conclusion: vla-eval 为 VLA 模型提供了高效、可复现且标准化的评估基础设施，有助于推动该领域的公平比较和快速迭代。
---

## 摘要
视觉-语言-动作（VLA）模型通常使用由各模型仓库独立维护的特定基准测试脚本进行评估，这导致了代码重复、依赖冲突以及协议规范不明确等问题。我们提出了 vla-eval，这是一个开源评估框架，通过基于 Docker 的环境隔离和 WebSocket msgpack 协议，实现了模型推理与基准测试执行的解耦。模型只需实现单个 predict() 方法即可完成集成；基准测试则通过四方法接口进行集成；完整的交叉评估矩阵可自动运行。完成一次完整的评估仅需两条命令：vla eval serve 和 vla eval run。该框架支持 13 个仿真基准测试和 6 个模型服务器。通过回合分片（episode sharding）和批处理推理实现的并行评估使吞吐量提升了 47 倍，在大约 18 分钟内即可完成 2000 个 LIBERO 回合。利用该基础设施，我们对一个已发表的 VLA 模型在三个基准测试上进行了可复现性审计，发现这三个基准测试均能紧密复现已发表的数值，同时也揭示了未记录的需求、模糊的终止语义以及可能在无形中扭曲结果的隐藏归一化统计数据。此外，我们还发布了一个 VLA 排行榜，汇总了 17 个基准测试中的 657 项已发表结果。框架、评估配置及所有复现结果均已公开。

## Abstract
Vision Language Action VLA models are typically evaluated using per benchmark scripts maintained independently by each model repository, leading to duplicated code, dependency conflicts, and underspecified protocols. We present vla eval, an open source evaluation harness that decouples model inference from benchmark execution through a WebSocket msgpack protocol with Docker based environment isolation. Models integrate once by implementing a single predict() method; benchmarks integrate once via a four method interface; the full cross evaluation matrix works automatically. A complete evaluation requires only two commands: vla eval serve and vla eval run. The framework supports 13 simulation benchmarks and six model servers. Parallel evaluation via episode sharding and batch inference achieves a 47x throughput improvement, completing 2000 LIBERO episodes in about 18 minutes. Using this infrastructure, we conduct a reproducibility audit of a published VLA model across three benchmarks, finding that all three closely reproduce published values while uncovering undocumented requirements ambiguous termination semantics and hidden normalization statistics that can silently distort results. We additionally release a VLA leaderboard aggregating 657 published results across 17 benchmarks. Framework, evaluation configs, and all reproduction results are publicly available.

---

## 论文详细总结（自动生成）

以下是对论文《vla-eval: A Unified Evaluation Harness for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉-语言-动作（VLA）模型的评估生态系统高度碎片化。每个模型仓库通常独立维护特定基准测试（Benchmark）的脚本，导致了代码冗余、严重的依赖冲突（如不同基准要求不同的 Python 版本或物理引擎）、实验协议不透明（如缺失种子点、归一化统计数据）以及评估速度极慢（单次完整评估可能耗时十数小时）。
*   **研究背景**：随着 OpenVLA、π0 等模型的涌现，亟需一个像语言模型领域的 `lm-evaluation-harness` 一样的统一工具，来标准化机器人操作任务的评估流程。

### 2. 方法论
*   **核心思想**：采用**客户端-服务器架构**，将“模型推理”与“基准测试执行”彻底解耦。
*   **关键技术细节**：
    *   **通信协议**：通过 WebSocket 结合 msgpack 二进制序列化进行数据交换，传输观测值（obs）、动作（action）及状态信息。
    *   **环境隔离**：基准测试运行在独立的 **Docker 容器**中，解决物理引擎和库的依赖冲突；模型服务器通过 PEP 723 及其隔离环境运行。
    *   **统一接口**：模型端只需实现一个 `predict()` 方法；基准测试端通过 `reset`、`step`、`make_obs`、`get_step_result` 四个方法进行适配。
    *   **并行化策略**：引入**回合分片（Episode Sharding）**和**批处理推理（Batch Inference）**。通过需求/供应模型优化并行参数，确保环境生成的观测速度与模型推理速度匹配，防止队列堆积。

### 3. 实验设计
*   **支持的基准测试**：集成了 13 个主流仿真基准，包括 LIBERO、CALVIN、SimplerEnv、ManiSkill2、RoboCasa、RLBench 等，涵盖 6D 到 14D 的动作空间。
*   **支持的模型**：集成了 CogACT、OpenVLA、π0、GR00T N1、X-VLA 等 6 种模型服务器。
*   **可复现性审计**：以 DB-CogACT 模型为对象，在 LIBERO（2000 个回合）、CALVIN（1000 个链式序列）和 SimplerEnv（288 个回合）上进行了复现实验，对比已发表的论文数值。
*   **排行榜建设**：通过 AI 智能体（Claude Code）辅助和人工审核，从 2685 篇论文中提取并标准化了 657 项结果，构建了覆盖 17 个基准测试的 VLA 排行榜。

### 4. 资源与算力
*   **算力设备**：实验中使用了 **NVIDIA H100 GPU** 作为模型服务器。
*   **效率提升**：
    *   在 LIBERO 基准上，使用 50 个分片和 Batch Size=16，将 2000 个回合的评估时间从 14 小时缩短至 **18 分钟**（提升 47 倍）。
    *   CALVIN 评估时间从 8.6 小时缩短至 33 分钟（提升 16 倍）。

### 5. 实验数量与充分性
*   **实验规模**：进行了数千个回合的仿真实验，涵盖了从空间关系到长程任务的多种维度。
*   **充分性**：实验不仅验证了框架的性能提升，还深入探讨了导致复现差异的底层原因。通过对 509 个模型配置的横向对比，揭示了当前 VLA 领域 81% 的模型仅在单一基准上评估的现状，证明了统一框架的必要性。

### 6. 主要结论与发现
*   **性能复现**：框架能够将模型性能复现至与原论文误差在 ±3% 以内的水平。
*   **隐藏的陷阱**：审计发现了两个导致复现困难的关键因素：
    1.  **模糊的终止语义**：如 SimplerEnv 中某些标志位仅代表瞬时成功而非回合结束，提前停止会虚高得分。
    2.  **隐藏的归一化统计**：如 CALVIN 需要特定的训练集均值和标准差进行观测值预处理，而这些数据往往未在评估文档中注明。
*   **并行潜力**：合理的并行配置可使 VLA 评估从“天级”缩短至“分钟级”。

### 7. 优点
*   **标准化与解耦**：通过 Docker 和统一接口解决了长期困扰机器人学习领域的“依赖地狱”问题。
*   **极高的评估效率**：显著降低了研究人员进行多基准测试对比的门槛。
*   **透明度**：保存完整的 Docker 镜像标签、种子和配置，确保了实验的绝对可复现性。
*   **社区贡献**：提供了目前最全面的 VLA 性能排行榜。

### 8. 不足与局限
*   **场景限制**：目前仅针对**仿真环境**，未涵盖真实机器人（Real-robot）评估。
*   **指标单一**：主要关注任务成功率（Success Rate），尚未集成运动质量、效率、安全性等更丰富的评估维度。
*   **排行榜数据源**：排行榜结果多源于论文自述，而非全部通过该框架重新运行验证，可能存在原始论文报告偏差。

（完）
