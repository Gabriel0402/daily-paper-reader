---
title: "CORAL: Scalable Multi-Task Robot Learning via LoRA Experts"
title_zh: CORAL：通过 LoRA 专家实现的可扩展多任务机器人学习
authors: "Yuankai Luo, Woping Chen, Tong Liang, Zhenguo Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09298v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于多任务机器人学习的视觉-语言-动作 (VLA) 模型
tldr: 针对视觉-语言-动作（VLA）模型在多任务学习中存在的任务干扰和存储开销大的问题，本文提出了CORAL框架。该框架通过冻结预训练骨干网络并为每个任务分配独立的轻量化LoRA专家，利用动态推理引擎进行指令路由。这种参数隔离机制有效消除了负迁移和灾难性遗忘，在真实机器人和仿真基准测试中均显著优于联合训练，为可扩展的终身机器人学习提供了实用且高效的方案。
selection_source: fresh_fetch
motivation: 多任务微调常因任务间梯度冲突导致负迁移，且为每个任务保存完整模型会带来沉重的存储和部署负担。
method: CORAL冻结VLA骨干网络，为每个任务挂载独立的LoRA专家，并由管理器根据语言指令实现零开销的专家切换。
result: 在真实双臂机器人及LIBERO等仿真基准上，CORAL显著优于联合训练，并能有效应对指令歧义和新任务的持续引入。
conclusion: CORAL通过参数隔离实现了无干扰的多任务学习，为构建可扩展的终身机器人学习系统提供了一种骨干网络无关的通用框架。
---

## 摘要
在现实世界的机器人中部署视觉-语言-动作（VLA）模型暴露了一个核心的多任务学习挑战：如何协调多任务机器人学习中的任务干扰。当多个任务在单一阶段进行联合微调时，来自不同任务的梯度可能会发生冲突，导致负迁移并降低每个任务的性能。然而，为每个任务维护一个单独的完整检查点通常在存储和部署上是不可行的。为了解决这一困境，我们提出了 CORAL，这是一个与主干网络和具身形式无关的框架，旨在减轻多任务干扰，同时保持对连续新任务流的自然扩展性。CORAL 冻结单个预训练的 VLA 主干网络，并为每个任务附加一个轻量级的低秩自适应（LoRA）专家；在运行时，一个动态推理引擎（CORAL 管理器）将语言指令路由到相应的专家，并实时切换专家，且具有零推理开销。这种严格的参数隔离避免了复杂的门控网络，并从结构上防止了参数级的跨任务干扰；作为一项附加功能，它还支持按顺序引入新任务，而不会因灾难性遗忘导致参数覆盖。我们在现实世界的 Galaxea R1 双臂移动协作机器人和三个仿真基准测试（LIBERO、WidowX、Google Robot）上验证了 CORAL。结果表明，CORAL 克服了细粒度的指令歧义，并显著优于联合训练，为终身多任务机器人学习提供了一个实用且可扩展的系统。网站：https://frontierrobo.github.io/CORAL

## Abstract
Deploying Vision-Language-Action (VLA) models in real-world robotics exposes a core multi-task learning challenge: reconciling task interference in multi-task robotic learning. When multiple tasks are jointly fine-tuned in a single stage, gradients from different tasks can conflict, causing negative transfer and reducing per-task performance. Yet maintaining a separate full checkpoint per task is often storage- and deployment-prohibitive. To address this dilemma, we present CORAL, a backbone- and embodiment-agnostic framework designed primarily to mitigate multi-task interference while remaining naturally extensible to a continuous stream of new tasks. CORAL freezes a single pre-trained VLA backbone and attaches one lightweight Low-Rank Adaptation (LoRA) expert per task; at runtime, a dynamic inference engine (the CORAL Manager) routes language instructions to the appropriate expert and swaps experts on the fly with zero inference overhead. This strict parameter isolation avoids complex gating networks and prevents parameter-level cross-task interference by construction; as an added capability, it also enables sequentially introducing new tasks without parameter overwriting caused by catastrophic forgetting. We validate CORAL on a real-world Galaxea R1 dual-arm mobile manipulator and three simulation benchmarks (LIBERO, WidowX, Google Robot), where CORAL overcomes fine-grained instructional ambiguity and substantially outperforms joint training, yielding a practical and scalable system for lifelong multi-task robot learning. Website: https://frontierrobo.github.io/CORAL

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **CORAL** 的框架，旨在解决视觉-语言-动作（VLA）模型在多任务机器人学习中的干扰与扩展性难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多任务机器人学习中，联合微调（Joint Fine-tuning）多个任务常会导致**梯度冲突和负迁移**（即提升某些技能时损害了其他技能），尤其是在指令细微差别导致歧义时。
*   **部署困境**：为每个任务保存完整的模型参数虽然能避免干扰，但存储开销巨大（O(N) 复杂度），难以在边缘设备部署。
*   **终身学习挑战**：顺序学习新任务会导致**灾难性遗忘**，即新参数覆盖旧知识。
*   **研究背景**：现有的混合专家模型（MoE）通常需要复杂的门控网络，且推理开销大；而参数高效微调（PEFT）如 LoRA 虽能减小开销，但多用于静态部署而非动态多任务切换。

### 2. 方法论
*   **核心思想**：采用“**冻结主干 + 任务特定 LoRA 专家**”的架构。利用机器人任务中“语言指令天然包含任务索引”的特性，实现确定性的专家路由。
*   **两阶段学习流程**：
    1.  **具身感知通用预训练**：在多样化数据集上训练 VLA 主干网络（如 SimVLA 或 π0.5），学习通用的控制、运动学和视觉语义，随后永久冻结该主干。
    2.  **轻量化任务专家训练**：为每个任务训练独立的 LoRA 适配器。通过极短的训练步数（如 50 步或 1-5 个 Epoch）进行隐式正则化，防止过拟合。
*   **关键技术细节**：
    *   **双重注入策略**：在 VLM 编码器和动作头的注意力层中均插入 LoRA 模块。
    *   **CORAL 管理器**：动态推理引擎。根据语言指令 $T$ 直接路由到专家索引 $k$，实现“零开销”切换。
    *   **权重合并机制**：切换专家时，将 LoRA 的低秩矩阵直接合并进主干权重，推理时无额外计算量（FLOPs）或延迟。

### 3. 实验设计
*   **仿真基准测试**：
    *   **LIBERO**：包含 40 个任务（Spatial, Object, Goal, Long 四个子集）。
    *   **WidowX (Simpler-Bridge)**：评估高保真环境下的表现。
    *   **Google Robot (Simpler-Fractal)**：测试变体聚合得分和鲁棒性。
*   **真实世界实验**：
    *   **平台**：Galaxea R1 Lite（23 自由度双臂移动协作机器人）。
    *   **场景**：8 个复杂的双臂协作任务（如合书、拆笔帽、折 T 恤、插吸管等）以及新能力获取（开门、按电梯按钮）。
*   **对比方法**：包括 π0, SimVLA, X-VLA, RT-2-X, VLA-Adapter 等 SOTA 模型，以及“独立全量微调”和“联合全量微调”基准。

### 4. 资源与算力
*   **存储效率**：单个 Rank-16 的 LoRA 专家仅占约 **26 MB**（以 0.8B 参数模型为例），比全量模型缩小约 100 倍。40 个专家总计仅需约 1 GB 存储。
*   **推理开销**：专家切换在单 GPU 上可在 **100 ms** 内完成，推理过程无额外延迟。
*   **训练算力**：文中未详细列出具体的 GPU 型号和总训练时长，但强调了训练过程非常轻量（每个专家仅需 50 步微调），暗示了极低的算力需求。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流仿真基准和 10 余个真实世界复杂任务，总计测试任务超过 50 个。
*   **充分性**：
    *   **主干无关性验证**：在 SimVLA (0.8B) 和 π0.5 (3B) 两种不同规模的主干上均验证了有效性。
    *   **消融与对比**：对比了联合训练（证明了干扰的存在）和独立训练（证明了 CORAL 能达到接近全量微调的效果）。
    *   **客观性**：使用了官方测试协议和变体聚合得分，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能领先**：CORAL 在 LIBERO 上达到了 **99.3%** 的平均成功率，刷新了 SOTA 记录。
*   **解决干扰**：通过参数隔离，CORAL 彻底消除了联合训练中的负迁移问题，在复杂任务上的表现远超联合微调。
*   **克服遗忘**：由于专家参数完全独立，顺序学习新任务时对旧任务的成功率影响为零（从结构上解决了灾难性遗忘）。
*   **扩展性强**：极小的存储占用使得在边缘设备上维护数百个技能专家成为可能。

### 7. 优点
*   **架构优雅**：利用语言指令作为天然路由，避开了 MoE 中最难训练的门控网络。
*   **部署友好**：零推理延迟和极低的存储需求非常适合实时机器人系统。
*   **通用性**：不依赖特定的 VLA 架构或机器人硬件，具有很强的普适性。
*   **鲁棒性**：在处理细粒度指令歧义（如不同楼层的电梯按钮）时表现出色。

### 8. 不足与局限
*   **路由依赖**：高度依赖语言指令的清晰度。如果指令存在歧义或无法映射到已知专家，系统可能失效。
*   **知识共享受限**：由于专家之间严格隔离，任务间的潜在正向迁移（知识共享）可能无法被充分利用（作者提到未来将研究层次化专家结构来解决此问题）。
*   **表示层干扰**：虽然参数隔离了，但共享的冻结主干在处理极端分布外（OOD）视觉场景时，其表示能力可能成为瓶颈。

（完）
