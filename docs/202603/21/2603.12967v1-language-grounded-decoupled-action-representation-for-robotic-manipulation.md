---
title: Language-Grounded Decoupled Action Representation for Robotic Manipulation
title_zh: 机器人操作中基于语言引导的解耦动作表示
authors: "Wuding Weng, Tongshu Wu, Liucheng Chen, Siyu Xie, Zheng Wang, Xing Xu, Jingkuan Song, Heng Tao Shen"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12967v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于机器人操作和动作原语的视觉语言理解
tldr: 针对机器人操作中高层视觉语言理解与底层动作控制不匹配的问题，本文提出LaDA框架。该框架利用自然语言作为语义桥梁，将动作解耦为平移、旋转和夹爪控制三个可解释原语。通过语义引导的软标签对比学习和自适应加权策略，LaDA实现了动作原语的跨任务对齐，显著提升了机器人在处理新任务或语义相关任务时的泛化能力和运动一致性。
selection_source: fresh_fetch
motivation: 解决机器人操作中高层语义理解与底层动作控制之间的异构性，以及在处理新任务时动作生成不够鲁棒的问题。
method: 提出LaDA框架，将动作解耦为平移、旋转和夹爪控制三个原语，并结合语义引导的对比学习与自适应加权策略。
result: 在LIBERO、MimicGen仿真基准及真实世界实验中，LaDA展现了强大的性能并能有效泛化至未见任务。
conclusion: 通过引入细粒度的解耦动作表示和语义对齐机制，成功增强了机器人操作任务中的动作一致性与跨任务泛化能力。
---

## 摘要
高层视觉语言理解与底层动作控制之间的异构性仍然是机器人操作中的一个根本挑战。尽管最近的方法在特定任务的动作对齐方面取得了进展，但它们在为新任务或语义相关任务生成鲁棒且准确的动作时往往面临困难。为此，我们提出了基于语言引导的解耦动作表示（LaDA）框架，该框架利用自然语言作为语义桥梁来连接感知与控制。LaDA 引入了一个由三个可解释动作原语（平移、旋转和夹持器控制）组成的细粒度中间层，为底层动作提供了显式的语义结构。它进一步采用了一种语义引导的软标签对比学习目标，以对齐跨任务的相似动作原语，从而增强泛化能力和运动一致性。受课程学习启发，一种自适应权重策略动态平衡了对比目标和模仿目标，以实现稳定且有效的训练。在模拟基准测试（LIBERO 和 MimicGen）以及真实世界演示中的广泛实验验证了 LaDA 取得了强大的性能，并能有效地泛化到未知或相关的任务。

## Abstract
The heterogeneity between high-level vision-language understanding and low-level action control remains a fundamental challenge in robotic manipulation. Although recent methods have advanced task-specific action alignment, they often struggle to generate robust and accurate actions for novel or semantically related tasks. To address this, we propose the Language-Grounded Decoupled Action Representation (LaDA) framework, which leverages natural language as a semantic bridge to connect perception and control. LaDA introduces a fine-grained intermediate layer of three interpretable action primitives--translation, rotation, and gripper control--providing explicit semantic structure for low-level actions. It further employs a semantic-guided soft-label contrastive learning objective to align similar action primitives across tasks, enhancing generalization and motion consistency. An adaptive weighting strategy, inspired by curriculum learning, dynamically balances contrastive and imitation objectives for stable and effective training. Extensive experiments on simulated benchmarks (LIBERO and MimicGen) and real-world demonstrations validate that LaDA achieves strong performance and generalizes effectively to unseen or related tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Language-Grounded Decoupled Action Representation for Robotic Manipulation》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：机器人操作中高层视觉语言理解（语义）与底层动作控制（运动学）之间存在严重的**异构性**。
*   **研究背景**：
    *   现有的视觉-语言-动作（VLA）模型通常将感知与控制耦合在一起，缺乏可解释性。
    *   潜在动作学习（Latent Action Learning）虽能压缩动作空间，但缺乏明确语义，难以跨任务迁移。
    *   语言条件策略通常使用粗粒度的离散原语（如“向前移动”），无法捕捉精确的运动参数（如位移量、旋转轴）。
*   **动机**：利用自然语言作为“语义桥梁”，构建一种既具有语义基础又能在任务间迁移的细粒度动作表示。

### 2. 方法论
LaDA 框架的核心思想是将连续的 7-自由度（DoF）动作解耦为可解释的语言引导原语。
*   **动作解耦（Action Decomposition）**：将动作 $a_t$ 投影为三个正交原语：
    1.  **平移原语 ($\Delta T$)**：如“沿 [方向] 移动 [距离] 米”。
    2.  **旋转原语 ($\Delta R$)**：如“绕 [轴] 旋转 [角度] 度”。
    3.  **夹爪原语 ($G$)**：离散指令“打开”或“关闭”。
*   **语义引导的软标签对比学习（SCL）**：
    *   **软标签相似度矩阵 $S$**：不使用 0/1 硬标签，而是根据平移、旋转和夹爪属性的匹配程度计算连续的亲和力权重。
    *   **双路径对齐**：包含“动作-动作对齐”和“动作-原语对齐”，确保动作嵌入空间既能聚类相似意图，又能锚定到语言描述上。
*   **自适应损失加权（Adaptive Weighting）**：
    *   受课程学习启发，利用移动平均（MA）估计模仿损失（$L_{IL}$）和对比损失（$L_{CL}$）的量级。
    *   动态调整两者权重，防止训练初期模仿信号占主导而忽略语义对齐，确保训练稳定性。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：使用 Open X-Embodiment (OXE) 数据集的子集（约 2250 万帧图像），涵盖 22 种机器人形态。
    *   **仿真基准**：
        *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个套件，评估长程任务和语义泛化。
        *   **MimicGen**：评估接触密集型任务（如穿线、三件套组装）。
    *   **真实世界**：使用 Franka Emika Panda 机械臂进行“抓取并放置”实验。
*   **对比方法**：对比了包括 OpenVLA、Octo、Diffusion Policy、CLIP-RT、LAPA、UniACT 等在内的 15 种以上主流 VLA 模型。

### 4. 资源与算力
*   **模型规模**：LaDA 参数量约为 **0.6B**（基于 ViT-L/14 骨干网络）。
*   **算力说明**：文中提到使用了预训练的 CLIP 编码器和轻量级 MLP 适配器。**未明确列出**具体的 GPU 型号、数量及总训练时长，但指出其参数量仅为 CLIP-RT (1.3B) 的一半左右，具有更高的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 的 40 个任务上进行了评估，每个任务 50 次随机试验。
    *   在 MimicGen 的 9 个复杂任务上进行了对比。
    *   进行了**泛化性实验**（跨任务和相似任务泛化）以及**多任务迁移实验**。
    *   **消融实验**：分别验证了软标签对比学习（SCL）和自适应加权（AW）的有效性。
*   **充分性评价**：实验设计非常充分，涵盖了从基础性能到长程操作、从仿真到现实、从单任务到跨任务泛化的全方位评估，结果具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：LaDA 在 LIBERO 基准上达到了 **93.6%** 的平均成功率，超越了所有对比基准（包括参数量更大的模型）。
*   **泛化能力强**：在未见过的指令（如跨任务“推”动作）下，LaDA 表现出远超 CLIP-RT 的零样本/少样本迁移能力。
*   **语义一致性**：t-SNE 可视化证明 LaDA 学习到了更紧凑、语义结构更清晰的动作嵌入空间，不同任务间的相似动作原语在空间中自然重叠。

### 7. 优点
*   **解耦表示**：将复杂的 7-DoF 动作分解为语言可描述的原语，极大地提高了模型的可解释性。
*   **数据效率**：通过语义引导，在较小的参数规模下实现了优于大规模模型的性能。
*   **训练稳定性**：自适应加权策略有效解决了多目标优化中的不平衡问题。

### 8. 不足与局限
*   **原语离散化风险**：虽然微调阶段使用了连续回归，但预训练阶段对动作原语的离散化分桶可能会损失一定的运动精度。
*   **语言依赖性**：模型性能高度依赖于自动生成的语言描述质量，若描述存在偏差，可能影响语义对齐效果。
*   **现实场景复杂度**：真实世界实验主要集中在相对简单的拾取放置任务，对于更复杂的非结构化环境（如动态障碍物）的验证尚不充分。

（完）
