---
title: "Not All Features Are Created Equal: A Mechanistic Study of Vision-Language-Action Models"
title_zh: 并非所有特征都生而平等：视觉-语言-动作（VLA）模型的机械论研究
authors: "Bryce Grant, Xijia Zhao, Peng Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19233v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉-语言-动作模型的机理性研究
tldr: 本研究对六种不同规模（80M-7B参数）的视觉-语言-动作（VLA）模型进行了机理性分析，旨在揭示其如何将多模态输入转化为机器人动作。通过激活注入、稀疏自编码器和线性探针等技术，研究发现视觉路径在动作生成中起主导作用，而语言的敏感性则高度依赖于任务结构。研究还识别了不同模型路径在编码动作程序与目标语义上的功能分工，并发布了Action Atlas交互式探索工具。
selection_source: fresh_fetch
motivation: 旨在填补关于VLA模型如何内部处理多模态信息并将其转化为具体物理动作的理解空白。
method: 对六种主流VLA模型应用激活注入、稀疏自编码器（SAEs）和线性探针，在四个基准测试上进行了超过39万次的实验分析。
result: 实验表明视觉路径主导动作生成，语言仅在视觉上下文不足时起作用，且专家路径与VLM路径分别负责动作执行与语义理解。
conclusion: 揭示了VLA模型内部特征贡献的不均衡性，证明了动作程序与语义表示在激活空间中是可分离的。
---

## 摘要
视觉-语言-动作（VLA）模型在单一架构中结合了感知、语言和运动控制，但它们如何将多模态输入转化为动作仍不为人所知。我们将激活注入（activation injection）、稀疏自编码器（SAEs）和线性探针应用于六个模型，参数规模涵盖 80M 至 7B，在四个基准测试的 394,000 多个展开回合（rollout episodes）中进行了实验。在所有架构中，视觉路径在动作生成中占据主导地位：将基准激活注入到空提示（null-prompt）回合中可恢复近乎一致的行为，而跨任务注入则会将机器人引导至源任务位置（99.8% 的 X-VLA 回合与源轨迹对齐），这揭示了与场景坐标绑定而非抽象任务表示的空间受限运动程序。语言敏感性取决于任务结构而非模型设计：当视觉上下文唯一指定任务时，语言会被忽略；当多个目标共享一个场景时，语言变得至关重要（在错误提示下，X-VLA libero_goal 从 94% 降至 10%，而 libero_object 无论如何都保持在 60-100%）。在所有三种多路径架构（Pi-0.5、SmolVLA、GR00T）中，专家路径编码运动程序，而 VLM 路径编码目标语义（专家注入产生的行为位移是后者的 2 倍），子空间注入证实了这些路径占据了可分离的激活子空间。对于大多数架构，逐标记（per-token）的 SAE 处理对动作保真度至关重要，尽管均值池化（mean-pooling）提高了 X-VLA 的保真度。对比识别恢复了 82 个以上的操作概念，因果消融揭示了 28-92% 的零效应率敏感性，且与表示宽度无关。我们发布了 Action Atlas (https://action-atlas.com)，用于对所有六个模型的 VLA 表示进行交互式探索。

## Abstract
Vision-Language-Action (VLA) models combine perception, language, and motor control in a single architecture, yet how they translate multimodal inputs into actions remains poorly understood. We apply activation injection, sparse autoencoders (SAEs), and linear probes to six models spanning 80M--7B parameters across 394,000+ rollout episodes on four benchmarks. The visual pathway dominates action generation across all architectures: injecting baseline activations into null-prompt episodes recovers near-identical behavior, while cross-task injection steers robots toward source-task positions (99.8\% of X-VLA episodes align with the source trajectory), exposing spatially bound motor programs tied to scene coordinates rather than abstract task representations. Language sensitivity depends on task structure, not model design: when visual context uniquely specifies the task, language is ignored; when multiple goals share a scene, language becomes essential (X-VLA \texttt{libero\_goal}: 94\%$\to$10\% under wrong prompts vs.\ \texttt{libero\_object}: 60--100\% regardless). In all three multi-pathway architectures (\pizhalf{}, SmolVLA, GR00T), expert pathways encode motor programs while VLM pathways encode goal semantics ($2\times$ greater behavioral displacement from expert injection), and subspace injection confirms these occupy separable activation subspaces. Per-token SAE processing is essential for action fidelity on most architectures, though mean-pooling improves fidelity on X-VLA. Contrastive identification recovers 82+ manipulation concepts, and causal ablation reveals sensitivity spanning 28--92\% zero-effect rates independent of representation width. We release \textbf{Action Atlas} (https://action-atlas.com) for interactive exploration of VLA representations across all six models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Not All Features Are Created Equal: A Mechanistic Study of Vision-Language-Action Models》（并非所有特征都生而平等：视觉-语言-动作模型的机理性研究）的结构化分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管视觉-语言-动作（VLA）模型在机器人控制领域取得了巨大进展，但其内部决策机制仍是一个“黑盒”。目前尚不清楚这些模型是真正理解并遵循语言指令，还是仅仅依赖微调过程中学到的视觉-运动先验（Visual-Motor Priors）。
*   **核心问题**：VLA 模型如何将多模态输入（视觉、语言、本体感受）转化为具体的物理动作？不同架构之间的内部表示是否存在共性？
*   **整体含义**：本研究通过机理性解释（Mechanistic Interpretability）方法，系统性地揭示了 VLA 模型的内部运作逻辑，为诊断机器人行为异常、提升模型鲁棒性提供了理论依据。

### 2. 论文提出的方法论
研究者结合了四种关键技术对模型进行“解剖”：
*   **激活注入（Activation Injection）**：类似于神经科学中的电刺激实验。将一个成功回合（Source）的层激活替换到另一个回合（Target）中，观察行为变化。包括：
    *   *空注入（Null Injection）*：在无语言提示时注入激活。
    *   *跨任务注入（Cross-task Injection）*：在完全不同的场景间注入激活。
*   **反事实提示（Counterfactual Prompting）**：系统性修改文本指令（如空提示、否定指令、对象替换、中途切换），测试模型对语言的敏感度。
*   **稀疏自编码器（SAEs）**：将稠密的神经激活分解为稀疏、可解释的特征（如“杯子”、“抓取”等概念）。
    *   *关键创新*：提出**逐标记（Per-token）处理**，证明在动作生成中，均值池化会破坏关键的时间/空间信息。
*   **线性探针（Linear Probes）**：训练简单的线性分类器，测试中间层是否包含特定的状态信息（如物体位置、任务成功率）。

### 3. 实验设计
*   **研究对象**：涵盖 6 种架构，参数量从 80M 到 7B。
    *   VLA 模型：$\pi_{0.5}$、OpenVLA-OFT、X-VLA、SmolVLA、GR00T N1.5。
    *   对照组：ACT（无语言能力的视觉模型）。
*   **基准测试（Benchmarks）**：
    *   **LIBERO**：包含 4 个子集（Goal, Object, Spatial, Long），侧重知识迁移。
    *   **MetaWorld**：50 个桌面操作任务，分为不同难度等级。
    *   **SimplerEnv**：用于评估真实世界策略在仿真中的表现。
    *   **ALOHA**：双臂协作任务。
*   **对比维度**：跨架构验证、路径功能分工、语言敏感性分析、视觉鲁棒性测试。

### 4. 资源与算力
论文明确列出了使用的硬件资源：
*   **计算集群**：1 个 8×A100-SXM4-80GB 集群。
*   **个人工作站**：1 张 NVIDIA RTX 5090 (32GB)，2 张 RTX 4090 (24GB)。
*   **数据规模**：总实验数据超过 **7.1 TB**，包括 4.3 TB 的激活记录、152 GB 的 SAE 检查点，以及超过 **394,000 个** 展开回合（Rollout Episodes）。

### 5. 实验数量与充分性
*   **实验规模**：进行了 12 种类型的实验，总计近 40 万次仿真运行，这在解释性研究中属于极大规模。
*   **充分性**：研究不仅在单一模型上深入，还跨越了 6 种不同的架构（从流匹配到连续回归），确保了结论的普适性。
*   **客观性与公平性**：通过设置 ACT 作为无语言对照组，以及在不同难度、不同环境（SimplerEnv）下重复实验，消除了特定架构或环境带来的偏差。

### 6. 主要结论与发现
*   **视觉路径主导论**：在所有模型中，视觉路径决定了动作的生成。即使没有语言提示，只要注入视觉路径的激活，模型就能恢复 70% 以上的成功率。
*   **语言敏感性的“环境依赖”**：模型是否听从指令取决于场景是否有歧义。如果视觉上只有一个物体，模型会忽略指令；只有当场景中有多个潜在目标时，语言才起作用。
*   **空间绑定的运动程序**：跨任务注入实验发现，机器人会跑向源任务中物体所在的空间坐标，即使当前场景中那里什么都没有。这说明 VLA 编码的是“空间坐标序列”而非抽象的“物体关系”。
*   **路径功能分工**：在多路径模型（如 $\pi_{0.5}$、GR00T）中，专家路径负责“怎么做”（运动控制），VLM 路径负责“做什么”（目标语义）。

### 7. 优点（亮点）
*   **规模空前**：这是首个对 VLA 模型进行如此大规模、跨架构的机理性研究。
*   **因果验证**：不只是观察相关性，而是通过激活注入和 SAE 消融进行了大量的因果干预实验。
*   **实用工具**：发布了 **Action Atlas** 交互式平台，允许其他研究者可视化探索这些模型的内部特征。
*   **发现深刻**：揭示了“逐标记处理”对 VLA 解释性的必要性，纠正了以往直接套用 LLM 解释方法的误区。

### 8. 不足与局限
*   **仿真局限**：所有实验均在仿真环境中完成。虽然使用了 SimplerEnv 评估 sim-to-real，但真实物理世界中的噪声和微调可能会改变表示结构。
*   **提示词简单**：反事实提示主要针对简单的指令修改，未涉及复杂的组合指令或长程逻辑推理。
*   **时间对齐风险**：激活注入可能因源任务和目标任务的时间步不完全匹配而产生伪影。
*   **转向敏感性**：部分模型（如 $\pi_{0.5}$）对特征干预极其敏感，微小的修改可能导致灾难性失败，这限制了基于解释性的实时干预应用。

（完）
