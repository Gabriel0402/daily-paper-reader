---
title: Structured prototype regularization for synthetic-to-real driving scene parsing
title_zh: 面向合成到真实驾驶场景解析的结构化原型正则化
authors: "Jiahe Fan, Xiao Ma, Sergey Vityazev, George Giakos, Shaolong Shu, Rui Fan"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16083v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 自动驾驶场景解析
tldr: 本研究针对自动驾驶场景解析中合成到真实域的差异问题，提出了一种结构化原型正则化框架。该方法通过引入类别原型来增强特征空间的类间分离度和类内紧凑性，并结合熵值噪声过滤和像素级注意力机制优化伪标签质量。实验证明，该方法在多个基准测试上超越了现有技术，显著提升了模型在真实环境下的泛化能力和语义结构一致性。
selection_source: fresh_fetch
motivation: 现有的无监督域自适应方法多侧重于全局特征对齐，忽视了特征空间中类别间的语义结构关系，限制了模型的泛化性能。
method: 提出一种利用类别原型强化特征聚类判别性、结合熵过滤优化伪标签并利用像素级注意力精细化对齐的无监督域自适应框架。
result: 在代表性的合成到真实驾驶场景基准测试中，该方法一致性地优于近年来的最先进方法。
conclusion: 显式正则化语义特征结构对于提升驾驶场景解析任务中合成到真实域自适应的鲁棒性至关重要。
---

## 摘要
驾驶场景解析对于自动驾驶车辆在复杂的现实交通环境中可靠运行至关重要。为了减少对昂贵的像素级标注的依赖，带有自动生成标签的合成数据集已成为一种流行的替代方案。然而，由于合成到真实的领域差异，在合成数据上训练的模型在应用于现实场景时通常表现不佳。尽管无监督领域自适应在缩小这一差距方面取得了成功，但大多数现有方法主要关注全局特征对齐，而忽视了特征空间的语义结构。结果导致类间语义关系建模不足，限制了模型的泛化能力。为了应对这些挑战，本研究引入了一种新型无监督领域自适应框架，该框架通过显式正则化语义特征结构，显著提升了现实场景下的驾驶场景解析性能。具体而言，所提方法利用特定类别的原型来强制实现类间分离和类内紧凑，从而增强了特征簇的可区分性和结构一致性。一种基于熵的噪声过滤策略提高了伪标签的可靠性，而像素级注意力机制则进一步细化了特征对齐。在代表性基准测试上的广泛实验表明，所提方法始终优于近期的先进方法。这些结果强调了在驾驶场景解析任务中，保持语义结构对于实现稳健的合成到真实自适应的重要性。

## Abstract
Driving scene parsing is critical for autonomous vehicles to operate reliably in complex real-world traffic environments. To reduce the reliance on costly pixel-level annotations, synthetic datasets with automatically generated labels have become a popular alternative. However, models trained on synthetic data often perform poorly when applied to real-world scenes due to the synthetic-to-real domain gap. Despite the success of unsupervised domain adaptation in narrowing this gap, most existing methods mainly focus on global feature alignment while overlooking the semantic structure of the feature space. As a result, semantic relations among classes are insufficiently modeled, limiting the model's ability to generalize. To address these challenges, this study introduces a novel unsupervised domain adaptation framework that explicitly regularizes semantic feature structures to significantly enhance driving scene parsing performance in real-world scenarios. Specifically, the proposed method enforces inter-class separation and intra-class compactness by leveraging class-specific prototypes, thereby enhancing the discriminability and structural coherence of feature clusters. An entropy-based noise filtering strategy improves the reliability of pseudo labels, while a pixel-level attention mechanism further refines feature alignment. Extensive experiments on representative benchmarks demonstrate that the proposed method consistently outperforms recent state-of-the-art methods. These results underscore the importance of preserving semantic structure for robust synthetic-to-real adaptation in driving scene parsing tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Structured prototype regularization for synthetic-to-real driving scene parsing》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：解决自动驾驶场景解析中，模型在合成数据（源域）上训练但在真实世界（目标域）应用时，由于“领域间隙”（Domain Gap）导致的性能大幅下降问题。
*   **研究动机**：
    *   **标注成本高昂**：真实场景的像素级标注极其耗时（如 Cityscapes 单张图需 1.5-3.3 小时）。
    *   **现有方法缺陷**：目前的无监督域自适应（UDA）方法多侧重于全局特征对齐（如对抗学习），往往忽视了特征空间的**语义结构**。
    *   **结构建模不足**：现有基于原型的对比学习方法通常将原型视为孤立点，未能显式建模类间分离度（Inter-class separability）和类内紧凑性（Intra-class compactness），导致在复杂真实环境下的泛化能力受限。

### 2. 核心方法论：结构化原型正则化（SPR）
该论文提出了一种名为 **SPR** 的框架，核心思想是通过显式正则化特征空间的语义结构来增强对齐效果。
*   **类别原型估计**：计算每个语义类别的特征质心（Prototype），并结合源域和目标域的原型进行线性更新，作为特征空间的稳健锚点。
*   **原型-原型交互（结构化建模）**：
    *   计算**类间交互矩阵**：量化不同类别间的相关性，识别语义模糊区域。
    *   计算**类内交互矩阵**：量化同一类别在不同特征通道间的相关性，衡量特征的一致性。
    *   **正则化项**：利用上述矩阵对原始原型进行修正，通过超参数 $\lambda_e$ 和 $\lambda_a$ 强制拉开类间距离并压缩类内分布。
*   **原型-像素交互（精细化对齐）**：
    *   **自适应伪标签过滤**：基于欧氏距离计算预测结果的熵值（Entropy），仅保留熵值最低（最可靠）的前 $\alpha$ 比例的像素作为伪标签，避免噪声干扰。
    *   **像素级注意力机制**：在对比损失函数中引入基于熵的权重，使模型更加关注边界或语义模糊的挑战性区域。
*   **对比学习目标**：结合修正后的结构化原型，构建源域到源域、目标域到原型的对比损失，实现跨域特征的语义一致性对齐。

### 3. 实验设计
*   **数据集与场景**：
    1.  **GTA5 $\to$ Cityscapes**：从合成游戏场景到真实城市街道。
    2.  **SYNTHIA $\to$ Cityscapes**：从另一种合成数据集到真实街道（空间布局差异更大）。
    3.  **Cityscapes $\to$ ACDC**：从正常天气到恶劣天气（雾、雨、雪、夜），验证模型在极端环境下的鲁棒性。
*   **Benchmark 与对比方法**：
    *   基准模型采用 **DeepLab-v2 (ResNet-101)**。
    *   对比了超过 30 种先进方法，包括 AdaptSegNet、ADVENT、FADA、ProDA、SePiCo、CACP 等。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **Intel Xeon Gold 6154 CPU** 和单张 **NVIDIA RTX 3090 GPU (24GB)** 的异构系统。
*   **训练细节**：
    *   优化器：SGD（学习率 $2.5 \times 10^{-4}$，动量 0.9）。
    *   训练时长：限制为 20 个 Epoch。
    *   推理性能：在 ResNet-101 后端下达到 **23 FPS**，延迟为 43.4ms，满足实时性要求。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了三大主流基准测试，并进行了详尽的定量分析（mIoU 指标）和定性可视化（t-SNE 聚类图、分割效果图）。
*   **消融实验**：针对类内交互、类间交互、像素级注意力、自适应阈值等每个组件都做了独立验证，证明了各模块的贡献。
*   **参数敏感性分析**：对正则化权重 $\lambda$ 和过滤比例 $\alpha$ 进行了网格搜索，展示了超参数对性能的影响。
*   **客观性**：实验遵循了 UDA 领域的标准评估协议，对比方法涵盖了从 2017 年到 2025 年的经典及最新 SOTA 算法，实验设计全面且公平。

### 6. 主要结论与发现
*   **性能提升显著**：在 GTA5 $\to$ Cityscapes 任务中，SPR 将基准 mIoU 从 37.1% 提升至 **53.1%**（结合自训练后达到 54.4%）。
*   **结构正则化的重要性**：显式约束类间分离和类内紧凑比单纯的全局对齐更能产生判别性强的特征。
*   **大类保护**：相比于一些过度优化长尾类别而牺牲导航关键类（如道路、人行道）的方法，SPR 在保持大类准确率方面表现优异（道路 IoU 达 97.9%）。
*   **恶劣环境鲁棒性**：在 ACDC 数据集上的表现证明了该方法能有效应对光照和纹理损坏带来的领域偏移。

### 7. 优点与亮点
*   **结构化视角**：不同于以往将原型视为孤立点的做法，SPR 引入了矩阵化的交互建模，捕捉了特征空间的内在几何结构。
*   **噪声鲁棒性**：通过熵值过滤和注意力机制，巧妙地解决了目标域伪标签质量低下的痛点。
*   **部署友好**：该方法仅在训练阶段增加计算开销，**推理阶段不增加任何额外参数或模块**，非常适合自动驾驶实时系统。

### 8. 不足与局限
*   **训练开销**：虽然推理很快，但训练时计算高维特征通道间的相关矩阵会增加一定的内存占用和计算时间。
*   **源域依赖**：虽然论文探讨了无源（Source-free）场景的可能性，但 SPR 在完全脱离源域数据时的性能相比专门的 SFUDA 方法仍有差距。
*   **超参数敏感性**：正则化权重 $\lambda$ 需要针对不同数据集进行微调以达到最优平衡，缺乏一种完全自动化的参数确定机制。

（完）
