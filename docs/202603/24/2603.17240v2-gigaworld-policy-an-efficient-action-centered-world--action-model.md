---
title: "GigaWorld-Policy: An Efficient Action-Centered World--Action Model"
title_zh: GigaWorld-Policy：一种高效的以动作为中心的世界-动作模型
authors: "Angen Ye, Boyuan Wang, Chaojun Ni, Guan Huang, Guosheng Zhao, Hao Li, Hengtao Li, Jie Li, Jindi Lv, Jingyu Liu, Min Cao, Peng Li, Qiuping Deng, Wenjun Mei, Xiaofeng Wang, Xinze Chen, Xinyu Zhou, Yang Wang, Yifan Chang, Yifan Li, Yukun Zhou, Yun Ye, Zhichao Liu, Zheng Zhu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17240v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于机器人策略学习的以动作为中心的世界动作模型
tldr: 本研究针对现有世界-动作模型（WAM）推理开销大且视觉与动作表征耦合的问题，提出了GigaWorld-Policy。该模型采用以动作为中心的设计，通过因果建模确保动作预测不依赖于未来视频生成，从而实现高效推理。在推理时，视频生成变为可选，显著提升了速度。实验表明，该模型在保持高成功率的同时，推理速度比领先基准快9倍，在RoboTwin 2.0任务中表现优异。
selection_source: fresh_fetch
motivation: 现有的世界-动作模型因视觉与动作推理耦合导致计算开销巨大，且动作预测精度受限于视频生成质量。
method: 提出一种以动作为中心的因果设计，使模型在预测动作的同时可选地生成视频，并通过大规模机器人数据集进行预训练。
result: "在真实机器人平台上，该模型比Motus快9倍且成功率提升7%，在RoboTwin 2.0上性能比pi-0.5提升了95%。"
conclusion: GigaWorld-Policy证明了通过解耦动作预测与视频生成，可以在不牺牲物理一致性的前提下显著提升机器人策略的推理效率和任务表现。
---

## 摘要
基于预训练视频生成骨干网络初始化的世界-动作模型（World-Action Models, WAM）在机器人策略学习中展现出巨大潜力。然而，现有方法面临两个阻碍性能和部署的关键瓶颈。首先，对未来视觉动力学和相应动作进行联合推理会产生巨大的推理开销。其次，联合建模往往使视觉和运动表示纠缠在一起，导致运动预测的准确性严重依赖于未来视频预测的质量。为解决这些问题，我们提出了 GigaWorld-Policy，这是一种以动作为中心的 WAM，它在学习二维像素-动作动力学的同时，实现了高效的动作解码，并支持可选的视频生成。具体而言，我们将策略训练表述为两个耦合的部分：模型根据当前观测预测未来动作序列，并同时根据预测的动作和相同的观测生成未来视频。该策略受到动作预测和视频生成的共同监督，提供了更丰富的学习信号，并通过视觉动力学约束促使产生符合物理规律的动作。通过防止未来视频 token 影响动作 token 的因果设计，显式的未来视频生成在推理时是可选的，从而在部署期间实现更快的动作预测。为了支持这一范式，我们策划了一个多样化的大规模机器人数据集，用于预训练一个以动作为中心的视频生成模型，随后将其作为机器人策略学习的骨干网络。在真实机器人平台上的实验结果表明，GigaWorld-Policy 的运行速度比领先的 WAM 基准模型 Motus 快 9 倍，同时任务成功率提高了 7%。此外，与 pi-0.5 相比，GigaWorld-Policy 在 RoboTwin 2.0 上的性能提升了 95%。

## Abstract
World-Action Models (WAM) initialized from pre-trained video generation backbones have demonstrated remarkable potential for robot policy learning. However, existing approaches face two critical bottlenecks that hinder performance and deployment. First, jointly reasoning over future visual dynamics and corresponding actions incurs substantial inference overhead. Second, joint modeling often entangles visual and motion representations, making motion prediction accuracy heavily dependent on the quality of future video forecasts. To address these issues, we introduce GigaWorld-Policy, an action-centered WAM that learns 2D pixel-action dynamics while enabling efficient action decoding, with optional video generation. Specifically, we formulate policy training into two coupled components: the model predicts future action sequences conditioned on the current observation, and simultaneously generates future videos conditioned on the predicted actions and the same observation. The policy is supervised by both action prediction and video generation, providing richer learning signals and encouraging physically plausible actions through visual-dynamics constraints. With a causal design that prevents future-video tokens from influencing action tokens, explicit future-video generation is optional at inference time, allowing faster action prediction during deployment. To support this paradigm, we curate a diverse, large-scale robot dataset to pre-train an action-centered video generation model, which is then adapted as the backbone for robot policy learning. Experimental results on real-world robotic platforms show that GigaWorld-Policy runs 9x faster than the leading WAM baseline, Motus, while improving task success rates by 7%. Moreover, compared with pi-0.5, GigaWorld-Policy improves performance by 95% on RoboTwin 2.0.

---

## 论文详细总结（自动生成）

### GigaWorld-Policy：高效的以动作为中心的世界-动作模型深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前机器人操控领域中**视觉-语言-动作（VLA）模型**和**世界-动作模型（WAM）**的局限性提出了改进方案。
*   **背景**：传统的 VLA 模型（如 $\pi_0$）虽然推理快，但面临“监督稀疏”问题，即动作标签维度低且缺乏物理一致性约束。而新兴的 WAM 通过预测未来视频来增强物理理解，但存在**推理延迟高**（必须生成完整视频才能得出动作）和**误差累积**（视频预测的小错会导致动作解码崩溃）的问题。
*   **核心问题**：如何在利用未来视觉动力学作为密集监督信号的同时，实现低延迟、不依赖显式视频生成的实时机器人控制？

#### 2. 方法论：核心思想与关键技术
GigaWorld-Policy 提出了一种**以动作为中心（Action-Centered）**的架构设计：
*   **核心思想**：在训练阶段，利用未来视频生成作为辅助任务来提供物理规律的正则化；在推理阶段，通过因果掩码设计，使得模型可以跳过视频生成，直接从当前观测解码动作。
*   **关键技术细节**：
    *   **因果自注意力掩码（Causal Self-Attention Mask）**：这是该模型最核心的设计。模型将当前观测、状态、动作和未来视频 token 打包。掩码规定：动作 token 只能关注当前观测和状态，严禁关注未来视频 token。这消除了动作对视频生成的依赖，确保了推理的高效性。
    *   **统一架构**：基于 5B 参数的 Diffusion Transformer (DiT) 骨干网络，使用单一堆栈处理所有模态，通过不同的位置编码区分视觉（2D）和动作（1D）信号。
    *   **训练目标**：采用 **Flow Matching（流匹配）** 算法。联合优化动作预测损失（$\mathcal{L}_{action}$）和视频动力学损失（$\mathcal{L}_{video}$）。
    *   **三阶段课程学习**：
        1.  **基础预训练**：从大规模互联网视频模型初始化。
        2.  **具身数据预训练**：在约 10,000 小时的机器人和人类第一视角视频上训练，注入物理先验。
        3.  **目标任务微调**：在特定机器人的轨迹数据上进行指令对齐。

#### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：使用 **RoboTwin 2.0** 平台，涵盖 50 多个操控任务（如调整瓶子、叠块、开笔记本等），并引入了强随机化的背景和光照。
    *   **真实世界**：使用 AgileX PiPER 6-DoF 机械臂，设计了四个复杂任务：清理桌面（含几何排序约束）、扫描二维码（涉及工具使用和视觉对齐）、叠碗、扫垃圾。
*   **Benchmark 与对比方法**：
    *   **VLA 类基准**：$\pi_{0.5}$、GigaBrain-0、X-VLA。
    *   **WAM 类基准**：Motus（目前的 SOTA）、Cosmos-Policy。

#### 4. 资源与算力
*   **算力消耗**：论文明确提到在公共数据集上进行了 **6000 GPU 小时** 的预训练。
*   **硬件环境**：推理延迟测试是在 **NVIDIA A100 GPU** 上进行的。
*   **参数规模**：模型骨干为 **5B（50亿）参数** 的 Diffusion Transformer。
*   **训练细节**：全局 Batch Size 为 256，使用 AdamW 优化器，学习率采用余弦退火策略（$1 \times 10^{-4}$ 到 $1 \times 10^{-6}$）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖了 50 多个任务，每个任务 100 个测试回合，数据量大且具有统计意义。
    *   实机实验针对 4 个任务，每个任务 20 次试验，并采用了分级评分制（如抓取成功得 0.5，放置成功再得 0.5）。
    *   **消融实验**：非常充分，分别验证了预训练阶段的作用、未来预测帧数（$\Delta$ 步长）的影响、因果掩码的必要性以及数据效率（证明了仅用 10% 数据即可达到 VLA 全量效果）。
*   **客观性**：对比了当前最强的开源和闭源模型，且在相同硬件（A100）上测试推理频率，评价标准客观。

#### 6. 主要结论与发现
*   **推理效率极高**：GigaWorld-Policy 实现了 **0.36s** 的单次推理延迟，比 SOTA 模型 Motus 快了 **9 倍**。
*   **性能卓越**：在实机任务中，成功率比 Motus 高出 **7%**；在仿真任务中，性能比 $\pi_{0.5}$ 提升了 **95%**。
*   **数据效率**：由于引入了强大的视频物理先验，该模型在仅使用 10% 的演示数据时，表现即可持平全量数据训练的 VLA 模型。
*   **视频预测的价值**：实验证明，即使推理时不生成视频，训练时的视频监督也能显著提升动作的准确性和稳定性。

#### 7. 优点（亮点）
*   **解耦设计**：巧妙地通过因果掩码解决了 WAM 模型“高延迟”的痛点，使视频生成变为“可选”而非“必须”。
*   **物理一致性**：通过预测未来视觉演化，模型生成的动作更符合物理常识，减少了“幻觉”动作。
*   **强大的预训练范式**：成功将互联网视频知识转化为机器人操控能力，展示了极佳的迁移效果。

#### 8. 不足与局限
*   **算力门槛**：尽管推理优化显著，但 5B 参数的 Diffusion 模型在边缘设备（如机器人板载计算单元）上直接运行仍有挑战，目前仍依赖 A100 等高性能 GPU。
*   **任务复杂度限制**：虽然实机任务包含多步规划，但主要集中在单臂操控，对于更复杂的双臂协同或极高精度的装配任务，实验覆盖尚显不足。
*   **视频预测步长**：消融实验显示预测过密的未来帧反而可能降低性能，这暗示模型在处理极短时间内的精细动力学时可能仍存在瓶颈。

（完）
