---
title: "Focus on Background: Exploring SAM's Potential in Few-shot Medical Image Segmentation with Background-centric Prompting"
title_zh: 聚焦背景：通过以背景为中心的提示探索 SAM 在少样本医学图像分割中的潜力
authors: "Yuntian Bo, Yazhou Zhu, Piotr Koniusz, Haofeng Zhang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21287v1"
tags: ["query:mmseg"]
score: 9.0
evidence: SAM模型在医学图像分割中的微调与提示策略
tldr: 针对少样本医学图像分割（FSMIS）中SAM模型因边界模糊易导致过度分割的问题，本文提出FoB框架。该方法将分割任务重新定义为提示定位任务，通过生成并定位背景提示来约束SAM的分割范围。FoB利用上下文信息捕捉前景与背景的空间依赖，并结合医学图像的固有结构模式进行逐步优化，在多个数据集上达到了SOTA性能，展现了极强的跨域泛化能力。
selection_source: fresh_fetch
motivation: 传统FSMIS方法存在性能瓶颈，且SAM在处理医学图像时因解剖边界模糊常导致严重的过度分割现象。
method: 提出FoB框架，通过生成类别无关的背景提示并将其定位到查询图像中，利用上下文建模和结构约束来精细化背景预测。
result: 在三个多样化的医学图像数据集上，FoB显著优于现有基准模型，实现了最先进的分割性能。
conclusion: 聚焦背景的提示生成策略能有效抑制SAM的过度分割，显著提升其在少样本医学影像任务中的准确性和泛化性。
---

## 摘要
传统的少样本医学图像分割（FSMIS）方法面临性能瓶颈，阻碍了其更广泛的临床应用。尽管 Segment Anything Model (SAM) 展现出强大的类别无关分割能力，但由于解剖边界模糊，将其直接应用于医学图像往往会导致过度分割。在本文中，我们将基于 SAM 的 FSMIS 重新表述为提示定位任务，并提出了 FoB（Focus on Background），这是一种以背景为中心的提示生成器，通过提供准确的背景提示来约束 SAM 的过度分割。具体而言，FoB 通过生成类别无关的支持背景提示并将其直接定位在查询图像中，弥合了分割与提示定位之间的差距。为了解决新类别提示定位的挑战，FoB 建模了丰富的上下文信息，以捕获前景与背景的空间依赖关系。此外，受医学图像中背景提示固有结构模式的启发，FoB 将该结构建模为约束，以逐步优化背景提示的预测。在三个不同医学图像数据集上的实验表明，FoB 以较大优势优于其他基准模型，在 FSMIS 上达到了最先进的性能，并展现出强大的跨域泛化能力。我们的代码可在 https://github.com/primebo1/FoB_SAM 获取。

## Abstract
Conventional few-shot medical image segmentation (FSMIS) approaches face performance bottlenecks that hinder broader clinical applicability. Although the Segment Anything Model (SAM) exhibits strong category-agnostic segmentation capabilities, its direct application to medical images often leads to over-segmentation due to ambiguous anatomical boundaries. In this paper, we reformulate SAM-based FSMIS as a prompt localization task and propose FoB (Focus on Background), a background-centric prompt generator that provides accurate background prompts to constrain SAM's over-segmentation. Specifically, FoB bridges the gap between segmentation and prompt localization by category-agnostic generation of support background prompts and localizing them directly in the query image. To address the challenge of prompt localization for novel categories, FoB models rich contextual information to capture foreground-background spatial dependencies. Moreover, inspired by the inherent structural patterns of background prompts in medical images, FoB models this structure as a constraint to progressively refine background prompt predictions. Experiments on three diverse medical image datasets demonstrate that FoB outperforms other baselines by large margins, achieving state-of-the-art performance on FSMIS, and exhibiting strong cross-domain generalization. Our code is available at https://github.com/primebo1/FoB_SAM.

---

## 论文详细总结（自动生成）

以下是对论文《Focus on Background: Exploring SAM's Potential in Few-shot Medical Image Segmentation with Background-centric Prompting》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：少样本医学图像分割（FSMIS）中，直接应用基础模型 SAM（Segment Anything Model）会因医学图像对比度低、解剖边界模糊而导致严重的**过度分割（Over-segmentation）**。
*   **研究动机**：作者发现，精确的**背景提示（Background Prompts）**比前景提示更能有效约束 SAM 的分割边界。然而，现有方法（如 ProtoSAM）多关注前景提示生成，忽视了背景提示的定位。因此，本文旨在将 FSMIS 重新定义为一个“以背景为中心”的提示定位任务。

### 2. 方法论
论文提出了 **FoB (Focus on Background)** 框架，包含三个核心模块：
*   **背景提示原型构建 (BPPC)**：从支持图像（Support Image）的掩码中，通过膨胀操作提取目标边缘外的差异区域，并利用掩码平均池化（MAP）提取 $N_p$ 个背景提示原型。
*   **以背景为中心的上下文建模 (BCM)**：
    *   首先抑制查询图像（Query Image）中的前景特征，突出背景。
    *   利用 Transformer 建模背景提示与前景之间的空间依赖关系，生成初步的背景提示热图和坐标。
*   **结构引导的提示精细化 (SPR)**：
    *   **图结构传播 (SPG)**：利用医学图像背景提示通常呈“环形”分布的先验知识，构建结合自适应结构和环形先验的图，通过图卷积（GCN）校准提示特征。
    *   **迭代变形精细化 (IDR)**：借鉴变形注意力机制，根据更新后的特征迭代调整提示坐标，使其更符合解剖结构。
*   **损失函数**：引入区域感知对比损失（RAC Loss）防止背景提示落入前景区域，并结合热图回归、坐标回归和前景分类损失进行端到端训练。

### 3. 实验设计
*   **数据集**：
    *   **Abd-CT**：腹部 CT 扫描（肝、肾、脾等）。
    *   **Abd-MRI**：腹部 MRI 扫描。
    *   **Skin-DS**：皮肤病变图像（黑色素瘤、痣等）。
*   **实验设置**：采用 1-way 1-shot 设置。分为 Setting I（测试类可能在训练背景中出现）和 Setting II（测试类完全不可见）。
*   **对比基准 (Benchmarks)**：
    *   传统 FSMIS 方法：ALPNet, RPT, GMRD, PGRNet 等。
    *   基于 SAM 的方法：ProtoSAM, AM-SAM。
    *   跨域实验对比：RobustEMD, FAMNet。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 4080S GPU**。
*   **训练细节**：训练时长为 36K 次迭代，Batch Size 为 1。
*   **优化器**：Adam 优化器，初始学习率 $1 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同模态、不同解剖区域的数据集上进行了验证，并采用了 **5 折交叉验证**。
*   **消融实验**：非常充分。涵盖了模块有效性（BCM, SPR）、损失函数作用、提示点数量（$N_f, N_p$）、结构图构建策略、超参数（如高斯标准差 $\sigma$、迭代次数 $\kappa$）等。
*   **客观性**：通过跨域实验（CT $\to$ MRI）验证了模型的泛化性，并对比了不同 SAM 后端（如 SAM-Med2D），实验设计严谨、公平。

### 6. 主要结论与发现
*   **背景提示至关重要**：实验证明，增加背景提示能显著提升 SAM 在医学图像上的 Dice 分数，有效抑制了边界溢出。
*   **性能领先**：FoB 在 Abd-CT 上达到了 86.21% 的 Dice 分数，远超此前最先进的方法。
*   **跨域鲁棒性**：由于 FoB 学习的是空间布局和几何关系（领域无关特征），其在跨模态任务中表现出极强的迁移能力。

### 7. 亮点
*   **视角新颖**：打破了“强化前景”的惯性思维，通过“强化背景约束”解决了 SAM 在医学领域的适配难题。
*   **即插即用**：FoB 作为一个独立的提示生成器，无需微调 SAM 庞大的参数，推理效率高。
*   **结构先验**：巧妙地将解剖结构的“环形先验”引入图卷积网络，提升了提示点定位的物理合理性。

### 8. 不足与局限
*   **复杂结构适配性**：对于极度不规则或细长结构（如血管），固定的提示点数量和环形先验可能不再适用。
*   **提示点数量敏感**：虽然实验给出了推荐值，但在不同任务中可能需要手动调整提示点数量以达到最优平衡。
*   **应用限制**：目前主要针对 2D 切片进行处理，对于 3D 空间连续性的利用尚有提升空间。

（完）
