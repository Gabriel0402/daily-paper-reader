---
title: Synchronous Signal Temporal Logic for Decidable Verification of Cyber-Physical Systems
title_zh: 用于信息物理系统可判定验证的同步信号时序逻辑
authors: "Partha Roop, Sobhan Chatterjee, Avinash Malik, Nathan Allen, Logan Kenwright"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25531v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于信息物理系统可判定验证的同步信号时序逻辑
tldr: 针对网络实体系统（CPS）安全性验证中信号时序逻辑（STL）静态验证不可判定这一难题，本文提出同步信号时序逻辑（SSTL）。通过引入固定采样步长和信号不变性假设（SIH），将STL转化为可判定的片段。该方法支持静态安全性和活性验证，并能通过SPIN工具进行模型检测，在人类心脏模型等案例中验证了其有效性。
selection_source: fresh_fetch
motivation: 传统的信号时序逻辑（STL）在进行静态验证时通常是不可判定的，限制了其在关键安全系统中的应用。
method: 提出SSTL逻辑，基于信号不变性假设（SIH）将连续信号离散化，并将其转换为谓词线性时序逻辑（LTL_P）。
result: 证明了SIH是SSTL与STL等价的充要条件，并利用SPIN模型检测器实现了对CPS属性的可判定验证。
conclusion: SSTL为CPS提供了一个可判定的形式化验证框架，在保证验证精度的同时解决了静态验证的计算难题。
---

## 摘要
许多信息物理系统（CPS）运行在安全关键型环境中，其中正确执行、可靠性和可信性至关重要。信号时序逻辑（STL）为检查安全关键型 CPS 提供了一个形式化框架。然而，STL 的静态验证通常是不可判定的，除非使用基于运行时的验证方法，但这些方法存在局限性。我们提出了同步信号时序逻辑（SSTL），它是 STL 的一个可判定片段，支持静态安全性和活性属性验证。在 SSTL 中，我们假设信号在固定的离散步长（称为 tick）进行采样，并提出了信号不变性假设（SIH），该假设受到同步程序中类似假设的启发。我们定义了 SSTL 的语法和语义，并证明了 SIH 是 STL 公式与其对应的 SSTL 公式之间等价的充分必要条件。通过将 SSTL 转换为 LTL_P（定义在谓词上的 LTL），我们能够使用 SPIN 模型检测器进行可判定的模型检测。我们在 33 节点人类心脏模型和其他案例研究中演示了该方法。

## Abstract
Many Cyber Physical System (CPS) work in a safety-critical environment, where correct execution, reliability and trustworthiness are essential. Signal Temporal Logic (STL) provides a formal framework for checking safety-critical CPS. However, static verification of STL is undecidable in general, except when we want to verify using run-time-based methods, which have limitations. We propose Synchronous Signal Temporal Logic (SSTL), a decidable fragment of STL, which admits static safety and liveness property verification. In SSTL, we assume that a signal is sampled at fixed discrete steps, called ticks, and then propose a hypothesis, called the Signal Invariance Hypothesis (SIH), which is inspired by a similar hypothesis for synchronous programs. We define the syntax and semantics of SSTL and show that SIH is a necessary and sufficient condition for equivalence between an STL formula and its SSTL counterpart. By translating SSTL to LTL_P (LTL defined over predicates), we enable decidable model checking using the SPIN model checker. We demonstrate the approach on a 33-node human heart model and other case studies.

---

## 论文详细总结（自动生成）

## 论文总结：用于信息物理系统可判定验证的同步信号时序逻辑 (SSTL)

### 1. 核心问题与整体含义
*   **研究背景**：信息物理系统（CPS）如自动驾驶、医疗设备等运行在安全关键环境中。信号时序逻辑（STL）是描述这类系统属性的标准工具。
*   **核心痛点**：在连续时间域下，STL 的静态验证（Static Verification）通常是**不可判定（Undecidable）**的。现有的运行时验证或控制器合成方法在覆盖范围或扩展性上存在局限，且目前几乎没有方法能静态验证 STL 中的“活性”（Liveness，即“好事总会发生”）属性。
*   **研究目标**：提出一种名为 **SSTL（同步信号时序逻辑）** 的离散化逻辑片段，通过将连续信号映射到离散的同步步长（Ticks）上，实现 CPS 属性的可判定静态验证。

### 2. 方法论
*   **核心思想**：借鉴同步编程范式，假设系统在离散的时间点采样并反应。
*   **关键技术细节**：
    *   **时间离散化**：定义映射函数 $[t] = \lfloor t/\Delta t \rfloor$，将实数时间轴划分为固定长度的步长 $\Delta t$。
    *   **信号不变性假设 (SIH)**：这是 SSTL 的基石。假设信号在两个连续的采样点（Tick）之间保持不变。论文证明了 SIH 是 SSTL 与 STL 语义等价的充分必要条件。
    *   **逻辑转换 ($SSTL \to LTL_P$)**：
        *   将 SSTL 公式递归转换为带谓词的线性时序逻辑（$LTL_P$）。
        *   **时限处理**：对于带时间窗口的算子（如 $\Box_{[a,b]}$），引入位置索引 $j_0$ 和谓词 `within[a, b]`，将其转化为相对位置的约束。
    *   **验证流程**：使用 Promela 语言对系统建模（结合 C 代码处理连续动力学），将转换后的 $LTL_P$ 公式输入 **SPIN 模型检测器** 进行自动化验证。

### 3. 实验设计
*   **实验场景**：
    1.  **交通灯控制器**：基础离散系统，验证互斥、安全和公平性等布尔属性。
    2.  **行人过街系统**：包含整数信号（排队人数），验证阈值触发和响应时间。
    3.  **33 节点人类心脏模型**：复杂的生理电信号系统。验证房室传导（AV Conduction）延迟和心室不应期等精确的时间约束。
*   **对比与基准**：
    *   **理论对比**：与 Fainekos & Pappas、Deshmukh 等人的离散化/鲁棒性方法进行了对比（见论文表 2 和表 3）。
    *   **故障注入对比**：在心脏模型中引入“传导阻滞”等疾病模型，对比健康与患病状态下属性的满足情况。

### 4. 资源与算力
*   **硬件说明**：论文未明确列出具体的 CPU 或 GPU 型号。
*   **时间开销**：简单属性（交通灯）验证约需 1.5s；复杂属性（心脏模型、行人过街）需 6s 至 309s 不等。
*   **内存开销**：心脏模型和行人过街系统的验证内存需求较高，分别达到约 **9000 MB** 和 **7000 MB**。

### 5. 实验数量与充分性
*   **实验规模**：针对三个案例共验证了约 15 组不同的 SSTL 属性，涵盖了安全性、活性、有界响应等多种类型。
*   **充分性评价**：
    *   **多样性**：实验覆盖了从纯离散到连续信号离散化的不同复杂度。
    *   **深度**：对心脏模型的验证不仅停留在“通过”，还通过模拟疾病（AV Block 等）验证了框架检测违规行为的能力。
    *   **客观性**：使用了工业级工具 SPIN，验证深度设定为 200,000 层，具有较高的可信度。

### 6. 主要结论与发现
*   **可判定性**：证明了 SSTL 的验证问题是可判定的，解决了 STL 静态验证的理论难题。
*   **等价性**：在满足 SIH（采样率符合奈奎斯特准则）的情况下，SSTL 验证结果与连续 STL 完全一致。
*   **活性验证**：成功实现了对 CPS 活性属性的静态验证，这是该领域的一项重要进展。
*   **实用性**：通过将连续动力学嵌入 Promela 的 C 代码块，该方法可以处理复杂的物理过程。

### 7. 优点
*   **理论严谨**：建立了 SIH 假设，并给出了 SSTL 与 STL 等价性的形式化证明。
*   **兼容性强**：通过转换为 $LTL_P$，能够直接利用现有的成熟模型检测工具（如 SPIN），无需从零开发验证引擎。
*   **功能全面**：同时支持安全性和活性属性的验证，而以往方法多侧重于安全性。

### 8. 不足与局限
*   **采样率依赖**：SIH 假设要求采样频率必须足够高（满足奈奎斯特率），否则可能漏掉 Tick 之间的瞬时违规事件。
*   **状态爆炸风险**：对于包含大量连续变量或极高精度要求的系统，离散化后的状态空间会迅速膨胀，导致内存溢出。
*   **手动建模成本**：目前从 SSTL 到 $LTL_P$ 的转换以及 Promela 模型的构建仍涉及较多人工操作，自动化程度有待提高。
*   **精度损失**：为了适配有限状态检测，浮点数被量化为固定精度的整数，这在极端敏感的系统中可能引入偏差。

（完）
