---
title: Coverage Games
title_zh: 覆盖博弈
authors: "Orna Kupferman, Noam Shenwald"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20398v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 覆盖者与干扰者之间的多智能体规划与覆盖博弈
tldr: 本文提出了一种名为“覆盖博弈”的新型多智能体规划框架，旨在解决系统在不完全控制智能体或面对对抗环境时，如何通过多个智能体协作完成一组目标的问题。研究探讨了覆盖者与干扰者之间的博弈机制，分析了博弈的确定性、目标分解等理论特性，并对判定胜负的计算复杂度进行了深入研究，涵盖了从单人到多智能体及固定目标数等多种场景。
selection_source: fresh_fetch
motivation: 针对多智能体系统中系统无法完全控制所有个体或需在对抗环境下完成多目标的复杂规划需求。
method: 引入覆盖者与干扰者的对抗模型，允许目标在智能体间动态分解，并研究其博弈论属性与判定算法。
result: 确立了覆盖博弈的理论基础，给出了判定胜负的紧致复杂度分析，并探讨了特殊约束下的计算特性。
conclusion: 该框架为多机器人监控、多线程系统覆盖及资源防耗尽等实际应用提供了有效的理论支撑和算法指导。
---

## 摘要
我们引入并研究了覆盖博弈（coverage games）——一种用于多智能体规划的新型框架，适用于系统运行多个智能体但对其没有完全控制权，或者与由多个智能体组成的环境进行交互的场景。该博弈在拥有一组目标的覆盖者（coverer）与干扰者（disruptor）之间进行。覆盖者操作多个智能体，这些智能体与对抗性的干扰者进行交互。如果每个目标都由至少一个智能体满足，则覆盖者获胜；否则，干扰者获胜。因此，覆盖博弈通过允许在不同智能体之间进行（可能是动态的）目标分解，扩展了传统的多目标双人博弈。它们具有广泛的应用，既包括系统作为覆盖者的场景（例如，多机器人监控、多线程系统中的覆盖），也包括系统作为干扰者的场景（例如，防止资源耗尽、确保非拥塞）。我们首先研究了覆盖博弈的理论性质，包括确定性（determinacy）以及预先在智能体之间分解目标的能力。随后，我们研究了判定覆盖者还是干扰者获胜的问题。除了对问题紧确复杂性（tight complexity）的全面分析外，我们还考虑了一些有趣的特殊情况，例如单人博弈情况以及智能体或目标数量固定的场景。

## Abstract
We introduce and study coverage games - a novel framework for multi-agent planning in settings in which a system operates several agents but does not have full control on them, or interacts with an environment that consists of several agents. The game is played between a coverer, who has a set of objectives, and a disruptor. The coverer operates several agents that interact with the adversarial disruptor. The coverer wins if every objective is satisfied by at least one agent. Otherwise, the disruptor wins.   Coverage games thus extend traditional two-player games with multiple objectives by allowing a (possibly dynamic) decomposition of the objectives among the different agents. They have many applications, both in settings where the system is the coverer (e.g., multi-robot surveillance, coverage in multi-threaded systems) and settings where it is the disruptor (e.g., prevention of resource exhaustion, ensuring non-congestion).   We first study the theoretical properties of coverage games, including determinacy, and the ability to a priori decompose the objectives among the agents. We then study the problems of deciding whether the coverer or the disruptor wins. Besides a comprehensive analysis of the tight complexity of the problems, we consider interesting special cases, such as the one-player cases and settings with a fixed number of agents or objectives.

---

## 论文详细总结（自动生成）

这是一份关于论文《Coverage Games》（覆盖博弈）的结构化深度分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：论文引入并定义了“覆盖博弈”（Coverage Games），这是一种新型的多智能体规划框架。它主要解决在**非完全控制**或**对抗环境**下，系统如何利用多个智能体协作完成一组预设目标的问题。
*   **研究动机**：传统的多目标博弈通常假设单一智能体或系统拥有完全控制权。但在现实应用（如多机器人巡逻、网络安全防御、多线程系统覆盖）中，系统往往操作多个智能体，且这些智能体需在对抗性环境（干扰者）中动态地分配和完成任务。
*   **整体含义**：该研究扩展了传统的双人多目标博弈，允许目标在智能体之间进行动态分解，并从博弈论和计算复杂度的角度填补了多智能体对抗性覆盖任务的理论空白。

### 2. 论文提出的方法论
*   **核心思想**：博弈在“覆盖者”（Coverer）和“干扰者”（Disruptor）之间展开。覆盖者操作 $k$ 个智能体，干扰者使用单一策略对抗所有智能体。覆盖者获胜的条件是：**所有目标集合 $\beta$ 中的每一个目标，都至少被其中一个智能体生成的路径所满足**。
*   **关键技术细节**：
    *   **目标类型**：重点研究了 $\omega$-正则目标中的 B¨uchi（无限次访问某集合）和 co-B¨uchi（最终停留在某集合外）目标。
    *   **目标分解（Decomposability）**：研究了覆盖者是否能预先将目标分配给特定智能体。结论是通常无法预先分解，需根据博弈进程动态调整。
    *   **分叉（Forks）概念**：定义了“分叉”顶点，即覆盖者在此处可以采取不同策略将智能体群组拆分，以覆盖不同的子目标。
*   **算法流程**：
    *   提出了一个基于递归的 PSPACE 算法来判定覆盖者胜负。该算法通过寻找“分叉”点，将原博弈递归地分解为规模更小的子博弈。
    *   对于干扰者，通过“符号化描述策略”和“公平性要求（Fairness Requirement）”来判定其是否存在干扰策略。

### 3. 实验设计
*   **研究性质**：本论文属于**理论计算科学与博弈论**研究，因此没有传统意义上的“数据集”或“跑分实验”。
*   **验证方式（逻辑实验）**：
    *   **复杂度分析**：通过将经典的计算难题（如 3SAT、QBF、顶点覆盖问题）归约（Reduction）到覆盖博弈的各种变体中，来证明其计算复杂度的上下界。
    *   **案例分析**：设计了特定的博弈图（如 Figure 1, 3, 4）来证明覆盖博弈的非确定性（即可能双方都没有必胜策略）以及目标不可预先分解的特性。
*   **对比对象**：对比了传统的单人博弈、标准双人多目标博弈以及人口博弈（Population Games）。

### 4. 资源与算力
*   **说明**：文中**未提及**任何关于 GPU、服务器集群或训练时长的信息。
*   **原因**：该研究侧重于数学证明和算法复杂度的理论推导，不涉及深度学习训练或大规模数值模拟。

### 5. 实验数量与充分性
*   **充分性评价**：虽然没有数值实验，但其理论分析非常全面。
*   **覆盖范围**：
    *   分析了 B¨uchi 和 co-B¨uchi 两种目标类型。
    *   涵盖了单人（V1 或 V2 拥有所有节点）到双人的完整场景。
    *   进行了参数化复杂度分析：分别讨论了固定智能体数量 $k$、固定目标数量 $|\beta|$ 以及一般情况下的复杂度。
*   **客观性**：通过严谨的数学归约证明了所有复杂度类（P, NP, PSPACE, $\Sigma^P_2$）的紧致性（Tightness），逻辑闭环完整。

### 6. 论文的主要结论与发现
*   **非确定性**：覆盖博弈不满足传统双人博弈的确定性，即存在双方均无必胜策略的情况。
*   **复杂度定级**：
    *   **覆盖问题**：一般情况下是 **PSPACE-complete**。如果固定智能体数量 $k$，降为 **NP-complete**；如果固定目标数 $|\beta|$，则为 **PTIME**。
    *   **干扰问题**：一般情况下是 **$\Sigma^P_2$-complete**。
*   **目标分解**：证明了目标不能总是被预先分配，覆盖者的优势往往来自于在博弈过程中根据干扰者的行动动态“分叉”智能体。
*   **单人情况**：当覆盖者拥有所有控制权时，问题等同于多机器人路径规划覆盖，复杂度为 **NP-complete**。

### 7. 优点
*   **理论创新**：提出了一个非常符合现实多智能体协作逻辑的博弈模型，特别是“单一干扰者对抗多个独立智能体”的设定。
*   **分析深入**：不仅给出了总体的复杂度，还通过参数化分析揭示了智能体数量和目标数量对计算难度的具体影响。
*   **应用潜力**：为多机器人系统、网络防御和并发系统验证提供了坚实的理论基础。

### 8. 不足与局限
*   **策略独立性限制**：目前模型假设智能体之间没有通信，策略是相互独立的（虽然都针对同一个干扰者），这限制了其在强协作（如实时信息共享）系统中的应用。
*   **环境假设**：干扰者被假设为对所有智能体使用统一策略，这在某些场景下（如干扰者可以针对不同智能体采取不同行动）可能过于简化。
*   **目标类型**：目前仅限于 B¨uchi 和 co-B¨uchi 目标，尚未扩展到更复杂的 LTL 公式或带有概率的随机博弈场景。

（完）
