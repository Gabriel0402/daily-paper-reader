---
title: "DB SwinT: A Dual-Branch Swin Transformer Network for Road Extraction in Optical Remote Sensing Imagery"
title_zh: DB SwinT：一种用于光学遥感图像道路提取的双分支 Swin Transformer 网络
authors: "Zongyang He, Xiangli Yang, Xian Gao, Zhiguo Wang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24005v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用Swin Transformer在光学遥感图像中进行道路提取
tldr: 针对高分辨率遥感图像中道路易受遮挡导致提取不连续的问题，本文提出双分支Swin Transformer网络（DB SwinT）。该网络结合了Swin Transformer的长程建模能力与U-Net的多尺度融合策略，通过局部和全局双分支编码器分别捕捉细节与语义上下文，并利用注意力特征融合模块提升遮挡区域的提取精度，在主流数据集上表现优异。
selection_source: fresh_fetch
motivation: 解决复杂环境下道路因树木、建筑遮挡而导致的结构破碎和提取精度低的问题。
method: 提出一种结合Swin Transformer与U-Net的双分支架构，利用局部分支恢复细节，全局分支捕捉上下文，并通过注意力机制进行特征融合。
result: "在Massachusetts和DeepGlobe数据集上分别实现了79.35%和74.84%的IoU得分，显著提升了道路提取效果。"
conclusion: DB SwinT通过双分支特征互补有效解决了道路遮挡问题，是光学遥感图像道路提取的有效方案。
---

## 摘要
随着光学遥感图像空间分辨率的不断提高，准确的道路提取在城市规划、交通监测和灾害管理等应用中变得越来越重要。然而，在复杂的城乡环境中，道路提取仍然具有挑战性，因为道路经常被树木、建筑物和其他物体遮挡，导致结构破碎并降低了提取精度。为了解决这一问题，本文提出了一种用于道路提取的双分支 Swin Transformer 网络（DB SwinT）。该框架结合了 Swin Transformer 的长程依赖建模能力与 U-Net 的多尺度特征融合策略，并采用双分支编码器来学习互补的局部和全局表示。具体而言，局部分支侧重于恢复遮挡区域的精细结构细节，而全局分支则捕捉更广泛的语义上下文，以保持道路网络的整体连续性。此外，引入了注意力特征融合（AFF）模块，以自适应地融合来自两个分支的特征，进一步增强了遮挡道路段的表示。在 Massachusetts 和 DeepGlobe 数据集上的实验结果表明，DB SwinT 的交并比（IoU）得分分别达到 79.35% 和 74.84%，证明了其在光学遥感图像道路提取方面的有效性。

## Abstract
With the continuous improvement in the spatial resolution of optical remote sensing imagery, accurate road extraction has become increasingly important for applications such as urban planning, traffic monitoring, and disaster management. However, road extraction in complex urban and rural environments remains challenging, as roads are often occluded by trees, buildings, and other objects, leading to fragmented structures and reduced extraction accuracy. To address this problem, this paper proposes a Dual-Branch Swin Transformer network (DB SwinT) for road extraction. The proposed framework combines the long-range dependency modeling capability of the Swin Transformer with the multi-scale feature fusion strategy of U-Net, and employs a dual-branch encoder to learn complementary local and global representations. Specifically, the local branch focuses on recovering fine structural details in occluded areas, while the global branch captures broader semantic context to preserve the overall continuity of road networks. In addition, an Attentional Feature Fusion (AFF) module is introduced to adaptively fuse features from the two branches, further enhancing the representation of occluded road segments. Experimental results on the Massachusetts and DeepGlobe datasets show that DB SwinT achieves Intersection over Union (IoU) scores of 79.35\% and 74.84\%, respectively, demonstrating its effectiveness for road extraction from optical remote sensing imagery.

---

## 论文详细总结（自动生成）

以下是对论文《DB SwinT: A Dual-Branch Swin Transformer Network for Road Extraction in Optical Remote Sensing Imagery》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在高分辨率光学遥感图像中，道路提取常面临复杂背景的干扰，尤其是树木、建筑物和阴影的**遮挡**，导致提取的道路结构破碎、不连续，且边缘精度低。
*   **研究背景**：传统的 CNN 方法受限于局部感受野，难以捕捉长程依赖关系；而早期的 Transformer 方法虽然擅长全局建模，但在处理精细局部细节（如窄路、遮挡边缘）时表现不足。本文旨在结合两者的优势，解决复杂环境下的道路连通性问题。

### 2. 核心方法论
论文提出了 **DB SwinT** 框架，其核心思想是通过双分支结构平衡局部细节与全局语义：
*   **双分支编码器（Dual-Branch Encoder）**：
    *   **局部分支**：采用较小的 Patch Size ($s=4$)，专注于恢复遮挡区域的精细结构和边缘细节。
    *   **全局分支**：采用较大的 Patch Size ($s=8$)，旨在捕捉更广阔的语义上下文，确保道路网络的整体拓扑连通性。
*   **Swin Transformer 块**：利用窗口自注意力（W-MSA）和移动窗口自注意力（SW-MSA）机制，在降低计算复杂度的同时实现跨窗口的信息交互。
*   **注意力特征融合（AFF）模块**：引入多尺度通道注意力机制，通过自适应权重 $M$ 动态融合两个分支的特征。公式逻辑为：$Z = M \otimes X + (1-M) \otimes Y$，从而实现局部与全局特征的互补。
*   **U 型对称架构**：结合 U-Net 的跳跃连接（Skip Connections），将编码器的多尺度特征直接传递给解码器，减少下采样过程中的空间信息损失。

### 3. 实验设计
*   **数据集**：
    *   **DeepGlobe**：涵盖农村、热带雨林等复杂地形，道路宽度不一且植被遮挡严重。
    *   **Massachusetts Roads**：涵盖城市和郊区，建筑密集，阴影和交叉口复杂。
*   **Benchmark 与对比方法**：对比了 LinkNet50、U-Net、GAMSNet、纯 SwinT 以及单分支的 SwinT+U-Net。
*   **评价指标**：Precision（精确率）、Recall（召回率）、F1-score 和 IoU（交并比）。

### 4. 资源与算力
*   **硬件环境**：Intel Core i5-12400F CPU，**NVIDIA GeForce RTX 3060 GPU**（单卡）。
*   **软件框架**：PyTorch 1.11。
*   **训练细节**：
    *   优化器：SGD（动量 0.9）。
    *   学习率：初始 $2 \times 10^{-4}$，每 20 个 epoch 衰减为原来的 20%。
    *   Batch Size：4。
    *   训练轮数：100 Epochs。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了两组主流数据集的对比实验，并开展了详尽的**消融实验**。
*   **消融实验设计**：系统对比了单分支、双分支、三分支架构在不同 Patch Size 组合（$s=1, 4, 8, 12$）下的表现。
*   **充分性评价**：实验设计较为客观、公平。通过可视化结果展示了模型在处理遮挡和复杂交叉口时的优越性。特别是对“分支数量”的讨论，证明了双分支是性能与效率的最佳平衡点，实验论证充分。

### 6. 主要结论与发现
*   **性能提升**：DB SwinT 在 DeepGlobe 上达到 **79.35% IoU**，在 Massachusetts 上达到 **74.84% IoU**，显著优于对比的 CNN 和单分支 Transformer 模型。
*   **双分支的必要性**：局部分支能有效减少漏检（提升 Recall），全局分支能减少误检（提升 Precision），两者结合显著增强了道路的结构完整性。
*   **边际递减效应**：增加到三分支（TB SwinT）不仅增加了计算开销，反而因特征冗余和对齐难度增加导致性能下降。

### 7. 论文优点
*   **结构设计合理**：巧妙地利用不同 Patch Size 的 Swin Transformer 模拟多尺度特征提取，比单纯堆叠卷积层更具解释性。
*   **解决痛点明确**：针对“遮挡导致断裂”这一核心痛点，通过 AFF 模块实现了有效的特征补偿。
*   **讨论深入**：不仅展示了结果，还深入探讨了分支数量对模型性能的影响，为后续研究提供了参考。

### 8. 不足与局限
*   **计算复杂度**：虽然 Swin Transformer 降低了复杂度，但双分支结构相比轻量化 CNN 仍有较大的参数量和计算开销，可能限制其在嵌入式或实时处理场景的应用。
*   **泛化性验证**：实验主要集中在两个标准数据集，对于跨传感器（如不同卫星、不同光照条件）的域适应能力（Domain Adaptation）讨论较少。
*   **超参数敏感性**：Patch Size 的选择对结果影响较大，针对不同分辨率的图像可能需要重新调整。

（完）
