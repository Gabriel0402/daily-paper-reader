---
title: "DCP-CLIP:A Coarse-to-Fine Framework for Open-Vocabulary Semantic Segmentation with Dual Interaction"
title_zh: DCP-CLIP：一种基于双重交互的由粗到精开放词汇语义分割框架
authors: "Jing Wang, Huimin Shi, Quan Zhou, Qibo Liu, Suofei Zhang, Huimin Lu"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13951v1"
tags: ["query:mmseg"]
score: 10.0
evidence: 使用视觉语言基础模型进行开放词汇语义分割
tldr: 针对开放词汇语义分割（OVSS）中跨模态交互不足及海量类别带来的计算开销问题，本文提出DCP-CLIP框架。该框架采用由粗到精的策略，通过动态构建类别相关的文本特征，并显式建模图像空间特征与文本语义间的双向交互，实现了高效且精准的分割。实验证明，该方法在多个基准测试中均取得了领先的准确率和效率。
selection_source: fresh_fetch
motivation: 旨在解决现有OVSS方法中跨模态通信不充分以及处理大规模类别时计算成本过高的问题。
method: 提出一种由粗到精的框架，通过动态生成文本引导并结合空间-语义双向交互来优化分割掩码和类别预测。
result: 在多个OVSS基准测试上，DCP-CLIP在保持高精度的同时显著提升了推理效率。
conclusion: 该研究证明了动态文本构建与双向交互机制在提升开放词汇语义分割性能方面的有效性。
---

## 摘要
近年来，利用视觉-语言基础模型进行开放词汇语义分割（OVSS）取得了显著进展，但仍面临以下基本挑战：（1）文本与视觉空间之间的跨模态通信不足，以及（2）与海量类别交互带来的巨大计算开销。为了解决这些问题，本文提出了一种名为 DCP-CLIP 的新型由粗到精的 OVSS 框架。与以往主要依赖预设类别内容和 CLIP 固有的空间-类别交互能力的研究不同，我们动态构建与类别相关的文本特征，并显式建模空间图像特征与文本类别语义之间的双重交互。具体而言，我们首先利用 CLIP 的开放词汇识别能力来识别与图像上下文相关的语义类别，并据此动态生成相应的文本特征作为初始文本引导。随后，我们通过将文本引导中的语义信息跨模态整合到视觉表示中来进行粗分割，并通过整合来自编码器的空间丰富特征来恢复细粒度细节并提高空间分辨率，从而实现精细分割。最后，我们利用分割端的空间信息来优化每个掩码（mask）的类别预测，从而实现更精确的语义标注。在多个 OVSS 基准测试上的实验表明，DCP-CLIP 在准确性和效率方面均优于现有方法。

## Abstract
The recent years have witnessed the remarkable development for open-vocabulary semantic segmentation (OVSS) using visual-language foundation models, yet still suffer from following fundamental challenges: (1) insufficient cross-modal communications between textual and visual spaces, and (2) significant computational costs from the interactions with massive number of categories. To address these issues, this paper describes a novel coarse-to-fine framework, called DCP-CLIP, for OVSS. Unlike prior efforts that mainly relied on pre-established category content and the inherent spatial-class interaction capability of CLIP, we dynamic constructing category-relevant textual features and explicitly models dual interactions between spatial image features and textual class semantics. Specifically, we first leverage CLIP's open-vocabulary recognition capability to identify semantic categories relevant to the image context, upon which we dynamically generate corresponding textual features to serve as initial textual guidance. Subsequently, we conduct a coarse segmentation by cross-modally integrating semantic information from textual guidance into the visual representations and achieve refined segmentation by integrating spatially enriched features from the encoder to recover fine-grained details and enhance spatial resolution. In final, we leverage spatial information from the segmentation side to refine category predictions for each mask, facilitating more precise semantic labeling. Experiments on multiple OVSS benchmarks demonstrate that DCP-CLIP outperforms existing methods by delivering both higher accuracy and greater efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《DCP-CLIP: A Coarse-to-Fine Framework for Open-Vocabulary Semantic Segmentation with Dual Interaction》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：开放词汇语义分割（OVSS）旨在识别和分割图像中任意文本定义的类别。虽然基于 CLIP 等视觉-语言模型的方法取得了进展，但仍面临两大挑战。
*   **核心痛点**：
    1.  **跨模态通信不足**：文本语义空间与视觉空间之间的交互不够深入，导致像素级对齐不够精准。
    2.  **计算开销巨大**：在处理包含成百上千个类别的开放词汇集时，全量遍历所有类别会导致推理速度极慢且内存消耗高。
*   **研究目标**：提出一个由粗到精的框架 **DCP-CLIP**，通过动态类别选择和双重交互机制，在提升分割精度的同时显著降低计算成本。

### 2. 方法论
核心思想是构建一个“筛选-对齐-增强-验证”的闭环流程：
*   **动态类别选择 (DCS)**：不再遍历所有类别，而是利用图像级和 Patch 级特征预先筛选出与当前图像相关的候选类别。通过抑制主导类别信息来挖掘潜在的次要类别，从而构建一个精简的动态类别集。
*   **文本引导对齐 (TGA)**：
    *   将筛选出的类别嵌入与可学习 Prompt 结合，生成“文本引导”特征。
    *   利用 **Cross-Attention** 将高层文本语义注入视觉表示。
    *   利用 **Self-Attention** 建模成本体积（Cost Volume）内的上下文依赖，增强特征表达。
*   **空间增强解码器 (SED)**：
    *   采用 **Swin Transformer** 块捕捉局部空间关系。
    *   使用类 U-Net 的上采样结构，融合来自编码器的多尺度浅层特征和 CLIP 的注意力图，以恢复细粒度细节和空间分辨率。
*   **标签验证 (TV)**：在推理阶段，利用预测掩码的空间分布进行二次校验。对于预测为“未见过”类别的区域，重新计算其区域特征与文本嵌入的相似度，修正可能的分类错误。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：COCO-Stuff（包含 171 个类别）。
    *   **测试集 (Benchmarks)**：ADE20K-150、ADE20K-847（长尾分布）、Pascal Context-59、Pascal Context-459、Pascal VOC-20。
*   **对比方法**：涵盖了主流的 OVSS 方法，如 SAN、CAT-Seg、SED、OVSeg、FC-CLIP、ODISE 等。
*   **评价指标**：平均交并比 (mIoU)。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA RTX 3090 GPU**。
*   **训练细节**：
    *   迭代次数：80K iterations。
    *   Batch Size：4。
    *   优化器：AdamW。
    *   **时长**：训练总时长约为 **496.4 分钟**（约 8.3 小时），显著优于对比方法 SED（1302.4 分钟）。
*   **推理效率**：在单张 3090 GPU 上，推理时间约为 0.20s，GFLOPs 为 616.2。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同规模和难度的基准数据集上进行了全面测试，类别数从 20 跨越到 847。
*   **消融实验**：非常详尽，包括：
    1.  各组件（DCS, TGA, SED, TV）的有效性验证。
    2.  TGA 中不同注意力分支（CA/SA）的影响。
    3.  SED 中解码器层数、特征融合策略的对比。
    4.  DCS 中置信度阈值 $\theta$ 对精度与效率平衡的影响。
*   **客观性**：通过对比参数量、计算量（GFLOPs）、训练/推理时间以及 mIoU，多维度证明了方法的优越性，实验设计科学且公平。

### 6. 主要结论与发现
*   **性能领先**：DCP-CLIP 在所有 Benchmark 上均达到了 SOTA 性能。例如在 ADE20K-150 上，使用 ViT-L/14 后 mIoU 达到 38.1%，超过了之前的最佳方法。
*   **效率突破**：动态类别选择机制（DCS）在不损失精度（甚至因减少噪声而提升精度）的前提下，将推理速度提升了数倍，内存占用大幅降低。
*   **双向交互有效性**：证明了“文本引导视觉分割”和“视觉空间信息修正标签”的双向机制能有效弥合模态鸿沟。

### 7. 优点
*   **高效性**：解决了 OVSS 领域长期存在的“类别爆炸”导致的计算瓶颈问题。
*   **由粗到精的逻辑**：从类别筛选到特征对齐，再到空间恢复和标签修正，每一步都有明确的物理含义和性能贡献。
*   **泛化能力强**：在未见过的类别（Unseen classes）和长尾数据集（如 ADE20K-847）上表现出色。

### 8. 不足与局限
*   **超参数依赖**：DCS 模块中的阈值 $\theta$ 需要根据具体应用场景进行微调，以平衡速度和召回率。
*   **基础模型限制**：性能仍然受限于预训练 CLIP 模型的质量，若 CLIP 本身对某些生僻概念缺乏理解，该框架也难以完全修复。
*   **复杂场景挑战**：虽然引入了标签验证，但在极度遮挡或光照剧烈的复杂场景下，像素级的精确分割仍有提升空间。

（完）
