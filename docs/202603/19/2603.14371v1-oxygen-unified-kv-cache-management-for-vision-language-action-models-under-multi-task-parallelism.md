---
title: "OxyGen: Unified KV Cache Management for Vision-Language-Action Models under Multi-Task Parallelism"
title_zh: OxyGen：多任务并行下视觉-语言-动作模型的统一 KV 缓存管理
authors: "Xiangyu Li, Huaizhi Tang, Xin Ding, Weijun Wang, Ting Cao, Yunxin Liu"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14371v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉-语言-动作模型的KV缓存管理
tldr: 具身智能体常需并行处理对话、操作等异构任务，但现有推理系统因KV缓存管理孤立导致计算冗余。本文提出OxyGen，一种统一的KV缓存管理范式，将KV缓存视为跨任务和跨时间的共享资源。通过跨任务KV共享消除冗余预填充，并利用跨帧连续批处理解耦语言解码与动作生成。在π0.5模型上的实验表明，该方法显著提升了推理速度和任务频率，且不损失动作质量。
selection_source: fresh_fetch
motivation: 现有的视觉-语言-动作模型在多任务并行推理时，因KV缓存管理不统一导致严重的计算冗余和资源竞争。
method: 提出统一KV缓存管理范式，通过跨任务KV共享和跨帧连续批处理优化推理效率。
result: OxyGen实现了最高3.7倍的加速，同时支持超过200 tokens/s的语言吞吐量和70 Hz的动作频率。
conclusion: 统一KV缓存管理是实现具身智能体高效、实时多任务并行推理的关键技术路径。
---

## 摘要
具身智能体日益需要根据共享观测结果，在不同的时间约束下并行执行多项任务，如操作、对话和记忆构建。近期的混合 Transformer (MoT) 视觉-语言-动作模型 (VLA) 在架构上支持此类异构输出，但现有的推理系统由于冗余计算和资源竞争，无法在端侧部署中实现高效的多任务并行。我们发现孤立的 KV 缓存管理是其根本原因。为解决这一问题，我们提出了统一 KV 缓存管理，这是一种将 KV 缓存视为跨任务和跨时间的核心共享资源的推理范式。这种抽象实现了两项关键优化：跨任务 KV 共享消除了共享观测的冗余预填充，而跨帧连续批处理则将变长语言解码与跨控制周期的固定速率动作生成解耦。我们针对最流行的 MoT VLA 模型 $π_{0.5}$ 实现了该范式，并在代表性的机器人配置下进行了评估。OxyGen 相比孤立执行实现了高达 3.7 倍的加速，在不降低动作质量的情况下，同时提供了超过 200 tokens/s 的语言吞吐量和 70 Hz 的动作频率。

## Abstract
Embodied AI agents increasingly require parallel execution of multiple tasks, such as manipulation, conversation, and memory construction, from shared observations under distinct time constraints. Recent Mixture-of-Transformers (MoT) Vision-Language-Action Models (VLAs) architecturally support such heterogeneous outputs, yet existing inference systems fail to achieve efficient multi-task parallelism for on-device deployment due to redundant computation and resource contention. We identify isolated KV cache management as the root cause. To address this, we propose unified KV cache management, an inference paradigm that treats KV cache as a first-class shared resource across tasks and over time. This abstraction enables two key optimizations: cross-task KV sharing eliminates redundant prefill of shared observations, while cross-frame continuous batching decouples variable-length language decoding from fixed-rate action generation across control cycles. We implement this paradigm for $π_{0.5}$, the most popular MoT VLA, and evaluate under representative robotic configurations. OxyGen achieves up to 3.7$\times$ speedup over isolated execution, delivering over 200 tokens/s language throughput and 70 Hz action frequency simultaneously without action quality degradation.

---

## 论文详细总结（自动生成）

以下是对论文《OxyGen: Unified KV Cache Management for Vision-Language-Action Models under Multi-Task Parallelism》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：具身智能体（如家用机器人）需要同时处理多种异构任务，例如在执行抓取动作的同时进行语音对话或构建环境记忆。
*   **核心挑战**：虽然混合 Transformer (MoT) 架构的视觉-语言-动作模型（VLA，如 $\pi_{0.5}$）在模型层面支持多模态输出，但现有的推理系统采用“孤立执行”范式。
*   **痛点分析**：
    1.  **计算冗余**：同一帧的视觉观测被多次重复编码（Prefill），为不同任务生成相同的 KV 缓存。
    2.  **资源竞争**：动作生成（高频、固定速率）与语言解码（变长、跨帧）在硬件资源上相互阻塞，导致动作频率下降或语言吞吐量低。
*   **研究目的**：提出一种高效的推理范式，将 KV 缓存视为跨任务、跨时间的共享资源，以实现高效的多任务并行。

### 2. 方法论
OxyGen 提出了**统一 KV 缓存管理（Unified KV Cache Management）**范式，核心包含以下技术细节：
*   **跨任务 KV 共享（Cross-task KV Sharing）**：在每一帧中，VLM 主干网络仅对观测结果进行一次预填充（Prefill），生成的 KV 缓存由动作专家和语言专家共享，消除了重复计算。
*   **跨帧连续批处理（Cross-frame Continuous Batching）**：
    *   将语言解码从每帧的控制循环中解耦。
    *   利用“可恢复生成状态”记录每个请求的 KV 缓存、已生成 Token 和结束标志。
    *   将当前帧的新请求与前几帧未完成的请求进行批处理（Batching），在单次前向传播中同时推进多个语言任务，提高 GPU 利用率。
*   **统一 KV 管理器**：提供存储（Store）、检索（Retrieve）、更新（Update）和批处理/拆分（Batch/UnBatch）接口，协调不同任务的生命周期。

### 3. 实验设计
*   **目标模型**：目前最流行的 MoT VLA 模型 $\pi_{0.5}$。
*   **数据集/场景**：
    *   **LIBERO**：用于评估任务成功率和推理速度。
    *   **DROID** 与 **ALOHA**：用于评估不同机器人配置下的推理性能。
*   **基准方法（Benchmarks）**：
    *   **Baseline (Sequential)**：官方 OpenPi 系统的顺序孤立执行。
    *   **Parallel (MPS)**：利用 NVIDIA CUDA MPS 实现的简单多进程并行执行。
*   **评估指标**：动作频率 (Hz)、语言吞吐量 (tokens/s)、任务成功率、内存占用、功耗及单位请求能耗。

### 4. 资源与算力
*   **硬件平台**：单张 **NVIDIA RTX 4090 GPU**。这是典型的端侧/机器人载板推理硬件。
*   **软件环境**：基于 OpenPi 推理框架实现。
*   **训练时长**：文中未提及模型训练时长，因为该研究专注于**推理阶段**的系统优化，使用的是已发布的预训练权重（如 $\pi_{0.5}$-LIBERO）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流机器人基准测试上进行了详尽的性能对比。
    *   进行了**消融实验**，分别验证了“跨任务共享”和“连续批处理”的增益。
    *   测试了多种**工作负载模式**，包括均匀到达、泊松到达（Poisson arrivals）以及随机长度请求。
*   **充分性评价**：实验设计较为全面，不仅关注速度提升，还通过 LIBERO 测试集验证了优化后的系统**不会降低动作质量（成功率）**。对比实验考虑了硬件层面的并行（MPS），保证了对比的公平性。

### 6. 主要结论与发现
*   **显著加速**：OxyGen 相比基准系统实现了最高 **3.7 倍** 的加速。
*   **高性能并行**：在保持 **70 Hz** 高频动作生成的同事，实现了超过 **200 tokens/s** 的语言吞吐量。
*   **能效提升**：由于减少了对 VLM 权重的重复访问和冗余计算，单位请求能耗降低了高达 **78%**，平均功耗降低了约 **47%**。
*   **内存友好**：相比于简单并行（内存翻倍），OxyGen 仅增加了约 **15%** 的内存开销用于存储 KV 缓存状态。

### 7. 优点与亮点
*   **系统级创新**：首次将 LLM 服务中的连续批处理思想引入到具身智能 VLA 模型中，并解决了动作与语言任务时间约束不对称的问题。
*   **实用性强**：针对主流开源模型和消费级显卡优化，代码已开源，具有很强的工程落地价值。
*   **优雅的抽象**：通过统一 KV 管理器解耦了任务逻辑与计算调度，使系统能够灵活处理各种任务到达模式。

### 8. 不足与局限
*   **架构依赖**：该方法主要针对 MoT（混合 Transformer）架构的 VLA 模型，对于非共享主干网络的多模型组合方案适用性有限。
*   **硬件局限**：实验仅限于单 GPU 场景，未探讨在多显卡或更弱算力平台（如 Jetson Orin）上的表现。
*   **长序列压力**：虽然管理了 KV 缓存，但随着并行任务数量增加或上下文变长，显存压力仍可能成为瓶颈，文中未深入探讨极端长序列下的缓存驱逐策略。

（完）
