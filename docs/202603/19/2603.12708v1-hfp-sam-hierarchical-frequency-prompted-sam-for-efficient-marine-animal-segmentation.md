---
title: "HFP-SAM: Hierarchical Frequency Prompted SAM for Efficient Marine Animal Segmentation"
title_zh: HFP-SAM：用于高效海洋动物分割的分层频率提示 SAM
authors: "Pingping Zhang, Tianyu Yan, Yuhao Wang, Yang Liu, Tongdan Tang, Yili Ma, Long Lv, Feng Tian, Weibing Sun, and Huchuan Lu"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12708v1"
tags: ["query:mmseg"]
score: 10.0
evidence: SAM模型在海洋动物分割中的应用
tldr: 针对海洋动物分割（MAS）中复杂环境建模难以及SAM模型缺乏细粒度频率信息的问题，本文提出了HFP-SAM框架。该框架通过频率引导适配器（FGA）将频率先验注入冻结的SAM，利用频率感知点选择（FPS）生成点提示以优化预测，并引入全视角Mamba（FVM）增强上下文提取。实验证明，该方法在四个公开数据集上均达到了领先性能，有效提升了海洋生物分割的精度与效率。
selection_source: fresh_fetch
motivation: 现有的海洋动物分割方法难以处理长距离建模，且SAM模型在感知细粒度细节和频率信息方面存在不足。
method: 提出HFP-SAM框架，结合频率引导适配器、频率感知点选择机制以及全视角Mamba模块，实现高效的特征增强与上下文建模。
result: 在四个主流海洋动物分割数据集上的实验结果表明，该方法在性能指标上显著优于现有的深度学习模型。
conclusion: HFP-SAM通过引入频率域信息和Mamba架构，成功提升了SAM在复杂海洋环境下的分割能力，为高效海洋生物识别提供了新方案。
---

## 摘要
海洋动物分割（MAS）旨在从复杂的海洋环境中识别并分割海洋动物。以往大多数基于深度学习的 MAS 方法在长距离建模问题上存在困难。最近，Segment Anything Model (SAM) 在通用图像分割中受到了广泛关注。然而，它缺乏对细粒度细节和频率信息的感知能力。为此，我们提出了一种名为分层频率提示 SAM（HFP-SAM）的新型学习框架，用于高性能 MAS。首先，我们设计了一个频率引导适配器（FGA），通过频域先验掩码将海洋场景信息高效地注入冻结的 SAM 主干网络中。此外，我们引入了频率感知点选择（FPS），通过频率分析生成高亮区域。这些区域与 SAM 的粗略预测相结合，生成点提示并集成到 SAM 的解码器中以进行精细预测。最后，为了获得全面的分割掩码，我们引入了全视图 Mamba（FVM），以线性计算复杂度高效地提取空间和通道上下文信息。在四个公共数据集上的广泛实验证明了我们方法的优越性能。源代码可在 https://github.com/Drchip61/TIP-HFP-SAM 公开获取。

## Abstract
Marine Animal Segmentation (MAS) aims at identifying and segmenting marine animals from complex marine environments. Most of previous deep learning-based MAS methods struggle with the long-distance modeling issue. Recently, Segment Anything Model (SAM) has gained popularity in general image segmentation. However, it lacks of perceiving fine-grained details and frequency information. To this end, we propose a novel learning framework, named Hierarchical Frequency Prompted SAM (HFP-SAM) for high-performance MAS. First, we design a Frequency Guided Adapter (FGA) to efficiently inject marine scene information into the frozen SAM backbone through frequency domain prior masks. Additionally, we introduce a Frequency-aware Point Selection (FPS) to generate highlighted regions through frequency analysis. These regions are combined with the coarse predictions of SAM to generate point prompts and integrate into SAM's decoder for fine predictions. Finally, to obtain comprehensive segmentation masks, we introduce a Full-View Mamba (FVM) to efficiently extract spatial and channel contextual information with linear computational complexity. Extensive experiments on four public datasets demonstrate the superior performance of our approach. The source code is publicly available at https://github.com/Drchip61/TIP-HFP-SAM.

---

## 论文详细总结（自动生成）

### 论文总结：HFP-SAM —— 用于高效海洋动物分割的分层频率提示 SAM

#### 1. 核心问题与研究动机
*   **核心问题**：海洋动物分割（MAS）面临能见度低、光照多变、背景复杂（如伪装）以及水中悬浮颗粒物干扰等挑战。
*   **研究动机**：
    *   传统的 CNN 方法受限于局部感受野，难以建模长距离依赖。
    *   Transformer 虽然具备全局建模能力，但在 MAS 这种小规模数据集上难以充分训练。
    *   现有的通用大模型（如 SAM）在特定领域（如水下环境）表现不佳，且缺乏对细粒度细节和频域信息的感知。
    *   SAM 的提示（Prompt）设计（如简单的点或框）在噪声严重的海洋场景中不够鲁棒，且其解码器容易丢失细节。

#### 2. 方法论
HFP-SAM 框架由三个核心组件组成，旨在将频域先验与 SAM 的空间能力结合：
*   **频率引导适配器 (FGA)**：
    *   利用**离散哈尔小波变换 (DHWT)** 将图像分解为高低频分量。
    *   提取高频图（包含边缘和细节），通过滑动窗口选择响应值最高的区域作为先验掩码。
    *   将该频域先验注入冻结的 SAM 主干网络中，引导模型关注关键的结构信息，同时抑制背景噪声。
*   **频率感知点选择 (FPS)**：
    *   **核心思想**：无需额外的提示生成网络，直接结合频域图和 SAM 的初步（粗略）预测掩码。
    *   **流程**：在频率响应高的窗口内，根据粗略掩码的概率阈值筛选出正样本点和负样本点，作为精细化阶段的点提示（Point Prompts）。
*   **全视角 Mamba (FVM)**：
    *   在解码器阶段引入 **状态空间模型 (SSM)**。
    *   **空间 SSM**：沿四个方向扫描以捕捉长程空间依赖。
    *   **通道 SSM**：通过双向扫描建模通道间的相关性。
    *   **优势**：在保持线性计算复杂度的同时，实现了比自注意力机制更高效的全局上下文聚合。

#### 3. 实验设计
*   **数据集**：使用了四个公开的 MAS 数据集：**MAS3K**（最大规模）、**RMAS**、**UFO120** 和 **RUWI**。
*   **Benchmark 与对比方法**：
    *   **CNN 架构**：UNet++、BASNet、PFNet、MASNet 等。
    *   **Transformer 架构**：SETR、TransUNet、H2Former 等。
    *   **SAM 变体**：SAM-Adapter、Med-SAM、MAS-SAM、Dual-SAM 以及最新的 **SAM 2**。
*   **评价指标**：mIoU、$S_\alpha$（结构相似度）、$F_w^\beta$（加权 F 分数）、$mE_\phi$（增强对齐度）和 MAE（平均绝对误差）。

#### 4. 资源与算力
*   **硬件**：使用了一台 **NVIDIA RTX 3090 GPU**。
*   **软件/参数**：基于 PyTorch 实现，使用 AdamW 优化器，初始学习率 0.001，训练 50 个 epoch，batch size 为 6。
*   **效率**：文中提供了详细的推理延迟（ms/frame）和显存占用（Peak Mem）对比，证明了 HFP-SAM 在增加少量开销的情况下显著提升了性能。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个 MAS 数据集上进行了全面的性能对比。
    *   进行了详尽的**消融实验**，验证了 FGA、FPS、FVM 各组件的有效性。
    *   针对 FPS 的超参数（窗口大小、点数量、阈值）进行了敏感性分析。
    *   进行了**鲁棒性测试**（添加高斯和斑点噪声）以及**零样本（Zero-shot）泛化实验**（在伪装目标检测、息肉分割、显著性检测任务上测试）。
*   **评价**：实验设计非常充分，涵盖了从性能、效率、鲁棒性到泛化性的全方位评估，对比基准涵盖了从经典到最前沿的模型，具有很高的客观性和说服力。

#### 6. 主要结论
*   HFP-SAM 在所有四个 MAS 数据集上均达到了 **SOTA（当前最佳）** 性能。
*   相比于原始 SAM，mIoU 提升显著（在 MAS3K 上提升约 23%）；相比于之前的 SAM 适配方法（如 Dual-SAM），也有 1-5% 的进一步提升。
*   频域信息的引入能有效帮助模型在浑浊、复杂的海洋背景中精准定位目标边缘。
*   Mamba 架构在 MAS 任务的解码阶段比传统的卷积或 Transformer 解码器更具优势。

#### 7. 优点与亮点
*   **频域融合**：巧妙地将小波变换引入 SAM 适配过程，解决了水下图像高频噪声干扰的问题。
*   **自动提示生成**：FPS 机制实现了无需人工干预的高质量点提示生成，且计算开销极低。
*   **全视角建模**：FVM 模块同时考虑了空间和通道维度的长程依赖，增强了特征融合的深度。
*   **泛化性强**：模型不仅在海洋动物分割上表现优异，在医疗影像和自然场景分割中也展现了极强的迁移能力。

#### 8. 不足与局限
*   **误差累积风险**：FPS 依赖于第一阶段生成的“粗略掩码”。如果粗略预测完全遗漏了目标（如极小的鱼类），FPS 生成的点提示可能会出错，导致最终分割失败。
*   **超参数依赖**：虽然实验证明了鲁棒性，但 FPS 中的窗口大小和点选择数量在不同分辨率的图像下可能仍需微调以达到最优。
*   **应用限制**：目前主要针对静态图像分割，对于视频流中的实时动态海洋动物追踪与分割尚需进一步验证。

（完）
