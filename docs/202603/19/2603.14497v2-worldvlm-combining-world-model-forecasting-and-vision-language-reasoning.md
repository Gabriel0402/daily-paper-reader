---
title: "WorldVLM: Combining World Model Forecasting and Vision-Language Reasoning"
title_zh: WorldVLM：结合世界模型预测与视觉语言推理
authors: "Stefan Englmeier, Katharina Winter, Fabian B. Flohr"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14497v2"
tags: ["query:mmseg"]
score: 9.0
evidence: 视觉语言模型与世界模型结合用于自动驾驶场景理解
tldr: 自动驾驶系统需要高层场景推理和准确的动态预测。视觉语言模型（VLM）擅长上下文推理但空间理解有限，而世界模型（WM）擅长预测环境演变。本文提出WorldVLM，一种结合两者优势的混合架构。通过VLM生成高层行为指令来引导WM进行驾驶预测，实现了可解释且具备上下文感知能力的动作生成，为增强自动驾驶系统的泛化能力和动态预测提供了新思路。
selection_source: fresh_fetch
motivation: 旨在结合视觉语言模型的高层推理能力与世界模型的动态预测能力，以克服单一模型在自动驾驶中的局限性。
method: 提出WorldVLM架构，利用VLM生成行为指令作为引导，驱动世界模型进行场景演化预测和驾驶决策。
result: 评估了不同的条件引导策略，并深入分析了混合架构在处理复杂驾驶场景时的设计挑战。
conclusion: WorldVLM成功统一了上下文推理与动态预测，为构建更具解释性和鲁棒性的自动驾驶基础模型提供了有效路径。
---

## 摘要
自动驾驶系统依赖于能够对高层场景上下文进行推理并准确预测周围环境动态的模型。视觉语言模型（VLM）近期已成为决策和场景理解领域极具前景的工具，在上下文推理方面展现出强大的能力。然而，它们有限的空间理解能力限制了其作为端到端驾驶模型的有效性。世界模型（WM）通过内化环境动态来预测未来的场景演变。作为自动驾驶中的自身运动预测器和基础模型，世界模型在近期得到了探索，代表了解决该领域关键挑战的一个充满前景的方向，特别是在保持动态预测的同时增强泛化能力。为了利用基于上下文的决策和预测的互补优势，我们提出了 WorldVLM：一种统一了 VLM 和 WM 的混合架构。在我们的设计中，高层 VLM 生成行为指令来引导驾驶 WM，从而实现可解释且具有上下文感知能力的动作。我们评估了条件化策略，并对混合设计的挑战提供了见解。

## Abstract
Autonomous driving systems depend on on models that can reason about high-level scene contexts and accurately predict the dynamics of their surrounding environment. Vision- Language Models (VLMs) have recently emerged as promising tools for decision-making and scene understanding, offering strong capabilities in contextual reasoning. However, their limited spatial comprehension constrains their effectiveness as end-to-end driving models. World Models (WM) internalize environmental dynamics to predict future scene evolution. Recently explored as ego-motion predictors and foundation models for autonomous driving, they represent a promising direction for addressing key challenges in the field, particularly enhancing generalization while maintaining dynamic prediction. To leverage the complementary strengths of context-based decision making and prediction, we propose WorldVLM: A hybrid architecture that unifies VLMs and WMs. In our design, the high-level VLM generates behavior commands to guide the driving WM, enabling interpretable and context-aware actions. We evaluate conditioning strategies and provide insights into the hybrid design challenges.

---

## 论文详细总结（自动生成）

### 论文总结：WorldVLM —— 结合世界模型预测与视觉语言推理

#### 1. 核心问题与整体含义（研究动机和背景）
自动驾驶面临的核心挑战是在动态复杂的城市环境中实现安全、可信的决策。
*   **研究动机**：目前的视觉语言模型（VLM）虽然具备强大的高层场景理解和常识推理能力，但在精确的空间推理和几何理解上存在短板；而世界模型（WM）擅长通过学习环境动力学来预测未来场景演变，但缺乏语义推理和决策逻辑。
*   **核心问题**：如何将 VLM 的“高层推理”与 WM 的“底层物理预测”结合，以提升自动驾驶系统的泛化能力、安全性和可解释性。

#### 2. 方法论：核心思想与关键技术
WorldVLM 提出了一种混合架构，将驾驶任务分解为“行为规划”和“轨迹预测”两个阶段：
*   **行为规划 VLM**：
    *   **输入**：单路前视图像、导航指令（左转、右转、直行）和当前车速。
    *   **输出**：生成结构化的 JSON 格式数据，包含**理由（Justification）**、**动作描述（Action）**和**行为指令（Action Token）**。
    *   **技术细节**：在 VLM 顶层添加了一个行为头（三层 MLP），将语言模型的隐藏状态映射为低维控制向量（转向角 $\alpha$ 和速度 $v$）。
*   **轨迹预测世界模型（WM）**：
    *   **核心思想**：采用现有的 LAW 模型作为基础，将其改造为受 VLM 指令引导的预测器。
    *   **条件引导机制**：将 VLM 生成的行为向量进行零填充并嵌入，与 WM 的空间特征和路径点查询（Waypoint Queries）进行拼接，从而引导模型预测出符合语义意图的未来潜在特征和自身运动轨迹。

#### 3. 实验设计
*   **数据集**：基于 **nuScenes** 进行了扩展。作者利用 GPT-OSS-120B 模型结合 DriveLM 和 doScenes 数据，自动生成了包含“理由-动作”对的标注数据集。
*   **Benchmark 与对比方法**：
    *   **基准模型**：LAW（原始未加引导的世界模型）。
    *   **对比变体**：无行为引导（No Behavior）、使用真值运动向量引导（Motion Vector）、离散动作引导等。
*   **评估指标**：
    *   **驾驶性能**：L2 误差（米）、碰撞率（%）。
    *   **推理质量**：NLP 指标（BLEU、ROUGE、BERTScore）。

#### 4. 资源与算力
*   **训练算力**：使用了 **4 台 NVIDIA A40 GPU**（每台 46GB VRAM）进行世界模型的条件化训练；VLM 的微调在 2 台 GPU 上完成，训练了 4 个 epoch。
*   **推理算力**：在 **NVIDIA GeForce RTX 4090** 上，VLM 推理约需 1 秒（受限于长文本生成），而 WM 部分运行频率约为 12Hz。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   定量分析涵盖了 nuScenes 验证集的整体表现。
    *   进行了多项**消融实验**：包括不同的条件化架构（是否拼接行为特征）、不同的引导信号类型（连续向量 vs 离散标签）、以及 VLM 不同 Token 表示对控制效果的影响。
*   **充分性评价**：实验设计较为系统，通过 NLP 指标验证了推理的准确性，通过驾驶指标验证了预测的有效性。虽然样本分割（80/20 训练验证比）是自定义的，但对比实验涵盖了从感知到规划的多个维度，论证较为客观。

#### 6. 主要结论与发现
*   **语义引导有效性**：VLM 生成的行为指令能够有效引导 WM。在交互复杂的场景中，WorldVLM 表现出更谨慎的驾驶行为（如主动避让行人），安全性优于纯数据驱动的基准模型。
*   **性能权衡**：虽然 WorldVLM 在 L2 误差上与基准模型持平，但在长时预测（3s）的碰撞率上，通过语义引导显著降低了风险（从 1% 降至 0.39%）。
*   **引导信号选择**：细粒度的连续运动向量（角度+速度）比粗粒度的离散动作标签更能提升轨迹预测的精度。

#### 7. 优点（亮点）
*   **架构创新**：首次提出由 VLM 生成高层行为指令来条件化引导轨迹预测世界模型的框架。
*   **可解释性强**：通过“理由-动作”的文本输出，使系统能够解释“为什么要这么开”，增强了自动驾驶的透明度。
*   **数据贡献**：扩展了 nuScenes 数据集，增加了推理标注，并承诺开源代码和模型。

#### 8. 不足与局限
*   **感知局限**：目前仅处理单路前视图像，且受限于 LLaVA 编码器的裁剪，视野较窄，缺乏全向空间感知。
*   **实时性挑战**：VLM 的推理延迟（1s）较高，难以直接应用于高速实时闭环控制，需进一步优化推理频率解耦。
*   **评估局限**：主要集中在开环评估（Open-loop），缺乏在模拟器（如 CARLA）中的闭环（Closed-loop）安全性验证。
*   **数据偏差**：推理标注由大模型生成而非人工标注，可能存在幻觉或逻辑不一致的风险。

（完）
