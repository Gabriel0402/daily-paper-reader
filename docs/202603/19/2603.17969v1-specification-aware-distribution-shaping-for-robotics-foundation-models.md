---
title: Specification-Aware Distribution Shaping for Robotics Foundation Models
title_zh: 机器人基础模型的规范感知分布整形
authors: "Sadık Bera Yüksel, Derya Aksaray"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17969v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 机器人基础模型的信号时序逻辑约束
tldr: 针对机器人基础模型在执行复杂任务时缺乏安全性和时序逻辑保障的问题，本文提出了一种规范感知的动作分布优化框架。该方法无需修改预训练模型参数，通过在执行过程中引入信号时序逻辑（STL）约束，并结合前向动力学传播进行实时分布调整，确保机器人在满足时空约束的同时执行自然语言指令。
selection_source: fresh_fetch
motivation: 现有的机器人基础模型主要由数据驱动，难以在部署过程中提供关于安全性及复杂时空约束的形式化保证。
method: 提出一种在推理阶段通过前向动力学传播来优化动作分布的方法，以确保其满足信号时序逻辑（STL）定义的硬性约束。
result: 在多种仿真环境和复杂任务规范下的实验表明，该框架能有效引导预训练模型在不改变参数的情况下满足严苛的运行约束。
conclusion: 该研究为提升机器人基础模型的可靠性和任务合规性提供了一种无需重新训练的高效解决方案。
---

## 摘要
机器人基础模型在跨多种任务和环境执行自然语言指令方面展现出了强大的能力。然而，它们在很大程度上仍是数据驱动的，且在部署过程中缺乏对安全性以及时间相关规范满足情况的形式化保证。在实践中，机器人通常需要遵守涉及丰富时空要求的操作约束，例如有时限的目标访问、顺序目标以及持续的安全条件。在这项工作中，我们提出了一种规范感知的动作分布优化框架，该框架在不修改预训练机器人基础模型参数的情况下，在执行过程中强制执行一大类信号时序逻辑（STL）约束。在每个决策步骤中，该方法通过使用前向动力学传播对剩余时域进行推理，计算出一个满足硬性 STL 可行性约束的最小修改动作分布。我们在仿真中利用最先进的机器人基础模型，在多种环境和复杂规范下验证了所提框架。

## Abstract
Robotics foundation models have demonstrated strong capabilities in executing natural language instructions across diverse tasks and environments. However, they remain largely data-driven and lack formal guarantees on safety and satisfaction of time-dependent specifications during deployment. In practice, robots often need to comply with operational constraints involving rich spatio-temporal requirements such as time-bounded goal visits, sequential objectives, and persistent safety conditions. In this work, we propose a specification-aware action distribution optimization framework that enforces a broad class of Signal Temporal Logic (STL) constraints during execution of a pretrained robotics foundation model without modifying its parameters. At each decision step, the method computes a minimally modified action distribution that satisfies a hard STL feasibility constraint by reasoning over the remaining horizon using forward dynamics propagation. We validate the proposed framework in simulation using a state-of-the-art robotics foundation model across multiple environments and complex specifications.

---

## 论文详细总结（自动生成）

### 论文总结：机器人基础模型的规范感知分布整形 (Specification-Aware Distribution Shaping for Robotics Foundation Models)

#### 1. 核心问题与整体含义（研究动机和背景）
现有的机器人基础模型（如 SPOC、OpenVLA）虽然在处理自然语言指令和泛化到新环境方面表现出色，但它们本质上是数据驱动的黑盒模型，缺乏**形式化保证**。在实际部署中，机器人必须遵守复杂的时空约束（如“在60秒内充电”、“按顺序访问A和B”或“始终避开某区域”）。现有的方法（如 SafeDec）通常只能处理简单的安全性约束（即下一时刻不碰撞），无法处理涉及时间窗口或长时程顺序要求的**信号时序逻辑（STL）**规范。本文旨在提出一种无需重新训练模型、在推理阶段强制执行复杂 STL 约束的框架。

#### 2. 方法论：核心思想与关键技术
该方法的核心是在推理时对基础模型的动作分布进行“整形（Shaping）”，使其在满足约束的同时，尽可能保留原始模型的语义理解能力。

*   **核心思想**：将动作选择建模为一个受约束的优化问题。目标是找到一个新的动作分布 $\pi^*$，使其与基础模型原始分布 $\pi_{FM}$ 的 **KL 散度最小**，同时满足 STL 可行性约束。
*   **关键技术细节**：
    *   **STL 策略合成**：预先训练一个辅助策略 $\pi_\phi$（使用强化学习和漏斗形奖励整形技术），专门用于引导机器人满足特定的 STL 规范。
    *   **规范评估函数 $J_\phi$**：在每个决策步，利用已知动力学模型进行前向传播，评估“当前动作 + 随后执行辅助策略 $\pi_\phi$”是否能在未来时域内满足 STL 规范。
    *   **闭式解优化**：通过拉格朗日乘数法，推导出优化问题的闭式解。最终效果是：将不满足未来约束的动作概率设为零，并对剩余动作进行重采样。
    *   **算法流程**：查询基础模型获取原始分布 -> 评估各动作的 STL 可行性 -> 计算优化后的分布 -> 采样并执行动作。

#### 3. 实验设计
*   **仿真环境**：使用 AI2-THOR 3D 仿真器，提供高保真的室内环境。
*   **基础模型**：采用最先进的 **SPOC**（Shortest Path Oracle）模型。
*   **实验场景**：
    *   **案例 1：带时间窗口的充电任务**。指令为“寻找碗”，约束为“在 [0,60] 和 [80,140] 时间段内各访问一次充电站”。
    *   **案例 2：顺序目标访问与安全约束**。指令为“寻找平底锅”，约束为“按顺序访问区域1、2、3，且始终避开禁区”。
*   **基准对比（Benchmark）**：与未经修改的原始 SPOC 模型策略进行对比。

#### 4. 资源与算力
*   论文提到辅助策略 $\pi_\phi$ 是使用 **DQN** 在环境的 2D 占用网格图（Occupancy Map）抽象上训练的。
*   **具体算力说明**：文中**未明确列出**具体的 GPU 型号、数量或训练时长。由于基础模型是预训练的，且辅助策略是在简化的 2D 地图上训练，预计其计算开销远低于训练大型基础模型。

#### 5. 实验数量与充分性
*   **实验规模**：针对两个案例研究，分别进行了 **200 次独立随机仿真运行**。
*   **充分性评价**：
    *   **充分性**：200 次实验对于验证统计成功率较为充分。
    *   **客观性**：实验涵盖了导航、避障、顺序任务和时间窗口任务，展示了模型在不同复杂度下的表现。
    *   **局限性**：缺乏在真实物理机器人上的验证，且对比方法较为单一（仅对比了原始模型，未与其它受限解码方法如 SafeDec 在相同任务下直接对比）。

#### 6. 主要结论与发现
*   **100% 的 STL 满足率**：在所有测试中，该框架均能保证机器人满足 STL 约束（前提是动力学模型准确且任务初始可行）。
*   **保持主任务性能**：在案例 1 中，主任务成功率仅从 93.5% 轻微下降至 92.5%；在更复杂的案例 2 中，成功率为 82.5%。
*   **权衡关系**：实验发现，约束越复杂，对原始分布的修改就越大，主任务（自然语言指令）的成功率会有所下降，这体现了安全性与任务效率之间的权衡。

#### 7. 优点
*   **无需重训**：作为一种推理侧的插件式方法，它不需要修改基础模型的数亿个参数。
*   **长时程推理**：相比于只看下一帧的方法，该框架通过辅助策略和前向传播，具备了处理时间窗口和顺序任务的能力。
*   **形式化保证**：在确定性系统下提供了数学上的满足感保证。

#### 8. 不足与局限
*   **依赖环境先验**：该方法需要已知的动力学模型和环境的粗略结构图（用于训练辅助策略和前向传播），这在完全未知的环境中难以实现。
*   **计算开销**：在每个步骤进行长时程的前向传播可能会带来实时性挑战，尤其是当动作空间或状态空间变大时。
*   **模型失配风险**：如果用于前向传播的简化模型与真实物理环境存在较大差异，其可行性保证将失效。
*   **任务冲突**：当 STL 约束与自然语言指令完全冲突时，模型会优先选择满足约束，可能导致主任务彻底失败。

（完）
