---
title: Collaborative Multi-Agent Optimization for Personalized Memory System
title_zh: 个性化记忆系统的协同多智能体优化
authors: "Wenyu Mao, Haoyang Liu, Zhao Liu, Haosong Tan, Yaorui Shi, Jiancan Wu, An Zhang, Xiang Wang"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12631v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 使用大语言模型的多智能体记忆系统协作强化学习框架
tldr: 针对个性化大模型在长对话记忆管理中的局限，本文提出CoMAM框架。传统方法往往独立优化多智能体系统中的单个智能体，忽略了跨智能体协作对全局性能的影响。CoMAM通过强化学习将智能体执行建模为顺序马尔可夫决策过程，利用排名一致性动态整合局部任务奖励与全局准确性奖励，实现多智能体的协同优化。实验证明，该方法在记忆构建与检索任务中显著优于现有主流系统。
selection_source: fresh_fetch
motivation: 现有的多智能体记忆系统通常独立优化单个智能体，忽略了智能体间的协作依赖，导致局部优化难以保证全局系统性能。
method: 提出CoMAM框架，将智能体协作建模为顺序MDP，并利用基于排名一致性的自适应权重整合局部与全局奖励，实现联合强化学习优化。
result: 实验结果表明，CoMAM在多项指标上优于领先的记忆系统，验证了协同优化在提升个性化记忆系统性能方面的有效性。
conclusion: 通过协同强化学习对多智能体进行联合优化，能够有效对齐局部任务改进与全局系统目标，是提升复杂记忆系统性能的关键。
---

## 摘要
记忆系统通过缓解捕获长期用户与大语言模型（LLM）对话时的上下文窗口限制，对个性化 LLM 至关重要。通常，此类系统利用多个智能体来处理多粒度的记忆构建和个性化记忆检索任务。为了优化系统，现有方法侧重于通过提示工程或微调，独立地使智能体专门化于其局部任务。然而，它们忽视了跨智能体协作，而局部智能体的独立优化很难保证全局系统性能。为了解决这个问题，我们提出了一种用于多智能体记忆系统的协同强化学习框架（CoMAM），通过联合优化局部智能体来促进协作。具体而言，我们将智能体的执行正则化为序列马尔可夫决策过程（MDP），以将智能体间的依赖关系嵌入到状态转移中，从而产生局部任务奖励（例如记忆构建的信息覆盖率）和全局奖励（即问答准确性）。然后，我们通过局部奖励与全局奖励之间的群体级排名一致性来量化每个智能体的贡献，并将其作为自适应权重来分配全局信用并整合局部与全局奖励。每个智能体都通过这些整合后的奖励进行优化，使局部改进与全局性能保持一致。实验表明，CoMAM 优于领先的记忆系统，验证了我们提出的用于联合优化的协同强化学习的有效性。

## Abstract
Memory systems are crucial to personalized LLMs by mitigating the context window limitation in capturing long-term user-LLM conversations. Typically, such systems leverage multiple agents to handle multi-granular memory construction and personalized memory retrieval tasks. To optimize the system, existing methods focus on specializing agents on their local tasks independently via prompt engineering or fine-tuning. However, they overlook cross-agent collaboration, where independent optimization on local agents hardly guarantees the global system performance. To address this issue, we propose a Collaborative Reinforcement Learning Framework for Multi-Agent Memory Systems (CoMAM), jointly optimizing local agents to facilitate collaboration. Specifically, we regularize agents' execution as a sequential Markov decision process (MDP) to embed inter-agent dependencies into the state transition, yielding both local task rewards (e.g., information coverage for memory construction) and global rewards (i.e., query-answer accuracy). Then, we quantify each agent's contribution via group-level ranking consistency between local and global rewards, treating them as adaptive weights to assign global credit and integrate local-global rewards. Each agent is optimized by these integrated rewards, aligning local improvements with the global performance. Experiments show CoMAM outperforms leading memory systems, validating the efficacy of our proposed collaborative reinforcement learning for joint optimization.

---

## 论文详细总结（自动生成）

### 论文总结：用于个性化记忆系统的协同多智能体优化 (CoMAM)

#### 1. 核心问题与研究动机
*   **核心问题**：个性化大语言模型（LLM）需要记忆系统来处理长期的用户对话历史。现有的记忆系统通常由多个专门的智能体（如负责记忆构建、摘要提取、检索等）组成。
*   **研究动机**：目前主流的优化方法（如提示工程或独立强化学习）倾向于**独立优化**单个智能体。这种“局部专家”的集合模式忽略了智能体之间的跨智能体协作。例如，构建智能体可能存储了冗余信息，虽然完成了局部任务，却干扰了检索智能体的效率。论文指出，局部最优并不等于全局最优，因此需要一种能够促进多智能体协同进化的联合优化框架。

#### 2. 方法论：CoMAM 框架
*   **核心思想**：提出协同强化学习框架（CoMAM），通过将多智能体的异步执行过程建模为统一的马尔可夫决策过程（MDP），并引入自适应信用分配机制，实现局部任务目标与全局系统目标的对齐。
*   **关键技术细节**：
    *   **智能体定义**：系统包含三个核心智能体：**提取智能体**（提取细粒度事实）、**画像智能体**（抽象粗粒度偏好）和**检索智能体**（检索记忆并生成回答）。
    *   **执行轨迹正则化（MDP）**：将智能体间的依赖关系嵌入状态转移中。流程为：原始历史 $\to$ 提取智能体 $\to$ 细粒度记忆 $\to$ 画像智能体 $\to$ 粗粒度记忆 $\to$ 检索智能体 $\to$ 最终回答。
    *   **奖励设计**：
        *   **局部奖励**：如信息覆盖率（提取）、抽象合理性（画像）、检索精度（检索）。
        *   **全局奖励**：最终问答的准确性（QA Accuracy）。
    *   **自适应信用分配**：利用 **NDCG（归一化折损累计增益）** 衡量局部奖励排名与全局奖励排名的一致性。一致性得分越高，该智能体获得的全局奖励权重越大。
    *   **优化算法**：采用 **GRPO（群体相对策略优化）** 算法，通过群体样本的相对奖励进行策略更新，无需额外的 Critic 网络。

#### 3. 实验设计
*   **数据集与场景**：使用 **PersonaMem** 基准测试，包含 180 多个长期对话历史和约 6,000 个多项选择题。
*   **Benchmark 设置**：涵盖了三种历史长度（32K、128K、1M token）和七种查询类型（事实召回、偏好追踪、场景泛化等）。
*   **对比方法**：
    *   **无记忆模式**：直接输入（Base）、标准 RAG。
    *   **基于提示的记忆系统**：CAM、MemoryBank、A-Mem。
    *   **基于强化学习的记忆系统**：Mem1、Memory-R1（独立优化 SOTA）。
*   **模型骨干**：采用了 Qwen2.5 (3B/7B) 和 Llama-3.1/3.2 (3B/8B) 系列模型。

#### 4. 资源与算力
*   **模型规模**：提取和画像智能体使用 3B 模型，检索智能体使用 7B/8B 模型。
*   **超参数**：Batch size 为 128，Group size 为 8，训练 5 个 Epoch，上下文窗口设定为 32K。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但从模型规模和 GRPO 算法特性来看，通常需要多张 A100 或 H100 级别的显卡支持。

#### 5. 实验数量与充分性
*   **实验规模**：在 2 种模型家族、3 种上下文长度、7 种问题类型下进行了全面测试。
*   **消融实验**：
    *   验证了三个智能体各自的必要性。
    *   对比了“独立 RL”与“MDP 联合 RL”的效果。
    *   测试了不同信用分配策略（仅局部、仅全局、等权分配 vs. 自适应分配）的影响。
*   **评价**：实验设计较为充分且客观，通过多随机种子取平均值的方式保证了结果的公平性，消融实验有力地支撑了核心贡献点。

#### 6. 主要结论与发现
*   **性能领先**：CoMAM 在所有设置下均优于现有方法，相比独立优化的 RL 基准，准确率提升了 8.5% 至 16.7%。
*   **协同效应**：联合优化（Joint Optimization）显著优于独立优化，证明了在记忆系统中考虑智能体间依赖关系的必要性。
*   **信用分配有效性**：自适应权重机制能有效解决信用分配模糊问题，使智能体在提升局部能力的同时，更积极地为全局目标贡献。
*   **效率优势**：虽然单步训练更复杂，但由于采用并行联合训练，总收敛步数通常少于依次独立优化各智能体的总和。

#### 7. 优点
*   **系统性视角**：打破了以往“局部专家”的思维定式，从全局协同角度优化多智能体系统。
*   **数学建模严谨**：通过 MDP 正则化解决了异构、异步智能体的联合训练难题。
*   **自适应性强**：引入 NDCG 动态量化贡献度，避免了人工设定固定权重的偏差。

#### 8. 不足与局限
*   **智能体任务范围有限**：目前仅涵盖提取、画像、检索三类任务，尚未涉及记忆删除、冲突合并等更复杂的记忆管理功能。
*   **静态 MDP 假设**：训练时的 MDP 轨迹是相对静态的，可能与现实中动态触发、高频更新的推理环境存在分布差异。
*   **局部-局部对齐缺失**：虽然实现了局部-全局对齐，但智能体之间（如提取与画像之间）的直接显式对齐仍有提升空间。

（完）
