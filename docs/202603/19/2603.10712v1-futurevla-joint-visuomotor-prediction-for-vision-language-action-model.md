---
title: "FutureVLA: Joint Visuomotor Prediction for Vision-Language-Action Model"
title_zh: FutureVLA：视觉-语言-动作模型的联合视觉运动预测
authors: "Xiaoxu Xu, Hao Li, Jinhui Ye, Yilun Chen, Jia Zeng, Xinyi Chen, Linning Xu, Dahua Lin, Weixin Li, Jiangmiao Pang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10712v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 具身智能体的视觉-语言-动作模型
tldr: FutureVLA 针对具身智能体在视觉-动作联合预测中的不足，提出了一种新型的联合视觉运动预测架构。该模型通过解耦视觉状态与动作建模，并引入联合视觉运动门控机制，使模型能专注于连续物理动力学并有效利用环境约束。通过预训练提取通用物理先验并进行潜空间对齐，FutureVLA 在不改变推理架构的前提下显著提升了多种 VLA 框架的性能。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型在联合建模视觉感知与动作执行时，容易受无关视觉细节干扰或缺乏时间连续性，难以有效预测未来。
method: 提出 FutureVLA 架构，利用联合视觉运动门控机制在预训练中解耦视觉与动作流，并通过潜空间嵌入对齐策略将物理先验注入下游模型。
result: 广泛的实验证明，FutureVLA 能够持续提升现有多种 VLA 框架在复杂任务中的表现。
conclusion: 该研究证明了通过结构化解耦和联合预测建模，可以有效增强具身智能体对物理动态的理解与执行能力。
---

## 摘要
预测性前瞻对于智能具身智能体至关重要。由于机器人的运动执行本质上受其对环境几何视觉感知的约束，有效地预测未来需要捕捉这种紧密耦合的视觉运动相互作用。虽然最近的视觉-语言-动作（VLA）模型尝试引入未来引导，但它们在处理这种联合建模方面仍面临困难。现有的显式方法将模型容量分散到了与任务无关的视觉细节上，而依赖稀疏帧对的隐式方法则破坏了时间连续性。由于过度依赖视觉重建，这些方法变得由视觉主导，将静态场景上下文与动态动作意图纠缠在一起。我们认为，有效的联合视觉运动预测建模既需要时间连续性，也需要视觉条件的监督解耦。为此，我们提出了 FutureVLA，其特点是采用了一种新型的联合视觉运动预测架构。FutureVLA 旨在通过先解耦视觉和运动信息，然后联合编码广义物理先验来提取联合视觉运动嵌入。具体而言，在预训练阶段，我们利用异构操作数据集并引入联合视觉运动门控机制，从结构上将视觉状态保持与时间动作建模分离。它允许运动流专注于连续的物理动力学，同时显式地查询视觉标记以获取环境约束，从而产生高度泛化性的联合视觉运动嵌入。随后，在后训练阶段，我们采用潜嵌入对齐策略，使各种下游 VLA 模型能够在不修改其推理架构的情况下内化这些时间先验。大量实验表明，FutureVLA 持续改进了 VLA 框架。

## Abstract
Predictive foresight is important to intelligent embodied agents. Since the motor execution of a robot is intrinsically constrained by its visual perception of environmental geometry, effectively anticipating the future requires capturing this tightly coupled visuomotor interplay. While recent vision-language-action models attempt to incorporate future guidance, they struggle with this joint modeling. Existing explicit methods divert capacity to task-irrelevant visual details, whereas implicit methods relying on sparse frame pairs disrupt temporal continuity. By heavily relying on visual reconstruction, these methods become visually dominated, entangling static scene context with dynamic action intent. We argue that effective joint visuomotor predictive modeling requires both temporal continuity and visually-conditioned supervision decoupling. To this end, we propose FutureVLA, featuring a novel Joint Visuomotor Predictive Architecture. FutureVLA is designed to extract joint visuomotor embeddings by first decoupling visual and motor information, and then jointly encoding generalized physical priors. Specifically, in the pretraining stage, we leverage heterogeneous manipulation datasets and introduce a Joint Visuomotor Gating mechanism to structurally separate visual state preservation from temporal action modeling. It allows the motor stream to focus on continuous physical dynamics while explicitly querying visual tokens for environmental constraints, yielding highly generalizable joint visuomotor embeddings. Subsequently, in the post-training stage, we employ a latent embeddings alignment strategy, enabling diverse downstream VLA models to internalize these temporal priors without modifying their inference architectures. Extensive experiments demonstrate that FutureVLA consistently improves VLA frameworks.

---

## 论文详细总结（自动生成）

以下是对论文《FutureVLA: Joint Visuomotor Prediction for Vision-Language-Action Model》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
具身智能体（Embodied Agents）需要具备预测未来状态的能力，以约束当前的动作决策。然而，现有的视觉-语言-动作（VLA）模型在整合“未来引导”信号时存在两个主要缺陷：
*   **视觉主导的纠缠（Visual Dominance）：** 显式预测（如预测未来视频帧）或隐式重建方法往往过度关注与任务无关的视觉细节（如背景变化），导致静态环境上下文与动态动作意图混淆。
*   **时间不连续性（Temporal Discontinuity）：** 许多隐式方法依赖稀疏的帧对（如仅取起始帧和结束帧），破坏了动作执行所需的连续物理动力学信息。
**研究动机：** 提出一种能够同时保持时间连续性并实现“视觉-运动”监督解耦的联合预测架构，使模型能提取纯净的、受物理约束的运动先验。

### 2. 方法论
FutureVLA 采用两阶段训练范式：
*   **阶段一：联合视觉运动预训练（Joint Visuomotor Pretraining）**
    *   **视觉标记化：** 使用预训练且冻结的 **3D-VAE** 将连续视频剪辑（如17帧）编码为紧凑的时间标记，保留精细的时间结构。
    *   **联合视觉运动门控（Joint Visuomotor Gating）：** 这是核心机制。将时间标记分为“视觉流”和“运动流”。
        *   **视觉流：** 仅负责重建第一帧的潜嵌入，强制其保留初始环境的静态几何约束。
        *   **运动流：** 负责预测动作块（Action Chunk）。通过**门控交叉注意力机制**，运动流会显式地向视觉流查询空间特征（Spatial Affordances），从而生成受环境约束的物理动力学嵌入。
*   **阶段二：引导式 VLA 后训练（Guided VLA Post-training）**
    *   **潜嵌入对齐（Latent Alignment）：** 将预训练得到的“联合视觉运动嵌入”作为语义锚点。在训练下游 VLA 模型时，通过一个轻量级适配器，强制 VLA 的中间表示与这些具有未来感知能力的嵌入对齐。
    *   **推理优势：** 在推理阶段，VLA 模型不需要输入未来帧，仅凭当前观察即可利用内化的未来动力学先验。

### 3. 实验设计
*   **数据集与场景：**
    *   **仿真环境：** SimplerEnv（包含 Google Robot 和 WidowX 机器人）、LIBERO（Object, Spatial, Goal, Long 四个子集）以及 LIBERO-Plus（评估鲁棒性）。
    *   **真实世界：** 使用 Franka 机器人执行四类复杂任务：制作汉堡、插花、勺子舀豆子、擦白板（涉及接触力控制）。
*   **对比方法：**
    *   基础 VLA：OpenVLA-OFT, $\pi_0$, GR00T-N1.5。
    *   显式未来引导：WorldVLA, DreamVLA。
    *   隐式未来引导：UniVLA, Villa-X, LAPA。

### 4. 资源与算力
*   **算力设备：** 使用了 **16 张 NVIDIA A100 GPU**。
*   **训练时长：** 预训练阶段大约耗时 **3 天**。
*   **数据规模：** 预训练使用了来自 OXE 和 LIBERO 的混合数据集，包含约 **1560 万帧**图像。

### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多维度评估：** 涵盖了从仿真到真实世界、从单任务到长程任务（Long-horizon）的广泛场景。
*   **消融实验：** 针对“联合视觉运动门控”的有效性、时间采样密度（2/5/9/17帧对比）、时间跨度长度、以及重建目标（第一帧 vs 最后一帧）进行了深入对比。
*   **公平性对比：** 在统一的架构下重新实现了多种未来引导方法，排除了骨干网络差异带来的干扰。
*   **物理一致性分析：** 引入了“物理感知动作一致性（PAAC）”指标，定量证明了 FutureVLA 提取的嵌入与实际物理动作的相关性更强。

### 6. 主要结论与发现
*   **性能提升：** 在 SimplerEnv 上比无引导基线平均提升 **11.4%**；在真实世界任务中，成功率比强大的 $\pi_0$ 模型高出 **26.7%**。
*   **解耦的重要性：** 实验证明，将视觉重建与动作建模解耦，能有效防止模型陷入“视觉残差”陷阱，从而学习到真正的运动意图。
*   **连续性优势：** 连续多帧建模显著优于稀疏采样，尤其在长程任务中表现突出。

### 7. 优点
*   **架构创新：** 提出的门控解耦机制巧妙地解决了视觉细节干扰运动建模的问题。
*   **即插即用：** 通过潜空间对齐，该方法可以增强多种现有的 VLA 架构（如 GR00T-style 或 OFT-style），且不增加推理延迟。
*   **物理扎根：** 实验证明其学习到的嵌入具有极高的物理一致性，能更好地处理接触密集型任务（如擦白板）。

### 8. 不足与局限
*   **多模态感知局限：** 目前主要依赖视觉约束。对于极度依赖触觉或力反馈的任务（如精细的力控装配），仅靠视觉预测可能不足。
*   **动作块限制：** 性能受限于固定的动作块长度（Action Chunking），对于极长跨度的预测可能仍存在挑战。
*   **算力门槛：** 预训练阶段对 A100 等高性能算力仍有一定依赖。

（完）
