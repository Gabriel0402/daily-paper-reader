---
title: "PicoSAM3: Real-Time In-Sensor Region-of-Interest Segmentation"
title_zh: PicoSAM3：实时传感器内感兴趣区域分割
authors: "Pietro Bonazzi, Nicola Farronato, Stefan Zihlmann, Haotong Qin, Michele Magno"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11917v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 用于实时分割的SAM模型
tldr: PicoSAM3是一个专为边缘设备和传感器内执行设计的轻量级可提示视觉分割模型，拥有130万参数。它结合了密集CNN架构、ROI提示编码和高效通道注意力机制，并通过SAM2/SAM3进行知识蒸馏。该模型在Sony IMX500传感器上实现了11.82ms的实时推理，在保持高精度的同时满足了严格的内存和算力约束，为隐私敏感和低延迟应用提供了高效的分割方案。
selection_source: fresh_fetch
motivation: 旨在解决智能眼镜和物联网设备在隐私保护和低延迟场景下，对传感器内实时图像分割的需求。
method: 设计了包含1.3M参数的轻量级CNN，集成ROI提示编码与高效通道注意力，并利用SAM2/SAM3大模型进行知识蒸馏。
result: 在COCO和LVIS上表现优异，INT8量化模型在Sony IMX500传感器上的推理延迟仅为11.82ms且精度损失极小。
conclusion: 研究证明了在极受限的传感器硬件上实现高质量、空间灵活的可提示分割是可行且高效的。
---

## 摘要
实时、设备端分割对于延迟敏感和隐私感知的应用（如智能眼镜和物联网设备）至关重要。我们介绍了 PicoSAM3，这是一种轻量级的可提示视觉分割模型，针对边缘和传感器内执行进行了优化，包括在索尼 IMX500 视觉传感器上的部署。PicoSAM3 拥有 130 万个参数，结合了密集 CNN 架构、感兴趣区域提示编码、高效通道注意力（Efficient Channel Attention）以及来自 SAM2 和 SAM3 的知识蒸馏。在 COCO 和 LVIS 数据集上，PicoSAM3 分别实现了 65.45% 和 64.01% 的 mIoU，在相似或更低复杂度的情况下优于现有的基于 SAM 和面向边缘的基准模型。INT8 量化模型在保持准确性的同时，性能下降微乎其微，并能在 IMX500 上实现 11.82 毫秒延迟的实时传感器内推理，完全符合其内存和算子限制。消融研究表明，与监督训练相比，来自大型 SAM 模型的蒸馏可带来高达 +14.5% 的 mIoU 提升，并证明了直接在传感器层面实现高质量、空间灵活的可提示分割是可行的。

## Abstract
Real-time, on-device segmentation is critical for latency-sensitive and privacy-aware applications such as smart glasses and Internet-of-Things devices. We introduce PicoSAM3, a lightweight promptable visual segmentation model optimized for edge and in-sensor execution, including deployment on the Sony IMX500 vision sensor. PicoSAM3 has 1.3 M parameters and combines a dense CNN architecture with region of interest prompt encoding, Efficient Channel Attention, and knowledge distillation from SAM2 and SAM3. On COCO and LVIS, PicoSAM3 achieves 65.45% and 64.01% mIoU, respectively, outperforming existing SAM-based and edge-oriented baselines at similar or lower complexity. The INT8 quantized model preserves accuracy with negligible degradation while enabling real-time in-sensor inference at 11.82 ms latency on the IMX500, fully complying with its memory and operator constraints. Ablation studies show that distillation from large SAM models yields up to +14.5% mIoU improvement over supervised training and demonstrate that high-quality, spatially flexible promptable segmentation is feasible directly at the sensor level.

---

## 论文详细总结（自动生成）

### PicoSAM3：实时传感器内感兴趣区域分割论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何在资源极度受限的边缘设备（如智能眼镜、IoT节点）甚至图像传感器内部，实现高精度、低延迟且支持提示（Promptable）的实时图像分割。
*   **研究动机**：
    *   **硬件限制**：现有的分割大模型（如 SAM, SAM 2/3）基于 Transformer 架构，内存占用大且计算复杂度高，无法在仅有几 MB SRAM 的传感器（如 Sony IMX500）上运行。
    *   **隐私与延迟**：云端处理存在带宽瓶颈和隐私泄露风险，而“传感器内计算”（In-sensor Computing）能消除数据传输开销，实现即时感知。
    *   **算子兼容性**：许多边缘加速器不支持 Transformer 所需的非线性算子（如 Softmax、LayerNorm），需要纯 CNN 架构的替代方案。

#### 2. 方法论与关键技术
*   **核心思想**：设计一个超轻量级（1.3M 参数）的纯 CNN 架构，通过“隐式提示编码”和“大模型知识蒸馏”来弥补模型缩小带来的性能损失。
*   **关键技术细节**：
    *   **硬件友好型架构**：采用对称的 U-Net 风格编码器-解码器，使用深度可分离卷积。引入了**扩张瓶颈层（Dilated Bottleneck）**以扩大感受野，并集成**高效通道注意力（ECA）**进行特征重校准。
    *   **隐式 ROI 提示编码**：由于传感器仅支持 RGB 输入，无法输入坐标张量。PicoSAM3 通过**中心裁剪（Centered Cropping）**实现提示：将目标对象置于 96x96 裁剪区域的中心，使网络隐式学习到“中心即目标”的空间先验。
    *   **知识蒸馏流水线**：采用两阶段蒸馏。从强大的 **SAM 3** 教师模型中提取软掩码（Soft Mask）和置信度。
    *   **损失函数**：结合了 MSE、Dice Loss（针对教师模型）、平衡二元交叉熵（BCE）以及**面积保持项（Area Preservation Loss）**，防止模型预测出过小的无效掩码。
    *   **INT8 量化**：使用对称周通道量化，将模型大小从 5.26 MB 压缩至 1.31 MB。

#### 3. 实验设计
*   **数据集**：在 **COCO** 和 **LVIS** 数据集上进行验证。
*   **Benchmark 与对比方法**：
    *   **重量级模型**：SAM-H, SAM 2.1, SAM 3。
    *   **边缘优化模型**：FastSAM, TinySAM, EdgeSAM, MobileSAMv2, LiteSAM 以及前作 PicoSAM2。
*   **硬件环境**：实际部署在 **Sony IMX500** 智能视觉传感器上，通过 Raspberry Pi 5 进行模型下发。

#### 4. 资源与算力
*   **训练算力**：使用单台工作站，配备 **NVIDIA RTX A6000-48Q GPU**。
*   **评估算力**：使用 **NVIDIA Tesla T4 GPU** 进行基准测试。
*   **训练配置**：采用 AdamW 优化器，Batch Size 为 64，仅训练 **1 个 Epoch**（体现了蒸馏的高效性）。
*   **推理算力**：在 IMX500 传感器内置 DSP 上运行，功耗极低（亚瓦级）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流数据集（COCO/LVIS）的精度对比、针对不同组件（ROI 提示、蒸馏来源、架构改进）的**消融实验**、以及硬件端的延迟测试。
*   **充分性评价**：实验设计较为充分。通过与不同量级的 SAM 变体对比，证明了其在极小参数量下的优越性。消融实验清晰地展示了从 SAM 2 转向 SAM 3 蒸馏带来的增益。

#### 6. 主要结论与发现
*   **性能突破**：PicoSAM3 在 COCO 上达到 **65.45% mIoU**，在 LVIS 上达到 **64.01% mIoU**，显著优于 TinySAM 和 EdgeSAM 等更大规模的边缘模型。
*   **极低延迟**：在 IMX500 传感器上实现了 **11.82 ms** 的端到端推理延迟（约 84 FPS），完全满足实时性要求。
*   **量化鲁棒性**：INT8 量化后的精度损失极小（<0.2% mIoU），证明了纯 CNN 架构在低比特量化下的稳定性。
*   **分辨率敏感性**：发现大型 SAM 模型在 96x96 低分辨率输入下会出现特征崩溃，而专门优化的 PicoSAM3 在此约束下表现更优。

#### 7. 优点
*   **极致的硬件适配**：针对传感器内存（<8MB）和算子限制进行了深度定制，是真正意义上的“传感器内”模型。
*   **创新的提示方式**：通过 ROI 裁剪巧妙绕过了传感器无法处理辅助输入（坐标/掩码通道）的限制。
*   **高效蒸馏**：仅需 1 个 Epoch 的训练即可获得超越监督学习 14.5% mIoU 的提升。

#### 8. 不足与局限
*   **输入分辨率限制**：96x96 的输入分辨率较低，对于极小物体或复杂边缘的分割精度可能受限。
*   **单目标假设**：由于采用中心裁剪提示，模型主要针对 ROI 区域内的单个显著目标，处理多目标重叠场景的能力未详细讨论。
*   **应用范围**：目前高度依赖于 Sony IMX500 的硬件 ROI 功能，在其他缺乏此类硬件加速的平台上优势可能减弱。

（完）
