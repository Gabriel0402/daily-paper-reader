---
title: "DiT4DiT: Jointly Modeling Video Dynamics and Actions for Generalizable Robot Control"
title_zh: "DiT4DiT: 联合建模视频动力学与动作以实现可泛化的机器人控制"
authors: "Teli Ma, Jia Zheng, Zifan Wang, Chuili Jiang, Andy Cui, Junwei Liang, Shuo Yang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10448v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于通用机器人控制的视频动作模型
tldr: 本研究提出DiT4DiT，一种将视频扩散Transformer与动作扩散Transformer耦合的端到端视频-动作模型。针对现有VLA模型缺乏物理动力学理解的问题，该模型通过提取视频生成过程中的中间去噪特征来指导动作预测。实验表明，DiT4DiT在LIBERO和RoboCasa等基准测试中达到SOTA性能，显著提升了样本效率和收敛速度，并在真实机器人上展现出强大的零样本泛化能力。
selection_source: fresh_fetch
motivation: 现有的视觉-语言-动作模型多依赖静态图像预训练，缺乏对物理动力学的理解，而生成式视频模型蕴含丰富的时空结构。
method: 提出一种级联框架，利用双流匹配目标函数联合训练视频和动作扩散Transformer，通过视频生成的中间特征引导动作推理。
result: 在LIBERO和RoboCasa基准测试中取得领先成绩，样本效率提升10倍以上，收敛速度加快达7倍。
conclusion: 视频生成可以作为机器人策略学习的有效扩展代理，显著增强模型的泛化能力和学习效率。
---

## 摘要
视觉-语言-动作（VLA）模型已成为机器人学习中一种极具前景的范式，但其表示在很大程度上仍继承自静态图像-文本预训练，导致物理动力学只能从相对有限的动作数据中学习。相比之下，生成式视频模型编码了丰富的时空结构和隐式物理规律，使其成为机器人操作的理想基础。然而，现有文献尚未充分挖掘其潜力。为了弥补这一差距，我们推出了 DiT4DiT，这是一种端到端的视频-动作模型，它在一个统一的级联框架中将视频扩散 Transformer（Diffusion Transformer）与动作扩散 Transformer 耦合在一起。DiT4DiT 并不依赖于重建的未来帧，而是从视频生成过程中提取中间去噪特征，并将其作为动作预测的时间对齐条件。我们进一步提出了一种具有解耦时间步长和噪声尺度的双流匹配（dual flow-matching）目标，用于视频预测、隐藏状态提取和动作推理，从而实现两个模块的连贯联合训练。在仿真和真实世界基准测试中，DiT4DiT 均取得了最先进的结果，在 LIBERO 上达到了 98.6% 的平均成功率，在 RoboCasa GR1 上达到了 50.8%，且使用的训练数据大幅减少。在 Unitree G1 机器人上，它也表现出了卓越的真实世界性能和强大的零样本泛化能力。重要的是，DiT4DiT 将样本效率提高了 10 倍以上，并将收敛速度加快了多达 7 倍，证明了视频生成可以作为机器人策略学习的有效扩展代理（scaling proxy）。我们在 https://dit4dit.github.io/ 发布了代码和模型。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for robot learning, but their representations are still largely inherited from static image-text pretraining, leaving physical dynamics to be learned from comparatively limited action data. Generative video models, by contrast, encode rich spatiotemporal structure and implicit physics, making them a compelling foundation for robotic manipulation. But their potentials are not fully explored in the literature. To bridge the gap, we introduce DiT4DiT, an end-to-end Video-Action Model that couples a video Diffusion Transformer with an action Diffusion Transformer in a unified cascaded framework. Instead of relying on reconstructed future frames, DiT4DiT extracts intermediate denoising features from the video generation process and uses them as temporally grounded conditions for action prediction. We further propose a dual flow-matching objective with decoupled timesteps and noise scales for video prediction, hidden-state extraction, and action inference, enabling coherent joint training of both modules. Across simulation and real-world benchmarks, DiT4DiT achieves state-of-the-art results, reaching average success rates of 98.6% on LIBERO and 50.8% on RoboCasa GR1 while using substantially less training data. On the Unitree G1 robot, it also delivers superior real-world performance and strong zero-shot generalization. Importantly, DiT4DiT improves sample efficiency by over 10x and speeds up convergence by up to 7x, demonstrating that video generation can serve as an effective scaling proxy for robot policy learning. We release code and models at https://dit4dit.github.io/.

---

## 论文详细总结（自动生成）

以下是对论文《DiT4DiT: Jointly Modeling Video Dynamics and Actions for Generalizable Robot Control》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型大多依赖于在静态图像-文本数据上预训练的骨干网络（如 CLIP 或 LLM）。这种方式导致模型缺乏对物理世界动态变化和时空结构的理解，必须在下游任务中通过极其有限的机器人动作数据来学习物理规律。
*   **核心问题**：如何利用生成式视频模型（VGM）中蕴含的丰富物理先验和时空动力学，将其转化为高效、可泛化的机器人控制策略？
*   **整体含义**：论文提出了 **DiT4DiT**，通过将视频扩散 Transformer 与动作扩散 Transformer 耦合，证明了视频生成可以作为机器人策略学习的强大“扩展代理（Scaling Proxy）”，显著提升了学习效率和泛化能力。

### 2. 方法论
*   **核心思想**：采用“视频动力学预测 + 逆动力学动作推理”的范式。模型不直接从图像映射到动作，而是先通过视频 DiT 建模未来帧的生成过程，并从中提取中间特征来引导动作 DiT 生成控制轨迹。
*   **关键技术细节**：
    *   **双 DiT 架构**：由一个视频扩散 Transformer（基于 Cosmos-Predict2.5-2B）和一个动作扩散 Transformer（基于 GR00T-N1）组成。
    *   **中间特征提取**：不等待视频完全生成（重建像素），而是从视频 DiT 的中间层（实验发现第 18 层效果最好）提取去噪过程中的隐藏状态（Hidden States），作为动作预测的条件。
    *   **双流匹配（Dual Flow-Matching）**：提出一种统一的训练目标，同时优化视频生成和动作推理的速率场（Velocity Field）。
    *   **非对称三时间步方案（Tri-timestep Scheme）**：
        1.  $\tau_v$：视频训练时间步，均匀采样以学习完整去噪轨迹。
        2.  $\tau_f$：特征提取时间步，固定值以保证提供给动作模块的视觉信号稳定。
        3.  $\tau_a$：动作训练时间步，采用 Beta 分布采样，侧重于关键控制阶段。
*   **算法流程**：输入当前观测和语言指令 $\rightarrow$ 视频 DiT 进行单步前向传播提取特征 $\rightarrow$ 动作 DiT 结合机器人状态和视觉特征，通过流匹配采样生成动作序列。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真基准**：LIBERO（包含 Spatial, Object, Goal, Long 四个子集，共 1,693 条轨迹）和 RoboCasa-GR1（24 个家务任务，24,000 条轨迹）。
    *   **真实世界**：使用 Unitree G1 人形机器人，涵盖“插花”、“叠杯子”、“装箱”、“拉抽屉”等 7 类高精度或长程任务（1,400 条演示轨迹）。
*   **对比方法**：
    *   **SOTA VLA 模型**：$\pi_0$、$\pi_{0.5}$、OpenVLA、CogVLA、GR00T-N1.5/N1.6 等。
    *   **消融基准**：Qwen3DiT（将视频骨干替换为同规模的 Qwen3-VL 静态模型）。

### 4. 资源与算力
*   **训练算力**：使用了 **32 张 GPU**（型号未明确说明，但根据 Mondo/HKUST 背景推测为 A100 或 H100 级别），最大训练步数为 100,000 步。
*   **推理算力**：真实机器人部署时，在单张 **NVIDIA RTX 4090** 上运行。
*   **效率指标**：模型拥有约 **22 亿（2.2B）** 可训练参数。在 G1 机器人上实现了 **6Hz** 的闭环控制频率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础仿真到复杂人形机器人操作的广泛场景。
*   **消融实验**：针对特征提取层数、去噪步数、联合训练 vs. 解耦训练进行了详细对比。
*   **泛化性测试**：专门设计了针对未见过的物体类别、物体数量变化和环境剧烈变化的零样本（Zero-shot）泛化实验。
*   **充分性评价**：实验设计非常充分且客观。通过与参数量对齐的静态 VLM 基准（Qwen3DiT）对比，有力证明了视频动力学建模的优越性。

### 6. 主要结论与发现
*   **视频生成是更优的代理任务**：相比于物体检测（Grounding）或隐式特征对齐（FLARE 风格），视频生成目标能让模型收敛速度快 **7 倍**，样本效率提升 **10 倍**以上。
*   **SOTA 性能**：在 LIBERO（98.6%）和 RoboCasa（50.8%）上均刷新了纪录，尤其在长程任务（LIBERO-Long）上优势明显。
*   **中间特征胜过重建像素**：动作预测只需视频生成的中间特征，过多的去噪步数反而会导致性能下降（因为模型会过度关注像素重建而非动作语义）。
*   **联合训练的价值**：联合训练能显著提升特征的时间连续性（Silhouette Score 提升近一倍）。

### 7. 优点
*   **端到端集成**：首次通过双流匹配目标将视频生成与动作推理在扩散模型框架下深度统一。
*   **极高的效率**：在仅使用 GR00T-N1.5 约 15% 训练数据的情况下，实现了更强的真实世界表现。
*   **强大的物理感知**：能够处理插花、叠杯等对空间精度要求极高的任务，展现出优异的物理常识。

### 8. 不足与局限
*   **推理频率限制**：6Hz 的控制频率虽然能用，但低于基于 VLM 的基准（9-13Hz），对于极高速运动可能存在挑战。
*   **单视角依赖**：目前仅依赖单路第一视角相机，在发生肢体遮挡时可能存在鲁棒性风险。
*   **算力门槛**：尽管推理较快，但训练仍需 32 张 GPU 的集群，对小型研究团队仍有一定门槛。

（完）
