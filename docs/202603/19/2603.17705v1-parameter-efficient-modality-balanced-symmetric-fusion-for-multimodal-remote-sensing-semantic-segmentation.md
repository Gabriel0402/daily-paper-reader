---
title: Parameter-Efficient Modality-Balanced Symmetric Fusion for Multimodal Remote Sensing Semantic Segmentation
title_zh: 用于多模态遥感语义分割的参数高效模态平衡对称融合
authors: "Haocheng Li, Juepeng Zheng, Shuangxi Miao, Ruibo Lu, Guosheng Cai, Haohuan Fu, Jianxi Huang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17705v1"
tags: ["query:mmseg"]
score: 10.0
evidence: 利用视觉基础模型进行多模态遥感语义分割
tldr: 针对多模态遥感语义分割中预训练模型微调开销大及模态不平衡问题，本文提出MoBaNet框架。该框架采用对称双流架构，通过跨模态提示注入适配器（CPIA）实现深度交互，利用差异引导门控融合模块（DGFM）优化特征融合，并引入模态条件随机掩码（MCRM）策略。实验证明，该方法在显著减少可训练参数的同时，在ISPRS数据集上达到了SOTA性能。
selection_source: fresh_fetch
motivation: 旨在解决预训练视觉大模型在多模态遥感任务中微调成本高昂以及辅助模态易被抑制的模态不平衡挑战。
method: 提出一种基于冻结骨干网的对称双流架构，结合提示注入适配器、差异引导融合模块及条件随机掩码策略。
result: 在ISPRS Vaihingen和Potsdam基准测试中，以极少的可训练参数实现了最先进的分割精度。
conclusion: MoBaNet通过参数高效的对称融合与平衡策略，为多模态遥感数据的鲁棒语义分割提供了有效方案。
---

## 摘要
多模态遥感语义分割通过利用来自异构数据的互补物理线索来增强场景解释。尽管预训练视觉基础模型（VFMs）提供了强大的通用表示，但将它们应用于多模态任务通常会产生巨大的计算开销，并且容易出现模态不平衡问题，即在优化过程中辅助模态的贡献被抑制。为了应对这些挑战，我们提出了 MoBaNet，这是一个参数高效且模态平衡的对称融合框架。MoBaNet 基于大部分冻结的 VFM 主干网络，采用对称双流架构，在保留可泛化表示的同时，最大限度地减少可训练参数的数量。具体而言，我们设计了一个跨模态提示注入适配器（CPIA），通过生成共享提示并将其注入冻结主干下的瓶颈适配器中，来实现深层语义交互。为了获得用于解码的紧凑且具有判别力的多模态表示，我们进一步引入了差异引导门控融合模块（DGFM），该模块通过显式利用跨模态差异来引导特征选择，从而自适应地融合成对的阶段特征。此外，我们提出了一种模态条件随机掩码（MCRM）策略，通过在训练期间仅对一种模态进行掩码，并在模态特定分支上施加硬像素辅助监督，来缓解模态不平衡。在 ISPRS Vaihingen 和 Potsdam 基准测试上的大量实验表明，MoBaNet 以显著少于全量微调的可训练参数实现了最先进的性能，验证了其在鲁棒且平衡的多模态融合方面的有效性。本工作的源代码可在 https://github.com/sauryeo/MoBaNet 获取。

## Abstract
Multimodal remote sensing semantic segmentation enhances scene interpretation by exploiting complementary physical cues from heterogeneous data. Although pretrained Vision Foundation Models (VFMs) provide strong general-purpose representations, adapting them to multimodal tasks often incurs substantial computational overhead and is prone to modality imbalance, where the contribution of auxiliary modalities is suppressed during optimization. To address these challenges, we propose MoBaNet, a parameter-efficient and modality-balanced symmetric fusion framework. Built upon a largely frozen VFM backbone, MoBaNet adopts a symmetric dual-stream architecture to preserve generalizable representations while minimizing the number of trainable parameters. Specifically, we design a Cross-modal Prompt-Injected Adapter (CPIA) to enable deep semantic interaction by generating shared prompts and injecting them into bottleneck adapters under the frozen backbone. To obtain compact and discriminative multimodal representations for decoding, we further introduce a Difference-Guided Gated Fusion Module (DGFM), which adaptively fuses paired stage features by explicitly leveraging cross-modal discrepancy to guide feature selection. Furthermore, we propose a Modality-Conditional Random Masking (MCRM) strategy to mitigate modality imbalance by masking one modality only during training and imposing hard-pixel auxiliary supervision on modality-specific branches. Extensive experiments on the ISPRS Vaihingen and Potsdam benchmarks demonstrate that MoBaNet achieves state-of-the-art performance with significantly fewer trainable parameters than full fine-tuning, validating its effectiveness for robust and balanced multimodal fusion. The source code in this work is available at https://github.com/sauryeo/MoBaNet.

---

## 论文详细总结（自动生成）

以下是对论文《Parameter-Efficient Modality-Balanced Symmetric Fusion for Multimodal Remote Sensing Semantic Segmentation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：遥感（RS）语义分割面临光照变化、阴影和复杂背景的挑战。引入多模态数据（如RGB+DSM/SAR）可以提供互补的物理线索，增强场景解释的鲁棒性。
*   **核心问题**：
    1.  **计算开销大**：现有的视觉基础模型（VFM）参数量巨大，全量微调（Full Fine-tuning）成本极高。
    2.  **模态不平衡**：在优化过程中，模型往往过度依赖信息丰富的RGB模态，导致辅助模态（如深度信息）的贡献被抑制，形成“RGB主导”的捷径解。
    3.  **交互不足**：在冻结主干网络进行参数高效微调（PEFT）时，不同模态间的深层语义交互难以实现。

### 2. 方法论
论文提出了 **MoBaNet** 框架，核心思想是在保持主干网络冻结的前提下，通过对称双流架构实现高效且平衡的融合。
*   **对称双流架构**：采用共享权重的冻结VFM（如DINOv2或SAM）作为主干，仅微调极少数轻量化模块。
*   **跨模态提示注入适配器 (CPIA)**：在每个阶段前生成共享的语义提示（Prompts），并将其注入到瓶颈适配器（Adapter）中，在不破坏预训练特征空间的情况下实现深层语义交互。
*   **差异引导门控融合模块 (DGFM)**：显式计算两模态特征的差异（Discrepancy Cue），利用门控机制自适应地选择和融合特征，抑制噪声并强化互补信息。
*   **模态条件随机掩码 (MCRM)**：一种训练策略，在训练时随机对其中一种模态进行局部掩码，并对融合失败的“硬像素”施加辅助监督，强制模型学习辅助模态的判别性特征。

### 3. 实验设计
*   **数据集**：
    *   **ISPRS Vaihingen**：典型小城市区域，包含NIR-R-G和DSM数据。
    *   **ISPRS Potsdam**：历史城市中心，包含更高分辨率的RGB-NIR和DSM数据。
*   **Benchmark 与对比方法**：
    *   **指标**：总体精度（OA）、平均F1分数（mF1）、平均交并比（mIoU）。
    *   **对比对象**：包括单模态CNN（U-Net, PSPNet）、单模态Transformer（DC-Swin, UNetFormer）以及多模态融合网络（vFuseNet, CMFNet, FTransUNet, MANet等）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 4090 GPU (24GB)**。
*   **训练细节**：使用 AdamW 优化器，初始学习率 $3 \times 10^{-4}$，Batch Size 为 12（ViT-L 为 6），训练 50 个 Epoch，每个 Epoch 1000 次迭代。
*   **参数量**：可训练参数仅为 **6.18M**（以ViT-B为例），远低于全量微调。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流公开基准数据集上进行了详尽测试。
*   **消融实验**：分别验证了 CPIA、DGFM 和 MCRM 三个核心组件的有效性。
*   **鲁棒性测试**：专门设计了“模态缺失”推理实验，验证模型在失去DSM输入时是否仍能保持性能，证明了 MCRM 缓解模态依赖的效果。
*   **复杂度分析**：对比了参数量、推理延迟（Latency）、每秒帧数（FPS）和内存占用。
*   **评价**：实验设计客观、公平，对比基准涵盖了从经典到最新的SOTA模型，数据量充足，结论具有说服力。

### 6. 主要结论与发现
*   **性能领先**：MoBaNet 在两个数据集上均达到了 SOTA 性能（如在 Potsdam 上 mIoU 达到 88.03%）。
*   **参数高效**：仅需极少的可训练参数即可超越全量微调的多模态模型。
*   **平衡性提升**：MCRM 策略显著降低了模型对单一模态的依赖，在模态缺失的情况下，性能下降幅度远小于基准模型（下降 1.17% vs 23.06%）。
*   **通用性**：该框架在 DINOv2 和 SAM 等不同 VFM 主干上均表现稳定。

### 7. 优点（亮点）
*   **创新的交互机制**：CPIA 通过提示注入解决了冻结主干下跨模态交互难的问题。
*   **显式差异引导**：DGFM 利用模态差异作为引导信号，比盲目的拼接或加权融合更具物理意义。
*   **训练策略有效**：MCRM 是一种“即插即用”的训练技巧，不增加推理成本却能显著提升鲁棒性。

### 8. 不足与局限
*   **模态覆盖有限**：目前主要验证了 RGB+DSM 组合，对于 SAR 或多光谱数据的泛化能力尚待进一步验证。
*   **细小物体挑战**：尽管性能提升明显，但在处理极小物体（如汽车）和类别边缘（如低矮植被与树木）时仍存在一定的混淆。
*   **跨域验证不足**：论文未深入探讨跨城市、跨传感器的迁移学习表现。

（完）
