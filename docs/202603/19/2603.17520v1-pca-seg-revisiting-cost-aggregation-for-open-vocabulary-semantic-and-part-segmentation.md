---
title: "PCA-Seg: Revisiting Cost Aggregation for Open-Vocabulary Semantic and Part Segmentation"
title_zh: PCA-Seg：重新审视开放词汇语义与部分分割中的代价聚合
authors: "Jianjian Yin, Tao Chen, Yi Chen, Gensheng Pei, Xiangbo Shu, Yazhou Yao, Fumin Shen"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17520v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用视觉语言模型进行开放词汇语义分割
tldr: 本研究针对开放词汇语义与部分分割（OSPS）中现有方法因串行聚合导致语义与空间上下文知识干扰的问题，提出了并行代价聚合框架PCA-Seg。该框架通过专家驱动感知学习（EPL）模块整合语义与上下文流，并利用特征正交解耦（FOD）策略减少冗余。实验表明，PCA-Seg在八个基准测试上达到了领先水平，且参数量增加极小，有效提升了视觉语言对齐信息的捕获能力。
selection_source: fresh_fetch
motivation: 现有方法在处理代价体积时采用串行聚合结构，导致类别语义与空间上下文之间产生知识干扰。
method: 提出并行代价聚合（PCA-Seg）范式，结合专家驱动感知学习模块与特征正交解耦策略来捕获丰富的对齐信息。
result: 在八个基准测试中均取得SOTA性能，且每个并行块仅需增加0.35M的极少量参数。
conclusion: PCA-Seg通过并行结构和特征解耦有效缓解了知识干扰，为开放词汇分割任务提供了一种高效且鲁棒的解决方案。
---

## 摘要
视觉语言模型（VLMs）的最新进展在开放词汇语义与部分分割（OSPS）领域引起了广泛关注。然而，现有方法通过空间和类别聚合的串行结构从代价体（cost volumes）中提取图文对齐线索，导致类别级语义与空间上下文之间存在知识干扰。因此，本文提出了一种简单而有效的并行代价聚合（PCA-Seg）范式来缓解上述挑战，使模型能够从代价体中捕获更丰富的视觉语言对齐信息。具体而言，我们设计了一个专家驱动的感知学习（EPL）模块，该模块能够高效地整合语义流和上下文流。它包含一个多专家解析器，用于从多个视角提取互补特征。此外，还设计了一个系数映射器，用于自适应地学习每个特征的像素级权重，从而将互补知识整合到统一且鲁棒的特征嵌入中。此外，我们提出了一种特征正交化解耦（FOD）策略，以减轻语义流和上下文流之间的冗余，这使得 EPL 模块能够从正交化特征中学习多样化的知识。在八个基准测试上的广泛实验表明，PCA-Seg 中的每个并行块仅增加了 0.35M 参数，同时实现了最先进的 OSPS 性能。

## Abstract
Recent advances in vision-language models (VLMs) have garnered substantial attention in open-vocabulary semantic and part segmentation (OSPS). However, existing methods extract image-text alignment cues from cost volumes through a serial structure of spatial and class aggregations, leading to knowledge interference between class-level semantics and spatial context. Therefore, this paper proposes a simple yet effective parallel cost aggregation (PCA-Seg) paradigm to alleviate the above challenge, enabling the model to capture richer vision-language alignment information from cost volumes. Specifically, we design an expert-driven perceptual learning (EPL) module that efficiently integrates semantic and contextual streams. It incorporates a multi-expert parser to extract complementary features from multiple perspectives. In addition, a coefficient mapper is designed to adaptively learn pixel-specific weights for each feature, enabling the integration of complementary knowledge into a unified and robust feature embedding. Furthermore, we propose a feature orthogonalization decoupling (FOD) strategy to mitigate redundancy between the semantic and contextual streams, which allows the EPL module to learn diverse knowledge from orthogonalized features. Extensive experiments on eight benchmarks show that each parallel block in PCA-Seg adds merely 0.35M parameters while achieving state-of-the-art OSPS performance.

---

## 论文详细总结（自动生成）

这篇论文提出了 **PCA-Seg**，一种针对开放词汇语义分割（OVSS）和部分分割（OVPS）的并行代价聚合框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：开放词汇分割旨在识别图像中任意类别的物体或部分。现有主流方法（如 CAT-Seg, DeCLIP）通常采用**串行结构**进行代价聚合（Cost Aggregation），即先进行空间聚合（捕获上下文）再进行类别聚合（捕获语义），或者反之。
*   **核心问题**：论文指出串行结构会导致**知识干扰（Knowledge Interference）**。前一阶段的聚合偏差会传递并放大到下一阶段，导致语义信息与空间结构信息的混淆。例如，空间聚合可能会扭曲特定类别的语义特征，导致最终分类错误。

### 2. 方法论：核心思想与关键技术
PCA-Seg 的核心思想是将串行的代价聚合改为**并行架构**，并引入解耦与融合机制：
*   **并行代价聚合（Parallel Cost Aggregation）**：同时独立地进行空间聚合（Spatial Aggregation）和类别聚合（Class Aggregation），生成两个独立的特征流。
*   **专家驱动感知学习（EPL）模块**：
    *   **多专家解析器（Multi-Expert Parser）**：使用多个并行的卷积专家块从不同视角提取互补特征。
    *   **系数映射器（Coefficient Mapper）**：通过轻量级网络学习像素级的权重系数，对各专家的输出进行加权融合，生成最终的鲁棒嵌入。
*   **特征正交化解耦（FOD）策略**：
    *   为了确保并行流学习到的是互补而非冗余的信息，提出**正交化损失函数（$L_{od}$）**。
    *   该损失通过最小化空间特征与语义特征之间的余弦相似度平方，强制两者在特征空间中趋于正交，从而实现真正的知识解耦。

### 3. 实验设计
*   **数据集**：
    *   **语义分割（OVSS）**：在 COCO-Stuff 上训练，在 ADE20K (A-847, A-150)、PASCAL-Context (PC-459, PC-59)、PASCAL VOC (PAS-20, PAS-20b) 等 6 个基准上评估。
    *   **部分分割（OVPS）**：在 Pascal-Part-116 和 ADE20K-Part-234 上评估。
*   **对比方法**：对比了包括 CAT-Seg, DeCLIP, H-CLIP, PartCATSeg, OVSeg 等在内的多种 SOTA（最先进）方法。
*   **评估指标**：采用 mIoU（平均交并比）和 h-IoU（见过的和未见过类别的调和平均值）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A6000 GPU**。
*   **训练配置**：
    *   OVSS 任务：使用 ViT-B/16 和 ViT-L/14 主干网络，训练 80k 次迭代。
    *   OVPS 任务：训练 20k 至 30k 次迭代。
    *   优化器：AdamW，学习率在 $2\times10^{-4}$ 到 $4\times10^{-4}$ 之间。

### 5. 实验数量与充分性
*   **实验规模**：论文在 **8 个主流基准数据集**上进行了广泛测试，涵盖了从粗粒度语义到细粒度部分的多种场景。
*   **消融实验**：非常充分。作者对比了串行 vs 并行结构、EPL 模块各组件的有效性、专家数量（Z=4 最优）、FOD 损失权重（$\lambda=0.01$ 最优）以及不同融合策略的对比。
*   **客观性**：通过 CCA（典型相关分析）量化了专家间的冗余度，并可视化了特征解耦的过程，实验论证逻辑严密且客观。

### 6. 主要结论与发现
*   **性能领先**：PCA-Seg 在所有 8 个基准测试中均达到了 SOTA 性能。例如，在 A-150 数据集上比之前的 SOTA 提高了 1.8% mIoU。
*   **高效性**：尽管引入了并行分支和多专家模块，但每个并行块仅增加 **0.35M 参数**，计算开销（FLOPs）和显存增加在可接受范围内。
*   **解耦价值**：实验证明，强制特征正交化（FOD）能显著提升模型对细粒度信息的捕获能力。

### 7. 优点
*   **架构创新**：打破了代价聚合中惯用的串行思维，提出了更合理的并行解耦范式。
*   **即插即用**：EPL 模块和 FOD 策略设计简洁，易于集成到现有的视觉语言模型微调框架中。
*   **泛化能力强**：在语义分割和更难的部分分割任务上均表现出色，证明了其在不同粒度下的鲁棒性。

### 8. 不足与局限
*   **推理延迟**：虽然参数量增加较少，但并行结构和多专家解析器导致单张图像的推理时间略有增加（从 0.11s 增至 0.19s 每块），在实时性要求极高的场景下可能受限。
*   **超参数敏感性**：FOD 损失的权重 $\lambda$ 需要精细调优，不同任务下可能存在最优值的偏移。
*   **依赖预训练模型**：性能高度依赖于 CLIP 等基础模型的质量，对于基础模型未覆盖的极冷门领域，提升空间可能受限。

（完）
