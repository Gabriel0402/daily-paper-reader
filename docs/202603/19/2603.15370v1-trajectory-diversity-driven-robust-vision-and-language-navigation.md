---
title: Trajectory-Diversity-Driven Robust Vision-and-Language Navigation
title_zh: 轨迹多样性驱动的鲁棒视觉语言导航
authors: "Jiangyang Li, Cong Wan, SongLin Dong, Chenhao Ding, Qiang Wang, Zhiheng Ma, Yihong Gong"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15370v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于视觉语言导航智能体的强化学习框架
tldr: 视觉语言导航（VLN）目前主要依赖模仿学习，但在泛化性和鲁棒性方面表现不足。本文提出NavGRPO框架，利用组相对策略优化（GRPO）进行强化学习，通过探索多样化轨迹并在组内比较性能，使智能体在无需额外价值网络的情况下学习目标导向的导航策略。该方法在R2R和REVERIE基准测试中显著提升了性能，尤其在应对执行扰动时表现出极强的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的模仿学习方法在面对环境变化和执行扰动时缺乏泛化能力和鲁棒性。
method: 提出基于组相对策略优化的NavGRPO框架，通过组内性能比较和多样化轨迹探索来优化导航策略。
result: "在R2R和REVERIE测试集中分别实现了3.0%和1.71%的SPL提升，并在极端扰动下比基准模型高出14.89%的SPL。"
conclusion: 目标导向的强化学习训练能够显著增强视觉语言导航智能体的鲁棒性和泛化性能。
---

## 摘要
视觉语言导航（VLN）要求智能体遵循自然语言指令在写实环境中进行导航。目前的方法主要依赖于模仿学习，这导致其泛化能力有限，且对执行扰动的鲁棒性较差。我们提出了 NavGRPO，这是一个通过组相对策略优化（Group Relative Policy Optimization）学习目标导向导航策略的强化学习框架。通过探索多样化的轨迹并利用组内性能比较进行优化，我们的方法使智能体能够在不需要额外价值网络的情况下，识别出专家路径之外的有效策略。基于 ScaleVLN，NavGRPO 在 R2R 和 REVERIE 基准测试中表现出卓越的鲁棒性，在未知环境中 SPL 分别提升了 3.0% 和 1.71%。在极端的早期扰动下，我们证明了其相比基线模型有 +14.89% 的 SPL 增益，证实了目标导向的强化学习训练能构建出鲁棒性显著增强的导航策略。代码和模型将会开源。

## Abstract
Vision-and-Language Navigation (VLN) requires agents to navigate photo-realistic environments following natural language instructions. Current methods predominantly rely on imitation learning, which suffers from limited generalization and poor robustness to execution perturbations. We present NavGRPO, a reinforcement learning framework that learns goal-directed navigation policies through Group Relative Policy Optimization. By exploring diverse trajectories and optimizing via within-group performance comparisons, our method enables agents to distinguish effective strategies beyond expert paths without requiring additional value networks. Built on ScaleVLN, NavGRPO achieves superior robustness on R2R and REVERIE benchmarks with +3.0% and +1.71% SPL improvements in unseen environments. Under extreme early-stage perturbations, we demonstrate +14.89% SPL gain over the baseline, confirming that goal-directed RL training builds substantially more robust navigation policies. Code and models will be released.

---

## 论文详细总结（自动生成）

以下是对论文《Trajectory-Diversity-Driven Robust Vision-and-Language Navigation》的深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：视觉语言导航（VLN）要求智能体根据自然语言指令在3D真实场景中导航。
*   **核心痛点**：目前主流方法依赖**模仿学习（Imitation Learning, IL）**。IL存在两个致命缺陷：
    1.  **泛化能力差**：智能体过度拟合专家路径，难以应对未见过的环境。
    2.  **鲁棒性极低**：存在“分布偏移”问题。一旦智能体因扰动偏离专家路径，由于训练中未见过失败/偏离轨迹，它往往无法纠错或重新规划，导致任务失败。
*   **研究动机**：通过强化学习（RL）让智能体探索多样化的轨迹（包括成功和失败），学习如何从错误中恢复，从而构建目标导向且鲁棒的导航策略。

### 2. 方法论
论文提出了 **NavGRPO** 框架，其核心思想是利用**组相对策略优化（Group Relative Policy Optimization, GRPO）**。
*   **核心思想**：不再依赖绝对的专家示范，而是通过在同一指令下采样多条轨迹，进行组内性能比较来优化策略。
*   **关键技术细节**：
    1.  **组采样（Group Sampling）**：针对每个指令采样 $K$ 条独立轨迹，涵盖多样化的导航行为。
    2.  **组相对优势估计**：取消了传统强化学习中的价值网络（Critic），通过计算组内奖励的均值来估计优势函数（$\hat{A}_k = r_k - \text{mean}(\{r_1, ..., r_K\})$），降低了训练不稳定性。
    3.  **复合奖励函数**：
        *   **导航成功奖励**：采用指数衰减函数，强调精准到达目标。
        *   **路径效率奖励**：惩罚冗余的绕路行为。
        *   **步级进度系数**：利用空间度量量化每一步的进展，引导智能体在失败轨迹中也能识别出“正确的局部决策”。
    4.  **硬案例回放（Hard Case Replay）**：当某组采样全部失败时，触发有针对性的监督微调（SFT），防止智能体在困难指令上迷失方向。

### 3. 实验设计
*   **数据集/场景**：
    *   **R2R**：细粒度逐步指令导航。
    *   **REVERIE**：高层目标导向指令（涉及物体识别）。
    *   **R2R-CE**：连续环境下的导航（测试跨环境迁移能力）。
*   **Benchmark与对比方法**：
    *   以 **ScaleVLN** 和 **DUET** 为主要基准模型。
    *   对比了包括 RCM, HAMT, SEvol, BEVBert, GOAT 等在内的多种 SOTA 方法。
*   **鲁棒性测试**：设计了“全局随机扰动”和“早期强制错误扰动”两种极端场景，模拟传感器误差或初始决策失误。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A800 GPU**。
*   **算力开销**：
    *   训练步数：200k 步。
    *   内存占用：$K=4$ 时约 14.8GB，$K=8$ 时约 28.7GB。
    *   训练时长：每 1k 步耗时约 46-86 分钟（随采样数 $K$ 增加而增加）。
*   **优化**：采用了解耦的采样-训练架构，缓存视觉特征以减少重复计算，从而在多轨迹采样下保持了较好的内存效率。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流数据集上进行了验证，并针对奖励函数组件、GRPO 变体、采样数量 $K$、不同 RL 算法（PPO, A2C, REINFORCE）进行了详尽的消融实验。
*   **充分性评价**：实验设计非常充分且客观。通过引入扰动实验，直观地证明了 RL 相比 IL 在鲁棒性上的代差。使用了三个随机种子取平均值，确保了结果的可靠性。

### 6. 主要结论与发现
*   **性能提升**：NavGRPO 在 R2R 和 REVERIE 的未知环境中分别实现了 **+3.0%** 和 **+1.71%** 的 SPL 提升。
*   **鲁棒性飞跃**：在极端早期扰动下，NavGRPO 比基准模型高出 **14.89%** 的 SPL，证明了其强大的纠错和重新规划能力。
*   **算法优越性**：GRPO 变体（特别是 Dr.GRPO）在 VLN 任务中优于传统的 PPO，因为它通过组内比较提供了更稳定的梯度，且无需复杂的价值网络调优。

### 7. 优点
*   **架构简洁**：无需 Critic 网络，解决了 VLN 高维动作空间中价值函数难以收敛的问题。
*   **纠错能力**：通过多样化轨迹探索，智能体学会了“如果走错了该怎么办”，这是传统 IL 方法无法提供的。
*   **训练稳定**：结合了 SFT 和 RL 的优势，通过硬案例回放机制平衡了探索与利用。

### 8. 不足与局限
*   **基座依赖**：NavGRPO 的表现高度依赖于预训练视觉语言模型的质量，无法完全消除基座模型固有的视觉或语言理解偏差。
*   **计算成本**：尽管进行了内存优化，但相比纯 IL 训练，多轨迹采样仍然显著增加了训练的时间成本。
*   **复杂推理瓶颈**：在处理具有高度空间歧义（如“左边第二个房间”）的指令时，智能体仍可能出现推理错误。

（完）
