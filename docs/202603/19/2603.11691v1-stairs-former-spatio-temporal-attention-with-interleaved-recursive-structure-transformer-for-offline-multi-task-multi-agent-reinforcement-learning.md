---
title: "STAIRS-Former: Spatio-Temporal Attention with Interleaved Recursive Structure Transformer for Offline Multi-task Multi-agent Reinforcement Learning"
title_zh: STAIRS-Former：用于离线多任务多智能体强化学习的交错递归结构时空注意力 Transformer
authors: "Jiwon Jeon, Myungsik Cho, Youngchul Sung"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11691v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于离线多任务多智能体强化学习的Transformer架构
tldr: 针对离线多任务多智能体强化学习中智能体数量多变及长时序依赖建模不足的问题，本文提出 STAIRS-Former。该模型通过交错递归结构的 Transformer 引入空间和时间层级注意力机制，有效捕捉智能体间的协作关系与长程历史信息。结合令牌丢弃技术增强泛化性，在 SMAC-v2 等多个基准测试中达到 SOTA 性能。
selection_source: fresh_fetch
motivation: 现有的离线多智能体强化学习方法在处理变动智能体数量和长时序依赖时，存在注意力机制利用不足及历史信息捕捉受限的问题。
method: 提出一种具有空间和时间层级结构的 Transformer 架构，通过交错递归结构和令牌丢弃技术来优化智能体间的协调与长时序建模。
result: 在 SMAC、SMAC-v2、MPE 和 MaMuJoCo 等多个复杂的多智能体基准测试中，该方法显著优于现有模型并取得了新的最先进性能。
conclusion: STAIRS-Former 通过创新的时空注意力机制，有效提升了离线多任务多智能体强化学习的鲁棒性、泛化能力和长时序决策水平。
---

## 摘要
由于不同任务中智能体数量的变化以及对未知场景泛化的需求，利用多任务数据集进行离线多智能体强化学习（MARL）具有挑战性。先前的研究采用具有观测标记化和分层技能学习的 Transformer 来解决这些问题。然而，它们未能充分利用 Transformer 的注意力机制进行智能体间的协作，且依赖于单一的历史标记，这限制了它们在部分可观测 MARL 环境中捕捉长时程时间依赖关系的能力。在本文中，我们提出了 STAIRS-Former，这是一种增强了空间和时间层级的 Transformer 架构，能够在捕捉长交互历史的同时，实现对关键标记的有效关注。我们进一步引入了标记丢弃（token dropout），以增强在不同智能体群体规模下的鲁棒性和泛化能力。在包括 SMAC、SMAC-v2、MPE 和 MaMuJoCo 在内的多种多智能体基准测试及多任务数据集上的广泛实验表明，STAIRS-Former 一贯优于先前的方法，并达到了新的最先进性能。

## Abstract
Offline multi-agent reinforcement learning (MARL) with multi-task datasets is challenging due to varying numbers of agents across tasks and the need to generalize to unseen scenarios. Prior works employ transformers with observation tokenization and hierarchical skill learning to address these issues. However, they underutilize the transformer attention mechanism for inter-agent coordination and rely on a single history token, which limits their ability to capture long-horizon temporal dependencies in partially observable MARL settings. In this paper, we propose STAIRS-Former, a transformer architecture augmented with spatial and temporal hierarchies that enables effective attention over critical tokens while capturing long interaction histories. We further introduce token dropout to enhance robustness and generalization across varying agent populations. Extensive experiments on diverse multi-agent benchmarks, including SMAC, SMAC-v2, MPE, and MaMuJoCo, with multi-task datasets demonstrate that STAIRS-Former consistently outperforms prior methods and achieves new state-of-the-art performance.

---

## 论文详细总结（自动生成）

以下是对论文《STAIRS-Former: Spatio-Temporal Attention with Interleaved Recursive Structure Transformer for Offline Multi-task Multi-agent Reinforcement Learning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：离线多智能体强化学习（Offline MARL）旨在从静态数据中学习策略，避免昂贵的在线交互。但在**多任务（Multi-task）**场景下，面临两大挑战：
    1.  **智能体数量多变**：不同任务中智能体和实体的数量不同，需要架构具备极强的可扩展性。
    2.  **泛化性与长时序依赖**：在部分可观测环境（POMDP）中，现有方法（如基于 UPDeT 的架构）往往只使用浅层 Transformer 和单一历史标记，导致注意力分布过于均匀（无法聚焦关键实体），且难以捕捉长程历史信息。
*   **核心问题**：如何增强 Transformer 架构，使其在处理变动实体数量的同时，能更有效地建模实体间的复杂空间关系和长时序时间依赖？

### 2. 方法论：STAIRS-Former
STAIRS-Former 引入了空间和时间层级结构，核心组件包括：
*   **空间递归模块（Spatial Recursive Module）**：
    *   采用递归深度 Transformer（Spatial-Former）。通过在每一层内进行多次递归更新（权重共享），在不显著增加参数量的情况下加深模型，强化实体间的关系推理。
*   **层级时间模块（Temporal Module）**：
    *   **双尺度历史状态**：维护一个每步更新的低级历史标记（$h_L$）和一个每隔 $T_H$ 步由 GRU 更新的高级历史标记（$h_H$）。
    *   **时间特征学习（TFL）**：在注意力层后，为观测标记和历史标记分别设置独立的 FFN（前馈网络），防止空间关系和时间抽象特征相互干扰。
*   **令牌丢弃机制（Token-Dropout）**：
    *   在训练期间随机丢弃部分实体令牌（自身信息和历史信息除外）。这迫使模型不依赖于特定数量的实体，从而提高对未知任务（实体数量变化）的鲁棒性。
*   **训练算法**：基于 TD3+BC 目标函数，结合 Qatten 混合网络进行值分解，平衡 Q 值优化与行为克隆（BC）正则化。

### 3. 实验设计
*   **数据集与场景**：
    *   **SMAC & SMAC-v2**：星际争霸 II 挑战赛，包括 Marine-Easy/Hard、Stalker-Zealot 等任务集，涵盖同构/异构单位。
    *   **MPE**：协同导航（Cooperative Navigation）。
    *   **MaMuJoCo**：多智能体连续控制任务（HalfCheetah）。
*   **数据质量**：涵盖 Expert、Medium、Medium-Expert、Medium-Replay 四种质量。
*   **基准方法（Benchmarks）**：
    *   **UPDeT-m**：基础 Transformer 离线版本。
    *   **ODIS**：基于任务无关协调技能的方法。
    *   **HiSSD**：当前最先进的（SOTA）层级技能学习方法。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 GPU** (24GB 显存)。
*   **训练效率**：
    *   在 SMAC 任务上，训练 50k 步约需 **4 小时**。
    *   相比之下，SOTA 方法 HiSSD 需要约 7 小时 20 分钟。
*   **参数量**：STAIRS-Former 约为 **22 万**参数，远低于 HiSSD 的 68 万参数，但在性能上更优。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个大类基准测试、数十个子任务（包括已见和未见任务）上进行了测试。
*   **消融实验**：非常充分。分别验证了空间递归、时间层级、令牌丢弃、TFL 模块以及超参数（如递归步数、丢弃率）的影响。
*   **客观性**：所有实验均采用 **5 个随机种子**取平均值并标注标准差。
*   **深度分析**：引入了**休眠神经元分析（Dormant Neuron Analysis）**和**注意力热图可视化**，从模型内部机理和实际战术行为（如集火、拉扯）解释了性能提升的原因。

### 6. 主要结论与发现
*   **性能领先**：STAIRS-Former 在几乎所有 SMAC 和 SMAC-v2 任务中均显著优于 HiSSD 和 ODIS，尤其在未见任务（Unseen Tasks）上的泛化表现突出。
*   **空间层级的重要性**：空间递归模块对已见任务的性能提升最明显（提升实体间协作建模）。
*   **协同效应**：在未见任务上，空间、时间模块和令牌丢弃三者缺一不可，共同构成了模型的泛化能力。
*   **注意力质量**：可视化证明 STAIRS-Former 能随时间动态调整注意力（如优先攻击残血敌人），而传统 Transformer 的注意力往往是均匀且静态的。

### 7. 优点
*   **架构创新**：通过交错递归结构巧妙地解决了 Transformer 在 MARL 中“注意力不集中”的问题。
*   **高效性**：以更少的参数量和更短的训练时间实现了超越复杂层级技能模型（HiSSD）的效果。
*   **强解释性**：通过注意力图谱展示了模型学习到的高级战术，增强了黑盒模型的透明度。
*   **泛化能力**：Token-Dropout 简单有效，显著提升了跨任务的鲁棒性。

### 8. 不足与局限
*   **数据依赖性**：在极低质量或多样性极差的数据集（如某些 Medium-Replay 场景）上，提升幅度受限。
*   **超参数敏感度**：虽然实验证明了鲁棒性，但递归步数 $\nu$ 和时间间隔 $T_H$ 仍需根据任务复杂度进行微调。
*   **应用限制**：目前主要在离散动作空间（SMAC）和部分连续空间（MaMuJoCo）验证，对于极大规模智能体（如数百个）的计算开销仍待进一步评估。

（完）
