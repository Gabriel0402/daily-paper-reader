---
title: SAMSEM -- A Generic and Scalable Approach for IC Metal Line Segmentation
title_zh: SAMSEM —— 一种通用且可扩展的集成电路金属线分割方法
authors: "Christian Gehrmann, Jonas Ricker, Simon Damm, Deruo Cheng, Julian Speith, Yiqiong Shi, Asja Fischer, Christof Paar"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16548v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 将SAM2模型应用于集成电路金属线分割
tldr: 针对集成电路（IC）硬件安全验证中金属线分割的泛化性难题，本文提出SAMSEM。该方法基于Meta的SAM2模型，引入多尺度分割策略和拓扑损失函数，利用涵盖14种不同IC的大规模数据集进行微调。SAMSEM在跨工艺节点、材料和成像技术的场景下表现出卓越的泛化能力和极低的错误率，显著提升了IC后期验证的效率。
selection_source: fresh_fetch
motivation: 现有的IC金属线分割方法难以适应不同制造工艺和成像条件，导致模型在不同芯片间缺乏泛化能力。
method: 基于SAM2模型开发了多尺度分割方法，并引入拓扑损失函数以优化电气连通性而非仅关注像素级精度。
result: "在包含14种IC的数据集上，SAMSEM对已知芯片的错误率低至0.72%，对完全未见过的芯片错误率为5.53%。"
conclusion: SAMSEM作为一种通用且可扩展的工具，有效解决了IC制造后验证中金属线分割的关键挑战。
---

## 摘要
在全球化硬件供应链的背景下，硬件组件的安全性保障引起了广泛关注，特别是在密码学应用和高风险场景中。在集成电路（IC）的扫描电子显微镜（SEM）图像中识别金属线，是验证在不可信环境中制造的芯片是否存在恶意电路的关键步骤。由于制造工艺和技术的差异，此类验证通常需要针对每个目标 IC 调整参数和算法。通常，在某个 IC 图像上训练的机器学习模型无法准确检测其他 IC 上的金属线。为了应对这一挑战，我们通过将 Meta 的 Segment Anything Model 2 (SAM2) 适配到 IC 金属线分割领域，开发了 SAMSEM。具体而言，我们开发了一种多尺度分割方法，能够处理不同尺寸、分辨率和放大倍率的 SEM 图像。此外，我们采用了基于拓扑的损失函数以及基于像素的损失函数，使分割重点关注电气连通性而非像素级精度。基于超参数优化，我们对 SAM2 模型进行了微调，从而获得了一个能够跨不同技术节点、制造材料、样品制备方法和 SEM 成像技术进行泛化的模型。为此，我们利用了一个前所未有的 SEM 图像数据集，该数据集涵盖了 14 种不同 IC 的 48 个金属层。当在 7 个 IC 上进行微调时，SAMSEM 在对这些 IC 的其他图像进行评估时，错误率低至 0.72%。对于剩余的 7 个未见过的 IC，其错误率仍低至 5.53%。最后，当在所有 14 个 IC 上进行微调时，我们观察到的错误率为 0.62%。因此，SAMSEM 被证明是一个可靠的工具，显著推进了金属线分割领域的前沿，而金属线分割是制造后 IC 验证中的一个关键挑战。

## Abstract
In light of globalized hardware supply chains, the assurance of hardware components has gained significant interest, particularly in cryptographic applications and high-stakes scenarios. Identifying metal lines on scanning electron microscope (SEM) images of integrated circuits (ICs) is one essential step in verifying the absence of malicious circuitry in chips manufactured in untrusted environments. Due to varying manufacturing processes and technologies, such verification usually requires tuning parameters and algorithms for each target IC. Often, a machine learning model trained on images of one IC fails to accurately detect metal lines on other ICs. To address this challenge, we create SAMSEM by adapting Meta's Segment Anything Model 2 (SAM2) to the domain of IC metal line segmentation. Specifically, we develop a multi-scale segmentation approach that can handle SEM images of varying sizes, resolutions, and magnifications. Furthermore, we deploy a topology-based loss alongside pixel-based losses to focus our segmentation on electrical connectivity rather than pixel-level accuracy. Based on a hyperparameter optimization, we then fine-tune the SAM2 model to obtain a model that generalizes across different technology nodes, manufacturing materials, sample preparation methods, and SEM imaging technologies. To this end, we leverage an unprecedented dataset of SEM images obtained from 48 metal layers across 14 different ICs. When fine-tuned on seven ICs, SAMSEM achieves an error rate as low as 0.72% when evaluated on other images from the same ICs. For the remaining seven unseen ICs, it still achieves error rates as low as 5.53%. Finally, when fine-tuned on all 14 ICs, we observe an error rate of 0.62%. Hence, SAMSEM proves to be a reliable tool that significantly advances the frontier in metal line segmentation, a key challenge in post-manufacturing IC verification.

---

## 论文详细总结（自动生成）

以下是对论文《SAMSEM -- A Generic and Scalable Approach for IC Metal Line Segmentation》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：在硬件安全验证（Hardware Assurance）中，为了确保集成电路（IC）不含恶意电路（如硬件木马），需要通过扫描电子显微镜（SEM）对芯片进行逐层成像并提取网表。
*   **核心问题**：金属线分割是提取网表的关键步骤。然而，由于制造工艺、材料、缩放倍率及成像设置的差异，现有的分割模型泛化性极差。通常在 A 芯片上训练的模型在 B 芯片上表现糟糕，导致研究人员必须为每个新芯片手动标注数据并重新训练，效率极低。
*   **研究目标**：开发一种通用、可扩展且具备强泛化能力的金属线分割工具，使其无需针对新芯片进行重新训练即可实现高精度分割。

### 2. 方法论
SAMSEM 基于 Meta 的 **Segment Anything Model 2 (SAM2)** 进行了针对性改造，核心思想包括：
*   **多尺度分割架构**：
    *   **Model 1（全局）**：处理原始尺寸图像（缩放至 1024x1024），负责捕捉大尺度结构和全局上下文，防止局部误判。
    *   **Model 2（局部）**：处理 512x512 的图像块（Patch），负责捕捉细微的金属线间隙，减少因下采样导致的短路错误。
    *   **决策算法**：引入基于“斑点检测（Speckle Detection）”的启发式算法，自动判断并融合两个模型的输出，解决局部模型易产生的幻觉（Hallucination）问题。
*   **拓扑感知损失函数**：
    *   除了传统的像素级损失（BCE + Dice），引入了 **Betti Matching Loss**。该损失函数基于持续同调（Persistent Homology），专门惩罚改变电气连通性的错误（如断路或短路），使模型更关注电路的逻辑正确性而非仅仅是像素重合度。
*   **自动提示生成**：利用轻量级图像处理技术（阈值化+去噪）自动生成正向点提示（Point Prompts），实现全自动推理。

### 3. 实验设计
*   **数据集**：使用了前所未有的规模，包含 **14 种不同 IC** 的 48 个金属层，工艺节点跨越 **200nm 至 20nm**。
*   **Benchmark（评价指标）**：主要采用 **ESD（电气显著差异）错误率**，统计断路（Opens）、短路（Shorts）、假阳性（FPs）和假阴性（FNs）的数量。
*   **对比方法**：
    *   **SAMIC**（基于 SAM1 的 IC 分割方法）。
    *   **经典深度学习模型**：U-Net, DeepLabV3, FCN。
*   **实验设置**：将 14 个 IC 分为两组，7 个用于训练/验证（分布内），另外 7 个完全不参与训练（分布外/泛化性测试）。

### 4. 资源与算力
*   **硬件环境**：使用了一个包含 **8 台 Nvidia H100** 加速器的集群。
*   **训练时长**：
    *   在全数据集上进行一次完整的微调（Fine-tuning）在单张 H100 上约需 **72 小时**。
    *   超参数优化（使用 Optuna 框架）持续了约 **7 天**。
    *   消融实验和不同组件的微调时间在 34 到 51 小时不等。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括：
    *   **消融实验**：测试了 SAM2 不同组件（编码器、解码器、提示编码器）对结果的影响。
    *   **分布内/外对比**：验证了模型在已知和未知芯片上的表现。
    *   **训练规模实验**：测试了训练集中 IC 数量（从 1 个到 6 个）对泛化性能的提升曲线。
*   **充分性与公平性**：实验设计非常严谨。通过使用完全未见过的 IC 进行测试，客观地证明了泛化能力。对比模型均在相同的数据集上进行了重新训练和超参优化，确保了对比的公平性。

### 6. 主要结论与发现
*   **卓越的精度**：在分布内测试中，SAMSEM 的 ESD 错误率仅为 **0.72%**，而次优的 U-Net 为 4.44%。
*   **强大的泛化性**：在完全未见过的 7 个 IC 上，SAMSEM 维持了 **5.53%** 的低错误率，而其他所有对比模型（SAMIC, U-Net 等）的错误率均在 24% 以上，甚至高达 44%。
*   **拓扑损失的有效性**：引入 Betti Matching Loss 显著减少了短路和假阳性错误，提升了提取网表的可靠性。
*   **数据多样性至关重要**：实验表明，随着训练集中 IC 种类的增加，模型的泛化误差持续下降且尚未饱和。

### 7. 优点
*   **首创性**：首次将 SAM2 引入 IC 物理安全领域，并解决了大尺寸 SEM 图像的适配问题。
*   **实用性强**：多尺度策略有效解决了“全局上下文”与“局部细节”之间的矛盾。
*   **关注逻辑正确**：通过拓扑损失函数，将计算机视觉任务与电路提取的实际需求（连通性）紧密结合。
*   **开源贡献**：作者开源了模型权重和脚本，为硬件保证研究社区提供了强大的基准工具。

### 8. 不足与局限
*   **Ground Truth 风险**：由于缺乏原始设计文件（GDSII），标注数据依赖人工，可能存在微小的标注偏差，影响模型上限。
*   **计算成本**：SAM2 作为一个大型基础模型，其微调和推理的计算开销远高于 U-Net 等轻量级网络。
*   **样本制备缺陷敏感**：分析发现，严重的去层缺陷（如金属线被意外磨掉）仍会导致模型误判，这属于物理层面的不可抗力。
*   **法律与隐私限制**：受限于版权和法律风险，论文无法公开 14 个 IC 数据集中的大部分原始图像，这在一定程度上限制了完全的复现性。

（完）
