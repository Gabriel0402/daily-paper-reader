---
title: "GigaWorld-Policy: An Efficient Action-Centered World--Action Model"
title_zh: GigaWorld-Policy：一种高效的以动作为中心的世界-动作模型
authors: "Angen Ye, Boyuan Wang, Chaojun Ni, Guan Huang, Guosheng Zhao, Hao Li, Hengtao Li, Jie Li, Jindi Lv, Jingyu Liu, Min Cao, Peng Li, Qiuping Deng, Wenjun Mei, Xiaofeng Wang, Xinze Chen, Xinyu Zhou, Yang Wang, Yifan Chang, Yifan Li, Yukun Zhou, Yun Ye, Zhichao Liu, Zheng Zhu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17240v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于机器人策略学习的以动作为中心的世界动作模型
tldr: 本研究针对现有世界-动作模型（WAM）推理开销大且视觉与动作表示耦合的问题，提出了GigaWorld-Policy。该模型采用以动作为中心的设计，通过因果建模确保动作预测不依赖于未来视频生成，从而实现高效推理。在推理时，视频生成变为可选，显著提升了部署速度。实验表明，该模型在保持高成功率的同时，推理速度比领先基准快9倍，在RoboTwin 2.0任务中表现优异。
selection_source: fresh_fetch
motivation: 现有世界-动作模型因视觉与动作推理耦合导致推理开销巨大且动作预测精度受限于视频生成质量。
method: 提出一种以动作为中心的因果设计，使模型在预测动作的同时可选地生成视频，并利用大规模机器人数据集进行预训练。
result: "在真实机器人平台上，该模型推理速度比Motus快9倍，成功率提升7%，在RoboTwin 2.0上性能提升95%。"
conclusion: GigaWorld-Policy通过解耦动作与视觉生成，实现了高效且高性能的机器人策略学习，为实时世界模型应用提供了新方案。
---

## 摘要
从预训练视频生成骨干网络初始化的世界-动作模型 (World-Action Models, WAM) 在机器人策略学习中展现出巨大潜力。然而，现有方法面临两个阻碍性能和部署的关键瓶颈。首先，对未来视觉动态和相应动作进行联合推理会产生巨大的推理开销。其次，联合建模通常会使视觉和运动表示纠缠在一起，导致运动预测的准确性严重依赖于未来视频预测的质量。为解决这些问题，我们推出了 GigaWorld-Policy，这是一种以动作为中心的 WAM，它在学习 2D 像素-动作动态的同时，实现了高效的动作解码，并支持可选的视频生成。具体而言，我们将策略训练制定为两个耦合的部分：模型在当前观测的条件下预测未来动作序列，并同时在预测动作和相同观测的条件下生成未来视频。该策略受到动作预测和视频生成的共同监督，提供了更丰富的学习信号，并通过视觉动态约束鼓励产生符合物理规律的动作。通过防止未来视频标记 (tokens) 影响动作标记的因果设计，显式的未来视频生成在推理时是可选的，从而在部署期间实现更快的动作预测。为了支持这一范式，我们策划了一个多样化的大规模机器人数据集，用于预训练一个以动作为中心的视频生成模型，随后将其适配为机器人策略学习的骨干网络。在真实机器人平台上的实验结果表明，GigaWorld-Policy 的运行速度比领先的 WAM 基准模型 Motus 快 9 倍，同时任务成功率提高了 7%。此外，与 pi-0.5 相比，GigaWorld-Policy 在 RoboTwin 2.0 上的性能提升了 95%。

## Abstract
World-Action Models (WAM) initialized from pre-trained video generation backbones have demonstrated remarkable potential for robot policy learning. However, existing approaches face two critical bottlenecks that hinder performance and deployment. First, jointly reasoning over future visual dynamics and corresponding actions incurs substantial inference overhead. Second, joint modeling often entangles visual and motion representations, making motion prediction accuracy heavily dependent on the quality of future video forecasts. To address these issues, we introduce GigaWorld-Policy, an action-centered WAM that learns 2D pixel-action dynamics while enabling efficient action decoding, with optional video generation. Specifically, we formulate policy training into two coupled components: the model predicts future action sequences conditioned on the current observation, and simultaneously generates future videos conditioned on the predicted actions and the same observation. The policy is supervised by both action prediction and video generation, providing richer learning signals and encouraging physically plausible actions through visual-dynamics constraints. With a causal design that prevents future-video tokens from influencing action tokens, explicit future-video generation is optional at inference time, allowing faster action prediction during deployment. To support this paradigm, we curate a diverse, large-scale robot dataset to pre-train an action-centered video generation model, which is then adapted as the backbone for robot policy learning. Experimental results on real-world robotic platforms show that GigaWorld-Policy runs 9x faster than the leading WAM baseline, Motus, while improving task success rates by 7%. Moreover, compared with pi-0.5, GigaWorld-Policy improves performance by 95% on RoboTwin 2.0.

---

## 论文详细总结（自动生成）

### GigaWorld-Policy 论文结构化总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对机器人领域中 **Vision-Language-Action (VLA)** 模型面临的两个核心挑战：
*   **监督稀疏性（Supervision Sparsity）：** 传统的 VLA 模型主要依赖稀疏的动作标签进行监督，容易导致模型学习到环境的“快捷方式”而非物理一致的分布。
*   **世界-动作模型（WAM）的推理效率低下：** 虽然引入视频生成作为世界模型可以提供稠密监督，但现有的 WAM 通常要求在推理时同步生成未来视频，导致巨大的计算开销和延迟，且视频预测的误差会传播至动作解码。

**GigaWorld-Policy** 的核心含义是提出一种**以动作为中心（Action-Centered）**的高效模型，在训练时利用未来视觉动态作为稠密监督信号，但在推理时使视频生成变为“可选”，从而兼顾世界模型的理解能力与实时控制的低延迟需求。

#### 2. 论文提出的方法论
*   **核心思想：** 采用因果序列建模（Causal Sequence Model），将动作预测与视觉动力学预测解耦。通过特殊的掩码设计，确保动作预测不依赖于未来视频的生成。
*   **关键技术细节：**
    *   **架构：** 基于 50 亿参数（5B）的扩散 Transformer（Wan 2.2 DiT）。
    *   **因果掩码（Causal Masking）：** 动作 Token 仅关注当前观测和状态；未来视频 Token 则关注当前观测、状态及预测的动作。这种设计允许推理时跳过视频 Token 的生成。
    *   **多视图融合：** 将左、前、右三个视角的图像拼接为复合图像，在不改变模型输入结构的前提下实现跨视图推理。
    *   **训练目标：** 使用**流匹配（Flow Matching）**算法同时优化动作流（Action Flow）和视频流（Video Flow）。
    *   **课程学习（Curriculum Training）：** 
        1.  **初始化：** 大规模互联网视频预训练。
        2.  **具身预训练：** 结合 10,000 小时的机器人视频和人类第一视角视频（Ego4D 等），注入物理先验。
        3.  **后训练（Post-training）：** 在目标机器人的指令-动作轨迹上进行微调。

#### 3. 实验设计
*   **数据集/场景：**
    *   **仿真环境：** RoboTwin 2.0，包含 50 多个具有挑战性的操纵任务（如调整瓶子、堆叠方块、打开笔记本等），并引入了重度领域随机化（背景、光照、杂物）。
    *   **真实世界：** 使用 AgileX PiPER 6自由度机械臂，执行清理桌面、扫描二维码、叠碗、扫垃圾四个复杂任务。
*   **对比方法（Baselines）：**
    *   **VLA 类：** $\pi_{0.5}$、GigaBrain-0、X-VLA。
    *   **WAM 类：** Motus、Cosmos-Policy。

#### 4. 资源与算力
*   **算力消耗：** 论文明确提到在公共数据集上进行了 **6000 GPU 小时** 的预训练。
*   **硬件环境：** 推理延迟测试是在 **NVIDIA A100 GPU** 上完成的。
*   **模型规模：** 核心骨干网络为 5B 参数。

#### 5. 实验数量与充分性
*   **实验规模：** 仿真实验覆盖了 50 多个任务，每个任务 100 次测试；真实世界实验包含 4 个任务，每个任务 20 次尝试。
*   **消融实验：** 论文进行了多项深入的消融研究，包括：
    *   预训练配置的影响（证明了具身数据预训练的必要性）。
    *   未来帧预测数量（$\Delta$ 间隔）对性能的影响。
    *   因果掩码与全注意力掩码的对比。
    *   数据效率分析（证明其在 10% 数据下即可达到基准模型全量数据的效果）。
*   **评价：** 实验设计较为充分，涵盖了从仿真到现实的迁移，且对比了当前最前沿的同类模型，结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **高效性：** GigaWorld-Policy 在推理速度上比领先的 WAM 模型 Motus 快 **9 倍**（单步推理仅需 0.36s）。
*   **高性能：** 在真实世界任务中，成功率比 Motus 高出 7%；在仿真环境 RoboTwin 2.0 中，性能比 $\pi_{0.5}$ 提升了 **95%**。
*   **稠密监督的价值：** 实验证明，即使推理时不生成视频，训练阶段引入的视觉动力学监督也能显著提升动作预测的准确性和物理一致性。

#### 7. 优点
*   **解耦设计：** 巧妙地通过因果掩码解决了 WAM 模型推理延迟高的“顽疾”，使世界模型真正具备了实时部署的可能性。
*   **数据效率：** 强大的预训练策略使其在少量目标数据下表现优异，降低了机器人数据采集的成本。
*   **通用性：** 能够处理多视图输入并保持跨视图的一致性，适应复杂的真实环境。

#### 8. 不足与局限
*   **扩散模型固有开销：** 尽管比其他 WAM 快，但基于扩散过程的流匹配采样依然比纯自回归或判别式模型（如轻量级 VLA）慢，对于极高频（如 >50Hz）的控制场景可能仍有挑战。
*   **视频预测简化：** 为了效率，模型在训练时只预测稀疏的未来帧（每 12 步一帧），这可能在处理极精细、高速的物理交互时丢失部分动态细节。
*   **硬件依赖：** 5B 参数规模对于嵌入式边缘计算设备（如机器人端侧计算盒）仍有较大的显存和算力压力。

（完）
