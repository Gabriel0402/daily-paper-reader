---
title: Interpreting Context-Aware Human Preferences for Multi-Objective Robot Navigation
title_zh: 解释多目标机器人导航中上下文感知的人类偏好
authors: "Tharun Sethuraman, Subham Agrawal, Nils Dengler, Jorge de Heuvel, Teena Hassan, Maren Bennewitz"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17510v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 将 VLM 与强化学习结合用于机器人导航和语义推理
tldr: 本研究针对机器人在人类共享环境中难以理解上下文相关偏好的问题，提出了一种结合基础模型与多目标强化学习（MORL）的导航框架。该系统利用VLM提取环境语义，通过LLM将自然语言反馈转化为可更新的行为规则库，并将其映射为MORL策略的数值偏好向量。该方法实现了高层语义推理与低层实时控制的集成，在真实部署中展现了极高的意图捕捉准确性和行为自适应能力。
selection_source: fresh_fetch
motivation: 传统的机器人导航难以直接将自然语言描述且依赖环境上下文的人类偏好整合到低层运动控制策略中。
method: 结合VLM提取环境语义、LLM生成行为规则，并通过偏好转换模块将规则映射为多目标强化学习策略的参数。
result: 实验和用户研究证明，该系统能准确捕捉用户意图并生成一致的偏好向量，在多种室内环境中实现了灵活的行为自适应。
conclusion: 该框架通过整合高层语义推理与低层控制，显著提升了机器人在共享空间中导航的适应性、透明度和实时响应能力。
---

## 摘要
在人类共享环境中运行的机器人不仅必须实现安全和效率等任务级导航目标，还必须使其行为适应人类偏好。然而，由于人类偏好通常以自然语言表达并依赖于环境上下文，因此很难将其直接集成到低层机器人控制策略中。在这项工作中，我们提出了一套流水线，通过将基础模型与多目标强化学习（MORL）导航策略相结合，使机器人能够理解并应用依赖于上下文的导航偏好。因此，我们的方法将高层语义推理与低层运动控制相结合。视觉语言模型（VLM）从机载视觉观测中提取结构化的环境上下文，而大语言模型（LLM）将自然语言用户反馈转换为可解释的、依赖于上下文的行为规则，并存储在持久但可更新的规则存储器中。随后，偏好转换模块将上下文信息和存储的规则映射为数值偏好向量，这些向量对预训练的 MORL 策略进行参数化，以实现实时导航自适应。我们通过定量的组件级评估、用户研究以及在各种室内环境中的真实机器人部署，对所提出的框架进行了评估。我们的结果表明，该系统能够可靠地捕捉用户意图，生成一致的偏好向量，并在不同上下文中实现可控的行为自适应。总之，所提出的流水线提高了机器人在共享人类环境中运行的自适应性、透明度和可用性，同时保持了安全且响应迅速的实时控制。

## Abstract
Robots operating in human-shared environments must not only achieve task-level navigation objectives such as safety and efficiency, but also adapt their behavior to human preferences. However, as human preferences are typically expressed in natural language and depend on environmental context, it is difficult to directly integrate them into low-level robot control policies. In this work, we present a pipeline that enables robots to understand and apply context-dependent navigation preferences by combining foundational models with a Multi-Objective Reinforcement Learning (MORL) navigation policy. Thus, our approach integrates high-level semantic reasoning with low-level motion control. A Vision-Language Model (VLM) extracts structured environmental context from onboard visual observations, while Large Language Models (LLM) convert natural language user feedback into interpretable, context-dependent behavioral rules stored in a persistent but updatable rule memory. A preference translation module then maps contextual information and stored rules into numerical preference vectors that parameterize a pretrained MORL policy for real-time navigation adaptation. We evaluate the proposed framework through quantitative component-level evaluations, a user study, and real-world robot deployments in various indoor environments. Our results demonstrate that the system reliably captures user intent, generates consistent preference vectors, and enables controllable behavior adaptation across diverse contexts. Overall, the proposed pipeline improves the adaptability, transparency, and usability of robots operating in shared human environments, while maintaining safe and responsive real-time control.

---

## 论文详细总结（自动生成）

### 论文总结：解释多目标机器人导航中上下文感知的人类偏好

#### 1. 核心问题与整体含义（研究动机和背景）
在人类共享的环境中，机器人导航不仅需要完成任务（如避障、到达目标点），还必须遵循社会规范和个人偏好。然而，人类偏好具有**高度的上下文依赖性**（例如：在厨房和卧室的行为要求不同）且通常以**模糊的自然语言**表达。
*   **研究动机**：直接使用大模型（LLM/VLM）进行闭环控制存在高延迟、幻觉风险和安全性难以保障的问题；而传统的强化学习（RL）虽然实时性好，但难以理解复杂的语义偏好。
*   **核心目标**：开发一个混合框架，将基础模型的高层语义推理能力与多目标强化学习（MORL）的低层高效控制相结合，实现既安全、实时又符合人类个性化偏好的导航。

#### 2. 方法论：核心思想与关键技术
该论文提出了一个三阶段的推理流水线，将视觉观测和语言指令转化为控制参数：
*   **上下文预测器 (Context Predictor)**：使用视觉语言模型（VLM，如 Gemini 2.0 Flash）从机载摄像头图像中提取结构化信息，包括房间类型、物体及其距离、是否有人、光照条件等。
*   **规则更新器 (Rule Updater)**：使用 LLM 将用户的自然语言反馈（如“离我远点”）转化为可读的、存储在 JSON 格式规则库中的行为准则。它支持规则的增加、修改和删除，并保持与基础安全目标（避障、到达目标）的一致性。
*   **偏好转换器 (Preference Translator)**：LLM 根据当前上下文和规则库，生成一个四维数值偏好向量 $\lambda = (\lambda_{effic}, \lambda_{Odist}, \lambda_{Hdist}, \lambda_{velocity})$。
*   **低层控制**：该向量 $\lambda$ 直接作为预训练 MORL 策略的输入，动态调整导航行为（如权衡路径效率、与人的距离、与物体的距离和行驶速度），无需重新训练模型。

#### 3. 实验设计
研究采用了多层次的评估方案：
*   **组件评估**：
    *   **VLM 对比**：在 **MIT Indoor Scenes 数据集**上对比了 Gemini、GPT-4o、Pixtral 等模型在房间分类和物体识别上的准确率及推理延迟。
    *   **LLM 对比**：在 **SRIN 数据集**上评估了不同 LLM 生成偏好向量的准确性（与人工标注的 Ground Truth 对比）。
*   **用户研究**：招募 24 名参与者，通过李克特量表（Likert-scale）评估系统生成的规则和解释的可理解性、相关性。
*   **真实机器人部署**：使用 **Toyota HSR 机器人**在三种真实场景（办公室、家庭厨房、超市）中进行实验，验证系统在不同偏好指令下的行为表现。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**训练 MORL 策略所使用的具体 GPU 型号或训练时长。
*   **推理延迟**：文中记录了不同基础模型的推理时间。例如，Gemini-2.0-flash 的平均推理时间约为 2.2 秒，而 Pixtral-large 则需 9.2 秒。这表明高层推理并非每帧执行，而是基于事件或定时更新。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   VLM 评估使用了 70 张图像；LLM 评估使用了 27 个场景。
    *   用户研究包含 24 名样本，具有一定的统计学意义。
    *   真实机器人实验在 3 个迥异的场景中各重复 5 次，并记录了速度、距离、路径长度等定量指标。
*   **充分性评价**：实验设计较为全面，涵盖了从感知、推理到执行的全链路评估。通过与 Baseline（无偏好调节的 RL）对比，客观地展示了偏好向量对行为的调节作用。

#### 6. 主要结论与发现
*   **混合架构的有效性**：基础模型能够准确地将模糊语言转化为结构化规则，而 MORL 策略能根据这些规则实时调整物理行为。
*   **模型表现**：在测试的模型中，**Gemini 2.0 Flash** 在感知准确率和延迟之间达到了最佳平衡；**Mistral-Large-2.1** 在生成可解释规则和准确偏好向量方面表现最优。
*   **行为一致性**：实验证明，机器人能够根据指令在超市避开易碎品、在办公室降低速度、在家庭环境中保持社交距离，表现出良好的上下文适应性。

#### 7. 优点与亮点
*   **透明度与可解释性**：系统不仅生成控制向量，还提供自然语言解释和可查阅的规则库，增强了用户对机器人的信任。
*   **无需重训的自适应**：通过 MORL 的参数化特性，实现了“即插即用”的偏好调整，避免了针对每个新偏好进行微调的成本。
*   **持久化存储**：规则存储器允许机器人记住用户的长期偏好，并随环境变化动态更新。

#### 8. 不足与局限
*   **推理延迟限制**：VLM/LLM 的推理时间（秒级）意味着系统无法对瞬息万变的环境做出即时语义反应（例如突然跑出的小孩），仍需依赖低层 RL 的基础避障。
*   **目标维度有限**：目前仅定义了四个导航目标维度，对于更复杂的社会交互（如递送物品的姿态、礼让顺序）尚不支持。
*   **提示词依赖**：系统的性能高度依赖于 Prompt Engineering（提示词工程），不同模型的 Prompt 敏感度可能导致行为不一致。
*   **环境覆盖**：虽然测试了三种场景，但尚未在极端动态或室外复杂环境中验证其鲁棒性。

（完）
