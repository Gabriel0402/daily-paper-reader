---
title: "AgentVLN: Towards Agentic Vision-and-Language Navigation"
title_zh: AgentVLN：迈向智能体视觉语言导航
authors: "Zihao Xin, Wentong Li, Yixuan Jiang, Ziyuan Huang, Bin Wang, Piji Li, Jianke Zhu, Jie Qin, Shengjun Huang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17670v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 具身导航智能体的VLM大脑范式
tldr: AgentVLN是一个面向具身导航的高效框架，旨在解决视觉语言导航（VLN）中的空间感知受限和2D-3D表征不匹配问题。该框架采用“VLM作为大脑”的范式，通过解耦语义推理与感知规划，并引入跨空间表征映射、主动探索策略及查询驱动的感知思维链（QD-PCoT），显著提升了长程导航的准确性与鲁棒性，为边缘计算平台上的轻量化部署提供了新方案。
selection_source: fresh_fetch
motivation: 现有的VLN系统在处理复杂指令时面临空间感知不足、2D与3D表征不一致以及单目尺度模糊等挑战。
method: 提出一种基于POSMDP的框架，利用VLM进行高层推理，并通过跨空间映射、自纠错机制及感知思维链实现精准的路径规划与环境探索。
result: 在长程VLN基准测试中，AgentVLN一致优于现有的最先进方法，并展示了在边缘计算平台上的轻量化部署潜力。
conclusion: AgentVLN为下一代具身导航模型提供了一个实用的范式，通过解耦推理与感知，有效解决了非结构化环境中的导航难题。
---

## 摘要
视觉语言导航（VLN）要求具身智能体在未知环境中将复杂的自然语言指令转化为长程导航任务。尽管视觉语言模型（VLM）提供了强大的2D语义理解能力，但目前的VLN系统仍受限于有限的空间感知、2D-3D表示不匹配以及单目尺度二义性。在本文中，我们提出了AgentVLN，这是一种新颖且高效的具身导航框架，可部署在边缘计算平台上。我们将VLN建模为部分可观测半马尔可夫决策过程（POSMDP），并引入了“VLM作为大脑”的范式，通过即插即用的技能库将高层语义推理与感知和规划解耦。为了解决多层级表示的不一致性，我们设计了一种跨空间表示映射，将感知层的3D拓扑路标投影到图像平面，从而为VLM生成像素对齐的视觉提示。基于这一桥梁，我们集成了上下文感知的自我修正和主动探索策略，以从遮挡中恢复并抑制长轨迹上的误差累积。为了进一步解决非结构化环境中指令的空间歧义，我们提出了一种查询驱动的感知思维链（QD-PCoT）方案，使智能体具备主动寻求几何深度信息的元认知能力。最后，我们构建了AgentVLN-Instruct，这是一个大规模指令微调数据集，具有基于目标可见性的动态阶段路由。大量实验表明，AgentVLN在长程VLN基准测试中持续优于先前的最先进（SOTA）方法，为下一代具身导航模型的轻量化部署提供了一种实用的范式。代码：https://github.com/Allenxinn/AgentVLN。

## Abstract
Vision-and-Language Navigation (VLN) requires an embodied agent to ground complex natural-language instructions into long-horizon navigation in unseen environments. While Vision-Language Models (VLMs) offer strong 2D semantic understanding, current VLN systems remain constrained by limited spatial perception, 2D-3D representation mismatch, and monocular scale ambiguity. In this paper, we propose AgentVLN, a novel and efficient embodied navigation framework that can be deployed on edge computing platforms. We formulate VLN as a Partially Observable Semi-Markov Decision Process (POSMDP) and introduce a VLM-as-Brain paradigm that decouples high-level semantic reasoning from perception and planning via a plug-and-play skill library. To resolve multi-level representation inconsistency, we design a cross-space representation mapping that projects perception-layer 3D topological waypoints into the image plane, yielding pixel-aligned visual prompts for the VLM. Building on this bridge, we integrate a context-aware self-correction and active exploration strategy to recover from occlusions and suppress error accumulation over long trajectories. To further address the spatial ambiguity of instructions in unstructured environments, we propose a Query-Driven Perceptual Chain-of-Thought (QD-PCoT) scheme, enabling the agent with the metacognitive ability to actively seek geometric depth information. Finally, we construct AgentVLN-Instruct, a large-scale instruction-tuning dataset with dynamic stage routing conditioned on target visibility. Extensive experiments show that AgentVLN consistently outperforms prior state-of-the-art methods (SOTA) on long-horizon VLN benchmarks, offering a practical paradigm for lightweight deployment of next-generation embodied navigation models. Code: https://github.com/Allenxinn/AgentVLN.

---

## 论文详细总结（自动生成）

以下是对论文《AgentVLN: Towards Agentic Vision-and-Language Navigation》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉语言导航（VLN）要求智能体根据自然语言指令在未知的3D物理环境中导航。现有的视觉语言模型（VLM）虽然在2D语义理解上表现出色，但在具身导航中面临三大挑战：
    1.  **空间感知受限**：缺乏对3D几何结构和度量尺度的直观理解。
    2.  **表征不匹配**：生成的3D轨迹与VLM处理的2D视觉表征之间存在跨空间断层。
    3.  **单目尺度模糊**：仅靠RGB图像难以准确判断目标的深度和空间方位。
*   **研究背景**：现有的VLN系统通常参数量巨大（≥7B），且依赖远程云端推理，难以满足物理智能体在边缘计算平台上的实时控制需求。

### 2. 方法论
AgentVLN 提出了“**VLM作为大脑**”的具身导航范式，将高层认知推理与底层技能执行解耦。
*   **POSMDP 建模**：将导航任务形式化为部分可观测半马尔可夫决策过程，动作空间由一组即插即用的技能库（感知技能和规划技能）组成。
*   **跨空间表征映射**：
    *   感知层通过深度信息将3D路标点反投影到世界坐标系，再通过透视投影将其映射回当前2D图像平面。
    *   生成**像素对齐的视觉提示**（Visual Prompts），使VLM能直接在视觉空间内选择路径，消除了2D语义与3D物理结构间的鸿沟。
*   **查询驱动的感知思维链（QD-PCoT）**：
    *   赋予智能体“元认知”能力。当指令存在空间歧义时，智能体不直接输出坐标，而是先生成自然语言查询（如“椅子离我多远？”），调用感知技能获取几何反馈。
    *   通过多轮交互式推理，最终确定符合指令意图的目标像素坐标。
*   **上下文感知自纠错与主动探索**：在遇到遮挡或偏离轨迹时，利用历史上下文生成细粒度的原子动作（左转、右转、前进）进行航向修正。

### 3. 实验设计
*   **数据集**：在 **R2R-CE**（连续环境下的房间到房间导航）和 **RxR-CE**（多语言、长程导航）基准数据集上进行评估。
*   **对比方法**：对比了包括 NavGPT、NaVid、StreamVLN、InternVLA-N1、JanusVLN、EfficientVLN 和 DualVLN 在内的多种 SOTA 模型。
*   **评估指标**：成功率（SR）、路径长度加权成功率（SPL）、导航误差（NE）和预言机成功率（OS）。
*   **真实世界测试**：在 Unitree Go2 四足机器人上部署，测试了室内和室外复杂场景下的表现。

### 4. 资源与算力
*   **模型基础**：采用 **Qwen2.5-VL-3B** 作为核心大脑。
*   **算力投入**：实验在配备 **32 台 NVIDIA A100 GPU** 的计算集群上完成。
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 128，学习率为 2×10⁻⁵，采用余弦退火策略。
*   **部署平台**：得益于轻量化设计，该模型支持在 **Jetson 嵌入式边缘计算板**上进行实时本地推理。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流 VLN 基准上进行了全面测试，并构建了包含动态路由机制的大规模指令微调数据集 **AgentVLN-Instruct**。
*   **消融实验**：针对 VLM-as-Brain 范式、自纠错策略（CDFG）、感知思维链（QD-PCoT）以及时间上下文长度（1-10帧）分别做了详细的消融研究。
*   **客观性**：通过与参数量更大的模型（如 7B、8B 级别）对比，证明了其在更小参数规模下的优越性，实验设计较为公平且覆盖了从仿真到现实的完整链路。

### 6. 主要结论与发现
*   **性能领先**：AgentVLN-3B 在 R2R-CE 上的成功率（SR）达到 67.2%，显著超过了参数量更大的 InternVLA-N1（58.2%）和 DualVLN（64.3%）。
*   **效率极高**：在 SPL 指标上表现尤为突出，证明其规划的路径更接近最优路径，决策效率更高。
*   **解耦的有效性**：证明了将高层语义匹配与底层几何感知解耦，可以显著降低 VLM 的认知负担，提升泛化能力。
*   **边缘友好**：3B 的参数量使其摆脱了对云端推理的依赖，消除了通信延迟。

### 7. 优点与亮点
*   **范式创新**：提出的“VLM作为大脑”配合技能库的架构，使模型具有极强的可扩展性（更换传感器只需更新技能库，无需重训 VLM）。
*   **视觉提示设计**：通过像素对齐的 3D-2D 投影，巧妙解决了 VLM 难以理解 3D 空间的问题。
*   **主动元认知**：QD-PCoT 机制模拟了人类在不确定时“先询问/观察再行动”的行为，提高了复杂指令下的定位精度。
*   **实战性强**：不仅有仿真实验，还完成了四足机器人的实地部署，验证了算法的鲁棒性。

### 8. 不足与局限
*   **深度依赖**：虽然减少了对辅助深度模块的持续依赖，但感知层仍需深度图来构建局部地图和进行投影，在深度感知失效的环境下可能受限。
*   **多轮推理延迟**：QD-PCoT 虽然提高了精度，但多轮对话推理可能会增加单步决策的时间开销。
*   **动态环境挑战**：论文主要关注静态或半静态环境下的长程规划，对于高度动态（如人流密集）场景的避障和交互提及较少。

（完）
