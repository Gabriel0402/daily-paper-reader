---
title: OSM-based Domain Adaptation for Remote Sensing VLMs
title_zh: 基于 OSM 的遥感视觉语言模型领域自适应
authors: "Stefan Maria Ailuro, Mario Markov, Mohammad Mahdi, Delyan Boychev, Luc Van Gool, Danda Pani Paudel"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11804v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 遥感视觉语言模型的领域自适应
tldr: 针对遥感领域视觉语言模型（VLM）缺乏高质量标注数据的问题，本文提出OSMDA框架。该方法摒弃了对昂贵大型教师模型的依赖，利用OpenStreetMap（OSM）元数据，通过模型自身的OCR和图表理解能力为卫星图像生成高质量描述。通过在生成的语料库上进行微调，OSMDA-VLM在多个基准测试中达到SOTA水平，为遥感领域提供了一种低成本、可扩展的领域自适应新路径。
selection_source: fresh_fetch
motivation: 现有的遥感VLM自适应方法过度依赖昂贵的大型教师模型生成伪标签，限制了性能上限和扩展性。
method: 利用OpenStreetMap渲染图与卫星图像配对，通过基础VLM自身的理解能力结合OSM元数据自动生成标注，并进行自监督微调。
result: 在10个基准测试中优于9个竞争基线，与真实数据混合使用时达到SOTA性能，且训练成本显著降低。
conclusion: 结合众源地理数据（OSM）与强大的基础模型，是实现遥感领域视觉语言模型自适应的一种高效且可扩展的方案。
---

## 摘要
适应于遥感领域的视觉语言模型（VLM）严重依赖特定领域的图文监督，然而卫星和航空图像的高质量标注仍然稀缺且制作成本高昂。主流的伪标签流水线通过从大型前沿模型中蒸馏知识来填补这一空白，但这种对大型教师模型的依赖不仅成本高昂，限制了可扩展性，还将性能上限限制在教师模型的水平。我们提出了 OSMDA：一个消除这种依赖的自包含领域自适应框架。我们的核心见解是，一个能力强大的基础 VLM 可以充当其自身的标注引擎：通过将航空图像与渲染的 OpenStreetMap (OSM) 瓦片配对，我们利用模型的视觉字符识别和图表理解能力，生成由 OSM 海量辅助元数据增强的描述。随后，该模型仅使用卫星图像在生成的语料库上进行微调，从而得到 OSMDA-VLM，这是一种无需人工标注且不需要更强大外部模型的领域自适应 VLM。我们在涵盖图像-文本到文本任务的 10 个基准测试上进行了详尽评估，并与 9 个具有竞争力的基线进行了对比。当与真实数据等比例混合时，我们的方法达到了最先进的结果，同时训练成本显著低于依赖教师模型的方案。这些结果表明，在拥有强大基础模型的前提下，与众包地理数据进行对齐是实现遥感领域自适应的一条实用且可扩展的路径。数据集和模型权重将公开。

## Abstract
Vision-Language Models (VLMs) adapted to remote sensing rely heavily on domain-specific image-text supervision, yet high-quality annotations for satellite and aerial imagery remain scarce and expensive to produce. Prevailing pseudo-labeling pipelines address this gap by distilling knowledge from large frontier models, but this dependence on large teachers is costly, limits scalability, and caps achievable performance at the ceiling of the teacher. We propose OSMDA: a self-contained domain adaptation framework that eliminates this dependency. Our key insight is that a capable base VLM can serve as its own annotation engine: by pairing aerial images with rendered OpenStreetMap (OSM) tiles, we leverage optical character recognition and chart comprehension capabilities of the model to generate captions enriched by OSM's vast auxiliary metadata. The model is then fine-tuned on the resulting corpus with satellite imagery alone, yielding OSMDA-VLM, a domain-adapted VLM that requires no manual labeling and no stronger external model. We conduct exhaustive evaluations spanning 10 benchmarks across image-text-to-text tasks and comparing against 9 competitive baselines. When equally mixed with real data, our method achieves state-of-the-art results, while being substantially cheaper to train than teacher-dependent alternatives. These results suggest that, given a strong foundation model, alignment with crowd-sourced geographic data is a practical and scalable path towards remote sensing domain adaptation. Dataset and model weights will be made publicly available.

---

## 论文详细总结（自动生成）

以下是对论文《OSM-based Domain Adaptation for Remote Sensing VLMs》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：遥感领域（RS）拥有海量卫星图像，但缺乏高质量的“图像-文本”对标注。现有的视觉语言模型（VLM）自适应方法主要依赖昂贵的闭源大模型（如 GPT-4V、Gemini）生成伪标签。
*   **研究动机**：
    *   **成本高昂**：大规模调用商业模型 API 极其昂贵。
    *   **性能瓶颈**：学生模型的上限被限制在教师模型的水平，且会继承教师模型的幻觉。
    *   **可扩展性差**：依赖外部模型限制了数据合成的规模。
*   **整体含义**：提出一种名为 **OSMDA** 的自包含框架，利用开源的 OpenStreetMap（OSM）地理数据，让基础模型通过“看图识图”的方式自我标注，实现低成本、高性能的遥感领域自适应。

### 2. 方法论：核心思想与技术细节
OSMDA 的核心思想是利用基础 VLM 现有的 **OCR（光学字符识别）**和**图表理解能力**，通过渲染的地图瓦片引导模型生成地理描述。
*   **数据清洗与语义标注**：
    *   从 SkyScript 筛选 150 万张地理参考图像。
    *   过滤非视觉特征（如行政边界、地下设施）并脱敏（删除地名、商业名称）。
    *   使用 Qwen2.5-72B 将原始 OSM 标签（如 `amenity=fuel`）转换为简洁的语义标签（如“加油站”）。
*   **地图渲染（Map Rendering）**：使用 Mapnik 库将 OSM 数据渲染成与卫星图像地理对齐的地图瓦片。地图中包含道路、土地利用多边形和语义文本标签。
*   **伪标签生成（OSMDA-Captions）**：
    *   将“卫星图像 + 渲染地图”同时输入基础 VLM（InternVL3.5-8B）。
    *   模型利用 OCR 读取地图上的标签，结合卫星图的视觉特征，生成详细的地理描述。
*   **领域自适应训练**：
    *   **模型**：OSMDA-VLM。
    *   **训练**：仅使用卫星图像作为输入（推理时不需地图），在生成的 20 万条 OSMDA-Captions 上进行微调。
    *   **混合策略**：将合成数据与真实基准数据等比例混合训练，以平衡地理知识与任务格式。

### 3. 实验设计
*   **数据集与基准（10 个）**：
    *   **微调集**：NWPU-Captions, UCM-Captions, VRSBench, RSVQA-HR, RSVQA-LR。
    *   **泛化/零样本集**：AID, Million-AID, EuroSAT, XLRS-Bench, SkyScript-Bench。
*   **对比方法（9 个基线）**：包括 GeoChat, GeoPix, SkySenseGPT, VHM, EarthDial, LHRS-Bot-nova, Intern-S1-mini 等。
*   **评估指标**：G-Eval（使用 Qwen2.5-32B 作为裁判）、F1 分数、平均绝对误差（MAE）。
*   **统一协议**：针对以往研究中模型对指令格式敏感（Brittleness）的问题，作者设计了统一的评估提示词，确保对比的公平性。

### 4. 资源与算力
*   **硬件环境**：使用了 **16 台 NVIDIA H200 GPU**。
*   **训练细节**：
    *   采用 LoRA（秩为 16）进行高效微调。
    *   混合精度训练（bfloat16）。
    *   学习率 $1 \times 10^{-4}$，AdamW 优化器，训练 1 个 Epoch。
    *   总批次大小（Batch Size）为 32。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 个不同任务（分类、计数、详细描述、VQA）的基准测试，进行了详尽的跨模型对比。
*   **消融实验**：
    *   对比了“基础模型” vs “OSMDA 微调” vs “教师模型蒸馏”。
    *   验证了“联合训练”与“顺序训练”的效果差异。
*   **充分性评价**：实验设计非常全面且客观。通过引入“统一评估协议”，揭示了许多现有模型在指令微调上的过拟合问题，评估过程具有高度的透明度和可重复性。

### 6. 主要结论与发现
*   **SOTA 性能**：OSMDA-VLM 在 10 个基准测试中的 6 个达到最优，且在几乎所有测试中均位列前三。
*   **超越教师模型**：使用 OSMDA 方法（学生自标注）的效果优于使用更强大的外部教师模型（如 Gemma-3-27B）生成的标签。
*   **泛化能力强**：在零样本（Zero-shot）任务上表现优异，证明模型学到了真实的地理特征而非仅仅记忆模板。
*   **成本效益**：相比依赖 GPT-4V 等模型的流水线，OSMDA 的标注成本降低了数倍甚至数十倍。

### 7. 优点（亮点）
*   **自给自足（Self-contained）**：打破了对闭源商业大模型的依赖，利用开源地理数据实现闭环进化。
*   **创新的标注方式**：巧妙地将地理元数据转化为 VLM 可读的“视觉地图”，利用了模型已有的 OCR 能力。
*   **评估严谨**：指出了遥感 VLM 领域普遍存在的“指令脆弱性”，并提供了更可靠的统一评估框架。

### 8. 不足与局限
*   **地图诱导偏差**：模型性能受限于 OSM 数据的完整性。在 OSM 标注稀疏的区域（如某些工业区或偏远地区），模型生成的描述质量会下降。
*   **语义偏差**：模型倾向于使用 OSM 标签中的特定词汇，可能在某些 VQA 任务中产生词汇偏好。
*   **应用限制**：虽然推理时不需要地图，但训练阶段仍需地理对齐的卫星图和 OSM 数据，对于完全缺乏地图覆盖的极少数地区可能难以应用。

（完）
