---
title: Shielded Reinforcement Learning Under Dynamic Temporal Logic Constraints
title_zh: 动态时序逻辑约束下的屏蔽强化学习
authors: "Sadık Bera Yüksel, Ali Tevfik Buyukkocak, Derya Aksaray"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17152v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 屏蔽强化学习中的信号时序逻辑约束
tldr: 针对强化学习在处理复杂时空约束（如定期充电、限时访问）时的局限性，本文提出了一种基于屏蔽强化学习的框架。该方法结合了信号时序逻辑（STL）和序列控制屏障函数（CBF），能够在学习过程中强制执行复杂的STL规范，甚至包括对未知轨迹动态目标的访问。通过将形式化方法与无模型强化学习相结合，该框架在保证系统安全性的同时，实现了对动态任务的可靠执行，并通过仿真验证了其有效性。
selection_source: fresh_fetch
motivation: 传统的安全强化学习难以处理如定期任务或限时访问等复杂的时空约束。
method: 提出一种结合序列控制屏障函数（CBF）与无模型强化学习的框架，利用信号时序逻辑（STL）来定义和强制执行复杂任务。
result: 仿真实验表明，该方法能有效确保系统在学习过程中满足复杂的STL规范，并能应对具有未知轨迹的动态目标。
conclusion: 该研究扩展了安全强化学习的应用范围，使其能够处理包含动态目标的复杂时空任务，提升了机器人在真实环境中的部署潜力。
---

## 摘要
强化学习 (RL) 在各种机器人应用中展现出巨大潜力，但由于安全和操作约束，其在真实系统上的部署仍然受到限制。近年来，安全强化学习领域受到了广泛关注，该领域专注于在整个学习过程中施加安全约束。然而，真实系统通常需要比单纯的安全约束更复杂的约束，例如定期充电或在限定时间内访问特定区域。在学习过程中施加此类时空任务仍然是一个挑战。信号时序逻辑 (STL) 是一种用于指定实值信号时序属性的形式化语言，并提供了一种表达此类复杂任务的方法。在本文中，我们提出了一个框架，利用序列控制屏障函数 (sequential control barrier functions) 和无模型强化学习 (model-free RL)，以确保在整个学习过程中满足给定的 STL 任务。我们的方法通过执行丰富的 STL 规范扩展了传统的安全约束，这些规范可以涉及对轨迹未知的动态目标的访问。我们还通过各种仿真证明了我们框架的有效性。

## Abstract
Reinforcement Learning (RL) has shown promise in various robotics applications, yet its deployment on real systems is still limited due to safety and operational constraints. The safe RL field has gained considerable attention in recent years, which focuses on imposing safety constraints throughout the learning process. However, real systems often require more complex constraints than just safety, such as periodic recharging or time-bounded visits to specific regions. Imposing such spatio-temporal tasks during learning still remains a challenge. Signal Temporal Logic (STL) is a formal language for specifying temporal properties of real-valued signals and provides a way to express such complex tasks. In this paper, we propose a framework that leverages sequential control barrier functions and model-free RL to ensure that the given STL tasks are satisfied throughout the learning process. Our method extends beyond traditional safety constraints by enforcing rich STL specifications, which can involve visits to dynamic targets with unknown trajectories. We also demonstrate the effectiveness of our framework through various simulations.

---

## 论文详细总结（自动生成）

这篇论文提出了一种在强化学习（RL）训练过程中强制执行复杂时空约束的新框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的安全强化学习（Safe RL）大多关注简单的安全性（如避障）或仅在训练结束后保证策略安全。然而，现实中的机器人任务往往包含复杂的**时空约束**（如：每隔90秒必须充电一次、在特定时间窗口内访问动态目标）。
*   **研究动机**：在训练过程中，随机探索可能导致违反这些关键的操作约束，从而中断学习或损坏设备。现有的形式化方法（如线性时序逻辑 LTL）在处理连续信号和精确时间窗口方面存在局限。
*   **整体含义**：本文旨在开发一种“屏蔽（Shielding）”机制，利用**信号时序逻辑（STL）**和**序列控制屏障函数（Sequential CBFs）**，确保智能体在整个学习过程中（而非仅在学习后）都能满足复杂的动态任务要求。

### 2. 方法论
*   **核心思想**：将 STL 规范转化为一系列可由控制屏障函数（CBF）处理的约束，并在 RL 智能体输出动作时，通过一个二次规划（QP）求解器进行实时修正。
*   **关键技术细节**：
    *   **STL 建模**：使用 STL 表达复杂的任务，如“全局维持”、“最终到达”以及带时间窗口的嵌套逻辑。
    *   **最坏情况距离估计**：针对轨迹未知的动态目标，算法利用目标的最大速度界限，计算智能体与目标之间的“最坏情况距离”，从而实现保守但安全的导航。
    *   **序列 CBF（Sequential CBFs）**：根据任务的紧迫程度生成可行序列，并构造时间相关的 CBF。
    *   **屏蔽机制（Shielding）**：在每一步，RL 算法（如 SAC）提出原始动作 $u_{RL}$，系统求解一个最小化修正量 $\|u\|^2$ 的 QP 问题，输出修正动作 $u_{CBF}$。最终执行动作 $\tilde{u} = u_{RL} + u_{CBF}$。
    *   **理论保证**：论文证明了即使在执行器受限的情况下，通过引入松弛变量 $\epsilon$，任务违反（时间延迟）也是有界且可控的。

### 3. 实验设计
*   **场景设置**：
    *   **案例 1（定期充电）**：智能体在追求高奖励目标的同时，必须每隔 90 步访问两个移动充电站中的任意一个。
    *   **案例 2（复杂时空序列）**：智能体需在特定时间段内依次访问两个随机移动的目标，并同时满足周期性充电约束。
*   **Benchmark（基准）**：对比了标准的、无约束的 **Soft Actor-Critic (SAC)** 算法。
*   **评估指标**：训练过程中的累积奖励、STL 任务满足率。

### 4. 资源与算力
*   **硬件**：使用了一块 **NVIDIA RTX 4070 GPU**。
*   **软件/模型**：基于 Python 3.10 开发；SAC 网络包含 4 个隐藏层，每层 256 个神经元。
*   **训练细节**：文中提到结果是基于 10 次独立运行的平均值，但未明确说明单次训练的具体时长。

### 5. 实验数量与充分性
*   **实验规模**：设计了两个具有代表性的案例研究（Case 1 & Case 2），涵盖了逻辑析取（多选一）和复杂的顺序约束。
*   **充分性**：每个案例均进行了 10 次独立实验并给出了置信区间（标准差），证明了结果的统计意义。
*   **客观性**：实验客观地展示了“安全”与“奖励”之间的权衡——为了满足 STL 约束，受限智能体的最终奖励低于无约束智能体，这符合实际物理逻辑。

### 6. 主要结论与发现
*   **高满足率**：该框架在训练过程中实现了极高的任务满足率（案例 1 为 99.56%，案例 2 为 95.2%），而标准 SAC 无法保证满足这些复杂约束。
*   **动态适应性**：即使目标物体的未来轨迹完全未知，只要其速度有界，该方法就能有效引导智能体完成任务。
*   **训练稳定性**：通过将 STL 约束作为硬性屏蔽，智能体能够在不违反操作规程的前提下探索环境并学习优化主任务奖励。

### 7. 优点
*   **实时安全性**：解决了 RL 训练期间的合规性问题，而不仅仅是训练后的性能。
*   **处理动态目标**：相比于静态安全区域，该方法能处理移动目标和随时间变化的逻辑需求。
*   **数学严谨性**：提供了关于任务违反程度（时间松弛）的理论下界证明。

### 8. 不足与局限
*   **动力学模型简化**：实验主要基于单积分器（Single Integrator）动力学模型，对于具有高阶非线性动力学的复杂机器人系统，CBF 的构造会更加困难。
*   **计算开销**：每一步都需要求解一个 QP 问题，虽然对于简单系统很快，但在超高频控制或大规模多智能体场景下可能存在计算瓶颈。
*   **依赖先验界限**：算法需要预知目标的最大可能速度（$u_{max}$），如果该估计不准确，安全性保证可能会失效。
*   **奖励折损**：由于强制执行约束，智能体在某些情况下可能无法探索到理论上的全局最优奖励区域。

（完）
