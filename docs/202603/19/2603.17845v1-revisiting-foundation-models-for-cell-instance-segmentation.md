---
title: Revisiting foundation models for cell instance segmentation
title_zh: 重新审视用于细胞实例分割的基础模型
authors: "Anwai Archit, Constantin Pape"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17845v1"
tags: ["query:mmseg"]
score: 10.0
evidence: 对SAM、SAM2和SAM3在细胞实例分割中的评估与扩展
tldr: 本研究全面评估了针对细胞分割的专用基础模型（如CellPoseSAM、μSAM）与通用基础模型（如SAM、SAM2、SAM3）在多种显微成像任务中的表现。作者提出了一种名为自动提示生成（APG）的新型实例分割策略，旨在进一步提升基于SAM的显微镜基础模型性能。实验证明，APG显著增强了μSAM的效果，使其能与当前最先进的模型竞争，为未来开发更强大的显微镜基础模型提供了重要指导。
selection_source: fresh_fetch
motivation: 旨在评估并改进现有通用及专用基础模型在多样化显微图像细胞分割任务中的适用性与性能。
method: 对比评估了多种SAM系列模型，并引入了一种名为自动提示生成（APG）的新型实例分割策略。
result: APG策略显著提升了μSAM的分割精度，使其表现足以媲美当前最先进的CellPoseSAM模型。
conclusion: 该研究为将SAM类模型适配至显微成像领域提供了重要经验，并为构建更强大的领域基础模型指明了方向。
---

## 摘要
细胞分割是显微镜图像分析中的一项基础任务。目前已推出了几种用于细胞分割的基础模型，几乎所有模型都是 Segment Anything Model (SAM) 的扩展，并针对显微镜数据进行了改进。最近，SAM2 和 SAM3 相继发布，进一步提升并扩展了通用分割基础模型的能力。在本文中，我们在多种（光学）显微镜数据集上，针对细胞、细胞核和类器官分割等任务，全面评估了细胞分割基础模型（CellPoseSAM、CellSAM、$μ$SAM）和通用分割基础模型（SAM、SAM2、SAM3）。此外，我们引入了一种名为自动提示生成（APG）的新型实例分割策略，可用于进一步改进基于 SAM 的显微镜基础模型。APG 持续提升了作为基础模型的 $μ$SAM 的分割结果，并达到了与最先进模型 CellPoseSAM 相当的水平。此外，我们的工作为 SAM 风格模型在显微镜领域的适配策略提供了重要经验，并为创建更强大的显微镜基础模型提供了策略。我们的代码已在 https://github.com/computational-cell-analytics/micro-sam 公开发布。

## Abstract
Cell segmentation is a fundamental task in microscopy image analysis. Several foundation models for cell segmentation have been introduced, virtually all of them are extensions of Segment Anything Model (SAM), improving it for microscopy data. Recently, SAM2 and SAM3 have been published, further improving and extending the capabilities of general-purpose segmentation foundation models. Here, we comprehensively evaluate foundation models for cell segmentation (CellPoseSAM, CellSAM, $μ$SAM) and for general-purpose segmentation (SAM, SAM2, SAM3) on a diverse set of (light) microscopy datasets, for tasks including cell, nucleus and organoid segmentation. Furthermore, we introduce a new instance segmentation strategy called automatic prompt generation (APG) that can be used to further improve SAM-based microscopy foundation models. APG consistently improves segmentation results for $μ$SAM, which is used as the base model, and is competitive with the state-of-the-art model CellPoseSAM. Moreover, our work provides important lessons for adaptation strategies of SAM-style models to microscopy and provides a strategy for creating even more powerful microscopy foundation models. Our code is publicly available at https://github.com/computational-cell-analytics/micro-sam.

---

## 论文详细总结（自动生成）

这是一份关于论文《Revisiting foundation models for cell instance segmentation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：细胞实例分割是生物显微成像分析的基础。随着 Segment Anything Model (SAM) 及其后续版本（SAM2, SAM3）的发布，通用视觉基础模型在分割领域展现了强大潜力。然而，在显微镜领域，究竟是使用**通用模型**（如 SAM3）更好，还是使用**领域专用模型**（如 CellPoseSAM, $\mu$SAM）更好，目前缺乏全面的基准评估。
*   **核心问题**：
    1.  将 SAM 类模型适配到显微镜图像的最佳策略是什么？
    2.  通用模型（尤其是包含显微镜数据的 SAM3）是否已经让专用模型变得多余？
    3.  训练数据的规模和多样性对模型性能有何影响？

### 2. 论文提出的方法论：自动提示生成 (APG)
论文提出了一种名为 **Automatic Prompt Generation (APG)** 的新算法，旨在不重新训练模型的情况下，提升基于 SAM 的显微镜模型的自动分割能力。
*   **核心思想**：结合了“专用解码器预测”与“SAM 提示词引导分割”的优点。
*   **算法流程**：
    1.  **初步预测**：利用 $\mu$SAM 的解码器预测前景概率图、物体中心距离图和边界距离图。
    2.  **候选区域提取**：对上述三图进行阈值处理并取交集，通过连通域分析（Connected Components）确定候选物体。
    3.  **自动生成提示点**：在每个连通域内，计算距离变换的最大值点，将其作为 SAM 的**点提示（Point Prompt）**。
    4.  **掩码细化**：将提示点输入 SAM 的提示编码器和掩码解码器，生成高质量掩码及预测 IoU。
    5.  **后处理**：利用非极大值抑制（NMS）根据预测 IoU 过滤重叠的掩码，并进行尺寸过滤。

### 3. 实验设计
*   **数据集规模**：使用了 **36 个公开数据集**，涵盖了四大类任务：
    1.  **荧光显微镜细胞核分割**（9个数据集）。
    2.  **荧光显微镜细胞分割**（9个数据集）。
    3.  **无标记（Label-free）显微镜细胞分割**（9个数据集）。
    4.  **组织病理学细胞核分割**（9个数据集）。
*   **对比方法 (Benchmark)**：
    *   **通用模型**：SAM (使用 AMG 模式), SAM2, SAM3 (使用文本提示)。
    *   **领域专用模型**：CellPoseSAM, CellSAM, $\mu$SAM (使用 AIS 模式), PathoSAM, CellPose3。
*   **评价指标**：平均分割精度 (mSA, Mean Segmentation Accuracy)，涵盖了从 0.5 到 0.95 的多个 IoU 阈值。

### 4. 资源与算力
*   **算力支持**：论文提到使用了 NHR@Göttingen 的超算集群 "Emmy" 进行计算。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量或训练总时长。由于 APG 方法是基于预训练的 $\mu$SAM 模型进行的推理改进，不需要重新训练，因此其主要的算力消耗在于 36 个数据集的大规模推理评估。

### 5. 实验数量与充分性
*   **实验组数**：在 36 个数据集上对 7-8 种主流方法进行了交叉对比，并针对 SAM3 的文本提示策略进行了消融实验。
*   **充分性与客观性**：
    *   **充分性**：实验覆盖了从 2D 到 3D（切片评估）的多种成像模态，样本量极大。
    *   **客观性**：作者使用了配对 Wilcoxon 符号秩检验（Wilcoxon signed-rank tests）来验证不同方法之间差异的统计显著性，确保结论不是偶然产生的。
    *   **公平性**：明确标注了哪些模型在哪些数据集的训练集上进行过训练，以区分“域内”和“域外”表现。

### 6. 论文的主要结论与发现
*   **专用模型依然领先**：尽管 SAM3 引入了显微镜数据，但专用模型（如 CellPoseSAM 和使用 APG 的 $\mu$SAM）在所有模态下依然显著优于通用模型。
*   **APG 的有效性**：APG 策略在不改变模型权重的前提下，显著提升了 $\mu$SAM 的性能，使其在多个基准上达到 SOTA 水平。
*   **SAM3 的局限性**：SAM3 对文本提示（Text Prompt）非常敏感。例如，提示“cell”有效，但提示“nucleus”可能失效；使用描述形状的词（如“irregular shapes”）有时比生物学术语效果更好。
*   **数据规模至上**：模型的表现与领域专用训练数据的规模高度正相关。

### 7. 优点
*   **即插即用**：APG 方法不需要重新训练模型，为现有 SAM 类模型提供了一种低成本的性能增强方案。
*   **评估全面**：这是目前针对显微镜基础模型最全面的一次“大阅兵”，涵盖了最新的 SAM3。
*   **统计严谨**：引入了显著性检验，增强了结论的说服力。

### 8. 不足与局限
*   **维度限制**：虽然涉及 3D 数据集，但评估仅限于 2D 切片，未充分探讨模型在 3D 空间连续性上的表现。
*   **提示词策略单一**：对于 SAM3，仅测试了文本提示，未测试其“示例图像提示（Example-based prompting）”功能，这可能低估了 SAM3 的潜力。
*   **提示类型局限**：APG 目前仅生成点提示，未探索效果通常更好的框提示（Box Prompt），作者提到在自动生成高质量框提示方面仍存在挑战。

（完）
