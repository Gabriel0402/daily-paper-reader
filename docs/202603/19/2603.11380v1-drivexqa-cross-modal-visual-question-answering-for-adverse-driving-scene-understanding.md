---
title: "DriveXQA: Cross-modal Visual Question Answering for Adverse Driving Scene Understanding"
title_zh: DriveXQA：用于不利驾驶场景理解的跨模态视觉问答
authors: "Mingzhe Tao, Ruiping Liu, Junwei Zheng, Yufan Chen, Kedi Ying, M. Saquib Sarfraz, Kailun Yang, Jiaming Zhang, Rainer Stiefelhagen"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11380v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 用于恶劣驾驶场景理解和视觉问答的多模态大语言模型
tldr: 本研究针对自动驾驶在恶劣场景下的理解难题，提出了DriveXQA数据集，包含4种视觉模态、多种传感器故障及天气条件下的10万余个问答对。同时设计了MVX-LLM架构，通过双交叉注意力（DCA）投影器高效融合多模态信息并减少冗余。该工作填补了多模态大模型在处理复杂驾驶环境传感器融合方面的空白，显著提升了系统在极端天气下的理解能力。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在利用多传感器信息理解自动驾驶中的恶劣及异常场景方面仍缺乏深入探索。
method: 提出了包含10万余个问答对的DriveXQA数据集，并设计了带有双交叉注意力投影器的MVX-LLM架构以实现高效的模态融合。
result: 实验表明DCA架构在雾天等挑战性条件下表现优异，GPT评分从基准的25.1大幅提升至53.5。
conclusion: 该研究通过新数据集和高效融合架构，为自动驾驶系统在复杂环境下的跨模态视觉问答提供了有效的解决方案。
---

## 摘要
融合具有互补模态的传感器对于维持对异常驾驶场景稳定且全面的理解至关重要。然而，多模态大语言模型（MLLMs）在利用多传感器信息理解自动驾驶汽车中的不利驾驶场景方面尚未得到充分探索。为了填补这一空白，我们提出了 DriveXQA，这是一个用于自动驾驶视觉问答（VQA）的多模态数据集。除了四种视觉模态、五种传感器故障情况和五种天气条件外，该数据集还包含 102,505 个问答对，分为三个级别：全局场景级、他心视角级和本车中心级。由于现有的 MLLM 框架均未采用多种互补的视觉模态作为输入，我们设计了 MVX-LLM，这是一种具有双交叉注意力（DCA）投影器的令牌高效架构，通过融合模态来缓解信息冗余。实验表明，我们的 DCA 在雾天等挑战性条件下实现了性能提升（GPTScore：53.5，而基准模型为 25.1）。所建立的数据集和源代码将公开发布。

## Abstract
Fusing sensors with complementary modalities is crucial for maintaining a stable and comprehensive understanding of abnormal driving scenes. However, Multimodal Large Language Models (MLLMs) are underexplored for leveraging multi-sensor information to understand adverse driving scenarios in autonomous vehicles. To address this gap, we propose the DriveXQA, a multimodal dataset for autonomous driving VQA. In addition to four visual modalities, five sensor failure cases, and five weather conditions, it includes $102,505$ QA pairs categorized into three types: global scene level, allocentric level, and ego-vehicle centric level. Since no existing MLLM framework adopts multiple complementary visual modalities as input, we design MVX-LLM, a token-efficient architecture with a Dual Cross-Attention (DCA) projector that fuses the modalities to alleviate information redundancy. Experiments demonstrate that our DCA achieves improved performance under challenging conditions such as foggy (GPTScore: $53.5$ vs. $25.1$ for the baseline). The established dataset and source code will be made publicly available.

---

## 论文详细总结（自动生成）

### 论文总结：DriveXQA - 用于不利驾驶场景理解的跨模态视觉问答

#### 1. 核心问题与整体含义（研究动机和背景）
自动驾驶系统在复杂环境下的安全导航依赖于对多传感器信息的全面理解。然而，现有的多模态大语言模型（MLLM）在处理**不利驾驶场景**（如大雾、强光过曝、传感器故障）时存在显著不足。
*   **研究动机**：现有的自动驾驶VQA数据集（如NuScenes-QA、DriveLM）大多集中在正常天气，且缺乏对传感器退化（Sensor Degradation）情况的系统性覆盖。
*   **核心问题**：如何构建一个能够模拟真实世界传感器失效和恶劣天气的多模态数据集，并设计一种能够高效融合多种互补传感器信息（RGB、深度、事件相机、LiDAR）的架构，以提高系统在极端条件下的推理可靠性。

#### 2. 方法论：核心思想与关键技术
论文提出了 **DriveXQA 数据集** 和 **MVX-LLM 框架**。
*   **DriveXQA 数据集**：
    *   **分层问答体系**：包含全局场景级（天气/交通密度）、他心视角级（物体间空间关系）和本车中心级（车道位置/传感器状态）。
    *   **覆盖范围**：5种天气（晴、阴、雨、雾、夜）和5种传感器故障（运动模糊、过曝、欠曝、LiDAR抖动、事件相机低分辨率）。
*   **MVX-LLM 架构**：
    *   **多模态编码**：使用共享的 CLIP-ViT 编码 RGB、深度和事件数据；使用 PointNet++ 独立编码 LiDAR 点云。
    *   **双交叉注意力（DCA）投影器**：
        *   **空间交叉注意力（Spatial CA）**：建立不同模态间的空间对应关系。
        *   **通道交叉注意力（Channel CA）**：在转置的特征上操作，增强跨通道的语义信息交换。
    *   **令牌聚合（Token Aggregation）**：对比了多种策略（GAP、QAttn、Spectral等），最终采用 **查询注意力（QAttn）** 机制，将冗余的视觉特征压缩为精简的令牌，以便 LLM 处理。

#### 3. 实验设计
*   **数据集/场景**：使用自建的 DriveXQA 数据集（7,885 帧图像，102,505 个 QA 对），按 8:1:1 划分训练/验证/测试集。
*   **对比方法（Baselines）**：
    *   **简单拼接（Prepend）**：直接将多模态特征串联。
    *   **令牌融合方法**：Self-query、GAP、QAttn(Spectral)、QAttn(DepthGate)。
    *   **投影器架构**：Honeybee、ParGo。
*   **评估指标**：传统 NLP 指标（BLEU-4, ROUGE-L, METEOR, CIDEr）以及 **GPTScore**（使用 GPT-4o-mini 作为裁判进行 1-5 分打分并归一化）。

#### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A100-40GB GPU**。
*   **训练细节**：采用分布式训练和混合精度优化；使用 Adam 优化器，学习率为 1e-4；使用 **LoRA**（Rank=16）进行参数高效微调。
*   **时长**：文中未明确给出具体的训练总时长，但提到了批大小（Batch Size）为 16。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了全量数据集的对比实验，覆盖了 10 种不同的天气和故障组合。
    *   **消融实验**：针对 DCA 的三个组件（sAttn, cAttn, QAttn）进行了组合测试；针对 4 种传感器模态（RGB, Depth, Event, LiDAR）的各种排列组合进行了 8 组对比实验。
    *   **人工验证**：随机抽取 360 个样本进行人工评分，证明人工评分与 GPTScore 的相关性（Spearman 系数 > 0.83）。
*   **评价**：实验设计较为充分，涵盖了架构组件、模态贡献和环境鲁棒性等多个维度，对比基准涵盖了当前主流的 MLLM 投影器技术，具有较好的客观性。

#### 6. 主要结论与发现
*   **MVX-LLM 表现优异**：在所有不利条件下均显著超过基准模型，平均 GPTScore 达到 **55.5**（相比 Prepend 提升明显，尤其在雾天从 25.1 提升至 53.5）。
*   **双注意力机制的必要性**：空间和通道注意力的结合比单一注意力效果更好，证明了跨模态信息在不同维度互补的重要性。
*   **模态互补性**：RGB 是基础，但 LiDAR 和深度信息在视觉受损（如过曝）时能提供关键的几何补偿；事件相机在特定光照条件下对 RGB 有良好的补充作用。

#### 7. 优点（亮点）
*   **数据集填补空白**：首次在自动驾驶 VQA 中系统性地引入了**事件相机（Event Camera）**模态和**传感器故障**场景。
*   **高效融合**：DCA 投影器解决了多模态输入导致的令牌爆炸问题，在保持性能的同时兼顾了计算效率。
*   **分层 QA 结构**：提出的三层 QA 体系更符合自动驾驶的实际逻辑需求（从宏观环境到微观操作）。

#### 8. 不足与局限
*   **仿真数据偏差**：DriveXQA 基于仿真环境生成，可能存在 **Sim-to-Real（仿真到现实）** 的差距，实际道路上的传感器噪声可能更复杂。
*   **时序信息缺失**：目前的模型基于**单帧**图像进行推理，而真实的驾驶理解往往需要处理视频序列中的时序上下文。
*   **规模限制**：虽然 QA 对数量较多，但原始场景帧数（约 7.9k）相对于一些超大规模驾驶数据集仍显不足。

（完）
