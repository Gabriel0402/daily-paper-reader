---
title: "SegFly: A 2D-3D-2D Paradigm for Aerial RGB-Thermal Semantic Segmentation at Scale"
title_zh: SegFly：一种用于大规模航空 RGB-热红外语义分割的 2D-3D-2D 范式
authors: "Markus Gross, Sai Bharadhwaj Matha, Rui Song, Viswanathan Muthuveerappan, Conrad Christoph, Julius Huber, Daniel Cremers"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17920v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 无人机航空红外-可见光语义分割
tldr: "针对无人机航拍语义分割中数据集规模受限及RGB-T对齐困难的问题，本文提出了一种基于几何驱动的2D-3D-2D范式。该方法利用多视图冗余，仅需标注不到3%的RGB图像，即可通过3D点云自动生成大规模RGB和热成像伪标签，并实现高精度的跨模态对齐。基于此构建了包含2万余张图像的SegFly数据集，显著提升了视觉基础模型在多模态场景理解中的性能。"
selection_source: fresh_fetch
motivation: 现有的航拍RGB-T数据集规模小且标注成本高，且在普通无人机上难以实现精确的跨模态对齐。
method: 提出一种2D-3D-2D范式，通过将少量标注的RGB图像投影至3D空间再重投影，实现标签自动传播和跨模态几何对齐。
result: "自动生成了97%的RGB和100%的热成像标签，并构建了包含2万张高分辨率图像的大规模SegFly数据集。"
conclusion: 几何驱动的自动标注流程能有效解决大规模多模态数据获取难题，为航拍场景理解提供了强有力的基准支持。
---

## 摘要
无人机（UAV）语义分割是航空场景理解的基础，但由于人工标注成本高昂以及现成无人机上 RGB-T 精确对齐的困难，现有的 RGB 和 RGB-T 数据集在规模、多样性和标注效率方面仍然有限。为了应对这些挑战，我们提出了一种可扩展的几何驱动 2D-3D-2D 范式，该范式利用高重叠航空图像中的多视图冗余，在统一框架内将标签从一小部分人工标注的 RGB 图像自动传播到 RGB 和热红外两种模态。通过将不到 3% 的 RGB 图像提升为语义 3D 点云并将其重新投影到所有视图中，我们的方法能够在大型图像集中生成密集的伪真值（pseudo ground-truth），自动生成 97% 的 RGB 标签和 100% 的热红外标签，在无需任何 2D 人工精细化的情况下，标注准确率分别达到 91% 和 88%。我们进一步将这种 2D-3D-2D 范式扩展到跨模态图像配准，利用 3D 几何作为中间对齐空间，在无需硬件级同步的情况下，实现了全自动、强像素级的 RGB-T 对齐，配准准确率达到 87%。通过将该框架应用于现有的地理参考航空图像，我们构建了 SegFly，这是一个大规模基准数据集，包含超过 20,000 张高分辨率 RGB 图像和 15,000 多对几何对齐的 RGB-T 图像对，涵盖了跨越多个高度和季节的各种城市、工业和农村环境。在 SegFly 上，我们建立了用于 RGB 和热红外语义分割的 Firefly 基线，并证明了传统架构和视觉基础模型都能从 SegFly 的监督中显著获益，突显了几何驱动的 2D-3D-2D 流水线在可扩展多模态场景理解方面的潜力。数据和代码可在 https://github.com/markus-42/SegFly 获取。

## Abstract
Semantic segmentation for uncrewed aerial vehicles (UAVs) is fundamental for aerial scene understanding, yet existing RGB and RGB-T datasets remain limited in scale, diversity, and annotation efficiency due to the high cost of manual labeling and the difficulties of accurate RGB-T alignment on off-the-shelf UAVs. To address these challenges, we propose a scalable geometry-driven 2D-3D-2D paradigm that leverages multi-view redundancy in high-overlap aerial imagery to automatically propagate labels from a small subset of manually annotated RGB images to both RGB and thermal modalities within a unified framework. By lifting less than 3% of RGB images into a semantic 3D point cloud and reprojecting it into all views, our approach enables dense pseudo ground-truth generation across large image collections, automatically producing 97% of RGB labels and 100% of thermal labels while achieving 91% and 88% annotation accuracy without any 2D manual refinement. We further extend this 2D-3D-2D paradigm to cross-modal image registration, using 3D geometry as an intermediate alignment space to obtain fully automatic, strong pixel-level RGB-T alignment with 87% registration accuracy and no hardware-level synchronization. Applying our framework to existing geo-referenced aerial imagery, we construct SegFly, a large-scale benchmark with over 20,000 high-resolution RGB images and more than 15,000 geometrically aligned RGB-T pairs spanning diverse urban, industrial, and rural environments across multiple altitudes and seasons. On SegFly, we establish the Firefly baseline for RGB and thermal semantic segmentation and show that both conventional architectures and vision foundation models benefit substantially from SegFly supervision, highlighting the potential of geometry-driven 2D-3D-2D pipelines for scalable multi-modal scene understanding. Data and Code available at https://github.com/markus-42/SegFly.

---

## 论文详细总结（自动生成）

这是一份关于论文《SegFly: A 2D-3D-2D Paradigm for Aerial RGB-Thermal Semantic Segmentation at Scale》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
无人机（UAV）航拍场景理解在物流、巡检和应急响应中至关重要。然而，现有的航拍语义分割面临三大瓶颈：
*   **标注成本极高**：高分辨率航拍图像结构复杂，像素级人工标注耗时耗力。
*   **跨模态对齐困难**：在普通无人机上，RGB（可见光）与 Thermal（热红外）传感器往往缺乏硬件级同步，导致像素级对齐极难实现。
*   **数据集规模受限**：现有 RGB-T 数据集通常仅包含数千个样本，难以支撑现代深度学习模型（尤其是视觉基础模型）的充分训练。

本文提出了 **SegFly** 框架，通过一种**几何驱动的 2D-3D-2D 范式**，利用多视图冗余性，实现从极少量标注到大规模多模态伪标签的自动传播，并解决了跨模态对齐难题。

### 2. 方法论：核心思想与关键技术
该方法的核心是 **2D-3D-2D 范式**，将语义信息在二维图像与三维空间之间进行闭环传递：

*   **2D-to-3D 阶段（标签提升）**：
    *   利用运动恢复结构（SfM）和多视图立体视觉（MVS）重建场景的 3D 点云。
    *   仅人工标注极少量的 RGB 图像（<3%），通过 2D-3D 对应关系将标签“提升”到 3D 点云中，并通过多数投票和 kNN 降噪生成语义 3D 点云。
*   **3D-to-2D 阶段（伪标签渲染）**：
    *   将语义点云投影回所有未标注的 RGB 和热红外视图。
    *   **关键技术细节**：采用四步渲染流程：(1) 带 Z-buffer 的语义投影；(2) 基于局部内核的遮挡过滤；(3) 基于 Splatting 的密度增强；(4) 深度引导的标签传播（确保前景优先）。
*   **跨模态几何配准**：
    *   不依赖 2D 特征匹配，而是分别重建 RGB 和热红外 3D 点云，使用 **ICP 算法**在 3D 空间对齐。
    *   通过**透镜畸变转移（Lens-distortion transfer）**将对齐结果映射回 2D，实现无需硬件同步的强像素级对齐。
*   **Firefly 基线模型**：
    *   采用三阶段训练：RGB 预训练（DINOv3 编码器）→ RGB-T 领域适配（使用 Rein 适配器）→ 热红外微调。

### 3. 实验设计
*   **数据集与场景**：构建了 **SegFly 数据集**，包含 20,606 张 RGB 图像和 15,007 对对齐的 RGB-T 图像。涵盖城市、工业、农村环境，涉及 4 个季节和 3 种飞行高度（30m, 40m, 50m）。
*   **Benchmark 与对比方法**：
    *   **标注质量评估**：将自动生成的伪标签与额外的人工真值对比。
    *   **分割模型对比**：对比了 UPerNet、SegFormer 以及本文提出的 Firefly。
    *   **基础模型评估**：测试了 CatSeg（开放词汇分割）和 AnyThermal 在 SegFly 上的零样本（Zero-shot）与微调性能。

### 4. 资源与算力
*   论文中**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   文中提到了使用参数高效微调（PEFT）技术（如 Rein、LoRA、Adapter），暗示了该方法在微调阶段对算力的需求相对优化，但在 3D 重建（SfM/MVS）阶段通常需要较强的计算资源。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 9 个不同场景，进行了多维度的定量评估。
*   **消融实验**：对 3D-to-2D 渲染流水线的四个步骤（遮挡过滤、Splatting 等）进行了详细消融，验证了每一步对准确性的贡献。
*   **客观性与公平性**：
    *   使用了独立于生成过程的测试集进行验证。
    *   对比了不同高度下的标注效率，证明了“一次标注，多高度复用”的有效性。
    *   实验设计较为充分，通过与现有 SOTA 模型和基础模型的对比，展现了数据集的增益。

### 6. 主要结论与发现
*   **高效标注**：仅需标注 2.84% 的数据，即可生成准确率达 91% (RGB) 和 88% (Thermal) 的大规模伪标签。
*   **对齐精度**：提出的几何配准方法达到了 87.05% 的像素级对齐准确率，优于传统的硬件同步或 2D 匹配方法。
*   **性能提升**：在 SegFly 伪标签上训练的模型，其性能显著优于仅在少量人工标注数据上训练的模型。
*   **基础模型增益**：视觉基础模型（如 CatSeg）在 SegFly 上微调后，FWmIoU 提升了近一倍，证明了该数据集对提升模型泛化能力的价值。

### 7. 优点
*   **范式创新**：2D-3D-2D 流程巧妙地利用了航拍图像的高重叠度，将几何约束转化为监督信号。
*   **低硬件门槛**：解决了非同步相机组的对齐问题，使得普通消费级无人机也能采集高质量 RGB-T 数据。
*   **规模化能力**：通过 3D 空间的中介作用，实现了跨高度、跨模态的标签自动传播，极大地降低了构建大规模数据集的门槛。

### 8. 不足与局限
*   **静态场景假设**：由于依赖 SfM/MVS 重建，该方法难以处理场景中的动态物体（如行驶的车辆、行人），导致这些类别的伪标签质量较低（文中将其归为 unlabeled）。
*   **热红外特性限制**：目前主要针对白天热红外图像，未深入探讨昼夜温差剧烈变化对几何重建的影响。
*   **计算开销**：虽然标注省力，但前期的 3D 重建和后期的点云渲染在大规模数据集上具有较高的计算成本。

（完）
