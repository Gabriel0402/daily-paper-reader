---
title: "KineVLA: Towards Kinematics-Aware Vision-Language-Action Models with Bi-Level Action Decomposition"
title_zh: KineVLA：通过双层动作分解构建运动学感知的视觉-语言-动作模型
authors: "Gaoge Han, Zhengqing Gao, Ziwen Li, Jiaxin Huang, Shaoli Huang, Fakhri Karray, Mingming Gong, Tongliang Liu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17524v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 机器人运动学感知的视觉-语言-动作模型
tldr: 本研究针对现有视觉-语言-动作（VLA）模型在处理精细运动学指令方面的不足，提出了KineVLA框架。该框架通过双层动作分解和推理Token，将任务目标的不变性与执行轨迹的运动学变异性显式解耦。通过构建包含丰富运动学属性的新型数据集，并在仿真与真实机器人平台上进行实验，KineVLA证明了其在实现精确、可控且具泛化性的机器人操控任务中的卓越性能。
selection_source: fresh_fetch
motivation: 现有的机器人指令往往缺乏对运动学细节（如方向、轨迹和姿态）的精确描述，限制了模型在复杂操控任务中的表现。
method: 提出一种双层动作表示方法，利用推理Token作为中间变量，将语言指令中的运动学要求与具体的动作执行进行对齐。
result: 实验结果显示，KineVLA在LIBERO和真实机器人测试中均优于强基准模型，展现出更高的操控精度和可控性。
conclusion: KineVLA通过显式建模运动学属性，为实现更精细、个性化的视觉-语言-动作交互提供了有效的解决方案。
---

## 摘要
在本文中，我们引入了一项全新的富含运动学信息的视觉-语言-动作（VLA）任务。在该任务中，语言指令在关键时刻密集地编码了从开始到完成的各种运动学属性（如方向、轨迹、朝向和相对位移），这与现有的仅粗略或部分捕捉运动学的动作指令不同，从而支持细粒度和个性化的操作。在这种设定下，任务目标保持不变，而执行轨迹必须适应指令级的运动学规范。为了应对这一挑战，我们提出了 KineVLA，这是一个视觉-语言-动作框架，通过双层动作表示和双层推理标记，显式地将目标层级的不变性与运动学层级的变异性解耦，这些标记作为显式的、受监督的中间变量来对齐语言和动作。为了支持这一任务，我们构建了跨越仿真和真实机器人平台的运动学感知 VLA 数据集，其特点是具有指令级的运动学变化和双层标注。在 LIBERO 和 Realman-75 机器人上进行的大量实验表明，KineVLA 在运动学敏感的基准测试中始终优于强大的 VLA 基线模型，实现了更精确、可控且具泛化性的操作行为。

## Abstract
In this paper, we introduce a novel kinematics-rich vision-language-action (VLA) task, in which language commands densely encode diverse kinematic attributes (such as direction, trajectory, orientation, and relative displacement) from initiation through completion, at key moments, unlike existing action instructions that capture kinematics only coarsely or partially, thereby supporting fine-grained and personalized manipulation. In this setting, where task goals remain invariant while execution trajectories must adapt to instruction-level kinematic specifications. To address this challenge, we propose KineVLA, a vision-language-action framework that explicitly decouples goal-level invariance from kinematics-level variability through a bi-level action representation and bi-level reasoning tokens to serve as explicit, supervised intermediate variables that align language and action. To support this task, we construct the kinematics-aware VLA datasets spanning both simulation and real-world robotic platforms, featuring instruction-level kinematic variations and bi-level annotations. Extensive experiments on LIBERO and a Realman-75 robot demonstrate that KineVLA consistently outperforms strong VLA baselines on kinematics-sensitive benchmarks, achieving more precise, controllable, and generalizable manipulation behaviors.

---

## 论文详细总结（自动生成）

### KineVLA：通过双层动作分解构建运动学感知的视觉-语言-动作模型

#### 1. 论文的核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型（如 OpenVLA、RT-2）主要关注“完成什么任务”（目标导向），而忽略了“如何精确执行”（运动学导向）。在现实交互中，相同的任务目标（如“把瓶子放进抽屉”）可能伴随不同的运动学约束（如“标签朝前”或“标签朝左”）。
*   **核心问题**：现有模型对细粒度运动学指令（方向、轨迹、姿态、相对位移）不敏感，往往将语义相似的指令映射到相同的执行轨迹上。
*   **研究动机**：旨在开发一种能够理解并执行“富含运动学信息”指令的模型，实现目标不变性（Goal Invariance）与运动学变异性（Kinematics Variability）的显式解耦。

#### 2. 论文提出的方法论
KineVLA 框架通过双层架构将高层语义目标与底层运动细节分离：
*   **双层动作表示（Bi-Level Action Representation）**：
    *   采用**残差向量量化变分自编码器（RVQ-VAE）**。
    *   **目标层（Goal-level）**：捕捉低频的语义结构和任务目标。
    *   **运动学层（Kinematics-level）**：捕捉高频的动作修正、接触细节和运动参数。
    *   **训练策略**：两阶段训练。第一阶段在多任务数据上预训练目标层码本；第二阶段冻结目标层，在运动学数据集上训练残差运动学码本。
*   **双层推理标记（Bi-Level Reasoning Tokens）**：
    *   引入类似思维链（CoT）的机制，模型在生成动作前先生成两段推理文本：一段描述粗粒度目标，一段描述细粒度运动学约束。
*   **互信息正则化（Mutual Information Regularization）**：
    *   为了确保推理文本与实际动作的一致性，通过 **InfoNCE 损失函数** 最大化推理标记与动作标记之间的条件互信息 $I(\text{Reasoning}; \text{Action} | C)$，防止执行漂移。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO-Goal-Relabeled**：对现有 LIBERO 任务进行重标注，加入运动学细节（6.3万帧）。
    *   **Kine-LIBERO**：新构建的大规模仿真数据集，包含 26.6万帧，涵盖多样化的物体部位交互和运动约束。
    *   **Kine-Realman**：真实机器人数据集，使用 Realman-75 机械臂通过遥操作收集（4.9万帧）。
*   **Benchmark 与对比方法**：
    *   对比了 **OpenVLA**（基于 Binning 的基准）、**$\pi_{0.5}$**（基于扩散模型的最新 VLA）和 **VQ-VLA**（单层 VQ 离散化模型）。
    *   评估指标包括：任务成功率（Goal Success）和运动学符合率（Kinematic Success）。

#### 4. 资源与算力
*   **GPU 型号与数量**：
    *   RVQ-VAE 训练：使用 **1 张 NVIDIA A100 GPU**。
    *   VLA 模型微调：使用 **4 张 NVIDIA A100 GPU**。
*   **训练细节**：
    *   微调步数为 50K 步，总 Batch Size 为 64。
    *   动作分块长度（Action Chunk）设置为 $K=5$。
    *   推理速度：KineVLA 的推理延迟约为 96ms，与单层 VQ 模型（84ms）接近，优于 OpenVLA（237ms）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真环境（LIBERO 平台）和真实世界（Realman-75 机器人）的多种任务。
*   **消融实验**：系统地验证了双层动作表示（Bi-Rep）、双层推理（Bi-Rea）以及互信息优化（MI）的贡献。
*   **充分性评价**：实验设计较为全面，不仅对比了成功率，还通过“干预实验”（Intervention）验证了推理 Token 对动作生成的因果影响。对比基线包含了当前最强的自回归和扩散类 VLA 模型，具有较高的客观性和公平性。

#### 6. 论文的主要结论与发现
*   **运动学感知至关重要**：传统 VLA 在目标达成上表现尚可，但在满足特定运动学约束（如特定抓取部位、特定轨迹）时表现较差。
*   **解耦的有效性**：通过双层码本显式分离目标和运动细节，显著提升了模型在复杂指令下的操控精度。
*   **推理与动作的对齐**：引入推理 Token 并通过互信息正则化，能有效提高模型的可解释性和执行的一致性。
*   **性能领先**：KineVLA 在运动学敏感的基准测试中大幅领先于 OpenVLA 和 $\pi_{0.5}$。

#### 7. 优点
*   **任务定义新颖**：首次明确提出了“目标不变但运动学变异”的 VLA 任务设定，更符合真实人机交互需求。
*   **架构设计合理**：双层 RVQ-VAE 结合 CoT 推理，既保留了离散 Token 的效率，又增强了对高频运动细节的建模能力。
*   **数据集贡献**：提供了包含 37.8万帧的高质量运动学标注数据集，填补了该领域的空白。

#### 8. 不足与局限
*   **场景局限性**：目前的实验主要集中在桌面操作（Tabletop Manipulation），尚未扩展到全身移动操控或更复杂的动态环境。
*   **坐标系依赖**：运动学指令依赖于固定的第三人称相机坐标系（如“左”、“右”），在多视角或相机移动场景下的鲁棒性有待验证。
*   **实时性挑战**：虽然推理速度尚可，但随着推理 Token 长度增加，自回归生成的延迟可能会成为高频闭环控制的瓶颈。

（完）
