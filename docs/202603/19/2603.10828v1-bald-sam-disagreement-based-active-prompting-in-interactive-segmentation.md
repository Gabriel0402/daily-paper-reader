---
title: "BALD-SAM: Disagreement-based Active Prompting in Interactive Segmentation"
title_zh: BALD-SAM：交互式分割中基于分歧的主动提示
authors: "Prithwijit Chowdhury, Mohit Prabhushankar, Ghassan AlRegib"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10828v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 在SAM交互式分割中使用主动提示
tldr: 本研究针对交互式分割中提示词放置依赖人工视觉评估的问题，提出了主动提示（Active Prompting）框架。通过将贝叶斯主动学习（BALD）引入SAM模型，开发了BALD-SAM。该方法通过量化模型预测头的不确定性，自动识别最具信息量的区域进行提示，从而提高交互效率。在16个跨领域数据集上的实验表明，BALD-SAM在分割质量上优于人类提示，甚至在某些情况下超过了Oracle提示，尤其擅长处理复杂结构。
selection_source: fresh_fetch
motivation: 现有的交互式分割流程过度依赖人工视觉判断来放置提示，缺乏一种基于模型自身准则来自动识别最具信息量区域的系统化方法。
method: 提出了BALD-SAM框架，通过冻结SAM骨干网络并对小型预测头进行贝叶斯不确定性建模，利用BALD准则实现空间主动提示选择。
result: 在涵盖自然、医疗等领域的16个数据集上，BALD-SAM在14个基准测试中排名第一或第二，且在最终分割质量上显著优于单次提示基线。
conclusion: BALD-SAM证明了通过量化认识不确定性来引导主动提示，可以有效提升大模型在复杂交互式分割任务中的效率和准确性。
---

## 摘要
Segment Anything Model (SAM) 通过空间提示彻底改变了交互式分割。虽然现有工作主要集中在各种设置下的提示自动化，但现实世界的标注工作流涉及迭代细化，标注者观察模型输出并有策略地放置提示以消除歧义。目前的流程通常依赖于标注者对预测掩码质量的视觉评估。我们假设，自动化交互式提示的一种原则性方法是使用模型导出的准则来识别下一个提示信息量最大的区域。在这项工作中，我们建立了主动提示（active prompting）：一种空间主动学习方法，其中图像内的位置构成未标记池，提示作为查询以优先处理信息丰富的区域，从而提高每次交互的效用。我们进一步提出了 BALD-SAM：一个通过量化认知不确定性，将基于分歧的贝叶斯主动学习 (BALD) 应用于空间提示选择的原则性框架。为此，我们冻结整个模型，仅对一个小型学习预测头应用贝叶斯不确定性建模，使得对于拥有数百万参数的大型基础模型而言，原本难以处理的不确定性估计变得可行。在涵盖自然、医学、水下和地震领域的 16 个数据集上，BALD-SAM 展示了强大的跨域性能，在 16 个基准测试中的 14 个排名第一或第二。我们通过涵盖 3 个 SAM 骨干网络和 35 个拉普拉斯后验配置（共计 38 个不同的消融设置）的全面消融实验验证了这些提升。除了强大的平均性能外，BALD-SAM 还超越了人工提示，在多个类别中甚至超过了真值提示 (oracle prompting)，同时在最终分割质量上始终优于单次 (one-shot) 基准，特别是在细长和结构复杂的物体上。

## Abstract
The Segment Anything Model (SAM) has revolutionized interactive segmentation through spatial prompting. While existing work primarily focuses on automating prompts in various settings, real-world annotation workflows involve iterative refinement where annotators observe model outputs and strategically place prompts to resolve ambiguities. Current pipelines typically rely on the annotator's visual assessment of the predicted mask quality. We postulate that a principled approach for automated interactive prompting is to use a model-derived criterion to identify the most informative region for the next prompt. In this work, we establish active prompting: a spatial active learning approach where locations within images constitute an unlabeled pool and prompts serve as queries to prioritize information-rich regions, increasing the utility of each interaction. We further present BALD-SAM: a principled framework adapting Bayesian Active Learning by Disagreement (BALD) to spatial prompt selection by quantifying epistemic uncertainty. To do so, we freeze the entire model and apply Bayesian uncertainty modeling only to a small learned prediction head, making intractable uncertainty estimation practical for large multi-million parameter foundation models. Across 16 datasets spanning natural, medical, underwater, and seismic domains, BALD-SAM demonstrates strong cross-domain performance, ranking first or second on 14 of 16 benchmarks. We validate these gains through a comprehensive ablation suite covering 3 SAM backbones and 35 Laplace posterior configurations, amounting to 38 distinct ablation settings. Beyond strong average performance, BALD-SAM surpasses human prompting and, in several categories, even oracle prompting, while consistently outperforming one-shot baselines in final segmentation quality, particularly on thin and structurally complex objects.

---

## 论文详细总结（自动生成）

这是一份关于论文《BALD-SAM: Disagreement-based Active Prompting in Interactive Segmentation》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：在交互式图像分割（如使用 SAM 模型）中，如何科学地决定“下一个提示点（Prompt）”应该放在哪里？
*   **研究动机**：目前的交互式分割流程高度依赖人工的视觉直觉。虽然 SAM 具备强大的零样本能力，但人类在放置提示点时存在主观性、疲劳感和效率低下的问题。
*   **整体含义**：论文提出将“主动学习（Active Learning）”引入空间提示选择过程，建立“主动提示（Active Prompting）”框架，旨在通过模型自身的不确定性反馈，自动识别图像中最具信息量的区域，从而减少人工交互次数并提升分割精度。

### 2. 方法论：BALD-SAM 框架
*   **核心思想**：利用“基于分歧的贝叶斯主动学习（BALD）”准则，选择那些能让模型不同参数样本产生最大预测分歧的空间位置作为提示点。
*   **关键技术细节**：
    *   **冻结骨干与轻量级头**：为了处理拥有数亿参数的 SAM，作者冻结了 SAM 的图像编码器、提示编码器和掩码解码器，仅在输出端添加一个轻量级的卷积预测头（约 3.5 万参数）。
    *   **拉普拉斯近似（Laplace Approximation）**：对该预测头的参数进行后验分布估计。通过在训练好的参数周围进行拉普拉斯近似，得到参数的概率分布。
    *   **蒙特卡洛采样与分歧度量**：在推理时，从后验分布中抽取多个预测头样本，生成一组概率图。
    *   **BALD 公式应用**：计算互信息（Mutual Information, MI），即“平均预测的熵”与“预测熵的平均值”之差。MI 最高的像素点即为认知不确定性（Epistemic Uncertainty）最高的区域，被选为下一个提示位置。
*   **算法流程**：输入图像 -> SAM 提取特征 -> 贝叶斯预测头采样 -> 生成“掩码分歧图” -> 自动选点并请求用户标注（正/负点）-> 更新提示集并重新分割 -> 循环直至满足停止准则。

### 3. 实验设计
*   **数据集**：涵盖 16 个数据集，分为四大领域：
    *   **自然图像**：MS COCO 中的 9 个类别（如狗、猫、巴士、领带等）。
    *   **医疗影像**：乳腺超声、息肉、皮肤病变。
    *   **水下影像**：NDD20（海豚）。
    *   **地震数据**：Netherlands F3（盐丘、白垩层）。
*   **Benchmark 与对比方法**：
    *   **迭代策略**：Human（真实人类标注序列）、Random（随机选点）、Entropy（基于边缘熵选点）、Oracle（基于真值的最优选点上限）。
    *   **单次（One-shot）几何策略**：Saliency、K-Medoids、Max Distance、Shi-Tomasi 角点检测。
*   **评价指标**：峰值归一化 $\Delta$ IoU（单次交互最大增益）、每轮平均增益、AUC（增益曲线下面积）以及最终 IoU。

### 4. 资源与算力
*   **硬件**：使用单台 **NVIDIA H200 GPU (150 GB)**。
*   **训练时长**：预测头训练最多 100 个 epoch，带有早停机制。
*   **推理与消融开销**：
    *   消融实验涉及 38 种不同配置，总计进行了约 **54,000 次前向传播**。
    *   总耗时约 **22.8 GPU 小时**。
    *   拉普拉斯后验拟合过程非常高效，仅需约 **3 分钟**。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。作者不仅在 16 个跨领域数据集上进行了测试，还针对 3 种 SAM 骨干（ViT-H, ViT-B, ViT-Tiny）进行了对比。
*   **消融深度**：针对拉普拉斯近似的子集大小（Subset Size）和后验采样数（Sample Count）进行了 35 种组合的详尽消融，确定了帕累托最优配置。
*   **客观性**：通过与 PointPrompt 数据集中的真实人类标注行为进行直接对比，证明了该算法在减少人类主观偏差方面的有效性。

### 6. 主要结论与发现
*   **性能卓越**：BALD-SAM 在 14/16 个基准测试中排名第一或第二，尤其在医疗和水下领域实现了全胜。
*   **超越人类与 Oracle**：在某些自然图像类别（如 Dog, Stop sign）中，BALD-SAM 的提示效率甚至超过了拥有真值信息的 Oracle 策略。
*   **复杂结构处理**：在处理细长物体（如领带）或复杂边界（如鸟类）时，BALD-SAM 的最终 IoU 显著优于所有单次几何启发式方法。
*   **领域泛化**：即使在 SAM 预训练分布之外的地震数据上，BALD 依然能提供极具启发性的提示路径。

### 7. 优点
*   **高效性**：通过冻结骨干网络，使得在基础模型上进行贝叶斯推理变得实际可行，计算开销极低。
*   **即插即用**：作为一个轻量级模块，可以适配任何已有的 SAM 变体，无需重新训练大模型。
*   **理论支撑**：相比简单的熵方法，BALD 能有效区分“数据本身的模糊性”和“模型知识的匮乏”，从而选出真正能减少模型不确定性的点。

### 8. 不足与局限
*   **骨干限制**：在地震数据等极端 OOD（分布外）场景下，虽然提示策略优于人类，但受限于 SAM 骨干网络本身的特征提取能力，最终的绝对分割精度仍有提升空间。
*   **冷启动依赖**：预测头仍需要少量的带提示标注数据进行预训练，虽然这部分数据量不大，但在完全无监督的场景下无法直接运行。
*   **采样开销**：虽然预测头很轻，但多次蒙特卡洛采样在对实时性要求极高的移动端设备上可能仍存在微小延迟。

（完）
