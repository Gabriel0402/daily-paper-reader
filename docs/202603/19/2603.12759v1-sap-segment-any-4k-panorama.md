---
title: "SAP: Segment Any 4K Panorama"
title_zh: SAP：分割万物 4K 全景图
authors: "Lutao Jiang, Zidong Cao, Weikai Chen, Xu Zheng, Yuanhuiyi Lyu, Zhenyang Li, Zeyu HU, Yingda Yin, Keyang Luo, Runze Zhang, Kai Yan, Shengju Qian, Haidi Fan, Yifan Peng, Xin Wang, Hui Xiong, Ying-Cong Chen"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12759v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 全景实例级分割的基座模型
tldr: 针对全景图像在基础模型上表现不佳的问题，本文提出SAP模型，专门用于4K高分辨率全景实例分割。该方法将全景图重构为固定轨迹的透视视频流，通过球形遍历采样重叠补丁，在保持4K分辨率的同时实现跨视图稳定传播。结合大规模合成数据集训练，SAP在真实世界4K全景基准上比SAM2提升了17.2 mIoU，展现了极强的零样本泛化能力。
selection_source: fresh_fetch
motivation: 现有的透视图像分割模型在处理360°全景图时因畸变和分辨率限制导致性能显著下降。
method: 将全景分割重新定义为固定轨迹的透视视频分割，通过球面连续采样重叠补丁并结合大规模合成数据进行训练。
result: 在真实世界4K全景基准测试中，SAP相比SAM2实现了17.2的零样本mIoU提升。
conclusion: SAP通过轨迹对齐的重构范式，有效解决了高分辨率全景分割的挑战，为具身智能等领域提供了高性能感知方案。
---

## 摘要
可提示实例分割在具身智能和增强现实（AR）系统中被广泛采用，然而在透视图像上训练的基础模型在 360° 全景图上的性能往往会下降。在本文中，我们介绍了 Segment Any 4K Panorama (SAP)，这是一个用于 4K 高分辨率全景实例级分割的基础模型。我们将全景分割重新表述为固定轨迹的透视视频分割，将全景图分解为沿连续球面遍历采样的重叠透视补丁。这种内存对齐的重新表述在保留原生 4K 分辨率的同时，恢复了稳定跨视图传播所需的平滑视点过渡。为了实现大规模监督，我们使用 InfiniGen 引擎合成了 183,440 张带有实例分割标签的 4K 分辨率全景图像。在这种轨迹对齐范式下训练的 SAP 可以有效地泛化到真实世界的 360° 图像，在真实世界 4K 全景基准测试中，相比不同尺寸的原始 SAM2，实现了 +17.2 的零样本 mIoU 提升。

## Abstract
Promptable instance segmentation is widely adopted in embodied and AR systems, yet the performance of foundation models trained on perspective imagery often degrades on 360° panoramas. In this paper, we introduce Segment Any 4K Panorama (SAP), a foundation model for 4K high-resolution panoramic instance-level segmentation. We reformulate panoramic segmentation as fixed-trajectory perspective video segmentation, decomposing a panorama into overlapping perspective patches sampled along a continuous spherical traversal. This memory-aligned reformulation preserves native 4K resolution while restoring the smooth viewpoint transitions required for stable cross-view propagation. To enable large-scale supervision, we synthesize 183,440 4K-resolution panoramic images with instance segmentation labels using the InfiniGen engine. Trained under this trajectory-aligned paradigm, SAP generalizes effectively to real-world 360° images, achieving +17.2 zero-shot mIoU gain over vanilla SAM2 of different sizes on real-world 4K panorama benchmark.

---

## 论文详细总结（自动生成）

### SAP: Segment Any 4K Panorama 论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的分割基础模型（如 SAM、SAM2）主要针对透视图像（Perspective Imagery）训练，在处理 360° 全景图时面临三大挑战：
    1.  **几何畸变**：等距柱状投影（ERP）会导致严重的形状扭曲。
    2.  **边界不连续**：全景图左右边缘在物理上相邻但在图像表达上断裂，导致掩码破碎。
    3.  **超高分辨率处理难**：4K 全景图包含海量细节，而主流模型通常需下采样至 1024px，导致小物体丢失。
*   **研究动机**：随着机器人、AR/VR 和具身智能设备的普及，对高分辨率全景实例分割的需求日益增长，但现有方法多集中于语义分割，缺乏鲁棒的实例级基础模型。

#### 2. 方法论：核心思想与技术细节
*   **核心思想**：将全景分割任务重构为**固定轨迹的透视扫描视频分割**。通过模拟相机在球面上连续移动，将全景图转化为一系列具有重叠区域的透视帧序列，从而利用 SAM2 的视频记忆机制实现跨视图的一致性。
*   **关键技术细节**：
    1.  **全景转透视视频**：定义 N 个模拟相机的内外参，沿球面轨迹采样。设置 90° 视场角（FoV）以平衡畸变与效率。
    2.  **列优先蛇形轨迹（Column-first Zigzag）**：设计了一种可循环的扫描路径，确保相邻帧之间仅有单一角度（经度或纬度）变化，提供平滑的视觉过渡，对齐 SAM2 的时间连续性假设。
    3.  **提示点投影**：将用户在 ERP 平面上的提示点投影到所有可见的透视帧中，作为 SAM2 的输入。
    4.  **掩码重投影与融合**：将各帧预测的掩码通过逆映射回传至 ERP 平面，采用元素级最大值（Element-wise Max）进行融合。
    5.  **轨迹对齐微调**：在合成数据上微调 SAM2 的存储器编码器和掩码解码器，使其适应特定的扫描运动模式。

#### 3. 实验设计
*   **数据集与场景**：
    *   **InfiniGen (合成)**：利用 InfiniGen 引擎合成的 18.3 万张 4K 全景图，包含 640 万个实例标签。
    *   **PAV-SOD (真实世界)**：包含 67 个 4K 全景视频，用于评估真实场景下的零样本（Zero-shot）性能。
    *   **HunyuanWorld-1.0 (生成式)**：使用混元模型生成的 8K 全景图，用于测试对不同艺术风格和超高分辨率的泛化性。
*   **对比方法**：
    *   **SAM2 (Tiny/Small/Base/Large)**：原始官方模型。
    *   **SAM2 + Scan**：仅使用扫描策略但不进行微调的基准。
*   **评估指标**：1-click mIoU（单点提示）和 3-click Error Correction mIoU（三点纠错）。

#### 4. 资源与算力
*   **数据合成算力**：使用了 **40,000 GPU 小时** 利用 InfiniGen 引擎生成合成数据集。
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 128，学习率为 2e-4，采用余弦退火调度。
*   **硬件说明**：文中未明确提及训练所用的具体 GPU 型号（如 A100 或 H100），但从 4 万小时的合成工作量来看，算力投入巨大。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个不同性质的基准（合成、真实、生成）上进行了全面测试。
    *   针对模型尺寸（T/S/B/L）进行了全覆盖对比。
    *   **消融实验**：涵盖了训练数据来源（混合训练 vs 单一训练）、扫描策略有效性、帧重叠比例、以及透视变换 vs 直接切片的对比。
*   **充分性评价**：实验设计非常充分。特别是通过“零样本”测试真实世界数据，客观地证明了模型从合成数据向真实数据迁移的能力。消融实验清晰地揭示了“轨迹对齐”和“重叠采样”是性能提升的关键。

#### 6. 主要结论与发现
*   **性能飞跃**：SAP 在真实世界 4K 全景基准上比原始 SAM2 平均提升了 **17.2 mIoU**。在 Tiny 模型上提升甚至达到了 24.2 mIoU。
*   **记忆对齐的重要性**：单纯使用扫描策略（SAM2+Scan）虽有提升，但必须经过轨迹对齐的微调，才能充分发挥 SAM2 记忆模块在处理球面拓扑时的潜力。
*   **解决边界问题**：通过循环扫描，SAP 有效解决了全景图两极和接缝处的分割断裂问题。

#### 7. 优点与亮点
*   **范式创新**：巧妙地将静态全景图处理转化为动态视频流处理，绕过了复杂的球面卷积设计，直接复用了成熟的视频分割基础模型。
*   **高质量数据贡献**：发布了大规模 4K 全景实例分割数据集，填补了该领域的空白。
*   **高分辨率支持**：原生支持 4K 甚至 8K 输入，不损失细节，且通过补丁化处理规避了显存溢出（OOM）问题。

#### 8. 不足与局限
*   **推理开销**：由于将单张图转化为包含数十帧（如 24 或 48 帧）的视频流，推理时间会显著长于直接处理单张图像的方法。
*   **合成数据偏差**：虽然通过混合训练缓解了灾难性遗忘，但模型核心知识仍高度依赖合成环境的几何分布，在极度复杂的真实动态光影下可能仍有提升空间。
*   **应用限制**：目前主要针对单张全景图，虽然文中提到未来可扩展至全景视频，但当前版本尚未实现跨时间帧的长期跟踪优化。

（完）
