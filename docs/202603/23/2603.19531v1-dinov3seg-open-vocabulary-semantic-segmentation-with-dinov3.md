---
title: "dinov3.seg: Open-Vocabulary Semantic Segmentation with DINOv3"
title_zh: dinov3.seg：基于 DINOv3 的开放词汇语义分割
authors: "Saikat Dutta, Biplab Banerjee, Hamid Rezatofighi"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19531v1"
tags: ["query:mmseg"]
score: 10.0
evidence: 使用视觉语言模型的开放词汇语义分割
tldr: 本研究针对开放词汇语义分割（OVSS）中视觉语言模型在密集预测任务上空间精度不足的问题，提出了 dinov3.seg 框架。该框架基于 DINOv3，通过结合全局与局部文本对齐、引入视觉特征的早期细化与图文相关性的后期细化机制，并结合高分辨率滑动窗口推理策略，显著提升了在复杂场景下的分割精度和鲁棒性，在多个基准测试中达到了领先水平。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型因全局对比学习目标而缺乏密集预测所需的空间精度，导致在复杂场景下的开放词汇分割效果受限。
method: 提出了一种结合全局与局部特征对齐、包含视觉特征早期细化与图文相关性后期细化的架构，并采用滑动窗口推理策略。
result: 在五个广泛使用的 OVSS 基准测试中，该方法一致性地超越了当前最先进的技术，证明了其有效性和鲁棒性。
conclusion: dinov3.seg 通过系统性的架构优化和多阶段特征细化，为开放词汇语义分割提供了一个高性能且稳健的解决方案。
---

## 摘要
开放词汇语义分割 (OVSS) 从一组由文本定义的开放类别中分配像素级标签，要求在推理阶段对未见类别具有可靠的泛化能力。尽管现代视觉语言模型 (VLM) 支持强大的开放词汇识别，但通过全局对比目标学习到的表示对于密集预测任务而言仍非最优，这导致许多 OVSS 方法不得不依赖于对图像-文本相似度图的有限适配或细化。这反过来限制了在复杂、拥挤场景中的空间精度和鲁棒性。我们推出了 dinov3.seg，将 dinov3.txt 扩展为一个专门用于 OVSS 的框架。我们的贡献主要分为四个方面：首先，我们设计了一个针对该骨干网络定制的任务特定架构，系统地借鉴了先前开放词汇分割工作中已确立的设计原则。其次，我们共同利用了与全局 [CLS] 标记以及来自基于 ViT 编码器的局部补丁级 (patch-level) 视觉特征相对齐的文本嵌入，有效地将语义辨别力与细粒度的空间局部性相结合。第三，与主要依赖事后相似度细化的现有方法不同，我们在图像-文本交互之前对视觉表示进行早期细化，随后对生成的图像-文本相关性特征进行后期细化，从而在拥挤场景中实现更准确、更鲁棒的密集预测。最后，我们提出了一种基于滑动窗口聚合的高分辨率局部-全局推理策略，在保持全局上下文的同时保留了空间细节。我们在五个广泛采用的 OVSS 基准测试上进行了大量实验以评估我们的方法。结果证明了该方法的有效性和鲁棒性，其表现始终优于当前的先进方法。

## Abstract
Open-Vocabulary Semantic Segmentation (OVSS) assigns pixel-level labels from an open set of text-defined categories, demanding reliable generalization to unseen classes at inference. Although modern vision-language models (VLMs) support strong open-vocabulary recognition, their representations learned through global contrastive objectives remain suboptimal for dense prediction, prompting many OVSS methods to depend on limited adaptation or refinement of image-text similarity maps. This, in turn, restricts spatial precision and robustness in complex, cluttered scenes. We introduce dinov3.seg, extending dinov3.txt into a dedicated framework for OVSS. Our contributions are four-fold. First, we design a task-specific architecture tailored to this backbone, systematically adapting established design principles from prior open-vocabulary segmentation work. Second, we jointly leverage text embeddings aligned with both the global [CLS] token and local patch-level visual features from ViT-based encoder, effectively combining semantic discrimination with fine-grained spatial locality. Third, unlike prior approaches that rely primarily on post hoc similarity refinement, we perform early refinement of visual representations prior to image-text interaction, followed by late refinement of the resulting image-text correlation features, enabling more accurate and robust dense predictions in cluttered scenes. Finally, we propose a high-resolution local-global inference strategy based on sliding-window aggregation, which preserves spatial detail while maintaining global context. We conduct extensive experiments on five widely adopted OVSS benchmarks to evaluate our approach. The results demonstrate its effectiveness and robustness, consistently outperforming current state-of-the-art methods.

---

## 论文详细总结（自动生成）

以下是对论文《dinov3.seg: Open-Vocabulary Semantic Segmentation with DINOv3》的结构化深度总结：

### 1. 核心问题与整体含义
该论文关注的是**开放词汇语义分割 (OVSS)** 任务，即模型需要识别并分割训练集中未出现的类别。
*   **研究动机**：现有的视觉语言模型（如 CLIP）主要通过全局对比学习进行预训练，这导致其特征在处理像素级密集预测时缺乏足够的空间精度和局部细节。
*   **背景**：虽然最近的 `dinov3.txt` 结合了 DINOv3 的强局部特征和文本对齐，但在 OVSS 任务中仍缺乏专门的密集预测优化。论文旨在通过显式优化，使 DINO 风格的 VLM 能够生成边界更清晰、类别区分度更高的分割结果。

### 2. 方法论
论文提出了 **dinov3.seg** 框架，其核心思想是增强视觉特征的判别力并优化图文匹配的准确性。
*   **全局-局部文本集成**：不同于以往只使用单一文本嵌入的方法，该模型同时提取与图像全局 [CLS] 标记对齐的“全局文本嵌入”和与局部补丁特征对齐的“局部文本嵌入”，通过集成两者的相关性图来增强语义识别。
*   **双阶段细化机制**：
    *   **早期细化 (Early Refinement)**：在图像与文本交互之前，使用基于窗口注意力的 AnyUp 模块对视觉特征进行细化，在保留图文对齐的同时提升特征的密集判别力。
    *   **后期细化 (Late Refinement)**：在生成图文相关性特征后，引入语义先验编码器（如 SAM 的 ViT-L）作为引导，通过空间细化块（Swin Transformer）和类别细化块（Transformer）进一步消除噪声并增强边界一致性。
*   **局部-全局聚合推理 (LGA)**：在推理阶段采用滑动窗口策略，将高分辨率图像的全局特征与重叠子图的局部特征进行聚合，确保在处理大尺寸图像时既有全局上下文，又有细粒度细节。

### 3. 实验设计
*   **数据集**：在 **COCO-Stuff** 上进行训练。在 **ADE20K** (150类和847类)、**Pascal Context** (59类和459类) 以及 **Pascal VOC** (20类) 五个基准数据集上进行测试。
*   **对比方法**：对比了包括 OVSeg、SAN、FC-CLIP、CAT-Seg、H-CLIP、HyperCLIP、ESCNet 和 DPSeg 在内的十余种最先进 (SOTA) 的 OVSS 方法。
*   **评估指标**：主要使用平均交并比 (mIoU)，并专门针对“已见类 (Seen)”和“未见类 (Unseen)”进行了拆分评估。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备了 **4 张 NVIDIA A100 80GB GPU** 的服务器上运行。
*   **训练细节**：使用 AdamW 优化器，Batch size 为 4，共训练 80,000 次迭代。
*   **学习率**：骨干网络和早期细化模块的学习率为 $2 \times 10^{-6}$，其余模块为 $2 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括在 5 个不同规模的数据集上进行性能对比。
*   **消融实验**：针对文本集成、早期细化、后期细化块数量、损失函数（Focal-Dice vs BCE）、推理策略（LGA）以及不同的语义先验编码器（SPE）和 VLM 骨干进行了多组消融实验。
*   **充分性评价**：实验设计非常客观且全面。通过 Seen/Unseen 的性能拆分，清晰地展示了模型在开放词汇泛化方面的真实能力，而非仅仅依赖于对训练类别的过拟合。

### 6. 主要结论与发现
*   **性能领先**：dinov3.seg 在所有测试基准上均达到了 SOTA 水平，尤其在类别极多的 A-847 和 PC-459 数据集上提升显著。
*   **泛化能力**：在未见类别上的性能增益通常高于已见类别，证明了该架构在处理新概念时的鲁棒性。
*   **细化有效性**：早期视觉细化和后期相关性细化的结合，显著改善了复杂场景（如拥挤、遮挡）下的分割质量。

### 7. 优点
*   **架构创新**：首次将 DINOv3 深度适配到 OVSS 任务，并提出了双阶段细化框架。
*   **空间精度高**：得益于 DINOv3 的自监督特征和 LGA 推理策略，模型在物体边界和细小结构的分割上表现优异。
*   **多模态对齐增强**：通过全局和局部文本嵌入的互补，解决了单一对齐方式在复杂语义下的模糊性。

### 8. 不足与局限
*   **计算开销**：模型参数量较大（约 1.18B），主要源于 DINOv3 巨大的骨干网络和 SAM 编码器，这限制了其在端侧设备上的部署。
*   **推理延迟**：虽然比部分扩散模型方法快，但 0.37s 的推理时间（A100 上）仍难以满足实时视频分割的需求。
*   **依赖性**：性能高度依赖于预训练 VLM 的质量，若基础模型存在偏见，可能会传导至分割结果。

（完）
