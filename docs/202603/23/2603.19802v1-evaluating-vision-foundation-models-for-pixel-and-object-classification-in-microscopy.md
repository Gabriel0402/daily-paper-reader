---
title: Evaluating Vision Foundation Models for Pixel and Object Classification in Microscopy
title_zh: 评估显微镜图像中像素和对象分类的视觉基础模型
authors: "Carolin Teuber, Anwai Archit, Tobias Boothe, Peter Ditte, Jochen Rink, Constantin Pape"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19802v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 评估SAM和视觉基础模型在显微镜语义分割中的应用
tldr: 本研究评估了视觉基础模型（VFM）在显微成像中像素和对象分类任务的表现。针对传统方法依赖手工特征且标注效率低的问题，研究者对比了通用模型（如SAM、SAM2、DINOv3）与领域特定模型（如μSAM、PathoSAM）在五个挑战性数据集上的效果。结果表明，结合浅层学习或注意力探测的VFM特征显著优于传统手工特征，为显微图像分析提供了新的基准和改进路径。
selection_source: fresh_fetch
motivation: 尽管深度学习在实例分割中表现优异，但显微镜下的交互式像素和对象分类仍主要依赖手工特征，亟需探索视觉基础模型的应用潜力。
method: 评估了包括SAM系列、DINOv3及领域特定模型在内的多种VFM，并结合浅层学习和注意力探测在五个多样化数据集上进行性能测试。
result: 实验证明，视觉基础模型提取的特征在像素和对象分类性能上一致且显著地优于传统的手工特征。
conclusion: 该研究为显微成像领域建立了VFM性能基准，证明了其在提升分类效率及准确性方面的巨大潜力，并为未来开发提供了方向。
---

## 摘要
深度学习是计算机视觉（包括生物医学成像）中大多数现代方法和工具的基础。然而，对于交互式语义分割（在此背景下通常称为像素分类）和交互式对象级分类（对象分类），基于特征的浅层学习仍然被广泛使用。这是由于该领域数据的多样性、缺乏大型预训练数据集，以及对计算和标签效率的需求。相比之下，显微镜中许多其他视觉任务（最显著的是细胞实例分割）的最先进工具已经依赖于深度学习，并且最近从视觉基础模型（VFMs），特别是 SAM 中获益匪浅。在这里，我们研究了与当前方法相比，VFMs 是否也能改进像素和对象分类。为此，我们在五个多样且具有挑战性的数据集上，评估了多种 VFMs，包括通用模型（SAM、SAM2、DINOv3）和领域特定模型（$μ$SAM、PathoSAM），并结合了浅层学习和注意力探测（attentive probing）。我们的结果表明，与手工设计的特征相比，性能有一致的提升，并为实际改进提供了清晰的路径。此外，我们的研究建立了显微镜领域 VFMs 的基准，并为该领域的未来发展提供了参考。

## Abstract
Deep learning underlies most modern approaches and tools in computer vision, including biomedical imaging. However, for interactive semantic segmentation (often called pixel classification in this context) and interactive object-level classification (object classification), feature-based shallow learning remains widely used. This is due to the diversity of data in this domain, the lack of large pretraining datasets, and the need for computational and label efficiency. In contrast, state-of-the-art tools for many other vision tasks in microscopy - most notably cellular instance segmentation - already rely on deep learning and have recently benefited substantially from vision foundation models (VFMs), particularly SAM. Here, we investigate whether VFMs can also improve pixel and object classification compared to current approaches. To this end, we evaluate several VFMs, including general-purpose models (SAM, SAM2, DINOv3) and domain-specific ones ($μ$SAM, PathoSAM), in combination with shallow learning and attentive probing on five diverse and challenging datasets. Our results demonstrate consistent improvements over hand-crafted features and provide a clear pathway toward practical improvements. Furthermore, our study establishes a benchmark for VFMs in microscopy and informs future developments in this area.

---

## 论文详细总结（自动生成）

以下是对论文《Evaluating Vision Foundation Models for Pixel and Object Classification in Microscopy》的结构化总结：

### 1. 核心问题与背景
*   **核心问题**：在显微镜图像分析中，尽管深度学习已在实例分割领域取得巨大成功，但**交互式像素分类**（语义分割）和**对象分类**任务仍广泛依赖于基于手工特征（如高斯模糊、Hessian矩阵等）的浅层学习方法（如随机森林）。
*   **研究动机**：手工特征虽然计算快、标注需求低，但在处理复杂、多样化的生物成像数据时表现有限。研究者试图探讨：**视觉基础模型（VFMs）**提取的特征是否能取代手工特征，在保持标注效率的同时显著提升分类性能。

### 2. 方法论
论文评估了将 VFM 作为特征提取器，并结合下游分类器的流水线：
*   **核心思想**：利用预训练好的 VFM 骨干网络（通常是 ViT 架构）提取多尺度特征图，将其作为输入传递给浅层学习器或轻量级探测头。
*   **关键技术细节**：
    *   **特征提取**：从 VFM 的不同层（如中间层和最后一层）提取特征，并进行上采样以匹配原始图像分辨率。
    *   **分类器方案**：
        1.  **浅层学习（Shallow Learning）**：使用随机森林（Random Forest）处理 VFM 特征，模拟 Ilastik 等工具的交互式工作流。
        2.  **注意力探测（Attentive Probing）**：在冻结的 VFM 特征上添加轻量级的 MLP 或注意力机制头进行微调。
*   **评估的模型**：
    *   **通用模型**：SAM (Segment Anything Model), SAM 2, DINOv3。
    *   **领域特定模型**：$\mu$SAM（针对显微镜优化）, PathoSAM（针对病理学优化）。

### 3. 实验设计
*   **数据集**：涵盖了 5 个具有代表性且挑战性的显微镜数据集：
    1.  **LiveCell**：相位对比显微镜下的细胞。
    2.  **TissueNet**：多种组织类型的荧光图像。
    3.  **DeepBacs**：细菌图像。
    4.  **Planaria**：涡虫的整体成像。
    5.  **Mouse Embryo**：小鼠胚胎发育图像。
*   **Benchmark（基准）**：以传统的**手工设计特征（Hand-crafted features）**为基准，这些特征模拟了主流工具 Ilastik 所使用的特征集。
*   **对比维度**：对比了不同 VFM 模型、不同特征提取层以及不同分类器组合在像素级和对象级分类上的表现。

### 4. 资源与算力
*   **算力说明**：论文中未详细列出具体的 GPU 型号、数量或总训练时长。
*   **推断**：由于涉及 SAM 2 和 DINOv3 等大型 ViT 模型，且包含特征提取过程，实验必然依赖于现代高性能 GPU（如 A100 或 V100 级别）。但作者强调了“浅层学习”部分对标注效率的要求，暗示其下游分类过程相对轻量。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 5 个跨物种、跨成像模态的数据集，对比了至少 5 种主流 VFM 模型，并进行了像素级和对象级两种任务的测试。
*   **充分性**：实验设计较为全面，不仅对比了通用模型与领域模型，还探讨了特征层选择对结果的影响。
*   **公平性**：所有模型均在相同的测试集上评估，并使用统一的评价指标（如 F1-score），确保了对比的客观性。

### 6. 主要结论与发现
*   **VFM 显著优于传统方法**：在所有数据集上，基于 VFM 特征的分类性能一致且显著地超过了手工设计特征。
*   **通用模型表现强劲**：SAM 2 和 DINOv3 等通用基础模型在显微镜任务中表现极佳，甚至在某些情况下优于专门针对生物医学训练的模型（如 $\mu$SAM）。
*   **特征层的重要性**：对于像素分类，结合中间层和深层特征通常能获得最佳效果，因为中间层保留了更多的空间细节。
*   **路径明确**：研究证明了将 VFM 集成到现有交互式分析工具（如 Ilastik, QuPath）中是提升显微镜图像分析效率的可行路径。

### 7. 优点
*   **实用性强**：直接针对生物学家常用的“交互式分类”工作流进行改进，具有很强的落地潜力。
*   **基准意义**：为显微镜领域应用 VFM 建立了清晰的性能基准，填补了该领域在语义分割任务上的评估空白。
*   **模型多样性**：对比了从通用到领域特定的多种前沿模型，结论具有普适性。

### 8. 不足与局限
*   **计算开销**：虽然分类器是轻量的，但 VFM 骨干网络的特征提取（推理过程）比计算手工特征要耗费更多的计算资源和显存，这可能限制其在普通个人电脑上的交互速度。
*   **数据偏差风险**：尽管涵盖了 5 个数据集，但显微镜成像种类繁多（如电镜、超分辨等），模型在未覆盖模态上的泛化能力仍需验证。
*   **实时性挑战**：论文未深入讨论在大尺寸图像（如全切片扫描图像）上进行实时交互时的延迟问题。

（完）
