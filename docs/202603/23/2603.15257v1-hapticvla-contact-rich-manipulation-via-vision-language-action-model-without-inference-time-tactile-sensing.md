---
title: "HapticVLA: Contact-Rich Manipulation via Vision-Language-Action Model without Inference-Time Tactile Sensing"
title_zh: HapticVLA：无需推理阶段触觉感知的视觉-语言-动作模型丰富接触操作
authors: "Konstantin Gubernatorov, Mikhail Sannikov, Ilya Mikhalchuk, Egor Kuznetsov, Makar Artemov, Ogunwoye Faith Ouwatobi, Marcelino Fernando, Artem Asanov, Ziang Guo, Dzmitry Tsetserukou"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15257v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于接触密集型机器人操作的视觉-语言-动作模型
tldr: HapticVLA 提出了一种无需推理时触觉传感器的视觉-语言-动作模型，用于处理富接触操作任务。该方法通过两阶段训练：首先利用安全感知奖励加权流匹配（SA-RWFM）训练具备触觉意识的专家模型，随后通过触觉蒸馏（TD）将该能力迁移至常规 VLA，使其能仅凭视觉和状态预测触觉特征。实验表明，该模型在真实场景中表现优异，超越了直接使用触觉反馈的基准模型。
selection_source: fresh_fetch
motivation: 触觉传感器硬件成本高且难以在不同机器人平台间复用，限制了具备触觉感知能力的 VLA 模型的广泛应用。
method: 采用两阶段法，先通过包含触觉奖励的流匹配训练专家模型，再将其触觉推理能力蒸馏到仅依赖视觉和状态输入的 VLA 模型中。
result: "在真实世界实验中，HapticVLA 达到了 86.7% 的平均成功率，显著优于包括推理时带有直接触觉反馈在内的各类基准模型。"
conclusion: 该研究证明了可以通过离线学习和蒸馏技术，在不依赖实时触觉硬件的情况下，赋予机器人模型复杂的富接触操作和安全感知能力。
---

## 摘要
触觉感知是视觉-语言-动作（VLA）架构的一项关键能力，因为它能在丰富接触的任务中实现灵巧且安全的操作。然而，对专用触觉硬件的依赖增加了成本，并降低了在不同机器人平台上的可复现性。我们认为，触觉感知操作可以进行离线学习，并在推理时无需直接触觉反馈即可部署。为此，我们提出了 HapticVLA，它分为两个紧密耦合的阶段：安全感知奖励加权流匹配（SA-RWFM）和触觉蒸馏（TD）。SA-RWFM 训练了一个流匹配动作专家，该专家结合了预计算的安全感知触觉奖励，对过大的抓取力和次优的抓取轨迹进行惩罚。TD 进一步将这种触觉感知能力转移到常规 VLA 中：我们从 SA-RWFM 教师模型中蒸馏出一个紧凑的触觉 token，并训练一个学生 VLA 从视觉和状态模态中预测该 token，从而在推理时无需车载触觉传感器即可生成触觉感知动作。这种设计在 VLA 中保留了丰富接触的触觉感知推理，同时在部署期间消除了对车载触觉传感器的需求。在真实世界实验中，HapticVLA 达到了 86.7% 的平均成功率，始终优于基准 VLA，包括那些在推理期间提供直接触觉反馈的版本。

## Abstract
Tactile sensing is a crucial capability for Vision-Language-Action (VLA) architectures, as it enables dexterous and safe manipulation in contact-rich tasks. However, reliance on dedicated tactile hardware increases cost and reduces reproducibility across robotic platforms. We argue that tactile-aware manipulation can be learned offline and deployed without direct haptic feedback at inference. To this end, we present HapticVLA, which proceeds in two tightly coupled stages: Safety-Aware Reward-Weighted Flow Matching (SA-RWFM) and Tactile Distillation (TD). SA-RWFM trains a flow-matching action expert that incorporates precomputed, safety-aware tactile rewards penalizing excessive grasping force and suboptimal grasping trajectories. TD further transfers this tactile-aware capability into a conventional VLA: we distill a compact tactile token from the SA-RWFM teacher and train a student VLA to predict that token from vision and state modalities, enabling tactile-aware action generation at inference without requiring on-board tactile sensors. This design preserves contact-rich tactile-aware reasoning within VLA while removing the need for on-board tactile sensors during deployment. On real-world experiments, HapticVLA achieves a mean success rate of 86.7%, consistently outperforming baseline VLAs - including versions provided with direct tactile feedback during inference.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **HapticVLA** 的新型视觉-语言-动作（VLA）模型框架，旨在解决机器人操作中对触觉传感器的依赖问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 VLA 模型主要依赖视觉感知，缺乏对物体硬度、脆性及接触力的理解，导致在处理易碎或软体对象（如鸡蛋、塑料罐）时容易造成损坏。
*   **研究动机**：虽然引入触觉传感器可以解决上述问题，但专用触觉硬件成本高昂、安装复杂且难以在不同机器人平台间复用。
*   **整体含义**：论文提出一种“离线学习触觉、在线无需传感器”的方案。通过在训练阶段利用触觉数据学习安全操作策略，并将其知识蒸馏到仅依赖视觉和状态输入的模型中，从而在推理阶段实现具备触觉意识的灵巧操作，而无需实际安装触觉传感器。

### 2. 方法论：核心思想与技术细节
HapticVLA 的训练分为三个紧密耦合的阶段：
*   **安全感知奖励计算（Safety-Aware Reward Calculation）**：
    *   利用触觉传感器（100个触点/指尖）采集压力分布、平均力、压力集中度及滑动情况。
    *   定义**安全奖励函数**：惩罚过大的抓取力、压力不均、物体滑动及非对称受力。
    *   计算**情节风险评分（Episode Risk）**：综合评估整个操作过程中的安全性。
*   **安全感知奖励加权流匹配（SA-RWFM）**：
    *   **核心思想**：在离线微调阶段，根据计算出的奖励对训练样本进行加权。高奖励（安全且成功）的样本权重更高，引导模型学习安全的动作分布。
    *   **技术细节**：采用流匹配（Flow Matching）作为动作生成器，并引入“锚点正则化（Anchor Regularization）”防止模型在强化学习微调过程中发生模式崩溃或偏离初始演示。
*   **触觉蒸馏（Tactile Distillation, TD）**：
    *   **教师模型**：使用带有触觉输入和 SA-RWFM 训练的专家模型。
    *   **学生模型**：常规 VLA（SmolVLA），仅输入视觉和关节状态。
    *   **过程**：学生模型通过模仿教师生成的动作序列（混合了真实演示和教师预测的动作），隐式地学习了教师模型中的力控逻辑和触觉推理能力。

### 3. 实验设计
*   **实验场景**：
    1.  **塑料罐取放**：测试对可变形物体的处理。
    2.  **华夫饼取放**：测试对易碎食品的精确力控。
    3.  **鸡蛋取放**：极高难度的脆性物体操作，需精确调节抓取宽度和力度。
*   **数据集**：
    *   **真实世界**：310 个情节（包含成功和故意失败的样本，用于奖励学习）。
    *   **仿真环境**：在 Isaac Sim 中构建数字孪生，生成 1000 个辅助情节以增强鲁棒性。
*   **基准模型（Benchmarks）**：
    *   **SmolVLA (0.45B)**：基础模型。
    *   **X-VLA (0.9B)**：跨具身适配性强的紧凑模型。
    *   **VLA-0**：在同类硬件上表现优异的大型模型。

### 4. 资源与算力
*   **推理硬件**：实验在 **NVIDIA Jetson Orin NX (16GB)** 边缘计算设备上运行，证明了模型的轻量化和高效性。
*   **训练算力**：文中未明确给出具体的训练 GPU 型号、数量及总训练时长，但提到使用了 SmolVLA 作为底座进行微调，且蒸馏过程是完全离线的。

### 5. 实验数量与充分性
*   **实验规模**：每个任务对每个模型进行了 **20 次独立测试**，总计进行了大量的真实机器人实验。
*   **消融实验**：针对“有无触觉蒸馏”和“同步/异步推理”进行了对比实验。
*   **充分性评价**：实验设计较为客观，涵盖了从软体到极易碎物体的梯度难度。通过对比包含触觉反馈的基准模型，证明了蒸馏后的模型甚至能超越直接使用传感器的模型，实验结果具有说服力。

### 6. 主要结论与发现
*   **性能卓越**：HapticVLA 达到了 **86.7%** 的平均成功率，远超所有基准模型（基准模型在鸡蛋任务上几乎全部失败）。
*   **蒸馏的有效性**：通过蒸馏学习到的“虚拟触觉”比直接处理原始传感器信号更有效，因为它过滤了噪声并对齐了视觉语义。
*   **安全性**：SA-RWFM 显著提升了模型在接触密集型任务中的安全性，有效避免了对物体的结构性损坏。

### 7. 优点
*   **硬件解耦**：最大的亮点在于推理时无需触觉传感器，极大地降低了部署成本和硬件门槛。
*   **安全集成**：将强化学习的奖励加权机制引入流匹配 VLA，解决了生成式模型难以约束力度的痛点。
*   **端到端优化**：从奖励计算到知识蒸馏形成闭环，使得轻量级模型也能具备复杂的物理推理能力。

### 8. 不足与局限
*   **数据依赖**：虽然推理不需要传感器，但**训练阶段仍需高质量的触觉数据**，这限制了其在完全没有触觉设备的实验室中的初始开发。
*   **任务泛化风险**：实验仅限于三个特定的取放任务，对于更复杂的动态接触（如擦拭、插入）的泛化能力尚待验证。
*   **样本量较小**：真实世界数据集（310个情节）相对较小，可能存在对特定环境或光照的过拟合风险。

（完）
