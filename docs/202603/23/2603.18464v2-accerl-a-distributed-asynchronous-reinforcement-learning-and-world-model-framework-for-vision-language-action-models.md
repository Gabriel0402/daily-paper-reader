---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种用于视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v2"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于视觉-语言-动作模型的分布式强化学习框架
tldr: 针对大规模视觉-语言-动作（VLA）模型在强化学习中面临的计算效率低和数据获取难等挑战，本文提出了AcceRL框架。该框架采用完全异步且解耦的设计，通过物理隔离训练、推理和采样过程消除同步障碍，并首次将可训练的世界模型集成到分布式异步流水线中以生成虚拟经验。实验证明，AcceRL在LIBERO基准上达到了SOTA水平，展现出超线性的吞吐量扩展能力和极高的采样效率。
selection_source: fresh_fetch
motivation: 大规模VLA模型在强化学习训练中存在计算效率低下和数据获取困难的瓶颈。
method: 提出一种完全异步解耦的框架，物理隔离训练、推理与采样，并集成可插拔的世界模型生成虚拟经验。
result: 在LIBERO基准测试中取得SOTA性能，实现超线性吞吐量扩展及卓越的硬件利用率。
conclusion: AcceRL为大规模VLA模型的高效分布式强化学习训练提供了一种极具扩展性和样本效率的解决方案。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样（rollouts）来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，以生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它表现出吞吐量的超线性扩展和高效的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。代码已在 https://github.com/distanceLu/AcceRL 公开。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO~\cite{liu2023libero} benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks. Code is publicly available at https://github.com/distanceLu/AcceRL.

---

## 论文详细总结（自动生成）

### AcceRL：分布式异步强化学习与世界模型框架论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
在大规模视觉-语言-动作（VLA）模型中应用强化学习（RL）面临两大瓶颈：
*   **计算效率低下**：传统同步RL框架存在“长尾效应”，即训练器必须等待最慢的采样节点完成，导致高性能GPU在等待物理模拟器时处于闲置状态。
*   **数据获取困难**：VLA模型参数量巨大（如7B），对数据需求极高，而物理模拟器的运行速度和真实环境交互成本限制了采样频率。
**AcceRL** 的提出旨在通过物理隔离训练、推理和采样过程，消除同步障碍，并引入世界模型（World Model）实现“在想象中学习”，从而大幅提升训练吞吐量和样本效率。

#### 2. 论文提出的方法论
AcceRL 采用了一种完全解耦的异步架构，核心技术包括：
*   **双层异步机制**：
    *   **宏观异步**：将训练节点（Trainer）与采样节点（Rollout）解耦。采样节点持续向分布式重放池发送轨迹，训练节点独立采样并更新参数，通过ZeRO优化器减少内存占用。
    *   **微观异步**：将环境交互与模型推理解耦。采用“推理即服务（Inference-as-a-Service）”模式，多个环境实例共享推理池，通过动态窗口机制实现高效的GPU批处理。
*   **集成可训练的世界模型**：
    *   首次在分布式异步流水线中集成即插即用的世界模型（基于扩散模型的DIAMOND架构）。
    *   通过“想象采样”生成虚拟经验，将在线样本效率提升了200倍（20,000%）。
*   **算法优化**：
    *   **GIPO算法**：采用高斯重要性采样策略优化（GIPO）替代标准PPO的硬截断，缓解异步架构中的策略滞后（Policy Lag）问题。
    *   **词表瘦身（Vocabulary Slimming）**：裁剪语言模型头，仅保留动作相关的Token，降低显存占用并提升吞吐量。
    *   **价值重计算与全局优势归一化**：确保在异步更新过程中，优势信号与当前策略保持一致。

#### 3. 实验设计
*   **数据集/场景**：使用了 **LIBERO** 机器人操作基准测试，涵盖四个子集：LIBERO-Spatial（空间）、LIBERO-Object（物体）、LIBERO-Goal（目标）和 LIBERO-Long（长程任务）。
*   **Benchmark**：以物理引擎 MuJoCo 为基础，利用 OSMesa 进行离屏渲染。
*   **对比方法**：
    *   **OpenVLA-OFT**：基于正交微调的监督学习基准。
    *   **RLinf-VLA** 与 **SimpleVLA-RL**：近期最先进的VLA强化学习框架。

#### 4. 资源与算力
*   **GPU型号与数量**：实验中使用了 **NVIDIA H200** GPU。
*   **扩展性测试**：
    *   训练端（Trainer）扩展至 **7张 GPU**。
    *   采样端（Rollout Workers）扩展至 **64个 节点**。
*   **训练时长**：文中未明确给出总训练小时数，但强调了在7张H200上达到了 **104.22 SPS**（每秒样本数）的超线性吞吐量。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括吞吐量扩展性实验、LIBERO全套任务性能对比、以及针对世界模型效率的专项测试。
*   **消融实验**：非常充分。分别针对“价值重计算机制”、“GIPO算法 vs PPO算法”以及“世界模型对样本效率的提升”进行了对比实验。
*   **客观性**：通过展示原始评估曲线（如GIPO与PPO的对比）揭示了异步系统初期的统计偏差，体现了实验的透明度和客观性。

#### 6. 论文的主要结论与发现
*   **性能领先**：AcceRL 在 LIBERO 所有类别中均达到 SOTA，尤其在长程任务（LIBERO-Long）中成功率达 99.1%，远超监督学习基准（90.7%）。
*   **超线性扩展**：得益于 ZeRO 优化和解耦设计，系统吞吐量随硬件增加呈超线性增长，GPU利用率保持在 94% 以上。
*   **样本效率飞跃**：引入世界模型后，仅需 10,000 次真实环境交互即可实现策略的快速突破，样本效率提升极其显著。

#### 7. 优点
*   **架构创新**：物理隔离训练、推理和采样的设计彻底解决了分布式RL中的硬件闲置问题。
*   **系统与算法结合**：不仅优化了系统工程，还针对异步带来的策略滞后问题提出了 GIPO 算法，具有很强的实用性。
*   **即插即用**：世界模型模块化设计，可根据需求灵活开启，极大地增强了框架的通用性。

#### 8. 不足与局限
*   **模型对齐限制**：论文提到目前该框架尚不支持大规模语言模型的后训练（Post-training）对齐，未来需扩展至全量模型对齐。
*   **环境依赖**：实验主要集中在模拟环境（LIBERO），尚未展示在真实物理机器人上的部署表现及跨环境的泛化能力。
*   **计算成本**：虽然效率高，但运行高保真扩散世界模型和大规模VLA推理池仍需要极高的硬件门槛（如H200集群）。

（完）
