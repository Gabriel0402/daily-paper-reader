---
title: "GHOST: Ground-projected Hypotheses from Observed Structure-from-Motion Trajectories"
title_zh: GHOST：基于观测到的运动恢复结构轨迹的地面投影假设
authors: "Tomasz Frelek, Rohan Patil, Akshar Tumu, Henrik I. Christensen"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20583v2"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用自监督学习为自动驾驶分割车辆轨迹
tldr: 本研究提出GHOST，一种自监督的单目图像车辆轨迹分割方法。通过利用大规模行车记录仪视频，将SfM恢复的相机轨迹投影至地面作为隐式监督信号，无需人工标注即可训练深度网络。该模型能从单张RGB图像预测符合场景布局和车道拓扑的可行路径，在NuScenes等数据集上表现出良好的泛化能力和跨平台迁移潜力，为自动驾驶路径规划提供了新思路。
selection_source: fresh_fetch
motivation: 旨在解决自动驾驶中复杂城市环境下，无需人工标注即可从单目图像中预测可行行驶轨迹的难题。
method: 利用SfM从大规模视频中提取自我运动轨迹并投影至地面生成空间掩码，以此作为标签训练深度分割网络。
result: 在NuScenes数据集上实现了可靠的轨迹预测，并成功通过微调迁移至电动滑板车平台，展现了极强的泛化性。
conclusion: 证明了大规模自我运动蒸馏能使模型隐式掌握道路结构，实现无需显式建模的高效路径假设估计。
---

## 摘要
我们提出了一种可扩展的自监督方法，用于在复杂城市环境下从单目图像中分割自动驾驶的可行车辆轨迹。利用大规模行车记录仪视频，我们将记录的本车运动视为隐式监督，并通过单目运动恢复结构（SfM）恢复相机轨迹，将其投影到地面上，从而在无需人工标注的情况下生成已行驶区域的空间掩码。这些自动生成的标签被用于训练一个深度分割网络，该网络在运行时仅凭单张 RGB 图像即可预测受运动约束的路径提议，而无需对道路或车道线进行显式建模。该模型在多样化、无约束的网络数据上进行训练，隐式地捕捉了场景布局、车道拓扑和交叉口结构，并能泛化到不同的相机配置。我们在 NuScenes 数据集上评估了我们的方法，展示了可靠的轨迹预测能力，并进一步证明了通过轻量级微调将其迁移到电动滑板车平台的可行性。我们的结果表明，大规模本车运动蒸馏可以产生超出演示轨迹的结构化且可泛化的路径提议，从而实现通过图像分割进行轨迹假设估计。

## Abstract
We present a scalable self-supervised approach for segmenting feasible vehicle trajectories from monocular images for autonomous driving in complex urban environments. Leveraging large-scale dashcam videos, we treat recorded ego-vehicle motion as implicit supervision and recover camera trajectories via monocular structure-from-motion, projecting them onto the ground plane to generate spatial masks of traversed regions without manual annotation. These automatically generated labels are used to train a deep segmentation network that predicts motion-conditioned path proposals from a single RGB image at run time, without explicit modeling of road or lane markings. Trained on diverse, unconstrained internet data, the model implicitly captures scene layout, lane topology, and intersection structure, and generalizes across varying camera configurations. We evaluate our approach on NuScenes, demonstrating reliable trajectory prediction, and further show transfer to an electric scooter platform through light fine-tuning. Our results indicate that large-scale ego-motion distillation yields structured and generalizable path proposals beyond the demonstrated trajectory, enabling trajectory hypothesis estimation via image segmentation.

---

## 论文详细总结（自动生成）

这是一份关于论文《GHOST: Ground-projected Hypotheses from Observed Structure-from-Motion Trajectories》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何在不依赖昂贵的人工语义标注、高精地图或复杂传感器（如LiDAR）的情况下，让自动驾驶系统仅通过单目摄像头图像就能理解环境并预测可行的行驶轨迹？
*   **研究背景**：传统的轨迹预测依赖大量人工标注或校准后的传感器数据。然而，互联网上存在海量的行车记录仪（Dashcam）视频，这些视频本身就包含了人类驾驶员在复杂环境下的“演示轨迹”。本研究旨在通过自监督学习，将这些海量视频中的隐式运动信息转化为可用于训练的监督信号。

### 2. 方法论
GHOST 框架主要包含三个阶段：
*   **SfM 轨迹提取**：使用 COLMAP（一种运动恢复结构算法）处理原始单目视频，提取相机的 6-DoF 位姿和稀疏 3D 点云。为了处理大规模数据，视频被切分为 20 分钟的片段进行并行处理。
*   **地面投影与掩码生成（关键技术）**：
    *   **高度估计**：由于单目 SfM 具有尺度不确定性，研究者利用 Theil-Sen 稳健回归算法，根据路面 3D 点云估计相机相对于地面的高度（$H_{cam}$）。
    *   **投影建模**：假设车辆遵循运动学自行车模型，将未来 5 秒的相机轨迹投影到地面，并根据相机高度赋予一定的物理宽度，生成空间轨迹掩码（GT Mask）。
*   **轨迹分割网络训练**：
    *   **模型架构**：采用带有 ResNet34 或 SegFormer 后端的 UNet 结构。
    *   **非对称损失函数**：设计了一种加权负对数似然损失，**加大对漏报（False Negative）的惩罚**。这迫使模型不仅学习视频中实际发生的单一轨迹，还能预测出场景中所有潜在的可行路径（多假设预测）。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：从 YouTube 收集的 30 小时、跨越 14 个城市、涵盖多种天气和光照条件的行车记录仪视频（处理后得到约 65 万帧标注数据）。
    *   **评估**：使用 **NuScenes**（OpenLane-V2 Subset B）数据集。这是一个完全分布外（OOD）的数据集，用于验证模型的泛化能力。
    *   **迁移实验**：在加州大学圣地亚哥分校（UCSD）校园内收集的 40 分钟电动滑板车视频（2.1 万帧）。
*   **Benchmark 与对比**：
    *   使用 **Soft IoU**（Ruzicka 相似度）作为主要评价指标。
    *   对比了 ResNet34 和 SegFormer 两种不同的视觉骨干网络。
    *   在滑板车实验中，对比了“从头训练”与“在大规模数据上预训练后微调”的效果。

### 4. 资源与算力
*   **算力说明**：文中提到使用了 GPU 加速的 Ceres Solver 进行 SfM 优化。
*   **训练细节**：预训练进行了 25 个 epoch，学习率为 $3 \times 10^{-4}$；滑板车微调进行了 10 个 epoch，学习率降至 $10^{-5}$。
*   **缺失信息**：论文**未明确指出**具体的 GPU 型号、数量以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：使用了 65 万帧的大规模自监督数据，这在同类研究中属于较大规模。
*   **充分性**：
    *   **泛化性验证**：在 NuScenes 上的评估证明了模型在未见过的城市和右舵驾驶环境下的鲁棒性。
    *   **多假设验证**：通过定性分析展示了模型在交叉口和分叉路口预测多条可行路径的能力。
    *   **跨平台验证**：成功从汽车迁移到电动滑板车，证明了方法的通用性。
*   **局限性**：定量对比实验相对较少，主要集中在自身架构的对比，缺乏与当前最先进的（SOTA）有监督轨迹预测方法的直接数值对比。

### 6. 主要结论与发现
*   **自监督可行性**：证明了直接从无约束的互联网视频中蒸馏本车运动（Ego-motion）是可行的，且能产生高质量的训练信号。
*   **隐式语义学习**：模型在没有显式车道线标注的情况下，学会了避开障碍物（如对向卡车）并识别道路拓扑结构。
*   **多模态预测**：非对称损失函数成功引导模型预测出“潜在”的可行路径，而不仅仅是复现视频中的单一路径。
*   **跨域迁移**：预训练模型在小规模特定领域（如滑板车）数据上微调后，表现远优于从头训练，显示出强大的特征提取能力。

### 7. 优点与亮点
*   **极高的扩展性**：无需人工标注，理论上可以利用无限的互联网视频数据进行持续进化。
*   **鲁棒的高度估计**：通过稳健回归解决了单目 SfM 的尺度缩放问题，使模型能适应不同安装高度的摄像头。
*   **架构简洁**：将复杂的轨迹预测问题简化为图像分割问题，推理效率高，易于部署。

### 8. 不足与局限
*   **几何约束限制**：SfM 在车辆直线行驶（低视差）时容易失效，导致部分数据无法生成有效掩码（约 35% 的数据被剔除）。
*   **静态场景假设**：SfM 依赖静态环境假设，在极度拥堵或动态物体密集的场景下，生成的轨迹标签可能存在噪声。
*   **地面平坦假设**：高度估计依赖局部地面平坦的假设，在剧烈坡度变化（如山路或立交桥）时可能产生误差。
*   **评估指标局限**：使用高精地图（HD Map）作为 Ground Truth 进行评估时，由于地图本身可能缺失连接关系，会导致模型预测正确但得分较低的情况。

（完）
