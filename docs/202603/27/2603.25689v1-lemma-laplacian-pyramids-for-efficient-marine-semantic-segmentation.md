---
title: "LEMMA: Laplacian pyramids for Efficient Marine SeMAntic Segmentation"
title_zh: LEMMA：用于高效海洋语义分割的拉普拉斯金字塔
authors: "Ishaan Gakhar, Laven Srivastava, Sankarshanaa Sagaram, Aditya Kasliwal, Ujjwal Verma"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25689v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 用于海洋遥感的轻量级语义分割
tldr: 针对无人船自主导航和海洋遥感监测中深度学习模型计算开销大的问题，本文提出LEMMA模型。该模型利用拉普拉斯金字塔增强边缘识别，在特征提取早期整合边缘信息，显著降低了模型复杂度和推理时间。在多个海洋数据集上，LEMMA在大幅减少参数量和计算量的同时，实现了最先进的分割精度，为资源受限环境下的实时应用提供了高效方案。
selection_source: fresh_fetch
motivation: 现有的深度学习语义分割模型因计算成本高、资源消耗大，难以在实时、低成本的海洋环境监测中部署。
method: 提出LEMMA架构，通过拉普拉斯金字塔在特征提取早期增强边缘信息，从而避免深层网络中昂贵的特征图计算。
result: "相比现有模型，LEMMA减少了高达71倍的参数量和84.65%的推理时间，并在溢油和Mastr1325数据集上分别达到93.42%和98.97%的精度。"
conclusion: LEMMA在保持极高性能的同时极大地提升了计算效率，是适用于资源受限海洋环境的理想轻量化语义分割方案。
---

## 摘要
海洋环境中的语义分割对于无人船（USVs）的自主导航以及溢油等沿海地球观测事件至关重要。然而，现有方法通常依赖于深度卷积神经网络（CNNs）和基于 Transformer 的架构，由于其高昂的计算成本和资源密集型特性，在部署方面面临挑战。这些局限性阻碍了在现实海洋环境中实现实时、低成本应用的实用性。为解决这一问题，我们提出了 LEMMA，这是一种专为资源受限下的精确遥感分割而设计的轻量级语义分割模型。所提出的架构利用拉普拉斯金字塔（Laplacian Pyramids）来增强边缘识别，这是在灾害响应、环境监测和沿海监控等复杂海洋环境中进行有效特征提取的关键组件。通过在特征提取过程的早期整合边缘信息，LEMMA 消除了在更深的网络层中进行高计算成本特征图计算的需求，从而大幅降低了模型大小、复杂度和推理时间。与现有模型相比，LEMMA 在来自不同平台的多个数据集上展示了最先进的性能，同时将可训练参数和计算需求降低了高达 71 倍，GFLOPs 降低了高达 88.5%，推理时间缩短了高达 84.65%。实验结果突显了其有效性和现实应用价值，包括在 Oil Spill 数据集上达到 93.42% 的 IoU，在 Mastr1325 数据集上达到 98.97% 的 mIoU。

## Abstract
Semantic segmentation in marine environments is crucial for the autonomous navigation of unmanned surface vessels (USVs) and coastal Earth Observation events such as oil spills. However, existing methods, often relying on deep CNNs and transformer-based architectures, face challenges in deployment due to their high computational costs and resource-intensive nature. These limitations hinder the practicality of real-time, low-cost applications in real-world marine settings.   To address this, we propose LEMMA, a lightweight semantic segmentation model designed specifically for accurate remote sensing segmentation under resource constraints. The proposed architecture leverages Laplacian Pyramids to enhance edge recognition, a critical component for effective feature extraction in complex marine environments for disaster response, environmental surveillance, and coastal monitoring. By integrating edge information early in the feature extraction process, LEMMA eliminates the need for computationally expensive feature map computations in deeper network layers, drastically reducing model size, complexity and inference time. LEMMA demonstrates state-of-the-art performance across datasets captured from diverse platforms while reducing trainable parameters and computational requirements by up to 71x, GFLOPs by up to 88.5\%, and inference time by up to 84.65\%, as compared to existing models. Experimental results highlight its effectiveness and real-world applicability, including 93.42\% IoU on the Oil Spill dataset and 98.97\% mIoU on Mastr1325.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LEMMA** 的轻量级语义分割模型，专门针对资源受限环境下的海洋遥感任务（如无人船导航和无人机溢油监测）进行了优化。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的先进语义分割模型（如基于深层 CNN 或 Transformer 的架构）虽然精度高，但计算开销巨大、参数量多、推理延迟高。
*   **研究动机**：在海洋环境中，无人船（USV）和无人机（UAV）通常搭载计算资源有限的嵌入式设备。现有的模型难以在这些平台上实现实时、低功耗的部署。此外，海洋场景存在强反光、低对比度（如油膜与水面）和动态波浪等挑战，需要模型具备极强的边缘识别能力。

### 2. 核心方法论
LEMMA 的核心思想是利用**拉普拉斯金字塔（Laplacian Pyramid）**分解来提取多尺度边缘信息，从而避免在深层网络中进行昂贵的特征图计算。
*   **拉普拉斯金字塔分解**：将原始图像分解为三个层级（L1, L2, L3/Residual）。L1 保留高频边缘细节，L2 为中频，L3 为低频残差。
*   **三分支架构**：
    *   **低级特征分支 (LFB)**：处理 L3 层级，通过残差块链提取基础结构特征。
    *   **中级特征分支 (MFB)**：融合 L2 层级与 LFB 的输出，利用金字塔提供的边缘线索精炼特征。
    *   **高级特征分支 (HFB)**：处理最高分辨率的 L1 层级，结合前两级的融合特征进行最终的掩码重建。
*   **关键技术细节**：
    *   使用**残差块（Residual Blocks）**作为基本构建单元。
    *   通过转置卷积（TransposeConv）进行上采样，并使用跳跃连接（Concatenation）融合不同尺度的特征。
    *   模型设计极度精简，通道数控制在 16 到 64 之间，以确保轻量化。

### 3. 实验设计
*   **数据集**：
    *   **MaSTr1325**：包含 1325 张由无人船拍摄的近海图像，涵盖水面、天空、障碍物等 5 类。
    *   **Oil Spill Drone Dataset**：包含 847 张高分辨率无人机航拍图像，专注于港口环境下的溢油检测。
*   **对比方法**：对比了包括 DeepLabv3+、PSPNet、UNet、HRNet 等通用模型，以及 WaSR-T、WODIS、ShorelineNet 等专门针对海洋场景的 SOTA 模型。
*   **评估指标**：mIoU（平均交并比）、参数量（Params）、计算量（GFLOPs）和推理时间（Inference Time）。

### 4. 资源与算力
*   **训练设备**：使用 NVIDIA TESLA P100 GPU。
*   **推理设备**：使用 NVIDIA 2080 GPU 和 Intel 4核 XEON CPU。
*   **训练参数**：Batch size 为 8，使用 Adam 优化器，训练 300 个 epoch。
*   **模型规模**：参数量仅约 1.0M 左右。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个视角完全不同（水面 vs. 航拍）的数据集上进行了验证，并与超过 20 种现有模型进行了对比。
*   **消融实验**：
    *   针对三个分支中**残差块的数量配置**（NRB L, M, H）进行了详尽测试，找到了性能与效率的最优平衡点（如 MaSTr1325 使用 7,7,1 配置）。
    *   对比了不同的**损失函数**（Focal Loss, Dice Loss, CE+Dice），最终确定 Focal Loss 效果最佳。
*   **评价**：实验设计较为全面，涵盖了精度、效率和鲁棒性测试，对比基准涵盖了从经典 CNN 到最新 Transformer 的多种架构，具有较高的客观性。

### 6. 主要结论
*   **极高的效率**：相比现有 SOTA 模型，LEMMA 的参数量减少了高达 **71倍**，GFLOPs 降低了 **88.5%**，推理时间缩短了 **84.65%**。
*   **卓越的精度**：在 MaSTr1325 上达到 **98.97% mIoU**，在 Oil Spill 数据集上达到 **93.42% mIoU**，性能媲美甚至超越了体积大得多的模型。
*   **跨平台鲁棒性**：模型能同时适应无人机航拍视角和无人船水平视角，证明了拉普拉斯金字塔在处理不同尺度边缘时的通用性。

### 7. 亮点
*   **边缘感知设计**：通过拉普拉斯金字塔在特征提取早期就引入边缘信息，这种“一箭双雕”的方法既增强了对细小障碍物（如浮标、油膜边缘）的识别，又大幅简化了网络深度。
*   **无需预训练**：LEMMA 可以从零开始训练（Train from scratch）并达到极高精度，不依赖 ImageNet 等大型数据集的预训练权重。
*   **极低延迟**：7.3ms 的推理时间使其非常适合部署在实时性要求极高的嵌入式系统上。

### 8. 不足与局限
*   **环境敏感性**：论文指出，在存在**强烈反光、波浪闪烁或强光眩晕**的情况下，拉普拉斯金字塔生成的边缘会变得模糊，导致模型出现误判（Failure Case）。
*   **架构固定**：目前的金字塔层级和残差块配置是静态的，未来可能需要根据图像内容的复杂程度引入动态深度分配。
*   **数据集规模**：虽然使用了标准数据集，但海洋环境极其多变，目前实验的数据集规模仍相对有限，可能存在特定场景下的泛化风险。

（完）
