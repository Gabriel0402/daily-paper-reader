---
title: "GHOST: Ground-projected Hypotheses from Observed Structure-from-Motion Trajectories"
title_zh: GHOST：基于观测到的运动恢复结构轨迹的地面投影假设
authors: "Tomasz Frelek, Rohan Patil, Akshar Tumu, Henrik I. Christensen"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20583v1"
tags: ["query:mmseg"]
score: 8.0
evidence: 自动驾驶中的车辆轨迹分割
tldr: 本研究提出GHOST，一种自监督学习方法，旨在从单目图像中分割出可行的车辆行驶轨迹。通过利用大规模行车记录仪视频，将自我运动作为隐式监督，利用SfM技术恢复相机轨迹并投影至地面生成空间掩码。该模型无需手动标注即可训练，能从单张RGB图像预测路径提案，隐式捕捉道路布局和车道拓扑，在NuScenes等数据集上表现出良好的泛化能力和跨平台迁移性。
selection_source: fresh_fetch
motivation: 针对自动驾驶中手动标注成本高且难以适应复杂多变环境的问题，探索一种无需人工干预的自监督轨迹预测方案。
method: 通过单目SfM技术从大规模视频中提取自我运动轨迹，并将其投影至地面生成伪标签，以此训练深度分割网络。
result: 在NuScenes数据集上验证了可靠的轨迹预测性能，并证明了模型在不同相机配置和电动滑板车平台上的泛化与迁移能力。
conclusion: 大规模自我运动蒸馏能够使模型隐式学习场景布局和车道拓扑，从而实现基于图像分割的高效轨迹假设估计。
---

## 摘要
我们提出了一种可扩展的自监督方法，用于在复杂城市环境下从单目图像中分割自动驾驶的可行车辆轨迹。利用大规模行车记录仪视频，我们将记录的本车运动视为隐式监督，并通过单目运动恢复结构（SfM）恢复相机轨迹，将其投影到地面上，从而在无需人工标注的情况下生成已行驶区域的空间掩码。这些自动生成的标签被用于训练一个深度分割网络，该网络在运行时仅凭单张 RGB 图像即可预测受运动约束的路径提议，而无需对道路或车道线进行显式建模。该模型在多样化、无约束的网络数据上进行训练，隐式地捕捉了场景布局、车道拓扑和交叉口结构，并能泛化到不同的相机配置。我们在 NuScenes 数据集上评估了我们的方法，展示了可靠的轨迹预测能力，并进一步证明了通过轻量级微调将其迁移到电动滑板车平台的可行性。我们的结果表明，大规模本车运动蒸馏可以产生超出演示轨迹的结构化且可泛化的路径提议，从而实现通过图像分割进行轨迹假设估计。

## Abstract
We present a scalable self-supervised approach for segmenting feasible vehicle trajectories from monocular images for autonomous driving in complex urban environments. Leveraging large-scale dashcam videos, we treat recorded ego-vehicle motion as implicit supervision and recover camera trajectories via monocular structure-from-motion, projecting them onto the ground plane to generate spatial masks of traversed regions without manual annotation. These automatically generated labels are used to train a deep segmentation network that predicts motion-conditioned path proposals from a single RGB image at run time, without explicit modeling of road or lane markings. Trained on diverse, unconstrained internet data, the model implicitly captures scene layout, lane topology, and intersection structure, and generalizes across varying camera configurations. We evaluate our approach on NuScenes, demonstrating reliable trajectory prediction, and further show transfer to an electric scooter platform through light fine-tuning. Our results indicate that large-scale ego-motion distillation yields structured and generalizable path proposals beyond the demonstrated trajectory, enabling trajectory hypothesis estimation via image segmentation.

---

## 论文详细总结（自动生成）

### GHOST: 基于观测到的 SfM 轨迹的地面投影假设论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在不依赖昂贵的人工语义标注、高精地图（HD Maps）或校准传感器套件的情况下，让自动驾驶系统学习理解车辆在复杂城市环境中的可行行驶路径？
*   **研究背景**：传统的行驶区域分割依赖大量人工标注，且难以捕捉驾驶行为中的社会约束和拓扑逻辑。然而，互联网上存在海量的行车记录仪视频，这些视频隐含了人类驾驶员在特定场景下的真实行驶轨迹。
*   **整体含义**：论文提出 GHOST 框架，通过“自我运动蒸馏”将原始视频转化为监督信号。它利用运动恢复结构（SfM）技术提取车辆轨迹并投影至地面，生成伪标签来训练分割网络，从而实现从单张图像预测多种潜在行驶假设的能力。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：将轨迹预测任务转化为图像空间的二值分割问题，利用 SfM 产生的几何一致性轨迹作为自动生成的监督信号。
*   **关键技术流程**：
    1.  **SfM 流水线**：使用 COLMAP 对无约束的单目视频进行处理，提取 6-DoF 相机位姿和稀疏 3D 点云。
    2.  **地面投影与高度估计**：
        *   由于单目 SfM 具有尺度不确定性，系统通过识别图像下方的地面特征点，利用 **Theil-Sen 稳健回归算法** 拟合地面平面，从而估算相机相对于地面的高度 $H_{cam}$。
        *   将未来 5 秒的相机轨迹沿重力向量投影到地面，生成具有一定物理宽度的空间掩码（Mask）。
    3.  **分割模型训练**：
        *   采用 UNet 架构（骨干网络为 ResNet34 或 SegFormer）。
        *   **加权非对称损失函数**：为了让模型不仅学会“走过的路径”，还能预测“潜在可行的路径”，设计了非对称损失。该损失加大了对漏报（False Negative）的惩罚（约 9 倍于误报），鼓励模型覆盖所有可能的行驶区域，产生多假设预测。

#### 3. 实验设计
*   **数据集**：
    *   **预训练**：从 YouTube 抓取 30 小时多样化行车记录仪视频，涵盖 14 个城市、多种天气和光照，最终生成 65 万帧标注数据。
    *   **评估**：使用 **NuScenes (OpenLane-V2 Subset B)**。该数据集提供高精地图，用于构建“可达性”基准（Ground Truth），验证模型在未见场景下的泛化力。
    *   **迁移实验**：收集了 40 分钟电动滑板车（Scooter）在校园内的行驶数据（2.1 万帧）。
*   **对比方法与指标**：
    *   对比了 ResNet34 和 SegFormer 两种骨干网络。
    *   使用 **Soft IoU (Ruzicka 相似度)** 作为主要评价指标，以衡量概率预测图与真实路径的重合度。

#### 4. 资源与算力
*   **算力说明**：文中提到使用 Ceres Solver 进行 GPU 加速的 SfM 优化，并使用了 ImageNet 预训练权重。
*   **具体细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。仅提到预训练进行了 25 个 epoch，滑板车微调进行了 10 个 epoch。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模预训练（65 万帧）。
    *   在 NuScenes 这一公认的自动驾驶基准上进行了定量评估。
    *   针对多车道场景进行了专门的子集评估（Multi-Lane）。
    *   完成了从汽车到电动滑板车的跨平台迁移实验。
*   **充分性评价**：实验设计较为充分，涵盖了从大规模自监督学习到跨域泛化的全过程。通过非对称损失的消融分析（定性展示），证明了模型预测多假设的能力。但缺乏与现有主流有监督行驶区域分割算法的直接定量对比。

#### 6. 主要结论与发现
*   **自监督可行性**：大规模本车运动蒸馏可以有效地替代人工标注，训练出的模型能隐式捕捉车道拓扑和交叉口结构。
*   **多假设预测**：通过非对称损失训练，模型不会坍缩到单一路径，而是能预测出如路口转弯、车道分叉等多种可行选择。
*   **强泛化性**：模型在训练时仅接触左舵环境，但在右舵环境（新加坡）和恶劣天气下依然表现稳健。
*   **跨平台迁移**：仅需少量微调，模型即可从汽车领域迁移到缺乏结构化数据的微出行工具（如滑板车）上。

#### 7. 优点与亮点
*   **高可扩展性**：无需任何人工干预即可处理海量互联网视频，极大地降低了数据获取成本。
*   **模型无关性**：不依赖特定的相机校准或高精地图，具有极强的部署灵活性。
*   **多模态输出**：通过简单的分割公式实现了复杂的轨迹假设预测，能够识别避障路径和备选路线。

#### 8. 不足与局限
*   **几何约束限制**：SfM 在车辆直线行驶、低视差（Low Parallax）情况下容易失效，导致约 1/3 的数据因无法恢复轨迹而被丢弃。
*   **动态障碍物影响**：目前的伪标签生成假设场景是静态的，若视频中存在大量动态车辆干扰 SfM，生成的掩码可能存在几何偏差。
*   **评估基准的局限**：使用静态高精地图作为评估基准无法反映模型避开动态障碍物（如文中提到的避开卡车）的正确性，可能导致评分被低估。
*   **高度假设**：依赖局部地面平坦的假设，在剧烈坡度变化（如山路或立交桥）时可能产生误差。

（完）
