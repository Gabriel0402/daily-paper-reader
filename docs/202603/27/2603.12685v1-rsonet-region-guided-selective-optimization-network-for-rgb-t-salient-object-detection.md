---
title: "RSONet: Region-guided Selective Optimization Network for RGB-T Salient Object Detection"
title_zh: RSONet：用于 RGB-T 显著目标检测的区域引导选择性优化网络
authors: "Bin Wan, Runmin Cong, Xiaofei Zhou, Hao Fang, Chengtao Lv, Sam Kwong"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12685v1"
tags: ["query:mmseg"]
score: 8.0
evidence: RGB-T显著目标检测的多模态融合
tldr: 针对RGB与热红外图像显著区域不一致的问题，本文提出RSONet。该网络分为区域引导和显著性生成两个阶段，通过CI和SF模块生成引导图并计算相似度，利用选择性优化（SO）模块融合特征以减轻模态分布不一致的影响。此外，引入DDE和MIS模块分别增强细节和定位信息。实验证明RSONet在RGB-T显著目标检测任务中优于27种主流方法。
selection_source: carryover_cache
motivation: 旨在解决RGB和热红外图像在显著目标区域分布上存在的不一致性问题。
method: 提出包含区域引导阶段和显著性生成阶段的双阶段网络，并引入选择性优化、细节增强及语义交互模块。
result: 在RGB-T数据集上的广泛实验表明，RSONet的性能优于27种现有的先进显著性目标检测方法。
conclusion: RSONet通过区域引导的选择性优化策略，有效提升了跨模态显著目标检测的准确性与细节表现。
---

## 摘要
本文关注 RGB 图像与热红外图像之间显著区域的不一致性。为了解决这一问题，我们提出了用于 RGB-T 显著目标检测的区域引导选择性优化网络（RSONet），该网络由区域引导阶段和显著性生成阶段组成。在区域引导阶段，设计了三个具有相同编码器-解码器结构的并行分支，并配备了上下文交互（CI）模块和空间感知融合（SF）模块，用于生成引导图，进而计算相似度得分。随后，在显著性生成阶段，选择性优化（SO）模块根据先前获得的相似度值融合 RGB 和热红外特征，以减轻两种模态之间显著目标分布不一致的影响。此后，为了生成高质量的检测结果，将采用多重密集连接和视觉状态空间块的密集细节增强（DDE）模块应用于低层特征，以优化细节信息。此外，在深层特征中加入了相互交互语义（MIS）模块，通过相互融合策略挖掘位置线索。我们在 RGB-T 数据集上进行了广泛的实验，结果表明所提出的 RSONet 与 27 种最先进的 SOD 方法相比，具有竞争力的性能。

## Abstract
This paper focuses on the inconsistency in salient regions between RGB and thermal images. To address this issue, we propose the Region-guided Selective Optimization Network for RGB-T Salient Object Detection, which consists of the region guidance stage and saliency generation stage. In the region guidance stage, three parallel branches with same encoder-decoder structure equipped with the context interaction (CI) module and spatial-aware fusion (SF) module are designed to generate the guidance maps which are leveraged to calculate similarity scores. Then, in the saliency generation stage, the selective optimization (SO) module fuses RGB and thermal features based on the previously obtained similarity values to mitigate the impact of inconsistent distribution of salient targets between the two modalities. After that, to generate high-quality detection result, the dense detail enhancement (DDE) module which adopts the multiple dense connections and visual state space blocks is applied to low-level features for optimizing the detail information. In addition, the mutual interaction semantic (MIS) module is placed in the high-level features to dig the location cues by the mutual fusion strategy. We conduct extensive experiments on the RGB-T dataset, and the results demonstrate that the proposed RSONet achieves competitive performance against 27 state-of-the-art SOD methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《RSONet: Region-guided Selective Optimization Network for RGB-T Salient Object Detection》的结构化深入总结：

### 1. 核心问题与研究背景
该论文针对 **RGB-T 显著目标检测（SOD）** 任务中的核心挑战：**模态间显著区域的不一致性**。
*   **研究动机**：在复杂场景下，RGB 图像可能受光照影响，而热红外（Thermal）图像可能受环境温度或材质影响，导致两者在识别显著目标时表现不一。
*   **现有痛点**：传统方法多采用简单的加法、乘法或拼接进行融合，这种“平权”策略在某一模态质量极差时会引入大量噪声，干扰最终的分割结果。

### 2. 方法论
论文提出了 **RSONet**，一个包含两个主要阶段的框架：

*   **区域引导阶段 (Region Guidance Stage)**：
    *   **并行分支**：设计了三个相同的编码器-解码器分支，分别输入 RGB、Thermal 以及两者的和（RT）。
    *   **上下文交互模块 (CI)**：针对不同层级的特征采用不同尺度的卷积核（1x1 到 7x7），捕捉多尺度上下文。
    *   **空间感知融合模块 (SF)**：利用空间注意力机制优化特征，生成三个引导图（$G_R, G_T, G_{RT}$）。
    *   **相似度计算**：通过比较 $G_R$ 和 $G_T$ 与 $G_{RT}$ 的相似度，自动判定哪个模态在当前场景下更可靠，从而引导后续融合。

*   **显著性生成阶段 (Saliency Generation Stage)**：
    *   **选择性优化模块 (SO)**：根据第一阶段的相似度得分，让更可靠的模态主导融合过程，减轻低质量模态的负面影响。
    *   **密集细节增强模块 (DDE)**：应用于低层特征，结合了多重密集连接和 **视觉状态空间（VSS）块**（借鉴了 Mamba 的思想），用于提取复杂的空间结构和边界细节。
    *   **相互交互语义模块 (MIS)**：应用于高层特征，通过不同空洞率的卷积和相互融合策略，精确锁定目标位置。

### 3. 实验设计
*   **数据集**：在三个主流 RGB-T 显著性检测数据集上进行评估：**VT5000**（大规模）、**VT1000** 和 **VT821**。
*   **对比方法**：对比了 **27 种** 最先进（SOTA）的方法，涵盖了基于 CNN 和 Transformer 的模型（如 SwinNet, CAVER, Samba, SAMSOD 等）。
*   **评价指标**：采用平均绝对误差 ($M$)、F-measure ($F_\beta$)、E-measure ($E_\xi$) 和 Structure-measure ($S_\alpha$)。

### 4. 资源与算力
*   **硬件环境**：使用单块 **NVIDIA GeForce RTX 4080 GPU**。
*   **软件框架**：基于 PyTorch 实现。
*   **训练细节**：输入图像尺寸调整为 384×384；使用 RMSprop 优化器；学习率 1e-4，动量 0.9；主干网络采用在 ImageNet 上预训练的 Swin Transformer。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比（3个数据集 vs 27种方法）和定性可视化对比。
*   **消融实验**：非常充分。分别验证了 SO 模块（对比了加法、乘法、拼接等不同融合策略）、DDE 模块、MIS 模块的有效性，并探讨了不同主干网络（ResNet 系列、SAM、DINO）对性能的影响。
*   **客观性**：通过跨数据集测试和多种指标评估，证明了模型在不同挑战场景（如低光照、热辐射相似、复杂轮廓）下的鲁棒性。

### 6. 主要结论
*   **性能领先**：RSONet 在所有三个数据集上的多个指标均达到或接近最优水平。特别是在 VT5000 数据集上，$F_\beta$ 提升了约 3.4%。
*   **模态选择有效性**：通过区域引导阶段选择主导模态，能显著降低模态不一致带来的噪声干扰。
*   **细节处理优异**：引入 VSS 块和密集连接显著增强了对不规则物体边界的刻画能力。

### 7. 优点与亮点
*   **动态融合策略**：不再盲目融合，而是先“评估”模态质量再“决策”融合权重，具有更强的自适应性。
*   **模块化设计**：CI、DDE 和 MIS 模块针对高低层特征的不同特性量身定制，逻辑清晰。
*   **引入前沿技术**：将视觉状态空间（VSS/Mamba 相关技术）应用于细节增强，提升了长程建模与局部细节的平衡。

### 8. 不足与局限
*   **计算开销**：由于采用了双阶段设计和三个并行引导分支，虽然参数量控制在 88M（中等水平），但**检测速度（9.4 FPS）**相对较慢，难以满足实时性要求极高的应用。
*   **极端场景失效**：当 RGB 和热红外图像**同时处于极低质量**（如极小目标或两者均无对比度）时，模型仍可能产生误检或漏检。
*   **大模型适配**：实验显示直接替换更强的冻结大模型（如 SAM/DINO）效果反而下降，说明模型对特定任务的适配器（Adapter）设计仍有优化空间。

（完）
