---
title: "OmniPatch: A Universal Adversarial Patch for ViT-CNN Cross-Architecture Transfer in Semantic Segmentation"
title_zh: OmniPatch：一种用于语义分割中 ViT-CNN 跨架构迁移的通用对抗补丁
authors: "Aarush Aggarwal, Akshat Tomar, Amritanshu Tiwari, Sargam Goyal"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20777v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 用于安全自动驾驶的鲁棒语义分割
tldr: 语义分割在自动驾驶中至关重要，但易受黑盒对抗攻击。现有方法多针对单架构或全图扰动，缺乏实用性。本文提出 OmniPatch 训练框架，旨在学习一种通用的对抗补丁。该补丁无需访问目标模型参数，即可在不同图像以及 ViT 和 CNN 架构之间实现高效的跨架构迁移攻击，显著提升了对抗补丁的通用性和实用价值。
selection_source: fresh_fetch
motivation: 现有的对抗攻击方法通常局限于特定架构或需要全图扰动，难以在未知的黑盒模型和实际场景中实现有效的跨架构迁移。
method: 提出 OmniPatch 框架，通过特定的训练流程学习一种能够同时在 ViT 和 CNN 架构上通用的对抗补丁。
result: 实验证明该补丁在无需目标模型参数的情况下，实现了优异的跨图像和跨架构（ViT 与 CNN）的对抗迁移效果。
conclusion: OmniPatch 为评估语义分割模型在现实世界中的鲁棒性提供了一种高效且通用的黑盒攻击方案。
---

## 摘要
鲁棒的语义分割对于安全自动驾驶至关重要，然而，在目标权重未知的情况下，已部署的模型仍然容易受到黑盒对抗攻击。大多数现有方法要么设计全图范围的扰动，要么针对单一架构优化补丁，这限制了它们的实用性和迁移性。我们引入了 OmniPatch，这是一个用于学习通用对抗补丁的训练框架，该补丁可以在图像以及 ViT 和 CNN 架构之间泛化，且无需访问目标模型参数。

## Abstract
Robust semantic segmentation is crucial for safe autonomous driving, yet deployed models remain vulnerable to black-box adversarial attacks when target weights are unknown. Most existing approaches either craft image-wide perturbations or optimize patches for a single architecture, which limits their practicality and transferability. We introduce OmniPatch, a training framework for learning a universal adversarial patch that generalizes across images and both ViT and CNN architectures without requiring access to target model parameters.

---

## 论文详细总结（自动生成）

这是一份关于论文《OmniPatch: A Universal Adversarial Patch for ViT-CNN Cross-Architecture Transfer in Semantic Segmentation》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：语义分割是自动驾驶等安全敏感任务的核心。然而，现有的对抗攻击方法存在局限性：要么是全图范围的扰动（物理不可实现），要么是针对特定模型架构优化的补丁（缺乏跨架构迁移性）。
*   **核心问题**：如何在目标模型参数未知的黑盒环境下，设计一种既能跨图像通用，又能从视觉 Transformer (ViT) 架构有效迁移到卷积神经网络 (CNN) 架构的物理可行对抗补丁？

### 2. 论文提出的方法论
OmniPatch 提出了一个两阶段训练框架，核心思想是利用 ViT 对补丁攻击的敏感性作为突破口，并引导其向 CNN 迁移。
*   **敏感区域放置 (Sensitive Region Placement)**：
    *   通过 ViT 替代模型计算类别的预测自熵，识别最不稳定的类别（如“电线杆”）。
    *   使用形态学膨胀扩大放置区域，并通过熵偏置采样（Entropy-biased sampling）将补丁放置在不确定性最高的像素区域。
*   **两阶段训练流程**：
    *   **第一阶段 (ViT 专用)**：优化补丁以破坏 ViT 的高置信度预测，使其产生分类错误。
    *   **第二阶段 (ViT+CNN 集成)**：引入 CNN 替代模型进行集成训练。使用 **JS 散度** 识别高迁移性像素，并引入 **梯度对齐 (Gradient Alignment)** 技术，通过最大化 ViT 和 CNN 梯度之间的余弦相似度，解决异构模型间梯度冲突导致的训练不稳定问题。
*   **辅助损失函数**：包含注意力劫持（针对 ViT）、边界破坏（诱导分割破碎化）和全变分（TV）正则化（控制视觉噪声）。
*   **物理鲁棒性**：应用变换期望（EOT）技术，模拟随机缩放、旋转和平移。

### 3. 实验设计
*   **数据集**：使用 **Cityscapes** 城市街景数据集（19 类，包含训练、验证和测试集）。
*   **替代模型 (Source)**：PIDNet-S (CNN) 和 SegFormer (ViT)。
*   **目标模型 (Target/Benchmark)**：PIDNet-M, PIDNet-L, BiSeNetV1, BiSeNetV2。
*   **对比方法**：
    *   干净图像 (Clean Image)
    *   随机补丁 (Random Patch)
    *   基准补丁 (Baseline Patch，引用自 Shekhar et al. 2025 的 PIDNet-S 训练补丁)。
*   **评估指标**：平均交并比 (mIoU)。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到训练进行了 20 个 epoch（两个阶段各 10 个），每个 epoch 包含 300 张随机采样的图像，每张图像进行 7 次攻击迭代。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个不同的目标模型上验证了迁移性。
    *   进行了详细的**消融实验**，包括：补丁放置策略（中心 vs 随机 vs 敏感区域）、补丁大小（1.9% 到 7.6% 面积）、散度度量（KL vs JS）以及梯度对齐的有无。
*   **充分性评价**：实验设计较为全面，涵盖了从轻量级到大型的多种 CNN 架构，并对比了随机和已有的基准方法。消融实验有效地证明了其提出的各个组件（如梯度对齐和敏感区域放置）的必要性。

### 6. 论文的主要结论与发现
*   **跨架构有效性**：OmniPatch 在所有测试的 CNN 模型上均实现了显著的 mIoU 下降（下降幅度在 10.33% 到 16.65% 之间），优于随机补丁和基准补丁。
*   **放置策略至关重要**：基于不确定性的敏感区域放置比随机或中心放置能产生更强的攻击效果。
*   **梯度对齐的作用**：在异构集成训练中，梯度对齐能显著提升补丁的迁移性能（mIoU 额外下降约 2-4%）。
*   **ViT 的脆弱性**：ViT 对补丁攻击表现出极高的敏感性，是理想的攻击起点。

### 7. 优点
*   **跨架构突破**：成功解决了 ViT 和 CNN 之间由于归纳偏置不同（全局注意力 vs 局部特征）导致的攻击迁移难点。
*   **算法创新**：引入梯度对齐来解决异构模型集成训练中的“梯度干扰”问题，具有较强的理论参考价值。
*   **自动化放置**：提出了一套自动化的敏感区域识别和补丁放置流程，提高了攻击的实用性。

### 8. 不足与局限
*   **视觉显著性**：补丁产生的噪声较为明显，容易被人类观察者察觉，缺乏隐蔽性。
*   **环境适应性**：目前的实验主要基于理想的图像数据，尚未在多变的天气（雨、雪）或极端光照条件下进行充分验证。
*   **物理验证缺失**：虽然使用了 EOT 模拟，但缺乏在真实物理世界（如打印补丁贴在路牌上）的实地测试。
*   **算力信息缺失**：未提供训练成本信息，难以评估该框架的计算效率。

（完）
