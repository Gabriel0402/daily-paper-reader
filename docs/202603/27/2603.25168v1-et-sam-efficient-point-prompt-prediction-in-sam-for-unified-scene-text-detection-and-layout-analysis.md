---
title: "ET-SAM: Efficient Point Prompt Prediction in SAM for Unified Scene Text Detection and Layout Analysis"
title_zh: ET-SAM：用于统一场景文本检测与布局分析的 SAM 高效点提示预测
authors: "Xike Zhang, Maoyuan Ye, Juhua Liu, Bo Du"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25168v1"
tags: ["query:mmseg"]
score: 9.0
evidence: SAM模型在场景文本检测和布局分析中的高效点提示预测应用
tldr: ET-SAM 针对基于 SAM 的统一场景文本检测与布局分析中推理延迟高和数据利用率低的问题，提出了一种高效框架。该框架通过轻量级点解码器生成词级热力图以减少点提示数量，并设计了联合训练策略以利用异构标注数据。实验表明，ET-SAM 在保持竞争性能的同时，推理速度提升了 3 倍，并在多个主流数据集上显著提高了 F 分数。
selection_source: fresh_fetch
motivation: 现有的基于 SAM 的文本检测方法依赖像素级分割产生大量点提示，导致推理缓慢且难以利用异构标注数据。
method: 引入轻量级点解码器生成少量关键点提示，并结合可学习任务提示的联合训练策略来整合不同层级的文本标注。
result: "在 HierText 上实现 3 倍推理加速，并在 Total-Text、CTW1500 和 ICDAR15 上平均提升了 11.0% 的 F 分数。"
conclusion: ET-SAM 通过优化点提示生成和数据训练策略，实现了高效且高性能的统一场景文本检测与布局分析。
---

## 摘要
基于 Segment Anything Model (SAM) 的先前工作在统一场景文本检测和布局分析方面取得了显著性能。然而，通常依赖像素级文本分割来采样数千个前景点作为提示，导致推理延迟不理想且数据利用率有限。为了解决上述问题，我们提出了 ET-SAM，这是一个基于 SAM 的高效框架，具有两个解码器，用于统一场景文本检测和布局分析。在技术上，我们定制了一个轻量级点解码器，生成单词热图以获取少量前景点，从而消除了过多的点提示并加速了推理。在不依赖像素级分割的情况下，我们进一步设计了一种联合训练策略，以利用具有异构文本级标注的现有数据。具体而言，将具有多级、仅单词级和仅行级标注的数据集并行组合为一个统一的训练集。对于这些数据集，我们在点解码器和分层掩码解码器中分别引入了三组相应的可学习任务提示，以减轻数据集之间的差异。大量实验表明，与之前的基于 SAM 的架构相比，ET-SAM 在 HierText 上获得竞争性能的同时，实现了约 3 倍的推理加速，并在 Total-Text、CTW1500 和 ICDAR15 上平均提高了 11.0% 的 F 分数。

## Abstract
Previous works based on Segment Anything Model (SAM) have achieved promising performance in unified scene text detection and layout analysis. However, the typical reliance on pixel-level text segmentation for sampling thousands of foreground points as prompts leads to unsatisfied inference latency and limited data utilization. To address above issues, we propose ET-SAM, an Efficient framework with two decoders for unified scene Text detection and layout analysis based on SAM. Technically, we customize a lightweight point decoder that produces word heatmaps for achieving a few foreground points, thereby eliminating excessive point prompts and accelerating inference. Without the dependence on pixel-level segmentation, we further design a joint training strategy to leverage existing data with heterogeneous text-level annotations. Specifically, the datasets with multi-level, word-level only, and line-level only annotations are combined in parallel as a unified training set. For these datasets, we introduce three corresponding sets of learnable task prompts in both the point decoder and hierarchical mask decoder to mitigate discrepancies across datasets.Extensive experiments demonstrate that, compared to the previous SAM-based architecture, ET-SAM achieves about 3$\times$ inference acceleration while obtaining competitive performance on HierText, and improves an average of 11.0% F-score on Total-Text, CTW1500, and ICDAR15.

---

## 论文详细总结（自动生成）

### ET-SAM：用于统一场景文本检测与布局分析的 SAM 高效点提示预测

#### 1. 核心问题与研究动机
*   **核心问题**：现有的基于 Segment Anything Model (SAM) 的统一场景文本检测与布局分析方法（如 Hi-SAM）存在两个主要瓶颈：
    1.  **推理延迟高**：依赖像素级分割结果并随机采样数千个前景点作为提示（Prompts），导致分层掩码解码和后处理阶段计算开销巨大。
    2.  **数据利用率低**：训练通常需要包含单词、文本行、段落等多层级的密集标注数据，而这类数据稀缺，限制了模型的扩展性。
*   **研究背景**：场景文本具有从单词到文本行再到段落的分层结构。统一检测与布局分析旨在通过一个框架同时处理这些层级，而 SAM 的引入虽然提升了精度，但牺牲了效率。

#### 2. 方法论
ET-SAM 提出了一个包含两个解码器的高效框架：
*   **点解码器 (Point Decoder)**：
    *   **核心思想**：取代随机采样，通过一个轻量级解码器预测单词中心热力图（Word Heatmap）。
    *   **技术细节**：从图像嵌入中生成热力图，提取局部峰值作为稀疏的点提示。这显著减少了输入到后续阶段的点数量（从数千个减少到数百个）。
*   **分层掩码解码器 (HM-Decoder)**：
    *   在点提示的引导下，同时预测四个层级的掩码：单个单词、行内词组、完整文本行和段落。
*   **任务提示 (Task Prompt)**：
    *   引入可学习的任务嵌入（Task 0: 多级；Task 1: 仅单词；Task 2: 仅文本行），用于调节解码器以适应不同数据集的分布和标注差异。
*   **联合训练策略 (Joint Training)**：
    *   设计了一种并行训练方案，将具有异构标注（多级、仅单词、仅行级）的数据集整合进统一训练池，通过伪标签技术解决部分数据集缺失单词级标注的问题。

#### 3. 实验设计
*   **数据集**：
    *   **多级标注**：HierText（核心 Benchmark，用于布局分析）。
    *   **单词级标注**：Total-Text, TextSeg, ICDAR2013, ICDAR2015。
    *   **行级标注**：CTW1500。
*   **对比方法**：
    *   **统一检测器**：UD (Unified Detector), Hi-SAM。
    *   **特定层级检测器**：DBNet++, PANet, DeepSolo, DPText-DETR, SRFormer 等。
*   **评价指标**：全景质量 (PQ)、F-score (F)、精确率 (P)、召回率 (R) 以及推理耗时。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA Tesla V100 (32GB) GPU** 进行联合训练；使用 **4 台 V100 GPU** 进行特定数据集的微调。
*   **训练配置**：
    *   联合训练：120 个 Epoch，Batch Size 为 3（每台 GPU），采用 AdamW 优化器。
    *   微调：HierText 微调 50 Epoch，其他数据集微调 80 Epoch。
*   **推理效率**：在 HierText 上，推理延迟从 Hi-SAM 的 4.4s 降至 **1.4s**（约 3 倍加速）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个主流数据集上进行了测试，涵盖了从水平到弯曲、从单词到段落的多种场景。
*   **消融实验**：
    *   验证了点解码器的有效性（对比随机采样）。
    *   分析了任务提示（Task Prompts）对跨数据集性能的影响。
    *   探讨了联合训练中不同数据组合的贡献。
    *   研究了热力图阈值对性能与点数量的平衡关系。
*   **充分性评价**：实验设计较为全面，通过与 SOTA 方法在相同训练数据下的公平对比（见附录 B），证明了其架构的优越性。

#### 6. 主要结论与发现
*   **效率提升**：通过预测稀疏点提示，ET-SAM 在保持甚至超过 Hi-SAM 性能的同时，实现了 **3 倍的推理加速**。
*   **性能增强**：在 HierText 上取得了竞争性结果；在 Total-Text、CTW1500 和 ICDAR15 上，相比 Hi-SAM 平均提升了 **11.0% 的 F-score**。
*   **数据扩展性**：联合训练策略证明了利用异构标注数据可以显著提升模型在特定任务上的鲁棒性。

#### 7. 优点
*   **高效性**：成功解决了 SAM 在文本任务中因点提示过多导致的计算冗余问题。
*   **通用性**：通过任务提示机制，使模型能够灵活处理不同粒度的检测任务，增强了对异构数据的兼容性。
*   **端到端**：实现了从检测到布局分析的统一流程，且不依赖复杂的像素级预分割。

#### 8. 不足与局限
*   **性能差距**：虽然优于 Hi-SAM，但在某些特定层级的检测任务上，仍略逊于专门优化的 SOTA 检测器（如 DPText-DETR），这可能归因于缺乏大规模合成数据的预训练。
*   **布局分析精度**：在处理极复杂的段落结构时，掩码分割仍存在细微缺陷，可能需要更精细的特征融合机制。
*   **热力图依赖**：对于极端弯曲的文本，中心线热力图的生成质量直接影响点提示的准确性。

（完）
