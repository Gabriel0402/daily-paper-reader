---
title: Temporal Logic Guidance for Action-Only Diffusion Policies with World Models
title_zh: 面向仅动作扩散策略的时间逻辑引导与世界模型
authors: "Moritz Zoellner, Anastasios Manganaris, Rohan Paleja"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22729v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 利用世界模型对动作扩散策略进行时态逻辑引导
tldr: "针对动作扩散策略在推理时难以控制行为模式的问题，现有方法需联合生成状态与动作，增加复杂度。本文提出利用世界模型实现信号时序逻辑鲁棒性的可微分评估，将梯度注入扩散过程以引导行为满足约束。在Robomimic搬运任务中，保持100%成功率的同时将约束违反率从80%降至4%。该方法无需重训练即可提升约束遵守度，为复杂人机交互场景提供高效控制方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22729v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 641, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22729v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 926, \"height\": 631, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22729v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 161, \"label\": \"Table\"}]"
motivation: 动作扩散策略能生成多模态行为，但推理时难以选择符合人类意图的模式，现有引导方法需联合生成状态与动作，增加复杂性和运行时开销。
method: 提出使用世界模型对信号时序逻辑鲁棒性进行可微分评估，将梯度注入动作扩散过程，无需重训练即可引导行为满足约束。
result: "在Robomimic Can Transport任务中，保持100%任务成功率，约束违反率从基线方法的80%以上降至4%。"
conclusion: 该方法有效提升约束遵守度且不牺牲任务性能，可扩展至更复杂约束和鲁棒性改进。
---

## 摘要
扩散策略能够实现多模态的机器人行为，但在推理时对行为模式的选择能力有限，尽管在人机交互场景中这种控制是可取的。先前针对这种控制缺失的解决方案利用信号时序逻辑（STL）表达人类意图，并为扩散策略推理提供相应的引导。然而，这些方法只能引导同时生成未来动作和状态的扩散策略，增加了复杂性和运行时开销。我们提出了一种新颖的引导方法，适用于仅动作扩散策略，该方法使用单独学习的世界模型来支持STL鲁棒性的可微评估，并将其梯度注入扩散过程。这在不重新训练的情况下将行为引导向约束满足，提高了约束遵循度同时保持了任务性能。在Robomimic的Can Transport任务中，我们的方法保持了100%的任务成功率，同时将约束违反率从基线方法的超过80%降低到4%。我们还讨论了向更鲁棒性和更复杂约束的扩展。

## Abstract
Diffusion policies enable multimodal robot behavior but offer limited ability to choose among behavior modes at inference time, even though such control is desirable in human-robot settings. Prior solutions to this lack of control have utilized Signal Temporal Logic (STL) to express human intentions and provide corresponding guidance for diffusion policy inference. However, these approaches can only guide diffusion policies that jointly generate future actions and states, increasing both complexity and runtime. We propose a novel guidance method for action-only diffusion policies that uses a separate learned world model to enable differentiable evaluation of STL robustness, with its gradient then injected into the diffusion process. This steers behavior toward constraint satisfaction without retraining, improving constraint adherence while preserving task performance. On the Can Transport task from Robomimic, our method maintains 100% task success while reducing constraint violations from over 80% for baseline methods to 4%. We also discuss extensions toward improved robustness and more complex constraints.

---

## 论文详细总结（自动生成）

好的，以下是对给定论文《Temporal Logic Guidance for Action-Only Diffusion Policies with World Models》的详细中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

-   **核心问题**：扩散策略能够从多样化的演示中学习并展现多模态的机器人行为，但在推理时缺乏有效机制来根据用户偏好或特定约束（如安全要求、任务规范）选择期望的行为模式。这种“行为模式选择不可控”的问题限制了其在人机交互场景中的应用。
-   **研究动机**：用户偏好（如“小心轻放”或“尽量多装”）可在推理时动态变化，重新训练策略成本高昂且不切实际。因此，需要一种**推理时引导机制**，在不重训练策略的前提下，将行为导向满足特定约束的方向。
-   **现有方法的局限**：先前的解决方案利用信号时序逻辑（STL）表达人类意图并引导扩散模型。然而，这些方法要求扩散策略**同时生成未来的动作和状态**（即联合动作-状态生成），这增加了模型的复杂性和运行时开销。许多现代扩散策略只生成动作序列，使得这类方法无法直接应用。

### 2. 论文提出的方法论：核心思想、关键技术细节

-   **核心思想**：利用一个**独立的、可微分的世界模型**来预测由扩散策略生成的动作序列所导致的未来状态轨迹，进而计算STL鲁棒性值的梯度，并将该梯度注入扩散过程的去噪步骤中，从而引导动作序列的生成，使其满足约束。
-   **关键技术细节**：
    1.  **框架组成**：
        -   **动作扩散策略**: \( \pi(a_{t:t+H} | s_t) \)，生成动作序列。
        -   **世界模型**: \( \hat{s}_{t+1} = f_\theta(s_t, a_t) \)，一个独立训练的、可微分的下一状态预测模型。
        -   **STL鲁棒性评估**: 用于计算状态轨迹 \( \tau \) 对给定STL规范 \( \phi \) 的满足程度，记为 \( \rho_\phi(\tau) \)。
    2.  **算法流程**：
        -   在扩散过程的每个去噪步 \( k \)，从当前噪声动作 \( a^k \) 获得一个较干净的动作序列 \( a^{k-1} \)。
        -   **世界模型展开**：将动作序列 \( a_{t:t+H} \) 和当前状态 \( s_t \) 输入世界模型，迭代得到预测的未来状态轨迹 \( \hat{s}_{t+1:t+H} \)。
        -   **计算引导目标**：定义一个引导目标 \( J(\phi, a_{t:t+H}, s_t) = \rho_\phi(\hat{s}_{t+1:t+H}) \)，即预测轨迹的STL鲁棒性。
        -   **计算梯度**：由于所有步骤（世界模型、STL鲁棒性）都是可微的，可以计算引导目标相对于动作序列的梯度 \( \nabla_{a^k} J \)。
        -   **注入梯度**：将该梯度添加到扩散过程的去噪更新中（公式2），从去噪步骤中对动作序列进行修正：
            \[
            a^{k-1} = \mu_\theta(a^k, s_t, k) + \lambda \nabla_{a^k} J(\phi, a^k, s_t) + \sigma_k \epsilon
            \]
            其中 \( \lambda \) 控制引导强度。此外，在去噪结束后，对最终的动作序列 \( a^0 \) 再进行少量梯度上升步骤以进一步优化约束满足度。

### 3. 实验设计

-   **数据集与场景**：采用 **Robomimic 框架中的 Can Transport 任务**。该任务要求机器人抓取一个易拉罐并将其放入一个盒子中。
-   **基准 (Benchmark)**：使用一个在混合人类演示上训练的扩散策略，该策略包含了多种行为模式。
-   **STL约束**：用一个STL规范约束罐子要保持直立：\( G(R_{zz} > \cos(5^\circ)) \)，即在整个轨迹中，罐子绕世界坐标系z轴的倾斜角度不超过5度。
-   **对比方法**：
    1.  **基础扩散策略 (Base Policy)**：无任何引导。
    2.  **采样与排序 (Sample & Rank)**：使用相同的鲁棒性目标函数，但通过采样多个动作轨迹并选择鲁棒性最高的那个来实现约束。
    3.  **本文方法 (Guidance, Ours)**。

### 4. 资源与算力

-   论文中**未明确说明**所使用的GPU型号、数量和训练时长等信息。

### 5. 实验数量与充分性

-   **实验数量**：主要在 **Can Transport** 单一任务上，针对上述三种方法，进行了 **50次 rollout** 评估。主要报告了三项指标：平均倾斜角度、任务成功率、约束违反率。
-   **充分性与客观性**：
    -   **优点**：实验设计直接，对比清晰，有力地证明了所提方法在约束违反率上的显著改善（从>80%降至4%），同时保持了100%的任务成功率，优于采样与排序基线。结果有力且直观。
    -   **不足**：实验**覆盖面较窄**。仅在单一任务、单一个STL约束下进行测试，缺乏在更多样化的任务、环境和更复杂的STL规范上的验证。这限制了对于方法泛化能力的结论强度。对比方法也相对简单。

### 6. 论文的主要结论与发现

-   本文提出的基于**世界模型和梯度引导**的方法，能够有效地在推理时指导**仅动作扩散策略**满足STL约束。
-   该方法**保持了100%的任务成功率**，同时将**约束违反率从基线方法的84%大幅降低到4%**。
-   与基于采样的选择方法（Sample & Rank）相比，**基于梯度的引导更有效**，能够更精确地将行为导向约束满足的区域，甚至可能恢复训练分布中不常见的行为模式。

### 7. 优点

-   **方法上的创新性**：巧妙地解耦了状态预测和动作生成。使用**独立的世界模型**来评估动作结果，使得该方法能直接应用于**更广泛的仅输出动作的扩散策略**，无需修改策略本身。
-   **灵活性高**：世界模型使得**评估的时域 (H') 可以与动作预测的时域 (H) 不同**。此外，用户可以在推理时通过自然语言接口（通过LLM）定义任意的STL约束，实现无需重训练的实时交互引导。
-   **实验验证有效**：在所选的单一任务上，实验结果**非常惊艳**，以极大的优势（4% vs 84%）超越了基线方法，有力地证明了其有效性。
-   **讨论未来方向**：论文在“未来工作”部分提到了向更复杂、更长时域约束的扩展思路，体现了方法的潜力和作者的深入思考。

### 8. 不足与局限

-   **实验验证的局限性**：如1、3、5点所述，**实验规模过小，仅有一个任务一个约束**。结果的普适性有待验证。没有提供消融实验（如不同引导强度 \( \lambda \) 的影响分析，或世界模型预测误差对性能的影响）。
-   **与联合生成方法的比较**：论文未与声称的“更复杂”的联合动作-状态生成方法进行直接对比，因此**缺乏计算效率和性能上的公平对比**，使其优越性论证不够完整。
-   **实际部署的挑战**：方法依赖于一个**高度准确且可微分的世界模型**。在复杂、高维的机器人操作任务中，学习这样一个世界模型本身就是一个巨大的挑战。世界模型的预测误差会直接影响引导梯度，可能导致策略失效。
-   **潜在偏差风险**：论文未讨论世界模型或STL鲁棒性计算中可能引入的偏差。例如，世界模型可能在特定状态-动作分布上表现更差，从而导致引导行为产生系统性偏差。

（完）
