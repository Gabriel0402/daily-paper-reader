---
title: Bellman Value Decomposition for Task Logic in Safe Optimal Control
title_zh: 安全最优控制中任务逻辑的 Bellman 价值分解
authors: "William Sharpless, Oswin So, Dylan Hirsch, Sylvia Herbert, Chuchu Fan"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.19532v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 将时态逻辑定义的复杂任务分解为贝尔曼价值图
tldr: 针对高维复杂任务中目标与安全规范难以平衡的问题，本文提出了一种基于贝尔曼价值分解的方法。研究证明了时序逻辑定义的复杂任务价值函数可分解为由特定贝尔曼方程（如Reach-Avoid等）连接的价值图。通过提出的VDPPO算法，将该分解结构嵌入神经网络进行求解。实验表明，该方法在处理异构团队和非线性动力学任务时，能自动平衡安全性与活跃性，显著优于现有基准。
selection_source: fresh_fetch
motivation: 旨在解决高维复杂任务中形式化自动机过于繁琐以及稀疏奖励函数难以调优的挑战。
method: 将复杂任务的贝尔曼价值分解为由Reach-Avoid、Avoid及新型Reach-Avoid-Loop方程连接的价值图，并利用VDPPO算法进行求解。
result: 在仿真和硬件实验中，该方法在处理涉及异构团队和非线性动力学的复杂任务时表现出优越的性能。
conclusion: 贝尔曼价值分解能有效组织复杂任务逻辑，实现安全与目标达成的自动平衡。
---

## 摘要
现实世界的任务涉及目标与安全规范的细微组合。在高维空间中，这一挑战更加严峻：形式化自动机变得繁琐，且稀疏奖励的组合往往需要费力的调优。在这项工作中，我们将 Bellman 价值的固有结构视为一种自然组织问题以提高自动化性能的手段。具体而言，我们证明了由时序逻辑定义的复杂任务的 Bellman 价值可以分解为一个 Bellman 价值图，该图由一组著名的 Bellman 方程（BEs）连接：到达-规避（Reach-Avoid）BE、规避（Avoid）BE 以及一种新型的到达-规避-循环（Reach-Avoid-Loop）BE。为了求解价值和最优策略，我们提出了 VDPPO，它将分解后的价值图嵌入到一个双层神经网络中，从而引导隐式依赖关系。我们进行了各种仿真和硬件实验，在涉及异构团队和非线性动力学的复杂高维任务上测试了我们的方法。最终，我们发现该方法相比现有基准显著提高了性能，并能自动平衡安全性和活跃性。

## Abstract
Real-world tasks involve nuanced combinations of goal and safety specifications. In high dimensions, the challenge is exacerbated: formal automata become cumbersome, and the combination of sparse rewards tends to require laborious tuning. In this work, we consider the innate structure of the Bellman Value as a means to naturally organize the problem for improved automatic performance. Namely, we prove the Bellman Value for a complex task defined in temporal logic can be decomposed into a graph of Bellman Values, connected by a set of well-known Bellman equations (BEs): the Reach-Avoid BE, the Avoid BE, and a novel type, the Reach-Avoid-Loop BE. To solve the Value and optimal policy, we propose VDPPO, which embeds the decomposed Value graph into a two-layer neural net, bootstrapping the implicit dependencies. We conduct a variety of simulated and hardware experiments to test our method on complex, high-dimensional tasks involving heterogeneous teams and nonlinear dynamics. Ultimately, we find this approach greatly improves performance over existing baselines, balancing safety and liveness automatically.

---

## 论文详细总结（自动生成）

### 论文总结：安全最优控制中任务逻辑的 Bellman 价值分解

#### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的机器人任务中，通常需要同时满足复杂的“目标达成”（Liveness）和“安全约束”（Safety）。传统的强化学习（RL）依赖于手动设计的奖励函数，这在处理时序逻辑（TL）定义的复杂任务时，往往面临奖励稀疏、难以调优以及难以提供最坏情况保证的问题。而基于形式化方法（如自动机）的路径规划在高维空间中又会遭遇“维度灾难”。
**本文的研究动机**在于利用 Bellman 价值函数的内在结构，将复杂的时序逻辑任务分解为多个简单的子任务，从而在不需要繁琐手动调优的情况下，实现高维系统中安全与目标的自动平衡。

#### 2. 方法论：核心思想与关键技术
论文提出了一种**Bellman 价值分解（Bellman Value Decomposition）**框架，其核心思想是将一个复杂的时序逻辑规范转化为一个由“原子 Bellman 方程”连接的**价值图（Value Graph）**。

*   **核心理论：** 证明了对于一大类时序逻辑谓词（包括 Until, Always, Eventually 等及其组合），其 Bellman 价值函数可以精确分解。
*   **原子 Bellman 方程：** 分解后的图由三种基本的方程连接：
    1.  **Reach-Avoid BE：** 处理“在避开障碍的同时到达目标”。
    2.  **Avoid BE：** 处理纯粹的安全约束。
    3.  **Reach-Avoid-Loop (RAℓ) BE（本文创新）：** 专门用于处理“始终-最终”（Always-Eventually）这类递归/循环任务，并证明了该方程在折扣极限下的收缩性（Contractive）。
*   **VDPPO 算法：** 
    *   开发了 **valtr** 工具，自动将 TL 规范解析并生成分解价值图（DVG）。
    *   提出 **VDPPO** 算法，将 DVG 嵌入到双层神经网络中。网络采用共享主干（Shared Trunk），通过 One-hot 编码区分不同的子任务节点。
    *   利用自助法（Bootstrapping）处理节点间的隐式依赖关系，直接从分解结构中提取密集的学习信号。

#### 3. 实验设计
研究团队在仿真和真实硬件上进行了广泛验证：
*   **仿真场景：**
    *   **DoubleInt：** 基础双积分器模型，用于测试逻辑的深度（嵌套）和广度（并行）。
    *   **Herding（驱赶任务）：** 异构团队协作，将目标群体驱赶至指定区域。
    *   **Delivery（配送任务）：** 涉及循环访问目标点和补给点的长程任务。
    *   **Manipulator：** 机械臂操作立方体和抽屉。
*   **Benchmark（对比方法）：**
    *   **LCRL：** 基于自动机增强状态空间的深度强化学习方法。
    *   **TL-MPPI：** 结合时序逻辑鲁棒性指标的模型预测路径积分控制。
*   **硬件实验：** 使用 Crazyflie 无人机群和 Unitree Go2 四足机器人进行异构协作实验。

#### 4. 资源与算力
论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到：
*   所有方法均使用 3 个随机种子进行训练。
*   评估基于 256 个随机初始条件。
*   VDPPO 与基准方法 LCRL 在相同的更新步数下进行对比。
*   消融实验显示，共享参数的网络结构比独立网络训练速度快近 $N$ 倍（$N$ 为分解节点数）。

#### 5. 实验数量与充分性
实验设计非常**充分且具有说服力**：
*   **维度覆盖：** 从简单的 2D 仿真到高维的异构机器人协作（无人机+四足机器人）。
*   **逻辑复杂度：** 专门设计了针对逻辑“深度”和“广度”的消融实验，验证了方法在处理嵌套逻辑时的优越性。
*   **客观性：** 引入了硬件实验，证明了该方法在处理非线性动力学、实时反馈和现实噪声时的鲁棒性。

#### 6. 主要结论与发现
*   **分解的有效性：** 复杂任务的 Bellman 价值确实可以分解为原子方程的组合，这为复杂逻辑提供了密集的梯度信号。
*   **性能提升：** VDPPO 在成功率和学习速度上显著优于 LCRL 和 TL-MPPI，尤其是在逻辑复杂度增加和智能体数量增多时，优势更加明显。
*   **自动平衡：** 该方法无需手动调整安全与目标之间的权重（Lagrangian 乘子），能够自动实现“在保证安全的前提下最大化目标达成”。
*   **硬件可行性：** 学习到的策略可以直接部署在异构机器人硬件上，并表现出复杂的涌现行为（如分工协作）。

#### 7. 优点
*   **理论严密：** 提供了关于价值分解和新型 RAℓ 方程收缩性的形式化证明。
*   **无需调优：** 解决了受限强化学习（Constrained RL）中拉格朗日参数难以调节的痛点。
*   **扩展性强：** 通过共享网络主干和价值图结构，能够高效处理具有大量子任务的复杂逻辑。
*   **端到端：** 从逻辑规范到硬件部署，流程自动化程度高。

#### 8. 不足与局限
*   **逻辑覆盖范围：** 虽然涵盖了大类时序逻辑，但对于某些极其特殊的非线性逻辑组合，分解可能依然复杂。
*   **状态增强依赖：** 尽管减少了对自动机的依赖，但在某些非马尔可夫任务中，仍可能需要一定程度的状态增强来保证最优性。
*   **计算开销：** 随着逻辑节点 $N$ 的增加，虽然共享主干缓解了压力，但价值图的拓扑排序和多节点引导更新仍比普通 PPO 复杂。

（完）
