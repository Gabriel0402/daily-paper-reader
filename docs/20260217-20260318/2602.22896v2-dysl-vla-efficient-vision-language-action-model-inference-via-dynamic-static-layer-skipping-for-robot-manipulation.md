---
title: "DySL-VLA: Efficient Vision-Language-Action Model Inference via Dynamic-Static Layer-Skipping for Robot Manipulation"
title_zh: DySL-VLA：通过动静态层跳过实现机器人操作的高效视觉-语言-动作模型推理
authors: "Zebin Yang, Yijiahao Qi, Tong Xie, Bo Yu, Shaoshan Liu, Meng Li"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22896v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 机器人操作的高效 VLA 模型推理
tldr: 针对视觉-语言-动作（VLA）模型在机器人任务中计算开销大的问题，本文提出DySL-VLA框架。该方法基于动作重要性差异，将模型层分为核心层和可跳过层，并引入先验-后验跳层指导机制。通过两阶段知识蒸馏训练，在保持精度的同时显著提升了推理速度并减少了参数量，为实时机器人操作提供了高效解决方案。
selection_source: fresh_fetch
motivation: VLA模型计算成本高，难以满足机器人实时操作的需求，且不同动作步骤对精度的要求存在差异。
method: 提出动态-静态跳层框架，将层分为信息层和增量层，并利用先验-后验指导机制及两阶段知识蒸馏进行优化。
result: "在Calvin数据集上，相比Deer-VLA成功长度提升2.1%，可训练参数减少85.7倍，且在同等精度下比RoboFlamingo快3.75倍。"
conclusion: DySL-VLA通过动态跳层策略有效平衡了VLA模型的推理效率与任务性能，是实现高效机器人操控的有力工具。
---

## 摘要
视觉-语言-动作（VLA）模型通过融合语言模型的推理能力与视觉模型的 3D 理解能力，在机器人操作等任务中取得了显著成功。然而，其高昂的计算成本仍然是需要实时性能的实际应用中的主要障碍。我们观察到，任务中的动作具有不同程度的重要性：关键步骤需要高精度，而次要步骤则可以容忍更大的方差。利用这一见解，我们提出了 DySL-VLA，这是一个通过根据每个动作的重要性动态跳过 VLA 层来降低计算成本的新型框架。DySL-VLA 将其层分为两类：始终执行的信息层（informative layers）和可选择性跳过的增量层（incremental layers）。为了在不牺牲准确性的情况下智能地跳过层，我们发明了一种先验-后验跳过引导机制，用于确定何时启动层跳过。我们还提出了一种跳过感知的两阶段知识蒸馏算法，以高效地将标准 VLA 训练为 DySL-VLA。实验表明，在 Calvin 数据集上，DySL-VLA 的成功长度比 Deer-VLA 提高了 2.1%，同时可训练参数减少了 85.7 倍，且在等精度条件下，相对于 RoboFlamingo 基准实现了 3.75 倍的加速。我们的代码可在 https://github.com/PKU-SEC-Lab/DYSL_VLA 获取。

## Abstract
Vision-Language-Action (VLA) models have shown remarkable success in robotic tasks like manipulation by fusing a language model's reasoning with a vision model's 3D understanding. However, their high computational cost remains a major obstacle for real-world applications that require real-time performance. We observe that the actions within a task have varying levels of importance: critical steps demand high precision, while less important ones can tolerate more variance. Leveraging this insight, we propose DySL-VLA, a novel framework that addresses computational cost by dynamically skipping VLA layers based on each action's importance. DySL-VLA categorizes its layers into two types: informative layers, which are consistently executed, and incremental layers, which can be selectively skipped. To intelligently skip layers without sacrificing accuracy, we invent a prior-post skipping guidance mechanism to determine when to initiate layer-skipping. We also propose a skip-aware two-stage knowledge distillation algorithm to efficiently train a standard VLA into a DySL-VLA. Our experiments indicate that DySL-VLA achieves 2.1% improvement in success length over Deer-VLA on the Calvin dataset, while simultaneously reducing trainable parameters by a factor of 85.7 and providing a 3.75x speedup relative to the RoboFlamingo baseline at iso-accuracy. Our code is available on https://github.com/PKU-SEC-Lab/DYSL_VLA.

---

## 论文详细总结（自动生成）

### DySL-VLA：通过动静态层跳过实现机器人操作的高效视觉-语言-动作模型推理

#### 1. 论文的核心问题与整体含义
*   **研究动机**：视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）虽然在机器人操控中表现出色，但其巨大的计算开销导致推理延迟高（通常仅 1-5 Hz），难以满足机器人实时物理交互（通常需 20-50+ Hz）的需求。
*   **核心观察**：作者发现机器人任务中的动作重要性并不相等（如“抓取”瞬间比“空中移动”更关键），且 VLA 模型内部不同层对最终预测的贡献也不同。
*   **整体含义**：本文旨在通过一种“按需分配计算资源”的策略，动态跳过不重要的模型层，在保证关键动作精度的前提下，大幅提升推理速度。

#### 2. 方法论
DySL-VLA 框架包含三个核心技术组件：
*   **动态-静态层跳过（Dynamic-Static Layer-Skipping）**：
    *   **静态层（Static Layers）**：通过分析层间激活值的余弦相似度，识别出对信息改变显著的“信息层”，这些层在推理中始终保留。
    *   **动态层（Dynamic Layers）**：信息改变较小的层被归为“增量层”，根据当前动作的重要性动态决定是否跳过。跳过时使用轻量级适配器（Adapter）来拟合被跳过层的功能。
*   **先验-后验跳过指导机制（Prior-post Skipping Guidance）**：
    *   **先验预测**：定义轨迹连续性（Continuity），利用前 $k$ 步动作的平滑度来评估当前动作的重要性。若连续性下降（意味着进入关键操作），则前移“允许跳过点”，强制执行更多层。
    *   **后验验证**：若检测到动作突变，系统会重新进行一次无跳过的完整推理进行验证，确保关键步骤不失误。
*   **跳过感知的两阶段知识蒸馏**：
    *   **第一阶段**：冻结 LLM 主干，仅训练适配器以总结动态层的激活信息。
    *   **第二阶段**：联合训练跳过控制器（Controller）和适配器，引入任务损失和稀疏性惩罚项，使模型学会自主选择跳过位置。

#### 3. 实验设计
*   **数据集与场景**：
    *   **CALVIN**：长程机器人操控基准，评估模型在连续子任务中的成功长度。
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，评估多任务泛化能力。
*   **对比方法**：
    *   **基准模型**：RoboFlamingo (3B), OpenVLA-oft (7B)。
    *   **加速方法**：DeeR-VLA（动态早期退出）、FlexiDepth（自适应跳层）、Random Skip（随机跳层）以及传统的 HULC、SPIL 等。
*   **评估指标**：平均成功长度（Success Length）、成功率（SR）、推理延迟（Latency）、可训练参数量。

#### 4. 资源与算力
*   **硬件平台**：
    *   训练使用 **NVIDIA RTX 4090** 和 **A6000**。
    *   部署与延迟测试在边缘计算平台 **NVIDIA Jetson Orin** 上完成。
*   **训练开销**：
    *   在 Calvin 数据集上，DySL-VLA 仅需 **37 GPU 小时**，而对比方法 DeeR-VLA 需要 112 GPU 小时。
    *   可训练参数量显著减少：3B 模型仅需训练 14M 参数，7B 模型为 226M 参数。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3B 到 7B 不同规模的 VLA 模型，并在两个主流机器人基准上进行了详尽测试。
*   **消融实验**：针对静态层比例、跳过步长 $\delta l$、轨迹长度 $k$ 以及各个模块（先验/后验指导、两阶段蒸馏）的有效性进行了多组对比实验。
*   **客观性**：实验对比了等精度下的加速比，并提供了在嵌入式设备（Jetson Orin）上的真实表现，证明了其在资源受限环境下的实用性。

#### 6. 主要结论与发现
*   **性能提升**：在 Calvin 数据集上，DySL-VLA 比 DeeR-VLA 成功长度提升 2.1%，同时可训练参数减少 85.7 倍。
*   **加速效果**：相比 RoboFlamingo 基准，实现了 **3.75 倍** 的推理加速；在 Jetson Orin 上，OpenVLA-oft 的控制频率可从低频提升至 **23.2 Hz**。
*   **层重要性**：验证了 VLA 模型中存在明显的“信息瓶颈”层，保留这些层并跳过冗余层是平衡效率与精度的关键。

#### 7. 优点
*   **动作感知**：不同于传统的均匀剪枝或量化，该方法能根据机器人任务的实时需求动态调整计算量。
*   **训练高效**：无需微调庞大的 LLM 主干网络，仅训练轻量级控制器和适配器，极大地降低了微调成本。
*   **部署友好**：属于“Kernel-free”方法，不需要专门的硬件算子支持，易于在各种机器人平台上集成。

#### 8. 不足与局限
*   **后验验证开销**：虽然关键动作占比小，但在动作频繁突变的复杂任务中，后验验证（重新推理）可能会瞬间增加延迟，影响控制的确定性。
*   **超参数敏感性**：静态层的选择比例和连续性阈值 $\eta$ 可能需要针对不同类型的机器人任务进行手动调优。
*   **基础模型依赖**：其性能上限仍受限于预训练 VLA 模型本身的理解能力，无法解决基础模型无法处理的逻辑错误。

（完）
