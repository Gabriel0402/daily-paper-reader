---
title: Recursive Belief Vision Language Action Models
title_zh: 递归信念视觉-语言-动作模型
authors: "Vaidehi Bagaria, Bijo Sebastian, Nirav Kumar Patel"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20659v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于长程任务的视觉-语言-动作模型
tldr: 针对视觉语言动作模型（VLA）在长程任务中因部分可观测性导致的动作重复和高延迟问题，本文提出RB-VLA。该模型引入递归信念架构，通过自监督世界模型维护紧凑的潜状态以记录任务历史与动力学。结合VLM提供的高层意图，RB-VLA在多阶段任务中显著提升了成功率并大幅降低了推理延迟，克服了现有模型缺乏持久状态表示的缺陷。
selection_source: fresh_fetch
motivation: 现有VLA模型多为观测驱动，缺乏持久的动作条件状态表示，导致在长程任务中难以处理感知混淆和部分可观测性问题。
method: 提出一种以信念为中心的架构，通过自监督学习维护包含历史和动力学信息的紧凑潜状态，并与VLM生成的意图共同驱动扩散策略。
result: "在多阶段取放和堆叠任务中，RB-VLA的成功率分别比pi_0高出52.5%和37.5%，同时推理延迟降低了5倍且内存不随时间增长。"
conclusion: 递归信念机制是提升VLA模型在复杂长程任务中时间与物理推理能力的关键，能实现更稳健的闭环控制。
---

## 摘要
视觉-语言-动作模型（VLA）必须使智能体能够在部分可观测的情况下执行长时程任务。然而，大多数现有方法仍以观测为驱动，依赖于短上下文窗口或对视觉-语言模型（VLM）的重复查询。这会导致任务进度丢失、感知混淆下的动作重复以及高推理延迟。虽然语义对齐很重要，但长时程操作从根本上需要持久的、以动作为条件的状态表示。当前的 VLA 缺乏此类表示，且表现出有限的时间和物理推理能力，使其不适用于多阶段控制。本文介绍了 RB-VLA，这是一种以信念为中心的架构，通过自监督世界模型目标进行训练，能够维持一个编码了任务相关历史、动力学和物体交互的紧凑潜状态。VLM 在每个任务中仅被查询一次以提供高层意图，而信念模块则负责跟踪任务进度，并在部分可观测的情况下实现阶段感知且具有因果基础的控制，且无需存储原始观测数据或随时间增加内存开销。信念和意图共同作为扩散策略的条件，以实现鲁棒的闭环执行。RB-VLA 在长时程基准测试中优于先前的 VLA，在多阶段取放和堆叠任务中，其成功率分别比 pi_0 高出 52.5% 和 37.5%。与基准模型相比，它还将推理延迟降低了多达五倍，并消除了现有 VLA 中观察到的随时间步增长的内存占用。消融实验表明，信念模块是性能提升的主要驱动力，使成功率从无信念时的 32.5% 提高到有信念时的 77.5%。

## Abstract
Vision-language-action models must enable agents to execute long-horizon tasks under partial observability. However, most existing approaches remain observation-driven, relying on short context windows or repeated queries to vision-language models (VLMs). This leads to loss of task progress, action repetition under perceptual aliasing, and high inference latency. While semantic grounding is important, long-horizon manipulation fundamentally requires persistent, action-conditioned state representations. Current VLAs lack such representations and exhibit limited temporal and physical reasoning, making them ill-suited for multi-stage control. This paper introduces RB-VLA, a belief-centric architecture trained with self-supervised world-model objectives that maintains a compact latent state encoding task-relevant history, dynamics, and object interactions. Queried once per task, the VLM provides high-level intent, while the belief tracks task progress and enables phase-aware, causally grounded control under partial observability without storing raw observations or scaling memory with time. The belief and intent jointly condition a diffusion policy for robust closed-loop execution. RB-VLA outperforms prior VLAs on long-horizon benchmarks, achieving 52.5 percent and 37.5 percent higher success rates on multi-stage pick-and-place and stacking tasks, respectively, compared to pi_0. It also reduces inference latency by up to five times relative to baselines and eliminates memory growth across timesteps observed in existing VLAs. Ablations show the belief module is the primary driver of performance, increasing success rates from 32.5 percent without belief to 77.5 percent with belief.

---

## 论文详细总结（自动生成）

### 递归信念视觉-语言-动作模型 (RB-VLA) 深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作模型（VLA）大多是“观测驱动”的，依赖短时上下文窗口或对大型视觉语言模型（VLM）的频繁查询。这导致在长程（Long-horizon）任务中容易出现任务进度丢失、感知混淆（如物体被遮挡后重复抓取）以及极高的推理延迟。
*   **研究动机**：长程操作本质上需要持久的、以动作为条件的状态表示。现有的 VLA 缺乏这种内部状态，且内存占用随时间步增长。本文旨在引入一种“递归信念”机制，使机器人能在部分可观测环境下，通过紧凑的潜状态跟踪任务进度和动力学。

#### 2. 方法论：核心思想与技术细节
RB-VLA 架构由三个核心组件组成，实现了语义推理与低级控制的解耦：
*   **递归信念估计器 (Belief Estimator)**：
    *   采用类似 RSSM（循环状态空间模型）的世界模型结构。
    *   **输入**：前一时刻信念 $b_{t-1}$、当前观测窗口（图像特征、机器人本体状态、动作历史）。
    *   **处理**：使用 Transformer 融合多模态 Token，通过学习到的注意力池化生成帧摘要。引入随机潜变量 $z_t$ 来捕捉不确定性。
    *   **训练目标**：包含未来潜状态预测（自监督）、KL 散度约束（结构化潜空间）以及逆动力学损失（确保信念与动作相关）。
*   **解耦的高层意图预测 (High-Level Intent)**：
    *   **核心思想**：VLM（如 Qwen2.5-VL）仅在任务开始时查询一次。
    *   通过注意力池化从 VLM 的隐藏状态中提取与任务相关的“意图向量” $I_t$。该向量在整个任务期间保持静态，除非需要重新规划。
*   **低级动作生成 (Action Generation)**：
    *   使用扩散 Transformer (DiT) 作为策略头。
    *   **条件**：同时基于当前的递归信念 $b_t$ 和静态意图 $I_t$。
    *   **输出**：以闭环方式高频（50Hz）生成动作块（Action Chunks）。

#### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：RoboSuite、LIBERO-Long、LIBERO-Object 以及 UR5 MuJoCo 环境。
    *   **任务类型**：单/多物体取放（含遮挡）、多块堆叠、家庭任务（如放进抽屉）。
*   **Benchmark 与对比方法**：
    *   对比了 **OpenVLA**、**$\pi_0$**、**GROOT-N1** 和 **RT-1-X** 等主流 VLA 模型。
    *   评估指标包括任务成功率、推理延迟和内存增长曲线。
*   **测试设置**：每个任务进行 40 次试验，随机化物体位置、光照和遮挡情况。

#### 4. 资源与算力
*   **硬件**：使用了 **12 台 NVIDIA A100 GPU (80GB)**。
*   **训练细节**：
    *   信念模型：训练 80k 次迭代，Batch Size 为 128。
    *   扩散策略：训练 100k 次迭代，Batch Size 为 32。
    *   数据集规模：约 40,000 条模拟轨迹（约 4000 万个时间步）。
*   **推理性能**：在单张 A100 上，推理延迟比 OpenVLA 降低了 5 倍。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 10 种不同类型的任务，总计 40,000 条轨迹，数据量充足。
*   **消融实验**：针对“帧编码器目标”、“随机变量 $z$”以及“信念反馈”进行了详细消融，证明了信念模块是性能提升的核心（成功率从 32.5% 提升至 77.5%）。
*   **客观性**：通过引入遮挡、随机化环境和真实世界部署（25 次真实试验），验证了模型的泛化能力和鲁棒性，实验设计较为公平且全面。

#### 6. 主要结论与发现
*   **性能飞跃**：在多阶段取放任务中，RB-VLA 的成功率比 $\pi_0$ 高出 **52.5%**，在堆叠任务中高出 **37.5%**。
*   **效率提升**：由于 VLM 仅需查询一次，推理速度大幅提升，且内存占用不随任务时长增加（恒定内存）。
*   **鲁棒性**：信念机制使机器人能在物体被遮挡后依然“记得”任务进度，有效解决了感知混淆问题。
*   **信念质量**：实验证明信念空间的相似性与未来动作/观测的相似性高度相关，说明模型确实学习到了物理动力学。

#### 7. 优点
*   **架构优雅**：成功解决了 VLA 模型在长程任务中“短视”和“高延迟”的痛点。
*   **内存友好**：递归信念将历史压缩为固定维度的向量，适合长时间运行的机器人系统。
*   **因果推理**：通过世界模型目标训练，使模型具备了初步的物理常识和因果推理能力，而非简单的图像-动作映射。

#### 8. 不足与局限
*   **Sim-to-Real 依赖**：虽然支持零样本迁移，但在真实世界部署时仍需 100 条真实轨迹进行微调以应对传感器噪声。
*   **VLM 静态假设**：目前将意图设为静态，若环境发生剧烈动态变化（如人类干预改变了任务目标），可能需要更复杂的重新查询机制。
*   **任务复杂度**：虽然在取放和堆叠上表现优异，但对于需要极高精细操作（如穿针引线）或极多物体交互的场景，信念状态的表征能力上限尚待验证。

（完）
