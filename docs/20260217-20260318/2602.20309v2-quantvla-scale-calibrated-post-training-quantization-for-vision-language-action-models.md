---
title: "QuantVLA: Scale-Calibrated Post-Training Quantization for Vision-Language-Action Models"
title_zh: QuantVLA：面向视觉-语言-动作模型的尺度校准训练后量化
authors: "Jingxuan Zhang, Yunta Hsieh, Zhongwei Wan, Haokun Lin, Xin Wang, Ziqi Wang, Yingtie Lei, Mi Zhang"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20309v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉-语言-动作 (VLA) 模型的训练后量化
tldr: QuantVLA是首个针对视觉-语言-动作（VLA）模型的训练后量化（PTQ）框架，旨在解决具身智能模型部署中的计算和内存瓶颈。它通过选择性量化布局、注意力温度匹配和输出头平衡技术，首次实现了对扩散Transformer（DiT）动作头的成功量化。该框架无需额外训练，在LIBERO基准测试中不仅超过了全精度基准的成功率，还显著降低了内存占用并提升了推理速度。
selection_source: fresh_fetch
motivation: 随着VLA模型规模扩大，其巨大的计算和内存需求限制了在资源受限的具身智能设备上的实际部署。
method: 提出了一种包含选择性量化布局、注意力温度匹配和输出头平衡的训练后量化框架，支持对语言骨干和DiT动作头进行低比特量化。
result: "在LIBERO任务中超越了全精度基准的成功率，实现约70%的内存节省和1.22倍的端到端推理加速。"
conclusion: QuantVLA为在严格的计算和功耗约束下实现可扩展、低比特的具身智能提供了一条高效且实用的路径。
---

## 摘要
视觉-语言-动作（VLA）模型为具身智能体统一了感知、语言和控制，但由于计算和内存需求迅速增加，特别是在模型扩展到更长的时间跨度和更大的骨干网络时，在实际部署中面临重大挑战。为了解决这些瓶颈，我们引入了 QuantVLA，这是一个无需训练的训练后量化（PTQ）框架。据我们所知，这是首个针对 VLA 系统的 PTQ 方法，也是首个成功量化扩散 Transformer（DiT）动作头的方案。QuantVLA 包含三个尺度校准组件：(1) 选择性量化布局，将语言骨干网络和 DiT 中的所有线性层整数化，同时保持注意力投影为浮点格式，以保留原始算子调度；(2) 注意力温度匹配，一种轻量级的逐头缩放机制，用于稳定注意力 logit，并在推理时折叠进反量化尺度中；(3) 输出头平衡，一种逐层残差接口校准，用于减轻投影后的能量漂移。该框架不需要额外的训练，仅使用少量的无标签校准缓冲区，并支持低比特权重和激活的整数算子，同时保持架构不变。在 LIBERO 上的代表性 VLA 模型中，QuantVLA 超过了全精度基线的任务成功率，在量化组件上实现了约 70% 的相对内存节省，并带来了 1.22 倍的端到端推理延迟加速，为在严格的计算、内存和功耗限制下实现可扩展的低比特具身智能提供了一条切实可行的路径。

## Abstract
Vision-language-action (VLA) models unify perception, language, and control for embodied agents but face significant challenges in practical deployment due to rapidly increasing compute and memory demands, especially as models scale to longer horizons and larger backbones. To address these bottlenecks, we introduce QuantVLA, a training-free post-training quantization (PTQ) framework that, to our knowledge, is the first PTQ approach for VLA systems and the first to successfully quantize a diffusion transformer (DiT) action head. QuantVLA incorporates three scale-calibrated components: (1) a selective quantization layout that integerizes all linear layers in both the language backbone and the DiT while keeping attention projections in floating point to preserve the original operator schedule; (2) attention temperature matching, a lightweight per-head scaling mechanism that stabilizes attention logits and is folded into the dequantization scales at inference; and (3) output head balancing, a per-layer residual interface calibration that mitigates post-projection energy drift. The framework requires no additional training, uses only a small unlabeled calibration buffer, and supports integer kernels for low-bit weights and activations while leaving the architecture unchanged. Across representative VLA models on LIBERO, QuantVLA exceeds the task success rates of full-precision baselines, achieves about 70% relative memory savings on the quantized components, and delivers a 1.22x speedup in end-to-end inference latency, providing a practical pathway toward scalable low-bit embodied intelligence under strict compute, memory, and power constraints.

---

## 论文详细总结（自动生成）

以下是对论文《QuantVLA: Scale-Calibrated Post-Training Quantization for Vision-Language-Action Models》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：视觉-语言-动作（VLA）模型（如 OpenVLA, $\pi_0$）通过整合大规模语言模型（LLM）和扩散 Transformer（DiT）动作头，实现了强大的具身智能。然而，这些模型巨大的参数量和计算开销限制了其在资源受限的机器人硬件上的实时部署。
*   **核心问题**：现有的效率优化方案多集中在视觉编码器或剪枝，而忽略了占据大量计算份额的语言骨干和 DiT 动作头。传统的训练后量化（PTQ）直接应用于 VLA 时，由于 DiT 对激活值波动极度敏感，会导致严重的性能下降（如动作不连贯、任务失败）。
*   **整体含义**：本文提出了 **QuantVLA**，这是首个针对 VLA 系统、特别是针对 DiT 动作头的训练后量化框架。它通过尺度校准技术，在不进行任何重新训练的情况下，实现了低比特量化并保持甚至提升了任务成功率。

### 2. 方法论
QuantVLA 的核心思想是识别并修复量化引起的“尺度漂移（Scale Drift）”。主要包含三个组件：
*   **选择性量化布局（Selective Quantization Layout）**：
    *   将语言骨干网络（LLM）和 DiT 中的所有线性层（MLP）进行整数化（如 W4A8）。
    *   **关键点**：保持注意力机制中的投影层（Q, K, V, O）为浮点格式。研究发现这些层对上游分布偏移最敏感，保留浮点格式可有效防止误差在深度网络中累积。
*   **注意力温度匹配（ATM, Attention Temperature Matching）**：
    *   量化会改变注意力 logit 的方差，导致 Softmax 分布过尖或过平（即温度漂移）。
    *   **技术细节**：通过无标签校准数据计算逐头（Per-head）缩放因子 $\alpha$，对量化后的 logit 进行校准，使其分布与全精度教师模型一致。
*   **输出头平衡（OHB, Output Head Balancing）**：
    *   量化会改变残差连接处的能量（RMS）。
    *   **技术细节**：引入逐层（Per-layer）残差接口校准因子 $\beta$，恢复残差注入的增益，确保层归一化（LayerNorm）的操作点稳定。
*   **底层支撑**：基于 DuQuant 的旋转技术，通过正交变换重新分布激活值中的离群点（Outliers），提高量化的鲁棒性。校准因子 $\alpha$ 和 $\beta$ 在推理时会被折叠进反量化尺度中，不增加额外的算子开销。

### 3. 实验设计
*   **评估模型**：OpenPI $\pi_{0.5}$ 和 GR00T N1.5（均采用 DiT 动作头）。
*   **基准测试（Benchmark）**：
    *   **LIBERO**：包含 Spatial（空间关系）、Object（物体操纵）、Goal（目标达成）、Long（长程任务）四个子集。
    *   **Simpler & Pick-and-Can**：用于验证跨任务的鲁棒性。
*   **对比方法**：
    *   FP16（全精度基线）。
    *   SmoothQuant（经典的 LLM 量化方法）。
    *   DuQuant（未针对 VLA 优化的量化方法）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA A100 GPU** 上进行。
*   **算力消耗**：
    *   由于是 PTQ（训练后量化），**无需重新训练**。
    *   **校准过程**：仅需极少量的无标签数据（32 个 batch，128 个步骤），在单台机器上即可快速完成。
    *   **推理提升**：实现了约 1.22 倍的端到端推理加速。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流 VLA 模型上进行了全面测试。
    *   涵盖了从 W4A8 到更激进的 W4A4 多种精度配置。
    *   进行了详细的消融实验，验证了 ATM、OHB 以及选择性量化布局各自的贡献。
*   **充分性与公平性**：实验对比了不同去噪步数（Denoising Steps）下的表现，并使用了标准的 LIBERO 评估协议。通过在多个不同性质的任务集上测试，证明了该方法的普适性。实验设计客观，充分考虑了内存节省与任务成功率的权衡。

### 6. 主要结论与发现
*   **性能卓越**：QuantVLA 在 W4A8 配置下，在 LIBERO 任务中的平均成功率不仅匹配、甚至在某些长程任务中**超过了 FP16 基线**（例如在 $\pi_{0.5}$ 上达到 97.6% vs 97.1%）。
*   **内存节省**：在量化组件上实现了约 **70% 的相对内存节省**（例如 $\pi_{0.5}$ 内存从 4.27GB 降至 1.28GB）。
*   **DiT 敏感性**：首次系统分析并证明了 DiT 动作头对量化引起的 logit 温度和残差能量漂移极其敏感，而 ATM 和 OHB 是解决该问题的关键。

### 7. 优点
*   **训练零成本**：无需昂贵的微调或重新训练，适合快速部署。
*   **架构保持**：不改变模型原始架构和算子调度，具有极高的工程实用性。
*   **首创性**：填补了 VLA 模型中 DiT 动作头量化研究的空白。
*   **性能增益**：量化过程意外地起到了一定的正则化作用，提升了模型在长程任务中的稳定性。

### 8. 不足与局限
*   **部分层保留浮点**：为了稳定性，注意力投影层仍需保持浮点格式，这意味着无法实现 100% 的全整数推理。
*   **实验场景限制**：主要在模拟环境（LIBERO, Simpler）中进行验证，缺乏在真实物理机器人硬件上的长时运行数据。
*   **校准依赖**：虽然校准数据量小，但仍需针对特定任务域的无标签数据来获取最佳的校准因子。

（完）
