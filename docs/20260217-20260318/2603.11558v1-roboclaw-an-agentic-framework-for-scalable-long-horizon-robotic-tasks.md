---
title: "RoboClaw: An Agentic Framework for Scalable Long-Horizon Robotic Tasks"
title_zh: RoboClaw：一种用于可扩展长程机器人任务的智能体框架
authors: "Ruiying Li, Yunlang Zhou, YuYao Zhu, Kylin Chen, Jingyuan Wang, Sukai Wang, Kongtao Hu, Minhui Yu, Bowen Jiang, Zhan Su, Jiayao Ma, Xin He, Yongjian Shen, Yangyang, Guanghui Ren, Maoqing Yao, Wenhao Wang, Yao Mu"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11558v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于机器人操作和任务执行的VLA框架
tldr: RoboClaw是一个统一的数据采集、策略学习与任务执行的智能体框架，旨在解决长程机器人任务中数据采集难和多策略执行脆弱的问题。它引入了“纠缠动作对（EAP）”机制，通过前向操作与逆向恢复动作的耦合实现自主复位和持续数据采集。该框架利用视觉语言模型（VLM）进行高层推理和策略调度，显著降低了人工干预，提升了长程任务的成功率和系统的可扩展性。
selection_source: fresh_fetch
motivation: 现有的机器人学习流程在数据采集、策略学习和部署上相互分离，导致对人工环境重置的高度依赖以及长程任务执行的脆弱性。
method: 提出RoboClaw框架，通过纠缠动作对（EAP）实现自主复位的数据采集，并利用VLM驱动的控制器进行高层推理与动态策略编排。
result: "在真实世界任务中，该方法比基准方法在长程任务成功率上提升了25%，同时减少了53.7%的人工投入时间。"
conclusion: RoboClaw通过统一采集与执行阶段的上下文语义，有效提升了机器人系统的自主性、稳定性和长程任务处理能力。
---

## 摘要
视觉-语言-动作（VLA）系统在语言驱动的机器人操控方面展现出强大潜力。然而，将其扩展到长程任务仍然具有挑战性。现有的流程通常将数据采集、策略学习和部署分开，导致对人工环境重置的严重依赖以及多策略执行的脆弱性。我们提出了 RoboClaw，这是一个智能体机器人框架，它在单一的 VLM 驱动控制器下统一了数据采集、策略学习和任务执行。在策略层面，RoboClaw 引入了纠缠动作对（Entangled Action Pairs, EAP），将前向操控行为与逆向恢复动作耦合，形成用于自主数据采集的自重置循环。该机制能够以极少的人工干预实现持续的同策略（on-policy）数据获取和迭代策略优化。在部署期间，同一个智能体执行高层推理，并动态编排已学习的策略原语以完成长程任务。通过在采集和执行过程中保持一致的上下文语义，RoboClaw 减少了这两个阶段之间的不匹配，并提高了多策略的鲁棒性。在真实世界操控任务中的实验表明，与传统的开环流程相比，该框架提高了稳定性和可扩展性，同时显著减少了整个机器人生命周期中的人力投入，在长程任务上的成功率比基准方法提高了 25%，并将人力时间投入减少了 53.7%。

## Abstract
Vision-Language-Action (VLA) systems have shown strong potential for language-driven robotic manipulation. However, scaling them to long-horizon tasks remains challenging. Existing pipelines typically separate data collection, policy learning, and deployment, resulting in heavy reliance on manual environment resets and brittle multi-policy execution. We present RoboClaw, an agentic robotics framework that unifies data collection, policy learning, and task execution under a single VLM-driven controller. At the policy level, RoboClaw introduces Entangled Action Pairs (EAP), which couple forward manipulation behaviors with inverse recovery actions to form self-resetting loops for autonomous data collection. This mechanism enables continuous on-policy data acquisition and iterative policy refinement with minimal human intervention. During deployment, the same agent performs high-level reasoning and dynamically orchestrates learned policy primitives to accomplish long-horizon tasks. By maintaining consistent contextual semantics across collection and execution, RoboClaw reduces mismatch between the two phases and improves multi-policy robustness. Experiments in real-world manipulation tasks demonstrate improved stability and scalability compared to conventional open-loop pipelines, while significantly reducing human effort throughout the robot lifecycle, achieving a 25% improvement in success rate over baseline methods on long-horizon tasks and reducing human time investment by 53.7%.

---

## 论文详细总结（自动生成）

以下是对论文《RoboClaw: An Agentic Framework for Scalable Long-Horizon Robotic Tasks》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管视觉-语言-动作（VLA）模型在简单任务上表现出色，但在处理真实世界中的**长程（Long-horizon）任务**时面临巨大挑战。
*   **核心痛点**：
    1.  **数据采集成本高**：依赖人工演示和频繁的手动环境重置。
    2.  **流程脱节**：数据采集、策略训练和部署执行通常是分离的，导致语义理解不一致和分布偏移（Distribution Mismatch）。
    3.  **执行脆弱**：长程任务中微小的误差会随步骤累积，缺乏有效的错误恢复机制。
*   **整体含义**：论文提出了 **RoboClaw** 框架，旨在通过一个统一的视觉语言模型（VLM）作为“大脑”，将机器人生命周期中的数据采集、策略学习和任务执行整合进一个闭环智能体系统中。

### 2. 方法论
*   **核心思想**：利用 VLM 的高层推理能力驱动底层 VLA 策略，并通过“自重置”机制实现自主数据规模化。
*   **关键技术细节**：
    *   **纠缠动作对 (Entangled Action Pairs, EAP)**：这是该框架的核心创新。它将“前向操作”（如把物体放入抽屉）与“逆向恢复”（如把物体拿出来）配对。通过这种方式，机器人可以自主完成环境重置，形成持续的同策略（on-policy）数据采集循环。
    *   **统一智能体架构**：采用 VLM 作为元控制器，通过**思维链（CoT）**推理环境状态。
    *   **模型上下文协议 (MCP)**：定义了一套标准接口，使 VLM 能够调用不同的“工具”（如启动策略、查询机器人状态、请求人工干预）。
    *   **结构化记忆机制**：包含角色身份（Role Identity）、任务级记忆（跟踪长程进度）和工作记忆（短期执行历史）。
*   **算法流程**：VLM 接收视觉观察和记忆，通过 CoT 决定下一步是执行特定策略、重试、切换策略还是请求人类帮助，执行结果会反馈并更新记忆。

### 3. 实验设计
*   **实验平台**：Agibot G01 双臂移动机器人（20 自由度 + 灵巧手）。
*   **实验场景**：涵盖了卧室梳妆台整理、厨房货架布置、书桌清理和便利店理货四个真实场景。
*   **具体任务**：包括身体乳放置、妆前乳放置（含关抽屉）、口红精准插入、纸巾擦拭（持续接触任务）。
*   **对比基准 (Benchmark)**：
    1.  **手动采集基准**：完全依赖人工演示和重置的传统流程。
    2.  **端到端 VLA 基准**：直接使用 $\pi_{0.5}$ 模型而不使用 RoboClaw 的智能体编排。
    3.  **理论预测值**：基于子任务成功率乘积计算的预期长程成功率。

### 4. 资源与算力
*   **模型微调**：底层策略基于 $\pi_{0.5}$，使用 **LoRA** 技术进行微调。
*   **训练参数**：Rank=16, Alpha=16, 精度为 bfloat16，Batch size 为 16，训练步数为 10k 步。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）以及总体的训练耗时。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 4 种不同类型的操控技能进行了 5 轮迭代训练评估。
    *   每项单项任务进行了 50 次测试。
    *   长程任务（涉及多个步骤的组合）进行了 20 次完整试验。
*   **充分性评价**：实验设计较为客观且充分。它不仅对比了任务成功率，还量化了**人力投入时间**（减少 53.7%）和**人工干预频率**（降低约 8 倍），从工程效率和模型性能两个维度验证了框架的有效性。

### 6. 主要结论与发现
*   **自主性显著提升**：EAP 机制使得机器人能够自主采集数据，显著降低了对人类的依赖。
*   **性能迭代增益**：随着迭代次数增加，复杂任务（如口红插入）的成功率从最初的 4% 提升至 46%。
*   **长程鲁棒性**：在长程任务中，RoboClaw 比纯 VLA 方法成功率高出 **25%**。这归功于 VLM 能够识别失败并主动调用恢复策略（Recovery Behaviors）。
*   **闭环进化**：系统能够将部署中的失败案例转化为新的训练数据，实现技能库的自我扩充。

### 7. 优点
*   **系统性创新**：打破了机器人领域“数据-训练-部署”的线性孤岛，提出了全生命周期的智能体方案。
*   **解决重置难题**：EAP 机制为解决强化学习和模仿学习中昂贵的“环境重置”问题提供了一个非常实用的工程思路。
*   **语义一致性**：由于采集和执行使用同一个 VLM 控制器，保证了任务理解在不同阶段的高度统一。

### 8. 不足与局限
*   **实时性挑战**：依赖云端大模型进行高层推理可能带来延迟，影响对瞬时变化的响应。
*   **逆向动作的局限性**：EAP 假设动作是可逆的。对于某些不可逆任务（如倒水、切割、焊接），该自重置机制难以直接应用。
*   **安全边界**：虽然有“呼叫人类”机制，但在极端危险或未定义的物理冲突中，VLM 的判断可能不够及时。
*   **算力细节缺失**：未披露具体硬件资源消耗，不便于其他研究者评估复现成本。

（完）
