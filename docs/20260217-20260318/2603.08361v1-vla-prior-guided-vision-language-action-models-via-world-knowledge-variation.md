---
title: "$Δ$VLA: Prior-Guided Vision-Language-Action Models via World Knowledge Variation"
title_zh: $Δ$VLA：基于世界知识变化的先验引导视觉-语言-动作模型
authors: "Yijie Zhu, Jie He, Rui Shao, Kaishen Yuan, Tao Tan, Xiaochen Yuan, Zitong Yu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08361v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于机器人操纵的先验引导视觉-语言-动作 (VLA) 模型
tldr: 针对现有视觉-语言-动作（VLA）模型侧重预测未来状态而非变化过程的问题，本文提出 ΔVLA 框架。该框架通过建模相对于当前世界知识先验的变化（Variation）来引导动作生成。核心贡献包括：利用 PWKE 提取关键环境特征，通过 LWVQ 在离散潜空间编码知识演变，并设计 CV-Atten 减少干扰。实验证明该方法在模拟和真实机器人任务中均达到 SOTA 水平且效率更高。
selection_source: fresh_fetch
motivation: 现有模型倾向于预测绝对的未来状态，缺乏对动作引起的世界知识变化过程的深入推理。
method: 提出一种先验引导框架，通过 PWKE 提取环境先验，利用 LWVQ 量化知识变化，并结合 CV-Atten 实现解耦学习。
result: 在模拟基准和真实机器人任务中均实现了最先进的性能，同时在计算效率上表现优异。
conclusion: 通过建模世界知识的相对变化而非绝对状态，能够更有效地增强 VLA 模型的感知与控制集成能力。
---

## 摘要
最近的视觉-语言-动作（VLA）模型通过统一感知、推理和控制，显著推进了机器人操作。为了实现这种集成，最近的研究采用了一种预测范式，通过建模未来的视觉状态或世界知识来引导动作生成。然而，这些模型强调预测结果，而不是推理变化的底层过程，而这对于确定如何行动至关重要。为了解决这个问题，我们提出了 $Δ$VLA，这是一个先验引导的框架，它相对于显式的当前世界知识先验来建模世界知识的变化以生成动作，而不是回归绝对的未来世界状态。具体而言：1）为了构建当前世界知识先验，我们提出了先验引导的世界知识提取器（PWKE），它在辅助头和先验伪标签的引导下，从视觉输入中提取可操作区域、空间关系和语义线索，从而减少冗余；2）在此基础上，为了表示世界知识在动作下如何演变，我们引入了潜世界变化量化（LWVQ），通过 VQ-VAE 目标学习离散潜空间来编码世界知识的变化，将预测从全模态转向紧凑的潜空间；3）此外，为了减轻变化建模过程中的干扰，我们设计了条件变化注意力（CV-Atten），以促进解耦学习并保持知识表示的独立性。在模拟基准和真实机器人任务上的广泛实验表明，$Δ$VLA 在提高效率的同时实现了最先进的性能。代码和真实世界执行视频可在 https://github.com/JiuTian-VL/DeltaVLA 获取。

## Abstract
Recent vision-language-action (VLA) models have significantly advanced robotic manipulation by unifying perception, reasoning, and control. To achieve such integration, recent studies adopt a predictive paradigm that models future visual states or world knowledge to guide action generation. However, these models emphasize forecasting outcomes rather than reasoning about the underlying process of change, which is essential for determining how to act. To address this, we propose $Δ$VLA, a prior-guided framework that models world-knowledge variations relative to an explicit current-world knowledge prior for action generation, rather than regressing absolute future world states. Specifically, 1) to construct the current world knowledge prior, we propose the Prior-Guided WorldKnowledge Extractor (PWKE). It extracts manipulable regions, spatial relations, and semantic cues from the visual input, guided by auxiliary heads and prior pseudo labels, thus reducing redundancy. 2) Building upon this, to represent how world knowledge evolves under actions, we introduce the Latent World Variation Quantization (LWVQ). It learns a discrete latent space via a VQ-VAE objective to encode world knowledge variations, shifting prediction from full modalities to compact latent. 3)Moreover, to mitigate interference during variation modeling, we design the Conditional Variation Attention (CV-Atten), whichpromotes disentangled learning and preserves the independence of knowledge representations. Extensive experiments on both simulated benchmarks and real-world robotic tasks demonstrate $Δ$VLA achieves state-of-the-art performance while improving efficiency. Code and real-world execution videos are available at https://github.com/JiuTian-VL/DeltaVLA.

---

## 论文详细总结（自动生成）

这篇论文提出了 **$\Delta$VLA**，一种基于世界知识变化（Variation）引导的视觉-语言-动作模型。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的预测型 VLA 模型（如预测未来图像或世界状态）侧重于“未来长什么样”，而忽略了“世界如何随动作改变”这一动态过程。
*   **研究动机**：
    1.  **缺乏因果锚点**：直接预测绝对未来状态而不参考当前状态，容易导致模型产生脱离现实的“幻觉”。
    2.  **冗余与不稳定性**：全模态预测（如像素级）包含大量与控制无关的冗余信息，且连续的变化表示在不同场景下波动剧烈，难以作为稳定的策略输入。
    3.  **目标**：通过建模相对于当前先验的**离散变化量**，实现更精准、高效的机器人操纵推理。

### 2. 方法论
$\Delta$VLA 框架由三个核心组件构成：
*   **PWKE（先验引导世界知识提取器）**：
    *   利用 SigLIP（语义强）和 DINOv2（空间强）的双编码器结构。
    *   提取三类关键知识：**可操作区域**（通过运动线索监督）、**深度信息**（空间几何）和**语义特征**。
    *   引入辅助头（Auxiliary Heads）和伪标签（来自 SAM, Depth-Anything v2 等）进行显式监督，过滤视觉冗余。
*   **LWVQ（潜世界变化量化）**：
    *   采用 VQ-VAE 架构，在无监督下学习一个离散的“变化码本”（Codebook）。
    *   将当前与未来状态之间的连续差异压缩为离散的潜变量（Latent Variation），作为动作生成的紧凑接口。
*   **CV-Atten（条件变化注意力机制）**：
    *   设计了一种结构化掩码注意力。
    *   强制每个变化标记（Variation Token）仅关注其对应的知识先验（如深度变化仅关注深度先验），减少跨模态干扰，实现解耦学习。

### 3. 实验设计
*   **数据集与场景**：
    *   **模拟环境**：LIBERO（包含 Spatial, Object, Goal, Long 四个子集，共 40 个任务）和 RoboTwin 2.0（8 个双臂协作任务）。
    *   **真实世界**：在 Galaxea R1 Lite 和 AgileX Cobot Magic 两个平台上执行长程任务（如抽屉操作、鞋子对齐、折叠 T 恤、水果盘摆放）。
*   **对比方法**：
    *   通用 VLA 模型：OpenVLA, $\pi_0$, RT 系列等。
    *   预测型模型：CoT-VLA, Seer, DreamVLA, UniVLA 等。

### 4. 资源与算力
*   **训练算力**：使用了 **8 张 NVIDIA A800 (80GB) GPU**。
*   **推理算力**：真实世界部署在单张 **RTX 5090 (32GB) GPU** 上。
*   **训练时长**：在 LIBERO 上微调约 60K 步；训练效率显著优于基准，每 10k 步耗时约 **4.9 小时**（对比 OpenVLA 的 11.7 小时）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 50 多个模拟任务和 4 个复杂的真实世界长程任务。
*   **消融实验**：非常充分（Table V-X），分别验证了 PWKE、LWVQ、CV-Atten 的有效性，并探讨了 Token 数量、伪标签噪声敏感度、连续 vs 离散表示等细节。
*   **客观性**：通过在不同机器人硬件平台（单臂/双臂、移动/固定）上测试，证明了模型的泛化能力和鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：$\Delta$VLA 在 LIBERO 平均成功率达到 **97.8%**，在 RoboTwin 2.0 达到 **80.4%**，均刷新了 SOTA 记录。
*   **效率提升**：推理延迟降低至 **0.105s**，吞吐量提升至 **76.2 Hz**，训练成本降低了约 60%。
*   **变化建模的优越性**：实验证明，预测“变化量”比预测“绝对未来状态”更能帮助模型理解动作的因果关系，尤其在长程任务中表现更稳健。

### 7. 优点
*   **创新的范式转换**：将 VLA 的预测目标从“状态”转向“变化”，更符合物理交互的本质。
*   **离散化接口**：通过 VQ-VAE 引入离散变化标记，解决了连续变化量难以训练的问题，提高了策略的稳定性。
*   **高效性**：通过 PWKE 压缩视觉输入，显著降低了计算开销，适合实时机器人控制。

### 8. 不足与局限
*   **依赖伪标签**：PWKE 的训练依赖于现有的视觉大模型（如 SAM, Depth-Anything）生成的伪标签，虽然论文测试了其对噪声的容忍度，但底层视觉模型的偏差仍可能传递。
*   **任务复杂度限制**：尽管在长程任务表现优异，但实验场景仍集中在桌面操纵，对于更复杂的全身协调或极高动态的环境（如接球）尚未验证。
*   **离线预训练**：LWVQ 的码本需要预先训练，对于完全未见过的物理变化类型（如流体、破碎）可能存在表示能力上限。

（完）
