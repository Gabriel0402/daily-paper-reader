---
title: "VCA: Vision-Click-Action Framework for Precise Manipulation of Segmented Objects in Target Ambiguous Environments"
title_zh: VCA：用于目标歧义环境中已分割物体精确操作的视觉-点击-动作框架
authors: "Donggeon Kim, Seungwon Jan, Hyeonjun Park, Daegyu Lim"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.23583v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于机器人操作的视觉-语言-动作 (VLA) 模型
tldr: 针对视觉语言动作（VLA）模型在处理相似物体时存在的语言歧义和认知负荷问题，本文提出了Vision-Click-Action (VCA) 框架。该框架利用预训练分割模型，将繁琐的文本指令替换为直接的点击式视觉交互。操作者通过在机器人2D视图中点击选择目标，显著降低了解析错误和认知负担。实验证明，VCA在目标模糊的环境中实现了精确的实例级机器人抓取与操控。
selection_source: fresh_fetch
motivation: 传统的视觉语言动作模型在面对多个相似物体时，容易因语言描述的模糊性导致识别错误和高认知负荷。
method: 提出VCA框架，通过预训练分割模型实现点击式视觉交互，允许用户直接在2D相机视图中指定目标物体。
result: 实验结果验证了VCA框架在指定目标物体的实例级操控中具有极高的有效性和精确度。
conclusion: VCA为现实世界中的机器人操控提供了一种比语言驱动接口更实用、可扩展且低误差的替代方案。
---

## 摘要
视觉-语言-动作（VLA）模型对语言的依赖引入了歧义、认知开销，以及在精确物体识别和顺序任务执行方面的困难，特别是在存在多个视觉相似物体的环境中。为了解决这些局限性，我们提出了 Vision-Click-Action (VCA)，这是一个利用预训练分割模型，将冗长的文本指令替换为基于点击的直接视觉交互的框架。通过允许操作员在机器人的 2D 摄像头视图中通过视觉选择清晰地指定目标物体，VCA 减少了理解误差，降低了认知负荷，并为现实世界的机器人操作提供了一种实用且可扩展的语言驱动界面替代方案。实验结果验证了所提出的 VCA 框架实现了对指定目标物体的有效实例级操作。实验视频可在 https://robrosinc.github.io/vca/ 获取。

## Abstract
The reliance on language in Vision-Language-Action (VLA) models introduces ambiguity, cognitive overhead, and difficulties in precise object identification and sequential task execution, particularly in environments with multiple visually similar objects. To address these limitations, we propose Vision-Click-Action (VCA), a framework that replaces verbose textual commands with direct, click-based visual interaction using pretrained segmentation models. By allowing operators to specify target objects clearly through visual selection in the robot's 2D camera view, VCA reduces interpretation errors, lowers cognitive load, and provides a practical and scalable alternative to language-driven interfaces for real-world robotic manipulation. Experimental results validate that the proposed VCA framework achieves effective instance-level manipulation of specified target objects. Experiment videos are available at https://robrosinc.github.io/vca/.

---

## 论文详细总结（自动生成）

这是一份关于论文《VCA: Vision-Click-Action Framework for Precise Manipulation of Segmented Objects in Target Ambiguous Environments》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型高度依赖自然语言指令。然而，在包含多个视觉相似物体的复杂环境中，语言描述往往具有**歧义性**（例如：难以快速描述一堆蓝色方块中的某一个），且会给操作者带来较高的**认知负荷**。
*   **核心问题**：如何提供一种比语言更直接、更精确的交互方式，以实现机器人在目标模糊环境中的精确实例级操作？
*   **整体含义**：提出 VCA 框架，通过“点击即选择”的视觉交互代替繁琐的文本指令，利用实时分割模型（SAM2）生成的掩码（Mask）来引导机器人动作。

### 2. 论文提出的方法论
*   **核心思想**：将实时交互式分割与模仿学习策略（ACT）相结合。用户通过点击图像选择目标，系统生成动态追踪的掩码，并将其作为策略网络的条件输入。
*   **关键技术细节**：
    *   **实时 SAM2 适配**：针对原始 SAM2 不支持中途动态交互的问题，VCA 修改了记忆库（Memory Bank）机制。当用户点击时，更新特定类别的记忆并重置时间上下文，支持在线添加、修改或删除目标掩码。
    *   **VCA 架构**：
        *   **图像编码**：使用 ResNet-34 提取多视角（头部和手腕相机）的 RGB 特征。
        *   **掩码编码**：使用修改后的 ResNet-18 将单通道二进制掩码编码为与图像特征对齐的特征图。
        *   **特征融合**：将图像特征、掩码特征、机器人本体感受状态和 CVAE 潜变量拼接，输入 Transformer 编码器。
        *   **动作预测**：基于 Action Chunking Transformer (ACT)，通过 Transformer 解码器预测一段固定长度的动作序列（Action Chunk），并使用时间集成（Temporal Ensembling）确保平滑性。

### 3. 实验设计
*   **实验场景**：
    1.  **方块分类任务（双臂）**：在 8 个颜色相似的方块中选择特定方块放入对应篮子。
    2.  **汉诺塔任务（单臂）**：在 7 个颜色完全相同、大小不同的圆环中选择特定目标套入杆中。
*   **Benchmark 与对比方法**：
    *   **Vanilla ACT**：原始的动作分块 Transformer（不含掩码输入）。
    *   **Text-guided ACT**：尝试了多种文本引导变体（如 Small BERT、Slot Attention），但因语义对齐困难，表现极不稳定，未纳入最终定量对比。
*   **泛化性测试**：引入了新颜色物体、新形状物体以及复杂的背景（格子布、碎花布）来测试模型的鲁棒性。

### 4. 资源与算力
*   **推理算力**：使用了一台配备 **NVIDIA RTX 4070Ti** 的专用服务器运行掩码生成服务，处理速度约为 **50ms/帧**。
*   **训练细节**：使用 AdamW 优化器，批大小（Batch Size）为 128，学习率为 $1 \times 10^{-4}$，训练步数高达 **200k steps**。
*   **数据量**：方块分类任务收集了 608 个演示视频，汉诺塔任务收集了 330 个演示视频。
*   **未明确说明**：文中未明确指出训练这些模型所使用的具体 GPU 型号及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：在基础任务和泛化性任务中，每种条件均进行了 **100 次真实世界测试**，总计测试次数较多。
*   **充分性评价**：实验设计较为充分，涵盖了从基础性能到极端视觉偏移（Distribution Shift）的评估。通过对比 Vanilla ACT，客观地展示了掩码引导在消除“目标选择错误”方面的优势。

### 6. 论文的主要结论与发现
*   **精确性**：VCA 在目标选择上达到了 **0 错误率**，而基准模型在相似物体干扰下常会出现选择错误。
*   **闭环控制**：点击生成的掩码可以有效地作为闭环控制的条件，支持动态任务中断和重置（Reset Prompt）。
*   **视觉鲁棒性**：在背景剧烈变化（如深色格子布）时，VCA 的成功率显著高于基准模型，说明实例级掩码有助于目标定位。
*   **行为观察**：掩码主要提供粗略的目标定位，而精细的抓取调整仍依赖于手腕相机的原始视觉特征。

### 7. 优点
*   **交互直观**：解决了 VLA 模型中“语言难以描述特定实例”的痛点，显著降低了人工操作的认知负担。
*   **实时性强**：通过对 SAM2 的巧妙适配，实现了支持动态交互的实时目标追踪。
*   **架构通用**：VCA 框架具有模块化特性，可以轻松扩展到多相机视角和不同的策略骨干网络。

### 8. 不足与局限
*   **交互成本**：虽然降低了语言描述负担，但仍需要人工点击介入，尚未实现完全自主的高层规划。
*   **泛化局限**：在极端几何形状改变（如圆环变方环）或极高对比度背景下，性能仍会出现大幅下降，说明掩码引导不能完全弥补底层视觉特征的分布偏移。
*   **依赖手腕相机**：实验发现精细操作高度依赖手腕视角，若手腕相机受损或遮挡，仅靠掩码特征可能无法完成任务。

（完）
