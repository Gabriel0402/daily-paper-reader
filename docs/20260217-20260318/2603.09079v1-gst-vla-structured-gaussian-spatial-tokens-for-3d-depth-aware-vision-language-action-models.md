---
title: "GST-VLA: Structured Gaussian Spatial Tokens for 3D Depth-Aware Vision-Language-Action Models"
title_zh: GST-VLA：用于 3D 深度感知视觉-语言-动作模型的结构化高斯空间标记
authors: "Md Selim Sarowar, Omer Tariq, Sungho Kim"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09079v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 具备3D深度感知的视觉语言动作模型
tldr: GST-VLA 针对现有 VLA 模型缺乏几何结构的问题，提出了高斯空间分词器（GST）和 3D 深度感知思维链（DA-CoT）。GST 将深度和语义特征转化为 128 个 3D 高斯基元，捕捉局部表面方向和几何置信度；DA-CoT 则通过监督中间空间推理步骤增强模型能力。结合流匹配动作专家，该模型在 LIBERO 和 SimplerEnv 基准测试中均取得了领先性能，显著提升了机器人操作的精确度。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型通常将视觉观测编码为缺乏内在几何结构的 2D 补丁，限制了其在需要精确 3D 感知的机器人操作任务中的表现。
method: 提出高斯空间分词器将特征转化为 3D 高斯基元，并利用深度感知思维链（DA-CoT）监督空间接地、接触几何等中间推理步骤。
result: "在 LIBERO 任务中达到 96.4% 的成功率，在 SimplerEnv 中达到 80.2%，相比现有模型分别提升了 2.0% 和 5.4%。"
conclusion: 通过引入结构化的 3D 几何表征和显式的空间推理链，GST-VLA 证明了深度感知对于提升 VLA 模型在精密任务中性能的关键作用。
---

## 摘要
VLA 模型将视觉观测编码为不具备内在几何结构的 2D patch 标记。我们提出了 GST-VLA，包含两项贡献。首先，高斯空间分词器（GST）将冻结的稠密深度和冻结的语义 patch 特征转换为 $N_g=128$ 个各向异性 3D 高斯原语（primitives），每个原语由度量残差均值 $\mu \in \mathbb{R}^3$、对数尺度协方差 $\log \sigma \in \mathbb{R}^3$ 以及学习到的不透明度 $\alpha \in (0,1)$ 参数化。协方差特征结构编码了局部表面方向，而不透明度提供了每个原语的几何置信度，这两者都是标量深度无法获取的。带有学习查询的空间注意力池化将固定的标记预算集中在几何显著区域，而非均匀分布。其次，3D 深度感知思维链（DA-CoT）推理监督了四个结构化的中间空间思维，包括 3D 物体定位、抓取可操作性接触几何、成对度量距离以及粗略的 SE(3) 航路点，并将其作为训练损失中的显式生成目标。在 DA-CoT 生成过程中，每个 VLM Transformer 块中的交叉注意力子层提供了对原始 256 个原语高斯场的直接访问。一个具有 3 亿参数、包含混合专家（MoE）前馈子层的流匹配动作专家，通过条件 ODE 积分，并以 VLM 隐藏状态和 DA-CoT 输出为双重交叉注意力条件，解码出 7 自由度（7-DoF）增量动作块。通过三个渐进阶段的复合损失 $\mathcal{L}_\mathrm{flow} + \mathcal{L}_\mathrm{CoT} + \mathcal{L}_\mathrm{depth}$ 训练，GST-VLA 在 LIBERO 上达到了 96.4%（+2.0%），在 SimplerEnv 上达到了 80.2%（+5.4%）。消融实验分离了每个 GST 组件、每个 DA-CoT 思维以及每个训练阶段的贡献，证实了在对精度要求较高的任务中存在独立且协同的增益。

## Abstract
VLA models encode visual observations as 2D patch tokens with no intrinsic geometric structure. We introduce GST-VLA with two contributions. First, the Gaussian Spatial Tokenizer (GST) converts frozen dense depth and frozen semantic patch features into $N_g{=}128$ anisotropic 3D Gaussian primitives, each parameterized by a metric residual mean $μ\in \mathbb{R}^3$, log-scale covariance $\log σ\in \mathbb{R}^3$, and learned opacity $α\in (0,1)$. The covariance eigenstructure encodes local surface orientation, and opacity provides per-primitive geometric confidence, both inaccessible from scalar depth. Spatial attention pooling with learned queries concentrates the fixed token budget on geometrically salient regions rather than distributing uniformly. Second, 3D Depth-Aware Chain-of-Thought (DA-CoT) reasoning supervises four structured intermediate spatial thoughts, covering 3D object grounding, grasp affordance contact geometry, pairwise metric distances, and coarse SE(3) waypoints, as explicit generation targets in the training loss. A cross-attention sublayer at every VLM transformer block provides direct access to the raw 256-primitive Gaussian field during DA-CoT generation. A 300M-parameter flow-matching action expert with mixture-of-experts feedforward sublayers decodes 7-DoF delta action chunks via conditional ODE integration, conditioned on both VLM hidden states and DA-CoT outputs through dual cross-attention. Trained with composite $\mathcal{L}_\mathrm{flow} + \mathcal{L}_\mathrm{CoT} + \mathcal{L}_\mathrm{depth}$ across three progressive stages, GST-VLA achieves 96.4% on LIBERO (+2.0%), and 80.2% on SimplerEnv (+5.4%). Ablations isolate the contribution of each GST component, each DA-CoT thought, and each training stage, confirming independent and synergistic gains concentrated on precision demanding tasks.

---

## 论文详细总结（自动生成）

### GST-VLA：用于 3D 深度感知视觉-语言-动作模型的结构化高斯空间标记

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型通常将视觉观测视为 2D 补丁（patch）标记，缺乏内在的几何结构。即使是引入了深度信息的模型（如 DepthVLA），也仅是将深度作为像素级的标量值处理，无法捕捉表面的方向（法线）或几何置信度，且缺乏在生成动作前的中间空间推理机制。这导致模型在处理需要毫米级精度的任务（如边缘抓取、精密插入）时表现不佳。GST-VLA 的提出旨在通过结构化的 3D 几何表征和显式的空间推理链，增强机器人操作的精确性和鲁棒性。

#### 2. 方法论：核心思想与关键技术
GST-VLA 集成了五个阶段的流水线，核心在于两个创新点：

*   **高斯空间分词器（GST）：**
    *   **核心思想：** 将 2D 语义特征和 3D 深度图转换为 $N_g=128$ 个各向异性的 3D 高斯原语（Primitives）。
    *   **技术细节：** 
        1.  **反向投影：** 利用深度图和相机内参将像素提升至 3D 空间。
        2.  **参数估计：** 使用 MLP 估计每个补丁的残差均值 $\mu$（精细位置）、对数尺度协方差 $\sigma$（编码表面方向）和不透明度 $\alpha$（编码几何置信度）。
        3.  **3D 傅里叶位置编码：** 允许模型通过位置代码计算度量距离。
        4.  **空间注意力池化：** 使用学习到的查询（Queries）将 256 个原始标记压缩为 128 个，使模型关注几何显著区域。
    *   **正则化：** 引入可微深度渲染损失，确保高斯场与实际深度一致。

*   **深度感知思维链（DA-CoT）：**
    *   **核心思想：** 在生成动作前，要求 VLM 显式生成四个结构化的空间思维：
        1.  **3D 物体接地（$c_1$）：** 目标物体的 3D 质心。
        2.  **抓取可操作性（$c_2$）：** 接触点几何及法线。
        3.  **度量空间关系（$c_3$）：** 物体间的相对距离。
        4.  **SE(3) 运动规划（$c_4$）：** 粗略的末端执行器航路点。
    *   **架构：** VLM 的每个 Transformer 块通过交叉注意力直接访问原始的高斯场。

*   **动作专家：** 一个 3 亿参数的流匹配（Flow-matching）Transformer，采用混合专家（MoE）结构，根据 VLM 状态和 DA-CoT 输出解码 7-DoF 动作。

#### 3. 实验设计
*   **数据集/场景：**
    *   **LIBERO：** 包含 130 个任务，涵盖空间、物体、目标和长程操作。
    *   **SimplerEnv：** 基于 BridgeData V2 的视觉匹配仿真环境。
    *   **数据高效操作实验：** 涵盖拾取放置、堆叠、抽屉、插入、薄物体抓取和杂乱场景。
*   **Benchmark 与对比方法：**
    *   对比了 **OpenVLA、CogACT、SpatialVLA、$\pi_0$** 等最先进的 VLA 模型。
    *   设置了消融变体：GST-VLA†（仅 GST，无 DA-CoT）和 GST-VLA‡（仅 DA-CoT，使用普通深度标记）。

#### 4. 资源与算力
*   **硬件：** 使用了 **8 台 NVIDIA A100-80GB GPU**。
*   **训练时长：** 
    *   第一阶段（预训练）：80,000 步。
    *   第二阶段（LoRA 适配）：40,000 步。
    *   第三阶段（全量微调）：20,000 步。
*   **推理速度：** 在单张 A100 上运行频率为 **6.2 Hz**。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **主实验：** 在三个主流基准测试上进行了全面对比。
*   **消融实验：** 针对 GST 的每个组件（位置编码、池化方式、不透明度等）、DA-CoT 的每个思维组件、训练阶段以及不同的 3D 表征形式（点云、各向同性高斯等）进行了详细测试（共计约 20+ 组消融对比）。
*   **充分性评价：** 实验设计非常充分，不仅验证了整体性能，还深入探讨了各组件间的协同效应（Synergy），并提供了推理误差的定量分析和可视化，具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先：** GST-VLA 在 LIBERO 上达到 96.4% 成功率，在 SimplerEnv 上达到 80.2%，显著优于现有模型。
*   **精度提升：** 在精密插入和薄物体抓取等对几何精度要求极高的任务中，提升最为显著（相比 DepthVLA 提升约 8-9%）。
*   **协同效应：** 结构化的高斯标记与显式的空间推理链结合，产生的增益大于两者单独提升的总和。
*   **鲁棒性：** 3D 度量坐标系下的高斯标记对视觉域偏移（如光照、背景变化）具有更好的稳定性。

#### 7. 优点
*   **创新的表征：** 首次将 3D 高斯原语引入 VLA 模型作为空间标记，有效编码了表面方向和置信度。
*   **可解释的推理：** DA-CoT 使模型的 3D 理解过程变得显式且可监督，提高了决策的透明度。
*   **训练策略：** 三阶段训练协议有效地解决了几何校准与语义推理之间的对齐问题。
*   **高效性：** 相比于处理全量深度图，GST 通过注意力池化实现了更高效的标记利用。

#### 8. 不足与局限
*   **深度依赖：** 模型性能高度依赖于冻结的单目深度估计器的质量。在反光表面（金属、玻璃）或严重遮挡的情况下，深度估计失效会导致模型崩溃。
*   **推理开销：** 引入 DA-CoT 生成步骤增加了约 38ms 的延迟，虽然 6.2Hz 满足基本需求，但相比 OpenVLA（9.4Hz）略慢。
*   **应用限制：** 目前主要在仿真环境中验证，真实世界的复杂光影和动态场景可能对高斯标记的稳定性提出更高挑战。

（完）
