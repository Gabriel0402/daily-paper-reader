---
title: Reachability-based Temporal Logic Verification for Reliable LLM-guided Human-Autonomy Teaming
title_zh: 基于可达性时序逻辑验证的可靠大语言模型引导人机协作
authors: "Joonwon Choi, Kartik Anand Pant, Karthik Nune, Inseok Hwang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08633v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于大模型引导的人机协作的信号时态逻辑 (STL)
tldr: 本研究针对大语言模型（LLM）引导的人机协作（HAT）中的可靠性问题，提出了一种基于可达性分析的信号时序逻辑（STL）验证框架。该框架利用LLM作为自然语言与STL之间的翻译器，并引入STL可行性过滤器（SFF）对生成的逻辑进行验证。通过将复杂公式分解为子公式并进行并行可达性分析，系统能识别不可行指令并向用户提供详细反馈，从而确保任务规划的安全性和可靠性。
selection_source: fresh_fetch
motivation: 旨在解决LLM在将人类自然语言指令转化为自主系统动作时，可能产生不可行或不安全指令的可靠性挑战。
method: 提出一种STL可行性过滤器，通过分解复杂逻辑公式并利用可达性分析验证各子公式在动力系统中的可行性。
result: 实验证明该框架能有效识别并过滤不可行的子公式，并根据验证结果生成有意义的反馈以引导用户修改指令。
conclusion: 该框架通过提供安全且信息丰富的双向沟通机制，显著提升了LLM引导下人机协作系统的可靠性与安全性。
---

## 摘要
我们提出了一种基于可达性的框架，利用信号时序逻辑（STL）实现可靠的大语言模型（LLM）引导的人机协作（HAT）。在该框架中，LLM 被用作翻译器，将人类操作员给出的自然语言指令转换为相应的 STL 规范，反之亦然。我们提出了一种 STL 可行性过滤器（SFF）来检查生成的 STL 的可行性。SFF 首先将复杂且嵌套的 LLM 翻译结果分解为一组更简单的子公式，以便进行并行化处理并生成信息丰富的反馈。随后应用可达性分析方法来验证每个子公式对于目标动力系统是否可行：如果可行，则执行任务规划；否则，予以拒绝。所提出的 SFF 能够识别不可行的子公式，而不仅仅是为整个 STL 提供布尔验证结果，从而促进 LLM 生成反馈，请求人类修改指令。因此，该框架通过在人类操作员和自主智能体之间实现安全且信息丰富的通信，从而实现更可靠的人机协作。我们的实验表明，所提出的框架能够成功过滤掉不可行的子公式，并基于此类信息生成信息丰富的反馈。

## Abstract
We propose a reachability-based framework for reliable LLM-guided human-autonomy teaming (HAT) using signal temporal logic (STL). In the proposed framework, LLM is leveraged as a translator that transfers natural language commands given by a human operator into corresponding STL specifications or vice versa. An STL feasibility filter (SFF) is proposed to check the feasibility of the generated STL. The SFF first decomposes the complex and nested LLM translation into a set of simpler subformulas for parallelization and informative feedback generation. The reachability analysis method is then applied to verify if each subformula is feasible for a target dynamical system: if feasible, perform mission planning, otherwise, reject it. The proposed SFF can identify infeasible subformulas, more than simply providing the boolean verification results for the whole STL, thereby facilitating the feedback generation of LLM to request modification of the command to the human. Consequently, the proposed framework can allow more reliable HAT by enabling safe and informative communication between the human operator and the autonomous agent. Our experiments demonstrate that the proposed framework can successfully filter out infeasible subformulas and generate informative feedback based on such information.

---

## 论文详细总结（自动生成）

### 论文总结：基于可达性时序逻辑验证的可靠大语言模型引导人机协作

#### 1. 核心问题与整体含义
本文旨在解决**大语言模型（LLM）在人机协作（HAT）场景中缺乏安全性和可靠性保证**的问题。虽然 LLM 在理解人类自然语言指令并将其转化为机器人动作方面表现出色，但在处理安全关键型任务时，LLM 可能会生成物理上不可行或违反安全约束的指令。目前大多数方法属于“事后验证”，而本研究提出了一种**“事前验证”框架**，在任务规划和执行前，通过形式化方法确保指令的可行性，并能向人类提供具体的反馈。

#### 2. 方法论
该框架主要由三个核心组件构成：
*   **LLM 翻译器**：将人类的自然语言指令翻译为**信号时序逻辑（STL）**规范。
*   **STL 可行性过滤器（SFF）**：这是核心创新点，包含两个步骤：
    *   **STL 分解**：利用重写规则将复杂的嵌套 STL 公式分解为多个简单的子公式（分为 Level 0/1/2 三种分解深度），以便于并行计算和精确定位不可行部分。
    *   **可达性分析**：利用 **Hamilton-Jacobi (HJ) 可达性分析**计算后向可达管（BRT）。通过检查系统的初始状态是否在 BRT 内，来形式化验证目标动力系统是否能满足该 STL 子公式。
*   **闭环反馈与规划**：
    *   若验证通过，使用**混合整数线性规划（MILP）**生成最优轨迹。
    *   若验证失败，SFF 识别出具体的不可行子公式，并将其传回 LLM，由 LLM 生成解释性反馈，告知人类操作员为何指令不可行（例如：时间不足或区域冲突）。

#### 3. 实验设计
研究设计了三个主要实验场景，并使用了多种 LLM（ChatGPT-4o, Claude 3.7 Sonnet, Llama 4）进行对比：
*   **场景 1（逻辑可行性检查）**：模拟校车驾驶任务，设置了特定的时间窗口和禁行区。对比了 LLM 的“自我可行性判断”与 SFF 的形式化验证结果。
*   **场景 2（反馈生成与实机验证）**：在复杂的障碍物环境中，验证系统识别不可行指令并生成自然语言反馈的能力，并使用 **Crazyflie 纳米无人机**进行实机飞行实验。
*   **场景 3（分解效率对比）**：对比了原始 STL 公式与不同分解级别（Level 0/1/2）在 MILP 规划中的计算耗时。

#### 4. 资源与算力
*   **硬件平台**：实验在一台配备 **Intel i7-13620H 处理器**的笔记本电脑上运行。
*   **软件工具**：使用 `stlpy` 和 `Gurobi` 求解器处理 MILP，使用 `hj-reachability` 库计算可达性。
*   **算力说明**：文中未提及使用高性能 GPU 进行训练，主要计算压力在于 HJ 可达性分析的数值解法和 MILP 的优化求解。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从纯仿真逻辑验证到实机无人机飞行的全流程。在计算时间评估中，进行了 **100 次蒙特卡洛模拟**以获取平均值和方差。
*   **充分性评价**：实验设计较为全面，不仅验证了算法的正确性（是否能发现错误），还验证了实用性（计算效率提升）和交互性（反馈质量）。对比了主流的三个顶级 LLM，增强了结论的客观性。

#### 6. 主要结论与发现
*   **LLM 的局限性**：实验证明，即使是 GPT-4o 等顶级模型，在面对复杂时空约束时也会产生错误的“可行性”直觉，而 SFF 能准确拦截这些错误。
*   **分解的优势**：STL 分解不仅能帮助定位错误，还能显著提升规划效率。实验显示，分解后的 STL 使 MILP 的计算时间**缩短了约 50%**。
*   **反馈的价值**：通过定位具体的不可行子公式，LLM 生成的反馈比简单的“不可行”布尔值更有助于人类修正指令。

#### 7. 优点
*   **形式化保证**：将 LLM 的灵活性与可达性分析的严谨性结合，为 LLM 驱动的机器人提供了坚实的安全底座。
*   **高效性**：通过 STL 分解实现了并行化验证，并缓解了 MILP 求解的计算负担。
*   **可解释性**：系统能够“告诉”用户哪里错了，而不仅仅是拒绝执行，提升了人机协作的透明度。

#### 8. 不足与局限
*   **保守性风险**：Level 1 和 Level 2 的分解是**保守的**（充分非必要条件），这意味着某些物理上可行的复杂指令可能会因为分解后的严苛约束而被 SFF 误拒。
*   **子公式关联性**：目前的框架独立验证每个子公式，可能无法完全捕捉子公式之间复杂的耦合冲突（作者提到未来将引入多目标到达-规避问题来解决）。
*   **计算扩展性**：HJ 可达性分析在处理高维状态空间时存在“维度灾难”，这限制了该框架在极其复杂的动力系统中的应用。

（完）
