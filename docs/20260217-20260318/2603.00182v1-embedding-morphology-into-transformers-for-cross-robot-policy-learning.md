---
title: Embedding Morphology into Transformers for Cross-Robot Policy Learning
title_zh: 将形态学嵌入 Transformer 以实现跨机器人策略学习
authors: "Kei Suzuki, Jing Liu, Ye Wang, Chiori Hori, Matthew Brand, Diego Romeres, Toshiaki Koike-Akino"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2603.00182v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于跨机器人策略学习的视觉-语言-动作 (VLA) 模型
tldr: 针对跨机器人策略学习中模型难以感知机器人形态的问题，本文提出了一种形态感知的Transformer策略。通过引入运动学Token、拓扑感知注意力偏置和关节属性调节三大机制，将机器人的运动学结构和语义信息显式注入模型。实验表明，该方法在多种机器人形态上均优于传统的VLA基准模型，显著提升了单体及跨实体的策略鲁棒性和性能。
selection_source: fresh_fetch
motivation: 现有的Transformer策略通常对机器人形态不敏感，仅依赖观察推断运动结构，限制了跨机器人学习的性能和鲁棒性。
method: 提出了包含运动学Token化、拓扑感知注意力偏置和关节属性调节的形态注入机制，将机器人结构作为归纳偏置引入模型。
result: 在多种机器人形态上的实验结果显示，该方法在性能和鲁棒性上均一致优于pi0.5 VLA等基准模型。
conclusion: 将机器人形态结构显式嵌入Transformer架构能有效提升跨机器人策略学习的效果，是实现通用机器人智能的关键。
---

## 摘要
跨机器人策略学习——即训练单一策略使其在多种具身（embodiments）上表现良好——仍然是机器人学习中的一个核心挑战。基于 Transformer 的策略，例如视觉-语言-动作（VLA）模型，通常是具身无关的，必须纯粹从观测中推断运动学结构，这可能会降低跨具身的鲁棒性，甚至限制单一具身内的性能。我们提出了一种具身感知的 Transformer 策略，通过三种机制注入形态学信息：(1) 运动学标记（kinematic tokens），通过跨关节分解动作并利用逐关节时间分块（temporal chunking）来压缩时间；(2) 拓扑感知注意力偏置，将运动学拓扑编码为自注意力中的归纳偏置，鼓励沿运动学边缘进行消息传递；(3) 关节属性条件化，通过逐关节描述符增强拓扑结构，以捕捉连接性之外的语义。在多种具身上，这种结构化集成相比于原始的 pi0.5 VLA 基线一致地提升了性能，表明在单一具身内和跨具身场景下的鲁棒性均得到了增强。

## Abstract
Cross-robot policy learning -- training a single policy to perform well across multiple embodiments -- remains a central challenge in robot learning. Transformer-based policies, such as vision-language-action (VLA) models, are typically embodiment-agnostic and must infer kinematic structure purely from observations, which can reduce robustness across embodiments and even limit performance within a single embodiment. We propose an embodiment-aware transformer policy that injects morphology via three mechanisms: (1) kinematic tokens that factorize actions across joints and compress time through per-joint temporal chunking; (2) a topology-aware attention bias that encodes kinematic topology as an inductive bias in self-attention, encouraging message passing along kinematic edges; and (3) joint-attribute conditioning that augments topology with per-joint descriptors to capture semantics beyond connectivity. Across a range of embodiments, this structured integration consistently improves performance over a vanilla pi0.5 VLA baseline, indicating improved robustness both within an embodiment and across embodiments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Embedding Morphology into Transformers for Cross-Robot Policy Learning》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的视觉-语言-动作（VLA）模型（如 $\pi_{0.5}$）通常是“具身无关”的，模型必须仅从观测数据中隐式学习机器人的运动学结构。这导致在面对硬件变化、设计升级或跨平台部署时，模型的鲁棒性较差，且难以在不同形态的机器人之间共享知识。
*   **核心问题**：如何将机器人的物理形态（形态学拓扑和关节语义）作为显式的**归纳偏置**注入到 Transformer 架构中，以提升跨机器人策略学习的性能和泛化能力。

### 2. 方法论
论文提出了一种具身感知的 Transformer 策略，核心包含三大机制：
*   **运动学标记（Kinematic Tokens, KT）**：
    *   **空间分解**：将原本耦合在一起的动作序列按关节（Joint）拆分，每个关节拥有独立的 Token。
    *   **时间分块**：引入参数 $G$ 对动作序列进行时间压缩，将每个关节在一段时间内的动作聚合为一个向量，从而在保留空间结构的同时降低计算复杂度。
*   **拓扑感知注意力偏置（Topology-aware Attention Bias）**：
    *   将机器人的运动学图（Kinematic Graph）引入自注意力机制。
    *   **Mix-Mask 策略**：交替使用“硬掩码层”（仅允许有物理连接的关节互相通信）和“全连接层”（允许全局协调），平衡了局部消息传递与全局同步。
    *   **Soft-Mask 变体**：尝试基于最短路径距离（SPD）学习注意力偏置表。
*   **关节属性条件化（Joint-attribute Conditioning）**：
    *   利用 **FiLM（特征线性调制）** 机制，将关节的物理描述符（如关节类型、旋转轴、运动限位、摩擦系数等）注入到运动学 Token 的嵌入中，使模型能区分功能相似但参数不同的关节。

### 3. 实验设计
*   **数据集与场景**：
    *   **DROID**：使用 Franka Panda 机械臂的子集，评估语言条件下的多种抓取任务。
    *   **Unitree G1 Dex1**：评估 16 自由度的人形机器人手部操作任务。
    *   **SO101**：用于多具身混合训练的小型机械臂。
*   **基准（Benchmark）**：以最先进的 **$\pi_{0.5}$ VLA** 模型作为核心基线。
*   **对比实验**：
    *   单具身性能对比（Vanilla vs. KT vs. Mask vs. FiLM）。
    *   多具身混合训练（Panda + SO101）。
    *   消融实验：时间分块大小 $G$、辅助 Token（AKT）数量、偏置初始化策略等。

### 4. 资源与算力
*   **模型规模**：实验涉及两种微调模式：**AP-FT**（仅动作策略微调，约 4.5 亿参数）和 **Full-FT**（全量微调，约 35 亿参数）。
*   **训练细节**：训练步数在 60k 至 125k 步之间，Batch Size 为 8 或 32。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100/H100）或具体显存消耗，但考虑到其基于 $\pi_{0.5}$（PaliGemma 架构），通常需要高性能计算集群支持。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三种不同自由度和结构的机器人形态，每种配置均进行了 300 次随机种子的 Rollout 测试，并提供了 95% 置信区间。
*   **充分性**：实验设计非常详尽，不仅验证了整体性能提升，还通过大量的消融实验（如表 5、6、7）深入探讨了每个组件（如分块大小、Token 容量、初始化方式）对结果的影响。
*   **客观性**：对比了 Hard-Mask 和 Soft-Mask 的多种变体，并诚实地指出了 Soft-Mask 在当前架构下优化不稳定的问题，表现出较高的学术客观性。

### 6. 主要结论与发现
*   **形态注入有效性**：显式嵌入机器人形态显著提升了成功率。在 DROID 任务中，完整模型（KT+Mix-Mask+FiLM）的平均成功率从 19.7% 提升至 **47.4%**。
*   **结构化偏置的价值**：即使在单机器人训练中，引入运动学拓扑也能帮助模型更快、更稳地学习跨关节协调。
*   **语义的重要性**：仅有拓扑连接是不够的，关节的物理属性（FiLM 注入）对于消除运动歧义至关重要。
*   **多具身协同**：在混合训练中，形态感知模型能有效缓解不同机器人间的干扰，实现更优的 Macro 成功率。

### 7. 优点
*   **架构创新**：成功将传统的图神经网络（GNN）思想与现代大参数量 VLA 模型的 Token 接口相结合。
*   **Mix-Mask 策略**：提出了一种简单且高效的方法来解决 Transformer 在处理图结构时的局部/全局权衡难题。
*   **通用性**：方法不依赖于特定的机器人，具有很强的形态扩展性。

### 8. 不足与局限
*   **Soft-Mask 优化难题**：尽管 Soft-Mask 理论上更灵活，但实验显示其性能不如 Hard-Mask，且对初始化极其敏感，这限制了其在更复杂场景下的应用。
*   **缺乏真机验证**：所有实验均在仿真环境（IsaacLab, LeIsaac）中完成，尚未在真实物理世界中验证形态注入对 Sim-to-Real 差距的影响。
*   **零样本迁移未深入**：虽然验证了混合训练，但未重点展示模型对完全未见过的（Unseen）机器人形态的零样本泛化能力。

（完）
