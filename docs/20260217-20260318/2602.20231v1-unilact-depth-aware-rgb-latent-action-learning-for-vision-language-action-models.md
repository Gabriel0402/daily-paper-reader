---
title: "UniLACT: Depth-Aware RGB Latent Action Learning for Vision-Language-Action Models"
title_zh: UniLACT：面向视觉-语言-动作模型的深度感知 RGB 潜动作学习
authors: "Manish Kumar Govind, Dominick Reilly, Pu Wang, Srijan Das"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20231v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: VLA模型的深度感知潜动作学习
tldr: UniLACT 针对现有视觉语言动作（VLA）模型在预训练中仅依赖 RGB 视频导致缺乏 3D 几何结构的问题，提出了深度感知的潜在动作学习框架。通过 UniLARN 统一 RGB 和深度信息的嵌入空间，并利用逆向和前向动力学目标进行预训练。该方法使模型能够继承更强的空间先验，在仿真和真实环境的多种操作任务中显著优于仅基于 RGB 的基准模型。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型预训练主要依赖 RGB 观测，缺乏对精确操作至关重要的 3D 几何结构和空间先验。
method: 提出 UniLACT 模型及 UniLARN 框架，通过逆向和前向动力学目标学习 RGB 与深度的统一潜在动作表示，并将其作为伪标签进行深度感知预训练。
result: 实验证明 UniLACT 在域内和跨域预训练、以及已知和未知操作任务中均优于仅使用 RGB 的潜在动作基准模型。
conclusion: 引入深度感知的潜在动作表示能有效增强 VLA 模型的空间理解能力，提升机器人在复杂操作任务中的性能。
---

## 摘要
从无标注视频中学习的潜动作表示最近已成为一种极具前景的范式，用于在没有显式机器人动作监督的情况下预训练视觉-语言-动作（VLA）模型。然而，仅从 RGB 观测中导出的潜动作主要编码了外观驱动的动力学，缺乏显式的 3D 几何结构，而这对于精确且涉及丰富接触的操作至关重要。为了解决这一局限性，我们引入了 UniLACT，这是一种基于 Transformer 的 VLA 模型，它通过深度感知的潜预训练融入了几何结构，使下游策略能够继承更强的空间先验。为了促进这一过程，我们提出了 UniLARN，这是一个基于逆向和前向动力学目标的统一潜动作学习框架，它在学习 RGB 和深度的共享嵌入空间的同时，显式地建模了它们的跨模态交互。这种表述产生了模态特定且统一的潜动作表示，作为 UniLACT 深度感知预训练的伪标签。在仿真和真实世界设置中的广泛实验证明了深度感知统一潜动作表示的有效性。UniLACT 在域内和域外预训练方案下，以及在已见和未见的操作任务上，均一致优于基于 RGB 的潜动作基线。

## Abstract
Latent action representations learned from unlabeled videos have recently emerged as a promising paradigm for pretraining vision-language-action (VLA) models without explicit robot action supervision. However, latent actions derived solely from RGB observations primarily encode appearance-driven dynamics and lack explicit 3D geometric structure, which is essential for precise and contact-rich manipulation. To address this limitation, we introduce UniLACT, a transformer-based VLA model that incorporates geometric structure through depth-aware latent pretraining, enabling downstream policies to inherit stronger spatial priors. To facilitate this process, we propose UniLARN, a unified latent action learning framework based on inverse and forward dynamics objectives that learns a shared embedding space for RGB and depth while explicitly modeling their cross-modal interactions. This formulation produces modality-specific and unified latent action representations that serve as pseudo-labels for the depth-aware pretraining of UniLACT. Extensive experiments in both simulation and real-world settings demonstrate the effectiveness of depth-aware unified latent action representations. UniLACT consistently outperforms RGB-based latent action baselines under in-domain and out-of-domain pretraining regimes, as well as on both seen and unseen manipulation tasks.

---

## 论文详细总结（自动生成）

### UniLACT: 深度感知 RGB 潜动作学习的视觉-语言-动作模型总结

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型预训练通常依赖大规模无标注的 RGB 视频，通过学习“潜动作”（Latent Actions）来减少对昂贵的人工遥操作数据的依赖。然而，**仅基于 RGB 的潜动作表示主要捕捉外观动态，缺乏显式的 3D 几何结构信息**。这导致机器人在执行需要精确感知空间距离、接触关系或避障的任务（如精准抓取、放置）时表现欠佳。本论文旨在研究如何将深度信息（Depth）融入潜动作预训练中，使模型在推理阶段仅使用 RGB 输入时，也能继承强大的空间几何先验。

#### 2. 方法论：核心思想与关键技术
论文提出了 **UniLACT** 模型及其配套的潜动作学习框架 **UniLARN**。其核心流程分为三个阶段：

*   **阶段 1：UniLARN（统一潜动作学习）**
    *   **核心思想**：利用逆向动力学模型（IDM）和前向动力学模型（FDM）在共享的离散潜空间中同时学习 RGB 和深度的特征。
    *   **技术细节**：
        1.  **IDM**：将当前帧和未来帧（RGB-D 对）映射到连续嵌入空间。
        2.  **共享 Codebook**：使用向量量化（VQ）将 RGB 和深度的嵌入映射到同一个共享的离散代码本中。
        3.  **统一表示**：将模态特定的潜变量融合并再次量化，生成“统一潜动作”（Unified Latent Action）。
        4.  **FDM**：利用统一潜动作重建未来的 RGB 和深度观测，确保潜空间保留了跨模态的动力学信息。
*   **阶段 2：统一潜动作预训练**
    *   使用 UniLARN 生成的潜动作作为伪标签，训练一个自回归 Transformer（UniLACT）。模型根据 RGB 图像和文本指令预测潜动作标记。
*   **阶段 3：动作微调**
    *   在少量有标注的机器人轨迹上微调，将预测的潜动作映射为真实的 7 自由度控制命令（位置、旋转、夹持器）。
*   **推理特性**：深度信息仅在训练阶段使用，**推理时模型仅需 RGB 图像和文本指令**。

#### 3. 实验设计
*   **仿真环境**：使用 **CALVIN** 基准测试，采用挑战性的 `ABC -> D` 设置（在 A、B、C 环境训练，在未见的 D 环境测试泛化性）。
*   **真实世界**：使用 7 自由度 **xArm7** 机械臂，设置了 4 个任务（包括“捡起胡萝卜放入碗中”、“移动物体”等），涵盖已见任务和零样本泛化任务。
*   **Benchmark 与对比方法**：
    *   主要对比基准是 **Moto**（目前最先进的基于 RGB 潜动作的 VLA 模型）。
    *   同时对比了 RT-1、Robo-Flamingo、GR-1、HULC 等主流 VLA 或模仿学习模型。
*   **数据集**：除了 CALVIN 仿真数据，还使用了 **Open X-Embodiment (OXE)** 跨物种数据集进行域外预训练。

#### 4. 资源与算力
*   **硬件**：实验在 **4 台 NVIDIA H200 GPU** 上运行。
*   **软件/模型**：UniLACT 基于 GPT-2 骨干网络，视觉编码器使用 ViT-L，文本编码器使用 T5。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到了在微调阶段保持了与基准模型相同的迭代次数以确保公平。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验：在 CALVIN 上进行了 1000 个回合的评估，涵盖 5 个连续任务序列。
    *   真实世界：每个任务进行 10 次试验，随机化物体初始位置。
    *   消融实验：针对视觉模态（仅 RGB、仅深度、简单拼接 vs. 统一空间）、输出模态类型、单任务 vs. 多任务目标进行了详细对比。
*   **充分性与客观性**：实验设计较为严谨，通过“域外预训练”验证了模型的扩展性，并通过任务级分析（图 2）客观展示了深度信息在“几何中心任务”上的显著优势，而在“外观驱动任务”上与 RGB 基准持平。

#### 6. 主要结论与发现
*   **性能提升**：在 CALVIN 仿真中，UniLACT 比纯 RGB 基准（Moto）在平均完成序列长度上提升了 **29.2%**。
*   **空间理解**：在真实世界中，UniLACT 的成功率比 Moto 高出 **10%**，尤其在避障和精准放置方面表现更稳健。
*   **深度先验的价值**：实验证明，即使推理时不输入深度图，预训练阶段引入的深度感知潜动作也能显著增强模型对 3D 结构的推理能力。
*   **跨模态对齐**：在共享潜空间中联合学习比独立学习两种模态效果更好。

#### 7. 优点（亮点）
*   **推理高效性**：通过“训练时使用深度，推理时丢弃深度”的设计，既获得了 3D 空间能力，又没有增加部署时的传感器负担或计算延迟。
*   **统一潜空间**：UniLARN 框架通过 IDM-FDM 目标有效地解决了 RGB 和深度信息的跨模态对齐问题。
*   **泛化能力**：在未见过的环境（CALVIN D）和未见过的真实任务中均表现出良好的零样本迁移能力。

#### 8. 不足与局限
*   **深度图质量依赖**：在处理缺乏深度信息的 OXE 数据集时，依赖预训练的深度估计模型（Depth-Anything-V2）生成伪深度，这可能引入估计误差。
*   **真实世界样本量**：真实世界实验每项任务仅 10 次测试，样本量相对较小，可能存在一定的偶然性。
*   **动态场景限制**：目前的潜动作学习主要基于静态背景下的物体操作，对于高速动态环境或复杂光影变化的鲁棒性尚待验证。

（完）
