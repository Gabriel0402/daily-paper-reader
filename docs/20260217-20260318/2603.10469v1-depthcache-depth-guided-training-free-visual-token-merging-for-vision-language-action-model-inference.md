---
title: "DepthCache: Depth-Guided Training-Free Visual Token Merging for Vision-Language-Action Model Inference"
title_zh: DepthCache：用于视觉-语言-动作模型推理的深度引导免训练视觉 Token 合并
authors: "Yuquan Li, Lianjie Ma, Han Ding, Lijun Zhu"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10469v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 机器人VLA模型推理中的视觉Token合并
tldr: DepthCache是一个无需训练的视觉Token合并框架，旨在加速视觉-语言-动作（VLA）模型的推理。它利用深度信息作为结构先验，对近景工作区和远景背景应用差异化的合并比例，并结合时间冗余和运动自适应策略优化计算。该方法在保持机器人操作成功率的同时，显著提升了推理速度，且无需修改模型即可适配多种VLA架构。
selection_source: fresh_fetch
motivation: 现有的视觉Token压缩方法因均匀处理Token而破坏了机器人控制所需的空间推理能力，导致推理延迟高且性能下降。
method: 提出一种基于深度的空间差异化合并策略，结合跨帧的时间分布合并和基于末端执行器动态的运动自适应压缩。
result: "在LIBERO基准测试中实现高达1.28倍的推理加速，且在多种VLA模型上的成功率下降不到1%，显著优于现有的剪枝和合并基准。"
conclusion: DepthCache通过深度引导的Token管理，在不修改模型的前提下有效平衡了VLA模型的推理效率与操作精度，提升了闭环控制的响应速度。
---

## 摘要
视觉-语言-动作（VLA）模型实现了通用的机器人操控，但面临高推理延迟的问题。这一瓶颈源于大型语言骨干网络处理的海量视觉 Token。现有方法通常采用均匀的 Token 剪枝或合并，这会损害机器人控制中至关重要的空间推理能力。我们提出了 DepthCache，这是一个利用深度信息作为结构先验进行视觉 Token 压缩的免训练框架。它根据深度将观测结果划分为不同区域，并应用空间差异化的合并比例，在压缩远景背景的同时保留近场工作空间。为了利用时间冗余，DepthCache 将合并过程分散到连续帧中，在减少每步计算量的同时确保表示的一致性。此外，运动自适应流水线根据末端执行器的动力学进一步优化了辅助视角的压缩。该框架无需修改模型，可推广至多种 VLA 架构。在 LIBERO 基准测试中，DepthCache 在三种 VLA 模型（pi_0.5、OpenVLA、GR00T）上实现了高达 1.28 倍的推理加速，且平均成功率下降不到 1%，而剪枝和合并基准方法在同等压缩率下会导致 4-24% 的性能下降。在物理机械臂上的真实世界实验表明，DepthCache 在延迟敏感场景中实现了更快的任务吞吐量和更灵敏的闭环控制。

## Abstract
Vision-Language-Action (VLA) models enable generalist robotic manipulation but suffer from high inference latency. This bottleneck stems from the massive number of visual tokens processed by large language backbones. Existing methods either prune or merge tokens uniformly, degrading the spatial reasoning essential for robotic control. We present DepthCache, a training-free framework that leverages depth as a structural prior for visual token compression. It partitions observations into depth-based regions and applies spatially differentiated merge ratios, preserving the near-field workspace while compressing the distant background. To exploit temporal redundancy, DepthCache distributes the merging process across consecutive frames, ensuring consistent representations while reducing per-step computation. A motion-adaptive pipeline further optimizes auxiliary view compression based on end-effector dynamics. The framework requires no model modification, generalizing across diverse VLA architectures. On the LIBERO benchmark, DepthCache achieves up to 1.28x inference speedup with less than 1% average success rate degradation across three VLA models (pi_0.5, OpenVLA, GR00T), whereas pruning and merging baselines incur 4--24% degradation at comparable compression. Real-world experiments on a physical manipulator demonstrate that DepthCache enables faster task throughput and more responsive closed-loop control in latency-sensitive scenarios.

---

## 论文详细总结（自动生成）

### DepthCache：用于视觉-语言-动作模型推理的深度引导免训练视觉 Token 合并

#### 1. 核心问题与研究动机
*   **核心问题**：视觉-语言-动作（VLA）模型在机器人操控中表现优异，但由于大型语言模型（LLM）骨干网络需要处理海量的视觉 Token（通常每张图数百个），导致推理延迟极高，难以满足实时闭环控制的需求。
*   **研究动机**：
    *   **剪枝（Pruning）的局限性**：直接丢弃 Token 会破坏空间连续性，导致机器人对细微空间关系的感知能力下降。
    *   **现有合并（Merging）的不足**：现有的 Token 合并方法通常在视觉编码器内部进行，且对全图采用统一的合并比例，忽略了机器人操作中“近景工作区需高精度、远景背景可高压缩”的特性。
    *   **生物启发**：人类视觉在注视目标时，中央凹区域保持高分辨率，而边缘区域分辨率较低但保留空间上下文。

#### 2. 方法论
DepthCache 是一个**免训练、模型无关**的框架，其核心思想是将深度信息作为结构先验，引导空间差异化和时间连续的 Token 压缩。
*   **主视角流水线（Primary View Pipeline）**：
    1.  **双重保护机制**：在初始化阶段，结合 LLM 的**跨模态注意力（语义保护）**和深度图的**空间梯度（几何边缘保护）**，识别并锁定任务关键 Token 不参与压缩。
    2.  **基于深度的区域划分**：利用 K-Means 对非保护 Token 按深度聚类，远景区域分配高合并率，近景区域分配低合并率。
    3.  **渐进式 Token 合并（Progressive Merging）**：为了避免单帧大幅压缩导致的动作不连续，将合并过程分散到 $W$ 个连续帧中逐步完成，确保特征平滑过渡。
    4.  **动态重置**：通过监测深度变化检测场景变动（如物体被移动），触发重新初始化。
*   **辅助视角（腕部相机）流水线**：
    *   采用**运动自适应状态机**。在机械臂快速移动（Transit）时进行高强度压缩；在精细操作（Fine Manipulation）阶段恢复全分辨率。
*   **算法实现**：该框架运行在视觉编码器之外，通过双向软匹配（Bipartite Soft Matching）实现 Token 聚合，无需修改模型权重。

#### 3. 实验设计
*   **数据集与基准**：
    *   **仿真环境**：使用 **LIBERO** 基准测试，涵盖 Spatial、Object、Goal、Long 四个子集，共 40 个任务。
    *   **真实世界**：使用 PIPER 6 自由度机械臂，执行抓取放置、叠方块、开抽屉等任务。
*   **对比方法**：
    *   **剪枝类**：FastV、SP-VLA（动作感知剪枝）。
    *   **合并类**：ToSA（空间感知合并）、ToMe（通用 Token 合并）。
*   **测试模型**：在三种不同架构的 VLA 模型上验证：**$\pi_{0.5}$** (3.3B)、**OpenVLA** (7B) 和 **GR00T** (2.2B)。

#### 4. 资源与算力
*   **硬件环境**：所有实验（仿真与实物）均在单张 **NVIDIA RTX 4090 (24GB)** GPU 上运行。
*   **训练时长**：由于该方法是 **Training-free（免训练）** 的，因此无需训练算力投入，直接在推理阶段插入。
*   **推理延迟**：文中详细记录了每步推理的毫秒级延迟，用于计算加速比。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验涵盖 40 个任务，每个任务评估 100 个 Episode，数据量充足。
    *   消融实验针对深度划分、双重保护、渐进合并、重初始化等核心组件进行了逐一验证。
    *   参数敏感性分析探讨了最大合并率 $r_{max}$ 和渐进步长 $\eta$ 的影响。
*   **客观性与公平性**：在三种主流 VLA 架构上进行交叉验证，并与最新的剪枝和合并基准在相同压缩率（Token 留存率 $\rho$）下对比，实验设计较为严谨、客观。

#### 6. 主要结论与发现
*   **性能保持**：在 LIBERO 基准上，DepthCache 实现了 **1.07x 至 1.28x** 的推理加速，而平均成功率下降**不到 1%**。
*   **优于基准**：相比之下，传统的剪枝和合并方法在同等加速下会导致 4% 到 24% 的成功率大幅下降。
*   **实物表现**：在真实世界中实现了 **1.33x** 的加速，显著提升了多物体排序任务的吞吐量（耗时减少 22.7%），并增强了机器人在受到扰动时的恢复响应速度。

#### 7. 优点（亮点）
*   **免训练与通用性**：无需微调模型，可直接适配任何接受视觉 Token 输入的 VLA 模型。
*   **深度引导的合理性**：首次将深度图从“感知输入”转变为“结构先验”，符合机器人操作的物理特性。
*   **时间稳定性**：通过渐进式合并解决了 Token 压缩中常见的跨帧特征跳变问题，使机器人动作更平滑。

#### 8. 不足与局限
*   **加速上限受限**：作为推理侧优化，它仅压缩了视觉 Token，但无法加速 LLM 的动作解码阶段（如扩散模型的去噪过程），受阿姆达尔定律限制，整体加速比难以达到量级提升。
*   **依赖深度质量**：在深度传感器噪声较大或透明/高反光物体场景下，区域划分的准确性可能会受到影响。
*   **任务覆盖**：虽然在 LIBERO 上表现良好，但在更复杂、动态性极强的非结构化环境中的鲁棒性仍需进一步验证。

（完）
