---
title: "Critic in the Loop: A Tri-System VLA Framework for Robust Long-Horizon Manipulation"
title_zh: 闭环评论者：一种用于鲁棒长程操作的三系统 VLA 框架
authors: "Pengfei Yi, Yingjie Ma, Wenjiang Xu, Yanan Hao, Shuai Gan, Wanting Li, Shanlin Zhong"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05185v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 结合VLM推理与VLA执行的三系统框架
tldr: 针对视觉机器人操作中高层推理与底层控制的平衡难题，本文提出了一种名为“Critic in the Loop”的三系统VLA框架。该框架结合了负责全局推理的VLM、负责快速执行的VLA以及负责动态调度的轻量级视觉Critic。Critic通过实时监控工作空间，在常规任务中维持VLA闭环执行，并在检测到异常时触发VLM重规划，从而在减少昂贵VLM调用的同时，显著提升了长程任务的鲁棒性和自主性。
selection_source: fresh_fetch
motivation: 现有的VLM推理延迟高，而快速VLA模型在复杂长程任务中缺乏语义深度，难以兼顾实时性与认知规划。
method: 提出由VLM大脑、VLA小脑和视觉Critic组成的三系统架构，利用Critic动态调度控制权并集成人类启发式规则以打破死循环。
result: 在挑战性的长程操作基准测试中，该方法在减少VLM查询次数的同时，实现了最先进的性能表现。
conclusion: 通过视觉引导的动态调度机制，该框架有效增强了机器人在分布外场景下的鲁棒性，为复杂机器人任务提供了高效的层次化解决方案。
---

## 摘要
在视觉机器人操作中，平衡高层语义推理与底层反应式控制仍然是一个核心挑战。虽然视觉语言模型（VLM）在认知规划方面表现出色，但其推理延迟阻碍了实时执行。相反，快速的视觉-语言-动作（VLA）模型通常缺乏复杂长程任务所需的语义深度。为了弥补这一差距，我们提出了“闭环评论者”（Critic in the Loop），这是一种由动态 VLM-专家调度驱动的自适应分层框架。其核心是一个仿生三系统架构，包括用于全局推理的 VLM 大脑、用于反应式执行的 VLA 小脑以及一个轻量级的视觉评论者。通过持续监控工作空间，评论者动态地分配控制权。它通过 VLA 维持常规子任务的快速闭环执行，并在检测到任务停滞或失败等执行异常时，自适应地触发 VLM 进行重新规划。此外，我们的架构无缝集成了受人类启发的规则，以直观地打破无限重试循环。这种基于视觉的调度最小化了昂贵的 VLM 查询，同时显著增强了系统在分布外（OOD）场景下的鲁棒性和自主性。在具有挑战性的长程操作基准测试上的全面实验表明，我们的方法达到了最先进的性能。

## Abstract
Balancing high-level semantic reasoning with low-level reactive control remains a core challenge in visual robotic manipulation. While Vision-Language Models (VLMs) excel at cognitive planning, their inference latency precludes real-time execution. Conversely, fast Vision-Language-Action (VLA) models often lack the semantic depth required for complex, long-horizon tasks. To bridge this gap, we introduce Critic in the Loop, an adaptive hierarchical framework driven by dynamic VLM-Expert scheduling. At its core is a bionic Tri-System architecture comprising a VLM brain for global reasoning, a VLA cerebellum for reactive execution, and a lightweight visual Critic. By continuously monitoring the workspace, the Critic dynamically routes control authority. It sustains rapid closed-loop execution via the VLA for routine subtasks, and adaptively triggers the VLM for replanning upon detecting execution anomalies such as task stagnation or failures. Furthermore, our architecture seamlessly integrates human-inspired rules to intuitively break infinite retry loops. This visually-grounded scheduling minimizes expensive VLM queries, while substantially enhancing system robustness and autonomy in out-of-distribution (OOD) scenarios. Comprehensive experiments on challenging, long-horizon manipulation benchmarks reveal that our approach achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

以下是对论文《Critic in the Loop: A Tri-System VLA Framework for Robust Long-Horizon Manipulation》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在机器人操作中，视觉语言模型（VLM）擅长高层认知规划但推理延迟高，而视觉-语言-动作（VLA）模型反应快但缺乏处理复杂长程（Long-horizon）任务的语义深度。
*   **核心问题**：如何平衡高层语义推理与底层实时控制，使机器人在面对复杂任务、环境扰动及分布外（OOD）场景时具备鲁棒的自适应能力。
*   **整体含义**：受人类认知启发，提出一种“三系统”架构，通过引入独立的“评论者（Critic）”来动态调度“大脑（推理）”与“小脑（执行）”，实现高效且鲁棒的闭环控制。

### 2. 论文提出的方法论
*   **三系统架构（Tri-System）**：
    *   **系统二（大脑/Brain）**：基于预训练 VLM（如 PaliGemma），负责全局推理，根据指令和环境生成语义子任务（如“抓取蓝色杯子”）。
    *   **系统一（小脑/Cerebellum）**：基于流匹配（Flow-matching）的连续动作生成网络，负责高频、反应式的底层控制。
    *   **系统三（评论者/Critic）**：轻量级视觉模型（如 Florence-2），异步监控执行进度并检测异常。
*   **关键技术细节**：
    *   **动态调度机制**：Critic 实时评估进度值 $V_t$（通过蒙特卡洛估算并离散化为文本标签）。当检测到子任务完成、发生意外（输出 `<aci>` 标记）或任务停滞时，才会唤醒“大脑”进行重规划，否则维持“小脑”的高频执行。
    *   **停滞检测与重置规则**：引入人类启发式规则，若进度长期不更新则触发物理状态重置（Reset），打破机械的无效重复循环。
    *   **自动化子任务标注流水线**：利用运动学启发式算法（RDP 几何过滤 + 夹爪状态）提取关键帧，再结合大模型检索生成语义标签，解决了长程任务数据标注昂贵的难题。

### 3. 实验设计
*   **硬件平台**：Cobot Magic ALOHA 双臂机器人，配备 3 个摄像头（1 个全局，2 个腕部）。
*   **实验场景**：
    1.  **整理餐具**：包括顺序摆放、乱序摆放、分布外（OOD）左侧杯子抓取、人为干扰（杯子被撞倒）。
    2.  **清理桌面**：涉及柔性物体操作（打开塑料袋、装入瓶子和纸巾）。
*   **对比方法（Benchmark）**：
    *   **Single-System $\pi_{0.5}$**：标准端到端模型，直接输出动作。
    *   **Dual-System $\pi_{0.5}$**：每步动作块都生成子任务的双系统模型，无 Critic 调度。
*   **数据集**：每个任务收集 200 条演示轨迹，针对异常恢复额外收集 100 条。

### 4. 资源与算力
*   **模型规模**：Critic 使用 Florence-2-base（约 0.2B 参数），Brain/Cerebellum 基于 $\pi_{0.5}$ 架构。
*   **训练细节**：使用 AdamW 优化器，学习率 $1 \times 10^{-6}$，训练 50 个 epoch，Batch Size 为每张 GPU 64。
*   **算力说明**：文中提到了“per GPU”，但**未明确说明具体的 GPU 型号和数量**，也未给出总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：每个场景进行 10 次真实世界测试，涵盖了从常规到极端干扰的多种情况。
*   **消融实验**：针对 OOD 泛化能力做了 4 组对比实验，分析了提示词格式、数据分布和调度逻辑的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了成功率，还通过定性分析展示了系统在面对意外时的恢复过程。对比基准选择合理，能够客观体现“三系统”在降低延迟和增强鲁棒性方面的优势。

### 6. 论文的主要结论与发现
*   **性能领先**：三系统架构在所有长程任务中均显著优于单系统和双系统基准，尤其在复杂干扰和柔性物体操作中表现突出。
*   **异步调度的优势**：通过 Critic 引导的按需推理，大幅降低了 VLM 的调用频率，解决了推理延迟导致的执行僵硬问题。
*   **OOD 泛化**：即使缺乏特定任务的训练数据（如用左手抓杯子），通过 Critic 检测停滞并触发重置规则，系统能利用跨任务学习到的技能完成任务。
*   **推理瓶颈**：消融实验发现，当前系统的主要瓶颈在于高层 VLM 的推理能力（容易产生分布过拟合），而非底层控制能力。

### 7. 优点
*   **架构创新**：将 Critic 独立出来作为调度核心，实现了“想”与“做”的解耦，符合生物学直觉。
*   **鲁棒性强**：通过 `<aci>` 异常标记和停滞重置规则，使机器人具备了初步的“自我纠错”能力。
*   **高效标注**：提出的自动化标注流水线极大地降低了长程任务对人工标注的依赖。

### 8. 不足与局限
*   **推理过拟合**：高层 VLM（系统二）在面对全新场景时仍可能给出错误的语义指令。
*   **数据依赖**：虽然有自动化标注，但仍依赖于一定规模的专家演示数据，尚未完全实现自主探索学习。
*   **应用范围**：实验主要集中在桌面操作，对于更复杂的移动操作或动态环境的适用性有待验证。

（完）
