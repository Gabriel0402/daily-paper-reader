---
title: "APPLV: Adaptive Planner Parameter Learning from Vision-Language-Action Model"
title_zh: APPLV：基于视觉-语言-动作模型的自适应规划器参数学习
authors: "Yuanjie Lu, Beichen Wang, Zhengqi Wu, Yang Li, Xiaomin Lin, Chengzhi Mao, Xuesu Xiao"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08862v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 从VLA模型中学习自适应规划器参数用于导航
tldr: 针对受限环境下的自主导航难题，本文提出APPLV框架。该方法结合了视觉语言模型（VLM）的场景理解能力与经典导航算法的安全性，通过回归头预测规划器参数而非直接输出动作。研究采用了监督学习和强化学习两种微调策略，在BARN数据集和物理机器人实验中均表现出优于现有方法的导航性能和泛化能力。
selection_source: fresh_fetch
motivation: 经典导航算法需繁琐的手动调参，而端到端学习和现有VLA模型在受限空间的精确控制及推理延迟方面存在不足。
method: 提出APPLV框架，利用预训练VLM配合回归头预测经典规划器的参数，并结合监督学习与强化学习进行优化。
result: 在BARN基准测试和实地实验中，APPLV在导航效率和对未知环境的泛化能力上均显著优于对比方法。
conclusion: 将基础模型的语义理解与经典规划器的精确控制相结合，是提升移动机器人复杂环境导航能力的有效途径。
---

## 摘要
移动机器人在高度受限环境中的自主导航仍然具有挑战性。经典的导航方法提供了安全保证，但需要针对特定环境进行参数调整；端到端学习绕过了参数调整，但在受限空间中难以实现精确控制。为此，最近的机器人学习方法在保留经典系统安全性的同时实现了参数调整的自动化，但在泛化到未知环境方面仍面临挑战。最近，视觉-语言-动作（VLA）模型通过利用基础模型的场景理解能力展现出了潜力，但在导航任务中仍面临精确控制和推理延迟的问题。在本文中，我们提出了基于视觉-语言-动作模型的自适应规划器参数学习（APPLV）。与直接输出动作的传统 VLA 模型不同，APPLV 利用带有回归头的预训练视觉-语言模型来预测用于配置经典规划器的规划器参数。我们开发了两种训练策略：基于采集的导航轨迹的监督学习微调，以及进一步优化导航性能的强化学习微调。我们在模拟的基准自主机器人导航（BARN）数据集和物理机器人实验中，针对多种运动规划器对 APPLV 进行了评估。结果表明，APPLV 在导航性能和对未知环境的泛化能力方面均优于现有方法。

## Abstract
Autonomous navigation in highly constrained environments remains challenging for mobile robots. Classical navigation approaches offer safety assurances but require environment-specific parameter tuning; end-to-end learning bypasses parameter tuning but struggles with precise control in constrained spaces. To this end, recent robot learning approaches automate parameter tuning while retaining classical systems' safety, yet still face challenges in generalizing to unseen environments. Recently, Vision-Language-Action (VLA) models have shown promise by leveraging foundation models' scene understanding capabilities, but still struggle with precise control and inference latency in navigation tasks. In this paper, we propose Adaptive Planner Parameter Learning from Vision-Language-Action Model (\textsc{applv}). Unlike traditional VLA models that directly output actions, \textsc{applv} leverages pre-trained vision-language models with a regression head to predict planner parameters that configure classical planners. We develop two training strategies: supervised learning fine-tuning from collected navigation trajectories and reinforcement learning fine-tuning to further optimize navigation performance. We evaluate \textsc{applv} across multiple motion planners on the simulated Benchmark Autonomous Robot Navigation (BARN) dataset and in physical robot experiments. Results demonstrate that \textsc{applv} outperforms existing methods in both navigation performance and generalization to unseen environments.

---

## 论文详细总结（自动生成）

### APPLV：基于视觉-语言-动作模型的自适应规划器参数学习 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在高度受限的环境（如狭窄通道、密集障碍物）中，移动机器人的自主导航面临“安全性”与“灵活性”的权衡。
*   **经典导航方法**：虽然具备安全保证和可解释性，但高度依赖环境特定的参数（如速度限制、膨胀半径），手动调参费时费力且难以适应动态变化。
*   **端到端学习**：虽然避开了调参，但缺乏安全保障，且在未见过的环境中泛化能力差。
*   **现有 VLA 模型**：虽然具备强大的场景理解能力，但直接输出底层动作（Action）时精度不足（厘米级控制难），且模型庞大导致的推理延迟难以满足实时控制需求。
**核心问题**：如何结合视觉语言模型（VLM）的语义理解能力与经典规划器的安全性，实现高效、稳健且具备泛化能力的自适应导航？

#### 2. 方法论
APPLV 提出了一种“模型预测参数”的范式，即利用 VLA 模型动态调整经典规划器的配置参数，而非直接控制机器人。
*   **核心思想**：将导航任务建模为元马尔可夫决策过程（Meta-MDP），由 VLA 模型充当“元策略”，输出规划器参数 $\phi$，再由经典规划器根据 $\phi$ 生成具体的控制动作。
*   **模型架构**：
    *   **视觉-语言骨干**：采用 **Qwen2.5-VL-3B**。输入包括自定义的“俯视图”图像（融合了激光雷达扫描、全局路径和机器人足迹）以及包含机器人状态的文本提示。
    *   **历史编码器（History Encoder）**：利用卷积网络和时间 Transformer 处理历史帧，捕捉环境的时间上下文。
    *   **DPT 回归头**：借鉴 Dense Prediction Transformer (DPT) 结构，融合 VLM 的多层隐藏状态和历史特征，通过 MLP 回归输出连续的参数矢量。
*   **训练策略**：
    *   **APPLV-SL（监督学习）**：利用专家启发式规则和现有算法（如 APPLR）生成的轨迹进行行为克隆（BC）。
    *   **APPLV-RLFT（强化学习微调）**：在 SL 基础上，使用 **TD3 算法**在仿真环境中进行强化学习，通过进度奖励、碰撞惩罚等函数进一步优化性能。

#### 3. 实验设计
*   **数据集/场景**：
    *   **仿真**：使用 **BARN (Benchmark Autonomous Robot Navigation)** 数据集，包含 300 个训练环境和 300 个全新的测试环境（高度受限的随机障碍物空间）。
    *   **物理实验**：使用 **Clearpath Jackal** 机器人，在真实的室内杂乱环境中进行验证。
*   **Benchmark 与对比方法**：
    *   **集成规划器**：DWA、TEB、MPPI、DDP（涵盖了采样、优化和动力学规划等不同类型）。
    *   **对比基准**：
        1.  **Heuristic Expert**：人工设计的启发式调参规则。
        2.  **APPLR**：基于激光雷达输入的强化学习参数调节方法。
        3.  **Transformer BC**：从零训练的同规模 Transformer 模型（用于验证预训练 VLM 的价值）。
        4.  **Zero-Shot VLM**：直接使用 GPT-4o 通过 Prompt 输出参数。

#### 4. 资源与算力
*   **硬件环境**：
    *   **训练/仿真服务器**：使用 **NVIDIA A100 GPU** 节点。
    *   **推理端**：物理实验中使用 **RTX 5070 Ti** 显卡作为推理服务器。
*   **算力规模**：
    *   仿真环境运行了 **250-300 个并行的 Gazebo 实例**。
    *   模型推理延迟在不同 GPU 上约为 **0.27s 至 0.47s** 之间。
*   **训练细节**：使用了 LoRA 技术进行参数高效微调（LoRA 权重约 330M），RL 阶段采用了分布式采集框架。

#### 5. 实验数量与充分性
*   **实验规模**：在 300 个未见过的仿真测试环境中进行了评估，每个环境测试 3 次，总计进行了数千次测试。
*   **充分性**：实验覆盖了四种主流的局部规划器，证明了方法的通用性；通过与 Transformer BC 的对比，客观证明了 VLM 预训练权重的必要性；物理实验进一步验证了从仿真到现实（Sim-to-Real）的迁移能力。
*   **公平性**：对比方法涵盖了从人工规则到最先进的 RL 方法，以及最新的大模型零样本能力，评估维度包括成功率、导航时间、碰撞率等，较为全面客观。

#### 6. 主要结论与发现
*   **性能领先**：APPLV 在所有规划器上的表现均优于基准方法，尤其在成功率和导航效率（得分）上提升显著。
*   **预训练优势**：带有预训练权重的 APPLV 显著优于从头训练的 Transformer，说明 VLM 的视觉语义知识有助于理解复杂的空间几何关系。
*   **RLFT 的作用**：强化学习微调能进一步提升监督学习的效果，使模型在处理极端受限空间时更加果断。
*   **参数预测的优势**：由于参数更新频率（如 2Hz）远低于动作控制频率（如 20Hz），APPLV 有效缓解了大型 VLA 模型的推理延迟问题。

#### 7. 优点（亮点）
*   **架构创新**：将 VLA 模型与经典规划器解耦，通过预测“参数”而非“动作”，兼顾了基础模型的智能与经典算法的安全性。
*   **多层特征融合**：利用 DPT 回归头提取 VLM 的多层隐藏状态，比仅使用最后一层特征能更精细地感知环境。
*   **泛化能力强**：在 BARN 这种极具挑战性的随机环境中表现出极强的零样本泛化能力。

#### 8. 不足与局限
*   **推理延迟**：尽管参数预测降低了频率要求，但单次推理仍需约 0.3s-0.5s，对于极高速运动的机器人可能仍存在滞后。
*   **物理部署偏差**：实验发现 DWA 和 TEB 在物理部署时受定位误差影响较大，导致性能下降，说明系统对底层导航框架的鲁棒性仍有依赖。
*   **数据依赖**：虽然使用了 RLFT，但初始阶段仍依赖高质量的专家演示或启发式数据进行 SL 预训练。

（完）
