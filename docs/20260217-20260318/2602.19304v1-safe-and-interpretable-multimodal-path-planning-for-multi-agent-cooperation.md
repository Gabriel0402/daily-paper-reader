---
title: Safe and Interpretable Multimodal Path Planning for Multi-Agent Cooperation
title_zh: 面向多智能体协作的安全且可解释的多模态路径规划
authors: "Haojun Shi, Suyu Ye, Katherine M. Guerrerio, Jianzhi Shen, Yifan Yin, Daniel Khashabi, Chien-Ming Huang, Tianmin Shu"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19304v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 基于VLM的多智能体协作与通信路径规划
tldr: 本研究针对多智能体协作中意图不明确导致的规划难题，提出了一种安全且可解释的多模态路径规划方法 CaPE（Code as Path Editor）。该方法利用视觉语言模型（VLM）将环境信息和语言通信转化为路径编辑程序，并通过基于模型的规划器进行安全验证。CaPE 作为一个即插即用模块，在自动驾驶、家庭任务和共同搬运等多种仿真及现实场景中表现出色，有效增强了机器人与人类或其他机器人的协作对齐能力，在保证安全性的同时实现了开放式协作。
selection_source: fresh_fetch
motivation: 在多智能体协作中，当智能体无法准确预测彼此意图时，需要一种能将语言通信转化为安全、可解释路径规划的机制。
method: 提出 CaPE 方法，利用视觉语言模型合成路径编辑程序，并由基于模型的规划器进行验证，实现从通信到路径更新的可靠映射。
result: 实验表明 CaPE 能作为即插即用模块显著提升机器人在自动驾驶和家庭协作等任务中根据语言指令调整路径的能力。
conclusion: 通过结合 VLM 的程序合成能力与模型化规划的安全性，该方法为开放式多智能体协作提供了一种兼顾灵活性与安全性的新方案。
---

## 摘要
分散式智能体之间的成功协作要求每个智能体能够根据其他智能体的行为快速调整其规划。在智能体无法确信地预测彼此意图和规划的场景中，语言通信对于确保安全至关重要。在这项工作中，我们专注于路径层面的协作，即智能体必须相互调整路径以避免碰撞或进行物理协作（如共同搬运）。具体而言，我们提出了一种安全且可解释的多模态路径规划方法 CaPE（Code as Path Editor），该方法根据环境和其他智能体的语言通信为智能体生成并更新路径规划。CaPE 利用视觉语言模型（VLM）合成路径编辑程序，并由基于模型的规划器进行验证，从而以安全且可解释的方式将通信落地为路径规划的更新。我们在多种模拟和真实场景中评估了我们的方法，包括自动驾驶、家庭环境和共同搬运任务中的多机器人及人机协作。实验结果表明，CaPE 可以作为即插即用模块集成到不同的机器人系统中，显著增强了机器人根据其他机器人或人类的语言通信调整其规划的能力。我们还展示了基于 VLM 的路径编辑程序合成与基于模型的规划安全性相结合，使机器人能够在保持安全性和可解释性的同时实现开放式协作。

## Abstract
Successful cooperation among decentralized agents requires each agent to quickly adapt its plan to the behavior of other agents. In scenarios where agents cannot confidently predict one another's intentions and plans, language communication can be crucial for ensuring safety. In this work, we focus on path-level cooperation in which agents must adapt their paths to one another in order to avoid collisions or perform physical collaboration such as joint carrying. In particular, we propose a safe and interpretable multimodal path planning method, CaPE (Code as Path Editor), which generates and updates path plans for an agent based on the environment and language communication from other agents. CaPE leverages a vision-language model (VLM) to synthesize a path editing program verified by a model-based planner, grounding communication to path plan updates in a safe and interpretable way. We evaluate our approach in diverse simulated and real-world scenarios, including multi-robot and human-robot cooperation in autonomous driving, household, and joint carrying tasks. Experimental results demonstrate that CaPE can be integrated into different robotic systems as a plug-and-play module, greatly enhancing a robot's ability to align its plan to language communication from other robots or humans. We also show that the combination of the VLM-based path editing program synthesis and model-based planning safety enables robots to achieve open-ended cooperation while maintaining safety and interpretability.

---

## 论文详细总结（自动生成）

以下是对论文《Safe and Interpretable Multimodal Path Planning for Multi-Agent Cooperation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在分散式多智能体环境（如人机协作或多机器人系统）中，智能体往往无法准确预测他人的意图。虽然语言通信是解决此类冲突的关键，但如何将**非结构化的自然语言指令**可靠地转化为**安全且精确的机器人路径规划**仍是一个巨大挑战。
*   **研究动机**：
    *   传统的路径规划（如RRT）缺乏对语义指令的理解。
    *   直接使用大模型（VLM）生成路径点（Waypoints）往往缺乏物理安全保证，容易导致碰撞。
    *   需要一种既能理解开放式语言指令，又能保证物理可行性，且过程可解释的规划框架。

### 2. 方法论：CaPE (Code as Path Editor)
CaPE 的核心思想是将路径规划视为一个“**语言引导的路径编辑**”过程，而非从零生成。
*   **核心流程**：
    1.  **联合规划器 (Joint Planner)**：利用支持同伦类（Homotopy-aware）的 RRT 算法生成多条候选路径，并预测其他智能体的可能路径。
    2.  **VLM 程序合成**：视觉语言模型（VLM）接收多模态输入（顶部视图地图、候选路径、他人预测路径、语言指令），生成基于特定领域语言（DSL）的路径编辑程序。
    3.  **DSL 指令集**：定义了五种原子操作：`select_path`（选择路径）、`modify_translation`（平移修改）、`modify_rotation`（旋转修改）、`wait`（等待）、`insert_waypoint`（插入路径点）。
    4.  **路径验证器 (Verifier)**：基于物理模型逐行检查 DSL 代码。如果某行编辑会导致碰撞，验证器将拒绝该修改，从而确保最终输出路径的绝对安全性。
*   **模型训练**：研究者微调了一个小型 VLM（Qwen-2.5-7B），使其能够熟练编写该 DSL 程序。

### 3. 实验设计
*   **实验场景**：
    1.  **多机器人协作 (SimWorld)**：模拟停车场环境，2-3 辆自动驾驶车通过语言协商避免碰撞。
    2.  **人机家务协作 (VirtualHome 2.0)**：机器人在公寓内根据人类指令（如“我要去冰箱，请绕行”）调整路径。
    3.  **现实世界共同搬运 (Stretch 3 机器人)**：人类与机器人共同抬着一根长管穿过障碍区，机器人需实时响应人类的口头修正。
*   **对比方法 (Baselines)**：
    *   **Planner-only**：仅靠规划器，不考虑语言。
    *   **VLM Agents**：直接让 VLM 输出控制动作（前、后、左、右）。
    *   **VLM Waypoint Generators**：让 VLM 直接生成路径点坐标。
    *   **RoboPoint**：专门用于机器人空间推理的预训练模型。
    *   **不同底座模型**：对比了 Gemini 3.0 Pro, GPT 5.2, Claude Opus 4.5 等。

### 4. 资源与算力
*   **训练算力**：使用了 **4 张 NVIDIA A100 GPU**。
*   **训练时长**：总计训练了 **36 小时**。
*   **数据集规模**：生成了 5 万条合成训练数据（包含地图、指令和对应的 DSL 代码）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从仿真到现实（Sim-to-Real）的跨越。
    *   仿真实验包含数十个复杂的停车场和公寓布局。
    *   现实实验邀请了 **10 名志愿者**进行实机测试，并收集了主观评价指标。
*   **消融实验**：
    *   验证了“同伦类规划器”相对于“单路径规划器”的优越性。
    *   验证了“安全验证模块”对提升成功率的必要性。
*   **评价客观性**：采用了成功率 (SR)、路径效率 (SEL)、推理耗时、Token 成本以及人类主观排名等多元指标，实验设计较为全面且客观。

### 6. 主要结论与发现
*   **性能领先**：CaPE 在所有场景下的成功率均远高于直接生成路径点的 VLM 或纯规划器。在 2 机器人协作中，微调后的 CaPE 达到了 **90% 的成功率**，而基准模型最高仅为 23.3%。
*   **安全性保障**：通过验证器拦截非法指令，CaPE 在仿真中实现了零碰撞（针对静态障碍物）。
*   **泛化能力**：尽管微调模型仅在合成数据上训练，但它能很好地泛化到现实世界中人类非正式、多样化的口语指令。
*   **效率与成本**：微调后的小型模型（7B）在推理速度上优于闭源大模型，且表现相当。

### 7. 优点与亮点
*   **即插即用**：CaPE 作为一个模块，可以轻松集成到现有的机器人导航栈中。
*   **可解释性强**：DSL 代码提供了清晰的中间层，人类可以直观看到机器人是因为理解了哪句指令而做出了哪项路径修改（如“在第5步等待2秒”）。
*   **鲁棒性**：通过“编辑”而非“生成”，保留了原始规划器的几何合理性，降低了 VLM 产生幻觉导致路径畸形的风险。

### 8. 不足与局限
*   **感知依赖**：系统假设视觉感知（地图构建）是可靠的，在极端噪声或动态遮挡环境下性能可能下降。
*   **意图预测误差**：如果对他人目标的预测（Goal Inference）出现严重偏差，生成的编辑程序可能无法达成最优协作。
*   **安全与意图的权衡**：当人类指令本身具有危险性时，验证器会拒绝执行，这可能导致机器人行为与人类预期不符，需要更复杂的反馈机制。
*   **群体规模**：目前实验主要集中在 2-3 个智能体，尚未验证在大规模群体协作中的表现。

（完）
