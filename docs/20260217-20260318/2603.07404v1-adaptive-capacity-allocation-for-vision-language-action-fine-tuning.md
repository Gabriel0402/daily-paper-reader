---
title: Adaptive Capacity Allocation for Vision Language Action Fine-tuning
title_zh: 视觉语言动作微调的自适应容量分配
authors: "Donghoon Kim, Minji Bae, Unghui Nam, Gyeonghun Kim, Suyun Lee, Kyuhong Shim, Byonghyo Shim"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07404v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉语言动作模型的秩自适应微调
tldr: 针对视觉语言动作模型（VLA）在机器人任务微调中固定秩难以适应不同任务复杂度的问题，本文提出LoRA-SP（Select-Prune）。该方法通过SVD风格的参数化和基于能量目标的动态路由，实现了输入和层级的自适应容量分配。在真实机器人实验中，LoRA-SP在显著减少参数量的同时，提升了多任务泛化性能，解决了传统LoRA在机器人领域表现不佳的痛点。
selection_source: fresh_fetch
motivation: 传统LoRA的固定秩难以满足机器人任务中高且多变的内在秩需求，导致多任务微调时性能受限且干扰严重。
method: 提出LoRA-SP，利用SVD参数化和能量目标驱动的路由器，动态选择和剪枝共享向量库中的活跃分量以实现自适应容量分配。
result: "在真实机器人操纵任务中，LoRA-SP比标准LoRA提升了高达31.6%的多任务成功率，且性能媲美甚至超过全量微调。"
conclusion: 通过动态秩分配和能量控制，LoRA-SP有效提升了VLA模型在复杂物理环境下的微调效率和跨任务泛化能力。
---

## 摘要
视觉语言动作模型（VLA）越来越多地应用于具身智能（Physical AI），但将预训练的 VLA 模型部署到未见过的环境、机器人本体或任务中仍需要进行适配。参数高效微调（PEFT），特别是 LoRA，在 VLA 策略中很常用，但其暴露的容量调节参数——秩（rank）——在迁移过程中并不统一：机器人领域的迁移表现出比语言微调更高且随任务变化的本征秩（intrinsic rank）。对于大语言模型（LLM），较小的秩（例如 $r \in \{4, 8\}$）就足够了，而谱分析表明 VLA 可能需要大得多的秩（例如 $r \approx 128$）或接近全秩，这种不匹配在多任务设置中会进一步恶化。我们提出了 LoRA-SP（Select-Prune），这是一种秩自适应微调方法，它用输入相关和逐层级的容量取代了固定秩更新。LoRA-SP 采用类 SVD 参数化方法，通过一个小型路由器的非负得分作为共享向量库上的奇异值。激活集通过累积平方得分的能量目标 $E(k) \ge η$ 来选择，通过我们的谱分析，这与近似误差建立了直接联系。在训练过程中，$η$ 将能量集中在少数方向上，并引导路由器在保持准确性的同时依赖更少的向量。这产生了紧凑的适配器，减少了跨任务干扰并提高了泛化能力。在未见过的 AgileX PiPER 机械臂上收集的四个真实机器人操作任务中，基于两种 VLA 骨干网络（$π_0$ 和 SmolVLA），LoRA-SP 在可训练参数远少于全量微调的情况下，性能与之持平或更优，且在多任务成功率上比标准 LoRA 提升了高达 31.6%，同时对秩的选择具有鲁棒性。

## Abstract
Vision language action models (VLAs) are increasingly used for Physical AI, but deploying a pre-trained VLA model to unseen environments, embodiments, or tasks still requires adaptation. Parameter-efficient fine-tuning (PEFT), especially LoRA, is common for VLA policies, yet the exposed capacity knob, the rank, does not transfer uniformly: robotics transfer exhibits a higher and task-varying intrinsic rank than language fine-tuning. Small ranks suffice for LLMs (e.g., $r \in \{4, 8\}$), while spectral analyses indicate VLAs may require much larger ranks (e.g., $r \approx 128$) or near-full rank, a mismatch that worsens in multi-task settings. We present LoRA-SP (Select-Prune), a rank-adaptive fine-tuning method that replaces fixed-rank updates with input- and layer-wise capacity. LoRA-SP uses an SVD-style parameterization with a small router whose nonnegative scores act as singular values over a shared vector bank. The active set is chosen by an energy target on the cumulative squared scores $E(k) \ge η$, providing a direct link to approximation error via our spectral analysis. During training, $η$ concentrates energy on a few directions and teaches the router to rely on fewer vectors while preserving accuracy. This yields compact adapters that reduce cross-task interference and improve generalization. On four real-robot manipulation tasks collected on an unseen AgileX PiPER arm, across two VLA backbones ($π_0$ and SmolVLA), LoRA-SP matches or exceeds full fine-tuning with far fewer trainable parameters, and improves multi-task success by up to 31.6% over standard LoRA while remaining robust to rank choice.

---

## 论文详细总结（自动生成）

### 视觉语言动作微调的自适应容量分配 (LoRA-SP) 论文总结

#### 1. 核心问题与研究动机
*   **研究背景**：视觉语言动作模型（VLA）在具身智能中表现出色，但在迁移到新环境、新机器人本体或新任务时，通常需要微调。
*   **核心痛点**：传统的参数高效微调方法（如 LoRA）使用固定秩（Rank）。研究发现，大语言模型（LLM）微调通常只需要很小的秩（如 4 或 8），但 VLA 模型在机器人任务中表现出更高且随任务变化的“本征秩”（Intrinsic Rank），通常需要 $r \approx 128$ 甚至接近全秩。
*   **多任务冲突**：在多任务设置下，固定秩会导致不同任务在共享的低秩子空间中产生干扰，降低泛化性能。

#### 2. 方法论：LoRA-SP (Select-Prune)
*   **核心思想**：将固定秩更新替换为基于输入和层级的自适应容量分配，利用类 SVD（奇异值分解）的参数化形式动态调整活跃向量的数量。
*   **关键技术细节**：
    *   **参数化形式**：更新矩阵表示为 $\Delta W(x) = U \text{diag}(s(x)) V$，其中 $U$ 和 $V$ 是预设的宽向量库（如初始秩设为 128），$s(x)$ 是由一个小型路由器（Router）生成的非负得分，充当“奇异值”。
    *   **选择（Select）机制**：根据得分 $s(x)$ 计算累积能量 $E(k)$。系统自动选择最小的 $k$，使得前 $k$ 个向量的能量占比达到阈值 $\eta$（如 0.9），从而动态确定当前输入所需的秩。
    *   **剪枝（Prune）机制**：引入**谱损失（Spectral Loss）** $\mathcal{L}_{spec} = 1 - E_k(x)$。该损失函数鼓励能量集中在少数关键方向上，引导路由器在保持精度的前提下使用更少的向量。
    *   **总损失函数**：结合了任务损失（如 Flow Matching）、谱损失和路由器正则化损失（用于平衡负载）。

#### 3. 实验设计
*   **实验场景**：使用 7 自由度的 **AgileX PiPER** 机械臂进行真实世界操作任务。该机器人本体未出现在预训练数据中，属于 OOD（分布外）设备。
*   **数据集**：收集了 480 个演示视频，涵盖四个任务：开锅盖（Open）、倒方块（Pour）、按按钮（Press）、抓放葡萄（Pick-Place）。
*   **基准模型（Backbones）**：
    *   **$\pi_0$**：基于 PaLIGemma 的高容量模型。
    *   **SmolVLA**：轻量级 VLA 模型。
*   **对比方法**：全量微调（Full FT）、标准 LoRA（不同秩 $r \in \{16, 32, 64, 128\}$）、AdaLoRA（自适应预算分配）、LoRA-MoE（混合专家 LoRA）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量及训练总时长。
*   **效率提及**：文中提到 LoRA-SP 旨在降低推理成本并提高参数效率，其可训练参数量仅为全量微调的约 9% ($\pi_0$) 到 17% (SmolVLA)。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个真实机器人任务，对比了 2 种主流 VLA 架构。
*   **消融实验**：针对谱损失的有效性、能量目标阈值 $\eta$ 的敏感度进行了详细测试。
*   **充分性评价**：实验设计较为充分，不仅对比了成功率，还通过谱分析展示了不同模块（视觉、语言、动作）对秩的需求差异，证明了自适应分配的必要性。

#### 6. 主要结论与发现
*   **性能优越**：LoRA-SP 在多任务成功率上显著超过标准 LoRA，在 $\pi_0$ 上提升了 23.3%，在 SmolVLA 上提升了 31.6%，性能接近甚至超过全量微调。
*   **秩的异质性**：实验证明视觉塔（Vision Tower）通常需要最高的秩，而语言和动作模块需要的秩相对较低且波动较大。
*   **抗干扰能力**：通过动态分配容量，LoRA-SP 有效减少了多任务学习中的负迁移（任务间干扰）。
*   **鲁棒性**：LoRA-SP 对初始秩的选择不敏感，能够自动收敛到合适的活跃秩。

#### 7. 优点
*   **理论支撑**：将秩的选择与 SVD 近似误差（Frobenius 范数误差）直接挂钩，具有较强的数学解释性。
*   **灵活性**：实现了“按需分配”，解决了机器人任务中不同动作复杂度对模型容量需求不一的问题。
*   **真实验证**：在全新的机器人硬件（PiPER）上验证了迁移能力，具有很强的实用价值。

#### 8. 不足与局限
*   **算力细节缺失**：缺乏具体的训练资源消耗数据，难以评估其在有限算力环境下的部署成本。
*   **任务多样性限制**：虽然在真实机器人上做了实验，但 4 个任务的规模相对较小，尚未在大规模多任务（如数百个任务）场景下验证路由器的稳定性。
*   **推理延迟**：虽然活跃秩减少了，但引入了额外的路由器计算和动态选择逻辑，论文未详细讨论这部分开销对实时控制频率的影响。

（完）
