---
title: "VLANeXt: Recipes for Building Strong VLA Models"
title_zh: VLANeXt：构建强力 VLA 模型的方案
authors: "Xiao-Ming Wu, Bin Fan, Kang Liao, Jian-Jian Jiang, Runze Yang, Yihang Luo, Zhonghua Wu, Wei-Shi Zheng, Chen Change Loy"
date: 2026-02-20
pdf: "https://arxiv.org/pdf/2602.18532v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 构建强力VLA模型的系统性研究与方案
tldr: 针对视觉-语言-动作模型（VLA）领域训练协议和评估标准不统一的问题，本文在统一框架下系统性地探索了VLA的设计空间。通过对基础组件、感知要素和动作建模三个维度的深入研究，总结出12项关键发现，并据此构建了高性能模型VLANeXt。该模型在LIBERO等基准测试中超越了现有SOTA方法，展现出卓越的泛化能力，并提供了统一的开源代码库以促进社区研究。
selection_source: fresh_fetch
motivation: 当前VLA模型的研究因训练协议和评估设置的不一致而显得碎片化，难以识别真正有效的设计选择。
method: 在统一框架下系统剖析基础组件、感知要素和动作建模三个维度的设计，提炼出12项构建强力VLA模型的关键准则。
result: VLANeXt模型在LIBERO和LIBERO-plus基准测试中优于现有SOTA方法，并在真实世界任务中表现出强大的泛化性能。
conclusion: 本研究通过提供标准化的设计指南和开源代码库，为构建高性能、通用化的VLA模型奠定了坚实基础。
---

## 摘要
随着大型基础模型的兴起，视觉-语言-动作模型（VLA）应运而生，利用强大的视觉和语言理解能力进行通用策略学习。然而，目前的 VLA 领域仍然处于碎片化和探索阶段。尽管许多团队已经提出了各自的 VLA 模型，但训练协议和评估设置的不一致使得难以确定哪些设计选择真正起作用。为了给这个不断发展的领域带来结构化指导，我们在统一的框架和评估设置下重新审视了 VLA 的设计空间。从类似于 RT-2 和 OpenVLA 的简单 VLA 基准出发，我们从三个维度系统地剖析了设计选择：基础组件、感知要素和动作建模视角。通过这项研究，我们提炼出 12 项关键发现，共同构成了构建强力 VLA 模型的实用方案。这一探索的成果是一个简单而有效的模型——VLANeXt。VLANeXt 在 LIBERO 和 LIBERO-plus 基准测试中优于之前的最先进方法，并在现实世界实验中展示了强大的泛化能力。我们将发布一个统一且易于使用的代码库，作为社区复现我们的发现、探索设计空间并在共享基础上构建新 VLA 变体的通用平台。

## Abstract
Following the rise of large foundation models, Vision-Language-Action models (VLAs) emerged, leveraging strong visual and language understanding for general-purpose policy learning. Yet, the current VLA landscape remains fragmented and exploratory. Although many groups have proposed their own VLA models, inconsistencies in training protocols and evaluation settings make it difficult to identify which design choices truly matter. To bring structure to this evolving space, we reexamine the VLA design space under a unified framework and evaluation setup. Starting from a simple VLA baseline similar to RT-2 and OpenVLA, we systematically dissect design choices along three dimensions: foundational components, perception essentials, and action modelling perspectives. From this study, we distill 12 key findings that together form a practical recipe for building strong VLA models. The outcome of this exploration is a simple yet effective model, VLANeXt. VLANeXt outperforms prior state-of-the-art methods on the LIBERO and LIBERO-plus benchmarks and demonstrates strong generalization in real-world experiments. We will release a unified, easy-to-use codebase that serves as a common platform for the community to reproduce our findings, explore the design space, and build new VLA variants on top of a shared foundation.

---

## 论文详细总结（自动生成）

以下是对论文《VLANeXt: Recipes for Building Strong VLA Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的视觉-语言-动作（VLA）模型研究处于“碎片化”和“原始汤”状态。虽然涌现了大量模型（如 RT-2, OpenVLA, $\pi_0$ 等），但由于训练协议、评估基准和设计选择（如架构、感知输入、动作表示）的不一致，研究者难以确定哪些设计因素真正对性能和泛化性起关键作用。
*   **整体含义**：本文旨在通过统一的框架和评估协议，系统性地剖析 VLA 的设计空间，提炼出一套构建强力 VLA 模型的“实用方案”（Recipes），并据此开发出高性能、高效率的模型 **VLANeXt**。

### 2. 方法论：核心思想与关键技术
论文从一个简单的基准（LLaMA 3.2-3B + SigLIP2）出发，从三个维度进行了系统演进：
*   **基础组件（Foundational Components）**：
    *   **策略模块设计**：放弃直接复用文本 Token，采用独立的、更深层的策略头（MetaQuery 风格，使用 16 个查询 Token 和 12 层 Transformer）。
    *   **动作分块（Action Chunking）**：采用长度为 8 的动作块，提高动作连贯性。
    *   **学习目标**：对比了分类、回归、扩散和流匹配（Flow Matching），最终选择 **流匹配**，因其在处理多峰分布时表现稳健且效率高。
    *   **VLM-Policy 连接**：提出 **软连接（Soft Connection）**，在 VLM 和策略模块之间插入可学习的查询作为潜空间缓冲，优于松散耦合或层层紧耦合。
*   **感知要素（Perception Essentials）**：
    *   **多视角融合**：结合第三人称视角和手眼（Wrist）相机视角，显著提升空间感知。
    *   **本体感受（Proprioception）**：发现将本体感受信息注入 **VLM 端**（而非直接注入策略头）效果最好，有助于多模态特征的深度融合。
    *   **时间步**：发现冗余的历史图像帧反而可能引入噪声，最终仅使用当前帧。
*   **动作建模视角（Action Modeling Perspectives）**：
    *   **频域辅助损失**：将动作生成视为时间序列预测，引入 **离散余弦变换（DCT）** 并在频域计算 MSE 损失。这能以极低的计算开销捕捉动作序列的结构化特征。
    *   **世界建模**：虽然预测未来帧（World Modeling）能提升性能，但因训练成本增加 3 倍，在最终方案中被舍弃以保持效率。

### 3. 实验设计
*   **数据集与基准**：
    *   **LIBERO**：包含 Spatial, Object, Goal, Long 四个子集，评估基础任务能力。
    *   **LIBERO-plus**：在 LIBERO 基础上引入光照、背景、相机位姿、语言描述等扰动，评估模型的 **鲁棒性与泛化性**。
    *   **真实世界**：使用 Franka Emika（单臂）和 Aloha（双臂）系统，涵盖清理桌面、开抽屉、抬筐等任务。
*   **对比方法**：
    *   **直接策略学习**：Diffusion Policy, Octo, MDT。
    *   **VLA 模型**：OpenVLA, $\pi_0$, $\pi_0$-Fast, WorldVLA, UniVLA, OpenVLA-OFT 等。

### 4. 资源与算力
*   **训练细节**：模型在 LIBERO 数据集上微调 10,000 步，Batch Size 为 256。
*   **学习率**：3B 以下模型为 $1 \times 10^{-4}$，更大模型为 $5 \times 10^{-5}$。
*   **算力说明**：文中提到了世界建模会使训练时间翻倍（3x），但 **未明确给出具体的 GPU 型号、数量及总训练时长**。通常此类规模（2B-7B 参数）的 VLA 模型训练需要 A100 或 H100 集群支持。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的消融实验（见 Table 1），涵盖了从架构到感知的 10 多个变量，每一步演进都有明确的数据支撑。
*   **充分性与公平性**：
    *   实验在统一的 LLaMA/Qwen 框架下进行，确保了对比的“苹果对苹果”。
    *   引入 LIBERO-plus 这一更难的基准，避免了在简单任务上性能饱和导致的评估偏差。
    *   涵盖了单臂到双臂、仿真到真实的跨度，实验设计非常客观且全面。

### 6. 主要结论与发现
1.  **软连接优于紧/松连接**：在 VLM 和 Policy 间加缓冲层更有利于特征传递。
2.  **本体感受应注入 VLM**：这比直接给 Policy 模块提供状态信息更有效。
3.  **频域建模是“免费的午餐”**：引入频域损失几乎不增加开销，但能显著提升动作预测精度。
4.  **VLM 骨干强度至关重要**：更强的视觉语言底座（如 Qwen3-VL）直接带动 VLA 性能提升。
5.  **VLANeXt 性能卓越**：以 2.5B 的参数量在 LIBERO-plus 上达到 80.1% 的成功率，远超 7B 规模的 OpenVLA-OFT（69.6%）。

### 7. 优点
*   **系统性强**：不是简单提出一个新模型，而是梳理了整个 VLA 的设计准则，具有极高的工程参考价值。
*   **效率与性能平衡**：识别并舍弃了高成本低收益的设计（如复杂的世界建模），保留了高效的频域损失。
*   **开源贡献**：承诺发布统一、轻量化的代码库，有助于解决 VLA 领域复现难的问题。

### 8. 不足与局限
*   **算力细节缺失**：未披露具体的硬件资源消耗，不便于其他团队评估复现成本。
*   **长程推理限制**：虽然在 LIBERO-Long 上表现不错，但对于需要极长序列推理或复杂逻辑规划的任务，文中探讨较少。
*   **数据依赖**：真实世界实验仍依赖于一定数量的专家演示（每项任务 50 序列），对于极低数据（Few-shot）场景的探索有限。

（完）
