---
title: "DySL-VLA: Efficient Vision-Language-Action Model Inference via Dynamic-Static Layer-Skipping for Robot Manipulation"
title_zh: DySL-VLA：通过动静态层跳过实现机器人操作的高效视觉-语言-动作模型推理
authors: "Zebin Yang, Yijiahao Qi, Tong Xie, Bo Yu, Shaoshan Liu, Meng Li"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22896v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 高效VLA推理的动态层跳减
tldr: 针对视觉-语言-动作（VLA）模型在机器人任务中计算开销大的问题，本文提出DySL-VLA框架。该方法基于动作重要性差异，将模型层分为核心层和可跳过层，并引入先验-后验跳层指导机制。通过两阶段知识蒸馏训练，在保持精度的同时显著提升了推理速度并减少了参数量，为实时机器人操作提供了高效解决方案。
selection_source: fresh_fetch
motivation: VLA模型计算成本高，难以满足机器人实时操作的需求，且不同动作步骤对精度的要求存在差异。
method: 提出动态-静态跳层框架，将层分为信息层和增量层，并利用先验-后验指导机制及两阶段知识蒸馏进行优化。
result: "在Calvin数据集上，相比Deer-VLA成功长度提升2.1%，可训练参数减少85.7倍，且在同等精度下比RoboFlamingo快3.75倍。"
conclusion: DySL-VLA通过动态跳层策略有效平衡了VLA模型的推理效率与任务性能，是实现高效机器人操控的有力工具。
---

## 摘要
视觉-语言-动作（VLA）模型通过融合语言模型的推理能力与视觉模型的 3D 理解能力，在机器人操作等任务中取得了显著成功。然而，其高昂的计算成本仍然是需要实时性能的实际应用中的主要障碍。我们观察到，任务中的动作具有不同程度的重要性：关键步骤需要高精度，而次要步骤则可以容忍更大的方差。利用这一见解，我们提出了 DySL-VLA，这是一个通过根据每个动作的重要性动态跳过 VLA 层来降低计算成本的新型框架。DySL-VLA 将其层分为两类：始终执行的信息层（informative layers）和可选择性跳过的增量层（incremental layers）。为了在不牺牲准确性的情况下智能地跳过层，我们发明了一种先验-后验跳过引导机制，用于确定何时启动层跳过。我们还提出了一种跳过感知的两阶段知识蒸馏算法，以高效地将标准 VLA 训练为 DySL-VLA。我们的实验表明，在 Calvin 数据集上，DySL-VLA 的成功长度比 Deer-VLA 提高了 2.1%，同时可训练参数减少了 85.7 倍，并且在等精度条件下相对于 RoboFlamingo 基准实现了 3.75 倍的加速。我们的代码可在 https://github.com/PKU-SEC-Lab/DYSL_VLA 获取。

## Abstract
Vision-Language-Action (VLA) models have shown remarkable success in robotic tasks like manipulation by fusing a language model's reasoning with a vision model's 3D understanding. However, their high computational cost remains a major obstacle for real-world applications that require real-time performance. We observe that the actions within a task have varying levels of importance: critical steps demand high precision, while less important ones can tolerate more variance. Leveraging this insight, we propose DySL-VLA, a novel framework that addresses computational cost by dynamically skipping VLA layers based on each action's importance. DySL-VLA categorizes its layers into two types: informative layers, which are consistently executed, and incremental layers, which can be selectively skipped. To intelligently skip layers without sacrificing accuracy, we invent a prior-post skipping guidance mechanism to determine when to initiate layer-skipping. We also propose a skip-aware two-stage knowledge distillation algorithm to efficiently train a standard VLA into a DySL-VLA. Our experiments indicate that DySL-VLA achieves 2.1% improvement in success length over Deer-VLA on the Calvin dataset, while simultaneously reducing trainable parameters by a factor of 85.7 and providing a 3.75x speedup relative to the RoboFlamingo baseline at iso-accuracy. Our code is available on https://github.com/PKU-SEC-Lab/DYSL_VLA.

---

## 论文详细总结（自动生成）

以下是对论文《DySL-VLA: Efficient Vision-Language-Action Model Inference via Dynamic-Static Layer-Skipping for Robot Manipulation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉-语言-动作（VLA）模型虽然在机器人操控中表现出色，但其庞大的大语言模型（LLM）主干导致计算开销巨大、推理延迟高（如 RT-2 仅 1-3 Hz），难以满足机器人实时物理交互（通常需 20-50 Hz）的需求。
*   **研究动机**：作者观察到机器人任务中不同动作的重要性不同（如“抓取”比“接近”更关键），且 VLA 模型不同层对预测的贡献不均。现有加速方法（如剪枝、量化、早期退出）往往对所有动作一视同仁，或在跳层时损失过多关键信息。

### 2. 方法论
DySL-VLA 提出了一种**动静态跳层推理框架**，核心技术包括：
*   **动态-静态层划分**：通过分析层间激活值的余弦相似度，将改变激活分布明显的层设为“静态层”（始终执行），其余设为“动态层”（可跳过）。
*   **先验-后验跳层指导机制**：
    *   **先验预测**：定义“轨迹连续性”指标，通过前 $k$ 步动作的差异判断当前是否处于关键阶段。若连续性下降（暗示进入关键动作），则前移“允许跳过点”，强制执行更多层。
    *   **后验验证**：若检测到动作突变，则重新进行无跳层的完整推理，以确保关键动作的绝对精度。
*   **跳层感知两阶段知识蒸馏**：
    *   **第一阶段**：仅训练轻量级适配器（Adapters），使其能够总结并拟合被跳过层的输出。
    *   **第二阶段**：联合训练跳层控制器（Controllers）和适配器，利用调和衰减概率选择训练层，解决随机初始化导致的收敛难问题。

### 3. 实验设计
*   **数据集/场景**：
    *   **CALVIN**：长程机器人操控基准，评估平均成功长度（0-5）。
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，评估成功率（SR）。
*   **对比方法**：
    *   全量模型：RoboFlamingo (3B), OpenVLA-oft (7B)。
    *   加速/高效模型：DeeR-VLA（早期退出）、FlexiDepth（自适应跳层）、SparseVLM（剪枝）等。
*   **评估指标**：任务成功率/长度、推理延迟（Latency）、可训练参数量、训练步数。

### 4. 资源与算力
*   **硬件**：训练使用 RTX 4090 (Calvin) 和 A6000 (LIBERO)；推理测试包含服务器端及嵌入式端 **NVIDIA Jetson Orin**。
*   **训练开销**：在 Calvin 任务上，DySL-VLA 仅需 **37 GPU 小时**，而对比方法 DeeR-VLA 需 112 小时。
*   **参数量**：可训练参数显著减少。例如在 3B 模型上仅需训练 14M 参数，比 DeeR-VLA（1.2B）减少了约 **85.7 倍**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流基准数据集的多个子任务，进行了大量的对比实验。
*   **消融实验**：针对先验预测、后验验证、两阶段蒸馏、静态层比例、轨迹长度 $k$ 等关键变量进行了详尽的消融研究（约 7 组主要消融实验）。
*   **充分性评价**：实验设计较为全面，不仅对比了算法性能，还深入分析了延迟构成和硬件实测表现，客观地展示了该方法在精度与速度之间的权衡。

### 6. 主要结论与发现
*   **性能提升**：在 Calvin 数据集上，DySL-VLA 的成功长度比 DeeR-VLA 提高 2.1%，同时推理速度提升 1.42 倍。
*   **高效加速**：相比原始 RoboFlamingo 模型，实现了 **3.75 倍** 的加速；在 Jetson Orin 上，OpenVLA-oft 的控制频率可提升至 **23.2 Hz**（结合 Action Chunking）。
*   **训练效率**：通过冻结 LLM 主干仅训练适配器，训练步数减少了 13.7 倍，极大地降低了微调成本。

### 7. 优点
*   **动作感知**：巧妙利用机器人动作的连续性作为跳层依据，符合物理操作的直觉。
*   **信息保留**：通过保留“静态层”而非简单的早期退出，避免了深层语义信息的彻底丢失。
*   **部署友好**：采用适配器和控制器结构，无需定制化算子（如剪枝/量化所需的特殊内核），易于在标准硬件上部署。
*   **极低训练成本**：参数量和训练时间的巨大优势使其更易于迁移到新场景。

### 8. 不足与局限
*   **启发式阈值**：连续性指标的阈值 $\eta$ 和轨迹长度 $k$ 可能需要针对不同机器人硬件或任务进行手动调优。
*   **极端情况风险**：虽然有后验验证，但在极高动态或不可预测的环境中，第一步关键动作的误判仍可能导致任务失败。
*   **依赖基础模型**：其性能上限仍受限于预训练 VLA 模型的原始能力。

（完）
