---
title: "LiteVLA-Edge: Quantized On-Device Multimodal Control for Embedded Robotics"
title_zh: LiteVLA-Edge：面向嵌入式机器人的量化端侧多模态控制
authors: "Justin Williams, Kishor Datta Gupta, Roy George, Mrinmoy Sarkar"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03380v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于嵌入式机器人的量化VLA模型
tldr: LiteVLA-Edge 针对嵌入式机器人部署难题，提出了一种在 Jetson Orin 硬件上实现全本地推理的视觉-语言-动作（VLA）模型流水线。该方法通过 FP32 监督微调、4位 GGUF 量化以及 llama.cpp GPU 加速推理，在 ROS 2 环境下实现了约 6.6Hz 的低延迟响应。该研究为嵌入式硬件上的紧凑型多模态控制提供了实用的系统路径和性能基准。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型因计算需求高和推理延迟大，难以在资源受限的嵌入式机器人硬件上直接部署。
method: 结合了 FP32 图像到动作的监督微调、4位 GGUF 后训练量化，并利用 llama.cpp 在 Jetson Orin 上进行 GPU 加速推理。
result: 在 ROS 2 集成环境下实现了 150.5 毫秒（约 6.6Hz）的端到端平均延迟，支持完全离线的实时控制。
conclusion: 该研究证明了在嵌入式硬件上运行反应式语言条件控制模型的可行性，并为未来端侧 VLA 评估提供了可复现的基准。
---

## 摘要
视觉-语言-动作 (VLA) 模型为感知、语言调节和动作生成提供了一个统一框架，但由于计算需求和推理延迟，许多现有系统仍难以部署在嵌入式机器人环境中。在本文中，我们提出了 LiteVLA-Edge，这是一个面向部署的 VLA 流水线，旨在 Jetson Orin 级硬件上实现完全端侧推理。我们的方法结合了 FP32 精度下的监督式图像到动作微调、训练后 4 位 GGUF 量化，以及通过 llama.cpp 运行时实现的 GPU 加速推理。在我们的部署配置下，LiteVLA-Edge 在完全离线运行于集成 ROS 2 的感知-推理-动作流水线中时，实现了 150.5 ms（约 6.6 Hz）的平均端到端延迟。我们的贡献并非引入新的策略目标，而是为在嵌入式硬件上本地执行紧凑型多模态控制模型提供了一条实际的系统路径，同时保留了感知、推理和执行之间的模块化接口。这些结果确立了反应式语言调节控制的时间可行性，并为未来机器人端侧 VLA 的任务级评估提供了一个可复现的基准。

## Abstract
Vision-Language-Action (VLA) models provide a unified framework for perception, language conditioning, and action generation, but many existing systems remain difficult to deploy in embedded robotic settings because of their computational requirements and inference latency. In this paper, we present LiteVLA-Edge, a deployment-oriented VLA pipeline for fully on-device inference on Jetson Orin-class hardware. Our approach combines supervised image-to-action fine-tuning in FP32 with post-training 4-bit GGUF quantization and GPU-accelerated inference through the \texttt{llama.cpp} runtime. Under our deployment configuration, LiteVLA-Edge achieves a mean end-to-end latency of 150.5\,ms (approximately 6.6\,Hz) while operating entirely offline within a ROS~2-integrated perception--reasoning--action pipeline. Rather than introducing a new policy objective, our contribution is a practical systems path for executing compact multimodal control models locally on embedded hardware while preserving modular interfaces between perception, reasoning, and actuation. These results establish timing feasibility for reactive language-conditioned control and provide a reproducible baseline for future task-level evaluation of on-device VLAs in robotics.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LiteVLA-Edge** 的系统，旨在解决视觉-语言-动作（VLA）模型在嵌入式机器人硬件上部署时面临的高延迟和高计算需求问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的 VLA 模型（如 RT-2, OpenVLA）虽然泛化能力强，但参数量巨大（通常 >7B），依赖云端或高性能桌面 GPU，无法满足嵌入式机器人对低功耗、低延迟及离线运行的严苛要求。
*   **核心问题**：如何在资源受限的边缘设备（如 NVIDIA Jetson）上实现能够进行“闭环控制”（即反应速度足够快，能实时修正动作）的多模态模型推理。
*   **整体含义**：LiteVLA-Edge 提供了一套实用的系统流水线，将 VLA 推理延迟降低到 150.5 毫秒（约 6.6 Hz），使机器人能够从“停顿思考”转向“实时反应”。

### 2. 方法论
*   **核心思想**：通过“高精度微调 + 激进量化 + 硬件加速运行时”的组合，在不牺牲语义推理能力的前提下压缩模型。
*   **关键技术细节**：
    *   **骨干网络**：采用轻量级的 **SmolVLM-256M**（蒸馏版）作为多模态基础。
    *   **监督微调 (SFT)**：在 FP32 精度下使用 **LoRA**（秩 r=8）进行图像到动作的映射训练，确保动作指令的精确性。
    *   **量化压缩**：训练后将模型转换为 **GGUF 格式**，并进行 **4位 (Q4_K_M) 量化**，显著减少内存占用和总线带宽压力。
    *   **推理加速**：利用 **llama.cpp** 库及其 CUDA 后端，将 42 层 Transformer 全部卸载到 Jetson Orin 的 GPU 上。
    *   **系统集成**：通过 **ROS 2** 节点发布 `geometry_msgs/Twist` 速度指令，实现感知-推理-动作的模块化解耦。

### 3. 实验设计
*   **硬件环境**：主要在 **NVIDIA Jetson AGX Orin (64GB)** 上进行评估，对比了 Raspberry Pi 4 等低端平台。
*   **对比对象**：
    *   **通用 VLA**：OpenVLA (7B)。
    *   **边缘优化 VLA**：EdgeVLA, Efficient VLA。
    *   **紧凑型 VLM**：Moondream2, TinyLLaVA, Qwen2-VL, PaliGemma（这些仅具备推理能力，不直接生成动作）。
*   **评估指标**：端到端推理延迟（ms）、推理频率（Hz）、延迟抖动（标准差 σ）。

### 4. 资源与算力
*   **推理算力**：使用了 **NVIDIA Jetson AGX Orin**，功耗配置在 40W 级别。
*   **训练算力**：文中提到在 FP32 下进行 LoRA 微调，但**未明确说明**具体使用的训练 GPU 型号、数量及训练总时长。
*   **部署配置**：上下文窗口设为 512，最大输出限制为 12 个 token，以最小化 KV 缓存开销。

### 5. 实验数量与充分性
*   **实验规模**：进行了 **300 次** 连续推理运行以测量延迟稳定性。
*   **充分性评价**：
    *   在**部署性能**（延迟、抖动、频率）方面的实验非常充分，提供了详尽的统计数据（均值、最值、标准差）。
    *   在**对比维度**上，涵盖了当前主流的轻量级多模态模型，对比具有客观性。
    *   **不足之处**：缺乏在多种不同物理机器人任务上的“成功率”对比实验，更多侧重于系统工程层面的可行性验证。

### 6. 主要结论与发现
*   **性能突破**：实现了 **150.5 ms** 的平均延迟，比之前的基准提升了约 **220%**。
*   **闭环可行性**：6.6 Hz 的频率达到了反应式视觉伺服控制的入门阈值，允许机器人在运动中根据视觉反馈修正轨迹。
*   **量化稳定性**：证明了 4 位量化在 SmolVLM-256M 架构下不会导致严重的“动作漂移”，且延迟抖动极低（σ < 0.2 ms），保证了 ROS 2 控制心跳的确定性。

### 7. 优点
*   **实用性极强**：提供了一条清晰的从模型训练到嵌入式 ROS 2 部署的路径，而非纯理论研究。
*   **全本地化**：不依赖云端，适用于国防、搜救等网络受限环境。
*   **低抖动**：极低的延迟标准差对于保持机器人控制系统的稳定性至关重要。
*   **开源兼容**：使用 GGUF 和 llama.cpp，具有良好的跨平台潜力。

### 8. 不足与局限
*   **任务评估缺失**：论文主要关注“推理速度”，未提供在复杂操作任务（如 Open X-Embodiment 数据集任务）中的具体成功率数据。
*   **硬件依赖**：虽然针对边缘端，但 6.6 Hz 的表现仍高度依赖 Jetson Orin 这一类高性能边缘模块，在更低端的 CPU 平台上可能仍无法达到实时性。
*   **泛化性验证**：由于模型参数量仅为 256M，其在处理未见过的复杂指令或场景时的泛化能力可能弱于 7B 规模的模型。

（完）
