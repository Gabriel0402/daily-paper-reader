---
title: Unleashing VLA Potentials in Autonomous Driving via Explicit Learning from Failures
title_zh: 通过显式学习失败经验释放自动驾驶中 VLA 模型的潜力
authors: "Yuechen Luo, Qimao Chen, Fang Li, Shaoqing Xu, Jaxin Liu, Ziying Song, Zhi-xin Yang, Fuxi Wen"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01063v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 通过强化学习优化的自动驾驶 VLA 模型
tldr: 针对自动驾驶中VLA模型在强化学习阶段因奖励稀疏和长尾场景探索受限导致的性能瓶颈，本文提出ELF-VLA框架。该框架通过引入结构化诊断反馈，将模糊的标量奖励转化为可解释的失败分析报告，并利用反馈引导的精炼机制生成高质量修正样本，从而有效解决了复杂场景下的规划难题，在NAVSIM基准测试中达到SOTA水平。
selection_source: fresh_fetch
motivation: 现有的VLA模型在强化学习中面临奖励信号稀疏且无法定位失败根源的问题，限制了其在长尾场景下的性能提升。
method: 提出ELF-VLA框架，通过生成详细的失败诊断报告并利用反馈引导策略修正，将高质量修正样本注入强化学习训练。
result: 在NAVSIM基准测试中，该方法在PDMS、EPDMS评分及高层规划准确率上均取得了当前最先进的性能。
conclusion: 通过显式学习失败原因并引入针对性反馈，可以有效释放VLA模型在复杂自动驾驶任务中的潜力。
---

## 摘要
自动驾驶中的视觉-语言-动作（VLA）模型在强化学习（RL）优化过程中经常遇到性能瓶颈。这种停滞源于受限于先前监督微调（SFT）的探索能力，导致在长尾场景中持续失败。在这些关键场景中，所有探索的动作都产生零值的驾驶得分。这种信息稀疏的奖励信号预示着失败，但未能识别其根本原因——是由于规划错误、推理缺陷还是轨迹执行不力。为了解决这一局限性，我们提出了通过显式学习失败经验的 VLA（ELF-VLA），这是一个利用结构化诊断反馈增强强化学习的框架。我们的方法不再依赖模糊的标量奖励，而是生成详细且可解释的报告，以识别具体的失败模式。随后，VLA 策略利用这种显式反馈来生成反馈引导的优化。通过将这些修正后的高奖励样本注入回强化学习训练批次中，我们的方法提供了有针对性的梯度，使策略能够解决无引导探索无法处理的关键场景。大量实验表明，我们的方法释放了 VLA 模型的潜在能力，在公开的 NAVSIM 基准测试中，在综合 PDMS、EPDMS 评分和高层规划准确性方面均达到了最先进（SOTA）的性能。

## Abstract
Vision-Language-Action (VLA) models for autonomous driving often hit a performance plateau during Reinforcement Learning (RL) optimization. This stagnation arises from exploration capabilities constrained by previous Supervised Fine-Tuning (SFT), leading to persistent failures in long-tail scenarios. In these critical situations, all explored actions yield a zero-value driving score. This information-sparse reward signals a failure, yet fails to identify its root cause -- whether it is due to incorrect planning, flawed reasoning, or poor trajectory execution. To address this limitation, we propose VLA with Explicit Learning from Failures (ELF-VLA), a framework that augments RL with structured diagnostic feedback. Instead of relying on a vague scalar reward, our method produces detailed, interpretable reports that identify the specific failure mode. The VLA policy then leverages this explicit feedback to generate a Feedback-Guided Refinement. By injecting these corrected, high-reward samples back into the RL training batch, our approach provides a targeted gradient, which enables the policy to solve critical scenarios that unguided exploration cannot. Extensive experiments demonstrate that our method unlocks the latent capabilities of VLA models, achieving state-of-the-art (SOTA) performance on the public NAVSIM benchmark for overall PDMS, EPDMS score and high-level planning accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：通过显式学习失败经验释放自动驾驶中 VLA 模型的潜力 (ELF-VLA)

#### 1. 核心问题与整体含义（研究动机和背景）
当前的自动驾驶视觉-语言-动作（VLA）模型在通过强化学习（RL）进行优化时，往往会遇到**性能瓶颈（Performance Plateau）**。
*   **探索受限**：模型在监督微调（SFT）阶段主要学习常见场景，导致在处理复杂、长尾的关键场景（如无保护左转、紧急避让）时探索能力不足。
*   **奖励稀疏且模糊**：现有的 RL 训练通常只依赖单一的标量奖励（如 PDMS 得分）。当模型失败时，这种低奖励信号无法告知模型失败的具体原因（是高层规划错误、逻辑推理缺陷，还是底层轨迹执行不力），导致模型难以从失败中有效学习。

#### 2. 方法论：核心思想与关键技术
本文提出了 **ELF-VLA** 框架，核心思想是将**结构化的诊断反馈**引入 RL 过程，使模型能够“显式地从失败中学习”。

*   **两阶段 SFT 训练**：
    1.  **认知阶段**：在海量驾驶知识问答数据集上预训练，增强模型对交通语义的理解。
    2.  **精炼阶段**：在混合了“基础输入”和“反馈输入”的数据集上微调，使模型同时具备轨迹预测和基于反馈进行修正的能力。
*   **带反馈的 GRPO 强化学习**：
    *   **高效样本筛选**：通过 SFT 模型预采样，过滤掉已掌握的简单场景，集中攻克低得分（困难）和高方差（模糊）的样本。
    *   **教师模型诊断**：当 VLA 模型在探索中持续失败时，引入强大的教师模型（如 Qwen3-VL-32B）生成结构化报告，包含元动作分析、思维过程分析、安全/效率失败分析及行动修正建议。
    *   **反馈引导精炼与重注入**：VLA 模型根据诊断报告生成修正后的高奖励轨迹，并将这些样本重新注入 GRPO 训练批次，提供明确的梯度导向。
    *   **策略塑造（Policy Shaping）**：引入技术手段处理反馈样本与原始输入之间的概率失配，防止训练崩溃并提高学习效率。

#### 3. 实验设计
*   **数据集与场景**：主要使用 **NAVSIM**（数据驱动的非反应式自动驾驶模拟基准），涵盖了大量的真实驾驶场景。
*   **Benchmark（基准指标）**：
    *   **PDMS (NAVSIM v1)**：综合考虑碰撞、道路合规性、进度和舒适度的闭环得分。
    *   **EPDMS (NAVSIM v2)**：更全面的扩展评分指标。
    *   **高层规划准确率**：评估纵向速度（加速/减速等）和横向路径（左转/右转等）的决策精度。
*   **对比方法**：包括传统的模块化方法（UniAD）、端到端模型（TransFuser, DiffusionDrive）以及最新的 VLA 模型（AutoVLA, DriveVLA）。

#### 4. 资源与算力
论文明确列出了算力消耗：
*   **硬件**：使用了 **32 块 NVIDIA H20 GPU**。
*   **训练时长**：
    *   第一阶段（预训练）：16 块 GPU，约 2 天。
    *   第二阶段（SFT）：16 块 GPU，约 8 小时。
    *   第三阶段（RL）：32 块 GPU，约 18 小时。
*   **推理效率**：通过 vLLM 加速，生成思维链（CoT）和轨迹的延迟约为 **0.1s**。

#### 5. 实验数量与充分性
*   **实验规模**：在 NAVSIM v1 和 v2 两个版本上进行了全面测试，并对比了超过 10 种 SOTA 方法。
*   **消融实验**：针对 RL 训练数据量、反馈机制（k 值选择）、策略塑造（PS）的作用、反馈触发阈值 $s$ 以及训练阶段的贡献度进行了详细的消融研究。
*   **客观性与公平性**：实验涵盖了从感知到决策的多个维度，通过可视化展示了模型从“错误轨迹”到“修正轨迹”的演变过程，验证了反馈机制的有效性。

#### 6. 主要结论与发现
*   **性能突破**：ELF-VLA 在 NAVSIM v1 上达到了 **91.0 PDMS**，在 v2 上达到了 **87.1 EPDMS**，均创下了纯视觉方案的新纪录（SOTA）。
*   **解决失败瓶颈**：相比传统 GRPO，ELF-VLA 将“全失败样本率”（即所有尝试均失败的比例）从 2.73% 显著降低至 1.08%。
*   **高层决策增强**：在决策准确率上显著优于参数量更大的通用模型（如 Qwen2.5-VL-72B），证明了领域特定反馈的价值。

#### 7. 优点（亮点）
*   **可解释性强**：将失败原因从模糊的数字转化为结构化的文本诊断，符合人类学习逻辑。
*   **闭环修正**：不仅指出错误，还通过“反馈-精炼-重注入”形成闭环，解决了 RL 探索效率低下的问题。
*   **数据高效**：通过难例筛选策略，仅用 24k 核心数据就达到了优于全量 85k 数据的效果。

#### 8. 不足与局限
*   **教师模型依赖**：学生模型（VLA）的性能上限在很大程度上受限于教师模型的诊断水平。
*   **模拟器局限**：实验主要在非反应式模拟器（NAVSIM）中进行，尚未在具有动态交互反应的复杂闭环环境（如 CARLA 或真实路测）中充分验证。
*   **计算开销**：虽然推理较快，但训练阶段需要调用大型教师模型生成反馈，增加了离线训练的计算成本。

（完）
