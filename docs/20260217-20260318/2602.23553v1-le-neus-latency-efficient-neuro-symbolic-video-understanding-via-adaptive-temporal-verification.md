---
title: "LE-NeuS: Latency-Efficient Neuro-Symbolic Video Understanding via Adaptive Temporal Verification"
title_zh: LE-NeuS：通过自适应时序验证实现延迟高效的神经符号视频理解
authors: "Shawn Liang, Sahil Shah, Chengwei Zhou, SP Sharan, Harsh Goel, Arnab Sanyal, Sandeep Chinchali, Gourav Datta"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23553v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 时态逻辑引导的视觉语言模型视频理解
tldr: 针对长视频问答（LVQA）中神经符号方法推理延迟过高的问题，本文提出LE-NeuS框架。该框架通过CLIP引导的两阶段自适应采样减少视觉冗余，并利用批处理命题检测实现并行推理。在保持高准确率的同时，将推理延迟从基准的90倍降低至约10倍，显著提升了长视频理解的效率，使其更适用于延迟敏感的应用场景。
selection_source: fresh_fetch
motivation: 现有的神经符号视频理解方法因密集的序列化命题检测导致推理延迟极高，限制了其在实际边缘设备上的部署。
method: 采用CLIP引导的两阶段自适应采样来跳过冗余帧，并结合批处理命题检测技术实现VLM推理的并行化。
result: "在多个基准测试中，LE-NeuS将推理延迟从基准的90倍大幅降至10倍，同时在复杂查询上保持了10%以上的准确率优势。"
conclusion: 该研究通过算法优化成功平衡了神经符号视频理解的精度与效率，为实时长视频分析提供了可行的技术方案。
---

## 摘要
长视频问答（LVQA）的神经符号方法通过将时序推理建立在形式化验证的基础上，展示了显著的准确率提升。然而，现有方法会产生极高的延迟开销，比基础视觉语言模型（VLM）提示慢多达 90 倍，使其在延迟敏感的边缘部署中变得不切实际。我们提出了 LE-NeuS，这是一个延迟高效的神经符号框架，它在保留时序逻辑引导的视频理解准确性优势的同时，大幅降低了推理延迟。我们的核心见解是，主要的计算瓶颈源于自动机构建过程中跨视频帧的顺序且密集的命题检测。我们通过两项原则性优化来解决这一问题：(1) CLIP 引导的两阶段自适应采样，利用视觉冗余跳过语义相似的帧，同时保留时序边界；(2) 批处理命题检测，在时序窗口内并行化 VLM 推理。在理论上，我们推导出了作为视频长度、命题复杂度和采样密度函数的延迟界限，确立了实现延迟效率的条件。在实验上，在部署于 NVIDIA H100 GPU 的 LongVideoBench 和 Video-MME 基准测试中，LE-NeuS 将延迟差距从 90 倍缩小到约 10 倍，同时在时序复杂的查询中保持了超过 10% 的准确率提升。

## Abstract
Neuro-symbolic approaches to long-form video question answering (LVQA) have demonstrated significant accuracy improvements by grounding temporal reasoning in formal verification. However, existing methods incur prohibitive latency overheads, up to 90x slower than base VLM prompting, rendering them impractical for latency-sensitive edge deployments. We present LE-NeuS, a latency-efficient neuro-symbolic framework that preserves the accuracy benefits of temporal logic-guided video understanding while drastically reducing inference latency. Our key insight is that the dominant computational bottleneck arises from sequential and dense proposition detection across video frames during automaton construction. We address this through two principled optimizations: (1) CLIP guided two-stage adaptive sampling that exploits visual redundancy to skip semantically similar frames while preserving temporal boundaries, and (2) batched proposition detection that parallelizes VLM inference across temporal windows. Theoretically, we derive latency bounds as a function of video length, proposition complexity, and sampling density, establishing conditions under which latency efficiency is achievable. Empirically, on LongVideoBench and Video-MME benchmarks deployed on NVIDIA H100 GPUs, LE-NeuS reduces the latency gap from 90x to approximately 10x while maintaining >10% accuracy gains on temporally complex queries.

---

## 论文详细总结（自动生成）

以下是对论文《LE-NeuS: Latency-Efficient Neuro-Symbolic Video Understanding via Adaptive Temporal Verification》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：长视频问答（LVQA）要求模型具备语义对齐、长程时序推理和组合推理能力。现有的神经符号（Neuro-Symbolic）方法（如 NeuS-QA）虽然通过时序逻辑（TL）和形式化验证显著提升了准确率，但其**推理延迟极高**（比基础 VLM 慢约 90 倍），主要瓶颈在于自动机构建过程中对视频帧进行的密集且顺序的命题检测。
*   **研究背景**：传统的均匀采样会丢失关键时序细节，而启发式检索缺乏逻辑保证。神经符号方法虽好，但因计算开销巨大，难以在边缘设备或实时场景中部署。

### 2. 方法论
LE-NeuS 提出了一个延迟高效的框架，核心思想是利用视频的语义稀疏性和视觉冗余来减少不必要的计算。
*   **CLIP 引导的两阶段自适应采样**：
    *   **阶段 1：语义相关性过滤**。利用轻量级的 CLIP 模型计算视频帧与查询命题之间的相似度，剔除无关背景，仅保留潜在相关的候选片段。
    *   **阶段 2：视觉冗余消除**。在候选片段中，通过计算相邻帧的视觉嵌入相似度，跳过视觉上几乎重复的帧，仅保留关键帧（Keyframes）。
*   **批处理命题检测（Batched Proposition Detection）**：
    *   改变了以往对每个“窗口-命题”对进行单独推理的模式，将多个命题在同一视觉上下文下进行批处理并行推理，充分利用 GPU 吞吐量并减少视觉编码器的重复计算。
*   **多段感兴趣帧（Multi-Segment FoI）检索**：
    *   不同于以往只提取单个连续窗口的做法，LE-NeuS 根据模型检测结果提取多个不连续的逻辑一致片段，从而在有限的 VLM 上下文窗口内集中更多有效证据。
*   **形式化分析**：论文推导了延迟上界公式，证明了延迟效率取决于采样密度（$\alpha$）和关键帧保留率（$\rho$）的乘积。

### 3. 实验设计
*   **数据集**：
    *   **LongVideoBench**：主要评估基准，涵盖长达 60 分钟的视频，侧重时序排序、因果推理等。
    *   **Video-MME**：侧重时序推理类别。
    *   **MLVU**：包含 Needle QA（长上下文检索）和 Ego Reasoning（动作序列理解）。
*   **对比方法**：
    *   **基础 VLM**：InternVL2.5-8B, Qwen2.5-VL-7B, VideoLLaMA3-7B, LLaVA-Video-7B（采用均匀采样）。
    *   **神经符号基准**：原始 NeuS-QA。
    *   **结构化推理框架**：VideoTree, VideoAgent, T*, LVNet 等。

### 4. 资源与算力
*   **硬件环境**：所有实验和延迟测量均在单张 **NVIDIA H100 GPU** 上完成。
*   **模型细节**：使用 InternVL2-8B 作为命题检测器，GPT-4o 用于查询到时序逻辑（LQ2TL）的翻译，CLIP ViT-B/32 用于自适应采样。
*   **耗时对比**：全局平均延迟从 NeuS-QA 的约 553.68 秒降低至 LE-NeuS 的 **44.20 秒**。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流 VLM 后端上进行了测试，涵盖了从短视频（15s）到长视频（1小时）的不同跨度。
*   **消融实验**：详细分析了批处理检测、自适应采样和多段检索对准确率和延迟的贡献。
*   **充分性评价**：实验设计较为充分，不仅对比了准确率，还系统地分析了不同视频长度下的延迟缩放趋势。通过在多个基准和多个模型后端上的表现，证明了该方法的通用性和鲁棒性。

### 6. 主要结论与发现
*   **显著提速**：LE-NeuS 实现了 **12.53 倍** 的全局平均加速，在长视频（1小时）场景下加速比达到 **13.66 倍**。
*   **准确率提升**：在 LongVideoBench 上，LE-NeuS 比 NeuS-QA 基准提升了约 **5.21%** 的准确率，比表现最好的结构化推理框架（VideoTree）高出 16% 以上。
*   **效率平衡**：成功将神经符号方法的延迟开销从 90 倍缩小到约 10 倍（相对于基础 VLM 采样），证明了逻辑引导的推理可以在不牺牲实用性的情况下实现。

### 7. 优点
*   **理论与实践结合**：不仅提供了经验性的加速，还给出了形式化的延迟界限分析。
*   **即插即用**：该框架不依赖于特定的 VLM 后端，具有很强的迁移性。
*   **多段检索优化**：通过非连续片段检索，解决了长视频中证据稀疏的问题，提高了 VLM 的信息密度。

### 8. 不足与局限
*   **延迟仍高于基础采样**：尽管提速显著，但 10 倍于基础 VLM 的延迟对于某些极度实时的边缘应用（如毫秒级响应）仍有挑战。
*   **依赖前端模型质量**：系统的性能高度依赖于 CLIP 的过滤效果和 LLM 对时序逻辑翻译的准确性，若前端翻译错误，后续验证将失效。
*   **计算开销波动**：延迟减少的程度取决于视频内容的复杂度和查询命题的密度，对于极度复杂的视频，加速效果可能会有所下降。

（完）
