---
title: Multi-Agent Temporal Logic Planning via Penalty Functions and Block-Coordinate Optimization
title_zh: 基于惩罚函数与块坐标优化的多智能体时序逻辑规划
authors: "Eleftherios E. Vlahakis, Arash Bahari Kordabad, Lars Lindemann, Pantelis Sopasakis, Sadegh Soudjani, Dimos V. Dimarogonas"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17434v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 信号时态逻辑(STL)下的多智能体规划
tldr: 针对多智能体信号时序逻辑（STL）规划中因高维性导致的计算难题，本文提出一种基于惩罚函数和块坐标下降（BCGD）的优化框架。该方法将STL规划转化为无约束松弛问题，通过交替优化各智能体变量降低复杂度，并结合双层优化方案确保任务满足。实验证明该框架在复杂多机器人场景下具有良好的可扩展性和收敛性。
selection_source: fresh_fetch
motivation: 传统多智能体STL规划在处理协作任务时面临严重的维度灾难，难以实现具有满足保证的可扩展合成。
method: 提出一种基于平滑STL语义的二次惩罚函数，并利用块坐标梯度下降法（BCGD）在双层优化架构中交替求解各智能体的决策变量。
result: 理论证明了算法能收敛至惩罚问题的驻点，并通过多机器人仿真验证了其在处理复杂约束时的计算效率和可扩展性。
conclusion: 该框架为大规模多智能体系统的时序逻辑规划提供了一种高效且具有理论保障的分布式优化解决方案。
---

## 摘要
在信号时序逻辑（STL）下的多智能体规划常受限于协作任务，由于问题固有的高维性，这些任务带来了计算挑战，阻碍了具有满足性保证的可扩展综合。为解决这一问题，我们将 STL 规划建模为任意多智能体约束下的优化程序，并引入了一种基于惩罚的无约束松弛方法。该方法可以通过块坐标梯度下降（BCGD）法高效求解，其中每个块对应单个智能体的决策变量，从而降低了复杂性。通过利用基于平滑 STL 语义定义的二次惩罚函数，我们证明了在标准正则性假设下，BCGD 迭代收敛至惩罚问题的驻点。为了强制执行可行性，BCGD 求解器被嵌入到一个双层优化方案中：在固定惩罚参数下执行内部 BCGD 更新，然后在外部循环中增加该参数，以逐步提高多智能体 STL 的鲁棒性。所提出的框架实现了可扩展计算，并通过各种复杂的多机器人规划场景进行了验证。

## Abstract
Multi-agent planning under Signal Temporal Logic (STL) is often hindered by collaborative tasks that lead to computational challenges due to the inherent high-dimensionality of the problem, preventing scalable synthesis with satisfaction guarantees. To address this, we formulate STL planning as an optimization program under arbitrary multi-agent constraints and introduce a penalty-based unconstrained relaxation that can be efficiently solved via a Block-Coordinate Gradient Descent (BCGD) method, where each block corresponds to a single agent's decision variables, thereby mitigating complexity. By utilizing a quadratic penalty function defined via smooth STL semantics, we show that BCGD iterations converge to a stationary point of the penalized problem under standard regularity assumptions. To enforce feasibility, the BCGD solver is embedded within a two-layer optimization scheme: inner BCGD updates are performed for a fixed penalty parameter, which is then increased in an outer loop to progressively improve multi-agent STL robustness. The proposed framework enables scalable computations and is validated through various complex multi-robot planning scenarios.

---

## 论文详细总结（自动生成）

### 论文总结：基于惩罚函数与块坐标优化的多智能体时序逻辑规划

#### 1. 核心问题与研究动机
*   **核心问题**：如何在保证满足复杂协作任务的前提下，实现大规模多智能体系统（MAS）的高效运动规划。
*   **研究动机**：
    *   **高维挑战**：多智能体系统在处理信号时序逻辑（STL）约束时，由于状态空间和约束耦合的爆炸式增长，传统方法（如混合整数线性规划 MILP）难以扩展。
    *   **现有方法局限**：现有的分布式或去中心化方法通常仅限于 STL 的特定子集，或者依赖于强假设（如初始解必须可行），缺乏处理复杂协作任务的通用性和形式化保证。
    *   **平滑优化的潜力**：利用平滑 STL 语义将逻辑约束转化为连续优化问题，为引入高效的大规模优化算法（如块坐标下降）提供了可能。

#### 2. 方法论
该论文提出了一种名为 **BCGD-PM** 的双层优化框架，核心步骤如下：
*   **平滑 STL 语义**：使用 log-sum-exp 和 softmax 函数对 STL 的非平滑算子（min/max）进行下近似，将逻辑公式转化为可微的鲁棒性函数 $\varrho_\phi^\Gamma$。
*   **惩罚函数构造**：将带约束的规划问题转化为无约束优化问题。引入二次惩罚项 $R(u) = \max\{0, -\varrho_\phi^\Gamma(u)\}^2$，当不满足 STL 约束时施加惩罚。
*   **块坐标梯度下降（BCGD）**：
    *   **核心思想**：将所有智能体的决策变量划分为多个“块”（每个块对应一个智能体）。
    *   **局部更新**：在每次迭代中，仅针对选定的智能体块求解子问题，利用成本函数的解耦性实现并行或序列化的高效计算。
    *   **二阶近似**：在子问题中使用 Hessian 矩阵的对角块近似，确保计算开销保持在智能体级别。
*   **双层优化方案（PM）**：
    *   **内层循环**：在固定惩罚参数 $\lambda$ 下，利用 BCGD 寻找惩罚问题的驻点。
    *   **外层循环**：逐步增大惩罚参数 $\lambda$，迫使解向可行域靠拢，最终满足原始 STL 约束。

#### 3. 实验设计
*   **实验场景**：**10 机器人运动规划问题**。
    *   **R2AM (Reach-Twice-Avoid-Meet)**：机器人需避障、访问采集区、访问交付区，并与特定盟友会面。
    *   **R2AMCA**：在 R2AM 基础上增加了全局智能体间的避障约束。
    *   **RURAMCA**：引入了更复杂的“Until”算子约束（如：必须保持在采集区直到到达交付区）。
*   **Benchmark（基准方法）**：
    *   **LBFGS-PM**：在相同的惩罚框架下，使用有限内存拟牛顿法（LBFGS）作为内层求解器进行对比。
*   **动力学模型**：分别测试了**线性动力学**和非线性的**单轮车（Unicycle）动力学**模型。

#### 4. 资源与算力
*   **软件环境**：使用 Python 编写，利用 **JAX 库**进行自动微分计算。
*   **算力说明**：文中**未明确提及**具体的硬件型号（如 GPU/CPU 核心数）。
*   **运行耗时**：
    *   线性模型下，BCGD 在不同场景耗时约 12s 至 35s。
    *   单轮车模型下，由于非线性增加，耗时显著增长，约 234s 至 480s。

#### 5. 实验数量与充分性
*   **实验规模**：针对 3 种不同复杂度的 STL 任务（R2AM, R2AMCA, RURAMCA）和 2 种动力学模型进行了多组实验。
*   **充分性评价**：
    *   **较为充分**：实验涵盖了从简单到复杂的逻辑约束，且智能体数量（10个）在同类研究中属于较高水平，验证了算法的可扩展性。
    *   **对比客观**：通过与成熟的 LBFGS 算法在同一框架下对比，客观展示了 BCGD 在处理高维块结构问题时的效率优势。

#### 6. 主要结论与发现
*   **收敛性保证**：理论证明了 BCGD 迭代能收敛至惩罚问题的驻点，且在惩罚参数足够大时，解能满足原始 STL 约束。
*   **计算效率**：BCGD 在处理高维多智能体约束时表现出极强的竞争力，尤其是在线性模型下，其运行速度优于 LBFGS。
*   **复杂约束处理能力**：该框架是首个系统性整合平滑 STL、块坐标优化和惩罚函数的方法，能够处理包含“Until”算子和全局避障在内的复杂协作任务。

#### 7. 优点
*   **高可扩展性**：通过块坐标分解，将计算压力分散到单个智能体层面，计算架构不随智能体数量增加而剧烈失效。
*   **通用性强**：支持任意满足 PNF 范式的 STL 公式，不限于特定的逻辑片段。
*   **理论完备**：提供了收敛性证明，并给出了确保可行解的惩罚参数条件。
*   **模块化**：惩罚框架可以灵活嵌入不同的内层求解器。

#### 8. 不足与局限
*   **局部最优风险**：由于 STL 鲁棒性函数通常是非凸的，惩罚方法可能会陷入局部极小值，导致无法找到可行解（尽管实验中表现良好）。
*   **参数敏感性**：惩罚参数的更新速率（$\eta_\lambda$）和初始值对收敛速度和解的质量有较大影响，可能需要针对不同任务进行调优。
*   **实时性限制**：在非线性动力学（如单轮车）下，计算耗时仍达到分钟级，对于高动态环境的实时重规划可能存在挑战。

（完）
