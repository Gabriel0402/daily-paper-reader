---
title: "LLM-X: A Scalable Negotiation-Oriented Exchange for Communication Among Personal LLM Agents"
title_zh: LLM-X：一种面向个人 LLM 智能体间通信的可扩展协商型交换平台
authors: "Giuliano Lorenzoni, Paulo Alencar, Donald Cowan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11376v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 个人LLM智能体之间的多智能体谈判通信
tldr: 本研究提出了LLM-X，一个专为个人大模型代理设计的可扩展协商交换环境，旨在实现代理间的直接结构化通信。与传统的工具中心化协议不同，LLM-X引入了消息总线和路由基座，并提供模式验证与策略执行保障。研究贡献了包含联邦网关和主题路由的架构、类型化消息协议，并首次对大规模多代理协商进行了实证评估。实验证明该系统在不同负载下具有良好的稳定性和性能权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11376v1/fig-001.webp\", \"caption\": \"Figure 1: LLM-X conceptual (negotiation-centric) architecture.\", \"page\": 3, \"index\": 1, \"width\": 1048, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2605.11376v1/fig-002.webp\", \"caption\": \"Figure 2: CNet-style conceptual negotiation sequence in LLM-X. Alice emits a CFP with a deadline; contractors reply with Offers. The Policy Engine governs when and how the winner is selected (Low: first-valid; Medium: bounded wait; High: wait-all). Acks ensure reliable delivery; retries occur on missing acknowledgments.\", \"page\": 3, \"index\": 2, \"width\": 1048, \"height\": 433}]"
motivation: 现有的代理协议主要关注代理与API的交互，缺乏支持个人LLM代理之间直接、结构化且可扩展的协商与协作机制。
method: 提出了一种由联邦网关、主题路由和策略执行组成的LLM-X架构，并设计了支持能力协商和合同网协调的类型化消息协议。
result: 实验显示严格的策略虽能提升系统鲁棒性与公平性，但会增加延迟和消息量，且系统在长时间持续负载下保持稳定。
conclusion: LLM-X为大规模个人代理间的复杂协商提供了稳健的基础设施，并揭示了代理策略与系统性能之间的关键权衡关系。
---

## 摘要
我们提出了个人 LLM 交换平台（LLM-X），这是一个面向协商的可扩展环境，旨在实现代表不同用户的个人智能体（LLM）群体之间的直接、结构化通信。与现有的侧重于智能体与 API 交互的以工具为中心的协议不同，LLM-X 引入了消息总线和路由基座，用于 LLM 之间的协作，并提供模式有效性和策略执行方面的保障。我们的贡献包括：（1）一种由联邦网关、基于主题的路由和策略执行组成的 LLM-X 架构；（2）一种支持能力协商和合同网式协作的有类型消息协议；（3）首次对大规模基于 LLM 的多智能体协商进行的实证评估。实验涵盖了 5、9 和 12 个智能体，在不同的协商策略（低、中、高）以及短期（分钟级）和长期（2小时、12小时）负载条件下进行。结果突显了明显的策略-性能权衡：更严格的策略提高了鲁棒性和公平性，但增加了延迟和消息量。长期运行实验证实，LLM-X 在持续负载下保持稳定，且延迟漂移在有限范围内。

## Abstract
We propose a personal-LLM exchange (LLM-X), a scalable negotiation-oriented environment that enables direct, structured communication across populations of personal agents (LLMs), each representing an individual user. Unlike existing tool-centric protocols that focus on agent-API interaction, LLM-X introduces a message bus and routing substrate for LLM-to-LLM coordination with guarantees around schema validity and policy enforcement. We contribute: (1) an architecture for LLM-X comprising federated gateways, topic-based routing, and policy enforcement; (2) a typed message protocol supporting capability negotiation and contract-net-style coordination; and (3) the first empirical evaluation of LLM-based multi-agent negotiation at scale. Experiments span 5, 9, and 12 agents, under distinct negotiation policies (Low, Medium, High), and across both short-run (minutes) and long-run (2h, 12h) load conditions. Results highlight clear policy-performance trade-offs: stricter policies improve robustness and fairness but increase latencies and message volume. Extended runs confirm that LLM-X remains stable under sustained load, with bounded latency drift.

---

## 论文详细总结（自动生成）

这是一份关于论文《LLM-X: A Scalable Negotiation-Oriented Exchange for Communication Among Personal LLM Agents》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：尽管大语言模型（LLM）已广泛作为个人助手，但它们通常处于孤立状态，缺乏直接的跨用户通信机制。现有的多智能体协议（如 MCP）多侧重于“智能体-API”的工具调用，而非“智能体-智能体”之间的复杂协商。
*   **核心问题**：如何构建一个可扩展、结构化且受策略约束的通信基座，使代表不同用户的异构 LLM 智能体能够进行高效、公平且安全的直接协商？

### 2. 方法论：LLM-X 架构与协议
*   **核心思想**：引入一个名为 LLM-X 的交换平台，作为智能体间通信的中间件，通过消息总线和路由基座强制执行模式（Schema）验证和协商策略。
*   **关键技术细节**：
    *   **分层架构**：包含联邦网关（负责 JWT 认证、速率限制）、控制平面（管理注册与共识）、策略引擎（执行决策规则）和观测组件。
    *   **有类型消息协议**：基于 JSON Envelope 封装，支持 CFP（征求建议）、Offer（报价）、Accept/Reject（接受/拒绝）和 Ack（确认）等消息类型。
    *   **协商模式**：支持经典的 **ContractNet (CNet)** 协议（一对多招标）和 **FIPA 迭代报价** 模式（双边多次讨价还价）。
    *   **策略引擎（Policy Engine）**：定义了三种决策逻辑：
        *   *Low*：收到第一个有效回复或超时即关闭。
        *   *Medium*：收到第一个有效回复后立即接受并结束。
        *   *High*：必须收集所有潜在报价后才进行决策。
    *   **传输层**：利用 NATS 消息系统实现异步通信，支持重试机制和指数退避。

### 3. 实验设计
*   **实验场景**：模拟一个发起者（Alice）与多个承包商（5、9、12 个智能体）之间的 ContractNet 协商过程。
*   **负载生成**：使用泊松过程（Poisson process）模拟真实的随机交互流量。
*   **对比维度**：
    *   **规模扩展性**：对比 5、9、12 个智能体在不同并发下的表现。
    *   **策略对比**：对比 Low、Medium、High 三种策略对系统性能的影响。
    *   **时间跨度**：分为短期运行（2分钟）、中期运行（2小时）和长期压力测试（12小时）。
*   **衡量指标**：每分钟消息量（吞吐量）、中位数（p50）及尾部（p95）延迟、轮次完成率、延迟漂移。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或服务器硬件配置。
*   **实现细节**：实验中的智能体使用 Python 包装器实现，以确保实验的可控性和可重复性。虽然逻辑是模拟的，但消息格式和协议完全兼容真实的 LLM API（如 OpenAI、Anthropic）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多组短期（2分钟）实验，每组重复 3 次以确保一致性。
    *   进行了 2 小时的中长期稳定性实验。
    *   进行了 12 小时的超长持续负载实验。
*   **充分性评价**：实验设计较为充分，涵盖了从并发规模到策略差异，再到时间稳定性的全方位评估。通过 12 小时的运行证明了系统无明显的延迟漂移，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **策略-性能权衡**：严格的策略（High）能保证公平性和报价完整性，但会显著增加消息开销和延迟；宽松策略（Low/Medium）响应快，但可能错过最优报价。
*   **线性扩展性**：随着智能体数量增加，消息量呈线性增长，延迟增长在毫秒级范围内受控，证明了架构的可扩展性。
*   **长期稳定性**：12 小时实验显示系统在持续负载下保持 100% 的轮次完成率，且延迟分布保持稳定，无系统性饱和或漂移。

### 7. 优点与亮点
*   **解耦设计**：将协商逻辑与通信基座分离，使得策略可以动态切换而无需重新编译。
*   **LLM 就绪（LLM-ready）**：虽然实验使用 Python 脚本，但其 JSON Schema 设计直接兼容主流 LLM 的结构化输出，具备极强的工程落地潜力。
*   **安全性保障**：通过网关层的 JWT 认证和模式验证，有效防御了非法消息注入和格式错误。

### 8. 不足与局限
*   **智能体同质化风险**：实验中的智能体行为由脚本控制，虽然协议支持异构 LLM，但论文未展示真实 LLM（如 GPT-4 与 Claude 3 混合）在复杂语义协商下的动态博弈结果。
*   **算力细节缺失**：缺乏对运行该交换平台所需的具体计算资源（CPU/内存/带宽）的量化分析。
*   **应用场景单一**：目前主要集中在 ContractNet 模式，对于更复杂的社会化协作或多轮冲突解决场景探讨较少。

（完）
