---
title: Macro-Action Based Multi-Agent Instruction Following through Value Cancellation
title_zh: 基于宏动作的多智能体指令遵循：通过价值抵消实现
authors: "Wo Wei Lin, Ethan Rathbun, Enrico Marchesini Xiang Zhi Tan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12655v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 适应自然语言指令的多智能体强化学习
tldr: 本研究针对多智能体强化学习中外部指令中断宏动作导致价值估计不一致的问题，提出了MAVIC方法。该方法通过在指令边界修正贝尔曼回溯目标，实现了在随机指令切换下的统一策略学习。实验证明，MAVIC在复杂协作任务中能显著提高指令遵循率，并有效兼顾基础任务性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12655v1/fig-001.webp\", \"caption\": \"Table 1: MacDec-POMDP definition where the subscript i denotes individual agent components.\", \"page\": 2, \"index\": 1, \"width\": 520, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2605.12655v1/fig-002.webp\", \"caption\": \"Figure 5: Action distribution frequency for successful delivery is shown by baseline no instruction while action distribution for unachievable instructions.\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 235}]"
motivation: 外部指令中断正在进行的宏动作时，传统的贝尔曼更新会导致跨指令背景的价值估计耦合与冲突。
method: 提出MAVIC框架，在指令切换边界通过修正引导目标来抵消指令目标变化，从而恢复一致的价值估计。
result: 实验结果显示，MAVIC在保持基础任务表现的同时，在复杂多智能体环境下实现了优异的指令遵循性能。
conclusion: MAVIC通过直接修改价值估计的引导目标，为处理具有长期目标的指令遵循任务提供了一种理论完备且有效的解决方案。
---

## 摘要
现实场景中的多智能体强化学习（MARL）可能需要适应外部自然语言指令，这些指令会中断正在进行的行为并与长远目标发生冲突。然而，将奖励以指令为条件会引入一种根本性的失效模式，因为贝尔曼更新会将不同指令上下文中的价值估计耦合在一起，导致当指令中断宏动作时出现价值不一致。我们提出了用于指令合规的宏动作价值修正（MAVIC），它通过修正传入的指令目标并恢复当前目标下的延续价值，在指令边界处修正贝尔曼备份。与奖励塑造不同，MAVIC 修改了自举目标本身，从而在统一策略内实现了随机指令切换下的连贯价值估计。我们提供了理论分析和一种 Actor-Critic 实现，并证明了在日益复杂的协作式多智能体环境中，MAVIC 在保持基础任务性能的同时，实现了极高的指令遵循度。

## Abstract
Multi-agent reinforcement learning (MARL) in real-world use cases may need to adapt to external natural language instructions that interrupt ongoing behavior and conflict with long-horizon objectives. However, conditioning rewards on instructions introduces a fundamental failure mode as Bellman updates couple value estimates across instruction contexts, leading to inconsistent values when instructions interrupt macro-actions. We propose Macro-Action Value Correction for Instruction Compliance (MAVIC), which corrects Bellman backups at instruction boundaries by correcting the incoming instruction objective and restoring the continuation value under the current objective. Unlike reward shaping, MAVIC modifies the bootstrapping target itself, enabling consistent value estimation under stochastic instruction switching within a unified policy. We provide theoretical analysis and an actor-critic implementation, and show that MAVIC achieves high instruction compliance while preserving base task performance in increasingly complex cooperative multi-agent environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Macro-Action Based Multi-Agent Instruction Following through Value Cancellation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的多智能体协作场景中，智能体需要根据人类的自然语言指令动态调整行为。这些指令往往会**中断**正在进行的长期任务（宏动作，Macro-Actions），并可能与原始目标发生冲突。
*   **核心挑战**：在多智能体强化学习（MARL）中，传统的贝尔曼更新（Bellman updates）会将不同指令背景下的价值估计耦合在一起。当指令中途改变时，价值函数会从受不同目标支配的状态进行自举（Bootstrapping），导致“**价值交叉污染**”（Cross-contamination）。
*   **后果**：这种污染会导致智能体为了维持基础任务性能而忽略指令，或者为了遵循指令而严重破坏基础任务的协调性。

### 2. 方法论：MAVIC 框架
论文提出了 **MAVIC (Macro-Action Value Correction)**，其核心思想是在指令切换的边界处对价值估计进行解耦。
*   **核心思想**：当指令发生变化时，修改贝尔曼备份的目标。它不是简单地进行奖励塑造（Reward Shaping），而是直接修正自举目标，减去新指令带来的价值贡献，并补偿原指令下的延续价值。
*   **关键技术细节**：
    *   **价值修正公式**：在指令从 $c$ 切换到 $c'$ 的时刻，修正后的奖励 $\bar{r}$ 为：$\bar{r}_t \leftarrow \bar{r}_t + \gamma^{\tau_m} [V(h_{t+1}, c) - V(h_{t+1}, c')]$。这确保了更新目标仅反映单一指令上下文。
    *   **架构设计**：采用 Actor-Critic 结构。使用预训练且冻结的 **BERT** 模型作为语言编码器，将指令嵌入与环境观测拼接。
    *   **宏动作处理**：当新指令到达时，强制终止当前执行的宏动作，并根据新指令重新采样宏动作，确保响应的即时性。
*   **理论保障**：论文通过引理和定理证明了该修正机制能使多指令问题等价于优化各个独立的指令条件目标，从而在理论上消除了交叉污染。

### 3. 实验设计
研究者在三个具有代表性的协作多智能体环境中进行了评估：
*   **实验场景**：
    1.  **Box Pushing (BP)**：双智能体协作推箱子，指令涉及改变目标或禁止推箱。
    2.  **Warehouse Tool Delivery (WTD)**：三智能体异构协作交付工具，指令涉及更改交付顺序。
    3.  **Overcooked (OC)**：三智能体（含一人类）协作烹饪，指令包含资源限制（如“不要使用左侧切菜板”）或特定动作请求。
*   **对比方法 (Baselines)**：
    *   **Naive**：直接将指令奖励加入系统，不进行价值修正。
    *   **Switch**：在不同指令类之间独立切换训练（试图规避耦合，但难以处理动态中断）。
    *   **Vanilla Mac-IAC**：不带指令的原始算法，作为基础任务性能的上限参考。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 5 个独立随机种子进行实验，并使用 95% 置信区间进行结果统计。算法基于现有的 Mac-IAC 代码库实现。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同复杂度的环境上进行了测试，每个环境包含多种类型的指令（约束型、任务型、动作型）。
*   **充分性评估**：
    *   **多维度指标**：同时评估了“基础任务性能”（Base Task Performance）和“指令遵循率”（Compliance）。
    *   **消融与对比**：对比了 Naive 和 Switch 模式，证明了 MAVIC 在处理奖励冲突时的优越性。
    *   **泛化性测试**：进行了分布外（OOD）指令测试，验证了模型对同义指令的理解能力。
    *   **客观性**：实验设计涵盖了从简单协作到复杂资源竞争的场景，结果显示 MAVIC 在所有场景下均表现稳健，实验较为充分。

### 6. 主要结论与发现
*   **消除权衡**：MAVIC 是唯一能在保持高基础任务得分的同时，实现近乎 100% 指令遵循率的方法。
*   **解决失效模式**：实验证实了 Naive 方法在面对负面约束（如“不要做某事”）时会发生崩溃，而 MAVIC 通过价值解耦成功避免了这一问题。
*   **稳定性**：相比于 Switch 训练法，MAVIC 在训练过程中表现出更低的方差和更高的收敛稳定性。
*   **OOD 表现**：通过冻结的语言编码器，MAVIC 能够理解未见过的同义指令，但在面对完全无关或不可行的指令时，行为会退化为某种统一的默认响应。

### 7. 优点
*   **理论完备性**：不仅提出了算法，还提供了关于价值解耦的数学证明。
*   **即插即用**：该价值修正机制可以集成到大多数基于宏动作的 Actor-Critic MARL 算法中。
*   **处理中断的能力**：专门针对宏动作被随机指令中断的场景设计，解决了长期以来 MARL 在动态人类交互中的痛点。

### 8. 不足与局限
*   **价值估计依赖**：MAVIC 的效果高度依赖于 Critic 对价值函数的准确估计。在部分可观测（PO）或奖励极度稀疏的环境下，修正项的方差可能会增大。
*   **语言泛化上限**：虽然使用了 BERT，但智能体的行为多样性受限于训练时见过的指令意图。对于完全新颖的意图（而非同义词），模型无法自发产生正确行为。
*   **探索问题**：论文未深入探讨如何引导智能体探索那些出现频率极低的罕见指令。

（完）
