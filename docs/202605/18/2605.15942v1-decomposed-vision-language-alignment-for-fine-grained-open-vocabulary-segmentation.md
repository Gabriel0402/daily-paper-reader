---
title: Decomposed Vision-Language Alignment for Fine-Grained Open-Vocabulary Segmentation
title_zh: 用于细粒度开放词汇分割的分解式视觉-语言对齐
authors: "Chenhao Wang, Yingrui Ji, Yu Meng, Yao Zhu"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15942v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 细粒度开放词汇分割
tldr: 针对开放词汇分割模型在处理细粒度属性与类别组合时泛化能力不足的问题，本文提出了一种分解式视觉-语言对齐框架。该框架将文本提示分解为独立的概念和属性标记，通过特征门控交叉注意力和对数空间相似度聚合，实现了对语义单元的精细化建模。该方法能无缝集成到现有Transformer架构中，显著提升了模型对未见组合的识别精度与泛化性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15942v1/fig-001.webp\", \"caption\": \"Fig. 1: Overall architecture of the proposed method. (a) Explicit Prompt Decomposition and Feature-Gated Cross-Attention. The compositional text prompt is explicitly decoupled into independent concept and attribute tokens. Visual queries interact with the concept via standard cross-attention, and with attributes via multiplicative feature-gating to enforce compositional constraints. (b) Log-Space AND Compositional Scoring. Concept and attribute tokens are independently matched with query embeddings. The resulting scores are converted to log-probabilities and aggregated additively to produce the final compositional matching score.\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2605.15942v1/fig-002.webp\", \"caption\": \"Table 1: Comparison with open-vocabulary segmentation methods under the compositional generalization protocol. Rd denotes the relative performance drop from seen to unseen compositions. AND-Eff measures how effectively the model composes independently learned attribute semantics at inference.\", \"page\": 11, \"index\": 2, \"width\": 712, \"height\": 200}, {\"url\": \"assets/figures/arxiv/2605.15942v1/fig-003.webp\", \"caption\": \"Fig. 2: Qualitative visualization of the Feature-Gated Cross-Attention mechanism for two examples. The top row prompt decomposes into concept building, attribute hipped roof building, and attribute residential building. The bottom row prompt decomposes into concept building, attribute flat roof building, and attribute commercial building. Panel a shows the original images. Panel b shows the concept attention maps. Panel c and Panel d display the CAGate activation maps for the two attributes. Panel e shows the final segmentation results. The three branches produce spatially distinct activations. The segmented instances coincide with their intersection.\", \"page\": 14, \"index\": 3, \"width\": 716, \"height\": 307}]"
motivation: 现有的开放词汇分割模型通常将细粒度描述视为整体，导致难以有效泛化到未见的物体类别与属性组合。
method: 提出将文本提示分解为概念和属性标记，并利用特征门控交叉注意力机制与对数空间评分聚合进行多模态对齐。
result: 在细粒度开放词汇分割基准测试中，该方法显著提高了模型对未见属性-类别组合的泛化能力。
conclusion: 通过显式分解语义单元并强化组合语义建模，该框架有效解决了细粒度场景下的视觉-语言对齐难题。
---

## 摘要
开放词汇分割模型通常难以泛化到物体类别和属性的未知组合，因为细粒度描述通常被编码为整体句子，导致多个语义单元相互纠缠。我们提出了一种分解式视觉-语言对齐框架，该框架将文本提示显式地分解为一个概念标记（concept token）和多个属性标记（attribute tokens），从而为每个语义单元实现独立的跨模态交互。在特征层面，我们引入了一个特征门控交叉注意力（Feature-Gated Cross-Attention）模块，该模块生成特定于属性的门控图，以乘法方式融合信息，从而有效地强化组合语义。在评分层面，每个标记的相似度在对数空间中进行聚合，产生稳定且可解释的组合匹配。该方法可以无缝集成到现有的基于 Transformer 的分割架构中，并显著提升了在细粒度开放词汇分割基准测试中对未知属性-类别组合的泛化能力。

## Abstract
Open-vocabulary segmentation models often struggle to generalize to unseen combinations of object categories and attributes, because fine-grained descriptions are typically encoded as holistic sentences that entangle multiple semantic units. We propose a Decomposed Vision-Language Alignment framework that explicitly factorizes textual prompts into a concept token and multiple attribute tokens, enabling separate cross-modal interactions for each semantic unit. At the feature level, we introduce a Feature-Gated Cross-Attention module that generates attribute-specific gating maps to fuse information in a multiplicative manner, effectively enforcing compositional semantics. At the scoring level, per-token similarities are aggregated in log-space, producing a stable and interpretable compositional matching. The method can be seamlessly integrated into existing transformer-based segmentation architectures and significantly improves generalization to unseen attribute-category compositions in fine-grained open-vocabulary segmentation benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Decomposed Vision-Language Alignment for Fine-Grained Open-Vocabulary Segmentation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的开放词汇分割（OVS）模型在处理“物体类别+多个属性”的细粒度组合描述时，泛化能力较弱。
*   **研究背景**：传统的 OVS 模型通常将文本提示（如“带平顶的工业建筑”）视为一个整体进行编码。这种“整体式编码”导致类别语义与属性语义高度纠缠。当模型在推理阶段遇到训练中未见过的属性-类别组合（Unseen Compositions）时，由于缺乏对语义单元的独立理解和重新组合能力，分割性能会大幅下降。
*   **研究目标**：实现一种能够显式分解语义单元并进行逻辑组合的框架，以提升模型在细粒度场景下的组合泛化能力。

### 2. 方法论：分解式视觉-语言对齐
该框架的核心思想是将复杂的文本描述拆解为独立的语义单元，并在特征和评分两个层面强制执行逻辑“与”（AND）约束。

*   **显式提示分解 (Explicit Prompt Decomposition)**：
    *   将文本提示分解为一个类别标记（Concept Token）和多个属性标记（Attribute Tokens）。
    *   为了保持语义语境，属性标记在编码时会与类别词拼接（例如，“平顶”被编码为“平顶建筑”），以避免属性语义的歧义。
*   **特征门控交叉注意力 (Feature-Gated Cross-Attention)**：
    *   **类别分支**：使用标准交叉注意力提取类别相关的视觉特征。
    *   **属性分支**：为每个属性生成独立的“门控图”（Gating Maps）。这些门控图通过 Sigmoid 函数归一化到 [0, 1] 之间。
    *   **特征融合**：将所有属性门控图与类别特征进行逐元素相乘。这种乘法操作模拟了逻辑“与”关系，即只有同时满足所有属性条件的区域特征才会被保留。
*   **对数空间组合评分 (Log-Space AND Compositional Scoring)**：
    *   独立计算类别和各属性的匹配得分。
    *   **对数空间聚合**：不直接在概率空间进行乘法（防止数值衰减和梯度不稳定），而是在对数空间（Log-space）对各单元的对数概率进行加权求和，从而实现稳定且可解释的组合匹配评分。

### 3. 实验设计
*   **数据集与场景**：
    *   **UBC Building Dataset**：卫星遥感图像，包含建筑的屋顶类型和用途属性。
    *   **PACO-LVIS**：通用物体属性数据集，包含颜色、材料、图案和透明度等属性。
*   **Benchmark 协议**：专门设计了“组合泛化评估协议”，将数据集划分为“已见组合”（Seen）和“未见组合”（Unseen），并引入了 **AND-Eff（组合效率）** 指标来衡量模型组合独立属性的能力。
*   **对比方法**：包括 OVSeg、FC-CLIP、CAT-Seg、X-Decoder 以及作为基座的 SAM3。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。
*   **集成方式**：作者提到该方法是“模型无关”的（Model-agnostic），实验中将其集成到了 SAM3 框架中，通过模块化替换实现了性能提升。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同领域的数据集（遥感与通用物体）上进行了对比测试。
    *   **消融实验**：针对提示编码策略（整体 vs 分解）、聚合机制（加法、最大值、概率乘法 vs 对数空间乘法）进行了详细对比。
    *   **对比实验**：与专门的组合零样本学习（CZSL）方法（如 AttrAsOp, CGE）的适配版进行了对比。
*   **充分性评价**：实验设计较为充分，通过 Seen/Unseen 的性能差距（Rd 指标）客观地展示了模型在处理新组合时的优越性。定性可视化分析（注意力图的分支展示）直观地证明了门控机制的有效性。

### 6. 主要结论与发现
*   **性能提升**：在 UBC 数据集上，该方法将 Unseen 组合的 AP 从 4.0 提升至 7.1，AND-Eff 指标从 0.533 显著提升至 0.959（接近理想组合状态）。
*   **逻辑约束的重要性**：实验证明，简单的特征加法或平均分无法有效处理细粒度约束，而基于乘法逻辑的门控机制是实现组合泛化的关键。
*   **稳定性**：对数空间评分聚合解决了直接进行概率乘法导致的梯度消失和优化不稳定问题。

### 7. 优点与亮点
*   **架构灵活性**：作为一种通用的对齐框架，它可以无缝集成到现有的 Transformer 分割模型中，无需改变原有的检测或分割头。
*   **可解释性强**：通过可视化不同属性分支的门控图，可以清晰地看到模型是如何通过“求交集”的方式定位目标的。
*   **解决语义纠缠**：显式分解策略有效地克服了视觉-语言模型中常见的“词袋”效应（即只认单词不认结构的问题）。

### 8. 不足与局限
*   **依赖分解质量**：模型性能高度依赖于文本提示的分解质量，如果属性描述本身模糊或难以拆分，效果可能会受限。
*   **计算开销**：虽然论文未详述，但为每个属性维护独立的注意力分支和门控计算，在处理具有极多属性的长描述时，可能会增加推理延迟和内存占用。
*   **数据集局限**：目前的验证主要集中在属性相对明确的建筑和 PACO 数据集上，对于更复杂、更抽象的自然语言描述（如复杂的空间关系）的泛化能力尚待验证。

（完）
