---
title: Runtime Monitoring of Perception-Based Autonomous Systems via Embedding Temporal Logic
title_zh: 通过嵌入时序逻辑实现基于感知的自主系统运行时监控
authors: "Parv Kapoor, Abigail Hammer, Ashish Kapoor, Karen Leung, Eunsuk Kang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12651v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于感知驱动自主系统监控的嵌入式时态逻辑
tldr: 针对感知驱动的自主系统在运行时监控中难以将高维传感器数据映射为离散逻辑的问题，本文提出了嵌入时序逻辑（ETL）。ETL直接在学习到的嵌入空间中运行，通过计算观测嵌入与目标嵌入之间的距离来定义谓词，从而捕捉复杂的感知概念。该方法结合了时序算子与符合性校准程序，以提供可靠的安全性评估。实验证明，ETL在多种机器人操作任务中能准确识别复杂的时序感知行为，有效解决了传统方法在语义对齐和计算效率上的局限。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12651v2/fig-001.webp\", \"caption\": \"Figure 3: Dual-predicate Boolean timelines for mw-pick-place-wall. Top panels show distances to grasp (zA) and place (zB) embeddings with thresholds ϵ∗ and ϵCP . Lower panels show ETL and ground-truth predicate activations.\", \"page\": 18, \"index\": 1, \"width\": 718, \"height\": 722}, {\"url\": \"assets/figures/arxiv/2605.12651v2/fig-002.webp\", \"caption\": \"Figure 2: Qualitative and quantitative ETL results.\", \"page\": 8, \"index\": 2, \"width\": 821, \"height\": 644}, {\"url\": \"assets/figures/arxiv/2605.12651v2/fig-003.webp\", \"caption\": \"Figure 1: Overview of embedding-based runtime monitoring. Top: Target observations OG = {og1, . . . , ogn} and the online observation ot are encoded by a pretrained vision encoder into target embeddings zG and the current embedding zt. An embedding predicate then evaluates whether zt reaches the target set. Bottom left: The embedding trajectory is projected onto its first two principal components, showing its evolution relative to the target embeddings. Bottom right: Runtime monitoring computes the distance from zt to zG over time, thresholds it using ϵ, and evaluates the temporal logic specification.\", \"page\": 4, \"index\": 3, \"width\": 814, \"height\": 406}]"
motivation: 传统运行时监控依赖于将传感器数据映射到离散逻辑，在处理高维感知数据时存在计算昂贵、脆弱且语义不一致的问题。
method: 提出嵌入时序逻辑（ETL），直接在学习到的嵌入空间中通过计算观测值与目标嵌入之间的距离来定义谓词并进行监控。
result: 在多个机器人操作环境中的实验表明，ETL能够准确监控具有时间序列特征的感知行为，并与真实语义保持高度一致。
conclusion: ETL为感知驱动的自主系统提供了一种高效、可靠且无需显式状态抽象的运行时监控新框架。
---

## 摘要
自主系统的运行时监控传统上依赖于将连续传感器观测映射到定义在低维状态变量上的离散逻辑命题。这种抽象在感知驱动的场景中会失效，因为此类映射需要额外的学习模块，而这些模块通常计算开销大、脆弱且语义不一致。在这项工作中，我们提出了嵌入时序逻辑（Embedding Temporal Logic, ETL），这是一种直接在学习到的嵌入空间中执行监控的时序逻辑。ETL 通过观测到的嵌入与源自参考观测的目标嵌入之间的距离来定义谓词。这种形式化方法允许规范捕捉高层感知概念（例如与视觉目标的相似性或对语义区域的规避），而这些概念使用传统谓词很难或无法表达。通过将这些谓词与时序算子组合，ETL 能够自然地表达时序扩展和顺序的感知行为。我们引入了 ETL 监控器用于评估有界嵌入轨迹上的规范，并提出了一种保形校准程序以提供可靠且面向安全的谓词评估。我们在多个操作环境中评估了我们的方法，结果表明 ETL 与真实语义具有很强的经验一致性，能够实现对时序组合行为的准确监控。

## Abstract
Runtime monitoring of autonomous systems traditionally relies on mapping continuous sensor observations to discrete logical propositions defined over low-dimensional state variables. This abstraction breaks down in perception-driven settings, where such mappings require additional learned modules that are often computationally expensive, brittle, and semantically misaligned. In this work, we propose Embedding Temporal Logic (ETL), a temporal logic that performs monitoring directly in learned embedding spaces. ETL defines predicates through distances between observed embeddings and target embeddings derived from reference observations. This formulation allows specifications to capture high-level perceptual concepts, such as similarity to visual goals or avoidance of semantic regions, that are difficult or impossible to express using traditional predicates. By composing these predicates with temporal operators, ETL naturally expresses temporally extended and sequential perceptual behaviors. We introduce ETL monitors for evaluating specifications over bounded embedding traces, along with a conformal calibration procedure that provides reliable and safety-oriented predicate evaluation. We evaluate our approach across multiple manipulation environments to show that ETL achieves strong empirical agreement with ground-truth semantics, including accurate monitoring of temporally composed behaviors.

---

## 论文详细总结（自动生成）

这是一份关于论文《Runtime Monitoring of Perception-Based Autonomous Systems via Embedding Temporal Logic》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：现代自主系统（如自动驾驶、机器人）越来越多地依赖深度学习生成的“嵌入表示”（Embeddings）进行感知和决策。
*   **核心痛点**：传统的运行时监控（Runtime Monitoring）依赖于将传感器数据映射到低维、离散的逻辑命题（如“距离障碍物 < 0.5米”）。但在复杂的感知任务中，这种映射需要额外的分类器或检测器，这些模块通常计算昂贵、容易失效（脆弱），且难以与复杂的视觉语义对齐。
*   **研究动机**：能否直接在学习到的嵌入空间中定义逻辑规范，从而跳过显式的状态估计步骤，实现对感知驱动系统更直接、高效的监控？

### 2. 方法论
*   **核心思想**：提出**嵌入时序逻辑（Embedding Temporal Logic, ETL）**。利用预训练视觉编码器（如 DINOv2, CLIP）的几何特性——即语义相似的场景在嵌入空间中距离更近，将逻辑谓词定义为观测向量与目标参考向量之间的距离函数。
*   **关键技术细节**：
    *   **ETL 语法与语义**：继承了线性时序逻辑（LTL）的算子（如 $\neg, \wedge, U, F, G$），但其原子谓词定义为：$\delta_{ap}(z) \bowtie \epsilon$，其中 $z$ 是当前观测的嵌入向量，$z_{target}$ 是目标状态的参考嵌入，$\bowtie$ 是比较算子，$\epsilon$ 是阈值。
    *   **鲁棒性度量**：引入了定量语义，通过计算距离阈值的余量来衡量系统满足规范的“程度”。
    *   **阈值校准策略**：
        1.  **F1-Optimal**：在校准集上搜索使 F1 分数最大化的阈值，平衡精确率和召回率。
        2.  **保形校准（Conformal Calibration）**：利用保形预测理论，在给定的错误水平 $\alpha$ 下，提供统计学上的召回率保证，确保在安全关键场景下不漏报违规行为。

### 3. 实验设计
*   **实验场景**：
    1.  **Dubins Car 导航**：在受控的 2D 环境中验证 ETL 谓词与基于真实状态（Ground Truth）逻辑的一致性。
    2.  **模拟机器人操作（D3IL & MetaWorld）**：包括物体堆叠（Stacking）、分类（Sorting）和取放（Pick-and-Place）任务，环境包含干扰物和复杂的物理接触。
    3.  **真实世界数据（DROID）**：使用大规模真实机器人操作数据集，验证 ETL 在非结构化、真实环境下的泛化能力。
*   **对比方法（Benchmarks）**：
    *   **PCA-kmeans**：基于主成分分析和聚类的异常检测方法。
    *   **logpZO**：基于流匹配（Flow-matching）的密度估计模型，用于检测分布外（OOD）样本。
    *   **VLM 基准**：使用最先进的视觉语言模型（Qwen2-VL-2B）直接对视频帧进行语义判断。

### 4. 资源与算力
*   **硬件设备**：实验使用了计算集群，配备了 **2 张 NVIDIA GeForce RTX 5090 GPU**（每张拥有 34.2 GB 显存）。
*   **模型实现**：使用了多种预训练编码器，包括 Dreamer 的 RSSM 编码器、DINOv2 以及 Ctrl-World 的 SVD VAE。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 Dubins Car 中使用了 100 条轨迹进行验证。
    *   在模拟操作中涵盖了 3 个不同的复杂环境。
    *   在 DROID 真实数据中选择了 5 类具有代表性的组合任务。
*   **充分性评价**：实验设计较为充分。作者不仅对比了传统的聚类和密度估计方法，还对比了最新的大语言模型（VLM）。此外，通过消融实验验证了不同阈值校准方法（F1 vs. Conformal）的效果，涵盖了从仿真到真实的完整频谱，论证具有较强的说服力。

### 6. 主要结论与发现
*   **语义一致性**：ETL 谓词能以极高精度（F1 分数通常 > 0.8）近似真实状态命题，证明了嵌入空间几何距离确实能承载复杂的逻辑语义。
*   **时序监控能力**：ETL 能够准确识别具有顺序要求的任务（如“先抓取 A 再放置到 B”），在 DROID 数据集上的顺序识别准确率表现优异。
*   **安全性保障**：保形校准能够提供分布无关的统计保证，在牺牲少量精确率的情况下显著提升召回率，这对安全监控至关重要。
*   **优于 VLM**：在处理视觉相似但语义不同的操作阶段时，ETL 比直接使用 VLM 提示词更加稳定和准确。

### 7. 优点
*   **无需显式建模**：跳过了脆弱的状态估计和目标检测环节，直接利用预训练模型的表征能力。
*   **数学严谨性**：将形式化方法的严谨性（时序逻辑）与统计学习的灵活性（保形预测）相结合。
*   **轻量化部署**：相比于运行大型 VLM，计算嵌入距离的开销极低，适合在线实时监控。

### 8. 不足与局限
*   **可解释性挑战**：嵌入空间是黑盒，当监控器发出警报时，难以像物理坐标那样直观地向人类解释“为什么”判定为违规。
*   **编码器依赖性**：监控效果高度依赖于预训练编码器的质量。如果编码器未能捕捉到任务相关的细微特征（如物体的微小位移），ETL 的性能会受限。
*   **目标设定需求**：仍需要用户提供“目标状态”的参考图像或嵌入，对于极其抽象或动态变化的规范，如何定义参考集仍是一个挑战。

（完）
