---
title: Distributed TD Tracking with Linear Function Approximation over Directed Communication Networks
title_zh: 有向通信网络上具有线性函数逼近的分布式TD追踪
authors: "Haocheng Yang, Shengchao Zhao, Yongchao Liu"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04466v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 有向网络上多智能体强化学习的分布式策略评估
tldr: 本文研究有向通信网络下的多智能体强化学习策略评估问题。针对现有方法在有向图中收敛慢且不稳定的挑战，提出了PP-DTD算法。该算法将Push-Pull机制与带线性函数逼近的TD学习相结合，并引入方差缩减技术。PP-DTD在独立同分布或马尔可夫样本下，衰减步长时可达O(T^-1)收敛率，是首个在有向图上达到单智能体TD性能水平的分布式算法，实验验证了其在协作导航任务中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.04466v1/fig-001.webp\", \"caption\": \"Fig. 2: Markovian setting.\", \"page\": 11, \"index\": 1, \"width\": 514, \"height\": 796}, {\"url\": \"assets/figures/arxiv/2605.04466v1/fig-002.webp\", \"caption\": \"Fig. 1: i.i.d. setting.\", \"page\": 11, \"index\": 2, \"width\": 514, \"height\": 796}]"
motivation: 旨在解决多智能体强化学习在有向通信网络环境下进行策略评估时，收敛速度慢且算法不稳定的问题。
method: 提出了结合Push-Pull通信机制、线性函数逼近TD学习以及方差缩减技术的PP-DTD分布式算法。
result: 理论证明该算法在衰减步长下可实现O(T^-1)的收敛率，且在常数步长下能线性收敛至最优解邻域。
conclusion: PP-DTD是首个在有向图上实现与单智能体TD算法相当收敛性能的分布式策略评估方案，具有显著的鲁棒性。
---

## 摘要
我们研究了有向通信网络上的多智能体强化学习（MARL）中的策略评估问题，其中智能体相互协作以探索未知环境并完成特定任务。我们提出了一种名为 PP-DTD 的 Push-Pull 型分布式算法，用于在线性函数逼近的时序差分（TD）学习框架下进行 MARL 策略评估。PP-DTD 将 TD 学习与 Push-Pull 机制相结合以适应有向通信网络，并进一步利用方差缩减技术来增强算法的稳定性和收敛速度。我们证明了在样本为独立同分布或马尔可夫的情况下，PP-DTD 在固定步长下能线性收敛至最优解的邻域，而在递减步长下能达到 $\mathcal{O}({T^{-1}})$ 的收敛率。据我们所知，PP-DTD 是首个在有向图上的 MARL 策略评估中实现与单智能体 TD 相当收敛率的分布式算法。在协作导航任务上的数值实验证明了 PP-DTD 的鲁棒性和有效性。

## Abstract
We study the policy evaluation problem in multi-agent reinforcement learning (MARL) over directed communication networks, where agents cooperate with each other to explore an unknown environment and accomplish a specific task. We propose a Push-Pull-type distributed algorithm, named PP-DTD, for policy evaluation in MARL within the framework of temporal difference (TD) learning with linear function approximation. PP-DTD integrates TD learning with the Push-Pull mechanism to accommodate directed communication networks, and further utilizes variance reduction techniques to enhance both algorithmic stability and convergence rate. We show that PP-DTD achieves linear convergence to a neighborhood of the optimum under constant step-sizes and a convergence rate of $\mathcal{O}({T^{-1}})$ under decaying step-sizes when the sample is independent and identically distributed or Markovian. To the best of our knowledge, PP-DTD is the first distributed algorithm for policy evaluation in MARL over directed graphs that achieves a comparable convergence rate to single-agent TD. The numerical experiments on cooperative navigation tasks demonstrate the robustness and effectiveness of PP-DTD.

---

## 论文详细总结（自动生成）

这是一份关于论文《Distributed TD Tracking with Linear Function Approximation over Directed Communication Networks》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文聚焦于**多智能体强化学习（MARL）中的策略评估问题**。在现实应用（如无人机群、自动驾驶、智能工厂）中，智能体之间的通信往往是**有向的**（不对称的），而现有的分布式时序差分（TD）算法大多假设通信网络是无向且平衡的。
*   **研究动机**：解决在有向通信网络下，如何实现高效、稳定且具有快速收敛保证的分布式策略评估。
*   **核心挑战**：有向图会导致权重不平衡，进而影响算法的共识收敛；同时，随机采样带来的方差和马尔可夫样本的偏差会降低收敛速度。

### 2. 论文提出的方法论：PP-DTD 算法
论文提出了一种名为 **PP-DTD (Push-Pull based Decentralized TD learning)** 的算法，其核心思想结合了以下三大技术：
*   **Push-Pull 机制**：为了适应有向图，算法采用双层通信结构。使用**行随机矩阵（Row-stochastic）**来混合智能体的参数估计（Pull），使用**列随机矩阵（Column-stochastic）**来追踪全局半梯度的平均值（Push）。这种设计不需要智能体了解邻居的入度信息，增强了网络灵活性。
*   **线性函数逼近**：利用特征向量 $\phi(s)$ 和参数 $\theta$ 的线性组合来近似价值函数，以应对大规模状态空间。
*   **混合方差缩减（Hybrid Variance Reduction）**：在更新步骤中引入动量项和方差缩减技术，通过利用历史梯度信息来抵消随机采样带来的噪声，从而提高算法稳定性。
*   **算法流程**：
    1.  **参数更新**：智能体根据本地追踪的梯度变量更新参数。
    2.  **共识步骤**：通过行随机矩阵与邻居交换参数，确保全局一致性。
    3.  **梯度估计**：计算随机半梯度，并应用方差缩减公式更新本地梯度估计。
    4.  **梯度追踪**：通过列随机矩阵传播梯度信息，实现对全局平均梯度的实时追踪。

### 3. 实验设计
*   **实验场景**：**协作导航任务（Cooperative Navigation）**。在该任务中，多个智能体需要协作覆盖多个目标点，同时避免碰撞。
*   **Benchmark（基准方法）**：主要对比了 **Push-SA** 算法（由 Lin 等人在 2024 年提出，是当时有向图分布式随机逼近的最前沿方法）。
*   **实验设置**：
    *   **网络规模**：分别在 $n=20, 40, 80$ 个智能体的网络上进行测试。
    *   **采样模式**：涵盖了**独立同分布（i.i.d.）**采样和更符合实际的**马尔可夫（Markovian）**轨迹采样。
    *   **步长策略**：对比了常数步长（快速收敛至邻域）和衰减步长（收敛至最优解）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件资源（如 GPU/CPU 型号、数量）以及具体的训练时长。
*   **实现细节**：实验部分提到参数 $a$ 和 $b$ 是在区间 $[10^{-3}, 5]$ 内通过 100 个等间距点进行优化的，这暗示了一定的超参数搜索计算量。

### 5. 实验数量与充分性
*   **实验组数**：针对 3 种网络规模（20/40/80）和 2 种采样环境（i.i.d./Markovian）进行了对比，每组实验运行了 10 次并取平均值。
*   **充分性评价**：实验设计较为**充分且客观**。它不仅验证了收敛速度，还通过共识误差（Consensus Error）和 TD 误差（TD Error）两个维度评估了算法性能。对比 Push-SA 这一强基准方法，证明了 PP-DTD 在最终误差和稳定性上的优势。

### 6. 主要结论与发现
*   **收敛率突破**：理论证明 PP-DTD 在衰减步长下达到了 **$\mathcal{O}(T^{-1})$** 的收敛率。这是首个在有向图上达到与单智能体 TD 算法相同收敛速率水平的分布式算法。
*   **稳定性**：在马尔可夫采样下，通过引入投影步骤（Projection Step），PP-DTD 表现出极强的鲁棒性，能够有效控制采样偏差。
*   **性能优势**：实验结果显示，PP-DTD 在所有测试规模下均优于 Push-SA，尤其是在处理异构数据和有向通信时，其共识速度更快，误差更低。

### 7. 优点（亮点）
*   **理论完备性**：提供了严谨的非渐近收敛分析，涵盖了常数步长和衰减步长两种情况。
*   **网络适应性**：Push-Pull 结构完美解决了有向图的权重失衡问题，且不需要复杂的入度计算，易于在分布式系统中部署。
*   **高效性**：结合方差缩减技术，使得算法在保证收敛精度的同时，显著提升了收敛速度。

### 8. 不足与局限
*   **函数逼近限制**：目前仅限于**线性函数逼近**。在处理极高维度或非线性极强的复杂环境时，其表现可能不如基于深度神经网络的非线性方法。
*   **超参数敏感性**：算法引入了多个超参数（如 $\alpha_t, \beta_t$ 以及 Push-Pull 的权重矩阵），在实际应用中可能需要精细调优才能达到最佳性能。
*   **实验场景单一**：虽然协作导航是经典任务，但缺乏在更大规模、更复杂动态环境（如高动态变化的拓扑结构）下的验证。

（完）
