---
title: Improving Vision-language Models with Perception-centric Process Reward Models
title_zh: 利用以感知为中心的过程奖励模型改进视觉语言模型
authors: "Yingqian Min, Kun Zhou, Yifan Li, Yuhuan Wu, Han Peng, Yifan Du, Wayne Xin Zhao, Min Yang, Ji-Rong Wen"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24583v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 具有过程奖励的视觉语言模型强化学习
tldr: 针对视觉语言模型在复杂推理中结果级监督过于粗糙的问题，本文提出了Perceval，一种以感知为中心的进程奖励模型。Perceval能实现标记级的错误定位，通过提取并比对图像声明与视觉证据来识别幻觉。将其集成到强化学习训练中可提供细粒度监督，并在推理阶段通过错误截断与重生成实现测试时扩展。实验证明该方法在多个基准测试中显著提升了模型性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24583v1/fig-001.webp\", \"caption\": \"Figure 3. Case study of the visual reasoning process from models trained with GRPO and our method.\", \"page\": 7, \"index\": 1, \"width\": 481, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.24583v1/fig-002.webp\", \"caption\": \"Table 1. Main results on multimodal benchmarks regarding visual search, perception-intensive reasoning and math&chart tasks. MRW and RWQA denote MME-RealWorld and RealWorldQA, respectively. Best and second best results in each group are highlighted in bold and underlined, respectively. ∗ indicates models capable of calling tools.\", \"page\": 6, \"index\": 2, \"width\": 1031, \"height\": 497}, {\"url\": \"assets/figures/arxiv/2604.24583v1/fig-003.webp\", \"caption\": \"Table 2. Comparison of different test-time scaling strategies, where Truncate and Truncate-Thinking denote our proposed Truncate–then–Regenerate and Truncate–Thinking–then–Regenerate methods, respectively.\", \"page\": 6, \"index\": 3, \"width\": 487, \"height\": 278}, {\"url\": \"assets/figures/arxiv/2604.24583v1/fig-004.webp\", \"caption\": \"Figure 1. An overview of our Process-Supervised GRPO framework. For each generated response, we use the Perceval to create a tokenlevel penalty mask. This mask is used to calculate a fine-grained token-level advantage, which is then incorporated into the GRPO objective to penalize hallucinatory tokens and improve the model’s perceptual grounding.\", \"page\": 3, \"index\": 4, \"width\": 1030, \"height\": 606}]"
motivation: 现有的强化学习方法主要依赖结果级监督，难以诊断和纠正视觉语言模型推理链条中的中间感知错误。
method: 提出Perceval模型，通过提取图像相关声明并与视觉证据比对来实现标记级错误定位，并将其应用于强化学习的细粒度奖励和推理时的测试时扩展。
result: 实验结果显示，Perceval在多个领域的基准测试中显著提升了多种视觉语言模型的推理能力，且在测试时扩展方面优于多数投票等策略。
conclusion: 以感知为中心的进程奖励模型为提升视觉语言模型的推理准确性和减少幻觉提供了一种通用且有效的细粒度监督策略。
---

## 摘要
近期在具有可验证奖励的强化学习（RLVR）方面的进展显著提升了视觉语言模型（VLM）的复杂推理能力。然而，其结果级监督过于粗糙，难以诊断和纠正推理链中的错误。为此，我们提出了 Perceval，这是一种能够实现标记级错误定位的过程奖励模型（PRM），它可以从响应中提取与图像相关的陈述，并将其与图像中的视觉证据逐一对比，最终返回包含感知错误的陈述。Perceval 使用感知密集型监督训练数据进行训练。随后，我们将 Perceval 集成到强化学习训练过程中以训练策略模型。具体而言，与应用序列级优势的传统 GRPO 相比，我们通过针对 Perceval 识别出的幻觉片段施加惩罚来应用标记级优势，从而实现细粒度的监督信号。除了增强训练过程外，Perceval 还可以辅助 VLM 在推理阶段的表现。利用 Perceval，我们可以截断模型响应中的错误部分，然后让模型直接重新生成响应，或者引导模型对其之前的输出进行反思。这一过程可以重复多次以实现测试时扩展。实验表明，在通过强化学习训练的多个推理 VLM 上，该方法在各领域的基准测试中均取得了显著提升，突显了以感知为中心的监督作为一种通用策略的前景。对于测试时扩展，它也表现出优于多数投票等其他策略的持续性能增益。我们的代码和数据将公开发布在 https://github.com/RUCAIBox/Perceval。

## Abstract
Recent advancements in reinforcement learning with verifiable rewards (RLVR) have significantly improved the complex reasoning ability of vision-language models (VLMs). However, its outcome-level supervision is too coarse to diagnose and correct errors within the reasoning chain. To this end, we propose Perceval, a process reward model (PRM) that enables token-level error grounding, which can extract image-related claims from the response and compare them one by one with the visual evidence in the image, ultimately returning claims that contain perceptual errors. Perceval is trained with perception-intensive supervised training data. We then integrate Perceval into the RL training process to train the policy models. Specifically, compared to traditional GRPO, which applies sequence-level advantages, we apply token-level advantages by targeting penalties on hallucinated spans identified by Perceval, thus enabling fine-grained supervision signals. In addition to augmenting the training process, Perceval can also assist VLMs during the inference stage. Using Perceval, we can truncate the erroneous portions of the model's response, and then either have the model regenerate the response directly or induce the model to reflect on its previous output. This process can be repeated multiple times to achieve test-time scaling. Experiments show significant improvements on benchmarks from various domains across multiple reasoning VLMs trained with RL, highlighting the promise of perception-centric supervision as a general-purpose strategy. For test-time scaling, it also demonstrates consistent performance gains over other strategies, such as major voting. Our code and data will be publicly released at https://github.com/RUCAIBox/Perceval.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Vision-language Models with Perception-centric Process Reward Models》的结构化深入分析总结：

### 1. 核心问题与整体含义
*   **研究背景**：虽然强化学习（RLVR）显著提升了视觉语言模型（VLM）的推理能力，但目前的监督信号主要集中在“结果级”（Outcome-level）。
*   **核心问题**：结果级奖励过于粗糙，无法诊断推理链条中间出现的感知错误（如幻觉、空间关系误判）。这种“稀疏奖励”导致模型难以精准定位错误步骤，限制了复杂视觉推理性能的进一步提升。
*   **研究目标**：开发一种以感知为中心的过程奖励模型（PRM），提供标记级（Token-level）的细粒度反馈，以纠正推理过程中的视觉偏差。

### 2. 方法论
论文提出了 **Perceval** 框架，包含 PRM 的训练、强化学习集成及推理优化：
*   **Perceval (PRM) 设计**：
    *   **错误定位模式**：采用“先思考后回答”模式。模型先提取响应中的感知声明，将其与图像证据比对，最后在 `<answer>` 标签中以 Python 列表形式返回错误的字符串片段。
    *   **训练流程**：通过查询选择（侧重视觉搜索）、Rollout 生成（收集 VLM 的原始输出）、自动标注（利用 Gemini-2.5-Pro 等强模型生成结构化反馈）和监督微调（SFT）四个阶段构建。
*   **过程监督强化学习 (PS-GRPO)**：
    *   **标记级优势重分配**：在 GRPO 算法基础上，利用 Perceval 识别出的幻觉片段生成二进制掩码 $M_i$。
    *   **公式调整**：将序列级优势 $\hat{A}_i$ 调整为标记级优势 $\hat{A}'_{i,t} = \hat{A}_i - \alpha \cdot m_{i,t} \cdot |\hat{A}_i|$。其中 $\alpha$ 是惩罚强度，专门针对幻觉标记施加额外惩罚。
*   **测试时扩展 (Test-time Scaling)**：
    *   **截断-重生成 (Truncate-then-Regenerate)**：发现错误后，截断错误片段及其后续内容，让模型基于正确的上下文重新生成。
    *   **截断-思考-重生成**：在截断处加入 Perceval 的反馈意见，引导模型进行自我反思后再生成。

### 3. 实验设计
*   **数据集与场景**：
    *   **视觉搜索/感知**：V* (V-Star), BLINK, MMStar, MME-RealWorld, RealWorldQA。
    *   **数学与图表推理**：MathVista, MATH-Vision, ChartQA。
*   **Benchmark 与对比方法**：
    *   **基座模型**：Qwen2.5-VL (3B 和 7B 版本)。
    *   **对比 RL 方法**：VLM-R1, LMM-R1, R1-VL, Perception-R1, Vision-R1, DeepEyes 等十余种近期主流的视觉强化学习模型。
*   **评估指标**：准确率（Accuracy），针对 ChartQA 使用松散准确率（Relaxed Accuracy）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **模型规模**：实验主要基于 3B 和 7B 参数规模的模型进行，PRM 模型（Perceval）也对应训练了 3B 和 7B 两个版本。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 个主流视觉推理基准测试，对比了超过 10 种 SOTA 模型，实验覆盖面广。
*   **消融实验**：针对惩罚强度参数 $\alpha$ 进行了详细测试（取值 0 到 0.3），验证了 $\alpha=0.1$ 为最优平衡点。
*   **客观性**：采用了统一的评估流水线（Greedy Decoding + GPT-4o-mini 辅助判定），并针对“奖励作弊”（Reward Hacking）进行了专门的趋势分析，证明了 Perceval 引导的是真实能力提升而非投机取巧。

### 6. 主要结论与发现
*   **性能提升**：Perceval 在 3B 和 7B 规模上均显著优于纯 GRPO 基准。在视觉搜索任务上，3B 模型平均提升约 4%。
*   **能力泛化**：尽管 PRM 主要针对感知错误进行训练，但这种能力的提升成功泛化到了数学和图表推理任务中，证明了“精准感知是复杂推理的基石”。
*   **推理优化有效性**：提出的“截断-重生成”策略在测试时扩展表现上优于传统的“多数投票”（Major Voting），尤其在处理模型自身难以发现的隐蔽错误时更有效。

### 7. 优点
*   **细粒度监督**：成功将粗糙的序列奖励细化到 Token 级别，解决了 RLVR 中的信度分配（Credit Assignment）难题。
*   **可解释性强**：Perceval 提供的反馈是结构化的文本片段，不仅能用于训练，还能直接用于推理阶段的纠错和反思。
*   **缓解奖励作弊**：通过干预优势计算而非直接给出标量分数，降低了策略模型过度拟合奖励模型的风险。

### 8. 不足与局限
*   **标注依赖**：PRM 的训练高度依赖于更强大的模型（如 Gemini-2.5-Pro）进行自动标注，这可能引入上位模型的偏见或错误。
*   **推理延迟**：测试时扩展（截断-重生成）虽然提升了准确率，但多次迭代会显著增加推理的时间成本和计算开销。
*   **惩罚噪声**：如消融实验所示，如果惩罚强度过大，可能会误伤幻觉片段中语法必需但事实无害的词汇（如冠词、介词），导致训练不稳定。

（完）
