---
title: Optimal Planning and Control under Signal Temporal Logic Specifications
title_zh: 信号时序逻辑约束下的最优规划与控制
authors: "Zuodong Pan, Xu Fang, Wei Ren"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24107v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于规划和控制的信号时序逻辑 (STL) 规范
tldr: 本研究针对非线性系统在信号时序逻辑（STL）约束下的规划与控制问题，提出了一种从局部到全局的综合策略。通过将复杂的STL任务分解为有限的局部任务，利用采样方法生成局部航点序列并构建安全走廊，最终通过求解优化问题得到满足任务要求的轨迹。该方法有效解决了复杂逻辑约束下的轨迹生成问题，并通过数值实验验证了其高效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24107v1/fig-001.webp\", \"caption\": \"Fig. 3. Illustration of the simulation results in Scenario 1. Here, πr := R(µr), r ∈ {1, . . . , 6}. (a) The results for generating the global waypoints, satisfaction pairs, and safe corridor. (b) The control inputs of the unicycle. (c) The trajectory of the unicycle.\", \"page\": 7, \"index\": 1, \"width\": 1066, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.24107v1/fig-002.webp\", \"caption\": \"Fig. 1. Overall architecture of the proposed approach. (a) The STL task Ψ is decomposed into Ψ1 and Ψ2 with uati(Ψ1) = [ξ0, ξ1] and uati(Ψ2) = [ξ1, ξ2], respectively. (b) The sequences of local waypoints (orange and green points) satisfying Ψ1 and Ψ2 are generated and then synthesized into the sequence of global waypoints. Blue points are the satisfaction pairs with respect to Ψ. (c) Based on the sequence of global waypoints, a safe corridor (red blocks) is constructed. An optimal position trajectory (red line) satisfying Ψ is derived.\", \"page\": 3, \"index\": 2, \"width\": 1066, \"height\": 284}]"
motivation: 旨在解决非线性系统在满足复杂信号时序逻辑（STL）规范时的最优路径规划与控制难题。
method: 采用任务分解策略，结合采样算法生成局部航点，并通过构建安全走廊和求解优化问题来实现全局轨迹合成。
result: 数值仿真和对比实验结果表明，该方法能够生成满足STL规范的有效位置轨迹。
conclusion: 该研究提供了一种处理非线性系统复杂逻辑约束的系统化框架，显著提升了规划任务的执行效率和安全性。
---

## 摘要
本文研究了非线性系统在信号时序逻辑（STL）约束下的规划与控制问题。我们首先将一个 STL 任务分解为有限个局部任务。通过基于采样的方法生成满足所有局部任务的局部路标点序列，并由此推导出相应的满足对集合。遵循从局部到全局的策略，将所有局部路标点序列合成为一个全局序列，并据此构建安全走廊。利用安全走廊和满足对集合，建立并求解一个优化问题，以获得满足 STL 任务的位置轨迹。最后，通过数值算例和对比结果证明了所提方法的有效性。

## Abstract
This paper addresses the planning and control problem for nonlinear systems under Signal Temporal Logic (STL) specifications. We first decompose an STL task into finite local tasks. A sampling-based method generates sequences of local waypoints to satisfy all local tasks, from which the corresponding satisfaction pair sets are derived. Following a local-to-global strategy, all sequences of local waypoints are synthesized into a global one, based on which a safe corridor is then constructed. Leveraging the safe corridor and the satisfaction pair sets, an optimization problem is formulated and solved to derive a position trajectory that satisfies the STL task. Finally, numerical examples and comparative results are presented to demonstrate the efficacy of the proposed approach.

---

## 论文详细总结（自动生成）

这是一份关于论文《Optimal Planning and Control under Signal Temporal Logic Specifications》（信号时序逻辑约束下的最优规划与控制）的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是如何为**非线性系统**（如移动机器人）设计一种既能满足**复杂信号时序逻辑（STL）**规范，又能保证**避障安全性**和**轨迹最优性**的规划与控制框架。
*   **背景**：传统的线性时序逻辑（LTL）缺乏显式时间约束，而 STL 引入了时间区间和定量语义。
*   **动机**：现有的 STL 控制方法（如基于 MILP 或控制障碍函数 CBF 的方法）在处理**嵌套公式**（如“在某段时间内每隔多久巡逻一次”）时面临计算爆炸或难以处理的问题。本文旨在提出一种高效的策略，解决包含非嵌套和嵌套 STL 任务的混合规范问题。

### 2. 方法论：核心思想与算法流程
论文提出了一种“三步走”的**局部到全局（Local-to-Global）**策略：
1.  **STL 任务分解**：
    *   将复杂的全局 STL 任务根据时间区间（UATI）分解为多个有限的局部任务。
    *   针对“始终（Always）”和“最终（Eventually）”等算子进行时间切分，确保每个局部区间内的子任务清晰可解。
2.  **局部到全局规划（采样阶段）**：
    *   使用**目标偏置的快速搜索随机树（Goal-Biased RRT）**在时空空间（Spatio-Temporal Space）内为每个局部任务生成路标点序列。
    *   通过 `STLSat` 算法验证采样点是否满足 STL 语义，并提取“满足对集合”（即哪些时间点必须在哪些区域内）。
    *   将所有局部路标点合成为全局路标点序列。
3.  **轨迹优化控制（优化阶段）**：
    *   **安全走廊构建**：基于全局路标点生成一系列凸的多胞体区域（Safe Corridor），将复杂的避障约束简化。
    *   **非线性优化求解**：利用 CasADi 工具箱，将轨迹平滑度、路径长度作为目标函数，将安全走廊和 STL 满足对作为约束条件，求解出最优的控制序列和连续轨迹。

### 3. 实验设计
*   **实验场景**：使用经典的**单轮车（Unicycle）**非线性动力学模型，在包含多个障碍物和目标区域的 2D 空间中进行仿真。
*   **对比 Benchmark**：主要对比了基于**混合整数线性规划（MILP-based）**的方法（Sun et al., 2022）。
*   **具体场景**：
    *   **场景 1**：包含复杂的嵌套任务（如周期性巡逻、条件到达、到达并停留）。
    *   **场景 2**：侧重于非嵌套 STL 公式的处理能力对比。
    *   **场景 3**：评估处理多个“到达并停留（Reach-Stay）”任务的性能。

### 4. 资源与算力
*   **硬件环境**：Intel Core i5-10400 处理器，16 GB RAM。
*   **软件环境**：MATLAB R2024a，使用 CasADi 优化工具箱。
*   **算力说明**：文中未提及使用 GPU，所有计算均在 CPU 上完成。计算时间在 3.44s 到 5.30s 之间，显示出该方法具有较好的实时性潜力。

### 5. 实验数量与充分性
*   **实验规模**：论文设计了 3 组代表性场景。
*   **充分性评价**：
    *   **对比维度**：从计算时间（Time）和轨迹长度（Traj Length）两个关键指标进行了量化对比。
    *   **客观性**：实验展示了 MILP 方法在处理某些嵌套逻辑（场景 1）时直接失效的情况，突出了本文方法的代际优势。
    *   **局限性**：实验主要集中在单机仿真，缺乏多机协作或真实物理环境下的消融实验（如不同采样步长对成功率的影响）。

### 6. 主要结论与发现
*   **处理能力更强**：所提方法能够处理 MILP 方法无法解决的复杂嵌套 STL 任务（如 `G F` 类型的巡逻任务）。
*   **效率更高**：在处理相同任务时，本文方法生成的轨迹更短（更优），且计算时间通常比 MILP 方法更短（例如场景 3 中，本文方法耗时 3.44s，而 MILP 需 10.66s）。
*   **解的质量**：通过安全走廊和非线性优化的结合，生成的轨迹比纯采样方法更平滑，更符合动力学约束。

### 7. 优点（亮点）
*   **解耦复杂性**：通过任务分解和安全走廊技术，将原本难以求解的非线性非凸优化问题转化为更易处理的形式。
*   **局部到全局策略**：采样阶段保证了逻辑上的可达性，优化阶段保证了物理上的最优性，兼顾了逻辑表达力和控制性能。
*   **通用性**：该框架不依赖于特定的线性化假设，适用于一般的非线性系统。

### 8. 不足与局限
*   **采样随机性**：底层依赖 RRT 采样，在极度狭窄或复杂的空间中，采样效率和成功率可能不稳定。
*   **假设限制**：论文假设嵌套任务的时间区间不重叠（Assumption 2），这限制了其在某些极度复杂的交织任务中的应用。
*   **静态环境**：目前的实验均在静态障碍物环境下进行，未讨论动态障碍物或不确定性环境下的鲁棒性。
*   **离散化依赖**：任务分解依赖于采样周期的倍数假设，对于极高精度的连续时间任务可能存在离散化误差。

（完）
