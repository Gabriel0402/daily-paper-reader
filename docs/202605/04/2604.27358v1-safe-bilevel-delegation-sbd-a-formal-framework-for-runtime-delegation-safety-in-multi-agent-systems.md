---
title: "Safe Bilevel Delegation (SBD): A Formal Framework for Runtime Delegation Safety in Multi-Agent Systems"
title_zh: 安全双层委派 (SBD)：多智能体系统中运行时委派安全的形式化框架
authors: Yuan Sun
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27358v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 大语言模型多智能体系统中运行时委派安全的正式框架
tldr: 本研究针对高风险环境下大模型智能体的子任务授权安全问题，提出了安全双层授权（SBD）形式化框架。该框架通过双层优化动态调整安全与效率的权衡，引入连续授权度实现从人工干预到自主执行的平滑过渡。研究证明了安全性单调性、策略收敛性及责任传播边界，并在医疗、金融和教育领域进行了实例化，为多智能体系统的运行时安全提供了理论保障。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的多智能体架构缺乏在执行过程中根据任务上下文动态调整安全与效率权衡的运行时机制。
method: 提出SBD框架，利用外层网络学习环境相关的安全权重，内层循环在概率安全约束下优化授权策略。
result: 理论证明了安全权重的单调性影响、内层策略的线性收敛性以及多级授权链中的责任传播上限。
conclusion: SBD为高风险领域的多智能体协作提供了一套可证明安全的动态授权方案，平衡了系统自主性与风险控制。
---

## 摘要
随着大语言模型 (LLM) 智能体被部署在关键任务环境中，如何安全地将子任务委派给专业化子智能体变得至关重要。现有工作主要在设计阶段解决多智能体架构选择问题，或提供广泛的经验性指导，但都未能提供一种在执行过程中随任务上下文变化动态调整安全与效率权衡的运行时机制。我们提出了安全双层委派 (Safe Bilevel Delegation, SBD)，这是一种用于分层多智能体系统中运行时委派安全的形式化框架。SBD 将任务委派建模为一个双层优化问题：外层元权重网络 phi 学习依赖于上下文的安全-效率权重 lambda(s) ∈ [0,1]；内层循环在满足概率安全约束 P(safe) >= 1-delta 的前提下优化委派策略 pi。连续委派度 alpha ∈ [0, 1] 控制转移给每个子智能体的决策权限，在完全人工干预 (alpha=0) 与完全自主执行 (alpha=1) 之间进行平滑插值。我们确立了三个理论结果：(1) 安全单调性——更高的外层安全权重会产生弱更安全的内层策略；(2) 内层策略收敛性——在标准平滑性假设下，内层问题的投影梯度下降呈线性收敛；(3) 问责传播界限，该界限在多跳委派链中分配责任，并具有可证明的单智能体上限。我们在三个关键领域——医疗 AI (MIMIC-III)、金融风险控制 (S&P 500) 和教育智能体监管 (ASSISTments) 中实例化了 SBD，并指定了数据集、安全约束集、基准模型和评估协议。本手稿完整展示了形式化框架和理论结果；按照文中所述协议进行的实证验证正在计划中，并将在后续修订版中报告。

## Abstract
As large language model (LLM) agents are deployed in high-stakes environments, the question of how safely to delegate subtasks to specialized sub-agents becomes critical. Existing work addresses multi-agent architecture selection at design time or provides broad empirical guidelines, but neither provides a runtime mechanism that dynamically adjusts the safety-efficiency trade-off as task context changes during execution.   We propose Safe Bilevel Delegation (SBD), a formal framework for runtime delegation safety in hierarchical multi-agent systems. SBD formulates task delegation as a bilevel optimization problem: an outer meta-weight network phi learns context-dependent safety-efficiency weights lambda(s) in [0,1]; an inner loop optimizes the delegation policy pi subject to a probabilistic safety constraint P(safe) >= 1-delta. The continuous delegation degree alpha in [0, 1] controls how much decision authority is transferred to each sub-agent, interpolating smoothly between full human override (alpha=0) and fully autonomous execution (alpha=1).   We establish three theoretical results: (1) Safety Monotonicity--higher outer safety weight produces a weakly safer inner policy; (2) Inner Policy Convergence--projected gradient descent on the inner problem converges linearly under standard smoothness assumptions; (3) an Accountability Propagation bound that distributes responsibility across multi-hop delegation chains with a provable per-agent ceiling. We instantiate SBD in three high-stakes domains--medical AI (MIMIC-III), financial risk control (S and P 500), and educational agent supervision (ASSISTments)--specifying datasets, safety constraint sets, baselines, and evaluation protocols. This manuscript presents the formal framework and theoretical results in full; empirical validation following the protocols described herein is planned and will be reported in a forthcoming revision.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**安全双层委派（Safe Bilevel Delegation, SBD）**的形式化框架，旨在解决大语言模型（LLM）多智能体系统在执行高风险任务时，如何动态、安全地将子任务授权给子智能体的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在医疗、金融等高风险领域，LLM 智能体需要将复杂任务拆解并委派给专门的子智能体。现有的多智能体架构多在设计阶段固定，缺乏一种能在运行时（Runtime）根据具体任务上下文动态平衡“安全性”与“效率”的机制。
*   **研究动机**：现有的委派机制要么过于保守（效率低下），要么缺乏安全保障（风险不可控）。研究者希望建立一套数学框架，使系统能在完全人工干预（低效但安全）与完全自主执行（高效但潜在风险）之间实现平滑、可证明安全的过渡。

### 2. 论文提出的方法论
SBD 框架将任务委派建模为一个**双层优化（Bilevel Optimization）问题**：
*   **外层优化（Outer Loop）**：利用元权重网络 $\phi$ 学习一个依赖于上下文的安全权重 $\lambda(s) \in [0,1]$。这个权重决定了系统在当前状态下对安全性的重视程度。
*   **内层优化（Inner Loop）**：在给定 $\lambda(s)$ 的情况下，优化委派策略 $\pi$。该优化受到概率安全约束 $P(\text{safe}) \ge 1-\delta$ 的限制，确保失败风险在可控范围内。
*   **连续委派度（Delegation Degree, $\alpha$）**：引入参数 $\alpha \in [0, 1]$，实现决策权的平滑转移。$\alpha=0$ 代表完全由人工干预，$\alpha=1$ 代表完全自主。
*   **理论保障**：
    1.  **安全单调性**：证明了增加外层安全权重必然导致内层策略趋向于更安全（或至少不更差）。
    2.  **收敛性**：证明了在标准平滑性假设下，内层问题的投影梯度下降具有线性收敛性。
    3.  **责任传播界限**：推导了多级委派链中的责任分配公式，并设定了单智能体的责任上限。

### 3. 实验设计
论文在三个关键领域设计了实例化方案和评估协议：
*   **医疗 AI**：使用 **MIMIC-III** 数据集，关注临床决策支持中的安全性。
*   **金融风险控制**：使用 **S&P 500** 相关数据，评估交易委派中的风险管理。
*   **教育监管**：使用 **ASSISTments** 数据集，研究智能导师系统对学生干预的授权。
*   **对比基准（Benchmarks）**：虽然文中提到指定了基准模型和评估协议，但本版本主要侧重于理论框架。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于本手稿侧重于形式化框架和理论证明，实证部分的算力消耗预计将在后续包含实验结果的修订版中披露。

### 5. 实验数量与充分性
*   **实验状态**：论文明确指出，目前版本**完整展示了形式化框架和理论结果**，而按照所述协议进行的**实证验证（Empirical Validation）正在计划中**。
*   **充分性评价**：从理论角度看，论文提供了严谨的数学证明（单调性、收敛性、责任界限），逻辑自洽。但由于缺乏实际运行的实验数据，其在真实复杂环境下的鲁棒性和性能表现尚待验证。

### 6. 论文的主要结论与发现
*   **动态平衡的可行性**：证明了可以通过双层优化在运行时动态调整安全与效率的权衡。
*   **理论完备性**：SBD 框架为多智能体协作提供了一套可证明安全的底层逻辑，特别是解决了多级委派中的“责任归属”难题。
*   **平滑过渡**：通过连续参数 $\alpha$，系统可以根据风险评估结果，在人类监督和机器自主之间实现无缝切换。

### 7. 优点
*   **形式化严谨**：将模糊的“安全委派”概念转化为严格的数学优化问题。
*   **运行时动态性**：突破了传统静态架构的限制，允许系统根据实时上下文（Context）调整策略。
*   **责任量化**：提出的责任传播界限对于高风险行业的合规性具有重要的参考价值。

### 8. 不足与局限
*   **缺乏实证数据**：当前版本仅有理论框架和实验设计，缺乏实际的实验结果支撑，无法评估算法在处理大规模数据时的计算开销。
*   **双层优化复杂度**：双层优化问题通常具有较高的计算复杂度，在实时性要求极高的场景下可能存在性能瓶颈。
*   **假设依赖**：理论证明依赖于“标准平滑性假设”，在极端非线性或离散的 LLM 决策空间中，这些假设的适用性可能受限。

（完）
