---
title: Runtime Monitoring of Perception-Based Autonomous Systems via Embedding Temporal Logic
title_zh: 基于嵌入时序逻辑的感知驱动自主系统运行时监控
authors: "Parv Kapoor, Abigail Hammer, Ashish Kapoor, Karen Leung, Eunsuk Kang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12651v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于感知驱动自主系统监控的嵌入式时态逻辑
tldr: 本研究针对感知驱动的自主系统，提出了一种嵌入时序逻辑（ETL）框架，旨在解决传统监控方法在处理高维传感器数据时映射困难且不稳定的问题。ETL直接在学习到的嵌入空间中运行，通过计算观测嵌入与目标嵌入间的距离来定义谓词，并结合时序算子表达复杂的感知行为。此外，引入了保形校准程序以确保监控的可靠性。该方法在机械臂操作任务中表现出色，能够准确评估符合真实语义的时序规范。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12651v2/fig-001.webp\", \"caption\": \"Figure 3: Dual-predicate Boolean timelines for mw-pick-place-wall. Top panels show distances to grasp (zA) and place (zB) embeddings with thresholds ϵ∗ and ϵCP . Lower panels show ETL and ground-truth predicate activations.\", \"page\": 18, \"index\": 1, \"width\": 718, \"height\": 722}, {\"url\": \"assets/figures/arxiv/2605.12651v2/fig-002.webp\", \"caption\": \"Figure 2: Qualitative and quantitative ETL results.\", \"page\": 8, \"index\": 2, \"width\": 821, \"height\": 644}, {\"url\": \"assets/figures/arxiv/2605.12651v2/fig-003.webp\", \"caption\": \"Figure 1: Overview of embedding-based runtime monitoring. Top: Target observations OG = {og1, . . . , ogn} and the online observation ot are encoded by a pretrained vision encoder into target embeddings zG and the current embedding zt. An embedding predicate then evaluates whether zt reaches the target set. Bottom left: The embedding trajectory is projected onto its first two principal components, showing its evolution relative to the target embeddings. Bottom right: Runtime monitoring computes the distance from zt to zG over time, thresholds it using ϵ, and evaluates the temporal logic specification.\", \"page\": 4, \"index\": 3, \"width\": 814, \"height\": 406}]"
motivation: 传统运行时监控依赖于将感知数据映射到离散逻辑命题，这在处理高维感知数据时计算昂贵、脆弱且语义不一致。
method: 提出嵌入时序逻辑（ETL），通过在嵌入空间中定义基于距离的谓词，并结合时序算子和保形校准程序直接对感知轨迹进行监控。
result: 在多个机械臂操作环境下的实验证明，ETL在监控复杂的时序感知行为时，与地面真值语义保持了高度的一致性。
conclusion: ETL为感知驱动系统的运行时监控提供了一种无需显式状态估计的高效方案，显著提升了复杂感知任务的安全性评估能力。
---

## 摘要
自主系统的运行时监控传统上依赖于将连续的传感器观测映射到定义在低维状态变量上的离散逻辑命题。这种抽象在感知驱动的场景中会失效，因为此类映射需要额外的学习模块，而这些模块通常计算开销大、脆弱且语义不一致。在这项工作中，我们提出了嵌入时序逻辑（ETL），这是一种直接在学习到的嵌入空间中执行监控的时序逻辑。ETL 通过观测到的嵌入与源自参考观测的目标嵌入之间的距离来定义谓词。这种形式化方法允许规范捕捉高层感知概念，例如与视觉目标的相似性或对语义区域的规避，而这些概念使用传统谓词很难或无法表达。通过将这些谓词与时序算子组合，ETL 能够自然地表达时序延伸和顺序的感知行为。我们引入了 ETL 监控器，用于评估有界嵌入轨迹上的规范，并结合了符合性校准程序，以提供可靠且面向安全的谓词评估。我们在多个操作环境中评估了我们的方法，结果表明 ETL 与真实语义具有很强的经验一致性，包括对时序组合行为的准确监控。

## Abstract
Runtime monitoring of autonomous systems traditionally relies on mapping continuous sensor observations to discrete logical propositions defined over low-dimensional state variables. This abstraction breaks down in perception-driven settings, where such mappings require additional learned modules that are often computationally expensive, brittle, and semantically misaligned. In this work, we propose Embedding Temporal Logic (ETL), a temporal logic that performs monitoring directly in learned embedding spaces. ETL defines predicates through distances between observed embeddings and target embeddings derived from reference observations. This formulation allows specifications to capture high-level perceptual concepts, such as similarity to visual goals or avoidance of semantic regions, that are difficult or impossible to express using traditional predicates. By composing these predicates with temporal operators, ETL naturally expresses temporally extended and sequential perceptual behaviors. We introduce ETL monitors for evaluating specifications over bounded embedding traces, along with a conformal calibration procedure that provides reliable and safety-oriented predicate evaluation. We evaluate our approach across multiple manipulation environments to show that ETL achieves strong empirical agreement with ground-truth semantics, including accurate monitoring of temporally composed behaviors.

---

## 论文详细总结（自动生成）

这篇论文由卡内基梅隆大学、华盛顿大学等机构的研究人员合作完成，提出了一种名为**嵌入时序逻辑（Embedding Temporal Logic, ETL）**的新框架，旨在解决感知驱动的自主系统在运行时监控（Runtime Monitoring）中的语义对齐与鲁棒性问题。

以下是对该论文的结构化深入分析：

### 1. 核心问题与研究动机
*   **核心问题**：传统的运行时监控（如使用 STL 或 LTL）依赖于将高维传感器数据（如图像）映射到低维、离散的逻辑命题（如“机器人坐标 < x”）。但在复杂的感知驱动系统中，这种映射需要额外的分类器或检测器，这些模块往往**计算昂贵、脆弱且容易产生语义偏差**。
*   **研究动机**：现代自主系统（如自动驾驶、机械臂）多基于深度学习的嵌入空间（Embedding Space）运行。论文试图探索：能否直接在这些预训练的嵌入空间中定义逻辑规范，从而跳过显式的状态估计步骤，实现更直接、高效的在线安全监控？

### 2. 方法论：嵌入时序逻辑 (ETL)
*   **核心思想**：利用预训练视觉编码器（如 DINOv2, CLIP）的几何特性——即语义相似的观测在嵌入空间中距离较近。ETL 将逻辑谓词定义为观测嵌入与“目标/参考嵌入”之间的距离关系。
*   **关键技术细节**：
    *   **嵌入谓词（Embedding Predicate）**：定义为 $ap \equiv (Z_{target}, d, \epsilon, \bowtie, a)$。其中 $Z_{target}$ 是目标状态的参考嵌入集合，$d$ 是距离函数（如 L2 或余弦距离），$\epsilon$ 是阈值。
    *   **语义定义**：如果观测嵌入 $z_t$ 与目标集合的距离（通过聚合算子 $a$ 计算，如 $min$）满足阈值条件，则谓词为真。
    *   **时序算子**：保留了经典时序逻辑的算子（如 $\neg, \wedge, U, F, G$），允许表达如“始终避开障碍物”或“先抓取再放置”等复杂时序行为。
    *   **阈值校准**：
        1.  **F1-Optimal**：在校准集上搜索使 F1 分数最大化的阈值。
        2.  **保形校准（Conformal Calibration）**：利用保形预测理论，在给定错误水平 $\alpha$ 下提供具有统计保证的召回率阈值，这对于安全敏感场景至关重要。

### 3. 实验设计
*   **实验场景**：
    1.  **简单导航（Dubins Car）**：在受控的 2D 环境中验证 ETL 与地面真值（Ground-truth）状态逻辑的一致性。
    2.  **模拟操作任务**：使用 D3IL（堆叠、分类任务）和 MetaWorld（取放任务）基准，涉及复杂的物体交互和干扰项。
    3.  **真实世界数据（DROID）**：使用大规模真实机器人操作数据集，评估 ETL 在非结构化环境中的表现。
*   **对比方法（Baselines）**：
    *   **PCA-kmeans**：基于主成分分析和聚类的方法。
    *   **logpZO**：基于流匹配（Flow-matching）密度模型的异常检测方法。
    *   **VLM Baseline**：使用最先进的视觉语言模型（Qwen2-VL-2B）进行直接推理。

### 4. 资源与算力
*   **硬件环境**：论文明确指出实验运行在计算集群上，使用了 **2 台 NVIDIA GeForce RTX 5090 GPU**，每台拥有 34.2 GB 显存。
*   **训练/推理时长**：文中未详细列出具体的训练总时长，但强调了运行时监控的轻量化特性，适合在线部署。

### 5. 实验数量与充分性
*   **实验规模**：
    *   Dubins Car 使用了 100 条轨迹进行验证。
    *   模拟环境涵盖了 3 种不同的复杂任务。
    *   真实世界 DROID 数据集选取了 5 种具有代表性的组合任务（如“瓶子移至炉灶”）。
*   **充分性评价**：实验设计较为全面，涵盖了从简单仿真到复杂真实数据的跨度。通过对比 F1 分数、准确率、召回率以及时序顺序一致性，验证了 ETL 的有效性。消融实验体现在不同阈值校准策略（F1 vs CP）的对比上。

### 6. 主要结论与发现
*   **语义一致性**：ETL 谓词能高度准确地近似地面真值命题，在导航任务中 F1 分数达到 0.80-0.85，在顺序任务中达到 100% 的排序准确率。
*   **性能优越**：在模拟操作任务中，ETL 的平均 F1 分数（0.817）优于 logpZO（0.790）和 PCA-kmeans（0.751）。
*   **真实世界可靠性**：在 DROID 数据集上，ETL 的表现显著优于基于 VLM 的监控方案（F1 分数 0.813 vs 0.390），证明了基于距离的几何谓词比纯语言推理更稳定。
*   **安全保证**：保形校准能有效提升召回率，减少安全违规的漏报。

### 7. 优点与亮点
*   **无需显式建模**：跳过了复杂的状态估计和目标检测，直接利用预训练模型的表征能力。
*   **数学严谨性**：将时序逻辑的严谨性与深度学习嵌入空间的灵活性结合，并引入保形预测提供统计保证。
*   **通用性强**：不依赖于特定的编码器，可随视觉基础模型（VFM）的进步而自动提升性能。

### 8. 不足与局限
*   **可解释性挑战**：虽然逻辑算子是可解释的，但“嵌入空间中的距离”对人类来说仍缺乏直观的物理含义（例如，很难解释 0.5 的距离具体代表什么物理偏差）。
*   **表征依赖性**：监控效果高度依赖于预训练编码器的质量。如果编码器未能捕捉到任务相关的细微特征（如物体的微小位移），监控器可能会失效。
*   **阈值敏感性**：尽管有校准机制，但在分布偏移（OOD）严重的场景下，静态阈值可能仍需动态调整。

（完）
