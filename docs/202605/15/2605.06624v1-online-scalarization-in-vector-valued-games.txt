Title: Online Scalarization in Vector-Valued Games

URL Source: https://arxiv.org/pdf/2605.06624v1

Published Time: Fri, 08 May 2026 01:46:48 GMT

Number of Pages: 8

Markdown Content:
# Online Scalarization in Vector-Valued Games 

Ehsan Asadollahi, Calvin Hawkins, Matthew Hale 1

Abstract — We study repeated multi-player vector-valued games in which a player observes a payoff vector each round and evaluates outcomes through linear scalarizations of those vectors. Different from most prior works, the choice of scalarization is treated as an online decision variable rather than a fixed modeling decision. We propose a bi-level learning framework in which an outer learner chooses a scalarization from a finite candidate class on a slow timescale, while a faster inner bandit no-regret learner selects actions using the scalar feedback induced by the chosen scalarization. Performance of this approach is defined with respect to a certain true weight vector, and the deployed scalarizations act as control signals that shape the induced payoff trajectory. We provide imple-mentable algorithms based on bandit online mirror descent with stabilized importance weighting, and we derive finite-time performance guarantees in the form of sublinear regret bounds. Experiments on a vector-valued extension of a canonical game show that convergence to the preferred equilibrium rises from roughly 50% under non-adaptive scalarization to about 80% 

under our proposed method. 

I. I NTRODUCTION 

Multi-agent systems often have more than one objective, and agents may operate under multiple criteria such as safety, efficiency, fairness, robustness, and compliance, which may not be naturally commensurate [1]–[3]. Such formulations naturally lead to vector-valued measures of performance, where each decision produces a multi-dimensional outcome and agents must evaluate that outcome according to their own preferences. In many settings, vector-valued decision-making proceeds by scalarizing vector-valued outcomes using a fixed weight vector that specifies the relative importance of the different objectives. We study such vector-valued outcomes in the setting of repeated games, but rather than treating a scalarization as a fixed modeling decision, we treat it as an algorithmic control knob that a player updates across repeated interactions. The trajectory of play depends on the choice of weight vector because that choice affects the player’s actions and thus how opponents respond to those actions. As a result, the time-varying weight vector used in a player’s learning rule can be chosen to shape its interactions. We study this approach in a multi-player repeated game with vector-valued outcomes, and we design strategies for a choice of focal player. The focal player evaluates perfor-mance using a fixed objective weight vector that defines its performance criterion, but it may deploy alternative weight vectors over time. The reason for using different weight              

> 1School of Electrical and Computer Engineering, Geor-gia Institute of Technology, Atlanta, GA, USA. Emails:
> {sasadoll3,chawkins64,mhale30 }@gatech.edu
> This work was supported by ONR under grant N00014-24-1-2432.

vectors is that we do not use a fully adversarial minimax model in which opponents can select responses to negate any potential benefit from such reweighting. In that worst-case setting, adapting weights on objectives cannot be expected to improve worst-case performance. Our aim is instead to understand how adaptation can improve performance in responsive environments in which the opponent’s behavior depends on the observed outcomes. For example, the opponent may itself be a learning agent, such as a no-regret learner. This formulation creates an endogenous coupling: changing the focal player’s deployed weight vector changes the player’s actions, which can change the opponents’ responses, which changes future outcomes and ultimately changes the player’s performance with respect to its objective. Consequently, simply deploying the objective weight vector need not be optimal in finite time. We therefore search over weight vectors using a bi-level learning architecture. The inner layer is an action-selection learner that operates under the deployed weight vector and updates a mixed action distribution from bandit feedback, meaning that it observes only the payoff of the action actually played rather than the payoffs of all actions. The outer layer selects the deployed weight vector and updates a distribution over deployed weight vectors on a slower timescale. The outer layer is evaluated using outcomes aggregated over time and scored under the fixed objective weight vector, while the inner layer uses per-round bandit feedback computed under the deployed weight vector. This separation between shaping and evaluation enables deployed-weight selection to influence the coupled learning dynamics without changing the focal player’s ultimate objective. To summarize, our contributions are as follows:  

> •

We propose a bi-level learning architecture for on-line scalarization in vector-valued games (Algorithms 1 and 2).  

> •

We derive finite-time regret bounds for both levels of the algorithm, and we show sublinear regret for both (Theorems 1 and 2).  

> •

We demonstrate experimentally in a representative two-equilibrium game that convergence to the objective-favored equilibrium increases from roughly 50% under standard no-regret learning to about 80% under our method (Section V). 

A. Related Work 

One classical viewpoint on vector-valued games is set-based and uses Blackwell’s approachability framework. In repeated vector-valued interactions in this framework, aplayer seeks to drive the time-average payoff toward a target 

> arXiv:2605.06624v1 [cs.GT] 7 May 2026

set C ⊆ Rd [4]. A set C is “approachable” if there exists a strategy such that the time-average payoff converges to C

against any opponent [5]–[11]. In contrast, we do not study robust convergence to a fixed target set. Instead, we study online selection of scalarizations as a mechanism for shaping equilibrium outcomes. Another classical viewpoint is direction-based and obtains a scalar decision criterion from vector outcomes through linear scalarization [1], [12]–[14]: a player selects a weight vector and evaluates outcomes by projection onto that di-rection. This approach produces a family of induced scalar games indexed by the choice of weight vectors. Under suitable regularity conditions, changing the scalarization can recover different weak equilibria of the original vector-valued game [15]. This equilibrium correspondence high-lights a key modeling point: changes in weight vectors can induce changes in the effective incentives and thereby induce changes in players’ best responses and the resulting equilibria. We act on this idea by developing a bi-level learning algorithm in which the player adaptively selects its weight vector online in order to shape the induced scalar game and, in turn, the resulting equilibrium outcomes. The rest of the paper is organized as follows. Section II reviews vector-valued games and states the problems we solve. Section III presents the bi-level learning protocol. Section IV develops finite-time regret guarantees. Section V reports empirical results, and Section VI concludes. 

Notation: We use R to denote the reals and N to denote the positive integers. For n ∈ N, we define [n] := {1, . . . , n }.Given a collection (zi)i∈N , we use z−i := ( zj )j̸ =i to denote the sub-collection of all components except the ith , and we use (zi; z−i) to denote the collection formed by combining zi

with z−i. We use ∥x∥p to denote the ℓp-norm of a vector x,and Bp(r) := {x : ∥x∥p ≤ r} is the ℓp-norm closed ball of radius r centered on the origin. The notation conv( S)

denotes the convex hull of a set S. For a finite set S, the notation ∆( S) denotes the probability simplex over S. We denote by cone( A) the conic hull of a set A, i.e., the set of all finite non-negatively weighted linear combinations of elements of A. We use Sd−1 := {x ∈ Rd : ∥x∥2 = 1 } to denote the unit sphere in Rd.II. B ACKGROUND AND PROBLEM STATEMENTS 

This section provides background on vector-valued games and then gives problem statements. 

A. Vector-Valued Games 

We study repeated interactions in a vector-valued game among n ∈ N players. Let 

N = [ n] (1) be the set of their indices, and consider T ∈ N rounds of the game. At each round t ∈ [T ], each player i ∈ N chooses an action 

ai,t ∈ A i, (2) where each Ai is a finite non-empty set. The payoff vector to player i for these actions is 

ui,t := ui(ai,t , a −i,t ) ∈ Rdi , ui : Ai × A −i → Rdi (3) for some di ∈ N. We write the time-averaged payoff over t ∈ [T ] as 

¯ui,T := 1

T

> T

X

> t=1

ui(ai,t , a −i,t ). (4) 

1) Cone-induced preferences and equilibria: For each i ∈ N , player i chooses a strategy xi ∈ Xi, where 

Xi is a nonempty compact convex set. The joint strategy profile is 

x = (x1, . . . , x n) ∈ X := Y

> i∈N

Xi. (5) If the joint strategy x is used at time t, then player i receives the payoff ui,t (x).In a repeated game, the action set Ai determines the set of actions available to player i at each round, and the strategy set Xi for player i contains the set of decision rules available to that player. Thus, a strategy 

xi ∈ Xi (6) may be interpreted as a rule for selecting actions from Ai.For example, if player i uses mixed actions, then Xi may be the simplex over the action set Ai.Preferences over payoff vectors are modeled by a cone-induced partial order. A non-empty set K ⊆ Rd is called a convex cone if αx + βy ∈ K for all x, y ∈ K and all 

α, β ≥ 0. For each i ∈ N , let 

Ki ⊆ Rdi (7) be a non-empty, closed, convex cone that encodes player i’s preferences. Then, for a, b ∈ Rdi we write 

a ≤Ki b if and only if b − a ∈ Ki, (8) which means that b is weakly preferred to a (namely, b is no worse than a) under the order induced by Ki. We write 

a < Ki b if b − a ∈ int( Ki), and then b is preferred to a

because it is strictly better than a.A vector-valued game takes the form 

G :=  N, {A i}i∈N , {ui}i∈N , {Xi}i∈N , {Ki}i∈N

, (9) where N is from (1), Ai is from (2), ui is from (3), Xi is from (6), and Ki is from (7). We now define equilibria for vector-valued games. 

Definition 1 (Equilibrium notions in a vector-valued game [15]) . Consider X from (5) . An element x⋆ ∈ X is called 

(i) a Nash equilibrium if for all i ∈ N and all xi ∈ Xi,the condition ui(xi; x∗−i) ≤Ki ui(x∗ 

> i

; x∗−i) implies that 

ui(x∗ 

> i

; x∗−i) ≤Ki ui(xi; x∗−i).

(ii) a weak Nash equilibrium if for all i ∈ N there does not exist xi ∈ Xi such that ui(xi; x∗−i) <Ki ui(x∗ 

> i

; x∗−i).We denote the corresponding sets of equilibria by NE( G) and WNE( G), respectively, and we have NE( G) ⊆ WNE( G).

In words, a Nash equilibrium is a profile at which each player’s outcome is maximal with respect to the cone order among all unilateral deviations, whereas a weak Nash equi-librium is a profile at which no player can obtain a strictly cone-better outcome by deviating unilaterally. 

2) Linear scalarization and equilibrium correspondence: 

A linear scalarization converts vector payoffs into scalar utilities by projecting vector payoffs onto a weight vector. For each i ∈ N , consider player i’s closed convex preference cone Ki and its induced order ≤Ki from (8). For each i ∈ N ,the admissible weights are naturally tied to player i’s cone-induced order through the dual cone of Ki. The Euclidean dual cone of Ki is 

K∗ 

> i

:= {ψi ∈ Rdi : ⟨ψi, y ⟩ ≥ 0 for all y ∈ Ki}.

If a joint strategy profile x ∈ X is used, then player i

receives the vector payoff ui(x). For each ψi ∈ K∗ 

> i

\ { 0},the corresponding linear scalarization is 

f ψi 

> i

(x) := ⟨ψi, u i(x)⟩.

Thus, if a strategy profile x is played, then player i’s vector payoff ui(x) is evaluated through the weight vector ψi.Now fix a weight profile ψ = (ψ1, . . . , ψ n) with 

ψi ∈ K∗ 

> i

\ {0} for all i ∈ N . This profile induces a scalarized game in which player i’s payoff is f ψi 

> i

(x).We write NE( ψ) for the set of Nash equilibria of this scalarized game. Restricting weights ψi to K∗ 

> i

\ { 0} ensures that the induced scalarization is monotone with respect to the preference order induced by Ki, which we prove in Section III. 

Proposition 1 (Scalarization characterization of weak Nash equilibria [15]) . For the vector-valued game G, the set of weak Nash equilibria satisfies 

WNE( G) = [ 

> ψ∈Qni=1 (K∗
> i\{ 0})

NE( ψ).

Proposition 1 implies that weight vectors can be used as an equilibrium-selection device: changing ψ changes the induced scalar game and hence changes which equilibria in 

NE( ψ) may be reached, while WNE( G) is determined by G.

B. Problem Formulation 

Proposition 1 shows that changing the scalarization can change which equilibria are reached. Motivated by this equi-librium correspondence, we distinguish between two roles for weight vectors in our setting: evaluation and deployment. For a fixed player i ∈ N , let 

ψobj  

> i

∈ K∗ 

> i

\ { 0} (10) denote the fixed objective weight vector that defines the performance criterion used to evaluate player i’s realized average vector payoff. Over a horizon T , player i’s objective performance is D

ψobj  

> i

, ¯ui,T 

E

,

where ¯ui,T is the time-averaged payoff from (4). During learning, however, player i need not use ψobj  

> i

inside its update rule. Instead, at each round t ∈ [T ], the player may deploy any weight vector ψi,t ∈ K∗ 

> i

\{ 0} and use it to guide action selection at round t. The deployed weight affects the player’s updates, which affect the player’s actions, which in turn can influence how the other players respond. 

Remark 1. In our formulation, the remaining players are not assumed to form a fully adversarial environment. Rather, they are modeled as payoff-responsive to the realized interaction; for example, they may update their own actions through no-regret learning based on their own scalar feedback. Consequently, the realized trajectory of play, and hence the average payoff ¯ui,T , depends on the deployed weight used by player i.

To make this dependence explicit, let ¯ui,T (ψi) denote the average payoff generated when player i fixes the deployed weight ψi over the full time horizon while the other players continue to adapt. The resulting objective value is then 

D

ψobj  

> i

, ¯ui,T (ψi)

E

.

Because the other players react to the realized interaction, this mapping need not be maximized at ψi = ψobj  

> i

. In other words, the weight vector that is best for evaluating outcomes need not be the weight vector that is optimal to deploy inside the learning dynamics. This setup leads to the following problem statements. 

Problem 1. Design an algorithm for player i that selects actions {ai,t }Tt=1 and adaptively chooses deployed weights 

{ψi,t }Tt=1 to maximize E

hD 

ψobj  

> i

, ¯ui,T 

Ei 

, where ψobj  

> i

is fixed. 

Problem 2. Provide a finite-time regret bound for the pro-posed algorithm. 

Problem 3. Empirically evaluate how adaptively chosen weights {ψi,t }Tt=1 lead to improvement in 

D

ψobj  

> i

, ¯ui,T 

E

rela-tive to a baseline that deploys ψi,t ≡ ψobj  

> i

for all t ∈ [T ].

III. A LGORITHMS 

This section solves Problem 1. We fix a focal player with index i ∈ N , and we introduce a bi-level learning algorithm for player i that deploys a scalarization ψi to shape its learning dynamics and update its mixed actions from bandit feedback. The following assumptions are first made. 

Assumption 1. Fix a focal player i ∈ N . Then: 

1) Bounded vector payoffs. The stage payoff map 

ui : Ai × A −i → Rdi is coordinate-wise bounded, i.e., there exists U > 0 such that 

∥ui(ai,t , a −i,t )∥∞ ≤ U

for all (ai,t , a −i,t ) ∈ A i × A −i.2) Polyhedral preference cone. The preference cone Ki

is polyhedral. 

3) Normalization of candidate weights. We restrict atten-tion to normalized candidate weights, that is, 

ψi ∈  K∗ 

> i

\ { 0} ∩ Sdi−1.

The following proposition shows that if the preference cone is polyhedral, then its dual cone admits a finite gen-erating set. 

Proposition 2 (Finite generating set) . Suppose Assump-tion 1.2 holds. Then there exists a finite set 

Ψi = {ψ1 

> i

, . . . , ψ mi } ⊆ K∗ 

> i

\ { 0}

such that K∗ 

> i

= cone(Ψ i).Proof. Since Ki is polyhedral, its dual cone K∗ 

> i

is also polyhedral. By the Minkowski–Weyl theorem [16], every polyhedral cone is finitely generated. Hence there exists a finite set Ψi = {ψ1 

> i

, . . . , ψ mi } ⊆ K∗ 

> i

\ { 0} such that 

K∗ 

> i

= cone(Ψ i).Assumption 1.2 and Proposition 2 imply that the algorithm we develop can be restricted to a finite candidate class of scalarizations obtained by normalizing the finite generating set of K∗ 

> i

.The following proposition shows that every scalarization induced by a dual-cone vector is order-preserving with respect to the cone-induced preference relation. 

Proposition 3 (Monotonicity of scalarizations under the cone order) . Consider the game G from (9) , and fix a player i ∈

N . Let Ki ⊆ Rdi be player i’s preference cone from (8) , and let K∗ 

> i

be its dual cone. If a, b ∈ Rdi satisfy a ≤Ki b, then for every ψi ∈ K∗ 

> i

we have ⟨ψi, a ⟩ ≤ ⟨ ψi, b ⟩. Equivalently, for every ψi ∈ K∗ 

> i

, the map z 7 → ⟨ ψi, z ⟩ is order-preserving with respect to ≤Ki .Proof. If a ≤Ki b, then b − a ∈ Ki. For any ψ ∈ K∗ 

> i

,by definition of the dual cone, ⟨ψ, b − a⟩ ≥ 0, i.e., 

⟨ψ, b ⟩ ≥ ⟨ψ, a ⟩.

Remark 2. We focus on a fixed focal player i ∈ N and, throughout the rest of this section, we drop the subscript i

from all quantities associated with that player. The remaining players are not modeled explicitly and may update their behavior arbitrarily, provided the resulting environment re-mains responsive rather than fully adversarial, as described in Remark 1. 

We next introduce the algorithm we use. 

A. Block Protocol 

We use a bi-level learning architecture. The outer layer operates on a slower timescale, and it selects a weight vector ψ ∈ Ψ to deploy in the inner layer, where 

Ψ = { ψ1, . . . , ψm} is the finite candidate set from Proposition 3. The inner layer operates on a faster timescale, and it selects actions using the currently deployed scalariza-tion. For each candidate ψj ∈ Ψ, the player maintains an associated mixed action distribution. Collecting these mixed action distributions gives a matrix of policies Q ∈ ∆( A)m ⊂

Rm×|A| . We use Q[j, ·] to denote the jth row of Q. For each j ∈ [m], the row Q[j, ·] ∈ ∆( A) is the policy used when deploying ψj . The outer layer maintains a distribution over candidate scalarizations, and this distribution is an element of ∆(Ψ) . The player changes its policies based on the scalar feedback it observes, and we use Q[j, ·]t to denote the player’s policy for ψj at round t.The outer layer operates in blocks of rounds of the game, in the sense that the player fixes a vector ψ and takes actions according to the fixed scalarized payoff via ψ for a fixed number of rounds. Fix a block length L ∈ N and let 

h := ⌈T /L ⌉ denote the number of blocks. For each block 

k ∈ [h], define 

s0(k) := ( k − 1) L + 1 , s1(k) := min {kL, T },

Ik := [ s0(k), s 1(k)] ∩ Z,

where s0(k) is the starting index of block k, s1(k) is the last index of block k, and Ik is the set of indices t of the rounds within block k. That is, the first block consists of rounds with indices t ∈ { s0(1) , s 0(1) + 1 , . . . , s 1(1) }, the second block consists of rounds with indices t ∈ { s0(2) , s 0(2) + 1, . . . , s 1(2) }, etc. At the start of block k, the player’s outer loop samples 

Jk ∼ pk, where pk ∈ ∆(Ψ) denotes the outer loop’s distribution over Ψ during block k. Then, its inner loop deploys ψJk for all t ∈ I k. The player samples actions 

at ∼ Q[Jk, ·]t, where Q[Jk, ·]t is the distribution at round t

over actions associated with the candidate scalarization with index Jk. It updates only the row Q[Jk, ·]t using the shaping reward 

r(Jk ) 

> t

:= ⟨ψJk , u t⟩, t ∈ I k.

This update is performed as follows. We form the IX-stabilized gradient estimate 1 bgt (see [18]–[21]) via 

bgt[a] := − I{at = a}

Q[Jk, ·]( at) + γq

r(Jk ) 

> t

, a ∈ A , (11) where I{·} denotes the indicator function, and γq > 0

is the implicit-exploration parameter used to stabilize the importance-weighted estimator. We then apply one online mirror descent (OMD) step [22] to the active policy row, and it takes the form 

Q[Jk, ·]t+1 ∈ arg min 

> q∈∆( A)

n

⟨q, bgt⟩ + 1 

> ηq

DRq

 q∥Q[Jk, ·]t

o

,

(12) where DRq (·∥· ) denotes the Bregman divergence associated with the regularizer Rq on the simplex ∆( A). All rows Q[j, ·]

for j̸ = Jk are left unchanged during time block k.The outer layer is evaluated using the objective weight-vector ψobj from (10) via the block-average reward 

robj  

> k

:= 1

|I k|

X

> t∈I k

⟨ψobj , u t⟩.

> 1Reviews of IX-stabilized gradient estimates and online mirror descent are provided in [17, Appendix A].

Using the IX estimator on ∆([ m]) with the preference vector index Jk and feedback robj  

> k

, we obtain bg(P ) 

> k

via 

bg(P ) 

> k

[j] := − I{Jk = j}

pk(j) + γp

robj  

> k

, j ∈ [m], (13) where γp > 0 is the implicit-exploration parameter used to stabilize the importance-weighted estimator. The quantity 

bg(P ) 

> k

is the estimated loss vector for the outer learner, and it converts the scalar feedback robj  

> k

from the sampled preference index into a full vector that is used in the OMD update and outer regret analysis. We then apply one OMD step to update 

pk using 

pk+1 ∈ arg min 

> p∈∆([ m])

n

⟨p, bg(P ) 

> k

⟩ + 1 

> ηp

DRp (p∥pk)

o

, (14) where DRp (·∥· ) denotes the Bregman divergence associated with the regularizer Rp on the simplex ∆(Ψ) .

Algorithm 1 OUTER ALG 

Require: Candidate scalarizations Ψ = {ψ1, . . . , ψ m}, time horizon T , block size L, objective weight vector ψobj ,step sizes ηp, η q > 0, implicit-exploration parameters 

γp, γ q ≥ 0, and regularizers Rp, R q 

> 1:

h ← ⌈ T /L ⌉; initialize p1 ∈ ∆([ m]) , Q ∈ ∆( A)m with no zero entries  

> 2:

for k = 1 , . . . , h do  

> 3:

Ik ← [( k − 1) L + 1 , min {kL, T }] ∩ Z 

> 4:

sample Jk ∼ pk 

> 5:

(Q, r obj  

> k

) ← INNER ALG (

Jk, ψ Jk , ψ obj , Ik, Q, η q , γ q , R q ) 

> 6:

Compute bg(P ) 

> k

with (13) using (pk, J k, r obj  

> k

, γ p) 

> 7:

Compute pk+1 using (14)  

> 8:

end for  

> 9:

return {pk}h+1  

> k=1

, Q

Algorithm 2 INNER ALG 

Require: Active index J ∈ [m], deployed scalarization 

ψJ , objective weight-vector ψobj , block index I ⊆ [T ],matrix Q ∈ ∆( A)m, step size ηq > 0, implicit-exploration parameter γq ≥ 0, and regularizer Rq 

> 1:

Sobj ← 0 

> 2:

for t ∈ I do  

> 3:

sample at ∼ Q[J, ·]; observe ut from (3)  

> 4:

Sobj ← Sobj + ⟨ψobj , u t⟩ 

> 5:

rt ← ⟨ ψJ , u t⟩ 

> 6:

Compute bgt by (11) using (Q[J, ·], a t, r t, γ q ) 

> 7:

Compute Q[J, ·]t+1 using (12)  

> 8:

end for  

> 9:

robj ← Sobj /|I|  

> 10:

return Q, robj 

Algorithms 1 and 2 implement the block protocol de-scribed above. Algorithm 1 updates the block-level distri-bution pk ∈ ∆([ m]) using the block feedback robj  

> k

, and calls Algorithm 2, which runs within the block and updates only the active row Q[Jk, ·] ∈ ∆( A) using shaping rewards induced by the deployed scalarization ψJk .IV. R EGRET ANALYSIS 

This section solves Problem 2 and derives finite-time regret bounds for the bi-level protocol in Algorithms 1 and 2. 

A. Inner-Layer Regret 

Fix a block k and its active index Jk ∈ [m]. Conditioned on choosing Jk, the inner algorithm faces a sequence of bandit linear losses over the simplex ∆( A) induced by the deployed scalarization ψJk . We compare the inner algorithm to the best fixed mixed action in hindsight, namely 

q⋆k ∈ arg min 

> q∈∆( A)

X

> t∈I k

q, bgt , (15) i.e., the single distribution that would have minimized the accumulated estimated loss if it had been played throughout the block. The following bound controls the inner algorithm’s performance relative to this hindsight benchmark. 

Theorem 1 (Inner block regret bound) . Suppose Assump-tion 1 holds, fix γq > 0, and suppose Algorithm 2 uses the negative entropy regularizer Rq (q) = P 

> a∈A

q(a) log q(a)

for the inner OMD update. Then, for the hindsight-optimal comparator q⋆k in (15) , and with Tk := |I k|, we have 

X

> t∈I k

Qt[Jk, ·] − q⋆k, bgt ≤√d U γq

p2Tk log |A| .

Proof. See [17, Appendix B]. Theorem 1 shows that, over each block, the inner al-gorithm incurs only sublinear regret relative to the best fixed mixed action chosen in hindsight for the deployed scalarization in that block. Thus, when the outer layer fixes a scalarization, the inner layer can learn to act nearly as well as the best fixed action rule for that scalarized objective. 

B. Outer-Layer Regret 

The outer update in Algorithm 1 runs over ∆([ m]) for h

rounds using the block-average feedback 

r⋆k := 1

|I k|

X

> t∈I k

⟨ψ⋆, u t⟩,

the IX estimator in (13) with parameters 

(qt, a t, r t, γ ) = (pk, J k, r ⋆k, γ p), and one OMD step in (14) with mirror map Rp. The following result bounds the regret of the outer algorithm, namely Algorithm 1, under these conditions. 

Theorem 2 (Outer block regret bound) . Suppose Assump-tion 1 holds, fix γp > 0, and suppose Algorithm 1 uses the negative entropy regularizer Rp(p) = Pmj=1 p(j) log p(j)

for the outer OMD update. Then, for any comparator p ∈

∆([ m]) ,

> h

X

> k=1

pk − p, bg(P ) 

> k

≤√d U γp

p2h log m. Proof. See [17, Appendix C]. Theorem 2 shows that the outer algorithm incurs only sublinear regret over blocks relative to any fixed distribution over candidate scalarizations chosen in hindsight. Thus, at the slower timescale, the outer layer can learn to select scalarizations nearly as well as the best fixed mixture of candidate weights for the objective feedback observed across blocks. 

C. Regret of the Bilevel Algorithm 

We define the regret of the bilevel algorithm by 

Rbi  

> T

:= 

> h

X

> k=1

pk − p⋆, bg(P ) 

> k

+

> h

X

> k=1

X

> t∈I k

Qt[Jk, ·] − q⋆k, bgt .

Here, p⋆ is the best fixed distribution over candidate scalar-izations chosen in hindsight, and q⋆k is the best fixed mixed action chosen in hindsight on block Ik for the scalarization deployed on that block. The following theorem establishes the regret bound for the full bilevel algorithm. 

Theorem 3 (Regret of the bilevel algorithm) . Suppose all conditions of Theorems 1 and 2 hold. Then the regret of the bilevel algorithm satisfies 

Rbi  

> T

≤ √d U 

√2h log mγp

+

p2hT log |A| 

γq

!

.

Proof. See [17, Appendix D]. Theorem 3 states that the total error of the bilevel proce-dure decomposes into two sublinear components: an outer regret term that measures how well the algorithm chooses among deployed scalarizations across blocks, and an inner regret term that measures how well the algorithm learns actions within each deployed block. The overall procedure performs nearly as well as a benchmark that, up to sublinear error, combines the best fixed outer weighting strategy with the best fixed within-block action choices under the deployed scalarizations. In this sense, the theorem formalizes the fact that the bilevel architecture can simultaneously learn which scalarizations are useful and learn how to act under them. V. S IMULATION RESULTS 

This section presents simulation results for a vector-valued extension of the classic Bach or Stravinsky game [23]. To demonstrate the performance of Algorithms 1 and 2 we compare with an exponential weights algorithm with implicit exploration (Exp-IX) from [19]. 

A. Problem Setup 

We consider a repeated two-action game with actions 

{B, S } for both players and a 4-dimensional vector payoff 

u(a, b ) ∈ R4 defined as 

B SB (1 , 1, 1, 0) (−1, 1, 1, −1) 

S (1 , −1, −1, 1) (0 , 1, 1, 1) 

At round t, the realized outcome is ut = u(at, b t). The focal player is evaluated using 

ψobj =

 √22 , √22 , 0, 0



,

while the opponent updates using the weight vector 

ϕobj =



0, 0, √22 , √22



.

Thus, the focal evaluation signal is ⟨ψobj , u t⟩, while the opponent learns from ⟨ϕobj , u t⟩.Under these fixed choices of ψobj and ϕobj , the induced scalar payoff matrix is 

B SB (√2, √22 ) (0 , 0) 

S (0 , 0) ( √22 , √2) 

which has two pure Nash equilibria, i.e., BB and SS . De-ployed weights can alter the induced incentives. For example, deploying 

ψ′ =

 √22 , 0, 0, − √22



inside the focal player’s updates, while evaluation remains along ψobj , yields the scalar game 

B SB ( √22 , √22 ) (0 , 0) 

S (0 , 0) (− √22 , √2) 

in which B is strictly dominant for the focal player and SS 

is no longer an equilibrium. 

B. Results and Comparisons to Baselines 

We evaluate our bi-level framework on the four dimen-sional game described above with 1, 000 independent runs that each consist of 10 , 000 rounds. We compare two sce-narios. In Scenario 1 (Exp-IX vs. Exp-IX) both players use standard exponential weights with implicit exploration (Exp-IX) [19], and in Scenario 2 (Bi-level vs Exp-IX) one player uses our bi-level architecture while the other uses Exp-IX. The bi-level player maintains a distribution over 

Ψ =

n

( √22 , √22 , 0, 0) , ( 12 , 12 , 12 , 12 ), ( 12 , 12 , − 12 , − 12 )

o

.

The choice of a weight vector ψ ∈ Ψ is performed every L =500 rounds using Algorithm 1 with a learning rate of ηp =0.1. The inner algorithm, Algorithm 2, uses ηq = 0 .1. For both algorithms, we use an implicit exploration parameter of γp = γq = 0 .2. In both scenarios, the baseline Exp-IX players use γ = 0 .2.

Figure 1 presents the distribution of equilibrium outcomes over the 1, 000 runs for both scenarios. Each run’s equi-librium is determined based on the majority action profile over the final 1, 000 steps. Because the learning dynamics are stochastic, the realized action profile in a single run does not necessarily settle on the same outcome for every round. We therefore classify each run by the equilibrium whose action profile appears most often over the final 1, 000 steps. In Scenario 1 (Exp-IX vs. Exp-IX), convergence to the BB B S None 

## 0

## 20 

## 40 

## 60 

## 80 

> % of runs  Scenario 1: Exp-IX vs Exp-IX
> Scenario 2: Bi-level vs Exp-IX

Fig. 1: Outcomes over 1000 runs ( T = 10 4). Scenario 1: Exp-IX vs. Exp-IX under fixed weights. Scenario 2: bi-level (Algorithms 1–2) vs. Exp-IX. We see that when both players use Exp-IX, the runs split nearly evenly between the 

BB and SS equilibria. When the focal player instead uses our bi-level method, the outcome distribution shifts strongly towards BB, showing that adaptive scalarization biases the learning dynamics toward the equilibrium preferred by the focal player. and SS equilibria happens with nearly the same frequency: 53% of runs end at BB , while 47% end at SS .In Scenario 2 (Bi-level vs Exp-IX) there is a strong asymmetry: 82% of runs converge to BB, 9% of runs converge to SS, and 9% converge to neither. This shift between the scenarios demonstrates that the bi-level player’s adaptive scalarization successfully biases the coupled learn-ing dynamics toward the BB equilibrium, which yields a higher payoff under its true objective weight vector 

ψobj = ( √2/2, √2/2, 0, 0) . The increase in non-convergent outcomes from 0% to 9% suggests that adaptive scalarization can sometimes delay convergence, though the overall benefit of steering toward the preferred equilibrium may outweigh this drawback. Figure 2 shows the mean scalarized reward trajectories for runs that converged to BB in the top plot, and it shows the same quantity for runs that converged to SS in the bottom plot, with the shaded regions corresponding to ±1 standard deviation. These trajectories are smoothed with a moving average over 300 rounds. The gold and blue lines depict 

⟨ψobj , u t⟩ and ⟨ϕobj , u t⟩, respectively. The overall trend is that the bi-level algorithm earns a higher reward on trajectories that end in BB and a lower reward on trajectories that end in 

SS. These trajectories demonstrate that adaptive scalarization enables the bi-level player to bias the coupled learning dynamics toward equilibria that are more favorable under its objective weight vector. These results collectively illustrate the core mechanism behind our approach: in a payoff-responsive environment, the interaction trajectory depends on how the focal player evaluates outcomes inside its learning updates. By adapt-ing the deployed scalarization, the focal player changes its own action updates, which in turn changes the opponent’s response and the induced equilibrium that emerges. As a result, the bi-level method can change its weights to steer play toward trajectories that yield higher payoff values under the fixed objective weight ψobj .

1

2

> Reward (Eq.  BB )
> Bi-level
> Exp-IX

0 0.2 0.4 0.6 0.8 1

·10 4

1

2

Round Reward (Eq.  SS )Fig. 2: Scalarized reward trajectories for Scenario 2 (Bi-level vs Exp-IX) by equilibrium type. The top plot shows BB 

equilibrium runs and the bottom plot shows SS equilibrium runs. The results were averaged over 1000 runs and the shaded regions correspond to ±1 standard deviation. We see that among runs that converge to BB, the bi-level player receives consistently higher reward than Exp-IX over the course of learning. Among the runs that converge to SS, the bi-level player attains lower reward, which is consistent with 

SS being less favorable under the focal player’s objective. VI. C ONCLUSION 

We studied repeated vector-valued games where evalu-ation uses a fixed objective weight, but the player may deploy alternative weights inside its updates to influence coupled adaptation. We proposed a bi-level algorithm with finite set of candidate weights and established finite-time regret bounds, along with experiments showing that adaptive deployment can steer outcomes toward a higher objective value than fixed-weight baselines. Future work will explore continuous weight selection and seek to reduce regret even further. REFERENCES [1] D. M. Roijers, P. Vamplew, S. Whiteson, and R. Dazeley, “A survey of multi-objective sequential decision-making,” J. Artif. Int. Res. , vol. 48, p. 67–113, Oct. 2013. [2] C. F. Hayes, R. R˘ adulescu, E. Bargiacchi, H. K¨ arnstr¨ om, M. Mac-farlane, J. Foerster, M. Dolan, and E. Howley, “A practical guide to multi-objective reinforcement learning and planning,” Springer Nature ,2021. [3] M. I. K. Khalil, I. U. Rahman, M. Zakarya, A. Zia, A. A. Khan, M. R. C. Qazani, M. Al-Bahri, and M. Haleem, “A multi-objective op-timisation approach with improved pareto-optimal solutions to enhance economic and environmental dispatch in power systems,” Scientific Reports , vol. 14, no. 1, p. 13418, 2024. [4] D. Blackwell, “An analog of the minimax theorem for vector payoffs,”   

> Pacific Journal of Mathematics , 1956. [5] V. Perchet, “Approachability, regret and calibration: Implications and equivalences,” Journal of Dynamics and Games , vol. 1, no. 2, pp. 181– 254, 2014.

[6] J. Abernethy, P. L. Bartlett, and E. Hazan, “Blackwell approachability and no-regret learning are equivalent,” in Proceedings of the 24th Annual Conference on Learning Theory (S. M. Kakade and U. von Luxburg, eds.), vol. 19 of Proceedings of Machine Learning Research ,(Budapest, Hungary), pp. 27–46, PMLR, 09–11 Jun 2011. [7] N. Shimkin, “An online convex optimization approach to blackwell’s approachability,” Journal of Machine Learning Research , vol. 17, no. 129, pp. 1–23, 2016. [8] V. Perchet and S. Mannor, “Approachability, fast and slow,” in 

Proceedings of the 26th Annual Conference on Learning Theory 

(S. Shalev-Shwartz and I. Steinwart, eds.), vol. 30 of Proceedings of Machine Learning Research , (Princeton, NJ, USA), pp. 474–488, PMLR, 12–14 Jun 2013. [9] D. Garber and M. Massalha, “Blackwell’s approachability with ap-proximation algorithms,” arXiv preprint arXiv:2502.03919 , 2025. [10] G. Farina, C. Kroer, and T. Sandholm, “Faster game solving via predictive blackwell approachability: Connecting regret matching and mirror descent,” in AAAI , 2021. [11] S. Mannor and J. N. Tsitsiklis, “Approachability in repeated games: Computational aspects and a stackelberg variant,” Games and Eco-nomic Behavior , vol. 66, pp. 315–325, May 2009. [12] M. Ehrgott, Multicriteria Optimization . Springer, 2005. [13] K. Miettinen, Nonlinear Multiobjective Optimization . Kluwer Aca-demic Publishers, 1999. [14] S. Boyd and L. Vandenberghe, Convex optimization . Cambridge university press, 2004. [15] D. K. . M. R. Giovanni P. Crespi, “Vector-valued games: charac-terization of equilibria in matrix games,” Mathematical Methods of Operations Research , 2025. [16] R. T. Rockafellar, Convex analysis , vol. 28. Princeton university press, 1997. [17] E. Asadollahi, C. Hawkins, and M. Hale, “Tech report: Online scalarization in vector-valued games,” tech. rep., Georgia Institute of Technology, 2026. Online at: https: //sites.gatech.edu/ece-corelab/files/2026/03/ Online_Scalarization_TechReport.pdf .[18] T. Koc´ ak, G. Neu, M. Valko, and R. Munos, “Efficient learning by implicit exploration in bandit problems with side observations,” in 

Advances in Neural Information Processing Systems (NeurIPS) 27 ,pp. 613–621, 2014. [19] G. Neu, “Explore no more: improved high-probability regret bounds for non-stochastic bandits,” in Proceedings of the 29th International Conference on Neural Information Processing Systems - Volume 2 ,NIPS’15, (Cambridge, MA, USA), p. 3168–3176, MIT Press, 2015. [20] T. Lattimore and C. Szepesv´ ari, Bandit Algorithms . Cambridge University Press, 2020. [21] S. Bubeck and N. Cesa-Bianchi, “Regret analysis of stochastic and nonstochastic multi-armed bandit problems,” Machine Learning ,vol. 5, no. 1, pp. 1–122, 2012. [22] S. Shalev-Shwartz, “Online learning and online convex optimization,” 

Found. Trends Mach. Learn. , vol. 4, p. 107–194, Feb. 2012. [23] M. J. Osborne and A. Rubinstein, A course in game theory . MIT press, 1994.