---
title: Online Scalarization in Vector-Valued Games
title_zh: 向量值博弈中的在线标量化
authors: "Ehsan Asadollahi, Calvin Hawkins, Matthew Hale"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06624v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 重复多玩家向量值博弈与标量化
tldr: 本研究探讨了多玩家向量值博弈中的在线标量化问题，旨在解决传统方法中权重固定且难以预设的局限。作者提出了一种双层学习框架，外层学习器在慢尺度上从候选类中选择标量化权重，内层强盗学习器则在快尺度上根据标量化反馈执行动作。通过基于稳定重要性权重的强盗在线镜像下降算法，该方法实现了亚线性遗憾界。实验表明，该方法能显著提升系统收敛至理想均衡点的概率，为动态环境下的多目标博弈提供了有效的决策支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06624v1/fig-001.webp\", \"caption\": \"Fig. 2: Scalarized reward trajectories for Scenario 2 (Bi-level vs Exp-IX) by equilibrium type. The top plot shows BB equilibrium runs and the bottom plot shows SS equilibrium runs. The results were averaged over 1000 runs and the shaded regions correspond to ±1 standard deviation. We see that among runs that converge to BB, the bi-level player receives consistently higher reward than Exp-IX over the course of learning. Among the runs that converge to SS, the bi-level player attains lower reward, which is consistent with SS being less favorable under the focal player’s objective.\", \"page\": 7, \"index\": 1, \"width\": 443, \"height\": 438}]"
motivation: 传统的向量值博弈通常假设标量化权重是固定的，但在实际应用中，权重往往需要作为在线决策变量进行动态调整以优化长期结果。
method: 提出一种双层学习框架，结合外层权重选择与内层强盗在线镜像下降算法，通过动态调整标量化信号来引导收益轨迹。
result: "理论上证明了该算法具有亚线性遗憾界，实验中将收敛至首选均衡点的成功率从50%提升至约80%。"
conclusion: 该研究证明了在线标量化在引导复杂博弈系统收敛至理想状态方面的有效性，为多目标决策问题提供了新的理论框架和算法工具。
---

## 摘要
我们研究了重复多玩家向量值博弈，其中玩家每轮观察一个收益向量，并通过这些向量的线性标量化来评估结果。与大多数先前的工作不同，标量化的选择被视为一个在线决策变量，而非固定的建模决策。我们提出了一个双层学习框架，其中外部学习器在较慢的时间尺度上从有限候选类中选择标量化方案，而较快的内部老虎机无悔学习器则利用所选标量化诱导的标量反馈来选择动作。该方法的性能是相对于某个真实的权重向量定义的，且部署的标量化方案充当了塑造诱导收益轨迹的控制信号。我们提供了基于带有稳定重要性权重的多臂老虎机在线镜像下降的可实现算法，并以次线性遗憾界的形式推导了有限时间性能保证。在一个典型博弈的向量值扩展上的实验表明，收敛到首选均衡的比例从非自适应标量化下的约 50% 提高到了我们所提方法下的约 80%。

## Abstract
We study repeated multi-player vector-valued games in which a player observes a payoff vector each round and evaluates outcomes through linear scalarizations of those vectors. Different from most prior works, the choice of scalarization is treated as an online decision variable rather than a fixed modeling decision. We propose a bi-level learning framework in which an outer learner chooses a scalarization from a finite candidate class on a slow timescale, while a faster inner bandit no-regret learner selects actions using the scalar feedback induced by the chosen scalarization. Performance of this approach is defined with respect to a certain true weight vector, and the deployed scalarizations act as control signals that shape the induced payoff trajectory. We provide implementable algorithms based on bandit online mirror descent with stabilized importance weighting, and we derive finite-time performance guarantees in the form of sublinear regret bounds. Experiments on a vector-valued extension of a canonical game show that convergence to the preferred equilibrium rises from roughly $50\%$ under non-adaptive scalarization to about $80\%$ under our proposed method.

---

## 论文详细总结（自动生成）

这是一份关于论文《Online Scalarization in Vector-Valued Games》（向量值博弈中的在线标量化）的结构化深入分析报告：

### 1. 论文的核心问题与整体含义
**研究动机与背景：**
在多智能体系统中，决策往往涉及多个不相容的目标（如安全、效率、公平等），通常通过线性标量化（将向量收益投影为标量）来处理。传统方法将标量化权重视为固定的建模决策。然而，在重复博弈中，对手的行为往往是响应式的（例如也是学习者）。
**核心问题：** 论文提出，标量化权重不应是静态的，而应被视为一个“在线决策变量”或“控制旋钮”。通过动态调整部署的权重，玩家可以塑造博弈的轨迹，诱导对手做出反应，从而引导系统收敛至对己方真实目标更有利的均衡点。

### 2. 论文提出的方法论
论文提出了一个**双层学习框架（Bi-level Learning Framework）**，旨在解决评估权重（真实目标）与部署权重（引导手段）的分离问题：

*   **核心思想：** 采用双尺度更新机制。外层学习器在慢尺度上选择标量化权重，内层学习器在快尺度上根据当前权重选择动作。
*   **关键技术细节：**
    *   **外层学习（Outer Learner）：** 在离散的候选权重集合 $\Psi$ 中维护一个概率分布。它根据“真实目标权重”评估每个时间块（Block）的平均收益，并使用带有隐式探索（IX）稳定重要性权重的在线镜像下降（OMD）来更新权重分布。
    *   **内层学习（Inner Learner）：** 在选定的标量化信号下，作为一个强盗（Bandit）无悔学习器运行。它仅观察实际执行动作的标量收益，并使用 OMD 更新动作策略。
    *   **算法流程：** 
        1. 将总时长 $T$ 分为若干块 $L$。
        2. 每块开始时，外层采样一个权重向量 $\psi$。
        3. 在该块内，内层根据 $\psi$ 诱导的标量收益执行动作并更新策略。
        4. 块结束时，外层根据真实目标 $\psi_{obj}$ 计算反馈并更新权重选择概率。
*   **理论保证：** 论文证明了该算法在内外两层均能实现**亚线性遗憾界（Sublinear Regret）**，确保了算法的收敛性和长期有效性。

### 3. 实验设计
*   **场景：** 扩展版的经典博弈“巴赫或斯特拉文斯基”（Bach or Stravinsky，又称性别大战）。作者将其收益设计为 4 维向量，模拟多目标环境。
*   **Benchmark（基准）：** 标准的指数权重隐式探索算法（Exp-IX），即玩家使用固定的真实目标权重进行学习。
*   **对比方法：**
    *   **场景 1：** 两个玩家均使用固定的 Exp-IX 算法。
    *   **场景 2：** 焦点玩家使用本文提出的双层自适应算法，对手使用固定的 Exp-IX。

### 4. 资源与算力
*   **算力说明：** 论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   **规模推测：** 实验涉及 1,000 次独立运行，每轮包含 10,000 步博弈。由于是基于离散动作空间的数值仿真，此类实验通常在标准 CPU 服务器或高性能工作站上即可完成，对算力需求相对较低，重点在于统计显著性。

### 5. 实验数量与充分性
*   **实验规模：** 进行了 1,000 次独立重复实验，以消除随机性影响。
*   **充分性评价：** 
    *   **统计严谨：** 通过 1,000 次运行的分布图展示了收敛至不同均衡点的概率，具有较强的统计说服力。
    *   **对比客观：** 明确展示了在相同环境下，自适应权重与固定权重对均衡选择的影响差异。
    *   **局限性：** 实验主要集中在一个特定的经典博弈扩展上，缺乏在更大规模动作空间或更复杂多玩家环境（如 5 玩家以上）中的泛化性验证。

### 6. 论文的主要结论与发现
*   **均衡引导能力：** 在固定权重的基准测试中，系统收敛至焦点玩家首选均衡点的概率仅为 53%。而使用双层自适应算法后，该概率显著提升至 **82%**。
*   **权重偏离的价值：** 实验证明，为了达到最优的长期目标，玩家在学习过程中“表现”出的偏好（部署权重）往往需要偏离其“真实”偏好。
*   **收敛权衡：** 虽然自适应标量化能引导至更好的均衡，但可能会略微增加不收敛（None）的情况（从 0% 升至 9%），这反映了策略动态调整带来的不稳定性代价。

### 7. 优点
*   **视角新颖：** 将标量化权重从“建模参数”转变为“控制变量”，为多目标博弈提供了全新的策略维度。
*   **理论完备：** 不仅提出了算法，还给出了严谨的有限时间遗憾界证明。
*   **实用性强：** 算法基于强盗反馈（Bandit Feedback），不需要知道对手的策略或完整的收益矩阵，符合现实中信息受限的场景。

### 8. 不足与局限
*   **候选集限制：** 目前外层学习器需要在预定义的有限权重候选类中进行选择，如何高效地在连续权重空间中进行在线搜索仍是挑战。
*   **环境假设：** 算法假设环境（对手）是响应式的而非完全对抗性的。如果对手采取极端的对抗策略，该方法的优势可能会减弱。
*   **超参数敏感性：** 双层学习涉及多个学习率（$\eta_p, \eta_q$）和探索参数（$\gamma_p, \gamma_q$），且块长度 $L$ 的选择对性能影响较大，论文未深入讨论超参数的自适应调节。

（完）
