---
title: Macro-Action Based Multi-Agent Instruction Following through Value Cancellation
title_zh: 基于宏动作并通过价值抵消实现的多智能体指令遵循
authors: "Wo Wei Lin, Ethan Rathbun, Enrico Marchesini Xiang Zhi Tan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12655v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 适应自然语言指令的多智能体强化学习
tldr: 本研究针对多智能体强化学习中外部指令中断宏动作导致价值估计不一致的问题，提出了MAVIC方法。该方法通过在指令边界修正贝尔曼回溯目标，消除了指令切换带来的价值偏差。实验证明，MAVIC在复杂协作环境中能有效兼顾指令遵循与基础任务性能，为动态指令下的多智能体协作提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12655v1/fig-001.webp\", \"caption\": \"Table 1: MacDec-POMDP definition where the subscript i denotes individual agent components.\", \"page\": 2, \"index\": 1, \"width\": 520, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2605.12655v1/fig-002.webp\", \"caption\": \"Figure 5: Action distribution frequency for successful delivery is shown by baseline no instruction while action distribution for unachievable instructions.\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 235}]"
motivation: 现有MARL在处理中断宏动作的外部指令时，由于贝尔曼更新耦合了不同指令背景下的价值估计，导致价值估计不一致。
method: 提出MAVIC方法，通过在指令边界修正贝尔曼回溯目标，抵消旧指令目标并恢复当前目标下的延续价值。
result: 在复杂的协作多智能体环境中，MAVIC在保持基础任务性能的同时，显著提高了指令遵循的合规性。
conclusion: MAVIC通过修正引导目标而非奖励塑造，实现了在随机指令切换下的统一策略一致性价值估计。
---

## 摘要
在实际应用场景中，多智能体强化学习（MARL）可能需要适应外部自然语言指令，这些指令会中断正在进行的行为并与长时程目标产生冲突。然而，将奖励以指令为条件会引入一种根本性的失效模式，因为贝尔曼更新（Bellman updates）会将不同指令上下文中的价值估计耦合在一起，导致当指令中断宏动作时出现价值不一致。我们提出了面向指令遵循的宏动作价值修正（MAVIC），它通过修正传入的指令目标并恢复当前目标下的延续价值，在指令边界处修正贝尔曼备份。与奖励塑造（reward shaping）不同，MAVIC 修改了自举目标（bootstrapping target）本身，从而在统一策略内实现了随机指令切换下的连贯价值估计。我们提供了理论分析和 Actor-Critic 实现，并证明了在日益复杂的协作式多智能体环境中，MAVIC 在保持基础任务性能的同时，实现了极高的指令遵循度。

## Abstract
Multi-agent reinforcement learning (MARL) in real-world use cases may need to adapt to external natural language instructions that interrupt ongoing behavior and conflict with long-horizon objectives. However, conditioning rewards on instructions introduces a fundamental failure mode as Bellman updates couple value estimates across instruction contexts, leading to inconsistent values when instructions interrupt macro-actions. We propose Macro-Action Value Correction for Instruction Compliance (MAVIC), which corrects Bellman backups at instruction boundaries by correcting the incoming instruction objective and restoring the continuation value under the current objective. Unlike reward shaping, MAVIC modifies the bootstrapping target itself, enabling consistent value estimation under stochastic instruction switching within a unified policy. We provide theoretical analysis and an actor-critic implementation, and show that MAVIC achieves high instruction compliance while preserving base task performance in increasingly complex cooperative multi-agent environments.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MAVIC**（Macro-Action Value Correction）的新框架，旨在解决多智能体强化学习（MARL）在执行长时程宏动作时，如何有效响应动态、随机到达的自然语言指令。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在现实世界中，多智能体系统（如协作机器人）需要根据人类的实时指令（如“别用那个砧板”或“先给我拿个西红柿”）调整行为。
*   **挑战**：这些指令往往会中断正在执行的“宏动作”（Macro-Actions，即一系列原子动作序列）。
*   **失效模式**：传统的强化学习通过贝尔曼更新（Bellman updates）估计价值。当指令改变时，如果直接将指令作为奖励条件，贝尔曼更新会跨越指令边界进行自举（Bootstrapping），导致不同指令背景下的价值估计发生“交叉污染”（Cross-contamination）。
*   **后果**：智能体可能会为了追求基础任务奖励而忽略指令，或者因为过度关注指令而导致基础任务性能大幅下降。

### 2. 方法论：MAVIC 核心思想与技术细节
MAVIC 的核心是通过**价值修正（Value Correction）**来解耦不同指令上下文下的价值估计。
*   **核心思想**：在指令发生切换的边界点，修改贝尔曼回溯的目标。它不是简单地改变奖励函数（Reward Shaping），而是直接修正自举目标，使得每个指令下的价值函数只反映该指令背景下的预期回报。
*   **关键技术细节**：
    *   **价值抵消公式**：当指令从 $c$ 切换到 $c'$ 时，修正后的奖励 $\bar{r}$ 会减去新指令在下一状态的估计价值 $V(s', c')$，并加上原指令在下一状态的延续价值 $V(s', c)$。
    *   **架构设计**：采用 Actor-Critic 结构。使用冻结的 **BERT** 语言编码器将自然语言指令转化为向量，并与智能体的观测历史拼接。
    *   **宏动作中断机制**：当新指令到达时，强制终止当前执行的宏动作，触发策略重新选择，并应用价值修正。
*   **理论保证**：论文证明了通过这种修正，可以将多指令优化问题分解为多个独立的指令条件子问题，从而在统一策略下实现价值一致性。

### 3. 实验设计
研究者在三个具有挑战性的协作多智能体环境中进行了评估：
*   **场景（Environments）**：
    1.  **Box Pushing (BP)**：双智能体协作推箱子，指令涉及避开特定目标或更改推送对象。
    2.  **Warehouse Tool Delivery (WTD)**：三智能体异构协作交付工具，指令涉及更改工具交付顺序。
    3.  **Overcooked (OC)**：三智能体（含一人类）协作烹饪，指令涉及资源约束（如“别用左边的砧板”）或特定动作请求。
*   **对比基准（Baselines）**：
    *   **Naive**：直接将指令奖励加入总奖励，不进行价值修正。
    *   **Switch**：在不同指令环境下独立训练（避免交叉污染，但难以处理动态切换）。
    *   **Vanilla Mac-IAC**：不带指令的标准宏动作 MARL（作为基础任务性能的上限）。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或具体的训练总时长。
*   文中提到使用了 5 个独立的随机种子进行实验，并使用了预训练且冻结的 BERT 模型作为语言编码器。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同的领域进行了测试，每个领域包含多种指令类型（约束型、请求型、重定向型）。
*   **充分性**：实验不仅测量了**基础任务性能**（Base Task Performance），还测量了**指令遵循度**（Compliance）。此外，还进行了**分布外（OOD）指令测试**，验证了模型对未见过的同义指令的泛化能力。
*   **客观性**：通过 95% 的置信区间展示了结果的稳定性，对比了多种基准方法，实验设计较为全面。

### 6. 主要结论与发现
*   **消除权衡**：MAVIC 是唯一能在保持高基础任务性能的同时，实现极高指令遵循度（如在 OC 环境中达到 99%）的方法。
*   **解决污染**：实验证实了 Naive 方法在指令中断时会出现严重的价值混淆，导致智能体在没有指令时也会表现出回避行为（如在 Overcooked 中因为害怕惩罚而永远不碰西红柿）。
*   **泛化能力**：得益于 BERT 编码器，MAVIC 能够理解并遵循与训练集语义相似但表述不同的新指令。

### 7. 优点
*   **理论严谨**：通过数学证明解决了 MARL 中长期存在的指令切换导致的价值不一致问题。
*   **高效性**：不需要在推理时运行昂贵的视觉语言模型（VLM）或大语言模型（LLM），计算开销低。
*   **灵活性**：能够处理异步到达的指令，并能无缝集成到现有的宏动作 Actor-Critic 算法中。

### 8. 不足与局限
*   **价值估计依赖**：MAVIC 的效果高度依赖于 Critic 对价值函数的准确估计。在部分可观测性极强或奖励极度稀疏的环境下，修正项可能会引入方差。
*   **语言理解深度**：虽然使用了 BERT，但系统本质上仍是将语言映射到控制行为，对于逻辑极其复杂的指令（如涉及多步推理的指令）可能仍有局限。
*   **探索问题**：论文未深入探讨如何在高维指令空间中进行有效探索，特别是对于出现频率极低的罕见指令。

（完）
