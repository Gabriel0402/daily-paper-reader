---
title: Vision-Based Runtime Monitoring under Varying Specifications using Semantic Latent Representations
title_zh: 基于语义潜表示的变规范视觉运行时监控
authors: "Bardh Hoxha, Oliver Schön, Hideki Okamoto, Lars Lindemann, Georgios Fainekos"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13923v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 研究基于视觉观测的信号时序逻辑 (STL) 运行时监控。
tldr: 本研究针对视觉观测下的过去时间信号时序逻辑（ptSTL）运行时监控，提出了一种具有可重用性的认证框架。通过引入“语义基”概念，即原子鲁棒性得分向量，实现了无需针对特定公式重新训练即可验证目标片段内的任何公式。结合共形预测技术提供有限样本保证，并在行人过马路和Waymo自动驾驶数据集上验证了该方法在长短时域下的有效性与可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13923v1/fig-001.webp\", \"caption\": \"Fig. 4. (a) Crossroad scenario: robot (blue) navigates toward the goal (green) via a CBF controller while three pedestrians converge. Cones show predicate sectors (45◦ half-angle); dashed circle: 𝑑safe=1.0 m. (b–d) 64×64 observations under image nuisances (fog, compression, noise).\", \"page\": 6, \"index\": 1, \"width\": 746, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2605.13923v1/fig-002.webp\", \"caption\": \"Fig. 1. Left: A single trained encoder enc𝜃 maps vision inputs to a latent interface B𝑡 from which formula-specific decoders evaluate any formula 𝜑 ∈ F in a target fragment F. Right: Two choices of interface basis.\", \"page\": 2, \"index\": 2, \"width\": 938, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2605.13923v1/fig-003.webp\", \"caption\": \"Fig. 5. Rolling and semantic monitors on a WOMD scenario (88 steps, 8.8 s). (a) Bird’s-eye view of the ego vehicle (blue) and surrounding agents (red); inset: 30 m monitoring viewport. (b–c) Rolling monitor for ⊡[0,4] 𝑝front and ⊡[0,4] 𝑝clear. (d–e) Semantic monitor for the same specifications. Ground truth: black; prediction: dashed; conformal band: blue; white = Safe, gray = Uncertain. Animated version: video.\", \"page\": 7, \"index\": 3, \"width\": 871, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2605.13923v1/fig-004.webp\", \"caption\": \"TABLE I Results under Level-2 (random-time) and Level-1 (episodewise) calibration (𝛼=0.10). CSR: certified safe rate. Prec: precision (fraction of safe certifications that are correct). FPR: false-positive rate. GT: ground-truth safe rate (“—” when GT=100%).\", \"page\": 8, \"index\": 4, \"width\": 1064, \"height\": 632}, {\"url\": \"assets/figures/arxiv/2605.13923v1/fig-005.webp\", \"caption\": \"Fig. 3. Monitor architectures for both benchmarks. Both share a CNN+attention encoder and temporal fusion module. (a) The rolling monitor predicts 𝑚 current-timestep predicates, accumulates them in a streaming buffer, and applies the conformal radius 𝑞𝜑 before temporal composition (interval STL). (b) The semantic-basis monitor predicts 2𝑚 | I | temporal atoms directly; 𝑞𝜑 is applied after composition via the formula’s parse tree. As a concrete instance, the safety specification 𝜑safe = ⊡[0,𝐾 ]𝜇clear has decoder dec𝜑safe (BA 𝑡 ) = 𝜌(⊡[0,𝐾 ]𝜇clear, 𝑥, 𝑡)— a single coordinate of BA 𝑡 . The reach-avoid specification\", \"page\": 5, \"index\": 5, \"width\": 412, \"height\": 533}]"
motivation: 旨在解决视觉监控中由于部分可观测性导致的安全性评估难题，并实现无需针对每个逻辑公式重复训练的通用监控器。
method: 提出语义基监控器和滚动预测监控器，利用共形校准技术为时序逻辑公式的鲁棒性提供统计意义上的安全保证。
result: 实验表明滚动监控器在短时域表现更优，而语义基监控器在长时域的认证边界比前者紧凑达4倍，且均满足覆盖率保证。
conclusion: 该方法成功实现了基于视觉的、可重用的运行时监控，为自动驾驶等复杂系统的安全性验证提供了理论支撑和实践方案。
---

## 摘要
我们研究了在部分可观测条件下，基于视觉观测的过去时间信号时序逻辑（ptSTL）的可认证运行时监控。监控器必须从图像中推断出与安全相关的量并提供有限样本保证，同时具备“可重用性”：一旦经过训练和校准，它应能认证目标片段中的任何公式，而无需针对每个公式进行重新训练。对于由有限时序原子字典诱导的片段，我们证明了“语义基”（即原子鲁棒性得分向量）是单调、1-Lipschitz 可重用接口类别中的最小预测目标：任何公式都由派生自解析树的确定性解码器进行评估，且单次符合性校准即可认证整个片段，无需并集界。我们还引入了一种“滚动预测监控器”，它仅预测当前的谓词值并在线重建时序历史；这更容易学习，但在长时界下会变得保守。在行人过马路基准测试中，滚动监控器在短时界下实现了更紧致的认证界限，而语义基监控器在长时界下的紧致度最高可达前者的4倍。我们在真实的 Waymo 驾驶数据上验证了所提出的监控器，实验结果表明两种监控器均满足符合性覆盖保证。

## Abstract
We study certified runtime monitoring of past-time signal temporal logic (ptSTL) from visual observations under partial observability. The monitor must infer safety-relevant quantities from images and provide finite-sample guarantees, while being \emph{reusable}: once trained and calibrated, it should certify any formula in a target fragment without per-formula retraining. For fragments induced by a finite dictionary of temporal atoms, we prove that the \emph{semantic basis}, the vector of atom robustness scores, is the minimum prediction target within the class of monotone, 1-Lipschitz reusable interfaces: any formula is evaluated by a deterministic decoder derived from the parse tree, and a single conformal calibration pass certifies the entire fragment with no union bound. We also introduce a \emph{rolling prediction monitor} that predicts only current predicate values and reconstructs temporal history online; this is easier to learn but grows conservative at long horizons. On a pedestrian-crossroad benchmark, rolling achieves tighter certified bounds at short horizons while the semantic-basis monitor is up to 4-times tighter at long horizons. We validate the presented monitors on real-world Waymo driving data, where both monitors satisfy the conformal coverage guarantee empirically.

---

## 论文详细总结（自动生成）

这是一份关于论文《Vision-Based Runtime Monitoring under Varying Specifications using Semantic Latent Representations》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**自动驾驶等复杂系统在视觉观测下的安全性监控**。核心挑战在于：
*   **部分可观测性**：监控器只能看到像素（图像），而安全规范（如距离、速度）定义在隐藏的物理状态上。
*   **规范的可变性（可重用性）**：在实际部署中，安全要求（STL公式）经常变化。传统的深度学习监控器通常针对特定公式训练，一旦规范改变就需要重新训练和校准，这在实际应用中效率极低。
*   **认证保证**：如何为基于学习的视觉预测提供具有统计意义的、有限样本的安全性认证（即保证预测的鲁棒性下界在 1-α 的概率内是真实的）。

### 2. 方法论：核心思想与关键技术
论文提出了一种基于**语义潜表示（Semantic Latent Representations）**的框架，主要包含以下关键点：

*   **语义基（Semantic Basis）**：
    *   定义了一个“原子字典”（Atomic Dictionary），包含基础的时序算子（如“过去 $K$ 步内一直安全”）。
    *   **核心思想**：不直接预测复杂公式的鲁棒性，而是预测这些“原子”的鲁棒性得分向量。
    *   **理论贡献**：证明了语义基是支持整个逻辑片段（Fragment）的**最小充分统计量**。只要预测了语义基，任何由这些原子组合而成的复杂公式都可以通过一个确定的、无需学习的解码器（由 min/max 构成的解析树）计算出来。
*   **两种监控架构**：
    *   **语义基监控器（Semantic-Basis Monitor）**：直接预测时序聚合后的原子值。优点是校准在时序组合之后，长时域（Long Horizon）下认证边界更紧致。
    *   **滚动预测监控器（Rolling Prediction Monitor）**：每步只预测当前谓词值，在线累积历史。优点是学习任务简单，短时域表现好，但长时域由于误差累积会变得保守。
*   **组合共形认证（Compositional Conformal Certification）**：
    *   利用 STL 鲁棒性语义的单调性和 1-Lipschitz 特性，通过**共形预测（CP）**技术，只需一次校准即可为整个逻辑片段提供统一的概率保证，且**不需要并集界（Union Bound）**校准，从而避免了随着公式复杂度增加而导致的保守性爆炸。

### 3. 实验设计
论文在两个主要基准上进行了验证：
*   **行人过马路场景（Crossroad Scenario）**：合成数据集。机器人通过控制屏障函数（CBF）导航，同时避开多个行人。监控器处理 64x64 的俯视图像，面临雾气、压缩和噪声等干扰。
*   **Waymo 开放运动数据集（WOMD）**：真实世界自动驾驶数据。包含 50,000 个训练场景，提取了 7 个安全关键谓词（如前向间距、车道保持等）。
*   **对比方法（Benchmark）**：
    *   **Observer Baseline**：基于传统区间 STL 语义和 Bonferroni 校正的基准。
    *   **Rolling vs. Semantic**：对比本文提出的两种不同接口架构。
    *   **不同校准级别**：Level-1（每段轨迹级别保证）和 Level-2（随机时间点级别保证）。

### 4. 资源与算力
*   **模型架构**：使用了 CNN + Attention 编码器处理图像序列，潜空间维度为 128。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或具体的训练时长。但从任务规模（Waymo 数据集 5 万场景，图像分辨率 64x64）推断，该实验在常规的单卡或多卡工作站（如 RTX 3090/4090 级别）即可完成。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了从合成数据到大规模真实驾驶数据的跨越。Waymo 数据集使用了近 5 万个场景进行训练，校准和测试集也达到了千量级，样本量充足。
*   **消融与对比**：
    *   对比了不同时域长度（$K=1$ 到 $K=16$）对认证紧致度的影响。
    *   测试了复合公式（Conjunction/Disjunction）的性能。
    *   验证了在不同图像噪声下的鲁棒性。
*   **客观性**：实验结果客观地展示了两种提出方法的“权衡”（Trade-off），即滚动监控器在短时域占优，而语义基监控器在长时域占优，没有刻意回避局限性。

### 6. 主要结论与发现
*   **紧致度优势**：语义基监控器在长时域（$K=16$）下的认证半径比滚动监控器紧致达 **4 倍**。
*   **认证有效性**：所有监控器在测试集上均严格满足了 $1-\alpha$（如 90%）的经验覆盖率要求，证明了理论的正确性。
*   **可重用性验证**：一旦语义基预测器训练完成，可以即时评估各种复杂的安全规范（如“先避障再达标”），无需重新训练。
*   **超越基准**：两种方法在所有测试公式上均显著优于带有 Bonferroni 校正的传统观察者基准。

### 7. 优点（亮点）
*   **理论严密**：不仅提出了方法，还证明了语义基的最小性，为视觉监控接口的设计提供了理论依据。
*   **高效校准**：通过利用 STL 的数学特性，规避了高维预测中常见的并集界惩罚，使得认证边界非常实用。
*   **架构灵活**：框架不依赖于特定的编码器（Encoder-agnostic），可以轻松更换为 ViT 或其他先进视觉模型。

### 8. 不足与局限
*   **分布偏移（Distribution Shift）**：虽然提到了鲁棒共形预测，但在极端环境变化（如完全未见的地理区域）下的泛化能力仍依赖于训练集的覆盖范围。
*   **逻辑片段限制**：目前主要针对 $\wedge/\vee$ 闭包的 ptSTL 片段，对于包含更复杂时序嵌套或未来算子的逻辑，语义基的维度可能会迅速增加。
*   **计算开销**：虽然推理很快，但语义基监控器需要预测多个时序原子的聚合值，这比简单的谓词预测增加了输出层的维度。

（完）
