---
title: "Failing Forward: Adaptive Failure-Informed Learning for Vision-Language-Action Models"
title_zh: 败中求进：面向视觉-语言-动作模型的自适应失败告知学习
authors: "Meng Zheng, Samhita Marri, Anwesa Choudhuri, Benjamin Planche, Zhongpai Gao, Van Nguyen Nguyen, Terrence Chen, Girish Chowdhary, Ziyan Wu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08434v2"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 视觉-语言-动作模型的自适应失败告知学习
tldr: 针对视觉-语言-动作（VLA）模型在仅依赖成功轨迹进行行为克隆时表现出的脆弱性，本文提出了自适应失败告知学习（AFIL）框架。该框架利用预训练模型在线生成失败轨迹作为负面引导，通过共享骨干网络的双动作生成器（DAGs）同时学习成功与失败行为。在推理阶段，利用失败生成器自适应地引导动作远离失败区域，显著提升了机器人在复杂任务中的鲁棒性和成功率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08434v2/fig-001.webp\", \"caption\": \"Figure 3: Visualization of in-domain (left) and out-of-domain (right) manipulation task setup. Row 1: exterior camera image, row 2: wrist camera image. See supplementary for more visualizations.\", \"page\": 8, \"index\": 1, \"width\": 738, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-002.webp\", \"caption\": \"Figure 2: Dual Action Generator (DAG)-VLA with Adaptive Failure-Informed Learning (AFIL).\", \"page\": 5, \"index\": 2, \"width\": 824, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-003.webp\", \"caption\": \"Table 4: Performance comparison on out-of-domain manipulation tasks. Success rates (%) reported over 25 configs × 3 runs for each task.\", \"page\": 9, \"index\": 3, \"width\": 764, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-004.webp\", \"caption\": \"Figure 1: Overview of the proposed Adaptive Failure-Informed Learning (AFIL) pipeline. AFIL uses online-generated failure trajectories as adaptive negative guidance, steering action generation away from failure-prone regions and toward more reliable success modes.\", \"page\": 2, \"index\": 4, \"width\": 782, \"height\": 293}]"
motivation: 现有的VLA模型仅学习成功案例，导致在执行过程中微小误差会累积成无法恢复的失败。
method: 提出AFIL框架，通过在线生成失败轨迹并训练双动作生成器，在扩散采样过程中利用失败分布进行自适应负向引导。
result: 在多种领域内外的机器人操纵任务中，AFIL在任务成功率和鲁棒性方面均优于现有的VLA基准模型。
conclusion: AFIL证明了利用失败经验作为负面反馈能有效增强VLA模型的泛化能力和纠错能力。
---

## 摘要
视觉-语言-动作（VLA）模型为可扩展的机器人操控提供了一种极具前景的范式，但其对仅包含成功案例的行为克隆的依赖使其显得脆弱；由于缺乏纠错训练信号，微小的执行误差会迅速累积成不可恢复的分布外失败。为了解决这一局限性，我们提出了自适应失败告知学习（AFIL），这是一个端到端框架，利用失败轨迹作为基于扩散和流的 VLA 策略的自适应负向引导。AFIL 使用预训练的 VLA 在线生成失败展开，避免了对手工设计失败模式或人机回环恢复的需求。随后，它在共享通用视觉-语言主干网络的同时，联合训练针对成功和失败行为的双动作生成器（DAGs），从而以有限的参数开销实现高效的失败感知策略学习。在采样过程中，失败生成器会自适应地引导动作生成远离易出错区域，并转向更可靠的成功模式，引导强度由每个扩散步骤中成功与失败分布之间的距离决定。在涵盖短时程和长时程设置的域内及跨域机器人操控任务上的实验表明，AFIL 相比现有的 VLA 基准模型一致地提高了任务成功率和鲁棒性，证明了其有效性、高效性和通用性。

## Abstract
Vision-language-action (VLA) models provide a promising paradigm for scalable robotic manipulation, yet their reliance on success-only behavioral cloning leaves them brittle; lacking corrective training signals, minor execution errors rapidly compound into unrecoverable, out-of-distribution failures. To address this limitation, we propose Adaptive Failure-Informed Learning (AFIL), an end-to-end framework that leverages failure trajectories as adaptive negative guidance for diffusion- and flow-based VLA policies. AFIL uses a pretrained VLA to generate failure rollouts online, avoiding the need for handcrafted failure-mode design or human-in-the-loop recovery. It then jointly trains Dual Action Generators (DAGs) for successful and failed behaviors while sharing a common vision-language backbone, enabling efficient failure-aware policy learning with limited parameter overhead. During sampling, the failure generator adaptively steers action generation away from failure-prone regions and toward more reliable success modes, with guidance strength determined by the per-diffusion-step distance between success and failure distributions. Experiments across in-domain and out-of-domain robotic manipulation tasks, covering both short- and long-horizon settings, show that AFIL consistently improves task success rates and robustness over existing VLA baselines, demonstrating its effectiveness, efficiency, and generality.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AFIL (Adaptive Failure-Informed Learning)** 的新框架，旨在通过利用“失败经验”来增强机器人视觉-语言-动作（VLA）模型的鲁棒性。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：当前的 VLA 模型（如 OpenVLA, $\pi_0$）主要依赖于“成功案例”的行为克隆（Behavioral Cloning）。
*   **核心问题**：这种“仅学习成功”的模式导致模型非常脆弱。在实际部署中，微小的执行误差会不断累积，由于模型从未见过失败状态或错误动作，它无法进行自我纠正，最终导致任务失败。
*   **动机**：借鉴图像生成领域中的“负面提示（Negative Prompting）”概念，研究者认为失败的轨迹同样包含重要信息——它们定义了动作空间中应当“避开”的区域。

### 2. 方法论：自适应失败告知学习 (AFIL)
*   **核心思想**：通过在线生成的失败数据来训练一个专门的“失败动作生成器”，并在推理采样阶段，利用该生成器作为负面引导，将机器人的动作推向成功分布，远离失败分布。
*   **关键技术细节**：
    *   **双动作生成器 (DAG)**：在共享同一个预训练视觉-语言模型（VLM）骨干网络的基础上，并行构建两个轻量级的动作头：一个学习成功轨迹（$G_{succ}$），一个学习失败轨迹（$G_{fail}$）。
    *   **在线失败数据生成**：不依赖人工标注，而是让预训练模型直接运行（Rollout），自动收集失败案例，并利用运动规划器对部分失败进行修正，形成对比数据。
    *   **自适应负向引导采样**：在扩散模型（Diffusion）或流匹配（Flow-matching）的采样过程中，最终的动作分值由成功分值减去失败分值决定。
    *   **自适应权重公式**：引入余弦相似度来动态调整引导强度 $\hat{\lambda}_\eta$。当成功和失败的预测方向高度一致时（如任务初期），减小引导；当两者分歧增大（出现失败迹象）时，增强引导。

### 3. 实验设计
*   **实验场景**：
    *   **ManiSkill 模拟环境**：涵盖短时程（堆叠方块、提拉木栓）和长时程（摆放餐具、水果分类）任务。
    *   **LIBERO 基准测试**：包括 LIBERO-Spatial, Object, Goal 和 Long 四个子集，用于验证知识迁移和长时程任务能力。
*   **对比方法**：
    *   基础 VLA 模型（如 $\pi_{0.5}$）。
    *   仅使用成功数据训练的模型。
    *   使用成功+失败修正数据训练的模型。
    *   静态失败告知学习（Static-FIL，使用固定引导权重）。
*   **Benchmark**：以任务成功率（Success Rate）作为核心评价指标。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **参数开销**：文中提到由于共享了 VLM 骨干网络，仅增加了一个轻量级的动作生成头，因此参数开销是“有限的（Limited parameter overhead）”。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ManiSkill 中，每个任务评估了 50 个配置 $\times$ 3 次运行 = 150 次 Rollouts。
    *   在 LIBERO 中，遵循标准协议，每个子集包含 10 个任务，总计进行了大量的重复实验。
    *   进行了**消融实验**，验证了引导尺度 $\alpha$ 的影响以及自适应权重的必要性。
*   **充分性与客观性**：实验覆盖了域内（In-domain）和分布外（OOD）场景，且对比了多个主流 VLA 基准，实验设计较为全面且符合学术规范。

### 6. 主要结论与发现
*   **性能提升**：AFIL 在所有任务中均显著优于仅学习成功的基准模型。在长时程任务中，提升尤为明显（例如在 ManiSkill 某长时程任务中比静态引导高出 12%）。
*   **鲁棒性增强**：在面对未见过的物体、颜色或杂乱环境（OOD 实验）时，AFIL 表现出更强的泛化能力。
*   **自适应的必要性**：实验证明，动态调整引导强度比固定权重更有效，因为它能避免在任务初期（成功与失败路径重合时）产生错误的排斥力。

### 7. 优点与亮点
*   **无需人工干预**：通过在线 Rollout 自动获取失败数据，解决了失败模式难以手工设计的难题。
*   **模型无关性**：该方法可以无缝集成到任何基于扩散或流匹配的 VLA 架构中。
*   **高效性**：通过共享骨干网络，在不显著增加推理延迟的情况下提升了性能。
*   **数学优雅性**：利用余弦相似度捕捉“语义意图”的偏离，为动作引导提供了合理的数学解释。

### 8. 不足与局限
*   **算力细节缺失**：缺乏具体的训练资源消耗报告，难以评估中小规模实验室的复现成本。
*   **失败定义的依赖**：虽然不需要手工设计失败模式，但仍需要一个自动化的“任务失败检测器”来触发数据收集，在极其复杂的开放场景下，准确定义失败可能仍具挑战。
*   **实时性挑战**：虽然增加了少量参数，但在每个扩散步骤都要调用两次动作头，对于实时性要求极高的机器人控制场景，可能会带来一定的计算压力。

（完）
