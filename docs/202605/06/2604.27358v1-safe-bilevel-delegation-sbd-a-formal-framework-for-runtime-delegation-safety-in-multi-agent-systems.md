---
title: "Safe Bilevel Delegation (SBD): A Formal Framework for Runtime Delegation Safety in Multi-Agent Systems"
title_zh: 安全双层授权 (SBD)：多智能体系统中运行时授权安全的形式化框架
authors: Yuan Sun
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27358v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: LLM多智能体系统运行时委派安全的正式框架
tldr: 针对大语言模型智能体在关键任务中的授权安全问题，本文提出安全双层授权（SBD）框架。该框架利用双层优化动态平衡安全与效率，引入连续授权度实现人工干预到自主执行的平滑过渡，并给出了安全性单调性、收敛性及责任传播边界的理论证明，为多智能体系统提供了运行时安全保障。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的多智能体架构缺乏在执行过程中根据任务上下文动态调整安全与效率权衡的运行时机制。
method: 采用双层优化模型，外层学习上下文相关的安全权重，内层在概率安全约束下优化授权策略。
result: 理论证明了安全单调性、内层策略的线性收敛性以及多跳授权链中的责任传播边界。
conclusion: SBD框架为高风险环境下多智能体系统的动态授权提供了严谨的数学基础和安全保障机制。
---

## 摘要
随着大语言模型 (LLM) 智能体被部署在关键任务环境中，如何安全地将子任务授权给专业子智能体变得至关重要。现有工作主要在设计阶段解决多智能体架构选择问题，或提供广泛的经验性指导，但两者都未能提供一种在执行过程中随任务上下文变化动态调整安全与效率权衡的运行时机制。我们提出了安全双层授权 (SBD)，这是一个针对分层多智能体系统中运行时授权安全的形式化框架。SBD 将任务授权建模为一个双层优化问题：外层元权重网络 phi 学习上下文相关的安全-效率权重 lambda(s)；内层循环在满足概率安全约束 P(safe) >= 1-delta 的前提下优化授权策略 pi。连续授权度 alpha 控制转移给每个子智能体的决策权限，在完全人工干预 (alpha=0) 与完全自主执行 (alpha=1) 之间进行平滑插值。我们确立了三个理论结果：(1) 安全单调性——较高的外层安全权重会产生（弱）更安全的内层策略；(2) 内层策略收敛性——在标准平滑性假设下，内层问题的投影梯度下降法呈线性收敛；(3) 责任传播界限——在多跳授权链中分配责任，并具有可证明的单智能体上限。我们在三个高风险领域（医疗 AI (MIMIC-III)、金融风险控制 (S&P 500) 和教育智能体监管 (ASSISTments)）中实例化了 SBD，并指定了数据集、安全约束集、基准模型和评估协议。本手稿完整展示了形式化框架和理论结果；按照文中所述协议进行的实证验证正在计划中，并将在后续修订版本中报告。

## Abstract
As large language model (LLM) agents are deployed in high-stakes environments, the question of how safely to delegate subtasks to specialized sub-agents becomes critical. Existing work addresses multi-agent architecture selection at design time or provides broad empirical guidelines, but neither provides a runtime mechanism that dynamically adjusts the safety-efficiency trade-off as task context changes during execution.   We propose Safe Bilevel Delegation (SBD), a formal framework for runtime delegation safety in hierarchical multi-agent systems. SBD formulates task delegation as a bilevel optimization problem: an outer meta-weight network phi learns context-dependent safety-efficiency weights lambda(s) in [0,1]; an inner loop optimizes the delegation policy pi subject to a probabilistic safety constraint P(safe) >= 1-delta. The continuous delegation degree alpha in [0, 1] controls how much decision authority is transferred to each sub-agent, interpolating smoothly between full human override (alpha=0) and fully autonomous execution (alpha=1).   We establish three theoretical results: (1) Safety Monotonicity--higher outer safety weight produces a weakly safer inner policy; (2) Inner Policy Convergence--projected gradient descent on the inner problem converges linearly under standard smoothness assumptions; (3) an Accountability Propagation bound that distributes responsibility across multi-hop delegation chains with a provable per-agent ceiling. We instantiate SBD in three high-stakes domains--medical AI (MIMIC-III), financial risk control (S and P 500), and educational agent supervision (ASSISTments)--specifying datasets, safety constraint sets, baselines, and evaluation protocols. This manuscript presents the formal framework and theoretical results in full; empirical validation following the protocols described herein is planned and will be reported in a forthcoming revision.

---

## 论文详细总结（自动生成）

这是一份关于论文《Safe Bilevel Delegation (SBD): A Formal Framework for Runtime Delegation Safety in Multi-Agent Systems》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体在医疗、金融等高风险领域的广泛应用，如何安全地将子任务授权给专门的子智能体成为核心挑战。
*   **研究动机**：现有的多智能体安全机制大多在“设计时”固定（如预设权限边界或静态架构），无法根据执行过程中的实时风险（运行时上下文）动态调整。
*   **核心问题**：主智能体应该向子智能体转移多少决策权？这种转移如何随风险环境的变化而动态演变？
*   **整体含义**：论文提出了 **SBD（安全双层授权）** 框架，旨在建立一套数学严谨的运行时机制，在保证任务效率的同时，通过动态调整“授权度”来确保系统安全性。

### 2. 方法论：核心思想与技术细节
SBD 将授权问题形式化为一个**双层优化（Bilevel Optimization）**问题：
*   **核心思想**：引入一个连续的授权度 $\alpha \in [0, 1]$。$\alpha=0$ 代表完全由人工或主智能体干预，$\alpha=1$ 代表完全自主执行。系统通过学习一个上下文相关的权重 $\lambda$ 来平衡安全与效率。
*   **外层优化（元权重学习）**：学习一个元权重网络 $\phi(s)$，它根据当前状态 $s$ 输出权重 $\lambda$。在风险高的状态下，$\lambda$ 增大，迫使系统优先考虑安全性。
*   **内层优化（策略学习）**：在给定 $\lambda$ 的情况下，优化授权策略 $\pi$，同时满足概率安全约束 $P(safe) \ge 1 - \delta$（即不安全决策的概率低于阈值 $\delta$）。
*   **理论保障**：
    1.  **安全单调性**：证明了外层安全权重越高，内层策略越安全。
    2.  **线性收敛性**：证明了在标准假设下，内层策略优化可以快速收敛。
    3.  **责任传播边界**：推导了多级授权链中责任分配的数学上限，确保责任可追溯且不会过度集中。

### 3. 实验设计
论文设计了三个高风险领域的评估协议（注：该版本为预注册计划，实验结果将在修订版中发布）：
*   **数据集/场景**：
    1.  **医疗 AI (MIMIC-III)**：基于 ICU 入院数据，决定是否授权子智能体进行临床分诊。
    2.  **金融风险控制 (S&P 500)**：在市场波动（如 COVID 期间）时，管理投资组合再平衡的授权。
    3.  **教育监管 (ASSISTments)**：针对“风险学生”自动调整教学内容的授权程度。
*   **对比方法（Baselines）**：
    *   **Fixed-$\alpha$**：固定授权度。
    *   **Safe-RL (CPO)**：传统的受限策略优化。
    *   **MARL-vanilla**：无安全机制的多智能体强化学习。
    *   **MaAS**：静态架构搜索方法。
    *   **SBD-NoOuter**：消融实验，移除外层元学习循环。
*   **评估指标**：安全率 (SR)、任务效率 (TE)、Pareto 曲线下面积 (SEA) 以及责任熵 (AE)。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA A100 (80GB)** GPU。
*   **训练参数**：外层迭代 500 次，内层迭代 50 次，Batch Size 为 256。
*   **时长**：在 A100 上，单次外层迭代约 0.8 秒，完成一个领域的完整训练耗时**少于 7 分钟**。这表明该框架具有极高的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：计划在 3 个完全不同的领域进行实验，每个领域包含 5 个基准对比和 4 组消融实验。
*   **充分性与客观性**：论文采用了“**预注册（Pre-registered）**”模式，即在得出结果前先公开实验协议、假设和伪造标准（Falsifiability criteria）。这种做法在学术上非常严谨，能有效防止“为了凑结果而改实验”，保证了研究的客观性和公平性。
*   **局限性**：由于当前版本（V1）尚未包含实际运行结果数据，其经验有效性仍有待验证。

### 6. 主要结论与发现
*   **动态权衡是关键**：授权不应是二进制的（是/否），而应是随上下文波动的连续值。
*   **理论闭环**：通过数学证明，确保了当外层网络识别到高风险时，系统会自动收紧授权（降低 $\alpha$），从而在理论上保证了运行时的安全性。
*   **责任可追溯**：提出的责任传播公式解决了多智能体系统中“责任稀释”的问题，为 AI 治理提供了形式化工具。

### 7. 优点
*   **运行时动态性**：弥补了设计时静态安全机制的不足，能处理未见过的风险上下文。
*   **理论完备**：不仅提出了算法，还给出了关于单调性、收敛性和责任边界的严格证明。
*   **计算高效**：利用隐式微分技术，使得双层优化的计算开销保持在可接受范围内。
*   **方法通用**：可以叠加在任何现有的多智能体架构之上。

### 8. 不足与局限
*   **假设依赖**：理论证明依赖于内层损失函数的“强凸性”，这在深度神经网络中并不总是成立。
*   **安全集预设**：SBD 假设安全约束集 $C$ 是已知的（由专家定义），但在复杂现实中，定义什么是“绝对安全”本身就很困难。
*   **单层安全限制**：目前只关注“授权决策”的安全性，如果子智能体在执行过程中发生内部错误，SBD 尚无法直接干预其内部逻辑。
*   **实验缺失**：当前版本仅有理论和协议，缺乏实际数据的支撑。

（完）
