---
title: A Survey of Multi-Agent Deep Reinforcement Learning with Graph Neural Network-Based Communication
title_zh: 基于图神经网络通信的多智能体深度强化学习综述
authors: "Valentin Cuzin-Rambaud, Laetitia Matignon, Maxime Morge"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25972v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 具有通信的多智能体深度强化学习综述
tldr: 本文综述了基于图神经网络（GNN）通信的多智能体深度强化学习（MARL）研究。在MARL中，智能体通过共享信息来协调行动，而GNN能有效处理交互图中的信息交换。针对该领域缺乏统一分类框架的问题，本文系统梳理了近期工作，并提出了一个通用的基于GNN的通信流程框架，旨在使相关方法的核心概念更易于理解和应用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25972v1/fig-001.webp\", \"caption\": \"Figure 1: An example of MPNN: the green node, aggregates features from its 1-hop neighbors ∈ N (1)\", \"page\": 3, \"index\": 1, \"width\": 493, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.25972v1/fig-002.webp\", \"caption\": \"Table 1: Taxonomy of GNN-based communication methods, organized in two categories: proxy-based (upper part) and distributed-based (lower part) according to the generic Algorithm 2. E(·) encodes the local representation, N t r (·) defines reachable agents, Gbuild(·) builds a graph, GNN is the architecture used, and Inner integration shows how to leverage the final representation.\", \"page\": 7, \"index\": 2, \"width\": 1037, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2604.25972v1/fig-003.webp\", \"caption\": \"Table 2: Tested communication constraints during execution\", \"page\": 9, \"index\": 3, \"width\": 469, \"height\": 335}]"
motivation: 旨在解决多智能体强化学习领域中，基于图神经网络的通信方法缺乏明确分类结构和统一框架的问题。
method: 通过对近期相关文献进行系统性综述，提出了一个通用的基于GNN的通信处理流程。
result: 成功构建了一个能够区分和分类不同GNN通信MARL方法的框架，使底层概念更加清晰。
conclusion: 该综述为研究人员提供了清晰的指导，有助于更好地理解和开发基于图神经网络的多智能体协作算法。
---

## 摘要
在多智能体强化学习（MARL）中，通信机制的集成使智能体能够通过共享信息更好地学习协调动作并收敛至目标。基于交互图，一类子方法利用图神经网络（GNN）来学习通信，使智能体能够通过融合交换的信息来改进其内部表示。随着研究的日益增多，我们注意到目前缺乏明确的结构和框架来区分和分类基于 GNN 通信的 MARL 方法。因此，本文对该领域的近期研究工作进行了综述。我们提出了一个通用的基于 GNN 的通信过程，旨在使这些方法背后的底层概念更加清晰且易于理解。

## Abstract
In multi-agent reinforcement learning (MARL), the integration of a communication mechanism, allowing agents to better learn to coordinate their actions and converge on their objectives by sharing information. Based on an interaction graph, a subclass of methods employs graph neural networks (GNNs) to learn the communication, enabling agents to improve their internal representations by enriching them with information exchanged. With growing research, we note a lack of explicit structure and framework to distinguish and classify MARL approaches with communication based on GNNs. Thus, this paper surveys recent works in this field. We propose a generalized GNN-based communication process with the goal of making the underlying concepts behind the methods more obvious and accessible.

---

## 论文详细总结（自动生成）

这是一份关于论文《基于图神经网络通信的多智能体深度强化学习综述》（A Survey of Multi-Agent Deep Reinforcement Learning with Graph Neural Network-Based Communication）的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在多智能体强化学习（MARL）中，智能体面临**部分可观测性**和**非平稳性**挑战。通信是解决这些问题的关键，而图神经网络（GNN）因其处理非欧几里得结构化数据和模拟智能体间动态交互的能力，成为学习通信机制的热门工具。
*   **核心问题**：尽管基于 GNN 的 MARL 通信方法层出不穷，但该领域缺乏统一的分类框架和结构化描述，导致不同方法间的差异和联系难以辨析。
*   **整体含义**：本文旨在通过提出一个通用的 GNN 通信流程模型，对现有主流方法进行系统性梳理、分类和对比，并探讨现实通信约束对这些算法的影响。

### 2. 论文提出的方法论
论文提出了一个**通用的基于 GNN 的通信处理流程**，将其抽象为四个关键阶段：
1.  **消息编码（Encode）**：使用 MLP、RNN 或 CNN 将局部观测 $o_i$ 映射为初始消息向量 $x_i$。
2.  **通信决策（Decide）**：确定与谁通信。分为**代理模式（Proxy）**（所有智能体发给中央节点）和**分布式模式（Distributed）**（仅与通信范围内的邻居通信）。
3.  **消息组合（Combine）**：
    *   **构图**：根据接收到的消息构建局部或全局图。
    *   **GNN 聚合**：利用消息传递神经网络（MPNN）机制（如 GCN、GAT、GraphSAGE）进行 $L$ 层聚合。
    *   **多轮通信**：在分布式模式下，智能体可交换中间层的隐藏表示，实现信息的远距离传播（Multi-hop）。
4.  **内部集成（Inner Integration）**：将 GNN 输出的通信感知表示集成到策略网络（Policy）或价值网络（Critic）中，指导动作选择或评估。

### 3. 实验设计与 Benchmark
作为一篇综述论文，其“实验”部分主要体现为对 12 种代表性算法的横向对比分析：
*   **对比方法**：包括 DICG, GA-Comm, MAGIC, GACG, LTS-CG（代理模式）以及 DGN, LSC, MAGE-X, MAGEC, GPPO, HetNet（分布式模式）。
*   **测试场景（Benchmark）**：文中提到的算法通常在 **Predator-Prey（捕食者-猎物）**、**StarCraft II (SMAC)**、**多机器人导航**、**巡逻任务**等经典 MARL 环境中进行验证。
*   **对比维度**：编码器类型、通信范围定义、构图方式、GNN 架构、集成层级以及对通信约束（带宽、噪声、丢包）的处理。

### 4. 资源与算力
*   **算力说明**：由于本文是综述性质，**并未提供具体的训练算力数据**（如 GPU 型号、训练时长等）。这些细节通常存在于被引用的原始论文中。
*   **算法复杂度**：文中指出代理模式（Proxy）在处理大规模智能体时会面临计算瓶颈，而分布式模式更具扩展性。

### 5. 实验数量与充分性
*   **覆盖范围**：论文深入分析了 12 种主要的 GNN 通信方法，涵盖了从 2020 年到 2026 年（预测发布时间）的前沿工作。
*   **充分性评价**：综述对每种方法的分类非常细致（见 Table 1），并专门讨论了现实约束（Table 2），这种多维度的分类对比是充分且客观的。
*   **公平性**：作者通过统一的算法框架（Algorithm 2 & 3）来描述所有方法，消除了原论文描述风格不一带来的偏差，确保了对比的公平性。

### 6. 主要结论与发现
*   **GNN 的优越性**：GNN 能有效处理动态变化的智能体数量和拓扑结构，通过多层聚合实现信息的隐式全局传播。
*   **模式差异**：代理模式有利于全局协调但依赖完美通信；分布式模式更符合现实约束但学习难度更高。
*   **现实鸿沟**：大多数现有研究忽略了带宽限制（LB）、消息噪声（NM）和通信丢失（CL），这限制了算法从仿真环境向现实世界的迁移。
*   **趋势**：结合时间序列信息（RNN）与空间结构信息（GNN）是提升部分可观测环境下性能的关键。

### 7. 优点与亮点
*   **通用框架**：提出的通用算法流程（Generic Process）极具参考价值，为后续研究者设计新算法提供了“标准模板”。
*   **约束分析**：不仅关注算法性能，还专门开辟章节讨论通信约束，体现了极强的工程实践导向。
*   **分类清晰**：将复杂的通信挑战拆解为四个标准步骤，使读者能迅速理解不同论文的创新点所在。

### 8. 不足与局限
*   **缺乏定量对比**：作为综述，未能在统一的 Benchmark 上给出各算法的性能排名（由于各原论文实验设置不一，这确实很难实现）。
*   **应用限制**：主要聚焦于协作式任务（Cooperative），对竞争或混合动机场景下的 GNN 通信讨论较少。
*   **理论深度**：对 GNN 聚合过程中的“过度平滑（Over-smoothing）”等图学习固有问题在 MARL 中的影响讨论不足。

（完）
