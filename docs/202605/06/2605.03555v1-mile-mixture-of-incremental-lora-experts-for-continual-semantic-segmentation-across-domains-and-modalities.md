---
title: "MILE: Mixture of Incremental LoRA Experts for Continual Semantic Segmentation across Domains and Modalities"
title_zh: MILE：用于跨领域和跨模态持续语义分割的增量 LoRA 专家混合模型
authors: "Shishir Muralidhara, Didier Stricker, René Schuster"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03555v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 使用LoRA专家的跨模态持续语义分割
tldr: 针对持续语义分割中跨领域和模态的遗忘问题，本文提出MILE框架。该方法利用LoRA为每个新任务实例化轻量级专家，同时冻结基础网络以防止知识覆盖。通过原型引导的门控机制动态选择专家，MILE在保持高性能的同时显著降低了参数开销，在稳定性、塑性和可扩展性上表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03555v1/fig-001.webp\", \"caption\": \"Table 5: Step-wise results (mIoU) for the task sequence RGB → Gray → IR representing modality shifts.\", \"page\": 11, \"index\": 1, \"width\": 725, \"height\": 192}, {\"url\": \"assets/figures/arxiv/2605.03555v1/fig-002.webp\", \"caption\": \"Fig. 1: Overview of the training process in MILE. Using the input images Xt, the domain prototype Pt is computed using a frozen pretrained network. LoRA expert Dt is instantiated and trained while keeping the base network frozen.\", \"page\": 3, \"index\": 2, \"width\": 721, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2605.03555v1/fig-003.webp\", \"caption\": \"Fig. 5: Qualitative comparison of segmentation results for domain-incremental learning of geographical domains using Cityscapes [2], BDD [30], and IDD [27].\", \"page\": 13, \"index\": 3, \"width\": 706, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2605.03555v1/fig-004.webp\", \"caption\": \"Table 1: Results (mIoU) on the sequence CSday → ACDCfog → ACDCrain → ACDCsnow → ACDCnight after learning all tasks.\", \"page\": 8, \"index\": 4, \"width\": 725, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2605.03555v1/fig-005.webp\", \"caption\": \"Table 3: Step-wise results (mIoU) for the task sequence RGB → IR → Gray representing modality shifts.\", \"page\": 9, \"index\": 5, \"width\": 725, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2605.03555v1/fig-006.webp\", \"caption\": \"Table 2: Step-wise results (mIoU) for the task sequence CS → BDD → IDD representing different geographical locations.\", \"page\": 9, \"index\": 6, \"width\": 725, \"height\": 233}]"
motivation: 现有的基于专家的持续学习方法在处理跨领域和模态任务时，面临扩展性差或参数隔离导致的学习受限问题。
method: 利用LoRA技术为每个任务构建轻量级专家模块，并采用原型引导的门控机制在推理阶段动态路由至最合适的专家。
result: 在多个跨领域和跨模态基准测试中，MILE以极低的参数增量实现了领先的分割精度，并有效解决了灾难性遗忘。
conclusion: MILE证明了结合参数高效微调与混合专家模型是实现高效、可扩展的持续语义分割的有效途径。
---

## 摘要
持续语义分割要求模型能够适应新的领域或模态，同时不牺牲在先前学习任务上的性能。基于专家的学习（其中特定任务模块专注于不同领域）已被证明在缓解遗忘方面非常有效。这些方法包括存在可扩展性问题的动态扩展，以及限制了学习新任务能力的参数隔离。我们提出了增量 LoRA 专家混合模型（MILE），这是一个用于跨领域和跨模态持续分割的模块化且参数高效的框架。MILE 利用低秩自适应（LoRA）为每个新任务实例化轻量级专家，同时保持预训练的基础网络冻结。每个专家仅在其任务数据上进行训练，从而避免覆盖先前学习的信息。原型引导的门控机制在推理时动态选择最合适的专家。MILE 在克服可扩展性限制的同时，实现了基于专家学习的优势。它在每个任务上仅需少量的参数增加，且需要数十个 LoRA 适配器才能达到单个完整模型的大小，使其在训练和存储方面都非常高效。在领域增量和模态增量基准测试中，MILE 在确保更好的稳定性、塑性和可扩展性的同时，取得了强劲的性能。

## Abstract
Continual semantic segmentation requires models to adapt to new domains or modalities without sacrificing performance on previously learned tasks. Expert-based learning, in which task-specific modules specialize in different domains, has proven effective in mitigating forgetting. These methods include dynamic expansion, which suffers from scalability issues, or parameter isolation, which constrains the ability to learn new tasks. We introduce Mixture of Incremental LoRA Experts (MILE), a modular and parameter-efficient framework for continual segmentation across both domains and modalities. MILE leverages Low-Rank Adaptation (LoRA) to instantiate lightweight experts for each new task while keeping the pretrained base network frozen. Each expert is trained exclusively on its task data, thus avoids overwriting previously learned information. A prototype-guided gating mechanism dynamically selects the most appropriate expert at inference. MILE achieves the benefits of expert-based learning while overcoming its scalability limitations. It requires only a marginal parameter increase per task and tens of LoRA adapters are needed before matching the size of a single full model, making it highly efficient in both training and storage. Across domain- and modality-incremental benchmarks, MILE achieves strong performance while ensuring better stability, plasticity, and scalability.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MILE (Mixture of Incremental LoRA Experts)** 的框架，旨在解决自动驾驶场景下语义分割模型在面对新领域（Domain）和新模态（Modality）时的持续学习问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在实际应用（如自动驾驶）中，语义分割模型经常面临**领域偏移**（如天气变化、地理位置迁移）和**模态变化**（如增加热成像或深度传感器）。传统的全参数微调会导致**灾难性遗忘**，而现有的专家模型方法要么因模型体积随任务线性增长而缺乏**扩展性**，要么因参数隔离限制了模型学习新任务的**塑性**。
*   **整体含义**：论文试图在“稳定性-塑性”困境中寻找平衡，提出一种既能保留旧知识、又能高效吸收新知识，且存储开销极小的模块化持续学习方案。

### 2. 方法论
*   **核心思想**：结合**低秩自适应 (LoRA)** 与**混合专家模型 (MoE)** 的思想。为每个新任务实例化一个轻量级的 LoRA 专家模块，同时保持预训练的基础网络（Base Network）完全冻结。
*   **关键技术细节**：
    *   **增量 LoRA 专家**：对于每个新任务 $t$，仅训练一组 LoRA 参数（矩阵 $A$ 和 $B$），秩 $r$ 设得很小（如 16）。基础网络权重 $W$ 保持不变，推理时通过 $W + BA$ 恢复特定任务的权重。
    *   **原型引导门控机制 (Prototype-based Gating)**：为了在推理时无需手动输入任务 ID（Task-ID），MILE 使用冻结的特征提取器计算每个任务训练集的平均特征，作为**领域原型 (Domain Prototype)**。
    *   **推理流程**：输入图像先经过冻结网络提取特征，门控网络计算该特征与所有存储原型的**余弦相似度**，自动路由至最匹配的 LoRA 专家进行分割预测。
    *   **异构标签处理**：由于每个专家是独立训练的，MILE 自然支持不同领域间类别不一致（Heterogeneous Labels）的情况。

### 3. 实验设计
*   **数据集与场景**：
    *   **恶劣天气序列**：Cityscapes (晴天) $\rightarrow$ ACDC (雾、雨、雪、夜)，共 5 个任务。
    *   **地理位置序列**：Cityscapes (德国) $\rightarrow$ BDD100K (美国) $\rightarrow$ IDD (印度)，涉及显著的视觉和类别分布差异。
    *   **模态增量序列**：Freiburg Thermal 数据集，包含 RGB $\rightarrow$ IR (红外) $\rightarrow$ Gray (灰度) 的转换。
*   **Benchmark 与对比方法**：
    *   对比了 **Single-Task (ST)**（独立训练，无遗忘上限）、**Fine-Tuning (FT)**（严重遗忘下限）、**Joint Training (JT)**（多任务联合训练）。
    *   对比了经典持续学习方法：**EWC**（正则化）、**ILT**（知识蒸馏）。
    *   对比了先进的专家模型：**PSS**、**MDIL**、**DRMN**。

### 4. 资源与算力
*   **模型架构**：使用 SegFormer-B5 作为骨干网络。
*   **参数规模**：LoRA 专家仅占总参数量的约 **5%**。论文指出，需要增加几十个 LoRA 专家，总参数量才会达到一个完整模型的大小。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了 Batch Size 为 12，初始学习率为 0.01，LoRA 微调学习率为 0.05。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了三大类增量场景，每类场景都有多步任务序列。
*   **消融与分析**：
    *   做了**任务顺序影响**实验（如 RGB/IR/Gray 不同排列）。
    *   提供了**门控混淆矩阵**，验证了原型路由的准确性。
    *   进行了**参数增长曲线分析**，对比了不同方法的可扩展性。
    *   包含**定性可视化对比**。
*   **充分性评价**：实验设计较为全面，覆盖了持续学习的核心评价指标（稳定性、塑性、扩展性），对比基准选择合理，结果具有说服力。

### 6. 主要结论与发现
*   **性能优异**：MILE 在所有基准测试中均接近 Single-Task 的性能，显著优于 EWC 和 ILT 等传统方法。
*   **解决遗忘**：由于基础网络冻结且专家参数隔离，MILE 理论上实现了**零遗忘**（针对已识别出的领域）。
*   **正向迁移**：在 ACDC 等相似领域任务中，MILE 表现出一定的正向迁移能力，性能甚至超过了独立训练的单任务模型。
*   **高效扩展**：相比于每增加一个任务就增加一个完整模型的 PSS 方法，MILE 的存储效率提升了约 20 倍。

### 7. 优点
*   **极高的参数效率**：利用 LoRA 极大地降低了持续学习的存储和计算开销。
*   **无需 Task-ID**：原型门控机制解决了推理时不知道输入属于哪个领域的难题，提高了实用性。
*   **模块化与灵活性**：可以轻松添加或删除特定领域的专家，且支持异构标签空间。
*   **架构无关性**：这种 LoRA 专家混合的思想可以推广到其他 Transformer 架构的视觉任务中。

### 8. 不足与局限
*   **原型依赖性**：原型门控依赖于预训练模型提取的特征。如果新领域与预训练领域（如 ImageNet 或 Cityscapes）差异极端巨大，特征提取器可能无法提供足够的判别力导致路由错误。
*   **硬路由限制**：目前采用的是“硬路由”（选择一个专家），而非多个专家的加权融合，这可能限制了模型处理“混合领域”图像的能力。
*   **算力细节缺失**：缺乏具体的训练时间对比，难以直观评估其在边缘设备上的实时训练潜力。

（完）
