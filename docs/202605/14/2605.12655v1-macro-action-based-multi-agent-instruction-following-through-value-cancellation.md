---
title: Macro-Action Based Multi-Agent Instruction Following through Value Cancellation
title_zh: 基于宏动作的多智能体指令遵循：通过价值抵消实现
authors: "Wo Wei Lin, Ethan Rathbun, Enrico Marchesini Xiang Zhi Tan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12655v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 结合自然语言指令和宏动作的多智能体强化学习
tldr: 本研究针对多智能体强化学习中自然语言指令中断宏动作导致的价值估计不一致问题，提出了MAVIC方法。该方法通过在指令边界修正贝尔曼回溯目标，实现了在随机指令切换下的统一策略学习。实验证明，MAVIC在保持基础任务性能的同时，显著提升了指令遵循的合规性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12655v1/fig-001.webp\", \"caption\": \"Table 1: MacDec-POMDP definition where the subscript i denotes individual agent components.\", \"page\": 2, \"index\": 1, \"width\": 520, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2605.12655v1/fig-002.webp\", \"caption\": \"Figure 5: Action distribution frequency for successful delivery is shown by baseline no instruction while action distribution for unachievable instructions.\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 235}]"
motivation: 外部指令中断正在进行的宏动作时，传统的贝尔曼更新会耦合不同指令背景的价值估计，导致学习不稳定。
method: 提出MAVIC算法，在指令切换边界通过修正引导目标来抵消旧指令影响并恢复当前目标的延续价值。
result: 在复杂的多智能体协作任务中，该方法在保证基础任务效率的前提下，实现了极高的指令遵循成功率。
conclusion: MAVIC为处理具有长时程目标和随机指令干扰的MARL问题提供了一种理论完备且有效的价值修正方案。
---

## 摘要
现实场景中的多智能体强化学习（MARL）可能需要适应外部自然语言指令，这些指令会中断正在进行的行为并与长远目标发生冲突。然而，将奖励以指令为条件会引入一种根本性的失效模式，因为贝尔曼更新会将不同指令上下文中的价值估计耦合在一起，导致当指令中断宏动作时出现价值不一致。我们提出了用于指令合规的宏动作价值修正（MAVIC），它通过修正传入的指令目标并恢复当前目标下的延续价值，在指令边界处修正贝尔曼备份。与奖励塑造不同，MAVIC 修改了自举目标本身，从而在统一策略内实现了随机指令切换下的连贯价值估计。我们提供了理论分析和一种 Actor-Critic 实现，并证明了在日益复杂的协作式多智能体环境中，MAVIC 在保持基础任务性能的同时，实现了极高的指令遵循度。

## Abstract
Multi-agent reinforcement learning (MARL) in real-world use cases may need to adapt to external natural language instructions that interrupt ongoing behavior and conflict with long-horizon objectives. However, conditioning rewards on instructions introduces a fundamental failure mode as Bellman updates couple value estimates across instruction contexts, leading to inconsistent values when instructions interrupt macro-actions. We propose Macro-Action Value Correction for Instruction Compliance (MAVIC), which corrects Bellman backups at instruction boundaries by correcting the incoming instruction objective and restoring the continuation value under the current objective. Unlike reward shaping, MAVIC modifies the bootstrapping target itself, enabling consistent value estimation under stochastic instruction switching within a unified policy. We provide theoretical analysis and an actor-critic implementation, and show that MAVIC achieves high instruction compliance while preserving base task performance in increasingly complex cooperative multi-agent environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Macro-Action Based Multi-Agent Instruction Following through Value Cancellation》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：在现实世界中，多智能体系统（MAS）需要执行长时程任务（如烹饪、仓库协作），并随时准备响应人类的自然语言指令。这些指令往往会**中断**正在进行的宏动作（Macro-Actions），并可能与原始任务目标发生冲突。
*   **核心问题**：传统的强化学习通过贝尔曼更新（Bellman Update）来估计价值。当指令改变奖励函数并中断宏动作时，不同指令背景下的价值估计会发生“**交叉污染（Cross-contamination）**”。即智能体在状态 A 的价值会受到未来可能收到的指令 B 的影响，导致学习不一致，表现为智能体要么忽略指令，要么在没有指令时表现极差。

### 2. 方法论（MAVIC）
*   **核心思想**：提出**宏动作价值修正（MAVIC）**机制。其核心是在指令切换的边界处，通过修改贝尔曼回溯（Bootstrapping）的目标，抵消新指令带来的价值偏差，并补偿当前指令下的延续价值。
*   **关键技术细节**：
    *   **价值解耦**：通过数学证明（引理1和定理1），MAVIC 证明了修改后的更新规则可以将多指令问题分解为多个独立的指令条件优化问题。
    *   **动态奖励修正**：当指令从 $c$ 切换到 $c'$ 时，修正后的奖励 $R'$ 定义为：
        $R' = R_c - \gamma V(s', c') + \gamma V(s', c)$
        其中，$- \gamma V(s', c')$ 减去了新指令背景下的预期回报，$+ \gamma V(s', c)$ 恢复了原指令下的延续价值。
    *   **架构设计**：采用 Actor-Critic 架构。使用冻结的 **BERT** 模型作为语言编码器处理自然语言指令，将其嵌入向量与环境观测拼接。
    *   **中断处理**：当新指令到达时，强制终止当前所有智能体的宏动作，进行价值修正更新，并根据新指令重新选择宏动作。

### 3. 实验设计
*   **实验场景**：
    1.  **Box Pushing (BP)**：双智能体协作推箱子，指令包括“去推小箱子”或“不要推箱子”。
    2.  **Warehouse Tool Delivery (WTD)**：三智能体异构协作（移动机器人与机械臂），指令涉及更改工具交付顺序。
    3.  **Overcooked (OC)**：三智能体协作烹饪，包含复杂的约束指令（如“不要使用左侧切菜板”）。
*   **对比基准（Baselines）**：
    *   **Naive**：直接将指令奖励加入环境，不进行价值修正。
    *   **Switch**：在不同指令环境下独立训练（避免交叉污染，但难以处理动态切换）。
    *   **Vanilla Mac-IAC**：无指令的原始算法（作为性能上限）。
*   **评估指标**：基础任务性能（Base Task Performance）和指令合规率（Instruction Compliance）。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练总时长。
*   文中提到使用了冻结的 BERT 模型进行文本编码，并基于已有的 Mac-IAC 开源代码库进行开发。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同的复杂多智能体环境中进行了测试。
*   **重复性与可靠性**：每个实验均使用 **5 个独立随机种子**进行训练，并给出了 95% 的置信区间。
*   **消融与扩展**：除了主实验外，还进行了**分布外（OOD）指令测试**，验证了模型对语义相近但未见过的指令的泛化能力，以及对不可行指令（如“飞起来”）的反应。
*   **充分性评价**：实验设计较为全面，涵盖了协作、竞争约束和动态响应等多个维度，对比基准的选择能够有效证明价值修正的必要性。

### 6. 主要结论与发现
*   **消除权衡**：MAVIC 是唯一能在保持高基础任务性能的同时，实现极高指令合规率（如 OC 环境下 99%）的方法。
*   **解决污染**：实验证实了 Naive 方法在面对负面约束指令（如“不要做某事”）时，会导致智能体在平时也由于价值耦合而不敢执行该动作，而 MAVIC 成功解决了这一问题。
*   **泛化性**：通过预训练语言模型，MAVIC 能够理解不同表述但语义相同的指令，并对 OOD 指令表现出一定的鲁棒性。

### 7. 优点与亮点
*   **理论完备**：不仅提出了算法，还提供了严格的数学证明，确保了价值解耦的正确性。
*   **即插即用**：该价值修正机制可以集成到任何基于宏动作的 Actor-Critic MARL 算法中。
*   **处理中断**：针对宏动作这种长时程行为，给出了优雅的异步指令中断与价值补偿方案。

### 8. 不足与局限
*   **价值估计依赖**：MAVIC 的效果高度依赖于 Critic 对价值函数的准确估计。在部分可观测（Partial Observability）严重或奖励极度稀疏的环境下，修正项可能引入较大方差。
*   **语言理解深度**：虽然使用了 BERT，但研究重点在于控制层的逻辑一致性，而非语言理解本身。对于极其复杂或存在歧义的逻辑指令，可能仍需更强的推理模型。
*   **探索问题**：论文未深入探讨如何引导智能体去探索那些出现频率极低的罕见指令场景。

（完）
