---
title: "LLM-X: A Scalable Negotiation-Oriented Exchange for Communication Among Personal LLM Agents"
authors: "Giuliano Lorenzoni, Paulo Alencar, Donald Cowan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11376v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 大语言模型智能体间的协调与谈判
tldr: 本研究针对个人大语言模型（LLM）智能体之间的直接通信需求，提出了LLM-X——一个可扩展的面向协商的交换平台。不同于传统的工具中心化协议，LLM-X通过消息总线和路由基座实现智能体间的结构化协作，并提供架构支持、类型化消息协议及策略执行。通过对不同规模智能体群体和协商策略的实验评估，该系统展示了在处理复杂协商任务时的稳定性与性能权衡，为多智能体系统的规模化应用提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11376v1/fig-001.webp\", \"caption\": \"Figure 1: LLM-X conceptual (negotiation-centric) architecture.\", \"page\": 3, \"index\": 1, \"width\": 1048, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2605.11376v1/fig-002.webp\", \"caption\": \"Figure 2: CNet-style conceptual negotiation sequence in LLM-X. Alice emits a CFP with a deadline; contractors reply with Offers. The Policy Engine governs when and how the winner is selected (Low: first-valid; Medium: bounded wait; High: wait-all). Acks ensure reliable delivery; retries occur on missing acknowledgments.\", \"page\": 3, \"index\": 2, \"width\": 1048, \"height\": 433}]"
motivation: 现有的智能体协议多侧重于智能体与API的交互，缺乏支持个人LLM智能体之间直接、结构化且可扩展的协商通信机制。
method: 提出了一种包含联邦网关、主题路由和策略执行的LLM-X架构，并设计了支持能力协商和合同网式协作的类型化消息协议。
result: 实验表明严格的策略能提升系统的鲁棒性和公平性，但会增加延迟和消息量，且系统在长达12小时的持续负载下保持稳定。
conclusion: LLM-X为多智能体协商提供了有效的通信基座，证明了在保证策略约束的同时实现大规模智能体协作的可行性。
---

## Abstract
We propose a personal-LLM exchange (LLM-X), a scalable negotiation-oriented environment that enables direct, structured communication across populations of personal agents (LLMs), each representing an individual user. Unlike existing tool-centric protocols that focus on agent-API interaction, LLM-X introduces a message bus and routing substrate for LLM-to-LLM coordination with guarantees around schema validity and policy enforcement. We contribute: (1) an architecture for LLM-X comprising federated gateways, topic-based routing, and policy enforcement; (2) a typed message protocol supporting capability negotiation and contract-net-style coordination; and (3) the first empirical evaluation of LLM-based multi-agent negotiation at scale. Experiments span 5, 9, and 12 agents, under distinct negotiation policies (Low, Medium, High), and across both short-run (minutes) and long-run (2h, 12h) load conditions. Results highlight clear policy-performance trade-offs: stricter policies improve robustness and fairness but increase latencies and message volume. Extended runs confirm that LLM-X remains stable under sustained load, with bounded latency drift.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **LLM-X** 的系统，旨在解决个人大语言模型（LLM）智能体之间缺乏标准化、可扩展通信机制的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前的 LLM 智能体大多处于孤立状态，主要与用户或特定的 API（工具）交互。现有的协议（如 MCP）侧重于“智能体-工具”交互，而缺乏一种能够支持大规模、异构智能体之间进行直接、结构化“协商与协作”的通信基座。
*   **核心问题**：如何构建一个可扩展的环境，让代表不同用户的个人 LLM 智能体能够遵循统一的协议进行复杂的协商（如合同网协议），同时保证通信的安全、合规和高效。

### 2. 论文提出的方法论
LLM-X 提出了一个面向协商的交换架构，其核心组件包括：
*   **核心思想**：将通信层从推理层中抽象出来，通过一个带有策略强制执行功能的“消息总线”来管理智能体间的互动。
*   **关键技术细节**：
    *   **联邦网关（Gateways）**：负责身份验证（JWT）、模式验证（JSON Schema）和速率限制，确保进入系统的消息格式正确且安全。
    *   **类型化消息协议**：基于经典的合同网（Contract Net, CNet）协议，定义了 `CFP`（招标）、`Offer`（投标）、`Accept/Reject`（接受/拒绝）和 `Ack`（确认）等消息类型。
    *   **策略引擎（Policy Engine）**：定义了三种协商策略：
        *   **Low（低要求）**：在超时前收到第一个回复或超时即关闭，风险是可能错过更优提议。
        *   **Medium（中要求）**：收到第一个有效提议后立即成交并关闭。
        *   **High（高要求）**：必须收集所有参与者的提议后才进行决策，确保公平和最优性。
    *   **传输层**：利用 NATS 消息系统实现异步的主题路由和可靠交付。

### 3. 实验设计
*   **实验场景**：模拟一个发起者（Alice）与多个承包商（Contractors）之间的协商过程。
*   **实验变量**：
    *   **智能体规模**：5、9、12 个智能体。
    *   **协商策略**：Low、Medium、High。
    *   **运行时间**：短期（2分钟）、长期（2小时、12小时）。
*   **对比基准（Benchmark）**：主要通过改变策略严苛程度来对比系统在延迟（p50/p95）、吞吐量（每分钟消息数）和回合完成率方面的表现。
*   **对比方法**：对比了不同策略在相同负载下的性能权衡。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或数量。
*   **原因分析**：由于 LLM-X 侧重于通信基座和协议效率，其实验中的智能体在原型阶段使用了 Python 包装器来模拟负载，以测试系统的扩展性和延迟。论文指出该架构是“LLM-ready”的，可以无缝接入 OpenAI 或 Anthropic 的 API，但本次实验重点在于通信基座的压力测试而非模型推理性能。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 5 到 12 个智能体的并发测试，并进行了长达 12 小时的持续负载实验。
*   **充分性评价**：
    *   **充分性**：实验设计较为全面，覆盖了短期爆发负载和长期稳定运行的情况。
    *   **客观性**：通过多次重复实验（独立随机种子）确认了结果的一致性。
    *   **局限性**：实验中的智能体行为相对简化（基于脚本的有效载荷），虽然验证了通信层的鲁棒性，但尚未在大规模真实 LLM 推理延迟环境下进行全链路测试。

### 6. 主要结论与发现
*   **策略-性能权衡**：严格的策略（High）能显著提高协商的公平性和完整性，但会带来更高的延迟和消息开销；宽松策略（Low/Medium）响应更快，但可能导致决策质量下降。
*   **系统稳定性**：LLM-X 在 12 小时的持续运行中表现出极高的稳定性，延迟漂移极小，证明了其作为基础设施的可靠性。
*   **线性扩展性**：随着智能体数量增加，消息量呈线性增长，而延迟增长保持在可控范围内（毫秒级）。

### 7. 优点
*   **架构解耦**：将协商逻辑（策略）与通信机制分离，支持即插即用的智能体集成。
*   **安全性与合规性**：通过网关层的模式验证和 JWT 验证，解决了多智能体环境中常见的格式错误和未授权访问问题。
*   **协议兼容性**：不仅支持 CNet，还扩展支持了 FIPA 风格的交替出价协议，具有良好的通用性。

### 8. 不足与局限
*   **真实推理延迟缺失**：实验主要测试了通信基座的延迟（毫秒级），但在实际应用中，LLM 的推理延迟通常在秒级，这可能会掩盖通信层的性能特征。
*   **智能体异构性有限**：实验中的智能体行为较为单一，未能充分展示在复杂、对抗性协商场景下的动态博弈。
*   **应用限制**：目前主要在受控环境下测试，尚未在复杂的互联网分布式环境中验证其联邦网关的性能。

（完）
