---
title: Runtime Monitoring of Perception-Based Autonomous Systems via Embedding Temporal Logic
title_zh: 基于嵌入时序逻辑的感知驱动自主系统运行时监控
authors: "Parv Kapoor, Abigail Hammer, Ashish Kapoor, Karen Leung, Eunsuk Kang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12651v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于感知驱动自主系统监控的时态逻辑
tldr: 针对感知驱动的自主系统在运行时监控中面临的离散逻辑映射困难、计算开销大及语义失真等挑战，本文提出了嵌入时序逻辑（ETL）。该方法直接在学习到的嵌入空间内操作，通过衡量观测嵌入与目标嵌入间的距离来定义谓词，从而捕捉复杂的感知概念。此外，引入了保形校准程序以增强安全性。实验证明，ETL在多种操作任务中能准确评估时序感知行为，为高维感知数据的逻辑监控提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12651v1/fig-001.webp\", \"caption\": \"Figure 3: Dual-predicate Boolean timelines for mw-pick-place-wall. Top panels show distances to grasp (zA) and place (zB) embeddings with thresholds ϵ∗ and ϵCP . Lower panels show ETL and ground-truth predicate activations.\", \"page\": 18, \"index\": 1, \"width\": 718, \"height\": 722}, {\"url\": \"assets/figures/arxiv/2605.12651v1/fig-002.webp\", \"caption\": \"Figure 2: Qualitative and quantitative ETL results.\", \"page\": 8, \"index\": 2, \"width\": 821, \"height\": 644}, {\"url\": \"assets/figures/arxiv/2605.12651v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of embedding-based runtime monitoring. Top: Target observations OG = {og1, . . . , ogn} and the online observation ot are encoded by a pretrained vision encoder into target embeddings zG and the current embedding zt. An embedding predicate then evaluates whether zt reaches the target set. Bottom left: The embedding trajectory is projected onto its first two principal components, showing its evolution relative to the target embeddings. Bottom right: Runtime monitoring computes the distance from zt to zG over time, thresholds it using ϵ, and evaluates the temporal logic specification.\", \"page\": 4, \"index\": 3, \"width\": 814, \"height\": 406}]"
motivation: 传统运行时监控依赖于将传感器数据映射到离散逻辑，在感知驱动的系统中存在计算昂贵、脆弱且语义不一致的问题。
method: 提出嵌入时序逻辑（ETL），直接在学习到的嵌入空间中通过计算观测值与目标嵌入之间的距离来定义谓词并进行监控。
result: 在多个操作环境中验证表明，ETL与真实语义具有高度一致性，能准确监控具有时序特征的复杂感知行为。
conclusion: ETL为感知驱动的自主系统提供了一种高效、可靠且符合语义的运行时监控方案，有效解决了高维感知数据的逻辑表达难题。
---

## 摘要
自主系统的运行时监控传统上依赖于将连续传感器观测映射到定义在低维状态变量上的离散逻辑命题。这种抽象在感知驱动的场景中会失效，因为此类映射需要额外的学习模块，而这些模块通常计算开销大、脆弱且语义不一致。在这项工作中，我们提出了嵌入时序逻辑（Embedding Temporal Logic, ETL），这是一种直接在学习到的嵌入空间中执行监控的时序逻辑。ETL 通过观测到的嵌入与源自参考观测的目标嵌入之间的距离来定义谓词。这种形式化方法允许规范捕捉高层感知概念，例如与视觉目标的相似性或对语义区域的规避，而这些概念使用传统谓词很难或无法表达。通过将这些谓词与时序算子相结合，ETL 自然地表达了时序扩展和顺序感知行为。我们引入了 ETL 监控器，用于评估有界嵌入轨迹上的规范，并结合了符合性校准（conformal calibration）程序，以提供可靠且面向安全的谓词评估。我们在多个操作环境中评估了我们的方法，结果表明 ETL 与地面真值语义具有很强的经验一致性，包括对时序组合行为的准确监控。

## Abstract
Runtime monitoring of autonomous systems traditionally relies on mapping continuous sensor observations to discrete logical propositions defined over low-dimensional state variables. This abstraction breaks down in perception-driven settings, where such mappings require additional learned modules that are often computationally expensive, brittle, and semantically misaligned. In this work, we propose Embedding Temporal Logic (ETL), a temporal logic that performs monitoring directly in learned embedding spaces. ETL defines predicates through distances between observed embeddings and target embeddings derived from reference observations. This formulation allows specifications to capture high-level perceptual concepts, such as similarity to visual goals or avoidance of semantic regions, that are difficult or impossible to express using traditional predicates. By composing these predicates with temporal operators, ETL naturally expresses temporally extended and sequential perceptual behaviors. We introduce ETL monitors for evaluating specifications over bounded embedding traces, along with a conformal calibration procedure that provides reliable and safety-oriented predicate evaluation. We evaluate our approach across multiple manipulation environments to show that ETL achieves strong empirical agreement with ground-truth semantics, including accurate monitoring of temporally composed behaviors.

---

## 论文详细总结（自动生成）

以下是对论文《Runtime Monitoring of Perception-Based Autonomous Systems via Embedding Temporal Logic》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现代自主系统（如自动驾驶、机器人）越来越多地依赖高维传感器（如摄像头）和深度学习生成的嵌入表示（Embeddings）。然而，传统的运行时监控技术（如信号时序逻辑 STL）通常定义在低维、可解释的状态变量（如位置、速度）上。
*   **核心挑战**：在感知驱动的系统中，将高维图像映射到离散逻辑命题非常困难。现有的方法通常需要额外的分类器或检测器，这些模块不仅计算昂贵、脆弱，且容易产生语义对齐偏差。
*   **整体含义**：本文旨在打破“感知”与“逻辑监控”之间的壁垒，提出一种直接在学习到的嵌入空间中定义和评估逻辑规范的方法，从而实现对复杂视觉行为的实时、高保信监控。

### 2. 方法论
*   **核心思想**：提出**嵌入时序逻辑（Embedding Temporal Logic, ETL）**。其关键见解是：预训练编码器（如 CLIP, DINOv2）已将语义相似性编码在几何空间中。因此，可以将逻辑谓词定义为观测嵌入与目标参考嵌入之间的“距离”。
*   **关键技术细节**：
    *   **嵌入谓词（Embedding Predicate）**：定义为 $ap \equiv (Z_{target}, d, \epsilon, \dots)$。当当前观测嵌入 $z_t$ 与目标集合 $Z_{target}$ 的最小距离小于阈值 $\epsilon$ 时，谓词为真。
    *   **时序算子**：继承了线性时序逻辑（LTL）的算子，如 $\Box$（总是）、$\diamondsuit$（最终）、$\mathcal{U}$（直到），用于表达顺序行为（如“先抓取再放置”）。
    *   **鲁棒性语义**：引入了定量语义，通过距离差值衡量系统满足规范的程度。
*   **阈值校准算法**：
    *   **F1-Optimal**：在校准集上搜索使 F1 分数最大化的阈值，平衡精确率和召回率。
    *   **保形校准（Conformal Calibration）**：利用保形预测理论，在不假设分布的前提下，为安全关键任务提供具有统计保证的召回率阈值（$\epsilon_{CP}$）。

### 3. 实验设计
*   **实验场景**：
    1.  **简单导航（Dubins Car）**：在受控的 2D 环境中验证 ETL 与地面真值（Ground-truth）状态逻辑的一致性。
    2.  **模拟操作任务**：使用 **D3IL**（分类、堆叠任务）和 **MetaWorld**（取放任务）基准，测试在复杂视觉和接触力学下的表现。
    3.  **真实世界数据**：使用 **DROID** 机器人操作数据集，评估在非结构化、真实环境中的多阶段任务监控能力。
*   **对比方法（Baselines）**：
    *   **PCA-kmeans**：基于主成分分析的聚类监控。
    *   **logpZO**：基于流匹配（Flow-matching）密度模型的确定性/不确定性监控。
    *   **VLM 强基线**：使用最先进的视觉语言模型 **Qwen2-VL-2B** 进行零样本（Zero-shot）任务完成度检测。

### 4. 资源与算力
*   **硬件环境**：实验运行在计算集群上，使用了 **2 台 NVIDIA GeForce RTX 5090 GPU**（每台显存 34.2 GB）。
*   **训练/推理说明**：由于 ETL 依赖预训练的编码器（如 DINOv2 或 Dreamer 编码器），其核心开销在于嵌入提取和距离计算，而非大规模模型训练。文中未详细列出具体的校准耗时，但强调了其运行时监控的轻量级特性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   导航任务使用了 100 条轨迹。
    *   模拟操作涵盖了 3 个不同的环境和多种任务类型。
    *   真实世界评估选取了 DROID 数据集中具有代表性的 5 类复杂操作序列。
*   **充分性评价**：实验设计较为全面，涵盖了从简单合成环境到复杂真实数据的跨度。通过对比 F1 分数、精确率、召回率以及顺序正确性（Ordering Agreement），客观地证明了 ETL 在处理时序组合行为上的优越性。

### 6. 主要结论与发现
*   **语义一致性**：ETL 谓词能高度准确地近似地面真值逻辑，在导航任务中 F1 分数达到 0.8-0.85，协议一致性超过 96%。
*   **时序表达力**：ETL 能够成功监控具有顺序性的任务（如 A $\to$ B），在 DROID 数据集中，其顺序检测准确率远高于 VLM 基线。
*   **校准有效性**：保形校准程序能有效提供面向安全的阈值，在牺牲极小精确率的情况下显著提升召回率。
*   **优于基线**：在模拟操作任务中，ETL 的平均 F1 分数（0.817）优于 logpZO（0.790）和 PCA-kmeans（0.751）。

### 7. 优点
*   **无需显式感知模块**：不需要训练专门的物体检测器或分类器，直接利用预训练模型的潜在空间。
*   **形式化保证**：结合了时序逻辑的严谨性和保形预测的统计保证，适合安全关键型应用。
*   **通用性强**：可以适配不同的编码器（DINOv2, CLIP, VAE）和不同的传感器模态。

### 8. 不足与局限
*   **可解释性欠缺**：嵌入空间中的谓词（距离阈值）不如物理状态变量（如“距离 < 0.5米”）直观，难以向人类用户解释失败的具体物理原因。
*   **依赖编码器质量**：监控性能高度依赖于预训练编码器是否能很好地分离任务相关的语义特征。如果两个语义不同的状态在嵌入空间中距离很近，监控器会失效。
*   **静态阈值风险**：目前的阈值校准是离线的，在环境发生剧烈分布偏移（OOD）时，可能需要在线自适应调整技术。

（完）
