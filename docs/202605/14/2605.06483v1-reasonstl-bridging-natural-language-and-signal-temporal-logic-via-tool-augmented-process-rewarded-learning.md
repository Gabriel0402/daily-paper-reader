---
title: "ReasonSTL: Bridging Natural Language and Signal Temporal Logic via Tool-Augmented Process-Rewarded Learning"
title_zh: ReasonSTL：通过工具增强的过程奖励学习连接自然语言与信号时序逻辑
authors: "Bowen Ye, Zhijian Li, Junyue Huang, Junkai Ma, Xiang Yin"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06483v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 连接自然语言与信号时序逻辑
tldr: 本研究针对自动驾驶和信息物理系统中自然语言向信号时序逻辑（STL）转换的难题，提出了ReasonSTL框架。该框架利用本地开源大模型，将翻译过程分解为显式推理、工具调用和结构化公式构建，并引入过程奖励训练和STL-Bench基准。实验证明，4B规模的模型即可达到SOTA性能，为形式化规范生成提供了低成本、隐私安全的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06483v1/fig-001.webp\", \"caption\": \"Figure 2: Overview of REASONSTL. The framework converts natural-language requirements into structured STL specifications through interleaved reasoning and deterministic tool execution, and is optimized with outcome-bounded process rewards to supervise both intermediate tool use and final formula construction.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2605.06483v1/fig-002.webp\", \"caption\": \"Table 8: Domain and scenario taxonomy of STL-BENCH.\", \"page\": 15, \"index\": 2, \"width\": 807, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2605.06483v1/fig-003.webp\", \"caption\": \"Table 7: Main fields in the structured STL JSON schema.\", \"page\": 14, \"index\": 3, \"width\": 799, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.06483v1/fig-004.webp\", \"caption\": \"Table 12: Training hyperparameters for REASONSTL.\", \"page\": 20, \"index\": 4, \"width\": 782, \"height\": 794}, {\"url\": \"assets/figures/arxiv/2605.06483v1/fig-005.webp\", \"caption\": \"Table 1: Summary of STL-BENCH construction and evaluation splits.\", \"page\": 5, \"index\": 5, \"width\": 818, \"height\": 326}, {\"url\": \"assets/figures/arxiv/2605.06483v1/fig-006.webp\", \"caption\": \"Table 2: Comparison with representative NL-to-STL datasets.\", \"page\": 5, \"index\": 6, \"width\": 818, \"height\": 248}]"
motivation: 旨在解决自然语言到STL转换中人工编写困难、商业模型成本高昂及隐私泄露风险等核心痛点。
method: 提出一种工具增强框架，通过显式推理、确定性工具调用和过程奖励训练来优化本地开源语言模型。
result: 基于ReasonSTL训练的4B模型在STL-Bench基准测试的自动指标和人工评估中均取得了当前最佳性能。
conclusion: ReasonSTL证明了小型本地模型在处理复杂形式化规范任务上的潜力，是高效且隐私友好的替代方案。
---

## 摘要
信号时序逻辑（STL）是一种表达能力强的形式化语言，用于规定实值实时信号的时空需求。它已广泛应用于自动系统和信息物理系统（CPS）的验证与合成。然而在实践中，用户通常使用自然语言而非结构化的 STL 公式来表达需求，这使得自然语言到 STL 的翻译成为一项关键且具有挑战性的任务。手动规范需要时序逻辑方面的专业知识且无法扩展，而调用商用大语言模型（LLM）API 会产生高昂的 token 成本，并可能向第三方服务泄露敏感的系统需求，从而引发工业部署中的隐私担忧。为了应对这些挑战，我们提出了 ReasonSTL，这是一个工具增强的框架，旨在使本地开源语言模型适配自然语言到 STL 的生成。ReasonSTL 将翻译过程分解为显式推理、确定性工具调用和结构化公式构建。我们进一步引入了过程奖励训练来监督工具使用轨迹和最终公式，并推出了 STL-Bench，这是一个基于真实世界信号的双语、计算感知基准测试。实验表明，使用 ReasonSTL 训练的 4B 模型在自动指标和人工评估中均达到了最先进的性能，证明了 ReasonSTL 为形式化规范起草提供了一种透明、低成本且保护隐私的替代方案。

## Abstract
Signal Temporal Logic (STL) is an expressive formal language for specifying spatio-temporal requirements over real-valued, real-time signals. It has been widely used for the verification and synthesis of autonomous systems and cyber-physical systems. In practice, however, users often express their requirements in natural language rather than in structured STL formulas, making natural-language-to-STL translation a critical yet challenging task. Manual specification requires temporal-logic expertise and cannot scale, while prompting commercial LLM APIs incurs substantial token costs and may expose sensitive system requirements to third-party services, raising privacy concerns for industrial deployment. To address these challenges, we present \textsc{ReasonSTL}, a tool-augmented framework that adapts local open-source language models for natural-language-to-STL generation. \textsc{ReasonSTL} decomposes the translation process into explicit reasoning, deterministic tool calls, and structured formula construction. We further introduce process-rewarded training to supervise both tool-use trajectories and final formulas, together with \textsc{STL-Bench}, a bilingual, computation-aware benchmark grounded in real-world signals. Experiments show that a 4B model trained with \textsc{ReasonSTL} achieves state-of-the-art performance in both automatic metrics and human evaluations, demonstrating that \textsc{ReasonSTL} provides a transparent, low-cost, and privacy-preserving alternative for formal specification drafting.

---

## 论文详细总结（自动生成）

这是一份关于论文《ReasonSTL: Bridging Natural Language and Signal Temporal Logic via Tool-Augmented Process-Rewarded Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**自然语言（NL）到信号时序逻辑（STL）的自动翻译**。STL 是描述自动驾驶、机器人等信息物理系统（CPS）时空需求的关键形式化语言。
*   **痛点**：手动编写 STL 需要极高的专业知识且效率低下；使用商业大模型（如 GPT-4）API 成本高昂，且涉及工业敏感数据，存在严重的隐私泄露风险。
*   **挑战**：STL 翻译不仅涉及语法转换，还涉及复杂的数值计算（如单位换算、时间归一化）、谓词接地和嵌套逻辑结构。现有方法往往缺乏中间推理过程，导致结果不可解释且易出错。

### 2. 方法论：ReasonSTL 框架
ReasonSTL 的核心思想是将翻译任务从“单步序列预测”转变为“**显式推理 + 确定性工具调用 + 结构化构建**”的复合过程。
*   **工具增强生成**：模型不直接输出公式字符串，而是先进行思考（Think），在需要时调用预定义的工具集（如 `parse_duration` 处理时间、`convert_unit` 处理单位、`eval_math_expr` 处理算术），最后根据工具返回的精确数值构建结构化的 JSON 树。
*   **结果约束的过程奖励（Outcome-bounded Process Supervision）**：
    *   引入过程奖励模型，对工具调用的每一步进行验证。
    *   **关键公式逻辑**：中间步骤的奖励受最终结果约束。即使中间步骤看起来合理，如果最终公式错误，中间步骤的奖励也会被大幅削减（通过一个因子 $\tau$ 限制），从而避免模型学习到“看似正确但逻辑误导”的推理路径。
*   **结构化表示**：使用 JSON 树代替扁平字符串，消除了括号优先级歧义，便于进行递归树匹配评估。

### 3. 实验设计
*   **基准测试 (Benchmark)**：
    *   **STL-Bench（本文提出）**：包含 28,880 个双语（中英）NL-STL 对，覆盖 6 个工程领域（自动驾驶、航空航天等）和 33 个场景。73% 的样本需要中间计算。
    *   **DeepSTL**：现有的标准 NL-to-STL 数据集，用于验证通用性能。
*   **对比方法**：
    *   **商业 API**：Claude-3.5-Opus、GPT-4o、DeepSeek-V3、Kimi-k1.5 等。
    *   **专用模型**：RESTL、KGST、DeepSTL 等先前研究中的 SOTA 方法。
*   **评估指标**：格式准确率（Format Acc）和公式准确率（Formula Acc，基于递归树匹配，允许一定的数值容差）。

### 4. 资源与算力
论文明确披露了硬件与训练细节：
*   **硬件**：训练使用 **8 张 NVIDIA H20 GPU**，推理评估使用单张 H20。
*   **时长**：
    *   ReasonSTL-Direct（不含推理）：训练约 20 小时。
    *   ReasonSTL（全功能版）：训练约 **5 天**（5000 步强化学习）。
*   **模型规模**：主要基于 Qwen2.5-4B（文中称为 Qwen3-4B，可能为笔误或预发布版本代号）进行微调。

### 5. 实验数量与充分性
实验设计非常充分且客观：
*   **多维度对比**：涵盖了自动指标评估、人工语义验证（100 组随机采样）以及在完全脱离模板的人工编写测试集上的泛化性测试。
*   **消融实验**：对比了仅 SFT、仅 RL、SFT+RL 以及有无工具增强等多种变体，验证了过程奖励的有效性。
*   **公平性**：对 API 模型使用了精心设计的 Few-shot Prompt，并对所有模型应用了相同的结构化验证逻辑。

### 6. 主要结论与发现
*   **小模型胜大模型**：经过 ReasonSTL 训练的 **4B 本地模型**在 STL-Bench 上的表现优于 GPT-4o 和 Claude-3.5 等顶级商业模型。
*   **推理的重要性**：显式推理和工具调用显著提升了处理复杂数值和单位换算的能力，Formula Acc 提升了约 10-15%。
*   **过程奖励的威力**：相比于只看结果的强化学习，过程奖励能有效减少模型在复杂嵌套逻辑上的“幻觉”。

### 7. 优点（亮点）
*   **隐私与成本平衡**：证明了在本地部署轻量级模型即可完成高质量的形式化规范起草。
*   **计算感知**：通过工具调用解决了传统 NLP 模型难以处理的精确数值计算问题。
*   **高质量基准**：STL-Bench 提供了丰富的领域接地（Domain Grounding）和工具使用标注，填补了该领域的空白。

### 8. 不足与局限
*   **推理开销**：显式推理和多轮工具调用增加了 token 输出长度，导致推理延迟高于直接生成模型。
*   **标注依赖**：虽然引入了模型辅助生成，但高质量的过程奖励仍依赖于确定性的验证规则，对于极度模糊的自然语言描述可能失效。
*   **安全风险**：尽管准确率达到 SOTA，但在安全关键型系统中，生成的公式仍需人工审核，不能完全实现无人值守的自动化。

（完）
