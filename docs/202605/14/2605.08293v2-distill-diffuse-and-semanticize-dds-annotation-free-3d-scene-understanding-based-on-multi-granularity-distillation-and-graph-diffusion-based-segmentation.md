---
title: "Distill, Diffuse, and Semanticize (DDS): Annotation-Free 3D Scene Understanding Based on Multi-Granularity Distillation and Graph-Diffusion-Based Segmentation"
title_zh: 蒸馏、扩散与语义化 (DDS)：基于多粒度蒸馏和图扩散分割的无标注三维场景理解
authors: "Yijing Wang, Ruonan Li, Qilin Wang, Rongqiang Zhao, Jie Liu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08293v2"
tags: ["query:mmseg"]
score: 9.0
evidence: 用于自动驾驶和具身感知的3D语义场景理解
tldr: 针对3D场景理解中点云标注成本高昂的问题，本文提出DDS框架。该框架结合了多粒度蒸馏和基于图扩散的分割技术，在保持轻量化超点结构的同时，引入视觉语义线索。通过在点、掩码原型及原型间进行蒸馏，并利用图扩散传播语义，DDS实现了无需标注的区域一致性语义识别，在多个指标上显著优于现有结构导向的基准方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08293v2/fig-001.webp\", \"caption\": \"Table 1: Quantitative comparison with representative structureoriented annotation-free baselines on the nuScenes dataset. The best result in each column is shown in bold. ↑ denotes higher is better.\", \"page\": 6, \"index\": 1, \"width\": 492, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2605.08293v2/fig-002.webp\", \"caption\": \"Table 2: Quantitative comparison with representative structureoriented annotation-free baselines on the SemanticKITTI dataset. The best result in each column is shown in bold. ↑ denotes higher is better.\", \"page\": 6, \"index\": 2, \"width\": 480, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2605.08293v2/fig-003.webp\", \"caption\": \"Figure 4: (a) and (b) show the visualized comparisons on the nuScenes and SemanticKITTI respectively, while (c) and (d) present the box-plot distributions of per-class IoU on the two datasets. The box represents the interquartile range, the blue solid line inside represents the median, and⋆ represents the mean value.\", \"page\": 11, \"index\": 3, \"width\": 1068, \"height\": 1256}, {\"url\": \"assets/figures/arxiv/2605.08293v2/fig-004.webp\", \"caption\": \"Figure 5: (a), (b), and (c) respectively depict BEV segmentation maps of nuScenes traffic scenes under sunny, nighttime, and rainy conditions.\", \"page\": 12, \"index\": 4, \"width\": 1068, \"height\": 1270}]"
motivation: 现有的无标注3D场景理解方法在强大的语义识别能力与轻量化的结构效率之间难以兼得。
method: 提出一种包含多粒度特征蒸馏、超点图扩散传播以及分割簇关联语义化步骤的轻量化框架。
result: "在真实世界数据集上的实验表明，DDS在oAcc、mAcc和mIoU等指标上分别提升了高达5.9%、8.1%和2.4%。"
conclusion: DDS为无标注3D场景理解提供了一种可扩展、可解释且高效的解决方案，显著提升了区域一致性和语义识别性能。
---

## 摘要
三维语义场景理解对于数字孪生、自动驾驶、智慧农业和具身感知至关重要，但点云的密集逐点标注仍然昂贵且难以扩展。现有的无标注方法通常在语义识别和结构效率之间面临权衡：开放词汇和基础模型驱动的方法提供了强大的语义先验，但往往伴随着巨大的计算开销；而基于超点、聚类和图推理的结构导向方法虽然轻量，但通常产生与类别无关的区域。我们提出了 DDS，这是一个资源高效的结构导向框架，用于区域一致且语义化的无标注三维场景理解。DDS 保留了轻量级的基于超点的组织范式，同时结合了来自投影特征和分割衍生掩码的视觉语义线索。它首先执行多粒度蒸馏，在点、掩码原型和原型间水平上引导三维骨干网络，然后在超点上应用图扩散，直接在三维空间中传播语义信息，从而在无需昂贵的谱分解或密集开放词汇三维特征场的情况下产生连贯的区域表示。最后，DDS 利用分割-聚类关联为类别无关的三维聚类分配可解释的语义名称。在真实世界数据集上的实验表明，DDS 在代表性的结构导向无标注基准方法中实现了最佳性能，oAcc、mAcc 和 mIoU 分别提高了高达 5.9%、8.1% 和 2.4%。这些结果表明，DDS 提高了区域一致性和轻量级语义识别，为无标注三维场景理解提供了一种可扩展且可解释的解决方案。

## Abstract
3D semantic scene understanding is essential for digital twins, autonomous driving, smart agriculture, and embodied perception, yet dense point-wise annotation for point clouds remains expensive and difficult to scale. Existing annotation-free methods often face a trade-off between semantic recognition and structural efficiency: open-vocabulary and foundation-model-driven methods provide strong semantic priors, but often come with substantial computational costs, while structure-oriented methods based on superpoints, clustering, and graph reasoning are lightweight but often produce category-agnostic regions. We propose DDS, a resource-efficient structure-oriented framework for region-consistent and semanticized annotation-free 3D scene understanding. DDS preserves the lightweight superpoint-based organization paradigm while incorporating visual semantic cues from projected features and segmentation-derived masks. It first performs multi-granularity distillation to guide the 3D backbone at the point, mask-prototype, and inter-prototype levels, then applies graph diffusion over superpoints to propagate semantic information directly in 3D, producing coherent region representations without costly spectral decomposition or dense open-vocabulary 3D feature fields. Finally, DDS uses segmentation-cluster association to assign interpretable semantic names to category-agnostic 3D clusters. Experiments on real-world datasets show that DDS achieves the best performance among representative structure-oriented annotation-free baselines, improving oAcc, mAcc, and mIoU by up to 5.9%, 8.1%, and 2.4%, respectively. These results demonstrate that DDS improves region consistency and lightweight semantic recognition, providing a scalable and interpretable solution for annotation-free 3D scene understanding.

---

## 论文详细总结（自动生成）

这是一份关于论文《Distill, Diffuse, and Semanticize (DDS): Annotation-Free 3D Scene Understanding Based on Multi-Granularity Distillation and Graph-Diffusion-Based Segmentation》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：3D点云的语义分割在自动驾驶和机器人领域至关重要，但手动进行逐点标注（Point-wise annotation）成本极高且难以大规模扩展。
*   **现有挑战**：目前的无标注（Annotation-free）方法存在两极分化：
    *   **基础模型驱动法**（如基于CLIP、DINOv2）：语义识别强，但计算和内存开销巨大，难以实时或在资源受限设备上运行。
    *   **结构导向法**（如基于聚类、超点）：轻量高效，但通常只能产生“类别无关”的几何区域，缺乏明确的语义标签。
*   **核心目标**：提出一个名为 **DDS** 的框架，旨在保持轻量化结构的同时，通过视觉知识迁移实现具有语义解释力的无标注3D场景理解。

### 2. 方法论：核心思想与关键技术
DDS 框架分为三个阶段：
*   **阶段 I：多粒度蒸馏 (Multi-Granularity Distillation)**
    *   **核心思想**：利用预训练的 2D 基础模型（DINOv2 提供特征，SAM3 提供掩码）作为教师，引导 3D 学生网络。
    *   **技术细节**：在三个层级进行约束：
        1.  **点级（Point-level）**：将 2D 特征投影到 3D 点上，进行余弦相似度对齐。
        2.  **掩码原型级（Mask-prototype level）**：利用 SAM3 生成的 3D 掩码组计算区域原型，强制 3D 区域特征与 2D 区域语义一致。
        3.  **原型间对比（Inter-prototype level）**：通过 InfoNCE 损失增强不同掩码区域之间的判别性。
*   **阶段 II：基于图扩散的分割 (Graph-Diffusion-Based Segmentation)**
    *   **核心思想**：在超点（Superpoint）构成的图上进行语义传播，以获得全局平滑的区域表示。
    *   **技术细节**：构建超点图，利用 RBF 核定义边权重。通过迭代公式 $H^{(t+1)} = (1-\alpha)F + \alpha \tilde{A}H^{(t)}$ 进行扩散。相比于传统的特征分解（Eigendecomposition），这种方法计算复杂度更低（$O(TN_s^2C)$）。
*   **阶段 III：语义化推理 (Semantic Inference)**
    *   **核心思想**：通过“分割-聚类关联”为无类别聚类分配语义名称。
    *   **技术细节**：在推理时，将预测的 3D 簇与 SAM3 投影的视觉标签进行重叠投票（Majority Voting），从而给每个簇贴上具体的语义标签（如“汽车”、“行人”）。

### 3. 实验设计
*   **数据集**：使用了两个大规模自动驾驶 LiDAR 数据集：**nuScenes**（32线）和 **SemanticKITTI**（64线）。
*   **Benchmark 与对比方法**：
    *   **结构导向基准**：K-means, IIC, PiCIE, GrowSP, PointDC, LogoSP 等。
    *   **参考基准（高性能/高开销）**：CLIP2Scene, OpenScene, AFOV 等。
*   **评估指标**：总体准确率 (oAcc)、平均准确率 (mAcc) 和平均交并比 (mIoU)。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 3090 GPU (24 GB)**。
*   **训练时长**：
    *   阶段 I（蒸馏）：在 nuScenes 上约 **4 小时**。
    *   阶段 II（分割）：约 **0.5 小时**。
*   **对比优势**：相比于 AFOV（需要 4 张 V100 训练 80 小时）或 OpenScene（4 张 A100 训练 24 小时），DDS 的资源消耗极低，体现了其“资源高效”的特点。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集上进行了完整测试，并提供了 BEV（鸟瞰图）可视化、误差图分析和每类 IoU 的统计分布（箱线图）。
*   **消融实验**：针对蒸馏策略（点级 vs 多粒度）、特征处理方法（PCA vs GFT vs 扩散）以及损失函数权重（$\lambda_{proto}, \lambda_{nce}$）进行了详细的消融研究。
*   **客观性**：实验设置了跨域验证（在 nuScenes 蒸馏，直接在 SemanticKITTI 测试），证明了模型的泛化能力。对比实验涵盖了从传统聚类到最新 SOTA 的多种方法，评价较为客观公平。

### 6. 主要结论与发现
*   **性能领先**：DDS 在结构导向的无标注方法中表现最佳，在 nuScenes 上 mIoU 达到 21.2%，在 SemanticKITTI 上达到 17.7%。
*   **效率卓越**：在单卡 3090 上即可完成训练，且推理过程不依赖昂贵的 3D 开放词汇特征场。
*   **鲁棒性**：可视化结果显示，DDS 在晴天、雨天和夜晚等不同环境条件下均能保持较好的 BEV 语义布局一致性。

### 7. 优点
*   **多粒度约束**：通过引入掩码原型，克服了单纯点对点对齐导致的物体内部不一致问题。
*   **高效图扩散**：提出了一种比谱分解更轻量的超点处理方式，适合大规模点云处理。
*   **可解释性**：通过推理时的关联机制，让无监督聚类结果具备了明确的语义含义，增强了实用性。

### 8. 不足与局限
*   **性能上限**：虽然在同类轻量级方法中领先，但与那些消耗巨大算力的全量开放词汇方法（如 OpenScene）相比，mIoU 仍有较大差距（21.2% vs 41.3%）。
*   **依赖 2D 预训练模型**：DDS 的效果高度依赖于 DINOv2 和 SAM3 的质量，如果 2D 模型在某些特定场景（如极端遮挡）失效，3D 端的蒸馏信号也会受损。
*   **类别限制**：语义化过程依赖于预定义的 Prompt 集，对于完全未见过的长尾类别，投票机制可能失效。

（完）
