---
title: Distributed TD Tracking with Linear Function Approximation over Directed Communication Networks
title_zh: 有向通信网络上具有线性函数逼近的分布式TD跟踪
authors: "Haocheng Yang, Shengchao Zhao, Yongchao Liu"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04466v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 有向网络上的多智能体强化学习分布式策略评估
tldr: 本文研究了有向通信网络下的多智能体强化学习策略评估问题。针对现有方法在有向图中收敛慢且不稳定的挑战，提出了PP-DTD算法。该算法将线性函数逼近的TD学习与Push-Pull机制相结合，并引入方差缩减技术以提升性能。理论证明该算法在独立同分布或马尔可夫样本下均能达到与单智能体TD相当的O(T^-1)收敛率，并通过实验验证了其在协作导航任务中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.04466v1/fig-001.webp\", \"caption\": \"Fig. 2: Markovian setting.\", \"page\": 11, \"index\": 1, \"width\": 514, \"height\": 796}, {\"url\": \"assets/figures/arxiv/2605.04466v1/fig-002.webp\", \"caption\": \"Fig. 1: i.i.d. setting.\", \"page\": 11, \"index\": 2, \"width\": 514, \"height\": 796}]"
motivation: 旨在解决多智能体强化学习在有向通信网络环境下进行分布式策略评估时，收敛速度慢且算法不稳定的问题。
method: 提出了PP-DTD算法，该算法集成了Push-Pull通信机制、线性函数逼近的TD学习以及方差缩减技术。
result: 理论证明该算法在衰减步长下可实现O(T^-1)的收敛率，且在恒定步长下能线性收敛至最优解邻域。
conclusion: PP-DTD是首个在有向图分布式策略评估中达到与单智能体TD算法相当收敛速度的方法，具有显著的鲁棒性和有效性。
---

## 摘要
我们研究了有向通信网络上多智能体强化学习（MARL）中的策略评估问题，其中智能体相互协作以探索未知环境并完成特定任务。我们提出了一种名为 PP-DTD 的 Push-Pull 型分布式算法，用于在线性函数逼近的时间差分（TD）学习框架下进行 MARL 策略评估。PP-DTD 将 TD 学习与 Push-Pull 机制相结合以适应有向通信网络，并进一步利用方差缩减技术来增强算法的稳定性和收敛速度。我们证明了在样本为独立同分布或马尔可夫分布的情况下，PP-DTD 在固定步长下能线性收敛至最优解的邻域，在递减步长下能达到 $\mathcal{O}({T^{-1}})$ 的收敛速度。据我们所知，PP-DTD 是首个在有向图上的 MARL 策略评估分布式算法，其收敛速度可与单智能体 TD 算法相媲美。在协作导航任务上的数值实验证明了 PP-DTD 的鲁棒性和有效性。

## Abstract
We study the policy evaluation problem in multi-agent reinforcement learning (MARL) over directed communication networks, where agents cooperate with each other to explore an unknown environment and accomplish a specific task. We propose a Push-Pull-type distributed algorithm, named PP-DTD, for policy evaluation in MARL within the framework of temporal difference (TD) learning with linear function approximation. PP-DTD integrates TD learning with the Push-Pull mechanism to accommodate directed communication networks, and further utilizes variance reduction techniques to enhance both algorithmic stability and convergence rate. We show that PP-DTD achieves linear convergence to a neighborhood of the optimum under constant step-sizes and a convergence rate of $\mathcal{O}({T^{-1}})$ under decaying step-sizes when the sample is independent and identically distributed or Markovian. To the best of our knowledge, PP-DTD is the first distributed algorithm for policy evaluation in MARL over directed graphs that achieves a comparable convergence rate to single-agent TD. The numerical experiments on cooperative navigation tasks demonstrate the robustness and effectiveness of PP-DTD.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种在有向通信网络环境下，针对多智能体强化学习（MARL）策略评估问题的新型分布式算法。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在**有向通信网络**中，让多个智能体通过协作高效、稳定地评估一个共同策略的价值函数。
*   **研究动机**：
    *   **网络现实性**：现有的分布式多智能体时间差分（TD）学习算法大多假设通信网络是无向的（对称的）。但在无人机、移动机器人等实际场景中，由于发射功率差异或环境干扰，通信链路往往是单向的（有向图）。
    *   **性能瓶颈**：现有的针对有向图的分布式算法（如基于 Push-sum 的方法）通常收敛速度较慢，且往往需要智能体了解其邻居的入度信息，限制了算法的灵活性。
    *   **理论差距**：缺乏一种能在有向图上达到与单智能体 TD 算法同等收敛效率（$O(T^{-1})$）的分布式算法。

### 2. 论文提出的方法论：PP-DTD 算法
*   **核心思想**：将**Push-Pull（推拉）通信机制**、**线性函数逼近**与**混合方差缩减技术**相结合。
*   **关键技术细节**：
    *   **Push-Pull 机制**：算法使用一个行随机矩阵（Row-stochastic）来混合智能体的参数估计（Push），同时使用一个列随机矩阵（Column-stochastic）来追踪全局半梯度的平均值（Pull）。这种设计解耦了参数一致性和梯度追踪，且不需要知道入度信息。
    *   **线性函数逼近**：使用特征向量的线性组合来近似价值函数，以应对大规模状态空间。
    *   **混合方差缩减**：引入了一种动量辅助的方差缩减技术，通过利用历史梯度信息来降低随机采样带来的噪声，从而确保估计误差随时间消失。
    *   **投影步骤（针对马尔可夫采样）**：在马尔可夫采样设置下，增加了一个投影步骤将参数限制在一定范围内，以增强算法在非独立同分布数据下的稳定性。

### 3. 实验设计
*   **实验场景**：**协作导航任务（Cooperative Navigation）**。智能体需要协作覆盖所有目标地标，同时避免碰撞。
*   **Benchmark（基准方法）**：**Push-SA**（一种基于 Push-sum 的分布式随机逼近算法，是目前有向图策略评估的代表性方法）。
*   **对比设置**：
    *   **网络规模**：分别在 $n=20, 40, 80$ 个智能体的网络上进行测试。
    *   **采样模式**：对比了独立同分布（i.i.d.）采样和马尔可夫（Markovian）采样两种情况。
    *   **步长策略**：对比了恒定步长（快速收敛至邻域）和递减步长（收敛至最优解）。

### 4. 资源与算力
*   论文中**未明确说明**具体的硬件配置（如 GPU/CPU 型号、数量）及具体的训练时长。实验主要是在模拟器环境下进行的数值仿真。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 种不同规模的网络（20/40/80 节点）和 2 种采样模式进行了实验。
*   **重复性**：每组实验均独立运行了 10 次并取平均值，记录了共识误差（Consensus Error）和平均绝对 TD 误差。
*   **充分性评价**：实验涵盖了算法的核心理论点（收敛速度、鲁棒性、可扩展性），对比了当前最相关的基准算法，结果较为客观。但实验场景相对单一（仅限于协作导航），未在更复杂的 MARL 环境中验证。

### 6. 主要结论与发现
*   **收敛速度**：理论证明并实验验证了 PP-DTD 在递减步长下达到了 $O(T^{-1})$ 的收敛率，这与单智能体 TD 算法的最优速率一致。
*   **有向图优势**：PP-DTD 成功解决了有向图中的权重不平衡问题，且在不需要邻居入度信息的情况下表现优于 Push-SA。
*   **稳定性**：方差缩减技术显著提升了算法的稳定性，尤其是在智能体数量增加时，PP-DTD 的误差下降更平滑且最终误差更低。

### 7. 优点
*   **理论完备**：是首个在有向图上证明能达到 $O(T^{-1})$ 收敛率的分布式策略评估算法。
*   **架构灵活**：Push-Pull 结构对网络拓扑的要求较低（仅需强连通），且不需要全局或入度信息，易于分布式部署。
*   **高性能**：结合了方差缩减，解决了分布式强化学习中常见的采样噪声大、收敛慢的问题。

### 8. 不足与局限
*   **函数逼近限制**：目前仅限于**线性**函数逼近，尚未扩展到深度神经网络（非线性逼近）。
*   **超参数敏感性**：算法引入了多个超参数（步长 $\alpha$、动量参数 $\beta$、投影半径 $R$），在实际应用中可能需要精细调优。
*   **任务范围**：仅讨论了策略评估（Policy Evaluation），即给定策略求价值，尚未涉及更复杂的策略优化（Policy Improvement）或 Actor-Critic 框架。

（完）
