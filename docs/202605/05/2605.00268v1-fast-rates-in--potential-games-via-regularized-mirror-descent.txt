Title: Fast Rates in $\alpha$-Potential Games via Regularized Mirror Descent

URL Source: https://arxiv.org/pdf/2605.00268v1

Published Time: Mon, 04 May 2026 00:17:33 GMT

Number of Pages: 24

Markdown Content:
# Fast Rates in α-Potential Games via Regularized Mirror Descent 

Claire Chen 

The Division of Physics, Mathematics and Astronomy California Institute of Technology 

clairechen@caltech.edu 

Yuheng Zhang 

Department of Computer Science University of Illinois Urbana-Champaign 

yuhengz2@illinois.edu 

## Abstract 

An α-potential game is a multi-player non-cooperative interaction in which a global potential function approximates individual player rewards up to a structural bias 

α. While identifying a Nash Equilibrium (NE) in generic general-sum games is known to be computationally intractable, the potential game structure enables tractable NE identification. In this paper, we study the offline learning of NE in 

α-potential games using KL regularization. To analyze this process, we propose a novel Reference-Anchored offline data coverage framework—a verifiable condition that anchors data requirements to a known reference policy rather than an unknown optimum. Building on this, we propose Offline Potential Mirror Descent (OPMD), a decentralized algorithm that achieves an accelerated eO(1 /n ) statistical rate, surpassing the standard eO(1 /√n) rate typical of offline multi-agent learning. This work characterizes the first fast-rate offline learning approach for α-potential games. 

## 1 Introduction 

Finding a Nash equilibrium in general-sum games is PPAD-complete (Daskalakis et al., 2009; Chen et al., 2009), establishing that equilibrium learning is computationally intractable in generic general-sum games. This fundamental barrier poses a significant challenge for multi-agent reinforcement learning (MARL), where the goal is to derive equilibrium policies in high-dimensional environments. Fortunately, recent theoretical advancements show that the α-potential game framework provides a general form for dynamic N -player non-cooperative games (Guo et al., 2023, 2025b; Li, 2025). This structure reduces the complex task of identifying Nash equilibria to a single-objective optimization problem, as any optimizer of the global potential function is guaranteed to be an equilibrium within a corresponding α-error bound (Guo et al., 2023). Such a reduction provides a mathematically tractable framework that bypasses the complexities of generic general-sum dynamics. Crucially, it has been shown that the α-potential structure is universal. Specifically, any finite non-cooperative game can be characterized as an α-potential game, where the magnitude of α

represents the inherent structural bias of the system (Guo et al., 2025a; Li, 2025). This universality encompasses practically significant scenarios such as traffic congestion, autonomous driving, smart grid management, and perturbed team games (Guo et al., 2025a). This structural alignment reduces the strategic search for Nash equilibria to a single-objective optimization task, thereby bypassing the PPAD-complete complexity inherent in generic general-sum games (Guo et al., 2025b; Li, 2025). 

> Preprint.
> arXiv:2605.00268v1 [cs.GT] 30 Apr 2026

In the offline setting, the difficulty of learning stable strategies is compounded by distribution shift, where the learned policies explore regions of the state-action space poorly represented in the historical logs (Levine et al., 2020). To establish sample-efficient recovery, standard methodologies typically rely on the principle of explicit pessimism (Jin et al., 2021; Zhang et al., 2023). These approaches utilize manually designed lower confidence bounds (LCB) or uncertainty bonuses to penalize values in regions with low data support (Cui and Du, 2022a; Zhong et al., 2022). While theoretically robust, these pessimistic mechanisms suffer from two primary drawbacks. First, they often require the construction of complex uncertainty quantifiers over joint action spaces, leading to significant computational overhead. Second, existing coverage conditions for these methods are typically anchored to an unknown optimal equilibrium, introducing a conceptual circularity—known as the "Curse of the Unknown Optimum"—that makes the assumptions difficult to verify in practice In this work, we establish that KL regularization relative to a fixed and known reference policy provides a standalone, pessimism-free mechanism for equilibrium learning in α-potential contextual bandits. To this end, we propose the Reference-Anchored coverage framework, a novel formulation of unilateral concentrability that resolves the conceptual circularity of prior work by anchoring data requirements to a verifiable reference policy rather than an unknown optimum. We prove that this regularized framework not only stabilizes learning but also leverages the inherent geometry of potential games to achieve superior statistical efficiency. Specifically, we show that decentralized regularized learning can achieve fast ˜O(1 /n ) statistical rates, bypassing the standard ˜O(1 /√n)

bottleneck found in generic multi-agent learning (Cui and Du, 2022a; Zhang et al., 2023). Our analysis centers on a unique geometric discovery: the KL-regularized Mirror Descent update forms an exact log-linear interpolation between the current iterate and the regularized best response. This structural identity allows us to recast the evaluation mismatch between best responses and the learning iterates as a vanishing optimization error; in contrast, traditional analyses are forced to treat this term as a fixed statistical bottleneck that prevents acceleration beyond the standard ˜O(1 /√n)

rate (Cui and Du, 2022a; Zhong et al., 2022). Consequently, we establish that first-order estimation errors cancel out within the decentralized product structure of potential-game iterates, enabling the accelerated ˜O(1 /n ) rate. Our key contributions are summarized as follows: • Reference-Anchored Coverage : We introduce a novel unilateral concentrability condition that anchors data requirements to a known reference policy rather than an unknown optimum. This formulation provides a verifiable and tractable framework for offline MARL, resolving the conceptual circularity of prior work. • Theoretical Fast-Rate Framework : We propose the Regularized Offline Potential Equi-librium (ROPE) algorithm to establish that a fast ˜O(1 /n ) statistical rate is achievable in regularized α-potential games. Our analysis demonstrates that the structural alignment of the potential function enables the cancellation of first-order errors, bypassing the ˜O(1 /√n)

bottleneck typical of generic general-sum games. • Decentralized Algorithmic Recovery : We develop Offline Potential Mirror Descent (OPMD), a computationally tractable algorithm that implements the regularized equilibria learning process through iterative self-play. We prove that OPMD preserves the ˜O(1 /n )

rate (up to the structural bias α) by exploiting the log-linear geometry of the Mirror Descent updates to resolve linear evaluation mismatch as a vanishing optimization error. 

## 2 Related Work 

Potential Games. The study of dynamic potential games generalizes the static framework to state-dependent environments, ensuring the guaranteed existence of pure Nash Equilibria and the global convergence of decentralized gradient play (Leonardos et al., 2021; Yan and Liu, 2025). To accom-modate the diverse structures of real-world strategic interactions, recent theoretical advancements have introduced the α-potential framework, which characterizes systems where unilateral deviations track potential changes up to a structural bias α ≥ 0 (Guo et al., 2025b; Li, 2025). Within this landscape, Guo et al. (2025b) established that the α-potential structure is universal, proving that any finite-state dynamic game can be characterized as an α-potential game. This characterization has been extended to stochastic differential games and constrained settings, with explicit characterizations of 

α in terms of player heterogeneity and interaction intensity (Guo et al., 2025b; Das and Eksin, 2024). 2Furthermore, Yan and Liu (2025) identified sufficient conditions on reward design and transition dynamics that allow for the systematic construction of potential games, with specific applications to autonomous driving and traffic management. While the α-potential framework reduces equilibrium search to a single-objective optimization problem, existing literature primarily focuses on the planning regime—where the optimizer has access to the potential landscape or best-response oracles (Guo et al., 2023; Das and Eksin, 2024)—or the online learning regime. In the latter, Ding et al. (2022) proposed independent policy gradient (IPG) algorithms for large-scale games, establishing iteration and sample complexity bounds that remain independent of the state-space size. Similarly, Li (2025) provided a comprehensive analysis of RL algorithms in α-potential games across both discrete and continuous time. However, these results are derived for the online setting, where agents benefit from active exploration and immediate environmental feedback. Despite the computational tractability afforded by the potential structure, the statistical properties of equilibrium learning in the offline regime—specifically under general function approximation and distribution shift—remain largely unexplored. Our work bridges this gap by demonstrating that the structural alignment of α-potential games can be exploited to achieve accelerated statistical rates in the offline setting without the need for explicit pessimistic mechanisms. 

General-Sum Games. The transition from single-agent reinforcement learning to multi-agent envi-ronments necessitates a shift from identifying value-optimal policies to recovering stable equilibria (Roughgarden, 2016; Cui and Du, 2022b). While single-agent objectives seek to maximize a unified return, multi-agent dynamics are characterized by individual strategic incentives, typically modeled through Coarse Correlated Equilibria (CCE) or Nash Equilibria (NE) (Daskalakis et al., 2009; Cui and Du, 2022a). Recovering a Nash Equilibrium is fundamentally challenging due to its PPAD-complete complexity in general-sum settings, which has historically led many methodologies to focus on the more computationally tractable CCE, where stability is established via no-regret learning or correlated strategies (Chen et al., 2009; Yan et al., 2024). However, the pursuit of Nash stability remains essential for decentralized applications requiring robust independent decision-making, where coordinated or correlated mechanisms are infeasible or strategically undesirable (Song et al., 2021; Cui and Du, 2022a). In the offline regime, the difficulty of equilibrium learning is compounded by the lack of interactive exploration and the presence of severe distribution shift (Levine et al., 2020; Jin et al., 2021). Standard methodologies typically address these gaps by adapting the principle of pessimism to the game-theoretic setting, utilizing uncertainty bonuses or lower confidence bounds (LCB) to penalize values in poorly explored joint-action regions (Zhong et al., 2022; Cui and Du, 2022a; Zhang et al., 2023). Despite their theoretical robustness, these pessimistic frameworks suffer from two primary bottlenecks. First, many existing approaches rely on complex uncertainty quantifiers over joint action spaces, leading to significant computational overhead unless restricted by specific unilateral concentrability conditions (Cui and Du, 2022a; Yan et al., 2024). Second, current algorithms that target Nash Equilibria often require centralized coordination or heavy iterative value updates that lack decentralized implementations capable of achieving accelerated statistical rates (Zhong et al., 2022; Zhang et al., 2023). Our work addresses these challenges by demonstrating that regularized decentralized learning can successfully recover Nash Equilibria at fast statistical rates without the need for explicit pessimistic bonuses, bypassing the computational and complexity barriers characteristic of traditional general-sum learning. 

## 3 Preliminaries 

We consider an m-player contextual bandit game possessing an α-potential structure. In this section, we formalize the environment, the regularized objectives, and the potential-based equilibrium concepts analyzed in this work. 

3.1 m-Player Contextual Bandits 

The game is defined by the tuple M = ( N , X , {A i}mi=1 , {r⋆i }mi=1 , ρ ), where N = [ m] is the set of players, X is the context space, and Ai is the finite action space for player i. In each round, a context 

x ∼ ρ is sampled. Players simultaneously select actions ai ∈ A i, forming a joint action profile 

a = ( a1, . . . , a m) ∈ A := Qmi=1 Ai. Each player i receives a reward r⋆i (x, a) ∈ [0 , 1] . A joint 3policy π : X → ∆( A) maps contexts to distributions over joint actions. In this work, we primarily focus on product policies, where π(a | x) = Qmi=1 πi(ai | x).

3.2 KL-Regularized Objectives 

KL regularization relative to a fixed reference policy has emerged as a fundamental mechanism for enforcing behavioral constraints and incorporating prior knowledge in multi-agent systems. In many practical scenarios, such as the alignment of large language models through Reinforcement Learning from Human Feedback, KL regularization is a functional requirement to prevent policy drift and ensure the model remains anchored to a trusted reference distribution (Munos et al., 2024). Within the offline setting, this regularization provides a dual benefit: it acts as a stabilizer against distribution shift and serves as an implicit pessimism mechanism that replaces the need for explicit, manually tuned pessimistic bonuses. For a fixed reference joint policy πref = ( πref 1 , . . . , π ref  

> m

) and a regularization coefficient η > 0, the regularized value function V πi for player i is defined as: 

V πi (x) := Ea∼π(·| x)[r⋆i (x, a)] − η−1KL  πi(· | x) ∥ πref  

> i

(· | x).

We denote the global regularized return as Ji(π) := Ex∼ρ[V πi (x)] . Each player i seeks to maximize their own regularized return given the strategies of their opponents π−i. The corresponding regularized best-response value is defined as V †,π −i 

> i

(x) := max π′ 

> i

V π′ 

> i,π −i
> i

(x).

3.3 The α-Potential Structure 

We leverage the framework of α-potential games to characterize the strategic interactions (Guo et al., 2025a; Li, 2025). A game is an α-potential game if there exists a global potential function 

Φ : Π → R such that for any player i, any joint policy π, and any unilateral deviation π′

> i

, the change in the individual return is tracked by the change in the potential function up to a structural bias α ≥ 0:

|(Ji(π′

> i

, π −i) − Ji(πi, π −i)) − (Φ( π′

> i

, π −i) − Φ( πi, π −i)) | ≤ α. 

This framework is universal, as any finite non-cooperative game can be characterized as an α-potential game for a sufficiently large α (Li, 2025; Guo et al., 2025b). In this work, we exploit this structure to reduce the search for equilibria to a potential maximization problem. 

3.4 α-Nash Equilibrium and Total Exploitability 

Our goal is to recover a joint policy π∗ that constitutes an α-Nash Equilibrium ( α-NE). Conceptually, an α-NE represents a state of near-stability where no player can unilaterally improve their regularized return by more than the structural bias α:

Ji(π∗ 

> i

, π ∗−i) ≥ sup 

> π′
> i

Ji(π′

> i

, π ∗−i) − α, ∀i ∈ [m].

For a learned policy ˆπ, we evaluate its performance using the NE Total Exploitability, or Nash Gap, which aggregates individual suboptimality to provide a scalar measure of the policy’s distance from equilibrium: 

Gap NE (ˆ π) := 

> m

X

> i=1

Ex∼ρ

h

V †,ˆπ−i 

> i

(x) − V ˆπi (x)

i

.

In this formulation, V †,ˆπ−i 

> i

(x) − V ˆπi (x) represents the maximum unilateral improvement available to player i given the fixed strategies of their opponents. The α-potential structure facilitates a reduction of this equilibrium search to a single-objective optimization problem (Guo et al., 2023). Formally, let ˆπ denote an ϵ-approximate maximizer of the global potential function Φ, satisfying: 

Φ(ˆ π) ≥ sup 

> π∈Π

Φ( π) − ϵ

for some optimization error ϵ ≥ 0. A core result in the characterization of α-potential games is that any such policy ˆπ is guaranteed to be an (ϵ + 2 α)-Nash Equilibrium of the underlying regularized game (Guo et al., 2025a). This structural alignment establishes potential optimization as a computationally tractable proxy for equilibrium learning, thereby bypassing the PPAD-complete complexity characteristic of generic general-sum interactions. 43.5 Offline Learning Model 

The learner has access to a static dataset D = {(xτ , aτ , rτ )}nτ =1 sampled from an empirical distri-bution μ(x, a). We assume access to function classes {Q i}mi=1 for reward estimation. Following standard literature, we assume realizability, i.e., r⋆i ∈ Q i. For any estimate ˆQi ∈ Q i, the pointwise regression error is Zi(x, a) := ˆQi(x, a) − r⋆i (x, a).

## 4 Data Coverage and Concentrability 

A fundamental challenge in offline reinforcement learning is the distribution shift between the logged dataset D and the target policy (Levine et al., 2020; Cui and Du, 2022a; Zhang et al., 2023). To establish sample-efficient recovery, one must assume the offline data provides sufficient coverage of the state-action regions relevant to the target objective. In this section, we review the landscape of concentrability assumptions in multi-agent systems and formalize our novel reference-anchored approach. 

4.1 From Single-Agent to Joint Concentrability 

In single-agent offline RL, sample efficiency is typically established under the assumption that the data distribution μ covers the optimal policy π∗ (Levine et al., 2020). 

Assumption 4.1 (Single-Agent Concentrability) . There exists a constant C∗ ≥ 1 such that 

ρ(x)π∗(a | x)

μ(x, a ) ∞

≤ C∗.

However, in multi-agent games, simply covering a single optimal profile is insufficient for learning a stable equilibrium (Cui and Du, 2022b). The most straightforward extension is joint concentrability ,which demands that the dataset support all possible simultaneous deviations by the agents. 

Assumption 4.2 (Joint Multi-Agent Concentrability) . There exists a constant Cjoint ≥ 1 such that for any joint policy π = Qmi=1 πi:

ρ(x)π(a | x)

μ(x, a) ∞

≤ Cjoint .

Here, the vector a denotes the joint action profile of all m players. This requirement scales with the product of action spaces ( Qmi=1 |A i|), resulting in an exponential growth in complexity known as the “curse of multiagents” (Cui and Du, 2022a). This scaling makes the assumption practically prohibitive for environments with many participants or a large action space. In contrast, Cui and Du (2022a) demonstrate that a strategy-wise approach can achieve linear scaling ( Pmi=1 |A i|), effectively breaking this curse by reducing the complexity to the sum of individual action spaces. 

4.2 Unilateral Concentrability and the Circularity Problem 

To bypass the exponential scaling of joint action spaces, recent works have adopted unilateral concentrability (Cui and Du, 2022a,b; Zhang et al., 2023). This principle requires that the dataset only needs to support scenarios where a single agent deviates from a target equilibrium strategy π∗

while all others remain stationary. 

Assumption 4.3 (Equilibrium-Anchored Unilateral Concentrability, Cui and Du (2022a)) . Let 

π∗ = ( π∗ 

> 1

, . . . , π ∗

> m

) be a target equilibrium policy. Let Πuni (π∗) := Smi=1 

 Πi × { π∗−i} be the set of unilateral deviations from π∗. There exists a constant C∗ 

> uni

≥ 1 such that for any π′ ∈ Πuni (π∗):

ρ(x)π′(a | x)

μ(x, a) ∞

≤ C∗

> uni

.

Cui and Du (2022a) demonstrated that unilateral concentration is a necessary assumption for learning a Nash Equilibrium (NE) in offline games. Crucially, Cui and Du (2022a) demonstrate that this strategy-wise approach allows the sample complexity to scale linearly with the sum of action spaces (Pmi=1 |A i|), effectively breaking the curse of multiagents. 5The Curse of the Unknown Optimum. Despite its improved scaling, classical unilateral concen-trability is anchored to an unknown optimal equilibrium policy π∗ (Cui and Du, 2022b; Zhang et al., 2023). This introduces a fundamental conceptual circularity: to define the data sufficiency required to find the equilibrium, one must already know the equilibrium itself. In general-sum settings, where equilibria are potentially non-unique and finding one is PPAD-complete (Daskalakis et al., 2009), this ambiguity makes the assumption theoretically ill-posed and practically unverifiable. 

4.3 Proposed: Reference-Anchored Unilateral Concentrability 

By utilizing KL regularization, we solve the circularity problem by anchoring the coverage require-ment to a fixed and known reference policy πref . This shifts the theoretical burden from an elusive optimal strategy to a verifiable anchor. 

Assumption 4.4 (Reference-Anchored Unilateral Concentrability) . Let Π = Qmi=1 Πi be the class of joint policies, and let πref = ( πref 1 , . . . , π ref  

> m

) be the fixed reference policy. We define the set of 

reference-anchored unilateral deviation policies Πref −uni ⊂ Π as the union of policies where exactly one player deviates to an arbitrary policy while all other players adhere to the reference policy: 

Πref −uni := 

> m

[

> i=1

 Πi × { πref  

> −i

} .

Let μ ∈ ∆( X × A ) denote the underlying joint context-action distribution of the offline dataset D.We assume there exists a constant Cuni ≥ 1 such that for any π′ ∈ Πref −uni , the density ratio is uniformly bounded: 

ρ(x)π′(a | x)

μ(x, a) ∞

≤ Cuni .

Advantages of Reference-Anchored Coverage. Assumption 4.4 offers two critical advantages over prior formulations: 1. Verifiability : Unlike π∗, the reference policy πref is known to the learner. It can be chosen as the behavior policy that generated the data or a previously validated iteration, allowing the coverage coefficient Cuni to be empirically estimated. 2. Tractability : By focusing on unilateral deviations, we maintain the Pmi=1 |A i| scaling derived in Cui and Du (2022a), effectively avoiding the exponential joint action space while providing a more robust stabilizer through KL anchoring. We further denote Cshift := exp( η(m − 1)) to quantify the density ratio shift between a learned policy and the reference policy across m − 1 opponents. Combined with Cuni , it characterizes the total statistical overhead for reference-anchored offline equilibrium learning. 

## 5 Regularized Offline Potential Equilibrium 

In this section, we establish the Regularized Offline Potential Equilibrium (ROPE) framework, which identifies α-Nash Equilibria in α-potential contextual bandits by solving a regularized empirical game. By incorporating KL regularization relative to a known reference policy, ROPE provides a mechanism for stable equilibrium recovery from offline data without relying on the manually tuned, explicit pessimistic bonuses often required in standard methodologies (Jin et al., 2021; Cui and Du, 2022a). The fundamental premise of the ROPE framework is to leverage the structural alignment between individual player utilities and the global potential function (Li, 2025; Guo et al., 2025a). In α-potential games, the search for a strategic equilibrium is computationally equivalent to a potential maximization task, where any approximate maximizer of the potential corresponds to an α-Nash Equilibrium (Guo et al., 2025a). 

5.1 The ROPE Framework 

The ROPE algorithm (Algorithm 1) operates through a two-stage process: statistical estimation followed by strategic computation. First, the learner utilizes the offline dataset D to construct 6empirical estimates of the reward functions for each player via regularized least-squares regression. These estimates serve as the foundation for the empirical game. Second, the algorithm computes a joint policy that constitutes an exact regularized Nash Equilibrium for this estimated environment. 

Algorithm 1 Regularized Offline Potential Equilibrium (ROPE)  

> 1:

Input: Offline dataset D, reference policies πref , regularization parameter η > 0, function classes {Q i}mi=1  

> 2:

for player i = 1 , . . . , m do  

> 3:

Empirical Estimation: Estimate the Q-function via regularized least-squares regression:  

> 4:

ˆQi ← argmin f ∈Q i

Pnτ =1 (f (xτ , aτ ) − rτ,i )2 

> 5:

end for  

> 6:

Equilibrium Computation: For each context x ∈ X , compute a joint product policy ˆπ(·| x) = Qmi=1 ˆπi(·| x) satisfying the exact regularized best-response condition for all i ∈ [m]: 

> 7:

ˆπi(·| x) ∈ argmax πi∈∆( Ai)

n

Eai∼πi,a−i∼ˆπ−i [ ˆQi(x, a)] − η−1KL( πi∥πref  

> i

)

o 

> 8:

for player i = 1 , . . . , m do  

> 9:

Value Update: Update the empirical value function for Player i: 

> 10:

ˆVi(x) ← Ea∼ˆπ [ ˆQi(x, a)] − η−1KL(ˆ πi∥πref  

> i

) 

> 11:

end for  

> 12:

Output: Learned joint policy ˆπ

Algorithmic Breakdown. The estimation phase in ROPE (Lines 2–5) relies on standard empirical risk minimization, where the complexity of the function classes {Q i}mi=1 and the dataset size n

govern the precision of the reward approximations (Cui and Du, 2022a; Zhang et al., 2023). The equilibrium computation phase (Lines 6–7) requires identifying a joint product policy ˆπ where every player’s strategy is a best response to the profile of their opponents within the empirical game. Crucially, the α-potential structure ensures that this fixed-point condition aligns with the global maximization of the latent potential function Φ (Li, 2025). In generic general-sum games, identifying such an equilibrium is generally PPAD-complete (Chen et al., 2009). However, the structural symmetry of the potential game, combined with the smoothing effect of the KL regularizer, ensures that this objective is well-posed and serves as a reliable proxy for recovery in the true environment. 

5.2 Statistical Guarantees 

By leveraging the Reference-Anchored Unilateral Concentrability framework (Assumption 4.4), we establish that the equilibrium identified by ROPE achieves superior statistical efficiency. The following theorem demonstrates that anchoring to a known reference policy allows the learner to exceed the standard statistical rates of generic multi-agent systems. 

Theorem 5.1. Under Assumption 4.4, and assuming the reward functions are realizable within 

{Q i}mi=1 , the estimated regularized joint policy ˆπ returned by ROPE achieves a Total Exploitability bounded by: 

Gap NE (ˆ π) ≤ eO

 mηC shift Cuni log |Q| 

n



.

The proof of Theorem 5.1 is provided in Appendix B.3.4. The accelerated eO(1 /n ) statistical rate is a consequence of the structural alignment between the learned equilibrium ˆπ and the evaluation trajectories of the best-response values. Specifically, within the per-iterate decomposition V †,ˆπ−i 

> i

−

V ˆπi = εpot ,i + δBR ,i + δIter ,i (Equation (1) in Appendix B), the linear parts of the evaluation bias 

δBR ,i and the iterate noise δIter ,i expand into expectations of the regression error Zi with opposite signs. Because ˆπ is a product policy, these evaluation distributions match perfectly, the linear Zi terms cancel exactly, and the potential ascent error εpot ,i vanishes by exact-NE optimality. By the Bregman bound on the regularized best-response gap (Lemma B.4, derived from Pinsker’s inequality and Fenchel duality), the remaining exploitability gap is dominated by a second-order squared residual. Under the unilateral data coverage guaranteed by Assumption 4.4, this residual scales with the in-sample squared regression error, which decays at the fast eO(1 /n ) rate. This result demonstrates 7that for environments with potential-aligned structures, solving the regularized empirical game is sufficient to bypass the standard eO(1 /√n) statistical bottleneck characteristic of generic offline learning. 

## 6 Offline Potential Mirror Descent 

While the ROPE framework (Algorithm 1) establishes that maximizing the latent potential function yields an α-Nash Equilibrium, it assumes access to an exact optimization oracle. In high-dimensional environments, explicitly constructing and optimizing the global potential Φ is often computationally prohibitive. To bridge this gap, we leverage the fact that in α-potential games, individual improve-ments naturally translate into global progress (Guo et al., 2023, 2025a). The proposed Algorithm 2 (OPMD) exploits this structure; it allows players to maximize their local utilities independently, serving as a practical, decentralized approach for maximizing the global potentia. 

6.1 The OPMD Algorithm 

The OPMD algorithm utilizes decentralized self-play to recover strategic equilibria without the need for a global solver. The process begins by estimating individual Q-functions via regularized least-squares regression over the offline dataset D. Following estimation, players enter an iterative inner loop where they perform local KL-regularized Mirror Descent updates based on the marginalized actions of their opponents. Upon completion, the algorithm returns an iterate ˜π = π(t∗) from the generated sequence. Because the game possesses an α-potential structure, the gradient of the global potential function Φ is composed of the individual player gradients. Consequently, when players independently update their strategies via Mirror Descent, the joint policy trajectory effectively performs decentralized ascent on the potential landscape. This structural alignment allows the algorithm to resolve the linear evaluation mismatch in δ(t)BR ,i + δ(t)Iter ,i between iterates and best responses as a vanishing optimization error, rather than a fixed statistical bottleneck, attaining the accelerated eO(1 /n ) statistical rate characterized in Theorem 6.1. 

Algorithm 2 Offline Potential Mirror Descent (OPMD)  

> 1:

Input: Offline dataset D, reference policies πref , regularization η > 0, function classes {Q i}mi=1 ,step size γ > 0, iterations T 

> 2:

for player i = 1 , . . . , m do  

> 3:

Empirical Estimation: Estimate Q-functions via regularized least-squares:  

> 4:

ˆQi ← argmin f ∈Q i

Pnτ =1 (f (xτ , aτ ) − rτ,i )2 

> 5:

Initialize base policy: π(1)  

> i

(·| x) = πref  

> i

(·| x) for all x ∈ X  

> 6:

end for  

> 7:

Decentralized Self-Play:  

> 8:

for t = 1 , . . . , T do  

> 9:

for player i = 1 , . . . , m do  

> 10:

Marginalized Value: Evaluate expected Q-value against opponents’ current policies:  

> 11:

¯Q(t) 

> i

(x, a i) = Ea−i∼π(t) 

> −i(·| x)

 ˆQi(x, a i, a−i) 

> 12:

Mirror Descent Update: Perform local KL-regularized policy improvement:  

> 13:

π(t+1)  

> i

(ai|x) ∝  πref  

> i

(ai|x) γη+γ



π(t) 

> i

(ai|x)

 ηη+γ

exp 

 ηγ η+γ ¯Q(t) 

> i

(x, a i)

 

> 14:

end for  

> 15:

end for  

> 16:

Output Selection: Sample an index t∗ ∼ Uniform (1 , . . . , T ) 

> 17:

Output: The product joint policy ˜π = π(t∗)

6.2 Statistical Guarantees 

By exploiting the inherent geometry of the Mirror Descent iterates, OPMD (Algorithm 2) achieves the fast statistical rate established for the ROPE framework, subject to the structural bias of the game. The 8expected suboptimality decomposes via V †,π (t) 

> −i
> i

− V π(t) 

> i

= ε(t)pot ,i + δ(t)BR ,i + δ(t)Iter ,i (potential-ascent residual, evaluation bias, and iterate noise; see Equation (1), Appendix B). 

Theorem 6.1. Under Assumption 4.4, and assuming reward realizability within {Q i}mi=1 , the policy 

˜π returned by OPMD satisfies 

Et∗ [Gap NE (˜ π)] ≤ eO

 m√m√T

| {z }

> optimization

+ mα |{z} 

> bias

+ eO

 mη C shift Cuni log |Q| 

> n

| {z }

> statistical

.

Consequently, for T ≥ n2, OPMD recovers an α-NE at the fast eO(1 /n ) statistical rate. 

6.3 Proof Sketch of Theorem 6.1 

The proof establishes that decentralized regularized updates successfully resolve the statistical mismatch bottleneck. We outline the core mechanism below; the complete derivation is provided in Appendix B. 

Per-Iterate Decomposition and Potential Ascent. We decompose the per-iterate gap into potential-ascent residual ε(t)pot ,i , evaluation bias δ(t)BR ,i , and iterate noise δ(t)Iter ,i (1) . By Lemma B.1, decentralized Mirror Descent updates monotonically increase the regularized potential ˆΦ, causing P 

> i

ε(t)pot ,i to decay at O(1 /√T ) toward the mα floor. 

Log-Linear Interpolation and Mismatch Resolution. Crucially, the update forms an exact log-linear interpolation between the current iterate and its regularized best response: 

π(t+1)  

> i

∝



π(t)

> i

 ηη+γ 

π†,(t)

> i

 γη+γ

.

Per Lemma B.2, this identity ensures the L1 distance to the optimum is proportional to the step distance ∥π(t) 

> i

− π(t+1)  

> i

∥1. Summing these steps over the iteration sequence recasts the linear evaluation mismatch as a vanishing O(1 /√T ) optimization error. 

Final Synthesis. The remaining δ(t)BR ,i +δ(t)Iter ,i contribution is dominated by second-order regression terms which decay at the fast O(1 /n ) rate under the unilateral coverage of Assumption 4.4. Setting 

T ≥ n2 ensures that computational optimization errors are dominated by the statistical limit. 

## 7 Conclusion 

This paper established that α-potential games allow for accelerated statistical efficiency in offline equilibrium learning through KL regularization. We introduced the ROPE framework (Algorithm 1) and the decentralized OPMD algorithm (Algorithm 2,) demonstrating that regularized Mirror Descent achieves a fast eO(1 /n ) statistical rate for α-Nash Equilibria (Theorem 5.1 and Theorem 6.1). By exploiting the geometric structure of potential-aligned product policies, our approach resolves the evaluation mismatch ( δ(t)BR ,i + δ(t)Iter ,i in our per-iterate decomposition) between iterates and best responses as a vanishing optimization error. This allows the algorithm to bypass the standard 

eO(1 /√n) statistical bottleneck characteristic of generic offline learning without requiring explicit pessimistic bonuses. Future research may extend these fast-rate guarantees to multi-step environments and explore the adversarial robustness of recovered equilibria under perturbations to the transition dynamics. Addi-tionally, investigating alternative divergences beyond the KL penalty may reveal broader classes of geometric identities for resolving distribution shift in complex strategic interactions. By establishing a pessimism-free foundation for fast-rate recovery, our work provides a statistically efficient path toward large-scale strategic decision-making in complex multi-agent environments. 9References 

Abdolmaleki, A., Springenberg, J. T., Tassa, Y., Munos, R., Heess, N., and Riedmiller, M. A. (2018). Maximum a posteriori policy optimisation. In Proceedings of the International Conference on Learning Representations . 13 Anonymous (2026). Pessimism-free offline learning in general-sum games via kl regularization. 

Concurrent submission to NeurIPS 2026 . 13, 14 Chen, X., Deng, X., and Teng, S.-H. (2009). Settling the complexity of computing two-player nash equilibria. Journal of the ACM (JACM) , 56(3):1–57. 1, 3, 7, 13 Cui, Q. and Du, S. S. (2022a). Provably efficient offline multi-agent reinforcement learning via strategy-wise bonus. Advances in Neural Information Processing Systems , 35:11739–11751. 2, 3, 5, 6, 7, 13 Cui, Q. and Du, S. S. (2022b). When are offline two-player zero-sum markov games solvable? 

Advances in Neural Information Processing Systems , 35:25779–25791. 3, 5, 6, 13 Das, S. and Eksin, C. (2024). Learning nash in constrained markov games with an α-potential. IEEE Control Systems Letters , 8:808–813. 2, 3 Daskalakis, C., Goldberg, P. W., and Papadimitriou, C. H. (2009). The complexity of computing a nash equilibrium. Communications of the ACM , 52(2):89–97. 1, 3, 6, 13 Ding, D., Wei, C.-Y., Zhang, K., and Jovanovic, M. (2022). Independent policy gradient for large-scale markov potential games: Sharper rates, function approximation, and game-agnostic convergence. In International Conference on Machine Learning , pages 5166–5220. PMLR. 3 Fujimoto, S., Meger, D., and Precup, D. (2019). Off-policy deep reinforcement learning without exploration. In Proceedings of the International Conference on Machine Learning . 13 Guo, X., Li, X., Maheshwari, C., Sastry, S., and Wu, M. (2023). Markov α-potential games. arXiv preprint arXiv:2305.12553 . 1, 3, 4, 8 Guo, X., Li, X., Maheshwari, C., Sastry, S., and Wu, M. (2025a). Markov α-potential games. IEEE Transactions on Automatic Control . 1, 4, 6, 8, 14 Guo, X., Li, X., and Zhang, Y. (2025b). An-potential game framework for-player dynamic games. 

SIAM Journal on Control and Optimization , 63(4):2964–3005. 1, 2, 4 Jin, Y., Yang, Z., and Wang, Z. (2021). Is pessimism provably efficient for offline rl? In International conference on machine learning , pages 5084–5096. PMLR. 2, 3, 6, 13 Kumar, A., Fu, J., Soh, M., Tucker, G., and Levine, S. (2019). Stabilizing off-policy Q-learning via bootstrapping error reduction. In Advances in Neural Information Processing Systems . 13 Lange, S., Gabel, T., and Riedmiller, M. (2012). Batch reinforcement learning. Reinforcement Learning: State-Of-The-Art . 13 Leonardos, S., Overman, W., Panageas, I., and Piliouras, G. (2021). Global convergence of multi-agent policy gradient in markov potential games. arXiv preprint arXiv:2106.01969 . 2 Levine, S., Kumar, A., Tucker, G., and Fu, J. (2020). Offline reinforcement learning: Tutorial, review, and perspectives on open problems. ArXiv Preprint . 2, 3, 5, 13 Li, G., Shi, L., Chen, Y., Chi, Y., and Wei, Y. (2024). Settling the sample complexity of model-based offline reinforcement learning. The Annals of Statistics , 52(1):233–260. 13 Li, X. (2025). An α-Potential Game Framework for Non-Cooperative Dynamic Games: Theory and Algorithms . University of California, Berkeley. 1, 2, 3, 4, 6, 7, 14 Liu, Y., Swaminathan, A., Agarwal, A., and Brunskill, E. (2020). Provably good batch off-policy reinforcement learning without great exploration. Advances in neural information processing systems , 33:1264–1274. 13 10 Munos, R., Valko, M., Calandriello, D., Azar, M. G., Rowland, M., Guo, Z. D., Tang, Y., Geist, M., Mesnard, T., Fiegel, C., et al. (2024). Nash learning from human feedback. In Forty-first International Conference on Machine Learning . 4, 13 Nayak, A., Yang, T., Yagan, O., Joshi, G., and Chi, Y. (2025). Achieving logarithmic regret in kl-regularized zero-sum markov games. arXiv preprint arXiv:2510.13060 . 13 Ouyang, L., Wu, J., Jiang, X., Almeida, D., Wainwright, C., Mishkin, P., Zhang, C., Agarwal, S., Slama, K., Ray, A., et al. (2022). Training language models to follow instructions with human feedback. In Advances in Neural Information Processing Systems . 13 Papadimitriou, C. H. (1994). On the complexity of the parity argument and other inefficient proofs of existence. Journal of Computer and system Sciences , 48(3):498–532. 13 Rafailov, R., Sharma, A., Mitchell, E., Manning, C. D., Ermon, S., and Finn, C. (2023). Direct preference optimization: Your language model is secretly a reward model. Advances in neural information processing systems , 36:53728–53741. 13 Rashidinejad, P., Zhu, B., Ma, C., Jiao, J., and Russell, S. (2021). Bridging offline reinforcement learning and imitation learning: A tale of pessimism. Advances in Neural Information Processing Systems , 34:11702–11716. 13 Roughgarden, T. (2016). Twenty lectures on algorithmic game theory . Cambridge University Press. 3 Schulman, J., Wolski, F., Dhariwal, P., Radford, A., and Klimov, O. (2017). Proximal policy optimization algorithms. ArXiv Preprint . 13 Shi, L., Li, G., Wei, Y., Chen, Y., and Chi, Y. (2022). Pessimistic q-learning for offline reinforcement learning: Towards optimal sample complexity. In International conference on machine learning ,pages 19967–20025. PMLR. 13 Song, Z., Mei, S., and Bai, Y. (2021). When can we learn general-sum markov games with a large number of players sample-efficiently? arXiv preprint arXiv:2110.04184 . 3 Uehara, M. and Sun, W. (2021). Pessimistic model-based offline reinforcement learning under partial coverage. arXiv preprint arXiv:2107.06226 . 13 Xie, T., Cheng, C.-A., Jiang, N., Mineiro, P., and Agarwal, A. (2021). Bellman-consistent pessimism for offline reinforcement learning. Advances in neural information processing systems , 34:6683– 6694. 13 Xie, T., Foster, D. J., Krishnamurthy, A., Rosset, C., Awadallah, A., and Rakhlin, A. (2024). Exploratory preference optimization: Harnessing implicit q*-approximation for sample-efficient rlhf. arXiv preprint arXiv:2405.21046 . 13 Xiong, W., Dong, H., Ye, C., Wang, Z., Zhong, H., Ji, H., Jiang, N., and Zhang, T. (2023). Iterative preference learning from human feedback: Bridging theory and practice for rlhf under kl-constraint. 

arXiv preprint arXiv:2312.11456 . 13 Yan, H. and Liu, M. (2025). Markov potential game construction and multi-agent reinforcement learning with applications to autonomous driving. In 2025 IEEE 64th Conference on Decision and Control (CDC) , pages 6838–6844. IEEE. 2, 3 Yan, Y., Li, G., Chen, Y., and Fan, J. (2024). Model-based reinforcement learning for offline zero-sum markov games. Operations research , 72(6):2430–2445. 3, 13 Ye, C., Xiong, W., Zhang, Y., Jiang, N., and Zhang, T. (2024). Online iterative reinforcement learning from human feedback with general preference model. 13, 24 Zhang, Y., Bai, Y., and Jiang, N. (2023). Offline learning in markov games with general function approximation. In International Conference on Machine Learning , pages 40804–40829. PMLR. 2, 3, 5, 6, 7, 13 Zhang, Y., Chen, C., and Jiang, N. (2026). Beyond pessimism: Offline learning in kl-regularized games. arXiv preprint arXiv:2604.06738 . 13 11 Zhang, Y., Yu, D., Peng, B., Song, L., Tian, Y., Huo, M., Jiang, N., Mi, H., and Yu, D. (2025). Iterative nash policy optimization: Aligning LLMs with general preferences via no-regret learning. In The Thirteenth International Conference on Learning Representations . 13 Zhao, H., Ye, C., Xiong, W., Gu, Q., and Zhang, T. (2025). Logarithmic regret for online kl-regularized reinforcement learning. arXiv preprint arXiv:2502.07460 . 13 Zhong, H., Xiong, W., Tan, J., Wang, L., Zhang, T., Wang, Z., and Yang, Z. (2022). Pessimistic mini-max value iteration: Provably efficient equilibrium learning from offline datasets. In International Conference on Machine Learning , pages 27117–27142. PMLR. 2, 3, 13 12 A Additional Related Work 

Foundations of Offline Reinforcement Learning. Offline reinforcement learning (RL) addresses the challenge of deriving optimal policies from static datasets without environment interaction (Lange et al., 2012; Levine et al., 2020). The central difficulty in this regime is the presence of distribution shift, where the target policy’s visitation frequency diverges from the logged behavior, often leading to divergent value estimates and catastrophic extrapolation errors (Fujimoto et al., 2019; Kumar et al., 2019). To establish sample-efficient guarantees, the literature has converged on the principle of 

pessimism , which penalizes value estimates in state-action regions lacking sufficient data support (Liu et al., 2020; Xie et al., 2021; Uehara and Sun, 2021). These conservative frameworks, often implemented via lower confidence bounds (LCB) or conservative value iteration, have been shown to achieve minimax optimality across a variety of linear and general function approximation settings (Rashidinejad et al., 2021; Jin et al., 2021; Li et al., 2024). 

Complexity and Learning in Multi-Agent Games. The transition from single-agent RL to multi-agent strategic environments significantly expands the computational and statistical complexity of the learning task. Finding a Nash Equilibrium (NE) in generic general-sum games is established as PPAD-complete, suggesting that equilibrium identification is fundamentally intractable for broad classes of interactions (Papadimitriou, 1994; Chen et al., 2009; Daskalakis et al., 2009). In the offline setting, this complexity is further compounded by the "curse of multiagents," where naive joint-action coverage requirements scale exponentially with the number of players (Cui and Du, 2022a,b). Recent advancements in offline Markov games have mitigated this by utilizing strategy-wise uncertainty bonuses and unilateral concentrability to recover stable equilibria such as Coarse Correlated Equilibria (CCE) and Nash Equilibria (Zhong et al., 2022; Zhang et al., 2023; Yan et al., 2024). Notably, while finding a Nash Equilibrium in generic general-sum games is well-established as PPAD-complete (Papadimitriou, 1994; Daskalakis et al., 2009; Chen et al., 2009), and identifying such equilibria from static datasets has been shown to be computationally prohibitive (Zhang et al., 2023; Anonymous, 2026), our work demonstrates that the α-potential structure provides a tractable alternative that bypasses these complexity barriers. 

Regularized Learning and LLM Alignment. KL regularization relative to a fixed reference policy has become a cornerstone of behavioral anchoring in reinforcement learning (Schulman et al., 2017; Xiong et al., 2023). By penalizing deviations from an initial distribution, this mechanism effectively stabilizes policy updates and prevents the "forgetting" or drift often seen in high-dimensional opti-mization (Abdolmaleki et al., 2018; Zhang et al., 2025). This approach is most prominently utilized in the alignment of Large Language Models (LLMs) via Reinforcement Learning from Human Feedback (RLHF), where a KL penalty ensures the model remains anchored to a trusted reference distribution (Ouyang et al., 2022; Rafailov et al., 2023; Munos et al., 2024). While regularized dynamics have been analyzed for single-agent tasks and zero-sum games (Xie et al., 2024; Ye et al., 2024; Zhao et al., 2025; Zhang et al., 2026), our work characterizes the efficacy of KL regularization for equilibrium learning in general-sum potential games, bypassing the need for explicit pessimistic bonuses. 

Statistical Efficiency and Fast Rates. The standard statistical rate for offline learning is typically characterized by eO(1 /√n) sample complexity (Jin et al., 2021; Shi et al., 2022). While recent investigations have identified accelerated eO(1 /n ) "fast rates" in specific settings, these results are restricted to single-agent problems or two-player zero-sum games that leverage skew-symmetry and minimax dynamics (Rashidinejad et al., 2021; Zhang et al., 2026; Nayak et al., 2025). In contrast, achieving such rates in general-sum settings is fundamentally more challenging due to the lack of competitive cancellation between players. By exploiting the geometric properties of the KL-regularized potential function and the log-linear interpolation of Mirror Descent iterates, we establish that the fast eO(1 /n ) rate is achievable for α-Nash equilibria, providing a new benchmark for statistical efficiency in multi-agent strategic decision-making. 

## B Proof 

The appendix is organized around three structural ingredients of our analysis. Section B.1 isolates how the α-potential landscape turns decentralized mirror descent into approximate ascent on the 13 global potential. Section B.2 develops the log-linear interpolation identity that bounds the iterate-to-best-response distance as a pure optimization quantity. Section B.3 combines these ingredients with a Bregman bound on the regularized best-response gap, a bias-cancellation identity, and a single concentrability-shift argument to close both Theorem 5.1 and Theorem 6.1. 

Per-iterate decomposition. Throughout the appendix we write the per-iterate gap of player i as a sum of three named quantities: 

V †,ˆπ−i 

> i

(x) − V ˆπi (x) = εpot ,i (x) + δBR ,i (x) + δIter ,i (x), (1) where, using the shorthand V † 

> i

≡ V †,ˆπ−i 

> i

and ˆV † 

> i

≡ ˆV †,ˆπ−i 

> i

,

εpot ,i (x) := ˆV † 

> i

(x) − ˆV ˆπi (x), (potential ascent error) 

δBR ,i (x) := V † 

> i

(x) − ˆV † 

> i

(x), (evaluation bias of best response) 

δIter ,i (x) := ˆV ˆπi (x) − V ˆπi (x). (iterate noise) Identity (1) is verified by adding and subtracting ˆV † 

> i

(x) and ˆV ˆπi (x) in the original gap: 

V † 

> i

(x) − V ˆπi (x) =  V † 

> i

(x) − ˆV † 

> i

(x) +   ˆV † 

> i

(x) − ˆV ˆπi (x) +   ˆV ˆπi (x) − V ˆπi (x)

= δBR ,i (x) + εpot ,i (x) + δIter ,i (x),

which matches the algebraic decomposition appearing in the offline-RL literature (cf. Anonymous (2026)). The potential ascent error εpot ,i is non-positive whenever ˆπ is an exact regularized empirical equilibrium of the estimated game (the ROPE setting); in the OPMD setting it is tracked by mirror-descent ascent on the empirical potential ˆΦ (Section B.1). The evaluation bias and the iterate noise are statistical quantities that we control through the regression error 

Zi(x, a) := ˆQi(x, a) − r⋆i (x, a).

For the OPMD analysis we attach an iteration index and write ε(t)pot ,i , δ (t)BR ,i , δ (t)Iter ,i .

Marginalization shorthand. For any opponent profile π−i and player i, define the marginalized true and empirical Q-values as 

Qπ−i 

> i

(x, a i) := Ea−i∼π−i(·| x)

r⋆i (x, a i, a−i),

ˆQi(x, a i, π −i) := Ea−i∼π−i(·| x)

 ˆQi(x, a i, a−i).

Both are functions of ai alone after marginalization, and the marginalized regression error inherits the same convention, Zi(x, a i, π −i) := Ea−i∼π−i [Zi(x, a i, a−i)] .

B.1 Structural Bias of the Potential Landscape 

In a generic general-sum game, bounding the empirical optimization residual P 

> i

ε(t)pot ,i requires a correlated-equilibrium argument and pays an eO(1 /√n) statistical price. The α-potential structure short-circuits this: by definition, every unilateral improvement in player i’s expected return ˆJi(π) := 

Eρ[ ˆV πi (x)] tracks a corresponding improvement in the global empirical potential ˆΦ, up to an additive bias of order α. We first record the gradient-form characterization of this bias, then show that independent KL-regularized mirror descent therefore performs decentralized ascent on ˆΦ without ever materializing a correlated policy. 

Gradient-form α-bias. The function-value α-bias from Section 3 transfers, on the polytope of product policies, to the gradient: for each player i and each iterate π(t) there exists a residual vector 

et,i ∈ R|X |·|A i| with 

∇πi

ˆΦ( π(t)) = ∇πi

ˆJi(π(t)) + et,i , ∥et,i ∥∞ ≤ α, (2) where ∥ · ∥ ∞ is dual to the per-player ∥πi∥1 used to measure policy distances; see Guo et al. (2025a); Li (2025). Because each ˆJi is multilinear in π with rewards bounded by 1, ˆJi is 1-Lipschitz in πi

14 w.r.t. ∥ · ∥ 1 and 1-smooth across player coordinates. By (2) , ˆΦ inherits the smoothness of P 

> i

ˆJi up to the α-bias, with smoothness constant LΦ = O(m) in the joint L1 norm: 

ˆΦ( π′) ≥ ˆΦ( π) + 

> m

X

> i=1

⟨∇ πi

ˆΦ( π), π ′ 

> i

− πi⟩ − LΦ

2

> m

X

> i=1

∥π′ 

> i

− πi∥21. (3) 

Lemma B.1 (Independent mirror descent is approximate potential ascent) . Assume the empirical game is an α-potential contextual bandit and that every player i ∈ [m] runs KL-regularized online mirror descent with constant stepsize γ ≤ 1/L Φ in Algorithm 2. Then the average per-iterate empirical optimization error satisfies 

1

T

> T

X

> t=1
> m

X

> i=1

Ex∼ρ

ε(t)pot ,i (x) ≤ O 

 m

√T



+ mα, (4) 

and the consecutive-iterate L1 steps satisfy 

> T

X

> t=1
> m

X

> i=1

π(t+1)  

> i

− π(t) 

> i
> 21

≤ O (m). (5) 

Proof. Throughout, ˆΦunreg (π) := Ex∼ρ, a∼π(·| x)[ˆ r(x, a)] denotes the unregularized empirical poten-tial: it is multilinear in the per-player marginals πi with rewards in [0 , 1] , hence globally LΦ-smooth in ∥ · ∥ 1 with LΦ = O(m) (cf. (3)). The regularized empirical potential is 

ˆΦreg (π) := ˆΦunreg (π) − 1

> ηm

X

> i=1

KL( πi∥πref  

> i

), (6) and the gradient-form α-bias (2) reads ∇πi

ˆΦunreg (π(t)) = ¯Q(t) 

> i

+ et,i with ∥et,i ∥∞ ≤ α, where 

¯Q(t) 

> i

(x, a i) is the marginalized empirical Q-value of Algorithm 2 (line 9) — the gradient of the 

unregularized empirical payoff in πi.

Step 1 — Unregularized descent and bridge to the regularized potential. Apply (3) to ˆΦunreg between 

π(t) → π(t+1) and split the linear term using (2): 

ˆΦunreg (π(t+1) ) − ˆΦunreg (π(t)) ≥

> m

X

> i=1

⟨ ¯Q(t) 

> i

, π (t+1)  

> i

− π(t) 

> i

⟩ − α

> m

X

> i=1

∥π(t+1)  

> i

− π(t) 

> i

∥1

− LΦ

> 2
> m

X

> i=1

∥π(t+1)  

> i

− π(t) 

> i

∥21. (7) By (6), the regularized potential change differs from (7) only by the reference-KL telescope: 

ˆΦreg (π(t+1) ) − ˆΦreg (π(t)) =  ˆΦunreg (π(t+1) ) − ˆΦunreg (π(t)) − 1

> ηm

X

> i=1

KL( π(t+1)  

> i

∥πref  

> i

) − KL( π(t) 

> i

∥πref  

> i

).

(8) The OPMD update of Algorithm 2 is the reference-anchored proximal step 

π(t+1)  

> i

= arg max 

> p∈∆( Ai)X

F (t) 

> i

(p), F (t) 

> i

(p) := ⟨ ¯Q(t) 

> i

, p ⟩ − 1 

> η

KL( p∥πref  

> i

) − 1 

> γ

KL( p∥π(t) 

> i

),

whose KKT conditions reproduce the closed form (12) . Optimality of π(t+1)  

> i

versus the trivial comparator π(t) 

> i

(so KL( π(t) 

> i

∥π(t) 

> i

) = 0 ) gives F (t) 

> i

(π(t+1)  

> i

) ≥ F (t) 

> i

(π(t) 

> i

), i.e., 

⟨ ¯Q(t) 

> i

, π (t+1)  

> i

− π(t) 

> i

⟩ ≥ 1

> η

KL( π(t+1)  

> i

∥πref  

> i

) − KL( π(t) 

> i

∥πref  

> i

) + 1 

> γ

KL( π(t+1)  

> i

∥π(t) 

> i

). (9) Substituting (9) into (7) and combining with the bridge (8) , the reference-KL telescopes cancel 

exactly between the prox-optimality bound and the regularizer change, leaving 

ˆΦreg (π(t+1) ) − ˆΦreg (π(t)) ≥ 1

> γm

X

> i=1

KL( π(t+1)  

> i

∥π(t) 

> i

) − α

> m

X

> i=1

∥π(t+1)  

> i

− π(t) 

> i

∥1

− LΦ

> 2
> m

X

> i=1

∥π(t+1)  

> i

− π(t) 

> i

∥21.

15 By Pinsker, 1 

> γ

KL( π(t+1)  

> i

∥π(t) 

> i

) ≥ 12γ ∥π(t+1)  

> i

− π(t) 

> i

∥21, and choosing γ ≤ 1/(2 LΦ) gives 12γ ≥ LΦ,so the proximal KL absorbs the smoothness penalty with residual LΦ/2:

ˆΦreg (π(t+1) ) − ˆΦreg (π(t)) ≥ LΦ

> 2
> m

X

> i=1

∥π(t+1)  

> i

− π(t) 

> i

∥21 − α

> m

X

> i=1

∥π(t+1)  

> i

− π(t) 

> i

∥1. (10) We deliberately use the trivial comparator u = π(t) 

> i

in (9) , to avoid introducing the moving comparator 

π†,(t) 

> i

(whose dependence on π(t) 

> −i

would prevent terms such as KL( π†,(t) 

> i

∥π(t+1)  

> i

) from telescoping across t). The optimization rate on ε(t)pot ,i is recovered structurally in Steps 3–4 below, with no comparator KL needed. 

Step 2 — Squared-step bound (5) . Telescoping (10) over t = 1 , . . . , T and using | ˆΦreg (π(T +1) ) −

ˆΦreg (π(1) )| ≤ O (m) (from r⋆i ∈ [0 , 1] and the uniform bound η−1KL( π(t) 

> i

∥πref  

> i

) ≤ 1, which holds because every iterate has the bounded-logit Gibbs form π(t) 

> i

∝ πref  

> i

exp( ηg ) with g ∈ [0 , 1] ), 

> LΦ
> 2
> T

X

> t=1
> m

X

> i=1

∥π(t+1)  

> i

− π(t) 

> i

∥21 ≤ O (m) + α

> T

X

> t=1
> m

X

> i=1

∥π(t+1)  

> i

− π(t) 

> i

∥1.

AM–GM bounds the α-term by LΦ

> 4

P 

> t,i

∥∆π∥21 + T mα 2/L Φ; absorbing the squared part into the LHS, LΦ

> 4

P 

> t,i

∥π(t+1)  

> i

− π(t) 

> i

∥21 ≤ O (m) + T mα 2/L Φ, and with LΦ = O(m) this gives (5) (the 

mα floor is absorbed into the final theorem’s eO). 

Step 3 — Structural identity for the potential ascent error. The variational form of the regularized best response gives, pointwise in x,

ˆV †,(t) 

> i

(x) − ˆV (t) 

> i

(x) = 1 

> η

KL  π(t) 

> i

(·| x) π†,(t) 

> i

(·| x), (11) i.e., ε(t)pot ,i (x) = η−1KL( π(t) 

> i

∥π†,(t) 

> i

). Indeed, ˆV †,(t) 

> i

= ⟨π†,(t) 

> i

, ¯Q(t) 

> i

⟩ − η−1KL( π†,(t) 

> i

∥πref  

> i

) and 

ˆV (t) 

> i

= ⟨π(t) 

> i

, ¯Q(t) 

> i

⟩ − η−1KL( π(t) 

> i

∥πref  

> i

); substituting ¯Q(t) 

> i

= η−1 log( π†,(t) 

> i

/π ref  

> i

) + η−1 log Z†,(t)

> i

and cancelling the constant via P

> a

(π†,(t) 

> i

− π(t) 

> i

) = 0 yields (11) . This identity is comparator-free at each t; no telescoping argument is required. Step 4 — KL-to-L1 via bounded logits, then chaining with Lemma B.2. Both π(t) 

> i

and π†,(t) 

> i

have the Gibbs form π ∝ πref  

> i

exp( ηg ) with g ∈ [0 , 1] (by induction from (12) for π(t) 

> i

, and with g = ¯Q(t) 

> i

for 

π†,(t) 

> i

), so ∥ log( π(t) 

> i

/π †,(t) 

> i

)∥∞ ≤ 2η. By Hölder, 

KL( π(t) 

> i

∥π†,(t) 

> i

) = X

> a

 π(t) 

> i

(a) − π†,(t) 

> i

(a) log π(t)   

> i(a)
> π†,(t)
> i(a)

− KL( π†,(t) 

> i

∥π(t) 

> i

)

≤ π(t) 

> i

− π†,(t)  

> i1

log( π(t) 

> i

/π †,(t) 

> i

) ∞ ≤ 2η π(t) 

> i

− π†,(t) 

> i1

.

Combining with (11), 

ε(t)pot ,i (x) ≤ 2 π(t) 

> i

(·| x) − π†,(t) 

> i

(·| x) 1.

By Lemma B.2, ∥π(t) 

> i

− π†,(t) 

> i

∥1 ≤ 4( η+γ) 

> νmin γ

∥π(t) 

> i

− π(t+1)  

> i

∥1, so 

ε(t)pot ,i (x) ≤ 8( η + γ)

νmin γ π(t) 

> i

(·| x) − π(t+1)  

> i

(·| x) 1.

Averaging over (t, i ) ∈ [T ]×[m], applying Cauchy–Schwarz across the T m summands, and invoking the squared-step bound (5), 

1

T

> T

X

> t=1
> m

X

> i=1

Eρ

ε(t)pot ,i 

 ≤ 8( η + γ)

νmin γ T 

√T m 

sX

> t,i

Eρ

∥π(t) 

> i

− π(t+1)  

> i

∥21



> (5)

≤ 8( η + γ)

νmin γ

r m · O (m)

T = O

 m

√T



,

which is (4) (the mα floor inherited from Step 2 enters additively). 16 For the idealized ROPE algorithm, ˆπ is an exact regularized NE of the empirical game, so every 

ˆπi coincides with its empirical best response, KL(ˆ π† 

> i

∥ˆπi) = 0 , and εpot ,i ≤ 0 pointwise. The decomposition (1) then collapses to P

> i

(δBR ,i + δIter ,i ), the regime analyzed in Section B.3. 

B.2 Geometric Stability of Mirror Descent 

The OPMD update in Algorithm 2 is, in fact, a closed-form log-linear interpolation between the current iterate and the regularized empirical best response. Recall the explicit form 

π(t+1)  

> i

(ai|x) ∝  πref  

> i

(ai|x) γη+γ  π(t) 

> i

(ai|x) ηη+γ exp 

 ηγ η+γ ¯Q(t) 

> i

(x, a i)



, (12) and the regularized empirical best response π†,(t) 

> i

(ai|x) ∝ πref  

> i

(ai|x) exp( η ¯Q(t) 

> i

(x, a i)) , equiva-lently exp( η ¯Q(t) 

> i

(x, a i)) ∝ π†,(t) 

> i

(ai|x)/π ref  

> i

(ai|x). Substituting the latter into (12), 

π(t+1)  

> i

(ai|x) ∝  πref  

> i

(ai|x) γη+γ  π(t) 

> i

(ai|x) ηη+γ  π†,(t) 

> i

(ai|x)/π ref  

> i

(ai|x) γη+γ

=  π(t) 

> i

(ai|x) ηη+γ  π†,(t) 

> i

(ai|x) γη+γ . (13) Equivalently, writing θ(t) 

> i

(x, ·) := log π(t) 

> i

(·| x) − log Z(t) 

> i

(x) and θ†,(t) 

> i

(x, ·) := log π†,(t) 

> i

(·| x) −

log Z†,(t) 

> i

(x) for the centered logits (with Z(t) 

> i

, Z †,(t) 

> i

the normalizing partition functions) and β := 

γ/ (η + γ) ∈ (0 , 1) for the mixing weight, equation (13) is the affine identity 

θ(t+1)  

> i

(x, ·) = (1 − β) θ(t) 

> i

(x, ·) + β θ †,(t) 

> i

(x, ·), ⇐⇒ θ(t) 

> i

− θ†,(t) 

> i

= 1

> β

 θ(t) 

> i

− θ(t+1) 

> i

.(14) We exploit this geometry to prove that the iterate-to-best-response distance is controlled by the consecutive-iterate step length, and hence by the squared-step bound (5). 

Lemma B.2 (L1 proportionality of the regularized log-linear interpolation) . Let νmin := min i,x,a πref  

> i

(a|x) > 0 and assume every iterate π(t) 

> i

inherits the same lower bound. For the interpolation (13) with mixing weight β = γ/ (η + γ) ∈ (0 , 1) ,

π(t) 

> i

(·| x) − π†,(t) 

> i

(·| x) 1 ≤ 4

νmin 

 η + γγ



π(t) 

> i

(·| x) − π(t+1)  

> i

(·| x) 1. (15) 

Proof. We use two Lipschitz facts about the softmax map between centered-logit space and the 

νmin -interior ∆νmin (Ai) := {p ∈ ∆( Ai) : pa ≥ νmin ∀a} of the simplex. 

Step 1 — Forward Lipschitz constant (logit → probability). Let p, p ′ ∈ ∆( Ai) correspond to centered logits θ, θ ′ ∈ R|A i| via pa = exp( θa)/ P 

> b

exp( θb). Define θs := (1 − s)θ + sθ ′ for s ∈ [0 , 1] and 

ps := softmax( θs). The Hessian of the log-partition function Φ( θ) = log P 

> a

exp( θa) is 

∇2Φ( θs) = diag( ps) − psp⊤ 

> s

,

so that dds ps = (diag( ps) − psp⊤ 

> s

)( θ′ − θ). By the mean-value theorem and the triangle inequality, 

∥p − p′∥1 =

Z 10

 diag( ps) − psp⊤

> s

(θ′ − θ) ds 1

≤

Z 10

h X 

> a

ps(a) |(θ′ − θ)a| + P 

> b

ps(b) ( θ′ − θ)b

i

ds 

≤ 2 ∥θ′ − θ∥∞, (16) where in the last step we bound each of the two terms by ∥θ′ − θ∥∞ using P 

> a

ps(a) = 1 .

Step 2 — Backward Lipschitz constant (probability → logit). For p, p ′ ∈ ∆νmin (Ai), the centered logits satisfy θa = log pa − 1

> |A i|

P 

> b

log pb. By the mean-value theorem applied to the function log 

on [νmin , 1] , | log pa − log p′

> a

| ≤ 1 

> νmin

|pa − p′

> a

| for every a, so 

∥θ − θ′∥∞ ≤ max  

> a

| log pa − log p′

> a

| + 1

> |A i|

X

> b

| log pb − log p′

> b

|≤ 1 

> νmin

max  

> a

|pa − p′

> a

| + 1 

> νmin |A i|

X

> b

|pb − p′

> b

| ≤ 1+1 /|A i| 

> νmin

∥p − p′∥1 ≤ 2 

> νmin

∥p − p′∥1,

(17) 17 Step 3 — Combining the two bounds. Apply (16) to the pair (π(t) 

> i

(·| x), π †,(t) 

> i

(·| x)) with logits 

(θ(t) 

> i

, θ †,(t) 

> i

), and (17) to the pair (π(t) 

> i

(·| x), π (t+1)  

> i

(·| x)) with logits (θ(t) 

> i

, θ (t+1)  

> i

): 

> 12

π(t) 

> i

− π†,(t) 

> i1
> (16)

≤ θ(t) 

> i

− θ†,(t) 

> i∞
> (14)

= 1 

> β

θ(t) 

> i

− θ(t+1)  

> i∞
> (17)

≤ 2 

> β ν min

π(t) 

> i

− π(t+1)   

> i1

= 2

> νmin

 η+γγ



π(t) 

> i

− π(t+1)  

> i1

.

Multiplying by 2 yields the claimed inequality. The geometric stability of the iterates translates the squared-step bound (5) into a uniform optimization rate on the iterate-to-best-response distance. 

Corollary B.3 (Optimization rate for the iterate-to-best-response L1 distance) . Under the assumptions of Lemma B.1 and Lemma B.2, with Cν := 4 /ν min and t∗ ∼ Unif([ T ]) ,

Et∗

h mX

> i=1

Ex∼ρ

 π(t∗) 

> i

(·| x) − π†,(t∗) 

> i

(·| x) 1

i

≤ Cν

η + γγ O

 m

√T



. (18) 

Proof. Apply Lemma B.2 pointwise in x and average over x ∼ ρ and t∗:

Et∗

h mX

> i=1

Eρ

∥π(t∗) 

> i

− π†,(t∗) 

> i

∥1

i

≤ Cν

2

η + γγ · 1

T

> T

X

> t=1
> m

X

> i=1

Eρ

∥π(t) 

> i

− π(t+1)  

> i

∥1

.

By Cauchy–Schwarz across the index pair (t, i ) ∈ [T ] × [m] and Jensen’s inequality on Eρ[ · ],

1

T

> T

X

> t=1
> m

X

> i=1

Eρ

∥π(t) 

> i

− π(t+1)  

> i

∥1

 ≤ 1

T

√T m 

sX

> t,i

Eρ

∥π(t) 

> i

− π(t+1)  

> i

∥21



> (5)

≤

r m · O (m)

T = O

 m

√T



,

which combined with the previous display yields (18). Treating η, γ, ν min as problem-dependent constants, the iterate-to-best-response L1 distance averages to eO(m/ √T ) — a pure optimization rate. This is the geometric mechanism that lets OPMD bypass the eO(1 /√n) Pinsker bottleneck of generic general-sum analyses: the linear evaluation mismatch between π(t) 

> i

and π†,(t) 

> i

(Section B.3.2) decays purely with the optimization horizon T , never with the dataset size n.

B.3 Synthesis of Statistical and Potential Rates 

This section proves the two main theorems. We first develop three reusable ingredients: a Breg-man bound on the regularized best-response gap (Section B.3.1), a bias-cancellation identity for 

δBR ,i + δIter ,i under product policies (Section B.3.2), and a single concentrability-shift argument (Section B.3.3). The two theorems are then short corollaries (Sections B.3.4 and B.3.5). 

B.3.1 Bregman bound on the regularized best-response gap 

The textbook claim that the log-partition function Φ is 1-smooth w.r.t. ∥ · ∥ ∞ is itself a consequence of Pinsker’s inequality (the strong convexity of the negative entropy in ∥ · ∥ 1) under Fenchel duality. We prove this from first principles, framed via Bregman divergences, and apply the resulting bound to the unilateral value gap. 

Lemma B.4 (Bregman bound on the regularized best-response gap) . Let Φ( θ) := log P 

> a∈A i

exp( θa)

denote the log-partition function. Then Φ is convex and 1-smooth with respect to ∥ · ∥ ∞:

DΦ(θ′ ∥ θ) := Φ( θ′) − Φ( θ) − ⟨∇ Φ( θ), θ ′ − θ⟩ ≤ 12 ∥θ′ − θ∥2

> ∞

. (19) 18 Consequently, with the natural-parameter logits θ† 

> i

(x, ·) := η Q ˆπ−i 

> i

(x, ·) + log πref ,i (·| x) and 

ˆθi(x, ·) := η ˆQi(x, ·, ˆπ−i) + log πref ,i (·| x), and the empirical regularized best response ˆπ† 

> i

(·| x) := softmax( ˆθi(x, ·)) , the unilateral best-response value gap ∆i(x) := V †,ˆπ−i 

> i

(x) − ˆV †,ˆπ−i 

> i

(x) satisfies 

∆i(x) ≤ Eai∼ˆπ† 

> i(·| x)

h

Qˆπ−i 

> i

(x, a i) − ˆQi(x, a i, ˆπ−i)

i

+ η 

> 2

Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i) 2

> ∞

.(20) 

The expectation is taken under ˆπ† 

> i

, the regularized best response to ˆπ−i on the empirical game, which equals the iterate ˆπi only when ˆπ is an exact regularized NE of the empirical game (as in ROPE); for OPMD iterates the two differ in general. Proof. Step 1 — Negative entropy is 1-strongly convex w.r.t. ∥ · ∥ 1 on ∆( Ai). Let H(p) := P 

> a

pa log pa. On the relative interior of the simplex ∇H(p)a = 1 + log pa, so its Bregman di-vergence reduces to the KL divergence, DH (p∥q) = P 

> a

pa log( pa/q a) = KL( p∥q). Pinsker’s inequality KL( p∥q) ≥ 12 ∥p − q∥21 is therefore exactly the statement that H is 1-strongly convex in 

∥ · ∥ 1:

H(p) ≥ H(q) + ⟨∇ H(q), p − q⟩ + 12 ∥p − q∥21.

Step 2 — Conjugate duality identifies Φ as the dually-smooth potential. The convex conjugate of H

on ∆( Ai) is the log-partition function: 

H∗(θ) = sup 

> p∈∆( Ai)

⟨θ, p ⟩ − H(p) = log X

> a

exp( θa) = Φ( θ),

with maximizer p⋆(θ) = softmax( θ) = ∇Φ( θ). By the conjugate duality between strong convexity and smoothness ( ?), if H is 1-strongly convex w.r.t. ∥ · ∥ 1, then Φ = H∗ is 1-smooth w.r.t. the dual norm ∥ · ∥ ∞:

Φ( θ′) ≤ Φ( θ) + ⟨∇ Φ( θ), θ ′ − θ⟩ + 12 ∥θ′ − θ∥2

> ∞

,

which is exactly (19). 

Step 3 — Translating the bound to value space. By the closed form of the regularized best response, 

V †,ˆπ−i 

> i

(x) = η−1Φ( θ† 

> i

) and ˆV †,ˆπ−i 

> i

(x) = η−1Φ( ˆθi), with θ† 

> i

− ˆθi = η Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i)

and ∇Φ( ˆθi) = softmax( ˆθi)( ·) = ˆ π† 

> i

(·| x) — the empirical regularized best response to ˆπ−i, not the iterate ˆπi. Applying (19) to (θ† 

> i

, ˆθi) and dividing by η yields (20). 

B.3.2 Bias cancellation under product policies 

We use Lemma B.4 to convert the linear part of δBR ,i into an expectation of the regression error Zi,and combine it with the on-policy expansion of δIter ,i to expose the algebraic cancellation that occurs under product policies. 

Pointwise expansion of the marginalized Q-difference. For any opponent profile π−i and any action ai, the marginalized true and empirical Q-values satisfy 

Qπ−i 

> i

(x, a i) − ˆQi(x, a i, π −i) = Ea−i∼π−i(·| x)

r⋆i (x, a i, a−i) − ˆQi(x, a i, a−i)

= Ea−i∼π−i(·| x)

 − Z i(x, a i, a−i) =: − Z i(x, a i, π −i). (21) 

Pointwise expansion of δBR ,i . Substituting (21) into the linear part of the Bregman bound (20), 

δBR ,i (x) ≤ Eai∼ˆπ† 

> i(·| x)

h

Qˆπ−i 

> i

(x, a i) − ˆQi(x, a i, ˆπ−i)

i

+ η 

> 2

Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i) 2

> ∞
> (21)

= Eai∼ˆπ† 

> i(·| x)

h

Ea−i∼ˆπ−i(·| x)

 − Z i(x, a i, a−i)i

+ η 

> 2

Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i) 2

> ∞

. (22) Because ˆπ = (ˆ πi, ˆπ−i) is a product policy in both algorithms we analyze, the nested expectation collapses into a joint expectation under the product profile (ˆ π† 

> i

, ˆπ−i):

Eai∼ˆπ†

> i

h

Ea−i∼ˆπ−i

 − Z i(x, a i, a−i)i

= E(ai,a−i)∼(ˆ π† 

> i,ˆπ−i)

 − Z i(x, a i, a−i).

19 Pointwise expansion of δIter ,i . The KL-regularized value functions satisfy V πi (x) =

Ea∼π [r⋆i (x, a)] − η−1 KL( πi∥πref  

> i

) and ˆV πi (x) = Ea∼π [ ˆQi(x, a)] − η−1 KL( πi∥πref  

> i

), so the en-tropy regularizers cancel in δIter ,i and the iterate noise unrolls into a pure expectation of Zi under 

ˆπ:

δIter ,i (x) = ˆV ˆπi (x) − V ˆπi (x)= Ea∼ˆπ(·| x)

 ˆQi(x, a) − r⋆i (x, a)

= Ea∼ˆπ(·| x)

Zi(x, a). (23) 

Combining the two terms. Summing (22)–(23) pointwise and integrating over x ∼ ρ,

Ex∼ρ

δBR ,i (x) + δIter ,i (x) ≤ Ex∼ρ

h

E(ai,a−i)∼(ˆ π† 

> i,ˆπ−i)

[−Z i] + Ea∼ˆπ [Zi]

i

+ η 

> 2

Ex∼ρ

h

Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i) 2

> ∞

i

.

Reorganizing the two linear Zi-terms into a single difference of evaluation distributions, 

Ex∼ρ

δBR ,i (x) + δIter ,i (x) ≤ Ex∼ρ

h

Ea∼ˆπ [Zi] − E(ai,a−i)∼(ˆ π† 

> i,ˆπ−i)

[Zi]

i| {z }

> linear mismatch i

+ η 

> 2

Ex∼ρ

h

Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i) 2

> ∞

i

. (24) The first bracket is the linear mismatch of player i; the second is the squared evaluation error , which we will bound through the concentrability shift in Section B.3.3. 

Factoring the linear mismatch into an L1 distance. Because both expectations in linear mismatch i share the same opponent marginal ˆπ−i, only the player-i marginal differs. Writing the difference explicitly, 

Ea∼ˆπ [Zi] − E(ai,a−i)∼(ˆ π† 

> i,ˆπ−i)

[Zi] = X

> ai

 ˆπi(ai|x) − ˆπ† 

> i

(ai|x) Zi(x, a i, ˆπ−i)

≤ ˆπi(·| x) − ˆπ† 

> i

(·| x) 1 · Zi(x, ·, ˆπ−i) ∞

≤ ˆπi(·| x) − ˆπ† 

> i

(·| x) 1 · ∥Z i∥∞, (25) where the first step uses Hölder’s inequality and the second uses ∥Z i(x, ·, π −i)∥∞ ≤ ∥Z i∥∞ for any marginal. 

Two regimes for the linear mismatch. 

1. ROPE. When ˆπ is the exact regularized NE returned by Algorithm 1, the first-order optimality of each ˆπi implies ˆπi(·| x) ≡ ˆπ† 

> i

(·| x) for every i and x. Hence ∥ˆπi − ˆπ† 

> i

∥1 = 0 pointwise, and the linear mismatch in (24) vanishes identically : the regression error Zi cancels exactly between δBR ,i and δIter ,i .2. OPMD. When ˆπ = π(t) is an OPMD iterate with t ∈ [T ], π(t)

> i

̸ = π†,(t) 

> i

in general, but Corollary B.3 bounds the L1 distance by a pure optimization quantity. Since the rewards and ˆQi are bounded in [0 , 1] , ∥Z i∥∞ ≤ 1, and combining (25) with Corollary B.3, 

Et∗

h mX

> i=1

linear mismatch (t∗)

> i

i

≤ Et∗

h mX

> i=1

Eρ

∥π(t∗) 

> i

− π†,(t∗) 

> i

∥1

i

> (18)

≤ Cν

η + γγ O

 m

√T



= eO

 m

√T



.

The linear mismatch therefore decays at the optimization rate eO(1 /√T ), never with the dataset size n.20 B.3.3 Concentrability shift to the offline distribution 

We now bound the squared evaluation error in (24) by the in-sample squared regression error under the offline distribution μ. The argument has three steps: (i) reduce the ∥ · ∥ ∞ over actions to a worst-case 

max ai via triangle and Jensen inequalities, (ii) shift the opponent profile from the iterate ˆπ−i to the reference πref  

> −i

at the cost of Cshift := exp( η(m − 1)) , (iii) shift the resulting reference-anchored deviation distribution to μ at the cost of Cuni via Assumption 4.4. 

Step (i) — Triangle and Jensen inequalities. Using the marginalized identity (21) and the triangle inequality applied at each ai,

Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i) ∞ = max 

> ai

Ea−i∼ˆπ−i

 − Z i(x, a i, a−i)

≤ max 

> ai

Ea−i∼ˆπ−i

|Z i(x, a i, a−i)|.

Squaring both sides and applying Jensen’s inequality (Eu)2 ≤ E[u2] with u = |Z i| inside the inner expectation, 

Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i) 2 

> ∞

≤



max 

> ai

Ea−i∼ˆπ−i |Z i|

2

≤ max 

> ai

Ea−i∼ˆπ−i

Zi(x, a i, a−i)2.

Taking expectations over x ∼ ρ and multiplying by η/ 2, 

> η
> 2

Ex∼ρ

h

Qˆπ−i 

> i

(·) − ˆQi(·, ˆπ−i) 2

> ∞

i

≤ η 

> 2

Ex∼ρ

h

max 

> ai

Ea−i∼ˆπ−i

Z2

> i

i

. (26) 

Step (ii) — Opponent shift to the reference policy. Each iterate π(t) 

> j

takes a regularized Gibbs form anchored to πref  

> j

. By induction on t from (12) , there exists a function g(t) 

> j

: X × A j → R that is a convex combination of empirical Q-values bounded in [0 , 1] such that 

π(t) 

> j

(aj |x) = πref  

> j

(aj |x) exp  η g (t) 

> j

(x, a j )P 

> a′∈A j

πref  

> j

(a′|x) exp( η g (t) 

> j

(x, a ′)) , g(t) 

> j

(x, a j ) ∈ [0 , 1] .

The same form holds with gj = ¯Qj for the regularized empirical NE returned by ROPE. Hence the per-player density ratio satisfies 

π(t) 

> j

(aj |x)

πref  

> j

(aj |x) = exp( η g (t) 

> j

(x, a j )) 

P 

> a′

πref  

> j

(a′|x) exp( η g (t) 

> j

(x, a ′)) ≤ exp( η max a g(t) 

> j

)exp( η min a g(t) 

> j

) ≤ eη ,

where the final inequality uses g(t) 

> j

∈ [0 , 1] , so the numerator is at most exp( η · 1) = eη and the denominator (a convex combination of exp( η g (t) 

> j

) ≥ 1) is at least exp( η · 0) = 1 . Multiplying these per-player ratios across the m − 1 opponents of player i (using independence of marginals under the product policy), 

ˆπ−i(a−i|x)

πref  

> −i

(a−i|x) = Y 

> j̸=i

ˆπj (aj |x)

πref  

> j

(aj |x) ≤ exp  η(m − 1)  =: Cshift . (27) Substituting (27) into the inner expectation in (26), 

Ea−i∼ˆπ−i

Zi(x, a i, a−i)2 = X

> a−i

ˆπ−i(a−i|x) Zi(x, a i, a−i)2

≤ Cshift 

X

> a−i

πref  

> −i

(a−i|x) Zi(x, a i, a−i)2

= Cshift Ea−i∼πref 

> −i

Zi(x, a i, a−i)2, (28) which holds uniformly in ai. Substituting (28) into (26),  

> η
> 2

Ex∼ρ

h

max 

> ai

Ea−i∼ˆπ−i

Z2

> i

i

≤ η C shift  

> 2

Ex∼ρ

h

max 

> ai

Ea−i∼πref 

> −i

Z2

> i

i

. (29) 21 Step (iii) — Unilateral concentrability shift to μ. The remaining max ai in (29) is digested by introducing a deterministic deviation. Define 

˜πi(·| x) := δa⋆i (x)(·), a⋆i (x) := arg max 

> ai

Ea−i∼πref 

> −i

Zi(x, a i, a−i)2,

and the joint deviation profile ˜π := (˜ πi, π ref  

> −i

). By construction ˜π ∈ Πref −uni (player i deviates while the others adhere to the reference). With this construction the max ai becomes an expectation under 

˜πi:

Ex∼ρ

h

max 

> ai

Ea−i∼πref 

> −i

Z2

> i

i

= Ex∼ρ, a∼˜π(·| x)

Zi(x, a)2

= E(x, a)∼μ

h ρ(x) ˜ π(a|x)

μ(x, a) Zi(x, a)2i

. (30) Applying Assumption 4.4 to the reference-anchored profile ˜π ∈ Πref −uni ,

ρ(x) ˜ π(a|x)

μ(x, a) ≤ Cuni , μ-a.e. ,

which substituted into (30) gives 

Ex∼ρ

h

max 

> ai

Ea−i∼πref 

> −i

Z2

> i

i

≤ Cuni E(x, a)∼μ

Zi(x, a)2. (31) Combining (26) – (31) , the squared evaluation error in (24) is bounded by the in-sample squared regression error under μ: 

> η
> 2

Ex∼ρ

h

Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i) 2

> ∞

i

≤ η C shift Cuni  

> 2

E(x, a)∼μ

Zi(x, a)2. (32) 

Fast-rate generalization for regularized least-squares. The expected in-sample squared regres-sion error is controlled by the standard fast-rate guarantee for empirical risk minimization with bounded outputs. 

Lemma B.5 (Fast-rate generalization for regularized least-squares) . Let Qi be a finite function class, and assume realizability r⋆i ∈ Q i and bounded rewards rτ,i ∈ [0 , 1] . The least-squares estimator 

ˆQi ∈ arg min f ∈Q i

Pnτ =1 (f (xτ , aτ ) − rτ,i )2 satisfies, with probability at least 1 − δ,

E(x, a)∼μ

Zi(x, a)2 = E(x, a)∼μ

( ˆQi(x, a) − r⋆i (x, a)) 2 ≤ 30 log(2 |Q i|/δ )

n . (33) Combining (32) with Lemma B.5, with probability at least 1 − δ, 

> η
> 2

Ex∼ρ

h

Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i) 2

> ∞

i

≤ 15 η C shift Cuni log(2 |Q i|/δ )

n .

This is the per-player squared evaluation error contribution; with |Q| := max i |Q i|, summing over i ∈ [m] via a union bound (replacing δ ← δ/m ) gives the total squared statistical residual 

eO m η C shift Cuni log |Q| /n .

B.3.4 Closing the bound: idealized ROPE rate 

Let ˆπ denote the policy returned by Algorithm 1. Because ˆπ is an exact regularized NE of the empirical game, every ˆπi is the empirical regularized best response to ˆπ−i, so the potential ascent error and the linear mismatch both vanish identically: 

εpot ,i (x) = ˆV †,ˆπ−i 

> i

(x) − ˆV ˆπi (x) = 0 , ˆπi(·| x) = ˆπ† 

> i

(·| x), ∀i, x. (34) Summing the per-iterate decomposition (1) over players, integrating over x ∼ ρ, and using (34), 

Gap NE (ˆ π) = 

> m

X

> i=1

Ex∼ρ

V †,ˆπ−i 

> i

(x) − V ˆπi (x)

=

> m

X

> i=1

Ex∼ρ

εpot ,i (x) + δBR ,i (x) + δIter ,i (x)

> (34)

=

> m

X

> i=1

Ex∼ρ

δBR ,i (x) + δIter ,i (x).

22 Apply the bias-cancellation identity (24) term by term. Since the linear mismatch is zero in the ROPE setting, 

> m

X

> i=1

Ex∼ρ

δBR ,i (x) + δIter ,i (x) ≤

> m

X

> i=1

n

Ex∼ρ[linear mismatch i]

| {z }  

> = 0 since ˆπi=ˆ π†
> i

+ η 

> 2

Ex∼ρ

h

Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i) 2

> ∞

io 

= η

> 2
> m

X

> i=1

Ex∼ρ

h

Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i) 2

> ∞

i

.

Apply the concentrability-shift bound (32) and the fast-rate generalization Lemma B.5 to each summand:  

> η
> 2

Ex∼ρ

h

Qˆπ−i 

> i

(x, ·) − ˆQi(x, ·, ˆπ−i) 2

> ∞

i (32) 

≤ η C shift Cuni  

> 2

E(x, a)∼μ

Zi(x, a)2

> (33)

≤ 15 η C shift Cuni log(2 |Q i|/δ i)

n ,

each holding with probability ≥ 1 − δi. Setting δi := δ/m and applying a union bound across 

i ∈ [m], with probability at least 1 − δ,

Gap NE (ˆ π) ≤

> m

X

> i=1

15 η C shift Cuni log(2 m|Q i|/δ )

n ≤ 15 m η C shift Cuni log(2 m|Q| /δ )

n ,

where |Q| := max i |Q i|. Substituting Cshift = exp( η(m − 1)) and absorbing log(2 m/δ ) into the eO

notation, 

Gap NE (ˆ π) ≤ eO

 m η C uni exp( η(m − 1)) log |Q| 

n



,

which is the fast eO(1 /n ) rate of Theorem 5.1. Although the exact-NE oracle is intractable in generic general-sum games, Section B.1 shows that the α-potential structure aligns the equilibrium with the global maximization of ˆΦ, motivating the practical OPMD analysis below. 

B.3.5 Closing the bound: practical OPMD rate 

We now turn to the iterate ˜π = π(t∗) with t∗ ∼ Unif([ T ]) . Averaging the per-iterate decomposition (1) over t∗ and over players, 

Et∗

Gap NE (˜ π) = 1

T

> T

X

> t=1
> m

X

> i=1

Ex∼ρ

h

ε(t)pot ,i (x) + δ(t)BR ,i (x) + δ(t)Iter ,i (x)

i

. (35) We bound the three contributions in turn. 

Term 1 — Potential ascent contribution. By Lemma B.1, with stepsize γ ≤ 1/L Φ and LΦ =

O(m),

1

T

> T

X

> t=1
> m

X

> i=1

Ex∼ρ

ε(t)pot ,i (x) ≤ O 

 m

√T



+ mα. (36) 

Term 2 — Linear mismatch contribution. Apply the bias-cancellation identity (24) to each iterate and player. The two parts of the right-hand side give 

1

T

> T

X

> t=1
> m

X

> i=1

Ex∼ρ

δ(t)BR ,i (x) + δ(t)Iter ,i (x) ≤ 1

T

X

> t,i

Ex∼ρ

|linear mismatch (t) 

> i

(x)|

+ η

2T

X

> t,i

Ex∼ρ

h

Qπ(t) 

> −i
> i

(x, ·) − ˆQi(x, ·, π (t) 

> −i

) 2

> ∞

i

.(37) 23 The first sum is bounded by chaining (25) , Lemma B.2, Cauchy–Schwarz, and the squared-step bound (5). Recalling ∥Z i∥∞ ≤ 1 (rewards and ˆQi both lie in [0 , 1] ), 

1

T

X

> t,i

Eρ

|linear mismatch (t) 

> i

| (25) 

≤ 1

T

X

> t,i

Eρ

∥π(t) 

> i

− π†,(t) 

> i

∥1

 · ∥Z i∥∞

> (15)

≤ Cν (η + γ)/γ T

X

> t,i

Eρ

∥π(t) 

> i

− π(t+1)  

> i

∥1



> C.-S.

≤ Cν (η + γ)/γ T

√T m 

sX

> t,i

Eρ

∥π(t) 

> i

− π(t+1)  

> i

∥21



> (5)

≤ Cν

η + γγ

r m · O (m)

T = O



Cν

η + γγm√m

√T



. (38) Treating η, γ, ν min as problem-dependent constants, this is eO(mpm/T ).

Term 3 — Squared statistical residual contribution. Applying the concentrability shift (32) pointwise in t,

η

2T

> T

X

> t=1
> m

X

> i=1

Ex∼ρ

h

Qπ(t) 

> −i
> i

(x, ·) − ˆQi(x, ·, π (t) 

> −i

) 2

> ∞

i (32) 

≤ η C shift Cuni 

2T

X

> t,i

E(x, a)∼μ

Z(t) 

> i

(x, a)2.

The pointwise estimator ˆQi does not depend on t (it is computed once from the offline dataset before any iteration), so Z(t) 

> i

≡ Z i. Applying Lemma B.5 per player with confidence δ/m and union-bounding across i ∈ [m], with probability at least 1 − δ,

η C shift Cuni 

2T

X

> t,i

Eμ

Zi(x, a)2 = η C shift Cuni 

2

> m

X

> i=1

Eμ

Z2

> i



> (33)

≤ 15 m η C shift Cuni log(2 m|Q| /δ )

n = eO

 m η C shift Cuni log |Q| 

n



.

(39) 

Final synthesis. Substituting (36), (38), and (39) into (35) via (37), 

Et∗

Gap NE (˜ π) ≤ O

 m

√T

| {z } 

> potential ascent (εpot )

+ eO

 m√m

√T

| {z } 

> geometric mismatch (δBR +δIter )

+ mα |{z} 

> bias floor

+ eO

 m η C shift Cuni log |Q| 

n

| {z }

> squared statistical residual

. (40) Both optimization rates decay as O(T −1/2). Setting T ≥ n2 makes the optimization terms strictly dominated by the statistical residual: O(m/ √T ) + eO(m√m/ √T ) ≤ eO(1 /n ). Substituting into (40), 

Et∗

Gap NE (˜ π) ≤ eO

 1

n



+ mα, 

with hidden constant scaling as m η C shift Cuni log |Q| and Cshift = exp( η(m−1)) . The exponential dependence on η(m − 1) is the price of converting opponent-policy density ratios via the Gibbs form of the iterates and is characteristic of KL-regularized analyses (cf. (Ye et al., 2024)). This proves Theorem 6.1. 24