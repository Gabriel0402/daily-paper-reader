---
title: Weather-Robust Scene Semantics with Vision-Aligned 4D Radar
title_zh: 基于视觉对齐 4D 雷达的天气鲁棒场景语义
authors: "Kali Hamilton, Christoffer Heckman"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07367v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 使用雷达和VLM的自动驾驶多模态感知
tldr: 针对摄像头和激光雷达在恶劣天气下失效的问题，本文提出一种将4D毫米波雷达与冻结的视觉语言模型（VLM）对齐的方法。通过将雷达编码器对齐至SigLIP视觉嵌入，并利用投影层解决Token范数失配问题，实现了鲁棒的场景语义描述。在K-RADAR数据集的雾雪天气测试中，该方法显著优于产生严重幻觉的视觉基准模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07367v1/fig-001.webp\", \"caption\": \"TABLE II K-RADAR SEQUENCE-LEVEL SPLIT. †ZERO-SHOT WEATHER (UNSEEN DURING TRAINING).\", \"page\": 6, \"index\": 1, \"width\": 664, \"height\": 562}, {\"url\": \"assets/figures/arxiv/2605.07367v1/fig-002.webp\", \"caption\": \"Fig. 6. Qualitative comparison across weather conditions. Columns: camera image, radar range-azimuth heatmap, parsed caption summaries. Rows: Normal (seq 5), Fog (seq 38), Heavy Snow (seq 46). Yellow ×= GT, cyan ×= prose predictions, green ×= JSON predictions. Radar RA maps remain interpretable in fog and heavy snow where camera imagery is saturated or obscured.\", \"page\": 7, \"index\": 2, \"width\": 914, \"height\": 770}, {\"url\": \"assets/figures/arxiv/2605.07367v1/fig-003.webp\", \"caption\": \"TABLE I DETECTION METRICS ON HELD-OUT WEATHER SEQUENCES. BOLD = BEST PER ROW.\", \"page\": 3, \"index\": 3, \"width\": 539, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2605.07367v1/fig-004.webp\", \"caption\": \"Fig. 2. Two-stage training pipeline. Top: Stage 1 aligns the radar encoder to frozen SigLIP [13] vision embeddings (MSE + cosine loss). Bottom: Stage 2 freezes the encoder, transfers projector weights, and fine-tunes LoRA [14] adapters on Qwen2.5-VL-3B [15] to generate structured captions from radar tokens.\", \"page\": 3, \"index\": 4, \"width\": 1071, \"height\": 381}]"
motivation: 摄像头和激光雷达在雨雪雾等恶劣天气下性能大幅下降，而毫米波雷达具有更强的环境适应性。
method: 将雷达编码器与冻结的SigLIP视觉嵌入对齐，并通过带有LayerNorm的投影层连接冻结的VLM来生成场景描述。
result: "在包含雾、轻雪和大雪的K-RADAR测试集中，雷达配置的性能全面超越了幻觉率超过90%的摄像头基准。"
conclusion: 实验证明了雷达与VLM结合在全天候场景理解中的潜力，并揭示了投影层归一化对跨模态对齐的关键作用。
---

## 摘要
摄像头和激光雷达在雨、雾、雪天性能会下降，而毫米波雷达基本不受影响。我们将雷达编码器与冻结的 SigLIP 视觉嵌入对齐，并通过一个拥有约 700 万个可训练参数的冻结视觉语言模型 (VLM) 解码结构化场景描述。在包含雾、小雪和大雪测试序列的 K-RADAR 数据集上，所有雷达配置的性能均优于摄像头基准，后者在这些条件下会产生超过 90% 的幻觉。我们发现，在将雷达连接到冻结的 VLM 时，Token 范数不匹配是主要的失效模式，并证明投影器输出的层归一化 (LayerNorm) 可以解决这一问题。对编码器复杂度、描述格式和池化策略的分析揭示了各种权衡，为未来的雷达-VLM 流水线设计提供了参考。

## Abstract
Cameras and LiDAR degrade in rain, fog, and snow, while millimeter-wave radar remains largely unaffected. We align a radar encoder to frozen SigLIP vision   embeddings and decode structured scene captions through a frozen vision-language model (VLM) with approximately 7M trainable parameters. On K-RADAR with   held-out fog, light snow, and heavy snow sequences, all radar configurations outperform a camera baseline that collapses to over 90% hallucination. We   identify a token-norm mismatch as the dominant failure mode when bridging radar to a frozen VLM and show that projector-output LayerNorm resolves it.   Analysis of encoder complexity, caption format, and pooling strategy reveals tradeoffs that inform future radar-VLM pipeline design.

---

## 论文详细总结（自动生成）

这是一份关于论文《Weather-Robust Scene Semantics with Vision-Aligned 4D Radar》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：自动驾驶中的主流传感器（摄像头和激光雷达）在雨、雾、雪等恶劣天气下性能会严重退化。相比之下，毫米波雷达（77 GHz）具有极强的穿透力，受天气影响极小。
*   **核心问题**：能否将 4D 毫米波雷达的特征空间与视觉语言模型（VLM）的嵌入空间对齐，从而利用冻结的 VLM 强大的语义理解能力，在恶劣天气下生成准确的场景描述（结构化字幕）？

### 2. 论文提出的方法论
该研究提出了一种两阶段的训练流水线，旨在将雷达编码器接入冻结的视觉语言模型（Qwen2.5-VL-3B）：
*   **输入表示**：将 4D 雷达张量（多普勒、距离、高度、方位角）进行高度最大投影，生成 5 通道（物理特征聚合）或 66 通道（完整多普勒分布）的 3D 张量。
*   **第一阶段：视觉对齐**：使用 ResNet-18 作为雷达编码器，通过 MSE 和余弦距离损失，将雷达特征与冻结的 **SigLIP** 视觉嵌入进行对齐。
*   **第二阶段：字幕微调**：冻结雷达编码器，通过一个 MLP 投影层将雷达 Token 喂入 VLM。使用 **LoRA**（秩为 16）对 VLM 的 Q/K 投影层进行微调。
*   **关键技术细节（LayerNorm 修复）**：作者发现雷达 Token 与 VLM 原生 Token 存在**范数失配**（雷达 Token 范数过大导致注意力饱和），通过在投影层输出端添加 **LayerNorm** 解决了 VLM 忽略雷达输入而产生幻觉的病态问题。
*   **结构化输出**：设计了 Prose（散文）和 JSON 两种字幕格式，将目标类别、距离和方位角转化为文本 Token。

### 3. 实验设计
*   **数据集**：使用 **K-RADAR** 数据集，这是首个包含多种天气条件下 3D 框标注的 4D 成像雷达数据集。
*   **Benchmark 与对比方法**：
    *   **基准模型**：一个基于摄像头的 VLM（使用相同的 LoRA 设置），用于展示视觉模型在恶劣天气下的崩溃情况。
    *   **消融实验**：对比了 5 通道 vs 66 通道输入、有无视觉对齐阶段、Prose vs JSON 格式、以及平均池化 vs 交叉注意力池化策略。
*   **评估指标**：采用“字幕即检测”（Caption-as-Detection）指标，包括类别 F1 分数、距离平均绝对误差（MAE）、方位角 MAE 和幻觉率。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练时长。
*   **模型规模**：使用了 Qwen2.5-VL-3B 模型，可训练参数量约为 700 万（LoRA 适配器 + 投影层）。

### 5. 实验数量与充分性
*   **实验规模**：使用了约 7.5k 帧进行训练，并在包含雾、轻雪、大雪的独立测试序列上进行了验证。
*   **充分性评价**：
    *   **客观性**：通过完全隔离的天气序列进行“零样本”天气评估，证明了雷达的鲁棒性。
    *   **消融充分**：针对投影层归一化、输入通道数、池化方式等关键变量做了详细对比。
    *   **局限性**：数据规模（约 8k 帧）对于 VLM 训练而言相对较小，且每种恶劣天气的测试序列仅有一个，结果可能存在一定的随机性。

### 6. 论文的主要结论与发现
*   **天气鲁棒性**：在雾天和雪天，摄像头基准模型的幻觉率超过 90%，而雷达模型保持了稳定的 F1 分数（>0.44）。
*   **范数匹配至关重要**：如果不进行投影层 LayerNorm 处理，VLM 会倾向于忽略雷达信号，仅靠语言先验生成内容。
*   **利用率鸿沟**：66 通道输入虽然在编码器特征层面更丰富（线性探测表现更好），但在有限数据下，VLM 对其对齐难度更大，导致最终字幕质量反而不如简单的 5 通道输入。
*   **格式影响**：JSON 格式在空间精度（方位角误差）上优于 Prose 格式。

### 7. 优点
*   **创新性**：首次尝试在真实 4D 雷达数据上实现与冻结 VLM 的生成式对齐，而非仅仅做分类或检测。
*   **工程洞察**：深入分析并解决了跨模态对齐中的 Token 范数失配问题，这对其他非视觉传感器接入 VLM 具有借鉴意义。
*   **实用性**：展示了在极低数据量（~8k 帧）下，通过视觉对齐实现传感器扩展的可能性。

### 8. 不足与局限
*   **空间精度受限**：由于将距离和角度作为文本 Token 预测，其精度远低于雷达传感器的物理分辨率（距离误差达 10 米以上）。
*   **自回归瓶颈**：自回归生成导致模型在处理多个目标时，学习能力在不同空间字段间分配不均。
*   **数据多样性**：实验仅限于 K-RADAR 数据集，尚未验证在其他雷达平台或更广泛场景下的泛化能力。

（完）
