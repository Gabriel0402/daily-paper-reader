---
title: Semantic Alignment in Hyperbolic Space for Open-Vocabulary Semantic Segmentation
title_zh: 双曲空间中的语义对齐用于开放词汇语义分割
authors: "Hoang M. Truong, Hai Nguyen-Truong, Dang Huynh"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08874v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 使用视觉语言模型的开放词汇语义分割
tldr: 本文针对开放词汇语义分割中图像级模型向像素级预测迁移时的层级与语义对齐失配问题，提出了HyRo框架。该方法在双曲空间的庞加莱球模型中解耦了层级对齐与语义对齐：通过调整双曲半径来对齐不同层级，并利用保持半径的正交变换优化同层级内的角度对齐。实验证明，HyRo在多个基准测试上均取得了当前最先进的性能，有效提升了语义分割的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08874v1/fig-001.webp\", \"caption\": \"Figure 1. HyRo rotates the text embeddings to achieve a smaller angle (β) relative to the target image embeddings compared to the initial angle (α). This geometric adjustment enables the model to resolve semantic ambiguities and produce more accurate, finegrained segmentations.\", \"page\": 1, \"index\": 1, \"width\": 471, \"height\": 466}, {\"url\": \"assets/figures/arxiv/2605.08874v1/fig-002.webp\", \"caption\": \"Table 1. Comparison with state-of-the-art methods on standard benchmarks. The best-performing results are presented in bold, while the second-best results are underlined. “E”: Euclidean Space. “H”: Hyperbolic Space.\", \"page\": 6, \"index\": 2, \"width\": 1026, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2605.08874v1/fig-003.webp\", \"caption\": \"Figure 4. Qualitative comparison between HyperCLIP [31] and our method on the A-847 setting of ADE20K. Our approach mitigates several semantic misalignment failures observed in HyperCLIP.\", \"page\": 7, \"index\": 3, \"width\": 1034, \"height\": 778}, {\"url\": \"assets/figures/arxiv/2605.08874v1/fig-004.webp\", \"caption\": \"Figure 2. Overview of HyRo. Embeddings are rotated around the origin in hyperbolic space using an orthogonal block matrix to minimize the angle between visual and textual features while preserving their hyperbolic radii, thereby enhancing cross-modal semantic alignment.\", \"page\": 3, \"index\": 4, \"width\": 496, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2605.08874v1/fig-005.webp\", \"caption\": \"Figure 5. Attention map visualization for target classes “person”, “building”, and “window” (top to bottom). Without HyRo, attention is diffusely spread across semantically irrelevant regions. With HyRo, attention is more concentrated on the target class regions, indicating improved semantic alignment.\", \"page\": 8, \"index\": 5, \"width\": 496, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2605.08874v1/fig-006.webp\", \"caption\": \"Figure 3. Overall architecture of HyRo. Given image and text inputs, Euclidean embeddings are mapped to the Poincaré ball via the exponential map. HyRo then decouples alignment into two stages: (1) Hierarchical Adjustment using block-diagonal radius scaling matrices to align granularity, and (2) Semantic Refinement using orthogonal rotation matrices to adjust angular relationships without altering the radius. The refined hyperbolic embeddings are mapped back to the tangent space for decoding.\", \"page\": 4, \"index\": 6, \"width\": 1036, \"height\": 248}]"
motivation: 现有方法在利用双曲几何建模层级关系时，往往忽略了同一层级内部嵌入向量之间的语义对齐偏差。
method: 提出HyRo框架，在庞加莱球模型中通过调整双曲半径实现层级对齐，并采用正交变换在保持半径的同时优化角度上的语义对齐。
result: 在标准开放词汇语义分割基准测试中，HyRo成功超越了现有技术，达到了最先进的性能表现。
conclusion: 通过在双曲空间中解耦并分别优化层级与语义对齐，可以更有效地提升视觉语言模型在密集预测任务中的表现。
---

## 摘要
开放词汇语义分割需要将 CLIP 等图像级视觉语言模型适配到密集的像素级预测，由于嵌入空间中层级结构与语义对齐之间的不匹配，这具有挑战性。虽然最近的工作利用双曲几何来建模层级关系，但它们对齐了跨层级的嵌入，却忽略了同一层级内嵌入之间的语义失配。在这项工作中，我们提出了 HyRo，这是一个在庞加莱球模型中解耦层级对齐和语义对齐的双曲微调框架。HyRo 通过调整双曲半径来对齐层级，并通过正交变换进行角度对齐来优化语义关系，该变换在理论上保持了双曲半径。在标准开放词汇语义分割基准上的实验表明，HyRo 相比于之前的方法实现了最先进的性能。

## Abstract
Open-vocabulary semantic segmentation requires adapting image-level vision-language models such as CLIP to dense pixel-level prediction, which is challenging due to the mismatch between hierarchical structure and semantic alignment in the embedding space. While recent works leverage hyperbolic geometry to model hierarchical relationships, they align embeddings across hierarchical levels but overlook semantic misalignment among embeddings within the same level. In this work, we propose HyRo, a hyperbolic fine-tuning framework that decouples hierarchical and semantic alignment in the Poincaré ball model. HyRo aligns hierarchical levels by adjusting the hyperbolic radius and refines semantic relationships through angular alignment using an orthogonal transformation that theoretically preserves the hyperbolic radius. Experiments on standard open-vocabulary semantic segmentation benchmarks demonstrate that HyRo achieves state-of-the-art performance over prior methods.

---

## 论文详细总结（自动生成）

这篇论文提出了 **HyRo**（Hyperbolic Rotation），一种针对开放词汇语义分割（Open-Vocabulary Semantic Segmentation, OVSS）的新型双曲空间微调框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：开放词汇语义分割旨在识别图像中未见过的语义类别。目前主流做法是适配预训练的视觉-语言模型（如 CLIP）。
*   **核心问题**：CLIP 最初是为图像级表示设计的，将其迁移到像素级预测时，存在“层级结构”与“语义对齐”之间的失配。
*   **动机**：虽然双曲空间（如庞加莱球模型）因其指数级体积增长特性非常适合建模层级关系，但现有方法（如 HyperCLIP）仅通过调整嵌入向量的**双曲半径**来对齐层级（如从图像级到像素级的粒度跨越），却忽略了同一层级内嵌入向量之间的**角度关系**（即语义相似度）。这会导致语义相近但类别不同的物体在空间中无法有效区分。

### 2. 方法论：核心思想与关键技术
HyRo 的核心思想是在双曲空间中**解耦层级对齐（半径）与语义对齐（角度）**。
*   **双曲半径调整（层级对齐）**：使用可学习的对角缩放矩阵 $S$，通过 M¨obius 矩阵-向量乘法调整嵌入向量的半径，以匹配像素级的粒度。
*   **双曲旋转变换（语义对齐）**：
    *   **核心技术**：引入正交变换矩阵 $R$。作者在理论上证明了在庞加莱球模型中，正交变换能够改变向量间的夹角（优化语义相关性），同时严格保持其双曲半径（不破坏已建立的层级结构）。
    *   **Cayley 变换**：为了确保矩阵 $R$ 的严格正交性，使用 Cayley 变换将无约束的偏斜对称矩阵映射为正交矩阵。
    *   **分块对角结构**：为了降低计算复杂度（从 $O(d^3)$ 降至 $O(d^3/n^2)$）并适配不同维度的视觉/文本嵌入，将大矩阵分解为多个独立的小块。
*   **流程**：输入特征先通过指数映射进入双曲空间，依次进行半径缩放和正交旋转，最后通过对数映射回到欧几里得空间，输入解码器进行成本聚合（Cost Aggregation）。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：COCO-Stuff。
    *   **评估集**：ADE20K（包含 A-150 和挑战性的 A-847 类别）、PASCAL-Context（PC-59 和 PC-459）、PASCAL VOC（PAS-20 和 PAS-20b）。
*   **Benchmark 与对比方法**：对比了包括 LSeg、ZegFormer、OVSeg、SAN、SED 以及最相关的双曲空间方法 HyperCLIP 在内的多种 SOTA 方法。
*   **评价指标**：平均交并比（mIoU）。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA A100 GPU**。
*   **训练细节**：每个 GPU 放置 1 个样本（总 Batch Size 为 8），训练迭代次数为 40,000 次。
*   **时长**：整个训练过程大约耗时 **8 小时**。

### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个主流基准测试上进行了全面对比，并提供了定性的可视化结果（分割图和注意力图）。
*   **消融实验**：
    *   验证了“半径缩放”与“旋转变换”的互补性（两者结合效果最好）。
    *   探讨了双曲空间曲率 $c$ 的影响（发现较小的曲率 $c=0.01$ 更有利于保持 CLIP 的泛化性）。
    *   测试了分块矩阵大小 $n$ 的影响。
*   **客观性**：实验遵循了开放词汇分割的标准协议，冻结了 CLIP 编码器以防止过拟合，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：HyRo 在 A-847 和 PC-459 等大规模词汇基准上表现尤为突出，证明了角度对齐在区分细粒度类别时的重要性。
*   **几何特性**：实验证实了在双曲空间中，单纯靠半径调整不足以解决语义歧义，通过正交变换优化角度可以显著增强跨模态的语义对应关系。
*   **可视化证据**：注意力图显示，HyRo 处理后的特征能更精准地集中在目标物体区域，减少了背景噪声的干扰。

### 7. 优点
*   **理论严谨**：为正交变换在双曲空间中保持半径提供了数学证明。
*   **解耦思想**：巧妙地将层级（深度/抽象度）与语义（方向/类别属性）分开优化，逻辑清晰。
*   **计算高效**：通过分块对角矩阵解决了高维嵌入下正交矩阵运算开销大的问题。

### 8. 不足与局限
*   **应用场景限制**：目前仅在静态图像分割上进行了验证，尚未扩展到更复杂的视频分割（如 MOSE 或 MeViS 数据集）。
*   **超参数敏感性**：虽然做了消融实验，但曲率 $c$ 和分块大小 $n$ 对不同数据集的影响仍存在波动，可能需要针对特定任务进行微调。
*   **增量改进**：虽然在多个指标上达到 SOTA，但相比于前作 HyperCLIP，在某些简单数据集（如 PAS-20b）上的提升幅度相对有限。

（完）
