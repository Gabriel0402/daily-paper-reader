---
title: "LLM-X: A Scalable Negotiation-Oriented Exchange for Communication Among Personal LLM Agents"
title_zh: LLM-X：一种面向个人 LLM 智能体间通信的可扩展协商型交换平台
authors: "Giuliano Lorenzoni, Paulo Alencar, Donald Cowan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11376v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 基于LLM的多智能体谈判与协作
tldr: 本研究针对个人大模型代理间缺乏直接、结构化通信的问题，提出了LLM-X架构。LLM-X是一个面向协商的可扩展交换环境，通过联邦网关、主题路由和策略执行机制，实现了代理间的直接协调。该研究贡献了支持能力协商的类型化消息协议，并在不同规模和负载条件下进行了实证评估，展示了策略强度与性能之间的权衡，为多代理协作提供了稳定可靠的基础设施。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11376v1/fig-001.webp\", \"caption\": \"Figure 1: LLM-X conceptual (negotiation-centric) architecture.\", \"page\": 3, \"index\": 1, \"width\": 1048, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2605.11376v1/fig-002.webp\", \"caption\": \"Figure 2: CNet-style conceptual negotiation sequence in LLM-X. Alice emits a CFP with a deadline; contractors reply with Offers. The Policy Engine governs when and how the winner is selected (Low: first-valid; Medium: bounded wait; High: wait-all). Acks ensure reliable delivery; retries occur on missing acknowledgments.\", \"page\": 3, \"index\": 2, \"width\": 1048, \"height\": 433}]"
motivation: 现有的工具中心化协议主要关注代理与API的交互，缺乏支持个人代理间直接、结构化协商与协调的专用环境。
method: 提出了包含联邦网关、主题路由和策略执行的LLM-X架构，并设计了支持能力协商和合同网式协调的类型化消息协议。
result: 实验表明，严格的策略虽能提升鲁棒性和公平性，但会增加延迟和消息量，且系统在长时间持续负载下表现稳定。
conclusion: LLM-X为大规模个人大模型代理间的协商提供了可扩展且稳定的通信框架，并揭示了策略配置对系统性能的关键影响。
---

## 摘要
我们提出了个人 LLM 交换平台（LLM-X），这是一个面向协商的可扩展环境，能够实现代表个人用户的多个个人智能体（LLM）之间的直接、结构化通信。与现有的侧重于智能体与 API 交互的以工具为中心的协议不同，LLM-X 引入了消息总线和路由底层，用于 LLM 之间的协作，并提供模式有效性（schema validity）和策略执行方面的保证。我们的贡献包括：（1）一种由联邦网关、基于主题的路由和策略执行组成的 LLM-X 架构；（2）一种支持能力协商和合同网式（contract-net-style）协作的有类型消息协议；（3）首次对大规模基于 LLM 的多智能体协商进行的实证评估。实验涵盖了 5、9 和 12 个智能体，在不同的协商策略（低、中、高）以及短期（分钟级）和长期（2小时、12小时）负载条件下进行。结果突显了明显的策略-性能权衡：更严格的策略提高了鲁棒性和公平性，但增加了延迟和消息量。长期运行实验证实，LLM-X 在持续负载下保持稳定，且延迟漂移在有限范围内。

## Abstract
We propose a personal-LLM exchange (LLM-X), a scalable negotiation-oriented environment that enables direct, structured communication across populations of personal agents (LLMs), each representing an individual user. Unlike existing tool-centric protocols that focus on agent-API interaction, LLM-X introduces a message bus and routing substrate for LLM-to-LLM coordination with guarantees around schema validity and policy enforcement. We contribute: (1) an architecture for LLM-X comprising federated gateways, topic-based routing, and policy enforcement; (2) a typed message protocol supporting capability negotiation and contract-net-style coordination; and (3) the first empirical evaluation of LLM-based multi-agent negotiation at scale. Experiments span 5, 9, and 12 agents, under distinct negotiation policies (Low, Medium, High), and across both short-run (minutes) and long-run (2h, 12h) load conditions. Results highlight clear policy-performance trade-offs: stricter policies improve robustness and fairness but increase latencies and message volume. Extended runs confirm that LLM-X remains stable under sustained load, with bounded latency drift.

---

## 论文详细总结（自动生成）

这是一份关于论文《LLM-X: A Scalable Negotiation-Oriented Exchange for Communication Among Personal LLM Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）作为个人助手的普及，当前的智能体大多处于“孤岛状态”，即用户仅与自己的智能体交互，而智能体之间缺乏直接、结构化的通信机制。现有的协议（如 MCP）多侧重于“智能体-工具/API”的交互，而非“智能体-智能体”的直接协商。这种隔离限制了多智能体在复杂任务（如规划、共识达成、资源分配）中的协作潜力。
**LLM-X 的核心目标**是建立一个可扩展的、面向协商的交换平台，为异构个人 LLM 智能体提供一个具备模式验证（Schema Validation）和策略执行（Policy Enforcement）能力的通信底层，从而支持大规模的多智能体动态协作研究。

### 2. 方法论：核心思想与关键技术
LLM-X 的核心思想是将协商逻辑与通信传输解耦，引入一个结构化的消息总线。
*   **系统架构**：
    *   **联邦网关（Gateways）**：负责身份验证（JWT）、模式验证（确保 JSON 负载符合预定义格式）和速率限制。
    *   **策略引擎（Policy Engine）**：定义了三种决策策略（低、中、高强度），控制发起者如何处理来自多个承包商的提议。
    *   **控制平面与观测性**：处理智能体注册、能力协商及详细的指标追踪（延迟、吞吐量、审计日志）。
*   **协商协议（CNet 风格）**：
    *   采用经典的“合同网”（Contract Net）协议流程：发起者发布招标（CFP）→ 承包商提交提议（Offer）→ 发起者根据策略选择（Accept/Reject）→ 确认（Ack）。
    *   同时支持 FIPA 风格的交替出价（Alternating Offers）模式。
*   **消息模型**：
    *   使用类型化的 JSON 信封，包含消息 ID、发送/接收方、能力声明、生存时间（TTL）和同意/范围上下文。这种设计确保了即使底层 LLM 产生随机输出，也能通过网关层的模式校验进行过滤和纠正。

### 3. 实验设计
*   **场景设置**：模拟一个发起者（Alice）与多个承包商（5、9、12 个智能体）之间的协商过程。
*   **对比维度（Benchmark）**：
    *   **策略对比**：低（Low，超时即关闭）、中（Medium，收到首个有效提议即接受）、高（High，等待所有提议后决策）。
    *   **规模对比**：5、9、12 个异构智能体。
    *   **时长对比**：短期（2 分钟）、长期（2 小时、12 小时）。
*   **评估指标**：每分钟消息量（CFP/Offer 计数）、中值（p50）和尾部（p95）延迟、每轮提议分布、系统稳定性（延迟漂移）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或数量。
*   **实现细节**：实验中的智能体被实例化为轻量级的 Python 包装器（Wrappers），用于模拟受控环境下的通信行为。虽然协议设计为“LLM 就绪”（可接入 OpenAI、Anthropic 等 API），但本次实验主要侧重于通信底层的压力测试和策略评估，而非 LLM 本身的推理开销。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多组 2 分钟的短期运行，涵盖了 3 种策略 × 3 种智能体规模。
    *   进行了 2 小时（中、高策略）和 12 小时（高策略）的长期稳定性实验。
*   **充分性评价**：实验设计较为系统，通过泊松过程模拟真实流量，并重复多次实验以确保一致性。对于通信底层的性能评估是充分的，但由于实验主要使用 Python 包装器而非全量实时调用昂贵的 LLM API，其在真实 LLM 推理延迟下的表现仍有待进一步验证。

### 6. 主要结论与发现
*   **策略-性能权衡**：
    *   **低/中策略**：响应速度快，吞吐量高，但可能牺牲公平性或错过更优提议。
    *   **高策略**：确保了提议的完整性和公平性，但增加了通信开销和同步延迟。
*   **可扩展性**：随着智能体数量从 5 增加到 12，消息流量呈近线性增长，延迟退化保持在毫秒级的可控范围内。
*   **长期稳定性**：12 小时的持续负载测试显示，系统没有明显的延迟漂移或吞吐量饱和，证明了架构的鲁棒性。

### 7. 优点与亮点
*   **结构化通信**：通过 JSON Schema 强制执行消息格式，解决了 LLM 输出随机性与系统确定性需求之间的矛盾。
*   **策略抽象化**：允许在不更改通信逻辑的情况下动态切换协商策略（低/中/高），增强了系统的灵活性。
*   **现实就绪性**：设计考虑了 JWT 鉴权、速率限制和审计追踪，具备向真实分布式系统迁移的潜力。

### 8. 不足与局限
*   **LLM 推理延迟缺失**：实验主要测试了通信底层的毫秒级延迟，而真实 LLM API 调用通常有秒级的延迟，这可能会掩盖通信层本身的性能特征。
*   **协商逻辑简单**：目前的实验主要基于预设的 Python 逻辑，尚未深入探讨 LLM 在复杂、多轮、非结构化文本协商中的认知表现。
*   **智能体规模上限**：实验最高仅测试了 12 个智能体，对于成百上千个智能体的大规模群体动态尚未涉及。

（完）
