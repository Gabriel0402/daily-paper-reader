---
title: Learning Agentic Policy from Action Guidance
title_zh: 从动作引导中学习智能体策略
authors: "Yuxiang Ji, Zengbin Wang, Yong Wang, Shidong Yang, Ziyu Ma, Guanhua Chen, Zonghua Sun, Liaoni Wu, Xiangxiang Chu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12004v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 带有动作指导的大语言模型智能体强化学习
tldr: 本研究针对大语言模型智能体强化学习中基础策略探索能力不足、难以触达奖励状态的问题，提出了ActGuide-RL框架。该方法利用日常交互中的动作数据作为计划式参考引导，通过混合策略训练将引导带来的探索增益内化到原始策略中。实验表明，该方法在GAIA和XBench基准测试上显著提升了性能，且无需昂贵的监督微调冷启动，为减少对SFT数据的依赖提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12004v1/fig-001.webp\", \"caption\": \"Table 1: Main results of ACTGUIDE-RL on search-agent benchmarks comparing foundation models, search-agent trained models, and RL baseline. The best results are indicated in bold.\", \"page\": 7, \"index\": 1, \"width\": 825, \"height\": 955}, {\"url\": \"assets/figures/arxiv/2605.12004v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of ACTGUIDE-RL framework. Conventional agentic RL can only obtain training signals within the base model in-capability region. ACTGUIDE-RL follows the principle of minimal intervention, dynamically introducing action data to guide the model toward out-region exploration. Such mixed rollouts are trained through mixed-policy optimization.\", \"page\": 3, \"index\": 2, \"width\": 826, \"height\": 341}]"
motivation: 解决大语言模型在强化学习中因基础策略探索能力有限而难以触达奖励状态并获取有效学习信号的问题。
method: 提出ActGuide-RL框架，将动作数据作为自适应引导注入探索过程，并通过混合策略训练实现引导增益的内化。
result: 在GAIA和XBench基准测试中，该方法相比零强化学习分别提升了10.7和19个百分点，性能可媲美传统的SFT+RL流程。
conclusion: ActGuide-RL通过可扩展的动作引导替代了沉重的SFT数据依赖，为智能体强化学习提供了一种高效的新范式。
---

## 摘要
大语言模型（LLM）的智能体强化学习（RL）关键取决于基础策略的探索能力，因为训练信号仅在其能力范围内产生。对于基础策略无法到达奖励状态的任务，需要额外的训练或外部引导来恢复有效的学习信号。我们没有依赖昂贵的迭代监督微调（SFT），而是利用了日常人类交互中产生的丰富动作数据。我们提出了 ActGuide-RL，它将动作数据作为计划式参考引导注入，使智能体策略能够克服通往奖励状态的可达性障碍。随后，通过混合策略训练对有引导和无引导的展开（rollouts）进行联合优化，将探索收益内化回无引导策略中。受收益-风险权衡的理论和实证分析启发，我们采用了最小干预原则，仅将引导作为一种自适应回退机制调用，在匹配任务难度的同时最小化离策略（off-policy）风险。在搜索智能体基准测试中，ActGuide-RL 相比于零样本强化学习（zero RL）有显著提升（在 GAIA 上提升 10.7 个百分点，在 Qwen3-4B 的 XBench 上提升 19 个百分点），且在无需任何冷启动的情况下，表现与 SFT+RL 流水线相当。这为智能体强化学习提供了一种新范式，即通过使用可扩展的动作引导来减少对大量 SFT 数据的依赖。

## Abstract
Agentic reinforcement learning (RL) for Large Language Models (LLMs) critically depends on the exploration capability of the base policy, as training signals emerge only within its in-capability region. For tasks where the base policy cannot reach reward states, additional training or external guidance is needed to recover effective learning signals. Rather than relying on costly iterative supervised fine tuning (SFT), we exploit the abundant action data generated in everyday human interactions. We propose \textsc{ActGuide-RL}, which injects action data as plan-style reference guidance, enabling the agentic policy to overcome reachability barriers to reward states. Guided and unguided rollouts are then jointly optimized via mixed-policy training, internalizing the exploration gains back into the unguided policy. Motivated by a theoretical and empirical analysis of the benefit-risk trade-off, we adopt a minimal intervention principle that invokes guidance only as an adaptive fallback, matching task difficulty while minimizing off-policy risk. On search-agent benchmarks, \textsc{ActGuide-RL} substantially improves over zero RL (+10.7 pp on GAIA and +19 pp on XBench with Qwen3-4B), and performs on par with the SFT+RL pipeline without any cold start. This suggests a new paradigm for agentic RL that reduces the reliance on heavy SFT data by using scalable action guidance instead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Agentic Policy from Action Guidance》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于解决**智能体强化学习（Agentic RL）中的“探索瓶颈”**。
*   **研究动机**：LLM 智能体在进行强化学习时，其训练信号高度依赖于模型自身的探索能力。如果任务难度超出了基础模型的能力范围（即处于“Out-region”），模型将无法触达奖励状态，导致梯度塌陷和训练停滞。
*   **背景**：传统做法是先进行昂贵的监督微调（SFT）作为冷启动，但这增加了对高质量专家轨迹数据的依赖。本文提出利用互联网和日常交互中海量的、低成本的**动作数据（Action Data）**作为引导，帮助模型跨越探索障碍。

### 2. 方法论：ActGuide-RL 框架
ActGuide-RL 的核心思想是**“最小干预原则”**，即仅在必要时引入动作引导，并将其内化到模型中。
*   **可达性动力学分析**：论文定义了“有效状态访问质量”（Effective state-visiting mass），量化了模型在特定步骤后触达成功的潜力。当该值骤降时，即出现了“可达性障碍”。
*   **动作引导注入**：将动作序列（如 API 调用、点击步骤）作为“计划式参考引导”附加在 Prompt 中，而非强制模型模仿。
*   **最小干预原则（Minimal Intervention）**：
    *   **自适应回退**：首先尝试无引导采样，若全部失败，则通过**二分查找**确定最小的引导步数 $k^*$，以平衡探索收益与离策略（Off-policy）风险。
*   **混合策略优化（Mixed-Policy Optimization）**：
    *   使用类似 GRPO 的目标函数，但在计算重要性采样比率（Importance Ratio）时，针对有引导的样本，分母使用有引导的分布，从而将引导下的成功经验“内化”到无引导的策略中。

### 3. 实验设计
*   **数据集/场景**：主要针对**搜索智能体（Search-agent）**任务。
    *   **域内基准**：GAIA（通用助手）、WebWalkerQA（网页遍历）、XBench（深度搜索）、BrowseComp-ZH（中文网页浏览）。
    *   **域外泛化**：GPQA（研究生水平问答）、TruthfulQA（事实性）、IFEval（指令遵循）。
*   **对比方法**：
    *   基础模型（Qwen2.5, Qwen3, DeepSeek-V3 等）。
    *   Vanilla RL（无引导的 GRPO）。
    *   SFT + RL 流水线（传统的两阶段训练）。
    *   专门训练的智能体模型（如 WebSailor, WebThinker）。

### 4. 资源与算力
*   **硬件配置**：训练和 Rollout 过程在配备 **8 张 NVIDIA H20 GPU** 的节点上运行。
*   **评估算力**：奖励分配（LLM-Judge）和测试评估使用了另外一个配备 **8 张 NVIDIA H20 GPU** 的独立节点。
*   **训练参数**：总训练步数为 64 步，Batch Size 为 32，Group Size 为 8。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了 4 个不同规模的基础模型（Qwen2.5-3B/7B, Qwen3-4B/8B），在 4 个核心基准和 3 个泛化基准上进行了测试。
*   **消融实验**：非常充分。作者对比了有无自适应机制、有无回退机制、有无混合策略优化，并研究了引导强度对性能的影响。
*   **鲁棒性测试**：专门设计了**动作噪声敏感性实验**，模拟了 10%-20% 的动作干扰，验证了框架在不完美数据下的稳定性。
*   **客观性**：通过与 SFT+RL 流水线的直接对比，客观展示了该方法在减少数据依赖方面的潜力。

### 6. 主要结论与发现
*   **显著提升**：ActGuide-RL 在所有测试模型上均优于 Vanilla RL。在 Qwen3-4B 上，GAIA 提升 10.7%，XBench 提升 19%。
*   **无需冷启动**：在没有 SFT 冷启动的情况下，ActGuide-RL 的表现与“SFT + RL”两阶段训练相当，甚至在某些任务上更优。
*   **内化能力**：模型不仅学会了执行动作，还通过引导学会了更长的交互链条（Turn 数和 Token 数显著增加）。
*   **保持泛化性**：相比 SFT 容易导致模型在域外任务（如 GPQA）上退化，ActGuide-RL 更好地保留了基础模型的通用能力。

### 7. 优点
*   **数据效率高**：利用了易于获取的动作轨迹（Action-only），而非昂贵的带有思维链（CoT）的专家数据。
*   **自适应性强**：通过二分查找实现最小干预，有效降低了离策略训练的分布偏移风险。
*   **即插即用**：可以作为现有 RL 算法（如 PPO, GRPO）的增强模块。

### 8. 不足与局限
*   **场景局限**：目前主要验证了搜索智能体（Stateless 场景），在更复杂的 CLI、GUI 或具身智能（Embodied AI）等具有强状态依赖的场景下，其表现尚待验证。
*   **数据处理细节**：论文未详细讨论如何从海量原始日志中自动化清洗和提取高质量动作数据的流程。
*   **对噪声的容忍上限**：实验显示当动作噪声达到 20% 时性能开始下降，说明该方法对极低质量的数据仍有一定敏感度。

（完）
