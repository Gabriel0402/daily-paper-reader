---
title: "PointGS: Semantic-Consistent Unsupervised 3D Point Cloud Segmentation with 3D Gaussian Splatting"
title_zh: PointGS：基于 3D 高斯泼溅的语义一致性无监督 3D 点云分割
authors: "Yixiao Song, Qingyong Li, Wen Wang, Zhicheng Yan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11520v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 集成SAM模型用于无监督3D点云分割
tldr: 本研究提出PointGS，一种利用3D高斯泼溅（3DGS）作为中间表示的无监督3D点云分割方法。针对2D预训练模型与3D离散点云之间的模态不匹配问题，PointGS通过3DGS重建填补空间间隙并消除投影重叠，结合SAM提取多视图语义并利用对比学习将其蒸馏至高斯基元，最后通过两步配准将语义映射回原始点云，在ScanNet-V2和S3DIS数据集上均取得了领先的分割性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11520v1/fig-001.webp\", \"caption\": \"Figure 1. In the conference room scene, the upper part of the figure shows that the sparse point cloud causes the foreground points and background points to overlap, while the lower part shows that in the Gaussian space, the background is completely blocked by the foreground.\", \"page\": 1, \"index\": 1, \"width\": 479, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2605.11520v1/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison of unsupervised segmentation on the S3DIS validation set. Each color represents one semantic class. For better understanding, we show some of the color and name matches at the bottom.\", \"page\": 6, \"index\": 2, \"width\": 968, \"height\": 494}, {\"url\": \"assets/figures/arxiv/2605.11520v1/fig-003.webp\", \"caption\": \"Figure 4. Qualitative comparison with Ground Truth of unsupervised segmentation on the ScanNet validation set. Each color represents one semantic class.\", \"page\": 11, \"index\": 3, \"width\": 1011, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2605.11520v1/fig-004.webp\", \"caption\": \"Figure 2. The pipeline of our method. Given an indoor point cloud, we first generate multi-view projections and apply Gaussian splatting. Then, the rendered images are segmented by SAM, and semantic cues are transferred back to 3D Gaussians. Finally, the segmented Gaussians are refined and aligned with the raw point cloud through the alignment module of Gaussians and points, and labels are propagated back to the original points.\", \"page\": 4, \"index\": 4, \"width\": 989, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2605.11520v1/fig-005.webp\", \"caption\": \"Figure 5. Qualitative comparison of different ablation settings on the ScanNet validation set.\", \"page\": 12, \"index\": 5, \"width\": 1001, \"height\": 296}]"
motivation: 旨在解决2D预训练模型与离散3D点云之间因模态不匹配导致的投影重叠和语义不一致问题。
method: 利用3D高斯泼溅作为统一中间表示，通过多视图渲染、SAM语义提取及对比学习蒸馏，实现从2D到3D的高效语义对齐。
result: "在ScanNet-V2和S3DIS数据集上分别实现了0.9%和2.8%的mIoU提升，显著超越了现有的无监督分割方法。"
conclusion: PointGS证明了3D高斯泼溅在桥接2D语义与3D点云方面的有效性，为无监督3D场景理解提供了新思路。
---

## 摘要
无监督点云分割对于具身智能和自动驾驶至关重要，因为它降低了全监督方法所需的高昂点级密集标注成本。虽然集成 Segment Anything Model (SAM) 等 2D 预训练模型来补充语义信息是一种自然的选择，但这种方法面临着离散 3D 点与连续 2D 图像之间的根本不匹配。这种不匹配导致了不可避免的投影重叠和复杂的模态对齐，从而损害了 2D-3D 迁移过程中的语义一致性。为了解决这些局限性，本文提出了 PointGS，这是一个简单且有效的无监督 3D 点云分割流水线。PointGS 利用 3D 高斯泼溅（3D Gaussian Splatting）作为统一的中间表示，以弥合离散-连续域的差距。首先，通过多视图观测将输入的稀疏点云重建为密集的 3D 高斯空间，填补空间间隙并编码遮挡关系，以消除投影引起的语义混淆。接着，从高斯空间渲染出多视图密集图像，并通过 SAM 提取 2D 语义掩码，随后通过对比学习将语义蒸馏到 3D 高斯基元中，以确保不同视图间语义分配的一致性。最后，通过两步配准将高斯空间与原始点云对齐，并通过对带标签的高斯基元进行最近邻搜索来分配点云语义。实验表明，PointGS 的性能优于现有的无监督方法，在 ScanNet-V2 上实现了 +0.9% 的 mIoU 提升，在 S3DIS 上实现了 +2.8% 的 mIoU 提升。

## Abstract
Unsupervised point cloud segmentation is critical for embodied artificial intelligence and autonomous driving, as it mitigates the prohibitive cost of dense point-level annotations required by fully supervised methods. While integrating 2D pre-trained models such as the Segment Anything Model (SAM) to supplement semantic information is a natural choice, this approach faces a fundamental mismatch between discrete 3D points and continuous 2D images. This mismatch leads to inevitable projection overlap and complex modality alignment, resulting in compromised semantic consistency across 2D-3D transfer. To address these limitations, this paper proposes PointGS, a simple yet effective pipeline for unsupervised 3D point cloud segmentation. PointGS leverages 3D Gaussian Splatting as a unified intermediate representation to bridge the discrete-continuous domain gap. Input sparse point clouds are first reconstructed into dense 3D Gaussian spaces via multi-view observations, filling spatial gaps and encoding occlusion relationships to eliminate projection-induced semantic conflation. Multi-view dense images are rendered from the Gaussian space, with 2D semantic masks extracted via SAM, and semantics are distilled to 3D Gaussian primitives through contrastive learning to ensure consistent semantic assignments across different views. The Gaussian space is aligned with the original point cloud via two-step registration, and point semantics are assigned through nearest-neighbor search on labeled Gaussians. Experiments demonstrate that PointGS outperforms state-of-the-art unsupervised methods, achieving +0.9% mIoU on ScanNet-V2 and +2.8% mIoU on S3DIS.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《PointGS: Semantic-Consistent Unsupervised 3D Point Cloud Segmentation with 3D Gaussian Splatting》** 的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**无监督 3D 点云语义分割**。
*   **研究动机**：全监督方法依赖昂贵的点级标注。虽然现有方法尝试引入 2D 预训练大模型（如 SAM）来提供语义先验，但面临**离散 3D 点云与连续 2D 图像之间的模态不匹配**。
*   **核心挑战**：稀疏点云在投影到 2D 平面时，由于缺乏深度顺序和遮挡关系，会导致前景与背景点重叠，产生语义混淆。此外，2D 掩码与 3D 点之间的对齐非常复杂，难以保持跨视图的语义一致性。

### 2. 方法论：核心思想与技术细节
PointGS 提出利用 **3D 高斯泼溅（3D-GS）** 作为统一的中间表示来桥接 2D 和 3D 领域。其流程分为四个关键阶段：

*   **从点云到 3D 高斯重建**：
    *   将原始稀疏点云通过多视图观测重建为密集的 3D 高斯空间。
    *   利用 3D-GS 的连续表示填补空间间隙，并引入“多视图一致性检查”剔除孤立的高斯噪声。
*   **语义信息蒸馏**：
    *   从高斯空间渲染出密集的 2D 图像，使用 **SAM** 提取多尺度语义掩码。
    *   为每个高斯基元分配可学习的**亲和特征（Affinity Feature）**。
    *   通过**对比学习**，将 2D 掩码的语义一致性蒸馏到 3D 高斯基元中，并利用“尺度门控（Scale-gate）”机制处理物体部件与整体的多粒度语义模糊。
*   **高斯与点云的对齐**：
    *   提取高斯基元的几何中心点，进行密度去噪和缩放调整。
    *   采用**两步 ICP 配准**：先进行粗配准，再通过 24 种轴向旋转组合寻找全局最优解，解决 3D-GS 重建坐标系与原始点云坐标系不一致的问题。
*   **标签迁移**：
    *   通过最近邻搜索（Nearest Neighbor），将带有语义标签的高斯基元信息传递给原始点云。

### 3. 实验设计
*   **数据集**：使用了两个主流的室内场景数据集：**ScanNet-v2**（1,613 个扫描场景，20 类）和 **S3DIS**（271 个场景，13 类，重点评估 Area 5）。
*   **Benchmark 与对比方法**：
    *   对比了传统的聚类方法（如 PC-HC, GrowSP, LogoSP）。
    *   对比了基于 2D 先验的方法（如 PointDC, U3DS³, WYPR）。
*   **评估指标**：平均交并比 (mIoU)、总体准确率 (oAcc) 和平均准确率 (mAcc)。

### 4. 资源与算力
*   **硬件**：实验在单张 **NVIDIA RTX 3090 GPU** 上完成。
*   **效率**：
    *   Vanilla 3D-GS 训练速度约为 43.27 迭代/秒。
    *   SAM 处理速度约为 0.35 帧/秒。
    *   每个场景运行 **10,000 次迭代**以平衡质量与效率。
*   **输入**：使用 770×770 像素的投影图像，每个场景生成约 150 个视图。

### 5. 实验数量与充分性
*   **实验规模**：在两个大型数据集上进行了全量测试，并提供了详细的类别级 mIoU 数据。
*   **消融实验**：非常充分。验证了 3D-GS 重建、两步 ICP 配准、亲和特征、多视图一致性检查等每个模块的增益。
*   **参数敏感性分析**：针对投影视图数量、角度间隔、投影分布类型（环绕 vs 平铺）、SAM 聚类参数（epsilon, min size）以及尺度门控（Scale Gate）进行了详尽的对比实验。
*   **客观性**：采用了无监督领域通用的匈牙利算法进行标签匹配，确保了与 Baseline 对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：PointGS 在 ScanNet-v2 上达到 **36.7% mIoU**（比 SOTA 提升 0.9%），在 S3DIS 上达到 **49.3% mIoU**（比 SOTA 提升 2.8%）。
*   **解决重叠问题**：实验证明 3D-GS 能有效解决稀疏点云投影时的前景/背景混淆问题。
*   **小物体识别**：定性分析显示，PointGS 在识别细小物体（如墙上的告示板）方面显著优于基于聚类的方法。

### 7. 优点（亮点）
*   **创新的中间表示**：巧妙地利用 3D-GS 的连续性和可微渲染特性，解决了 2D-3D 迁移中的几何遮挡难题。
*   **无需预训练**：该方法不依赖 3D 预训练模型或人工标注，具有极强的通用性。
*   **鲁棒的配准机制**：两步 ICP 解决了 3DGS 随机初始化带来的坐标偏移问题，这是很多同类研究忽略的细节。

### 8. 不足与局限
*   **计算开销**：虽然 3D-GS 训练较快，但每个场景仍需单独重建和蒸馏，对于超大规模实时场景可能存在延迟。
*   **室内局限性**：实验主要集中在室内场景，室外大规模自动驾驶场景（点云更稀疏、范围更广）的有效性尚未验证。
*   **参数依赖**：Scale Gate 等参数对分割粒度影响较大，不同复杂度的场景可能需要手动微调参数以达到最优效果。

（完）
