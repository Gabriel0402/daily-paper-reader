---
title: "REFNet++: Multi-Task Efficient Fusion of Camera and Radar Sensor Data in Bird's-Eye Polar View"
title_zh: REFNet++：鸟瞰极坐标视图下摄像头与雷达传感器数据的多任务高效融合
authors: "Kavin Chandrasekaran, Sorin Grigorescu, Gijs Dubbelman, Pavol Jancura"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11824v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 用于车辆感知的摄像头与雷达多模态传感器融合
tldr: 本研究提出REFNet++，旨在高效融合相机与雷达数据以提升自动驾驶感知能力。针对雷达数据噪声大和跨模态对齐难的问题，该方法利用变分编码器-解码器将前视相机图像转换至鸟瞰图极坐标域，并与雷达距离-方位角特征对齐。在RADIal数据集上的实验证明，该方案在车辆检测和自由空间分割任务中兼顾了高精度与计算效率，为多模态传感器融合提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11824v1/fig-001.webp\", \"caption\": \"TABLE II: Free space segmentation results on the test split. Abbreviations and details as in Table I.\", \"page\": 6, \"index\": 1, \"width\": 444, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2605.11824v1/fig-002.webp\", \"caption\": \"Fig. 2: Multitask resource-efficient fusion architecture: The input to the radar only network is the range-Doppler (RD) data, while the camera only network intakes front-view camera images. x0 to x4 are the feature maps from the respective encoder blocks. The encoder is connected to the decoder by a thick blue arrow, by which the encoded features are upscaled to higher resolutions. The skip connections are shown as dotted lines that preserves the spatial information. The radar and camera features are fused by concatenation on the subsequent heads. Predictions are in Bird’s Eye RA Polar View as shown in Fig. 1 and 3.\", \"page\": 3, \"index\": 2, \"width\": 1054, \"height\": 429}, {\"url\": \"assets/figures/arxiv/2605.11824v1/fig-003.webp\", \"caption\": \"TABLE III: Computational efficiency. Only models for which computational efficiency could be measured are included due to code availability. Abbreviations and details as in Table I.\", \"page\": 7, \"index\": 3, \"width\": 1062, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2605.11824v1/fig-004.webp\", \"caption\": \"Fig. 1: Architectural Overview: The variational encoderdecoder architecture learns the transformation from the frontview camera image to BEV, which corresponds to the RA domain. On the other hand, the radar encoder-decoder architecture learns to recover the angle information from the complex range-Doppler (RD) input, producing RA features. Free space segmentation and vehicle detection are performed on the resulting features fused by concatenation by the appropriate heads. (a) ground truth labels, (b) prediction results, and (c) predictions projected onto the camera image.\", \"page\": 1, \"index\": 4, \"width\": 506, \"height\": 360}]"
motivation: 针对雷达数据噪声大、分类弱以及与相机数据对齐困难的问题，寻求一种兼顾精度与计算效率的多模态融合方案。
method: 利用变分编码器-解码器将相机图像转换至BEV极坐标域，并与从雷达RD频谱恢复的RA特征在统一域内进行融合。
result: 在RADIal数据集的车辆检测和自由空间分割任务中，该方法表现优于现有先进技术。
conclusion: 通过在统一的极坐标域内对齐多模态传感器数据，REFNet++实现了鲁棒且高效的多任务环境感知。
---

## 摘要
摄像头传感器通常能提供车辆周围环境的真实视图，这对于环境感知至关重要。另一方面，价格低廉的雷达传感器因其在多变天气条件下的鲁棒性而变得极具价值。然而，由于其输出噪声较大且分类能力有限，雷达传感器在与其他传感器数据结合时效果最佳。具体而言，我们通过将雷达和摄像头数据对齐到统一域中来解决多模态传感器融合的挑战，不仅优先考虑准确性，还兼顾计算效率。我们的工作利用雷达的原始距离-多普勒（RD）谱和前视摄像头图像作为输入。为了实现有效融合，我们采用了一种变分编码器-解码器架构，学习将前视摄像头数据转换为鸟瞰图（BEV）极坐标域。同时，雷达编码器-解码器学习从产生距离-方位（RA）特征的RD数据中恢复角度信息。这种对齐确保了两种模态都在兼容的域中表示，从而促进了鲁棒且高效的传感器融合。我们使用RADIal数据集，针对车辆检测和自由空间分割任务，将我们的融合策略与最先进的方法进行了对比评估。

## Abstract
A realistic view of the vehicle's surroundings is generally offered by camera sensors, which is crucial for environmental perception. Affordable radar sensors, on the other hand, are becoming invaluable due to their robustness in variable weather conditions. However, because of their noisy output and reduced classification capability, they work best when combined with other sensor data. Specifically, we address the challenge of multimodal sensor fusion by aligning radar and camera data in a unified domain, prioritizing not only accuracy, but also computational efficiency. Our work leverages the raw range-Doppler (RD) spectrum from radar and front-view camera images as inputs. To enable effective fusion, we employ a variational encoder-decoder architecture that learns the transformation of front-view camera data into the Bird's-Eye View (BEV) polar domain. Concurrently, a radar encoder-decoder learns to recover the angle information from the RD data that produce Range-Azimuth (RA) features. This alignment ensures that both modalities are represented in a compatible domain, facilitating robust and efficient sensor fusion. We evaluated our fusion strategy for vehicle detection and free space segmentation against state-of-the-art methods using the RADIal dataset.

---

## 论文详细总结（自动生成）

这是一份关于论文《REFNet++: Multi-Task Efficient Fusion of Camera and Radar Sensor Data in Bird's-Eye Polar View》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
自动驾驶感知系统依赖多传感器融合。摄像头提供丰富的语义信息但缺乏深度，雷达（Radar）在恶劣天气下鲁棒且具备深度感知能力，但原始数据噪声大、角分辨率低。
*   **核心挑战**：如何将前视摄像头图像（透视视图）与雷达原始数据（距离-多普勒域）在统一的表征空间内高效对齐并融合。
*   **研究动机**：现有的融合方法往往计算开销巨大，或在视图转换（如从图像到鸟瞰图 BEV）时依赖复杂的预处理。REFNet++ 旨在通过一种**资源高效（Resource-Efficient）**的架构，在统一的**鸟瞰极坐标视图（Bird’s-Eye Polar View）**下实现车辆检测与自由空间分割的多任务处理。

### 2. 论文提出的方法论
REFNet++ 采用了一种双支路编码器-解码器架构，核心思想是**隐式学习视图转换**：
*   **摄像头支路（变分编码器-解码器）**：
    *   使用特征金字塔网络（FPN）提取前视图像特征。
    *   引入**变分自编码器（VAE）**的思想，通过重参数化技巧（Reparameterization Trick）学习潜在空间的均值（$\mu$）和对数方差（$\log[\sigma^2]$），从而隐式地将图像从透视视图转换为 BEV 极坐标域。
    *   这种随机性建模有助于提高网络对数据变异的泛化能力。
*   **雷达支路（RD 到 RA 转换）**：
    *   输入为 16 通道的复数距离-多普勒（RD）张量。
    *   通过多输入多输出（MIMO）预编码器和残差层提取特征。
    *   利用**通道交换（Channel Swapping）**策略，从 RD 数据中恢复角度信息，生成距离-方位角（RA）特征图。
*   **融合与多任务头**：
    *   在统一的 RA 极坐标域内，通过通道拼接（Concatenation）融合双模态特征。
    *   **检测头**：输出分类概率图（Focal Loss）和回归图（Smooth L1 Loss），用于车辆定位。
    *   **分割头**：输出 2D 概率图（BCE Loss），识别可行驶区域。

### 3. 实验设计
*   **数据集**：使用 **RADIal 数据集**。该数据集包含同步的摄像头、雷达（ADC 信号到点云全格式）、LiDAR 和 GPS 数据，具有 8,252 帧标注。
*   **Benchmark 与对比方法**：
    *   **车辆检测**：对比了 FFTRadNet、TFFTRadNet、ADCNet、ROFusion、EchoFusion 以及作者的前作 REFNet。
    *   **自由空间分割**：对比了 SparseRadNet、TransRadar、Occugrid 等基于雷达的方法。
*   **评估指标**：检测任务使用 AP、AR、F1 分数及位置误差（RE/AE）；分割任务使用 mIoU；效率评估使用 FPS、参数量、模型大小及 GPU 显存占用。

### 4. 资源与算力
*   **硬件**：使用单块 **Nvidia RTX A6000 GPU** 进行训练。
*   **训练细节**：
    *   Batch size 为 4，训练 100 个 epoch。
    *   使用 Adam 优化器，初始学习率为 1e-4，每 10 个 epoch 衰减 0.9。
    *   输入图像尺寸缩小为原始尺寸的 1/4（270x480）以节省内存。
*   **训练时长**：文中提到 REFNet++ 的训练速度比前作 REFNet 快约两倍。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了单任务（仅检测/仅分割）与多任务模式的对比。
*   **消融实验**：
    1.  **仅摄像头支路**：验证了单模态下的性能大幅下降，证明了融合的必要性。
    2.  **重参数化技巧验证**：对比了去掉 VAE 模块后的表现，发现不使用该技巧时训练时长增加三倍且无显著收益。
*   **公平性**：作者重新实现了多个基准模型（如 REFNet 的分割头），并在相同的数据分割（70% 训练/15% 验证/15% 测试）下进行测试，确保了对比的客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：在自由空间分割任务中，REFNet++ 显著优于现有方法（mIoU 达到 88.13%）。在检测任务中，其 F1 分数和定位精度（RE/AE）处于领先或第二的水平。
*   **效率优势**：在保持高精度的同时，REFNet++ 的推理速度（约 54-58 FPS）和显存占用（约 2.06 GB）表现优异，实现了精度与速度的最佳平衡。
*   **视图转换有效性**：变分编码器-解码器能够成功学习从前视图到 BEV 极坐标域的复杂非线性转换，无需手动校准参数的硬编码。

### 7. 优点（亮点）
*   **端到端学习**：通过 VAE 架构隐式学习视图转换，规避了相机内参/外参微小误差对物理转换带来的负面影响。
*   **计算高效**：相比于 EchoFusion 等重型模型，REFNet++ 参数量更少，更适合嵌入式自动驾驶平台。
*   **多任务协同**：成功证明了检测和分割任务在共享特征空间下可以相互促进，且训练速度更快。

### 8. 不足与局限
*   **类别局限性**：由于 RADIal 数据集的限制，目前仅针对“车辆”一类进行检测，未验证在行人、骑行者等多类别场景下的泛化能力。
*   **环境覆盖**：虽然雷达理论上对恶劣天气鲁棒，但实验主要在 RADIal 的标准场景下完成，缺乏极端天气（如暴雪、浓雾）下的专项量化分析。
*   **传感器依赖**：高度依赖雷达的 RD 谱质量，对于低成本、低分辨率雷达的适配性有待验证。

（完）
