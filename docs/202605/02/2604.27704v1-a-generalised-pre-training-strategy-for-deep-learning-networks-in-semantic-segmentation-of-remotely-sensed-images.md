---
title: A generalised pre-training strategy for deep learning networks in semantic segmentation of remotely sensed images
title_zh: 遥感图像语义分割中深度学习网络的通用预训练策略
authors: "Yuan Fang, Yuanzhi Cai, Jagannath Aryal, Qinfeng Zhu, Hong Huang, Cheng Zhang, Lei Fan"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27704v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 遥感图像语义分割
tldr: 针对遥感图像语义分割中ImageNet预训练模型与目标领域存在巨大领域差异的问题，本文提出了一种简单且通用的预训练策略。该策略通过引导模型在预训练阶段避免学习特定领域的特征，从而显著提升了预训练模型的泛化能力。在iSAID、MFNet等四个不同场景和模态的遥感数据集上的实验表明，该方法均达到了最先进的准确率，为构建计算机视觉与遥感通用的基础模型奠定了基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27704v1/fig-001.webp\", \"caption\": \"Fig. 2. Activation maps and prediction results (underneath) of Swin-T models on different scenes: (a) Original images, (b) CSP-3 with RGB input, (c) CSP-3 with BGR input, (d) IM-RGB with RGB input, (e) IM-RGB with BGR input.\", \"page\": 6, \"index\": 1, \"width\": 829, \"height\": 835}, {\"url\": \"assets/figures/arxiv/2604.27704v1/fig-002.webp\", \"caption\": \"Table 8. Semantic segmentation accuracies on Potsdam test set (%)\", \"page\": 7, \"index\": 2, \"width\": 869, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2604.27704v1/fig-003.webp\", \"caption\": \"Fig. 1. Overall architecture of the channel shuffling pre-training (CSP) strategy.\", \"page\": 3, \"index\": 3, \"width\": 854, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2604.27704v1/fig-004.webp\", \"caption\": \"Fig. 4. Visual comparison of semantic segmentation maps from ConvNeXt-T models with distinct pre-training strategies: A-input images; B-ground-truth maps; C-segmentation by ImageNet pre-trained ConvNeXt-T; D-by ConvNeXt-T pre-trained with CSP-3; E-by CSP-4; F-by CSP-5.\", \"page\": 8, \"index\": 4, \"width\": 841, \"height\": 907}, {\"url\": \"assets/figures/arxiv/2604.27704v1/fig-005.webp\", \"caption\": \"Table 3. Classification accuracies on ImageNet-1K validation set with different pre-trained models and spectral channel sequences\", \"page\": 4, \"index\": 5, \"width\": 869, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2604.27704v1/fig-006.webp\", \"caption\": \"Table 2. Detailed pre-training and fine-tuning setting\", \"page\": 4, \"index\": 6, \"width\": 871, \"height\": 639}, {\"url\": \"assets/figures/arxiv/2604.27704v1/fig-007.webp\", \"caption\": \"Table 4. Examples of advantageous classes on ImageNet-1K of Swin-T trained by CSP-3\", \"page\": 5, \"index\": 7, \"width\": 871, \"height\": 197}, {\"url\": \"assets/figures/arxiv/2604.27704v1/fig-008.webp\", \"caption\": \"Table 5. Semantic segmentation accuracies on iSAID validation set (%)\", \"page\": 5, \"index\": 8, \"width\": 869, \"height\": 213}]"
motivation: 解决ImageNet与遥感图像之间巨大的领域差异以及构建大规模遥感预训练数据集成本高、泛化性差的问题。
method: 提出一种新的预训练策略，旨在引导模型在预训练过程中不学习特定领域的特征，以增强其泛化性能。
result: 在iSAID、MFNet、PST900和Potsdam四个数据集上均取得了SOTA性能，显著提升了语义分割的准确率。
conclusion: 该研究提供了一种提升预训练模型泛化能力的有效方法，有助于开发适用于多种应用场景的统一基础模型。
---

## 摘要
在遥感图像分割中，深度学习模型通常先使用 ImageNet 等大型图像数据库进行预训练，然后在特定领域的数据集上进行微调。然而，这些微调模型的性能往往受到 ImageNet 图像与所处理的遥感图像之间巨大领域差异（即场景和模态的差异）的阻碍。因此，许多研究人员致力于建立大规模的特定领域图像数据集进行预训练，旨在提升模型性能。然而，建立此类数据集通常具有挑战性，需要付出巨大努力，且这些数据集在其他应用场景中的泛化能力往往有限。为了解决这些问题，本研究引入了一种新颖且简单的预训练策略，旨在引导模型在预训练期间避免学习预训练数据集中的特定领域特征，从而提高预训练模型的泛化能力。为了评估该策略的有效性，深度学习模型在 ImageNet 上进行预训练，随后在四个具有不同场景和模态的语义分割数据集（包括 iSAID、MFNet、PST900 和 Potsdam）上进行微调。实验结果表明，所提出的预训练策略在所有四个数据集上均达到了最先进的准确率，即 iSAID 的 mIoU 为 67.4%，MFNet 的 mIoU 为 56.9%，PST900 的 mIoU 为 84.22%，Potsdam 的 mF1 为 91.88%。本研究为开发适用于计算机视觉和遥感应用的统一基础模型奠定了基础。

## Abstract
In the segmentation of remotely sensed images, deep learning models are typically pre-trained using large image databases like ImageNet before fine-tuned on domain-specific datasets. However, the performance of these fine-tuned models is often hindered by the large domain gaps (i.e., differences in scenes and modalities) between ImageNet's images and remotely sensed images being processed. Therefore, many researchers have undertaken efforts to establish large-scale domain-specific image datasets for pre-training, aiming to enhance model performance. However, establishing such datasets is often challenging, requiring significant effort, and these datasets often exhibit limited generaliza-bility to other application scenarios. To address these issues, this study introduces a novel yet simple pre-training strategy designed to guide a model away from learning domain-specific features in a pre-training dataset during pre-training, thereby improving the generalisation ability of the pre-trained model. To evaluate the strategy's effectiveness, deep learning models are pre-trained on ImageNet and subsequently fine-tuned on four semantic segmentation datasets with diverse scenes and modalities, including iSAID, MFNet, PST900 and Potsdam. Experimental results show that the proposed pre-training strategy led to state-of-the-art accuracies on all four datasets, namely 67.4% mIoU for iSAID, 56.9% mIoU for MFNet, 84.22% mIoU for PST900, 91.88% mF1 for Potsdam. This research lays the groundwork for developing a unified foundation model applicable to both computer vision and remote sensing applications.

---

## 论文详细总结（自动生成）

这是一份关于论文《A generalised pre-training strategy for deep learning networks in semantic segmentation of remotely sensed images》的结构化分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
遥感图像（RS）语义分割通常依赖于在 ImageNet 上预训练的模型。然而，ImageNet（主要为 RGB 自然图像）与遥感图像（包含多光谱、热红外、高空视角等）之间存在巨大的**领域差异（Domain Gap）**。虽然建立大规模遥感专用数据集进行预训练是一种解决方法，但其成本极高且泛化性有限。本研究的核心问题是：**如何通过一种简单的策略，使模型在仅使用 ImageNet 预训练的情况下，也能获得极强的跨领域、跨模态泛化能力？**

### 2. 方法论：通道随机洗牌预训练（CSP）
论文提出了一种名为 **CSP (Channel Shuffling Pre-training)** 的策略，其核心思想是**引导模型在预训练阶段忽略特定的光谱/颜色特征，转而专注于学习通用的空间和结构特征**。
*   **核心流程**：
    1.  **通道扩展**：若目标模型需要 $n$ 个输入通道，而预训练数据集（如 ImageNet）只有 $m$ 个通道（通常 $m=3$），则将原始通道复制 $x$ 次并拼接，使总通道数 $x \times m \geq n$。
    2.  **随机洗牌（Channel Shuffling）**：在预训练的每一个迭代中，对这些通道进行随机排序。
    3.  **通道截取**：取洗牌后的前 $n$ 个通道作为模型输入。
*   **技术逻辑**：通过打破固定的 RGB 顺序，模型无法依赖特定的颜色模式（如“草是绿色的”）来分类，被迫学习更具鲁棒性的形状、纹理和空间关系，从而在面对遥感图像的不同光谱组合时表现更佳。

### 3. 实验设计
*   **数据集与场景**：
    *   **iSAID**：航空遥感图像（RGB），用于测试标准场景。
    *   **MFNet**：自动驾驶场景（RGB + 热红外 T），测试多模态。
    *   **PST900**：地下环境（RGB + 热红外 T），测试极端光照条件。
    *   **Potsdam**：遥感图像（RGB + 近红外 IR + 数字表面模型 DSM），测试多光谱与高度信息融合。
*   **基准（Benchmark）与对比方法**：
    *   **IM-RGB**：传统的 ImageNet 预训练模型（固定 RGB 顺序）。
    *   **RSP**：专门针对遥感图像设计的预训练模型（作为对比参考）。
    *   **架构**：在 CNN（ConvNeXt-T）和 Transformer（Swin-T）两种主流架构上均进行了验证。

### 4. 资源与算力
*   **硬件**：使用了 2 台 **Nvidia GeForce RTX 3090 GPU**。
*   **训练细节**：
    *   预训练：ImageNet-1K，300 个 epoch，batch size 为 128。
    *   微调：根据数据集不同，迭代次数在 10k 到 80k 之间。
    *   框架：PyTorch。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个完全不同领域的数据集上进行了测试，涵盖了 3 通道、4 通道和 5 通道等多种输入配置。
*   **充分性与公平性**：
    *   对比了不同网络架构（CNN vs Transformer），证明了策略的普适性。
    *   进行了光谱扰动实验（如 RGB 换成 BGR），通过激活图（Activation Maps）直观展示了 CSP 模型的稳定性。
    *   实验设置（如优化器、学习率衰减等）在对比组间保持一致，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **泛化性显著提升**：尽管 CSP 在 ImageNet 自身的分类准确率上略有下降（约 1.4%-3.8%），但在下游遥感任务中表现卓越。
*   **SOTA 性能**：在所有四个数据集上均达到或超过了现有先进水平（如 PST900 mIoU 提升了约 13.8%）。
*   **抗干扰能力**：CSP 模型对阴影、光照变化和传感器噪声具有更强的免疫力，能够识别出基准模型漏掉的遮挡目标。

### 7. 优点（亮点）
*   **极简且高效**：不需要收集额外的遥感数据，仅通过改变预训练时的通道处理方式即可实现。
*   **多模态兼容**：首次证明了单一预训练模型可以同时适配 RGB、多光谱和热红外等多种模态。
*   **打破领域壁垒**：为构建“统一视觉基础模型”提供了一种无需大规模遥感标注数据的新思路。

### 8. 不足与局限
*   **边缘检测问题**：论文提到模型在物体边界的定义上仍有模糊，精确的边缘分割能力有待加强。
*   **预训练精度损失**：在原始 ImageNet 任务上的精度下降表明，该策略是以牺牲特定领域的颜色敏感度为代价的，对于极度依赖颜色识别的任务可能不适用。
*   **应用范围限制**：目前仅在语义分割任务上验证，尚未在目标检测、超分辨率等其他视觉任务中广泛测试。

（完）
