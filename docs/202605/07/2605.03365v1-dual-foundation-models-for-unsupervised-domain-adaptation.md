---
title: Dual-Foundation Models for Unsupervised Domain Adaptation
title_zh: 用于无监督领域自适应的双基础模型
authors: "Yerin Cheon, Aruna Balasubramanian, Francois Rameau"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03365v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 用于自动驾驶和无监督域自适应的语义分割
tldr: 针对语义分割中无监督域适应（UDA）面临的伪标签置信度限制和原型偏差问题，本文提出了一种双基础模型框架。该框架结合了SAM和DINOv3两个互补的基础模型：利用SAM的超像素引导提示扩大目标域的学习范围，并利用DINOv3构建稳定的领域不变类原型。实验表明，该方法在GTA和SYNTHIA到Cityscapes的迁移任务上均取得了显著的性能提升，有效缓解了域间差异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03365v1/fig-001.webp\", \"caption\": \"Table 2: Semantic segmentation performance (IoU in %) on different UDA benchmarks. All methods’ results are taken from publications.\", \"page\": 11, \"index\": 1, \"width\": 725, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2605.03365v1/fig-002.webp\", \"caption\": \"Fig. 5: SAM Mask Comparison. Comparison between SAM AutoMask and our superpixel-based SAM with overlap-aware filtering.\", \"page\": 10, \"index\": 2, \"width\": 666, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2605.03365v1/fig-003.webp\", \"caption\": \"Fig. 6: t-SNE visualization on Cityscapes val set. Compared with the baseline without SAM and DINO, ours forms more compact target-feature clusters around DINOv3 prototype anchors (black crosses), indicating improved prototype-guided alignment.\", \"page\": 12, \"index\": 3, \"width\": 655, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2605.03365v1/fig-004.webp\", \"caption\": \"Fig. 1: Overview of our framework. (Blue) Source–Target Distillation: An online self-training scheme reduces the source–target domain gap using EMAupdated teacher predictions. (Yellow) Pseudo-Label Refinement: Superpixelprompted SAM masks are filtered, and each mask region is assigned a highconfidence, low-entropy pseudo-label. (Sec. 3.2 & Sec. 3.3) (Pink) Feature Alignment: Student features are projected into the same latent space as the DINOv3 prototypes and aligned with DINOv3-based class prototypes to learn domaininvariant representations, reducing source bias and improving generalization. (Sec. 3.4) All components interact cyclically, progressively improving pseudolabel quality and adaptation performance.\", \"page\": 5, \"index\": 4, \"width\": 700, \"height\": 364}]"
motivation: 传统的UDA方法受限于高置信度伪标签的局部性以及源域预训练模型带来的原型偏差。
method: 提出双基础模型框架，利用SAM进行超像素引导提示以覆盖更多目标像素，并结合DINOv3构建稳定的领域不变类原型。
result: "在GTA-to-Cityscapes和SYNTHIA-to-Cityscapes基准测试中，mIoU分别提升了1.3%和1.4%。"
conclusion: 通过整合SAM和DINOv3的互补优势，该框架显著增强了语义分割模型在无监督域适应任务中的鲁棒性和泛化能力。
---

## 摘要
语义分割提供了像素级的场景理解，这对于自动驾驶和细粒度感知任务至关重要。然而，训练分割模型需要对真实世界数据集进行昂贵且劳动密集型的标注。无监督领域自适应（UDA）通过在有标签的合成数据上训练模型并将其迁移到无标签的真实图像来解决这一问题。虽然概念上很简单，但由于领域差异（即合成数据与真实数据在视觉外观和场景结构上的差异），自适应过程具有挑战性。先前的方法通过像素级混合或特征级对比学习来弥合这一差距。然而，这些技术面临两个主要局限性：（1）对高置信度伪标签的依赖限制了模型仅能从目标域的一个子集中学习；（2）基于原型的对比方法从源域训练的模型中初始化类别原型，导致在自适应过程中产生有偏且不稳定的锚点。为了解决这些问题，我们提出了一个利用两个互补基础模型的双基础 UDA 框架。首先，我们采用 Segment Anything Model (SAM) 并结合超像素引导提示，从而能够学习超出高置信度预测范围的更广泛的目标像素。其次，我们引入 DINOv3，通过其鲁棒的表示学习构建稳定且领域不变的类别原型。我们的方法在 GTA-to-Cityscapes 和 SYNTHIA-to-Cityscapes 任务上，相较于强大的 UDA 基准模型，分别实现了 +1.3% 和 +1.4% mIoU 的持续提升。

## Abstract
Semantic segmentation provides pixel-level scene understanding essential for autonomous driving and fine-grained perception tasks. However, training segmentation models requires costly, labor-intensive annotations on real-world datasets. Unsupervised Domain Adaptation (UDA) addresses this by training models on labeled synthetic data and adapting them to unlabeled real images. While conceptually simple, adaptation is challenging due to the domain gap, i.e., differences in visual appearance and scene structure between synthetic and real data. Prior approaches bridge this gap through pixel-level mixing or feature-level contrastive learning. Yet, these techniques suffer from two major limitations: (1) reliance on high-confidence pseudo-labels restricts learning to a subset of the target domain, and (2) prototype-based contrastive methods initialize class prototypes from source-trained models, yielding biased and unstable anchors during adaptation. To address these issues, we propose a dual-foundation UDA framework that leverages two complementary foundation models. First, we employ the Segment Anything Model (SAM) with superpixel-guided prompting to enable learning from a broader range of target pixels beyond high-confidence predictions. Second, we incorporate DINOv3 to construct stable, domain-invariant class prototypes through its robust representation learning. Our method achieves consistent improvements of +1.3% and +1.4% mIoU over strong UDA baselines on GTA-to-Cityscapes and SYNTHIA-to-Cityscapes, respectively.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dual-Foundation Models for Unsupervised Domain Adaptation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在语义分割的无监督领域自适应（UDA）中，如何解决从合成数据（源域）向真实数据（目标域）迁移时的性能下降问题。
*   **研究背景**：
    *   **伪标签局限性**：现有方法通常只利用高置信度的伪标签进行自适应，这导致模型只能学习到目标域的一小部分像素，忽略了大量具有挑战性的区域。
    *   **原型偏差与不稳定**：基于特征对比学习的方法通常依赖源域模型初始化类别原型，这会导致原型带有源域偏差，且在训练过程中随教师网络波动而不稳定。
*   **整体含义**：本文旨在通过引入两个互补的视觉基础模型（SAM 和 DINOv3），利用它们强大的泛化能力来提供更可靠的监督信号和更稳定的特征对齐基准。

### 2. 方法论：双基础模型框架
该框架包含三个核心组件：
*   **超像素引导的 SAM 伪标签细化**：
    *   **核心思想**：利用 SAM 的物体分割能力将监督扩展到高置信度像素之外。
    *   **技术细节**：使用 SEEDS 算法生成超像素，并以其中心作为 SAM 的提示点（Prompt），生成结构连贯的掩码。
    *   **过滤机制**：提出“高置信度+低熵”准则。只有当一个 SAM 掩码内的像素同时满足高 Softmax 概率和高 Softmax 差值（Top-1 与 Top-2 概率之差）且类别一致时，才为整个掩码分配伪标签。
*   **基于 DINOv3 的领域无关原型构建**：
    *   **核心思想**：利用 DINOv3 提取的特征构建固定的、具有强泛化性的类别原型。
    *   **流程**：在源域上利用 Ground Truth 提取 DINOv3 特征并取平均，生成各类的原型向量。这些原型在训练期间保持固定，作为目标域特征对齐的“锚点”。
*   **特征对齐对比学习**：
    *   通过投影头将学生网络的特征映射到 DINO 空间，计算像素特征与 DINO 原型之间的温度缩放余弦相似度，并最小化原型对比损失（Prototype Contrastive Loss）。

### 3. 实验设计
*   **数据集**：
    *   **GTA → Cityscapes**：从合成游戏场景迁移到真实城市街道。
    *   **SYNTHIA → Cityscapes**：从另一种合成数据集迁移到真实场景。
*   **基准模型（Baselines）**：以 MIC (Masked Image Consistency) 为主要基准，同时在 DAFormer、HRDA、DACS 等多种 UDA 框架上验证。
*   **对比方法**：包括 ADVENT、ProDA、DAFormer、HRDA、MIC、COPT 等近年来的 SOTA 方法。

### 4. 资源与算力
*   **硬件设备**：论文提到在 **NVIDIA RTX 4070** GPU 上测量了离线掩码生成速度和在线训练吞吐量。
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 2。
*   **效率说明**：相比于 SAM 原生的自动掩码生成（AutoMask），本文提出的超像素引导方法减少了 13.9% 的提示点，提升了 15.2% 的在线训练吞吐量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流 UDA 任务上进行了完整的性能对比。
    *   针对不同网络架构（DeepLabV2, DAFormer）和不同 UDA 方法（DACS, DAFormer, HRDA, MIC）进行了交叉验证。
    *   进行了详细的消融实验，分别验证了 SAM 细化和 DINO 原型对齐的贡献。
*   **充分性评价**：实验设计较为充分，涵盖了从定量指标（mIoU）到定性可视化（t-SNE、分割图对比）的多个维度。通过在多种 Baseline 上叠加实验，证明了该方法的通用性和客观性。

### 6. 主要结论与发现
*   **性能提升**：在 GTA → Cityscapes 上 mIoU 提升了 **1.3%**，在 SYNTHIA → Cityscapes 上提升了 **1.4%**。
*   **难类优化**：该方法对“火车”、“自行车”、“围栏”等稀有或视觉模糊的类别提升显著。
*   **特征聚类**：t-SNE 可视化证明，引入 DINO 原型后，目标域的特征分布更加紧凑且围绕在类别锚点周围。
*   **效率优化**：超像素引导的 SAM 提示策略不仅提高了掩码的语义质量，还降低了计算开销。

### 7. 优点与亮点
*   **互补性设计**：巧妙结合了 SAM（提供空间/物体先验）和 DINO（提供语义/特征先验）的优势。
*   **解决原型偏差**：通过固定 DINO 提取的领域无关原型，有效避免了传统方法中源域偏差导致的“原型崩溃”问题。
*   **即插即用**：该框架不改变原有的分割网络结构，可以轻松集成到现有的自训练 UDA 流程中。

### 8. 不足与局限
*   **计算开销**：虽然优化了 SAM 的效率，但引入两个大型基础模型（SAM 和 DINOv3）作为辅助，在预处理阶段或多阶段训练中仍比纯端到端方法复杂。
*   **依赖基础模型质量**：方法的上限受限于 SAM 的分割精度和 DINOv3 的特征表达能力，若基础模型在某些特殊领域（如医学影像）表现不佳，该方法可能失效。
*   **超参数敏感性**：引入了新的阈值（如 $\tau'$ 熵阈值）和损失权重 $\lambda$，可能需要针对不同数据集进行微调。

（完）
