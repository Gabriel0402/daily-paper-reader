---
title: Optimal Planning and Control under Signal Temporal Logic Specifications
title_zh: 信号时序逻辑规范下的最优规划与控制
authors: "Zuodong Pan, Xu Fang, Wei Ren"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24107v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 规划与控制中的信号时序逻辑规范
tldr: 本研究针对非线性系统在信号时序逻辑（STL）约束下的规划与控制问题，提出了一种基于任务分解与采样合成的新框架。该方法首先将复杂的STL任务分解为局部任务，利用采样算法生成局部航点序列并构建安全走廊，最后通过求解优化问题获得满足全局任务的轨迹。该方法有效解决了非线性系统在复杂逻辑约束下的路径规划难题，并通过仿真验证了其高效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24107v1/fig-001.webp\", \"caption\": \"Fig. 3. Illustration of the simulation results in Scenario 1. Here, πr := R(µr), r ∈ {1, . . . , 6}. (a) The results for generating the global waypoints, satisfaction pairs, and safe corridor. (b) The control inputs of the unicycle. (c) The trajectory of the unicycle.\", \"page\": 7, \"index\": 1, \"width\": 1066, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.24107v1/fig-002.webp\", \"caption\": \"Fig. 1. Overall architecture of the proposed approach. (a) The STL task Ψ is decomposed into Ψ1 and Ψ2 with uati(Ψ1) = [ξ0, ξ1] and uati(Ψ2) = [ξ1, ξ2], respectively. (b) The sequences of local waypoints (orange and green points) satisfying Ψ1 and Ψ2 are generated and then synthesized into the sequence of global waypoints. Blue points are the satisfaction pairs with respect to Ψ. (c) Based on the sequence of global waypoints, a safe corridor (red blocks) is constructed. An optimal position trajectory (red line) satisfying Ψ is derived.\", \"page\": 3, \"index\": 2, \"width\": 1066, \"height\": 284}]"
motivation: 旨在解决非线性系统在满足复杂信号时序逻辑（STL）规范时的运动规划与控制难题。
method: 采用任务分解策略，结合采样算法生成局部航点并构建安全走廊，最终通过优化模型求解满足STL约束的全局轨迹。
result: 数值仿真和对比实验结果表明，该方法能够有效生成满足复杂逻辑任务的平滑轨迹。
conclusion: 该研究提出了一种从局部到全局的规划框架，为非线性系统在时序逻辑约束下的最优控制提供了可行方案。
---

## 摘要
本文研究了信号时序逻辑（STL）规范下非线性系统的规划与控制问题。我们首先将一个 STL 任务分解为有限个局部任务。采用一种基于采样的方法生成局部路标点序列以满足所有局部任务，并由此推导出相应的满足对集合。遵循从局部到全局的策略，将所有局部路标点序列综合为一个全局序列，并据此构建安全走廊。利用安全走廊和满足对集合，构建并求解一个优化问题，以推导出满足 STL 任务的位置轨迹。最后，通过数值算例和对比结果证明了所提方法的有效性。

## Abstract
This paper addresses the planning and control problem for nonlinear systems under Signal Temporal Logic (STL) specifications. We first decompose an STL task into finite local tasks. A sampling-based method generates sequences of local waypoints to satisfy all local tasks, from which the corresponding satisfaction pair sets are derived. Following a local-to-global strategy, all sequences of local waypoints are synthesized into a global one, based on which a safe corridor is then constructed. Leveraging the safe corridor and the satisfaction pair sets, an optimization problem is formulated and solved to derive a position trajectory that satisfies the STL task. Finally, numerical examples and comparative results are presented to demonstrate the efficacy of the proposed approach.

---

## 论文详细总结（自动生成）

这篇论文《Optimal Planning and Control under Signal Temporal Logic Specifications》提出了一种针对非线性系统在复杂逻辑约束下的运动规划与控制框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着移动机器人任务复杂度的提升，传统的简单任务（如追踪、稳定）已转向包含顺序、覆盖、循环等特征的复杂任务。信号时序逻辑（STL）因其能表达显式时间约束和定量鲁棒性而成为描述此类任务的首选。
*   **核心挑战**：现有的方法（如采样法、控制屏障函数 CBF、混合整数线性规划 MILP）在处理“嵌套”STL公式（例如“在某段时间内必须至少访问某地一次”的巡逻任务 $GF$）时存在计算爆炸或难以求解的问题，且非线性动力学系统的实时约束满足依然困难。
*   **研究目标**：开发一种能够处理包含非嵌套和嵌套 STL 规范的非线性系统最优规划与控制方法。

### 2. 方法论
该论文提出了一种“三步走”的**局部到全局（Local-to-Global）**策略：
1.  **STL 任务分解**：
    *   将全局联合应用时间间隔（UATI）划分为多个局部时间段。
    *   将复杂的全局 STL 任务分解为有限个局部子任务。对于“总是（G）”类型的任务进行拆分，对于“最终（F）”类型的任务则根据采样结果动态分配。
2.  **局部到全局规划**：
    *   **局部采样**：在每个局部时间段内，使用**目标偏置快速探索随机树（Goal-Biased RRT）**在时空（Spatio-Temporal）空间进行采样，生成局部路径。
    *   **满足性验证**：通过算法检查采样路径是否满足局部 STL 约束，并提取“满足对集合”（Satisfaction Pair Sets），记录必须处于目标区域的时间点。
    *   **全局合成**：将各段局部路标点连接成全局路标点序列。
3.  **轨迹优化控制**：
    *   **安全走廊构建**：基于全局路标点生成一系列凸的多面体区域（安全走廊），以确保避障。
    *   **优化求解**：将 STL 约束转化为点集约束，结合安全走廊约束，构建一个非线性优化问题（NLP）。目标函数旨在最小化控制输入变化（平滑性）和轨迹长度。利用 CasADi 工具箱求解最优控制序列。

### 3. 实验设计
*   **实验场景**：使用非线性单轮车（Unicycle）动力学模型在 2D 空间内进行仿真。
*   **场景设置**：
    *   **场景 1**：包含复杂的巡逻（$GF$）、保持（$FG$）和直到（$U$）任务。
    *   **场景 2**：侧重于非嵌套 STL 公式的对比。
    *   **场景 3**：侧重于多个“到达并保持（$FG$）”任务的对比。
*   **Benchmark（对比基准）**：与基于 **MILP（混合整数线性规划）** 的经典方法（Sun et al., 2022）进行对比。

### 4. 资源与算力
*   **硬件环境**：Intel Core i5-10400 处理器，16 GB RAM。
*   **软件环境**：MATLAB R2024a，使用 CasADi 进行非线性优化求解。
*   **算力说明**：文中未提及 GPU 使用情况，所有计算均在 CPU 上完成。计算时间在 3.44s 到 5.30s 之间，显示出较好的实时潜力。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 3 组代表性场景的仿真实验。
*   **充分性评价**：
    *   **客观性**：通过与 MILP 方法在计算时间、轨迹长度等量化指标上的对比，客观展示了所提方法的优势。
    *   **覆盖度**：实验涵盖了 MILP 无法处理的嵌套逻辑（场景 1），证明了该方法在逻辑表达处理能力上的突破。
    *   **局限性**：实验主要集中在单机器人的确定性环境下，缺乏多机器人协同或动态障碍物环境下的压力测试。

### 6. 主要结论与发现
*   **逻辑处理能力更强**：所提方法能够处理 MILP 方法无法直接求解的 $GI FI$（巡逻）等复杂嵌套逻辑。
*   **效率与质量双优**：在处理相同任务时，该方法生成的轨迹比 MILP 方法更短（例如场景 3 中缩短了约 27%），且计算时间显著降低（场景 3 中从 10.66s 降至 3.44s）。
*   **可行性**：通过安全走廊和优化控制的结合，能够确保非线性系统在满足复杂逻辑的同时实现平滑避障。

### 7. 优点
*   **任务分解策略**：通过将长时程任务分解为局部任务，有效缓解了采样算法在长时程规划中的搜索空间爆炸问题。
*   **解耦规划与控制**：先通过采样获得拓扑正确的路标点，再通过优化提升轨迹质量，兼顾了逻辑满足性和动力学最优性。
*   **通用性**：方法不依赖于特定的线性化假设，适用于一般的非线性系统。

### 8. 不足与局限
*   **采样随机性**：基于 RRT 的规划具有随机性，虽然最终有优化步骤，但初始路标点的质量仍会影响全局最优性。
*   **静态环境假设**：实验环境为静态障碍物，未讨论在动态、不确定环境下的鲁棒性。
*   **计算开销**：虽然比 MILP 快，但对于极高频的实时控制，非线性优化的求解时间可能仍是一个瓶颈。
*   **参数敏感性**：任务分解的时间点选择和 RRT 采样参数可能需要针对不同场景进行人工调优。

（完）
