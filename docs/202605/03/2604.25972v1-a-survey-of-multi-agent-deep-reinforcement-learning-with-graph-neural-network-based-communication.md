---
title: A Survey of Multi-Agent Deep Reinforcement Learning with Graph Neural Network-Based Communication
title_zh: 基于图神经网络通信的多智能体深度强化学习综述
authors: "Valentin Cuzin-Rambaud, Laetitia Matignon, Maxime Morge"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25972v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 基于GNN通信的多智能体深度强化学习综述
tldr: 本综述探讨了多智能体强化学习（MARL）中基于图神经网络（GNN）的通信机制。在MARL中，智能体通过共享信息来协调行动，而GNN能有效处理交互图中的信息交换。针对该领域缺乏统一分类框架的问题，本文系统性地回顾了近期研究，并提出了一个通用的基于GNN的通信流程，旨在使相关方法的核心概念更易于理解和应用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25972v1/fig-001.webp\", \"caption\": \"Figure 1: An example of MPNN: the green node, aggregates features from its 1-hop neighbors ∈ N (1)\", \"page\": 3, \"index\": 1, \"width\": 493, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.25972v1/fig-002.webp\", \"caption\": \"Table 1: Taxonomy of GNN-based communication methods, organized in two categories: proxy-based (upper part) and distributed-based (lower part) according to the generic Algorithm 2. E(·) encodes the local representation, N t r (·) defines reachable agents, Gbuild(·) builds a graph, GNN is the architecture used, and Inner integration shows how to leverage the final representation.\", \"page\": 7, \"index\": 2, \"width\": 1037, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2604.25972v1/fig-003.webp\", \"caption\": \"Table 2: Tested communication constraints during execution\", \"page\": 9, \"index\": 3, \"width\": 469, \"height\": 335}]"
motivation: 针对多智能体强化学习中基于图神经网络的通信方法缺乏明确的分类结构和统一框架的问题。
method: 调研了该领域的近期研究工作，并提出了一个通用的基于GNN的通信处理流程。
result: 成功对现有的基于GNN的MARL通信方法进行了系统化分类，并阐明了其底层核心概念。
conclusion: 本文为研究人员提供了清晰的GNN通信框架参考，有助于推动多智能体协同学习领域的进一步发展。
---

## 摘要
在多智能体强化学习（MARL）中，通信机制的集成使智能体能够通过共享信息更好地学习协调其动作并收敛于其目标。基于交互图，一类子方法采用图神经网络（GNN）来学习通信，使智能体能够通过交换的信息来丰富并改进其内部表示。随着研究的不断深入，我们注意到目前缺乏明确的结构和框架来区分和分类基于 GNN 通信的 MARL 方法。因此，本文对该领域的近期工作进行了综述。我们提出了一个通用的基于 GNN 的通信过程，旨在使这些方法背后的底层概念更加清晰易懂。

## Abstract
In multi-agent reinforcement learning (MARL), the integration of a communication mechanism, allowing agents to better learn to coordinate their actions and converge on their objectives by sharing information. Based on an interaction graph, a subclass of methods employs graph neural networks (GNNs) to learn the communication, enabling agents to improve their internal representations by enriching them with information exchanged. With growing research, we note a lack of explicit structure and framework to distinguish and classify MARL approaches with communication based on GNNs. Thus, this paper surveys recent works in this field. We propose a generalized GNN-based communication process with the goal of making the underlying concepts behind the methods more obvious and accessible.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Survey of Multi-Agent Deep Reinforcement Learning with Graph Neural Network-Based Communication》（基于图神经网络通信的多智能体深度强化学习综述）的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在多智能体强化学习（MARL）中，智能体面临**部分可观测性**（Partial Observability）和**环境非平稳性**（Non-stationarity）的挑战。通信是解决这些问题的关键手段。
*   **核心问题**：近年来，利用图神经网络（GNN）处理智能体间通信的研究激增，但该领域缺乏统一的分类框架和通用的算法流程，导致不同方法之间的关系模糊。
*   **研究动机**：本文旨在通过系统性综述，定义一个通用的 GNN 通信模型，对现有主流方法进行分类（代理通信 vs. 分布式通信），并探讨现实通信约束对算法的影响。

### 2. 论文提出的方法论
论文提出了一个**通用的基于 GNN 的通信处理流程**（Generalized GNN-based Communication Process），将其抽象为四个关键阶段：
1.  **消息编码（Message Encoding）**：智能体利用编码器 $E(\cdot)$（如 MLP、RNN）将原始局部观测 $o_i$ 转换为初始特征向量 $x_i$。
2.  **通信决策（Communication Decision）**：
    *   **代理模式（Proxy）**：所有智能体将消息发往中央代理，由代理构建全局图。
    *   **分布式模式（Distributed）**：智能体仅与通信范围（$N^{tr}$）内的邻居交换信息。
3.  **消息组合（Message Combination）**：
    *   利用 GNN 的**消息传递机制（MPNN）**进行聚合（Aggregation）和转换（Transformation）。
    *   支持**多轮通信**（Multi-round）：通过堆叠 $L$ 层 GNN，信息可以传播到 $L$ 跳之外的智能体，实现超越物理通信范围的信息共享。
4.  **内部集成（Inner Integration）**：将 GNN 输出的通信感知表示 $h_i^L$ 集成到策略网络（Policy）或价值网络（Value Function）中。

### 3. 实验设计
作为一篇综述论文，其实验部分主要体现为对 **12 种代表性算法**的横向对比分析：
*   **分析对象**：包括 DICG, GA-Comm, MAGIC, GACG, LTS-CG（代理类）以及 DGN, LSC, MAGE-X, MAGEC, GPPO, HetNet（分布式类）。
*   **测试场景（Benchmark）**：文中提到的算法通常在 Predator-Prey（捕食者-猎物）、Patrolling（巡逻）、Navigation（导航）和 SMAC（星际争霸挑战赛）等环境中验证。
*   **对比维度**：图构建方式（完全图、稀疏图、动态图）、GNN 架构（GCN、GAT、GraphSAGE）、通信约束处理能力。

### 4. 资源与算力
*   **算力说明**：由于本文是**综述性论文**，并未进行新的实验运行，因此**未提及**具体的 GPU 型号、数量或训练时长。文章侧重于对已有文献的技术参数和实验设置进行归纳。

### 5. 实验数量与充分性
*   **实验充分性评价**：
    *   本文通过两个核心表格（Table 1 和 Table 2）对 12 种方法进行了多维度对比。
    *   **Table 1** 详尽分类了各方法的编码器、通信范围、图构建函数、GNN 类型及集成方式，覆盖了算法设计的全生命周期。
    *   **Table 2** 专门分析了各方法对**现实约束**（如通信范围限制、智能体规模、带宽限制、噪声、丢包）的测试情况。
*   **客观性**：作者客观地指出了大多数现有研究在处理“丢包”和“带宽限制”等真实物理约束方面的不足。

### 6. 论文的主要结论与发现
*   **GNN 的优越性**：GNN 天然适合处理 MARL 中的动态交互结构，通过多层堆叠可以有效缓解局部观测限制。
*   **范式差异**：代理通信（Proxy）虽能提供全局视角，但依赖完美通信假设；分布式通信更具扩展性和鲁棒性，更适合实际部署。
*   **时间与结构结合**：发现将时间信息（RNN）与空间结构信息（GNN）结合（如 LTS-CG）是提升协调性能的有效趋势。
*   **现实鸿沟**：大多数算法在仿真中表现良好，但在面对非理想通信环境（有噪声、带宽受限）时仍缺乏足够的鲁棒性。

### 7. 优点
*   **框架通用性**：提出的通用算法流程（Algorithm 2 & 3）高度抽象，能够兼容绝大多数基于 GNN 的 MARL 变体。
*   **分类严谨**：清晰地界定了“代理通信”与“分布式通信”的边界，有助于研究者根据应用场景选择合适的架构。
*   **关注实战**：不仅关注算法性能，还特别强调了通信范围、带宽和噪声等物理约束，具有较强的工程指导意义。

### 8. 不足与局限
*   **缺乏定量对比**：由于各原始论文的实验设置（如超参数、硬件、环境版本）不统一，综述未能提供这些算法在统一基准下的量化性能排名。
*   **复杂度分析不足**：未深入探讨不同 GNN 架构带来的计算开销（计算复杂度）与通信开销（通信量）之间的权衡。
*   **新兴技术覆盖**：对于最新的大语言模型（LLM）辅助通信或变分推断在通信编码中的应用涉及较少。

（完）
