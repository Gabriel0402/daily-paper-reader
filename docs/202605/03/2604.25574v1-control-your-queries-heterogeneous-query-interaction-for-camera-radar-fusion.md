---
title: "Control Your Queries: Heterogeneous Query Interaction for Camera-Radar Fusion"
title_zh: 控制你的查询：用于相机-雷达融合的异构查询交互
authors: "Jialong Wu, Yihan Wang, Matthias Rottmann"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25574v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 自动驾驶的多模态感知与融合
tldr: 本研究针对自动驾驶中的相机-雷达融合问题，提出了名为ConFusion的3D目标检测框架。该框架引入了异构查询交互范式，通过结合图像查询、雷达查询和可学习的世界查询来优化初始化。核心贡献包括用于跨类型交互的QMix模块和用于特征交换的QSwap采样机制，显著提升了多模态特征融合的效率。在nuScenes数据集上，该方法达到了SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25574v1/fig-001.webp\", \"caption\": \"Table 2: Comparison with existing methods on the nuScenes val set. C: camera, R: radar.\", \"page\": 7, \"index\": 1, \"width\": 820, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2604.25574v1/fig-002.webp\", \"caption\": \"Table 1: Camera-3D sensor fusion methods for object detection. C: camera; L: LiDAR; R: radar.\", \"page\": 3, \"index\": 2, \"width\": 781, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2604.25574v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of ConFusion. ConFusion first extracts multi-modal feature maps from camera and radar inputs, and then initializes heterogeneous image, radar, and world queries. The queries are decoded by an L-layer transformer decoder, where QSwap enables constrained token sharing during feature sampling, while QMix performs dedicated cross-type attention after feature aggregation.\", \"page\": 4, \"index\": 3, \"width\": 825, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.25574v1/fig-004.webp\", \"caption\": \"Figure 5: Qualitative results on the nuScenes val set under challenging scenarios. Left: projected 3D detections on multi-view images. Right: BEV radar points, predictions in blue, ground truth in green.\", \"page\": 9, \"index\": 4, \"width\": 817, \"height\": 282}, {\"url\": \"assets/figures/arxiv/2604.25574v1/fig-005.webp\", \"caption\": \"Figure 6: QMix cross-type attention links. High-confidence boxes and their top-2 links are visualized.\", \"page\": 9, \"index\": 5, \"width\": 790, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.25574v1/fig-006.webp\", \"caption\": \"Figure 3: Heterogeneous query initialization and post-training distributions across decoder layers.\", \"page\": 5, \"index\": 6, \"width\": 664, \"height\": 228}]"
motivation: 现有的相机-雷达融合方法在多模态特征交互和查询初始化方面仍存在不足，难以充分利用传感器的互补性。
method: 提出了ConFusion框架，通过异构查询混合（QMix）和交互式查询交换采样（QSwap）实现图像、雷达与世界查询之间的深度交互。
result: 在nuScenes验证集上达到了59.1 mAP和65.6 NDS，在测试集上达到了61.6 mAP和67.9 NDS，刷新了SOTA记录。
conclusion: 异构查询交互范式能有效整合相机与雷达的互补信息，显著提升了3D目标检测的准确性和鲁棒性。
---

## 摘要
在自动驾驶中，相机-雷达融合提供了互补的感知能力和较低的部署成本。现有方法通过输入混合、特征图混合或基于查询的特征采样进行融合。我们提出了一种新的融合范式，称为异构查询交互，并展示了 ConFusion，一种相机-雷达 3D 目标检测器。ConFusion 结合了图像查询、雷达查询和分布在 3D 空间中的可学习世界查询，以改进查询初始化和目标覆盖范围。为了鼓励异构查询之间的跨类型交互，我们引入了异构查询混合（QMix），它在特征采样后执行专门的跨类型注意力机制，以巩固互补的目标证据。我们进一步提出了交互式查询交换采样（QSwap），通过允许相关查询在注意力和几何约束下交换信息丰富的特征令牌，从而改进特征采样。在 nuScenes 数据集上的实验表明，ConFusion 达到了最先进的性能，在验证集上达到 59.1 mAP 和 65.6 NDS，在测试集上达到 61.6 mAP 和 67.9 NDS。

## Abstract
In autonomous driving, camera-radar fusion offers complementary sensing and low deployment cost. Existing methods perform fusion through input mixing, feature map mixing, or query-based feature sampling. We propose a new fusion paradigm, termed heterogeneous query interaction, and present ConFusion, a camera-radar 3D object detector. ConFusion combines image queries, radar queries, and learnable world queries distributed in 3D space to improve query initialization and object coverage. To encourage cross-type interaction among heterogeneous queries, we introduce heterogeneous query mixing (QMix), which performs dedicated cross-type attention after feature sampling to consolidate complementary object evidence. We further propose interactive query swap sampling (QSwap), which improves feature sampling by allowing related queries to exchange informative feature tokens under attention and geometric constraints. Experiments on the nuScenes dataset show that ConFusion achieves state-of-the-art performance, reaching 59.1 mAP and 65.6 NDS on the validation set, and 61.6 mAP and 67.9 NDS on the test set.

---

## 论文详细总结（自动生成）

这篇论文提出了名为 **ConFusion** 的相机-雷达 3D 目标检测框架，以下是对该研究的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在自动驾驶领域，相机与雷达的融合因其低成本和互补性（相机提供语义，雷达提供速度和几何信息）而备受关注。
*   **核心问题**：现有的融合方法（如输入混合、特征图混合或同构查询采样）在处理相机和雷达的强异构性时存在局限。
*   **研究动机**：作者发现，即使是使用多种查询初始化的方法，在 Transformer 解码器中也往往依赖标准的自注意力机制。这种机制存在“同类型偏差”（Same-type bias），即查询更倾向于关注同类型的查询，导致跨模态的信息交换不充分，限制了融合效果。

### 2. 方法论
ConFusion 提出了一种**异构查询交互（Heterogeneous Query Interaction）**的新范式，核心思想是通过显式控制不同来源查询之间的交互来增强融合。
*   **异构查询初始化**：
    *   **图像查询（Image Queries）**：从 2D 检测提案中初始化，捕捉丰富的语义。
    *   **雷达查询（Radar Queries）**：从雷达 BEV 热图中初始化，利用速度和几何先验。
    *   **世界查询（World Queries）**：在 3D 空间中可学习的查询，用于覆盖前两者可能漏检的目标。
*   **关键技术模块**：
    1.  **QMix（异构查询混合）**：在特征聚合后执行。它使用掩码多头注意力机制，**禁止同类型查询之间的交互**（除了自关注），强制每个查询仅与另外两种类型的异构查询交换信息，从而巩固互补的目标证据。
    2.  **QSwap（交互式查询交换采样）**：在特征采样阶段执行。它允许查询在注意力和几何约束（查询自适应半径）下，从邻近的相关查询中“交换”或借用高分的特征令牌（Tokens），从而提高采样证据的质量。

### 3. 实验设计
*   **数据集**：使用自动驾驶权威数据集 **nuScenes**。
*   **Benchmark**：对比了该数据集上的 3D 目标检测排行榜。
*   **对比方法**：包括相机单模态方法（如 Sparse4D）、相机-雷达融合方法（如 RCBEVDet, CRN, RaCFormer, SpaRC 等）。
*   **评价指标**：主要使用 mAP（平均精度）和 NDS（nuScenes 检测得分），以及平移、缩放、方向、速度等误差指标。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA A100 GPU**。
*   **训练细节**：
    *   ResNet-50 版本训练了 36 个 epoch。
    *   ResNet-101 和 VoVNet 版本训练了 24 个 epoch。
    *   使用了 AdamW 优化器，学习率为 $4 \times 10^{-4}$，Batch Size 为 8。
    *   2D 预检测器和雷达热图头仅预训练了 6 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文在验证集（Val）和测试集（Test）上均进行了详尽对比，并提供了大量的消融实验。
*   **消融实验内容**：涵盖了组件贡献分析（QInit, QMix, QSwap）、查询数量配比、QMix 的插入策略（聚合前 vs 聚合后）、QSwap 的变体（半径选择、采样点数量）等。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了最终精度，还通过可视化注意力链路（Attention Links）直观展示了 QMix 如何打破同类型偏差，增强了结论的说服力。

### 6. 主要结论与发现
*   **性能领先**：ConFusion 在 nuScenes 验证集上达到 **59.1 mAP** 和 **65.6 NDS**，在测试集上达到 **61.6 mAP** 和 **67.9 NDS**，刷新了 SOTA 记录。
*   **交互的重要性**：显式的跨类型交互（QMix）比标准的自注意力机制能更有效地降低方向（mAOE）和平移（mATE）误差。
*   **采样优化**：QSwap 通过在几何邻域内共享特征令牌，进一步提升了检测的鲁棒性，尤其是在复杂场景下。

### 7. 优点
*   **范式创新**：从被动的“特征采样”转向主动的“异构查询交互”，解决了 Transformer 在多模态任务中的归纳偏置问题。
*   **效率与性能并重**：相比于一些需要 60 个 epoch 训练的方法（如 SpaRC），ConFusion 在更短的训练周期内达到了更高的精度。
*   **鲁棒性强**：在夜间、雨天和密集交通等挑战性场景下表现优异。

### 8. 不足与局限
*   **几何敏感性**：QSwap 模块在透视视图（PV）采样中表现不佳，因为投影采样对几何扰动过于敏感，目前仅适用于 BEV 采样。
*   **架构复杂性**：引入了三种查询来源和多个交互模块，增加了模型实现的复杂度和超参数调优的难度（如 QSwap 的半径因子和权重）。
*   **监督策略**：作者提到未来仍需探索更好的标签分配策略或语义对齐方法，以进一步优化异构查询的训练。

（完）
