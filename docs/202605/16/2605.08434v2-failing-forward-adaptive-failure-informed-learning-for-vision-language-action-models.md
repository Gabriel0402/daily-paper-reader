---
title: "Failing Forward: Adaptive Failure-Informed Learning for Vision-Language-Action Models"
title_zh: 败中求进：针对视觉-语言-动作模型的自适应失败告知学习
authors: "Meng Zheng, Samhita Marri, Anwesa Choudhuri, Benjamin Planche, Zhongpai Gao, Van Nguyen Nguyen, Terrence Chen, Girish Chowdhary, Ziyan Wu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08434v2"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 视觉-语言-动作模型的故障告知学习
tldr: 针对视觉-语言-动作（VLA）模型在行为克隆中因缺乏纠错信号而导致鲁棒性差的问题，本文提出自适应失败告知学习（AFIL）框架。该方法利用预训练模型在线生成失败轨迹作为负向引导，通过共享骨干网络的双动作生成器同时学习成功与失败行为。在采样阶段，模型根据分布差异自适应地引导动作远离失败区域，显著提升了机器人在复杂任务中的成功率和抗干扰能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08434v2/fig-001.webp\", \"caption\": \"Figure 3: Visualization of in-domain (left) and out-of-domain (right) manipulation task setup. Row 1: exterior camera image, row 2: wrist camera image. See supplementary for more visualizations.\", \"page\": 8, \"index\": 1, \"width\": 738, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-002.webp\", \"caption\": \"Figure 2: Dual Action Generator (DAG)-VLA with Adaptive Failure-Informed Learning (AFIL).\", \"page\": 5, \"index\": 2, \"width\": 824, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-003.webp\", \"caption\": \"Table 4: Performance comparison on out-of-domain manipulation tasks. Success rates (%) reported over 25 configs × 3 runs for each task.\", \"page\": 9, \"index\": 3, \"width\": 764, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-004.webp\", \"caption\": \"Figure 1: Overview of the proposed Adaptive Failure-Informed Learning (AFIL) pipeline. AFIL uses online-generated failure trajectories as adaptive negative guidance, steering action generation away from failure-prone regions and toward more reliable success modes.\", \"page\": 2, \"index\": 4, \"width\": 782, \"height\": 293}]"
motivation: 现有的VLA模型主要依赖成功示例进行行为克隆，导致其在面对执行误差时缺乏纠错能力，容易产生复合错误。
method: 提出AFIL框架，通过在线生成失败轨迹并利用双动作生成器在扩散模型采样过程中实施自适应负向引导。
result: 实验表明，AFIL在多种机器人操纵任务（包括长短时程及域外场景）中均显著优于现有VLA基准模型。
conclusion: AFIL通过有效利用失败数据提供负向反馈，为提升视觉-语言-动作模型的鲁棒性和泛化性提供了一种高效且通用的方案。
---

## 摘要
视觉-语言-动作（VLA）模型为可扩展的机器人操作提供了一种极具前景的范式，但其对仅包含成功案例的行为克隆的依赖使其显得十分脆弱；由于缺乏纠错训练信号，微小的执行误差会迅速累积成不可恢复的分布外失败。为了解决这一局限性，我们提出了自适应失败告知学习（AFIL），这是一个端到端框架，利用失败轨迹作为基于扩散（diffusion）和流（flow）的 VLA 策略的自适应负向引导。AFIL 使用预训练的 VLA 在线生成失败展开，从而避免了对手工设计失败模式或人机回环恢复的需求。随后，它在共享通用视觉-语言主干网络的同时，联合训练针对成功和失败行为的双动作生成器（DAGs），从而以有限的参数开销实现高效的失败感知策略学习。在采样过程中，失败生成器会自适应地引导动作生成远离易出错区域，并向更可靠的成功模式靠拢，引导强度由每个扩散步骤中成功与失败分布之间的距离决定。在涵盖短时程和长时程设置的域内及跨域机器人操作任务上的实验表明，AFIL 相比现有的 VLA 基准模型，一致地提高了任务成功率和鲁棒性，证明了其有效性、高效性和通用性。

## Abstract
Vision-language-action (VLA) models provide a promising paradigm for scalable robotic manipulation, yet their reliance on success-only behavioral cloning leaves them brittle; lacking corrective training signals, minor execution errors rapidly compound into unrecoverable, out-of-distribution failures. To address this limitation, we propose Adaptive Failure-Informed Learning (AFIL), an end-to-end framework that leverages failure trajectories as adaptive negative guidance for diffusion- and flow-based VLA policies. AFIL uses a pretrained VLA to generate failure rollouts online, avoiding the need for handcrafted failure-mode design or human-in-the-loop recovery. It then jointly trains Dual Action Generators (DAGs) for successful and failed behaviors while sharing a common vision-language backbone, enabling efficient failure-aware policy learning with limited parameter overhead. During sampling, the failure generator adaptively steers action generation away from failure-prone regions and toward more reliable success modes, with guidance strength determined by the per-diffusion-step distance between success and failure distributions. Experiments across in-domain and out-of-domain robotic manipulation tasks, covering both short- and long-horizon settings, show that AFIL consistently improves task success rates and robustness over existing VLA baselines, demonstrating its effectiveness, efficiency, and generality.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AFIL (Adaptive Failure-Informed Learning)** 的框架，旨在通过利用“失败经验”来增强机器人视觉-语言-动作（VLA）模型的鲁棒性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 VLA 模型（如 OpenVLA, $\pi_0$）主要依赖“仅成功”的演示数据进行行为克隆（Behavioral Cloning）。这种方法导致模型在面对微小执行误差时，由于缺乏纠错信号，误差会迅速累积，最终导致不可逆转的失败。
*   **研究背景**：传统的失败学习方法通常需要人工定义失败模式（如“抓取不稳”），或者依赖昂贵的人类干预。论文旨在探索一种自动化的、能利用失败轨迹作为“负面引导”的方法，使模型在推理时能主动避开易错区域。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将失败轨迹视为“负向提示（Negative Prompting）”，在扩散（Diffusion）或流匹配（Flow-matching）采样过程中，通过对比成功与失败的动作分布，引导模型生成更安全的动作。
*   **关键技术细节**：
    *   **双动作生成器 (DAG)**：在共享同一个预训练 VLM（如 SigLIP + Vicuna）骨干网络的基础上，并行训练两个轻量级的动作头：一个学习成功动作分布 $p_{succ}$，另一个学习失败动作分布 $p_{fail}$。
    *   **在线失败数据生成**：利用预训练 VLA 模型在线运行产生的失败轨迹，并结合运动规划器（Motion Planner）生成的纠错轨迹进行数据增强。
    *   **自适应失败告知采样 (AFIL)**：在推理阶段，最终的动作分值（Score）由成功分值减去失败分值得到。
    *   **自适应引导强度**：引入余弦相似度来动态调整引导权重 $\hat{\lambda}$。当成功和失败的预测方向一致时（如任务初期），减弱引导；当两者分歧较大时（如即将发生错误时），增强负向引导，迫使模型转向纠错路径。

### 3. 实验设计
*   **实验场景**：
    *   **ManiSkill 模拟环境**：包括短时程任务（堆叠方块、抬起插销）和长时程任务（摆放餐具、分类水果）。
    *   **LIBERO 基准测试**：涵盖 Spatial、Object、Goal 和 Long 四个子集，用于验证通用泛化能力。
*   **对比方法**：
    *   **Success-only**：仅使用成功数据训练的基准模型（基于 $\pi_{0.5}$）。
    *   **Success + Failure Correction**：加入纠错数据但无负向引导。
    *   **Static-FIL**：使用固定权重的失败告知引导。
    *   **SOTA 基准**：包括 Diffusion Policy, Octo, OpenVLA, CoT-VLA 和原始 $\pi_{0.5}$。
*   **评估指标**：任务成功率（Success Rate）。

### 4. 资源与算力
*   **论文说明**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **推测信息**：由于该研究基于 $\pi_{0.5}$ 架构（通常涉及数十亿参数的 VLM），且涉及在线轨迹生成和微调，可以推断其训练需要高性能计算集群（如 A100 或 H100 显卡）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ManiSkill 中，每个任务评估 150 次（50 种配置 × 3 次运行）。
    *   在 LIBERO 中，遵循标准协议，总计进行 1500 次评估（3 个随机种子 × 10 个任务 × 50 次运行）。
    *   进行了针对引导权重 $\alpha$ 的消融实验。
*   **充分性评价**：实验设计较为充分，涵盖了域内（In-domain）和域外（Out-of-distribution）场景，以及长短时程任务。对比实验包含了当前最先进的开源 VLA 模型，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能提升**：AFIL 在所有任务中均显著优于“仅成功”基准。在 ManiSkill 长时程任务中，成功率提升尤为明显（最高提升约 32%）。
*   **自适应的重要性**：自适应引导（Adaptive-FIL）比固定权重引导（Static-FIL）更有效，因为它能识别何时该忽略失败信号（如动作初期），何时该强化避障。
*   **泛化能力**：在面对未见过的物体几何形状、颜色和杂乱环境时，AFIL 表现出更强的鲁棒性。

### 7. 优点与亮点
*   **自动化程度高**：无需人工标注失败类型，通过在线 Rollout 自动获取失败数据。
*   **参数高效**：通过共享 VLM 骨干网络，仅增加极少量的动作头参数即可实现功能。
*   **即插即用**：该方法不改变底层生成逻辑，可广泛应用于各种基于扩散或流匹配的机器人策略。
*   **数学优雅性**：利用余弦相似度动态调整负向引导强度，解决了机器人动作序列中“初期重合、后期发散”的引导难题。

### 8. 不足与局限
*   **推理开销**：虽然参数增加不多，但推理时需要同时运行两个动作头并计算相似度，可能会对实时控制频率产生微小影响（文中未详细量化延迟）。
*   **失败数据的质量依赖**：如果在线生成的失败轨迹过于单一，可能无法覆盖所有潜在的错误模式。
*   **纠错轨迹的获取**：方法中提到的“纠错轨迹”依赖于运动规划器，对于某些难以用传统规划器解决的复杂接触任务，可能难以生成高质量的纠错数据。

（完）
