---
title: "UniTriGen: Unified Triplet Generation of Aligned Visible-Infrared-Label for Few-Shot RGB-T Semantic Segmentation"
title_zh: UniTriGen：面向少样本 RGB-T 语义分割的可见光-红外-标签对齐统一三元组生成
authors: "Ping Zhou, Haoyu Wang, Mengmeng Zheng, Lei Zhang, Wei Wei, Chen Ding, Fei Zhou"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14626v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 用于RGB-T语义分割的可见光-红外-标签对齐三元组
tldr: 针对RGB-T语义分割中对齐的三元组数据（可见光、红外、标签）稀缺且现有生成方法难以保持模态间一致性的问题，本文提出UniTriGen框架。该框架通过统一的扩散模型在共享潜空间内联合生成空间对齐且语义一致的三元组，并结合模态残差适配器和场景平衡采样策略。实验证明，该方法能从少量样本中生成高质量数据，显著提升多种分割模型的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14626v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison between existing paradigms and the proposed UniTriGen. (a) Cascaded Generation Paradigms that progressively assemble the target triplet through intermediate outputs. (b) Our UniTriGen directly generates spatially aligned, semantically consistent, and modality complementary VIS-IR-Label triplets from text prompts in an end-to-end manner.\", \"page\": 2, \"index\": 1, \"width\": 829, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2605.14626v1/fig-002.webp\", \"caption\": \"Table 1: Quantitative comparison of different methods on SemanticRT and PST900 datasets. The best results are highlighted in bold, while the second-best outcomes are denoted by underlined italic.\", \"page\": 7, \"index\": 2, \"width\": 822, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2605.14626v1/fig-003.webp\", \"caption\": \"Figure 3: Visualization of UniTriGen generation results on the SemanticRT dataset and PST900 dataset. The text prompt consists of scene and cotegories.\", \"page\": 8, \"index\": 3, \"width\": 829, \"height\": 394}, {\"url\": \"assets/figures/arxiv/2605.14626v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of the UniTriGen framework. The framework consists of two main components: Unified Triplet Generation Mechanism and SBCA Few-Shot Sampling.\", \"page\": 4, \"index\": 4, \"width\": 824, \"height\": 333}]"
motivation: 现有的级联生成方法难以保证可见光、红外和标签在空间结构及语义细节上的一致性，且对齐数据获取困难。
method: 提出一种统一的三元组生成机制，利用共享潜空间的扩散过程建模，并引入模态残差适配器与场景平衡采样策略。
result: 实验结果表明，UniTriGen能够利用有限的真实配对数据生成高质量的对齐三元组，有效增强了数据多样性。
conclusion: 该框架通过确保全局跨模态一致性，为少样本RGB-T语义分割提供了一种高效的数据增强解决方案。
---

## 摘要
RGB-T 语义分割需要严格对齐的可见光-红外-标签（VIS-IR-Label）三元组；然而，此类对齐的三元组数据在现实场景中往往十分稀缺。现有的生成式增强方法通常采用级联生成范式，将联合三元组生成分解为局部条件过程。因此，可见光、红外和标签在空间结构、语义内容和跨模态细节方面的一致性无法得到可靠维持。为了解决这一问题，我们提出了 UniTriGen，这是一个统一的三元组生成框架，在文本提示的引导下直接生成空间对齐、语义一致且模态互补的 VIS-IR-Label 三元组。UniTriGen 首先引入了一种统一的三元组生成机制，将 VIS、IR 和 Label 共同编码到一个共享的潜空间中，并利用扩散过程进行建模，以强制实现全局跨模态一致性。该机制中还进一步集成了轻量级的模态特定残差适配器，以适应特定的成像特性和输出格式。为了减轻有限配对三元组中场景和类别分布不平衡导致的生成偏差，UniTriGen 还采用了场景平衡且类别感知的少样本采样策略，旨在诱导更平衡的采样分布，并增强生成三元组的场景和类别多样性。实验表明，UniTriGen 能够从有限的真实配对数据中生成高质量的对齐三元组，从而在各种 RGB-T 语义分割模型上实现一致的性能提升。

## Abstract
RGB-T semantic segmentation requires strictly aligned VIS-IR-Label triplets; however, such aligned triplet data are often scarce in real-world scenarios. Existing generative augmentation methods usually adopt cascaded generation paradigms, decomposing joint triplet generation into local conditional processes. As a result, consistency among VIS, IR, and Label in spatial structure, semantic content, and cross-modal details cannot be reliably maintained. To address this issue, we propose UniTriGen, a unified triplet generation framework that directly generates spatially aligned, semantically consistent, and modality complementary VIS-IR-Label triplets under the guidance of text prompts. UniTriGen first introduces a unified triplet generation mechanism, where VIS, IR, and Label are jointly encoded into a shared latent space and modeled with a diffusion process to enforce global cross-modal consistency. Lightweight modality-specific residual adapters are further integrated into this mechanism to accommodate modality-specific imaging characteristics and output formats. To mitigate generation bias caused by imbalanced scene and class distributions in limited paired triplets, UniTriGen also employs a scene-balanced and class-aware few-shot sampling strategy, which induces a more balanced sampling distribution and enhances the scene and class diversity of generated triplets. Experiments show that UniTriGen generates high-quality aligned triplets from limited real paired data, thereby achieving consistent performance improvements across various RGB-T semantic segmentation models.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《UniTriGen: Unified Triplet Generation of Aligned Visible-Infrared-Label for Few-Shot RGB-T Semantic Segmentation》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
RGB-T 语义分割依赖于严格对齐的“可见光（VIS）-红外（IR）-语义标签（Label）”三元组数据。然而，在现实中获取此类对齐且带有像素级标注的数据成本极高，导致现有数据集规模较小。
*   **现有方法的缺陷**：目前的生成式增强方法多采用“级联范式”（例如：先生成可见光图，再翻译成红外图，最后生成标签）。这种分步处理导致误差累积，难以维持三者在空间结构、语义边界和跨模态细节上的一致性。
*   **研究目标**：提出一种统一的生成框架，能够直接从文本提示（Text Prompt）出发，端到端地生成空间对齐、语义一致且模态互补的三元组数据，以解决少样本（Few-Shot）场景下的训练难题。

### 2. 方法论：核心思想与关键技术
UniTriGen 的核心思想是将三元组视为一个整体，在共享的潜空间内进行联合建模。

*   **统一三元组生成机制 (Unified Triplet Generation Mechanism)**：
    *   **联合潜空间建模**：使用三个独立的 VAE 编码器将 VIS、IR 和 Label 映射到潜空间，并将它们的潜变量拼接成一个统一的表示 $z_0 = [z_V; z_I; z_L]$。
    *   **联合扩散过程**：基于 UniDiffuser 架构，在扩散过程中对拼接后的潜变量同时加噪和去噪。通过 U-ViT 骨干网络中的自注意力机制，模型能够学习到不同模态间的空间对应关系和语义耦合。
    *   **模态特定残差适配器 (Residual Adapters)**：由于不同模态的解码目标不同（VIS 关注纹理，IR 关注热响应，Label 关注离散区域），在解码前引入轻量级适配器对预测的潜变量进行微调，以提升各模态的保真度。
*   **场景平衡且类别感知 (SBCA) 采样策略**：
    *   **结构化提示词**：将文本提示格式化为“场景描述 + 类别列表”，显式引导模型感知环境和物体。
    *   **显式场景编码**：引入可学习的场景 Token，通过聚类方法区分不同的场景类型。
    *   **分层重采样**：根据场景频率和类别稀缺程度计算采样权重，增加长尾类别（如行人、自行车）在生成数据中的多样性。

### 3. 实验设计
*   **数据集与场景**：
    *   **SemanticRT**：使用 5% 的数据（仅 342 个训练对）模拟极端少样本场景。
    *   **PST900**：使用 50% 的数据（299 个训练对）。
*   **Benchmark 与对比方法**：
    *   **下游分割模型**：ResNet152, M-SpecGene, Sigma (基于 Mamba), MilNet (基于 Segformer)。
    *   **生成对比基准**：构建了三种级联基准：Seq-Cascade（顺序生成）、JointLabel-Cascade（图-标联合后转红外）、TriCond-Cascade（三条件级联）。
*   **评估指标**：主要使用平均交并比 (mIoU) 评估生成数据对下游分割任务的提升效果。

### 4. 资源与算力
*   **算力说明**：论文中提到使用了 AdamW 优化器进行 VAE 和扩散模型的训练，并对图像进行了 512x512 的尺寸处理。
*   **缺失信息**：**文中未明确给出具体的 GPU 型号（如 A100 或 V100）、显卡数量以及具体的训练时长（小时/天）**。这在生成式模型论文中是一个小遗憾，不便于评估其训练成本。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了详尽的定量对比。
    *   针对 4 种不同的下游分割架构验证了生成数据的通用性。
    *   进行了消融实验，分别验证了“统一生成机制”和“SBCA 采样策略”的有效性。
    *   进行了扩展分析，探讨了不同真实数据比例（5% 到 100%）以及不同合成数据规模（1:1 到 1:20）对性能的影响。
*   **客观性**：实验设计考虑了数据不平衡问题，并使用了统一的预处理流程，对比实验较为公平。

### 6. 主要结论与发现
*   **性能提升显著**：在只有 5% 真实数据的情况下，加入 UniTriGen 生成的数据后，SemanticRT 上的分割性能提升了约 2-5% mIoU。
*   **优于级联方法**：UniTriGen 在“仅使用合成数据训练”和“真实+合成数据混合训练”两种模式下均优于所有级联生成方案。
*   **跨模型通用性**：生成的数据不仅能提升传统的 CNN 模型，对基于 Mamba 和 Transformer 的新型分割网络同样有效。
*   **数据缩放效应**：随着合成数据比例的增加（如 1:20），下游任务的性能持续增长，证明了生成数据的质量和多样性。

### 7. 优点（亮点）
*   **端到端一致性**：通过共享潜空间解决了多模态数据生成中的“对齐”难题，避免了级联模型中的误差传播。
*   **解决数据偏差**：SBCA 策略有效地缓解了少样本学习中常见的场景和类别分布不均问题。
*   **实用性强**：生成的 Label 直接可用作监督信号，无需额外的伪标签处理步骤。

### 8. 不足与局限
*   **算力成本未知**：由于未披露训练资源消耗，难以评估该方法在普通开发者设备上的可迁移性。
*   **依赖预训练模型**：该框架依赖于 CLIP 等强大的预训练文本编码器，在某些极端垂直领域（如纯工业红外检测）可能存在语义对齐偏差。
*   **推理延迟**：扩散模型通常推理速度较慢，论文未讨论生成大规模增强数据集所需的时间成本。

（完）
