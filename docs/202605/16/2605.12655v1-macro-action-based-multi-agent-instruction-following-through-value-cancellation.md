---
title: Macro-Action Based Multi-Agent Instruction Following through Value Cancellation
title_zh: 基于宏动作并通过价值抵消实现的多智能体指令遵循
authors: "Wo Wei Lin, Ethan Rathbun, Enrico Marchesini Xiang Zhi Tan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12655v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 结合自然语言指令的多智能体强化学习
tldr: 针对多智能体强化学习中指令中断宏动作导致价值估计不一致的问题，本文提出MAVIC方法。该方法在指令边界修正贝尔曼更新目标，通过抵消旧指令影响并恢复当前目标价值，实现了一致的价值估计。实验证明，MAVIC在复杂协作任务中显著提升了指令遵循度，并保持了优异的基础任务性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12655v1/fig-001.webp\", \"caption\": \"Table 1: MacDec-POMDP definition where the subscript i denotes individual agent components.\", \"page\": 2, \"index\": 1, \"width\": 520, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2605.12655v1/fig-002.webp\", \"caption\": \"Figure 5: Action distribution frequency for successful delivery is shown by baseline no instruction while action distribution for unachievable instructions.\", \"page\": 8, \"index\": 2, \"width\": 826, \"height\": 235}]"
motivation: 现有的多智能体强化学习在处理中断宏动作的外部指令时，由于贝尔曼更新耦合了不同指令背景下的价值估计，会导致价值函数不一致。
method: 提出了MAVIC算法，通过在指令切换边界修正引导目标（bootstrapping target），从而在统一策略下实现一致的价值估计。
result: 在复杂的协作式多智能体环境中，MAVIC在保证基础任务表现的前提下，实现了极高的指令遵循率。
conclusion: MAVIC通过修正价值函数更新机制，有效解决了指令驱动型多智能体任务中的价值不一致问题，提升了系统的灵活性和合规性。
---

## 摘要
现实场景中的多智能体强化学习（MARL）可能需要适应外部自然语言指令，这些指令会中断正在进行的行为并与长远目标发生冲突。然而，将奖励以指令为条件会引入一种根本性的失效模式，因为贝尔曼更新会将不同指令上下文中的价值估计耦合在一起，导致当指令中断宏动作时出现价值不一致。我们提出了用于指令遵循的宏动作价值修正（MAVIC），它通过修正传入的指令目标并恢复当前目标下的延续价值，在指令边界处修正贝尔曼备份。与奖励塑造不同，MAVIC 修改了自举目标本身，从而在统一策略内实现了随机指令切换下的连贯价值估计。我们提供了理论分析和一种 Actor-Critic 实现，并证明了在日益复杂的协作式多智能体环境中，MAVIC 在保持基础任务性能的同时，实现了极高的指令遵循度。

## Abstract
Multi-agent reinforcement learning (MARL) in real-world use cases may need to adapt to external natural language instructions that interrupt ongoing behavior and conflict with long-horizon objectives. However, conditioning rewards on instructions introduces a fundamental failure mode as Bellman updates couple value estimates across instruction contexts, leading to inconsistent values when instructions interrupt macro-actions. We propose Macro-Action Value Correction for Instruction Compliance (MAVIC), which corrects Bellman backups at instruction boundaries by correcting the incoming instruction objective and restoring the continuation value under the current objective. Unlike reward shaping, MAVIC modifies the bootstrapping target itself, enabling consistent value estimation under stochastic instruction switching within a unified policy. We provide theoretical analysis and an actor-critic implementation, and show that MAVIC achieves high instruction compliance while preserving base task performance in increasingly complex cooperative multi-agent environments.

---

## 论文详细总结（自动生成）

以下是对论文《Macro-Action Based Multi-Agent Instruction Following through Value Cancellation》（MAVIC）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的多智能体协作场景中，机器人需要根据人类的自然语言指令动态调整行为。这些指令往往会**中断**正在执行的长时程“宏动作”（Macro-Actions），并可能与原始任务目标发生冲突。
*   **研究动机**：传统的宏动作多智能体强化学习（MacDec-POMDP）假设动作执行不被中断。如果简单地将指令作为奖励条件，会导致**贝尔曼更新的“交叉污染”**（Cross-contamination）。
*   **核心问题**：当新指令到达并中断当前动作时，价值函数会从属于不同目标的状态进行自举（Bootstrapping），导致价值估计不一致。这使得智能体要么为了基础任务而忽略指令，要么为了遵循指令而大幅降低基础任务的效率。

### 2. 论文提出的方法论：MAVIC
论文提出了 **MAVIC（Macro-Action Value Correction）**，其核心思想是在指令切换的边界处对价值估计进行“去耦合”修正。

*   **核心思想**：通过修正贝尔曼备份目标，确保每个指令上下文下的价值函数仅反映该指令下的预期回报，而不受未来随机切换的其他指令干扰。
*   **关键技术细节**：
    *   **价值修正机制**：当指令从 $c$ 切换到 $c'$ 时，算法会修改即时奖励。它减去新指令上下文下的未来价值估计 $V(s', c')$，并补偿回原指令上下文下的延续价值 $V(s', c)$。
    *   **公式逻辑**：修正后的回报 $G$ 在切换发生时被定义为：$G = \bar{r}_c + \gamma^{\tau} (V_c - V_{c'})$。这在数学上等同于在每个指令特定的子问题中独立优化。
    *   **架构设计**：采用 Actor-Critic 框架。使用预训练且**冻结的 BERT 编码器**将自然语言指令转化为向量，与环境观测拼接后输入策略网络和价值网络。
    *   **中断感知**：强制宏动作在指令更新时终止，以便智能体立即响应新指令。

### 3. 实验设计
论文在三个具有挑战性的协作式多智能体基准环境中进行了评估：
*   **场景设置**：
    1.  **Box Pushing (BP)**：双智能体协作推箱子，指令涉及改变目标或禁止推特定箱子。
    2.  **Warehouse Tool Delivery (WTD)**：三智能体仓库任务，指令涉及更改工具交付顺序。
    3.  **Overcooked (OC)**：三智能体烹饪协作，指令包含资源限制（如“不要使用左侧切菜板”）或特定请求。
*   **对比方法（Benchmarks）**：
    *   **Naive**：直接将指令奖励加入总奖励，不进行价值修正。
    *   **Switch**：在不同指令环境下独立训练（避免交叉污染，但缺乏动态切换能力）。
    *   **Vanilla Mac-IAC**：不带指令的标准算法（作为基础任务性能的上限）。
*   **评估指标**：基础任务性能（Base Task Performance）和指令遵循率（Instruction Compliance）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到基于 `Mac-IAC` 的原始代码库实现，使用了 BERT-base 模型作为语言编码器，并采用了异步训练框架。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同的环境领域进行了测试，每个实验运行了 **5 个独立随机种子**，并提供了 95% 的置信区间。
*   **充分性评价**：实验设计较为充分。除了主实验外，还包含了：
    *   **分布外（OOD）指令测试**：验证了模型对未见过的同义指令的泛化能力。
    *   **不可行指令分析**：观察智能体在面对无法完成的指令时的行为分布。
*   **客观性**：通过对比“Switch”基线，客观展示了 MAVIC 在处理动态中断方面的独特优势，而非仅仅是解决了多任务学习问题。

### 6. 主要结论与发现
*   **消除权衡**：MAVIC 是唯一能在保持高基础任务得分的同时，实现近乎 100% 指令遵循率的方法。
*   **解决交叉污染**：实验证明 Naive 方法在复杂任务（如 Overcooked）中会因为价值函数混乱而导致基础任务完全失败。
*   **鲁棒性**：MAVIC 在指令随机到达、频繁中断的情况下表现稳定，且能泛化到语义相近的自然语言表述。

### 7. 优点
*   **理论完备**：提供了数学证明，展示了价值修正如何实现指令特定问题的解耦。
*   **即插即用**：该价值修正机制可以集成到现有的多种 Actor-Critic 强化学习算法中。
*   **高效性**：通过修正自举目标而非复杂的奖励塑造（Reward Shaping），避免了手动调参的繁琐。

### 8. 不足与局限
*   **价值估计依赖**：MAVIC 的效果高度依赖于 Critic 网络对价值估计的准确性。在部分可观测性极强或奖励极度稀疏的环境下，修正项可能引入较大方差。
*   **语言理解深度**：虽然使用了 BERT，但研究重点在于控制层面的逻辑一致性，而非深层的语言推理。对于逻辑极其复杂的指令（如条件嵌套指令），其表现尚未验证。
*   **算力信息缺失**：缺乏训练成本的量化数据，难以评估其在大规模系统中的部署成本。

（完）
