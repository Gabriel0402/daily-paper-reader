---
title: Near-Optimal Last-Iterate Convergence for Zero-Sum Games with Bandit Feedback and Opponent Actions
title_zh: 具有老虎机反馈和对手动作信息的零和博弈的近最优最后迭代收敛
authors: "Soumita Hait, Ping Li, Haipeng Luo, Mengxiao Zhang"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09363v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 双人零和博弈中的学习动态
tldr: 本研究探讨了在带有多臂老虎机反馈且可观测对手动作的零和博弈中，最后一次迭代的收敛性。针对以往研究中最后一次迭代收敛速度受限的问题，本文提出一种基于对数屏障正则化博弈的高效算法，成功将收敛速度提升至近乎最优的 t^(-1/2)。该成果不仅在理论上克服了单智能体算法向博弈场景迁移的障碍，还在偏好学习和对决老虎机等实际场景中展现了优越性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09363v1/fig-001.webp\", \"caption\": \"Figure 1: Plots of duality gap vs. number of rounds for PMO-LB and five baseline algorithms\", \"page\": 9, \"index\": 1, \"width\": 817, \"height\": 276}]"
motivation: 旨在解决在仅有老虎机反馈的零和博弈中，最后一次迭代收敛速度慢于平均迭代收敛速度的理论瓶颈。
method: 提出一种通过求解估计的对数屏障正则化博弈并降低策略更新频率的高效算法。
result: 证明了在可观测对手动作的情况下，最后一次迭代能以高概率达到 t^(-1/2) 的近优收敛速度。
conclusion: 该研究填补了博弈论中特定反馈机制下的收敛速度空白，并为对决老虎机等应用提供了更优的理论保障。
---

## 摘要
博弈中学习动力学的最后迭代收敛（Last-iterate convergence）近期引起了广泛关注。在仅能观测到所选动作对损失的老虎机反馈（bandit feedback）双人零和博弈中，Fiegel 等人 (2025) 展示了对偶间隙在平均迭代和最后迭代收敛之间的分离：虽然前者可以通过标准的无悔算法在 t 轮后达到最优的 t^(-1/2) 速率，但后者在期望上收敛速度不快于 t^(-1/3)，在高概率下不快于 t^(-1/4)。然而，在许多实际场景（如偏好学习）中，玩家不仅能观测到自身的损失，还能观测到对手的动作。这提出了一个自然的问题：这种额外信息能否实现更快的最后迭代收敛？我们肯定地回答了这个问题，证明在该设定下，通过一种通过求解估计的对数势垒正则化（log-barrier-regularized）博弈来不频繁更新策略的高效算法，可以实现高概率下的 t^(-1/2) 最后迭代收敛。我们识别了阻碍多臂老虎机（单人情况）标准分析推广到博弈的根本障碍，并开发了一种新的分析方法来克服这些障碍。实验证实，我们的算法确实比朴素基准以及不利用对手动作反馈的先前方法收敛得更快。最后，我们指出，我们的结果还改进了对决老虎机（dueling bandits，一种具有反对称博弈矩阵的特殊情况）的研究结果。

## Abstract
Last-iterate convergence of learning dynamics in games has attracted significant recent attention. In two-player zero-sum games with bandit feedback, where only the loss of the selected action pair is observed, Fiegel et al. (2025) show a separation between average-iterate and last-iterate convergence in duality gap: while the optimal t^(-1/2) rate after t rounds is achievable for the former via standard no-regret algorithms, the latter cannot converge faster than t^(-1/3) in expectation or t^(-1/4) with high probability. However, in many practical settings, such as preference learning, the players observe not only their loss but also the opponent's action. This raises a natural question: can such additional information enable faster last-iterate convergence?   We answer this question affirmatively, showing that t^(-1/2) last-iterate convergence is achievable with high probability in this setting, via an efficient algorithm that updates its strategy infrequently by solving an estimated log-barrier-regularized game. We identify fundamental obstacles preventing standard analysis for multi-armed bandits, the single-player case, from generalizing to games, and develop a novel analysis to overcome them. Experiments confirm that our algorithm indeed converges faster than naive baselines and prior methods that do not exploit opponent-action feedback. Finally, we note that our results also improve those for dueling bandits, a special case with skew-symmetric game matrices.

---

## 论文详细总结（自动生成）

这篇论文针对博弈论中的学习动力学问题，提出了一种在特定反馈机制下实现近最优收敛速度的新算法。以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：在双人零和博弈中，**最后迭代收敛（Last-iterate convergence）**比平均迭代收敛更具实际意义（例如在神经网络训练中无需存储历史平均参数）。
*   **核心挑战**：在仅有“老虎机反馈”（只能看到结果，看不到梯度）的设定下，前人证明最后迭代收敛速度存在理论瓶颈（最高 $t^{-1/3}$ 或 $t^{-1/4}$），慢于平均迭代的 $t^{-1/2}$。
*   **核心问题**：如果玩家在获得老虎机反馈的同时，还能**观测到对手的动作**（这在定价、广告竞价、安全博弈中很常见），能否打破这一瓶颈，达到 $t^{-1/2}$ 的近最优收敛速度？

### 2. 方法论：PMO-LB 算法
*   **核心思想**：提出“带对数屏障正则化的阶段性极小极大优化”（Phased Minimax Optimization with Log-Barrier Regularization, **PMO-LB**）算法。
*   **关键技术细节**：
    *   **分阶段运行（Epoch-based）**：算法将时间分为长度倍增的阶段，仅在每个阶段结束时更新策略，从而降低计算开销。
    *   **对数屏障正则化（Log-Barrier）**：在求解估计的博弈矩阵时，引入对数屏障项来强制进行探索，防止策略过快地收敛到边界，确保所有动作都有一定的采样概率。
    *   **极小极大求解**：每一阶段，玩家根据上一阶段收集的数据构建估计矩阵 $\hat{A}_s$，并求解一个正则化后的鞍点问题来确定本阶段的采样策略 $(x_s, y_s)$。
    *   **乘性稳定性分析（Multiplicative Stability）**：这是本文的核心理论贡献。通过证明连续两个阶段的策略在乘性意义上是稳定的（即变化受限），克服了博弈场景中由于“估计对偶间隙需要探索潜在劣势策略”而导致的分析难题。

### 3. 实验设计
*   **实验场景**：使用了 **GUARD 绿色安全博弈（Green Security Game）** 基准测试环境。
*   **数据集/矩阵规模**：测试了两个具体的博弈矩阵，规模分别为 $61 \times 21$ 和 $39 \times 35$。
*   **对比方法（Baselines）**：
    *   **无对手信息算法**：Cai et al. (2023/2025), Fiegel et al. (2025/2026)。
    *   **朴素基准**：一种简单的将经验最优策略与均匀探索混合的算法（Naive Baseline）。
*   **衡量指标**：对偶间隙（Duality Gap）随轮数 $t$ 的下降速度。

### 4. 资源与算力
*   **硬件环境**：实验在一台运行 Ubuntu 24.04.3 LTS 的 Linux 服务器上完成。
*   **CPU**：配备两颗 Intel Xeon Gold 6530 处理器（共 64 个物理核心，128 个逻辑线程）。
*   **内存**：1.0 TiB RAM。
*   **GPU**：**文中未提到使用 GPU**，由于该算法涉及的是矩阵博弈的数值优化，主要依赖 CPU 计算。
*   **软件**：Python 3.13.12。

### 5. 实验数量与充分性
*   **实验规模**：每组实验运行了 $10^7$ 轮（Round），这是一个相当大的时间跨度，足以观察长期收敛趋势。
*   **重复性**：所有结果均基于 **10 次独立运行的平均值**。
*   **充分性评价**：实验涵盖了不同规模的矩阵，并对比了近年来该领域最先进的（SOTA）算法。通过 log-log 图清晰地展示了收敛斜率与理论预测（$t^{-1/2}$）的高度吻合，实验设计客观且对比公平。

### 6. 主要结论与发现
*   **理论突破**：证明了在可观测对手动作的情况下，最后迭代收敛可以达到 $O(t^{-1/2})$ 的高概率速率，消除了最后迭代与平均迭代之间的速度差距。
*   **实验验证**：PMO-LB 算法在安全博弈基准上的表现显著优于所有不利用对手信息的算法，且收敛斜率明显优于朴素的探索-利用算法。
*   **应用扩展**：该结果直接改进了**对决老虎机（Dueling Bandits）**问题的最后迭代收敛保证。

### 7. 优点
*   **理论完备性**：填补了博弈论中关于反馈机制与收敛速度关系的理论空白。
*   **计算高效**：算法仅需在每个阶段（对数级别次数）求解一次优化问题，而非每轮更新。
*   **强鲁棒性**：提供了高概率保证，而非仅仅是期望意义上的收敛。

### 8. 不足与局限
*   **维度依赖性**：目前的收敛速率对动作空间维度 $d$ 的依赖为 $d^2$，作者怀疑这可能不是最优的（理想情况可能是 $\sqrt{d}$）。
*   **反馈要求**：算法严格依赖于“能观测到对手动作”这一假设。在某些完全匿名的博弈环境中，该算法无法适用。
*   **矩阵限制**：虽然理论适用于一般矩阵，但实验主要集中在安全博弈这一特定类型的矩阵上，对于更大规模或更稀疏的博弈矩阵表现尚待验证。

（完）
