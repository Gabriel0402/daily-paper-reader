---
title: Taming the Curses of Multiagency in Robust Markov Games with Large State Space through Linear Function Approximation
title_zh: 通过线性函数近似克服具有大状态空间的鲁棒马尔可夫博弈中的多智能体诅咒
authors: "Jingchu Gai, Laixi Shi"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03125v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 具有大状态空间和线性函数逼近的多智能体强化学习
tldr: 本研究针对多智能体强化学习中的鲁棒性与数据效率难题，探讨了具有大规模状态空间的分布鲁棒马尔可夫博弈（RMGs）。通过引入线性函数近似（LFA）和总变差距离不确定性集，作者开发了在生成模型和在线交互设置下均能打破“多智能体诅咒”的算法。这是首个在无限状态空间下实现样本复杂度与智能体数量解耦的研究，显著提升了大规模复杂环境下的学习效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03125v2/fig-001.webp\", \"caption\": \"Table 1: The existing results of sample complexity in the generative model setting (or regret in the online setting) for finding an ε-approximate equilibrium in finite-horizon, multi-agent, general-sum robust Markov games (RMGs), omitting logarithmic factors. To our knowledge, this work provides the first algorithms that break the curse of multiagency for large (possibly infinite) state spaces using linear function approximation.\", \"page\": 3, \"index\": 1, \"width\": 941, \"height\": 360}]"
motivation: 现有的鲁棒马尔可夫博弈算法大多局限于表格设置，难以应对大规模状态空间及多智能体带来的指数级样本需求。
method: 采用线性函数近似处理大规模状态空间，并针对总变差距离定义的不确定性集开发了高效的采样算法。
result: 成功在生成模型和在线交互两种设定下打破了多智能体诅咒，实现了样本复杂度与智能体数量的解耦。
conclusion: 该研究为大规模鲁棒多智能体强化学习提供了理论保障，是首个在一般性RMGs中解决多智能体诅咒的工作。
---

## 摘要
多智能体强化学习（MARL）具有巨大潜力，但由于环境的不确定性而面临鲁棒性挑战。为了解决这一问题，分布鲁棒马尔可夫博弈（RMGs）在环境偏离不确定性集内的名义模型时优化最差情况下的性能。除了鲁棒性之外，MARL 同样紧迫的目标是数据效率——从随智能体数量呈指数增长的巨大状态和动作空间中进行采样，可能会导致多智能体诅咒。然而，目前可证明的数据高效 RMGs 算法局限于具有有限状态和动作空间的表格设置，这仅在小规模问题上具有计算可行性，使得具有大规模（或无限）状态空间的 RMGs 在很大程度上尚未得到探索。现有唯一超越表格设置的工作专注于一类受限 RMGs 的线性函数近似（LFA），使用了消失最小边界值假设，且其样本复杂度仍受多智能体诅咒的影响。在这项工作中，我们专注于具有 LFA 的通用 RMGs。对于由全变分距离定义的不确定性集，我们开发了可证明的数据高效算法，在生成模型设置和新提出的在线交互设置中都打破了多智能体诅咒。据我们所知，无论不确定性集如何构建，我们的结果都是首次打破具有大（可能是无限）状态空间的 RMGs 样本复杂度的多智能体诅咒。

## Abstract
Multi-agent reinforcement learning (MARL) holds great potential but faces robustness challenges due to environmental uncertainty. To address this, distributionally robust Markov games (RMGs) optimize worst-case performance when the environment deviates from the nominal model within a uncertainty set. Beyond robustness, an equally urgent goal for MARL is data efficiency -- sampling from vast state and action spaces that grow exponentially with the number of agents potentially leads to the curse of multiagency. However, current provably data-efficient algorithms for RMGs are limited to tabular settings with finite state and action spaces, which are only computationally manageable for small-scale problems, leaving RMGs with large-scale (or infinite) state spaces largely unexplored. The only existing work beyond tabular settings focuses on linear function approximation (LFA) for a restrictive class of RMGs using vanish minimal value assumption and still suffers from sample complexity with the curse of multiagency. In this work, we focuses on general RMGs with LFA. For uncertainty sets defined by total variation distance, we develop provably data-efficient algorithms that break the curse of multiagency in both the generative model setting and a newly proposed online interactive setting. To our knowledge, our results are the first to break the curse of multiagency of sample complexity for RMGs with large (possibly infinite) state spaces, regardless of the uncertainty set construction.

---

## 论文详细总结（自动生成）

这篇论文《Taming the Curses of Multiagency in Robust Markov Games with Large State Space through Linear Function Approximation》由卡内基梅隆大学（CMU）和约翰霍普金斯大学的研究人员撰写。该研究聚焦于多智能体强化学习（MARL）在处理大规模状态空间时的鲁棒性与效率问题。

以下是对该论文的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：多智能体强化学习在现实应用（如自动驾驶、金融市场）中面临两大挑战：
    1.  **鲁棒性挑战**：环境动态的微小扰动会导致策略性能大幅下降。
    2.  **多智能体诅咒（Curse of Multiagency）**：样本复杂度随智能体数量呈指数级增长（通常与联合动作空间 $\prod A_i$ 相关）。
*   **核心问题**：现有的鲁棒马尔可夫博弈（RMG）算法大多局限于“表格设置”（有限状态空间），而处理大规模或无限状态空间的研究极少，且现有工作仍无法摆脱多智能体诅咒。
*   **整体含义**：本文首次证明了在**线性函数近似（LFA）**下，可以设计出在样本复杂度上与智能体数量解耦（即打破多智能体诅咒）的鲁棒算法，适用于具有大规模状态空间的通用 RMG。

### 2. 方法论：核心思想与技术细节
论文针对两种数据采集机制提出了不同的算法：

#### A. 生成模型设置（Generative Model）
*   **核心算法**：`L-Robust-Q-FTRL`。
*   **关键技术**：
    *   **无限到有限的归约**：利用 Kiefer-Wolfowitz 定理，在无限的状态-动作空间中挑选出一组具有代表性的有限子集（大小约为 $d^2$），仅对这些点进行采样。
    *   **岭回归（Ridge Regression）**：通过采样数据估计名义模型（Nominal Model）的参数。
    *   **鲁棒贝尔曼算子**：利用对偶理论解决最差情况下的优化问题，更新鲁棒 Q 函数。

#### B. 在线交互设置（Online Interactive）
*   **核心算法**：`Online-L-Robust-Q-FTRL`。
*   **关键技术**：
    *   **混合采样（Hybrid Sampling）**：这是本文的一大创新。由于最差环境未知，算法使用**悲观鲁棒价值估计**来模拟对抗环境进行前 $h-1$ 步的轨迹生成，但在第 $h$ 步切换回名义模型采样，以确保线性结构的估计有效性。
    *   **双轨估计**：同时维护“乐观估计”（用于策略更新，促进探索）和“悲观估计”（用于近似对抗环境采样）。
    *   **FTRL 策略更新**：采用“跟随正则化领导者”（Follow-the-Regularized-Leader）算法来更新各智能体的策略。

### 3. 实验设计与 Benchmark
*   **实验性质**：本文是一篇**纯理论证明论文**，没有包含数值仿真实验。
*   **Benchmark（基准对比）**：作者通过数学推导，将本文算法的样本复杂度/遗憾值（Regret）与现有的最先进（SOTA）理论结果进行了对比（见论文 Table 1）：
    *   对比对象包括：`P2MPO` (Blanchet et al. 2024)、`DR-NVI` (Shi et al. 2024a)、`DRMG` (Farhat et al. 2025) 等。
    *   **对比维度**：状态空间规模（表格 vs 线性）、是否打破多智能体诅咒、不确定性集类型。

### 4. 资源与算力
*   **说明**：由于本论文侧重于算法的**可证明样本效率（Provable Sample Efficiency）**和收敛性分析，文中**未提及**任何具体的 GPU 型号、算力消耗或训练时长。其贡献在于数学复杂度的降低而非工程实现。

### 5. 实验数量与充分性
*   **理论验证**：论文提供了长达 40 页的严谨数学证明（附录 A-D），涵盖了鲁棒纳什均衡的存在性、生成模型下的样本复杂度分析以及在线设置下的遗憾值分析。
*   **充分性评价**：从理论研究的角度看，论证非常充分。作者不仅给出了上界分析，还详细讨论了如何通过技术手段（如混合采样）克服线性模型在非线性对抗环境下的失效问题。

### 6. 论文的主要结论与发现
*   **打破诅咒**：在生成模型下，样本复杂度为 $\tilde{O}(H^9 d^3 / \epsilon^4)$；在在线设置下，遗憾值为 $\tilde{O}(d \max A_i H^2 \sqrt{T})$。两者均只与单个智能体的最大动作空间 $\max A_i$ 相关，而非联合动作空间 $\prod A_i$。
*   **均衡存在性**：证明了在无限状态空间和全变分（TV）不确定性集下，鲁棒纳什均衡（Robust NE）依然存在。
*   **在线学习可行性**：提出了一种切实可行的在线交互协议，证明了即使在无法直接访问名义模型的情况下，通过与近似对抗环境交互也能实现亚线性遗憾。

### 7. 优点
*   **理论突破**：填补了大规模状态空间下鲁棒 MARL 理论的空白，是首个实现该领域“多智能体诅咒”解耦的工作。
*   **采样策略创新**：提出的“混合采样”机制巧妙地解决了线性函数近似与非线性鲁棒扰动之间的矛盾。
*   **通用性**：不要求鲁棒价值函数本身具有线性结构，仅要求名义模型线性，这使得算法更具普适性。

### 8. 不足与局限
*   **缺乏实证**：虽然理论完备，但没有在复杂仿真环境（如星际争霸或自动驾驶模拟器）中验证算法的实际表现。
*   **不确定性集限制**：目前主要针对全变分（TV）距离。对于 KL 散度或 Wasserstein 距离等其他常见不确定性集的扩展仍需进一步研究。
*   **线性假设**：虽然超越了表格设置，但依然依赖于线性函数近似（LFA），对于高度非线性的深度强化学习场景，该理论外推仍有挑战。

（完）
