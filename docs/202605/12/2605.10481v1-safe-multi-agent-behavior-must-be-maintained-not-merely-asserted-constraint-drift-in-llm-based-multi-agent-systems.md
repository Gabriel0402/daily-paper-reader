---
title: "Safe Multi-Agent Behavior Must Be Maintained, Not Merely Asserted: Constraint Drift in LLM-Based Multi-Agent Systems"
title_zh: 安全的多智能体行为必须得到维持，而非仅仅是断言：基于大语言模型的多智能体系统中的约束漂移
authors: "Tianxiao Li, Yixing Ma, Haiquan Wen, Zhenglin Huang, Qianyu Zhou, Zeyu Fu, Guangliang Cheng"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10481v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 基于大语言模型的多智能体系统中的安全关键约束
tldr: 本研究针对基于大语言模型的多智能体系统在执行长程任务时出现的“约束漂移”现象，即安全约束在记忆、通信和工具调用中逐渐失效的问题，提出了“约束状态治理”范式。该范式主张将安全约束作为显式的执行状态进行动态维护，而非仅在初始阶段声明，并利用约束原生强化学习在安全边界内优化系统效用，旨在确保智能体在复杂交互轨迹中的行为安全性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10481v1/fig-001.webp\", \"caption\": \"Figure 2: CSG turns rules into governed trajectories, and constraint native learning improves utility only within the admissible set.\", \"page\": 7, \"index\": 1, \"width\": 812, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2605.10481v1/fig-002.webp\", \"caption\": \"Table 2: Coverage of mechanism families for LLM-agent safety. Existing mechanisms provide useful local controls, but they do not by themselves maintain constraints as inherited, checkable, and auditable state across full multi-agent trajectories.\", \"page\": 4, \"index\": 2, \"width\": 827, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2605.10481v1/fig-003.webp\", \"caption\": \"Figure 1: (a) Multi-agent execution shifts safety from final-answer checking to trajectory-level constraint preservation; (b) constraint drift identifies where constraints lose operational force across memory, authority, information flow, accountability, and optimization.\", \"page\": 3, \"index\": 3, \"width\": 804, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2605.10481v1/fig-004.webp\", \"caption\": \"Table 1: Diagnostic drift modes derived from recurring interfaces in LLM-based multi-agent trajectories.\", \"page\": 3, \"index\": 4, \"width\": 790, \"height\": 221}]"
motivation: 随着LLM智能体从被动文本生成转向主动工具调用和多步协作，安全约束在长程执行过程中容易因记忆丢失或授权委托而发生弱化或失效。
method: 提出了“约束状态治理”研究范式，通过将安全约束维护为显式的执行状态，并结合约束原生强化学习在安全边界内提升系统性能。
result: 定义并分析了多智能体系统中普遍存在的“约束漂移”问题，指出仅靠提示词和输出检查无法在复杂工作流中保证持续的安全性。
conclusion: 多智能体系统的安全必须在执行全过程中被动态维护、继承和审计，使安全约束在智能体的实际行动轨迹中真正具备可操作性。
---

## 摘要
现代基于大语言模型（LLM）的智能体不再是消极的文本生成器。它们能够读取代码库、调用工具、浏览网页、执行代码、维护记忆、与其他智能体通信，并通过长程工作流采取行动。这种转变改变了安全性的基本单元。一个系统可能会产生合规的最终答案，但同时通过内部消息泄露隐私信息、授权超出其原始范围、调用带有敏感上下文的外部工具，或者丢失用于重建为何允许某项操作所需的证据。我们认为，基于 LLM 的多智能体系统中出现的许多故障具有共同的结构：安全关键约束在整个执行轨迹中未能保持有效。我们将这种现象称为“约束漂移”（constraint drift）：即约束在经过记忆、授权、通信、工具使用、审计和优化过程中发生的丢失、扭曲、削弱或松弛。本文持有的观点是，安全的多智能体行为必须得到维持，而不仅仅是断言。提示词、护栏、工具模式、访问控制和最终输出检查是必要的，但除非约束在执行过程中保持新鲜、可继承、可强制执行且可审计，否则它们是不充分的。我们提出了“约束状态治理”（Constraint State Governance）作为基于 LLM 的多智能体系统的研究范式。在该范式中，安全关键约束被维护为显式的执行状态，而约束原生强化学习仅在维护的安全边界内提高效用。其目标不是用僵化的规则冻结智能体系统，而是使安全性在现代智能体实际行动的轨迹中具备可操作性。

## Abstract
Modern LLM based agents are no longer passive text generators. They read repositories, call tools, browse the web, execute code, maintain memory, communicate with other agents, and act through long horizon workflows. This shift moves the unit of safety. A system may produce a compliant final answer while leaking private information through an internal message, delegating authority beyond its original scope, calling an external tool with sensitive context, or losing the evidence needed to reconstruct why an action was allowed. We argue that many emerging failures in LLM-based multi-agent systems share a common structure: safety critical constraints do not remain operative throughout the trajectory. We call this phenomenon constraint drift: the loss, distortion, weakening, or relaxation of constraints as they pass through memory, delegation, communication, tool use, audit, and optimization. The position taken here is that safe multi-agent behavior must be maintained, not merely asserted. Prompts, guardrails, tool schemas, access control, and final output checks are necessary, but they are insufficient unless constraints remain fresh, inherited, enforceable, and auditable across execution. We propose Constraint State Governance as a research paradigm for LLM-based multi-agent systems. In this paradigm, safety-critical constraints are maintained as explicit execution state, while constraint-native reinforcement learning improves utility only within maintained safety boundaries. The goal is not to freeze agentic systems under rigid rules, but to make safety operational across the trajectories through which modern agents actually act.

---

## 论文详细总结（自动生成）

这是一份关于论文《Safe Multi-Agent Behavior Must Be Maintained, Not Merely Asserted: Constraint Drift in LLM-Based Multi-Agent Systems》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 LLM 智能体从简单的文本生成器演变为能够调用工具、操作代码库和多步协作的“执行系统”，安全评估的维度发生了根本性位移。传统的安全检查（如护栏、输出过滤）主要关注最终答案是否合规，但忽略了**执行轨迹（Trajectory）**中的安全性。
论文提出了**约束漂移（Constraint Drift）**这一核心概念：指安全关键约束在智能体进行记忆检索、任务授权、跨智能体通信、工具调用及优化过程中，逐渐丢失、扭曲或失效的现象。作者强调，安全不能仅靠初始提示词的“断言”，而必须在整个执行生命周期中得到动态“维持”。

### 2. 方法论：约束状态治理（Constraint State Governance, CSG）
为了应对约束漂移，论文提出了 **CSG 范式**，包含两个耦合层：

*   **约束状态治理层（CSG Layer）：**
    *   **显式状态化：** 将自然语言约束转化为带签名的令牌（Token），包含唯一标识、版本、作用域（Scope）、谓词逻辑（Predicate）和审计追踪。
    *   **准入控制算法：** 在执行关键动作前，系统会检查该动作是否满足当前所有活跃约束。这包括：**新鲜度检查**（防止使用过时状态）、**能力校验**（防止授权范围扩大）、**流控检查**（防止敏感信息跨渠道泄露）和**证据校验**（确保破坏性操作有据可查）。
    *   **状态同步：** 约束作为执行状态的一部分随任务流转，而非依赖智能体的局部记忆。
*   **约束原生强化学习（Constraint Native RL）：**
    *   **定义可容许集：** 强化学习不再将约束视为软惩罚项，而是利用 CSG 产生的审计记录定义“可容许轨迹集”。
    *   **效用优化：** 仅在满足所有强制性约束的轨迹内优化任务效用。如果轨迹违反约束，它将被标记为违规证据，不参与效用竞争。

### 3. 实验设计
论文通过对 **AgentLeak** 数据集的重放（Replay）研究来验证其观点：
*   **数据集/场景：** 使用了包含医疗、金融、法律和企业领域的 4,979 条多智能体交互轨迹。
*   **Benchmark：** 评估三个渠道的隐私泄露率：最终输出（C1）、智能体间消息（C2）和共享内存写入（C5）。
*   **对比方法：** 
    1.  **无防御（No Defense）：** 原始轨迹。
    2.  **仅输出过滤（Output Only Filter）：** 仅检查最终答案。
    3.  **CSG Lite：** 论文提出的轻量化治理方案，在每个转换节点进行准入检查和脱敏。
    4.  **离线策略搜索：** 模拟约束原生学习，寻找在满足隐私约束前提下效用最高的脱敏策略。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   由于实验部分主要基于数据集的“反事实重放”（Counterfactual Replay）和离线策略搜索，而非大规模的在线强化学习训练，其对即时算力的需求可能相对较低，重点在于逻辑验证和统计分析。

### 5. 实验数量与充分性
*   **实验规模：** 基于近 5,000 条真实轨迹进行重放，样本量较大，覆盖了多个敏感领域。
*   **充分性：** 实验证明了“仅过滤输出”会导致内部渠道（C2, C5）存在极高的泄露风险（约 68.8%），而 CSG 能显著降低这一风险。
*   **局限性：** 实验主要集中在“信息流漂移”和“审计漂移”上，对于“授权漂移”和“效用诱导漂移”的验证更多停留在理论框架和算法描述层面，缺乏大规模在线环境下的闭环验证。

### 6. 主要结论与发现
*   **输出安全不等于轨迹安全：** 即使最终答案看起来合规，中间过程可能已经发生了严重的隐私泄露或越权操作。
*   **约束漂移是系统性风险：** 约束在多智能体协作的各个接口（记忆、授权、通信）处极易失效。
*   **治理优于过滤：** 将约束作为显式执行状态进行维护，并结合审计链，可以使违规行为变得可见、可追溯且可拦截。
*   **强化学习的范式转变：** 必须在安全边界内寻找最优解，而不是通过奖励函数在安全和效用之间做权衡。

### 7. 优点（亮点）
*   **理论深刻：** 准确捕捉到了 LLM 智能体从“文本生成”向“系统执行”转变过程中的安全痛点。
*   **框架完备：** 提出的 CSG 范式结合了计算机安全（能力访问控制、信息流跟踪）与 AI 学习（受限 RL）的成熟思想。
*   **诊断性强：** 定义了五种漂移模式（记忆、授权、信息流、问责、效用诱导），为后续研究提供了清晰的分类学。

### 8. 不足与局限
*   **实现复杂度：** 将模糊的自然语言约束转化为精确的可执行谓词（Predicate）在技术上具有挑战性，论文对此简化处理。
*   **性能开销：** 在每个关键动作前引入治理层和签名校验，可能会增加系统的延迟，论文未详细讨论性能损耗。
*   **实验环境：** 实验基于静态数据集重放，未能模拟智能体在被拦截/脱敏后的动态适应行为（即智能体可能会因为治理干预而改变后续策略）。
*   **应用限制：** 对于高度模糊、依赖人类主观判断的规范（如“语气要友好”），该框架的适用性较弱。

（完）
