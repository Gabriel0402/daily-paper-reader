---
title: "From Pixels to Concepts: Do Segmentation Models Understand What They Segment?"
title_zh: 从像素到概念：分割模型是否理解其分割的对象？
authors: "Shuang Liang, Zeqing Wang, Yuxian Li, Xihui Liu, Han Wang"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09591v1"
tags: ["query:mmseg"]
score: 10.0
evidence: 评估如SAM等可提示分割模型
tldr: 本研究探讨了可提示分割模型是否真正理解其分割的概念。作者提出CAFE基准，通过反事实属性操纵生成误导性语义线索。实验发现，模型即使掩码预测准确也难以区分误导性提示，揭示了定位质量与概念辨析间的差距，为诊断模型是否实现忠实语义对齐提供了新工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09591v1/fig-001.webp\", \"caption\": \"Figure 3: Overview of CAFE benchmark statistics. CAFE contains 2,146 paired counterfactual samples from three source datasets and spans three edit types: superficial mimicry (SM), context conflict (CC), and ontological conflict (OC). CAFE provides 656 positive prompts and 500 misleading prompts, forming 1,669 prompt pairs whose distribution is highly long-tailed, with 1,447 pair types appearing only once, indicating broad semantic coverage across counterfactual concept pairs.\", \"page\": 6, \"index\": 1, \"width\": 983, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2605.09591v1/fig-002.webp\", \"caption\": \"Figure 7: Additional examples from CAFE. Each sample consists of a counterfactually edited image, an inherited target mask, a semantically valid positive prompt, and a visually plausible but semantically invalid misleading prompt. The examples cover Superficial Mimicry (SM), Context Conflict (CC), and Ontological Conflict (OC), demonstrating the diversity of object categories, prompt pairs, and attribute-level counterfactual conflicts in CAFE.\", \"page\": 21, \"index\": 2, \"width\": 1002, \"height\": 927}, {\"url\": \"assets/figures/arxiv/2605.09591v1/fig-003.webp\", \"caption\": \"Figure 2: Examples in our CAFE. Each sample contains a counterfactually edited target image, a ground-truth mask for the target region, a positive prompt that is semantically valid for the target, and a misleading negative prompt that is visually plausible but semantically invalid. The examples cover three attribute-level intervention types: Superficial Mimicry (SM), Ontological Conflict (OC), and Context Conflict (CC).\", \"page\": 3, \"index\": 3, \"width\": 948, \"height\": 441}, {\"url\": \"assets/figures/arxiv/2605.09591v1/fig-004.webp\", \"caption\": \"Figure 8: IoU-threshold sensitivity of AFPR and ACSR on CAFEval2026. Both metrics are computed for SAM3 at a fixed score threshold t = 0.5 and swept over τ ∈ [0.3, 0.9] with step 0.1. Curves are flat for τ ∈ [0.3, 0.7] across every subset, indicating that the model’s wrong predictions overlap the source target with high IoU (≳ 0.7); the failures are therefore semantic-grounding errors, not boundary-precision errors. The drop at τ = 0.9 reflects re-classification of high-IoU errors from TA-FP to UA-FP (the image-level rate IL-FPR is preserved), supporting our use of τ = 0.3 as the canonical operating point. The dotted vertical line marks τ = 0.3.\", \"page\": 23, \"index\": 4, \"width\": 1056, \"height\": 522}, {\"url\": \"assets/figures/arxiv/2605.09591v1/fig-005.webp\", \"caption\": \"Figure 4: Overview of the CAFE dataset annotation pipeline. We draw image-annotation pairs from COCO, SA-Co, and LVIS. The images and annotations are first processed with affine transformations to fit the input size required by Gemini 3, and are then fed into Gemini 3 to generate corresponding editing instructions. Based on the generated instructions, we use nano-banana to perform image editing for all three counterfactual categories. The raw edits then undergo a three-stage filtering and cross-checking process. In the first stage, human annotators filter the raw edits and remove images with obvious artifacts. In the second stage, human annotators perform a fine-grained review of edit quality and prompt plausibility for both positive and negative prompts. In the third stage, three human editors cross-check all remaining pairs and produce the final high-quality dataset.\", \"page\": 13, \"index\": 5, \"width\": 1046, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2605.09591v1/fig-006.webp\", \"caption\": \"Table 2: Promptable Concept Segmentation (PCS) performance on CAFE. We evaluate open-vocabulary segmentation systems under three paradigms: end-to-end models, multi-model frameworks, and agentic methods. We report the standard PCS metrics [1], including cgF1, IL_MCC, and pmF1. Results are reported for three counterfactual categories: Superficial Mimicry (SM), Context Conflict (CC), and Ontological Conflict (OC); Overall aggregates over all categories. CAFE-SAM3 (GPT-5.5) substantially improves over direct SAM 3, especially on Ontological Conflict, indicating that explicit agentic verification helps reject semantically invalid masks under misleading prompts.\", \"page\": 8, \"index\": 6, \"width\": 1056, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2605.09591v1/fig-007.webp\", \"caption\": \"Figure 5: Data annotation engine used for human quality inspection. Human annotators use the interface to check edit plausibility, mask alignment, and prompt validity during the multi-round filtering process.\", \"page\": 19, \"index\": 7, \"width\": 1056, \"height\": 716}, {\"url\": \"assets/figures/arxiv/2605.09591v1/fig-008.webp\", \"caption\": \"Table 3: False-positive and concept-swap analysis on CAFE. FPR measures the proportion of misleading prompts that produce accepted masks, while AFPR reports the target-aligned false-positive rate after excluding unaligned false positives. ACSR measures the rate at which the positive prompt fails to produce a target-aligned true positive and the misleading negative prompt produces a target-aligned false positive on the same target region. UFPR and UCSR are the corresponding unaligned counterparts: UFPR counts unaligned false positives under misleading prompts, and UCSR counts cases where the positive prompt loses the target concept while the misleading negative prompt is hallucinated elsewhere in the image.\", \"page\": 9, \"index\": 8, \"width\": 1056, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2605.09591v1/fig-009.webp\", \"caption\": \"Figure 6: Illustration of category ambiguity and prompt disambiguation in ontological conflicts. Left: an object may validly belong to multiple categories, such as a toy deer belonging to both the toy and deer categories. Right: a cloud with an airplane-like shape visually resembles an airplane, but it remains a cloud rather than a real airplane. Therefore, CAFE uses precise negative prompts such as “real airplane” instead of ambiguous prompts such as “airplane” to reduce semantic ambiguity.\", \"page\": 20, \"index\": 9, \"width\": 1040, \"height\": 363}]"
motivation: 现有分割基准主要关注掩码精度，难以评估模型是真正理解了查询概念还是仅依赖视觉显著性等捷径。
method: 提出CAFE基准，通过反事实操纵目标区域的属性（如表面纹理、上下文或材质）来构建包含正向和误导性负向提示的配对样本。
result: 实验显示模型即使面对错误的语义提示也能生成精确掩码，表明强大的掩码预测能力并不等同于忠实的语义对齐。
conclusion: 现有的可提示分割模型在概念理解上存在缺陷，CAFE基准为评估和改进模型的语义忠实度提供了重要参考。
---

## 摘要
分割是众多下游应用的基础视觉任务。最近的可提示分割模型（如 Segment Anything Model 3, SAM3）将分割从类别无关的掩码预测扩展到以高级文本提示为条件的、由概念引导的定位。然而，现有的基准测试主要评估掩码准确性或对象存在性，尚不清楚这些模型是忠实地对查询概念进行接地（grounding），还是依赖于视觉上显著但语义上具有误导性的线索。我们引入了 CAFE：反事实属性事实性评估（Counterfactual Attribute Factuality Evaluation），这是一个用于评估可提示分割模型中概念忠实分割的新型基准。我们的 CAFE 建立在属性级反事实操作的基础上：保留目标区域和地面真值（ground-truth）掩码，同时修改表面外观、上下文或材料组成等属性，以引入误导性的语义线索。该基准包含 2,146 个成对的测试样本，每个样本由一张目标图像、一个地面真值掩码、一个正向提示词和一个误导性的负向提示词组成。这些样本涵盖了三个反事实类别：表面模仿（SM）、上下文冲突（CC）和本体冲突（OC）。我们在 CAFE 上评估了各种类型和规模的模型。实验揭示了定位质量与概念辨别力之间存在系统性差距：模型即使在面对误导性提示时也经常生成准确的掩码，这表明强大的掩码预测能力并不一定意味着忠实的语义接地。我们的 CAFE 提供了一个受控的基准，用于诊断可提示分割模型执行的是概念忠实的接地，还是由捷径驱动的掩码检索。

## Abstract
Segmentation is a fundamental vision task underlying numerous downstream applications. Recent promptable segmentation models, such as Segment Anything Model 3 (SAM3), extend segmentation from category-agnostic mask prediction to concept-guided localization conditioned on high-level textual prompts. However, existing benchmarks primarily evaluate mask accuracy or object presence, leaving unclear whether these models faithfully ground the queried concept or instead rely on visually salient but semantically misleading cues. We introduce CAFE: \textbf{C}ounterfactual \textbf{A}ttribute \textbf{F}actuality \textbf{E}valuation, a novel benchmark for evaluating concept-faithful segmentation in promptable segmentation models. Our \textbf{CAFE} is built on attribute-level counterfactual manipulation: the target region and ground-truth mask are preserved, while attributes such as surface appearance, context, or material composition are modified to introduce misleading semantic cues. The benchmark contains 2,146 paired test samples, each consisting of a target image, a ground-truth mask, a positive prompt, and a misleading negative prompt. These samples cover three counterfactual categories: Superficial Mimicry (\textbf{SM}), Context Conflict (\textbf{CC}), and Ontological Conflict (\textbf{OC}). We evaluate various model types and sizes on our CAFE. Experiments reveal a systematic gap between localization quality and concept discrimination: models often generate accurate masks even for misleading prompts, suggesting that strong mask prediction does not necessarily imply faithful semantic grounding. Our CAFE provides a controlled benchmark for diagnosing whether promptable segmentation models perform concept-faithful grounding rather than shortcut-driven mask retrieval.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Pixels to Concepts: Do Segmentation Models Understand What They Segment?》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着 SAM3 等可提示分割（Promptable Segmentation）模型的出现，分割任务已从单纯的像素定位演变为受文本概念引导的语义接地（Grounding）。然而，现有基准（如 COCO, LVIS）主要关注掩码（Mask）的几何精度，无法区分模型是真正理解了语义，还是仅仅通过视觉显著性（如形状、颜色）走“捷径”。
*   **核心问题**：当视觉属性（如纹理、背景、材质）发生反事实改变时，分割模型是否还能忠实于语义概念？例如，一个被涂成斑马纹的象，模型是否会因为纹理而错误地将其识别为斑马？

### 2. 论文提出的方法论：CAFE 基准
*   **核心思想**：提出 **CAFE (Counterfactual Attribute Factuality Evaluation)** 基准，通过保持目标区域形状（Mask）不变，仅修改其关键属性，构建“正向提示词（语义正确）”与“误导性负向提示词（视觉诱导但语义错误）”的配对测试。
*   **三种反事实场景**：
    1.  **表面模仿 (Superficial Mimicry, SM)**：修改物体表面纹理（如：花瓶涂上西瓜纹理）。正向：花瓶；负向：西瓜。
    2.  **上下文冲突 (Context Conflict, CC)**：将物体置于不合理的背景中（如：滑板者在雪地里）。正向：滑板者；负向：滑雪者。
    3.  **本体冲突 (Ontological Conflict, OC)**：改变物体的材质/本质（如：飞机形状的云）。正向：云；负向：真实飞机。
*   **算法流程与评估指标**：
    *   **Class-gated F1 (cgF1)**：结合图像级概念识别（IL-MCC）和定位质量（pmF1）。
    *   **目标对齐指标**：定义了 **AFPR**（目标对齐的误报率）和 **ACSR**（概念交换率），专门衡量模型在错误提示下产生高精度掩码的频率。

### 3. 实验设计
*   **数据集来源**：从 COCO-Val2017、LVIS-Val 和 SA-Co/Gold 中提取原始图像和标注。
*   **基准构建**：利用 Gemini 3 生成编辑指令，Nano-banana-2 进行图像编辑，并经过三轮严格的人工筛选，最终保留 2,146 个高质量配对样本。
*   **对比方法**：
    *   **端到端模型**：SAM3, YOLO-World, OpenSeeD。
    *   **多模型框架**：Grounded SAM2, OWLv2 + SAM1。
    *   **智能体方法**：CAFE-SAM3 (基于 GPT-5.5 的多轮推理验证变体)。

### 4. 资源与算力
*   **算力设备**：实验在 **NVIDIA RTX 5090 GPU**（32GB 显存）上运行。
*   **训练/推理时长**：论文未提及训练时长，因为所有实验均为**纯推理（Inference-only）**评估。
*   **模型规模**：使用了各基准模型的官方发布版本（如 SAM3, Grounding DINO base, SAM2.1 Hiera-Large 等）。

### 5. 实验数量与充分性
*   **实验规模**：对 5 种主流模型及 1 种智能体变体在 3 大反事实类别上进行了全面评估。
*   **充分性**：实验设计非常充分。除了标准指标外，还进行了 **IoU 阈值敏感性分析**（证明错误是语义性的而非边界精度问题）和**多轮对话推理实验**。
*   **客观性**：通过人工交叉验证（三名专家审核）确保了反事实编辑的语义有效性，并对非原生支持“存在性预测”的模型进行了阈值校准，确保了对比的公平性。

### 6. 论文的主要结论与发现
*   **定位不等于理解**：模型经常能为误导性提示生成几何上非常精确的掩码（高 AFPR），这证明它们在很大程度上依赖视觉捷径而非语义逻辑。
*   **本体冲突最难**：所有模型在“本体冲突（OC）”类别上表现最差，SAM3 的 IL-MCC 甚至出现负值，表明模型在区分“形状相似但本质不同”的物体时存在严重缺陷。
*   **推理能力是关键**：引入 MLLM（如 GPT-5.5）进行显式推理验证的 CAFE-SAM3 显著提升了性能（cgF1 从 38.5 升至 63.3），说明逻辑推理能有效缓解反事实诱导。

### 7. 优点
*   **视角独特**：首次系统性地探讨了分割模型在属性级反事实干预下的语义忠实度。
*   **高质量数据**：4.4% 的极低样本保留率确保了基准测试的严谨性和挑战性。
*   **诊断性强**：提出的 AFPR 和 ACSR 指标能精准定位模型“指鹿为马”的具体失效模式。

### 8. 不足与局限
*   **场景复杂度有限**：目前每个图像仅评估一个反事实目标，未涉及多目标或拥挤场景下的复杂交互。
*   **依赖编辑工具**：基准的质量受限于图像编辑模型（如 Nano-banana）的生成能力，可能存在微小的视觉伪影。
*   **应用限制**：主要针对文本提示模型，对于纯视觉提示（点、框）的鲁棒性探讨较少。

（完）
