---
title: Taming the Curses of Multiagency in Robust Markov Games with Large State Space through Linear Function Approximation
title_zh: 通过线性函数近似克服具有大状态空间的鲁棒马尔可夫博弈中的多智能体诅咒
authors: "Jingchu Gai, Laixi Shi"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03125v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 大状态空间鲁棒马尔可夫博弈中的多智能体强化学习
tldr: 本研究针对多智能体强化学习中的鲁棒性与数据效率挑战，探讨了具有大规模状态空间的鲁棒马尔可夫博弈（RMGs）。通过引入线性函数近似（LFA），作者针对全变分距离定义的不确定性集合，开发了在生成模型和在线交互设置下均能打破“多智能体诅咒”的算法。这是首个在无限状态空间下实现样本复杂度与智能体数量解耦的研究，显著提升了大规模多智能体系统的学习效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03125v1/fig-001.webp\", \"caption\": \"Table 1: The existing results of sample complexity in the generative model setting (or regret in the online setting) for finding an ε-approximate equilibrium in finite-horizon, multi-agent, general-sum robust Markov games (RMGs), omitting logarithmic factors. To our knowledge, this work provides the first algorithms that break the curse of multiagency for large (possibly infinite) state spaces using linear function approximation.\", \"page\": 3, \"index\": 1, \"width\": 941, \"height\": 360}]"
motivation: 旨在解决多智能体强化学习在面对环境不确定性时的鲁棒性问题，并克服大规模状态空间带来的“多智能体诅咒”。
method: 采用线性函数近似（LFA）方法，并针对全变分距离定义的鲁棒不确定性集合，设计了可证明数据高效的算法。
result: 在生成模型和在线交互两种设置下，成功实现了样本复杂度与智能体数量的解耦，打破了多智能体诅咒。
conclusion: 该研究首次证明了在大规模或无限状态空间的鲁棒马尔可夫博弈中，可以通过线性函数近似实现高效的样本利用率。
---

## 摘要
多智能体强化学习（MARL）具有巨大潜力，但由于环境的不确定性，面临着鲁棒性挑战。为了解决这一问题，分布鲁棒马尔可夫博弈（RMGs）在环境偏离不确定性集内的名义模型时，优化最坏情况下的性能。除了鲁棒性，MARL 的另一个同样紧迫的目标是数据效率——从随智能体数量呈指数级增长的巨大状态和动作空间中进行采样，可能会导致“多智能体诅咒”。然而，目前可证明数据高效的 RMGs 算法仅限于具有有限状态和动作空间的表格设置，这仅在小规模问题上具有计算可行性，使得具有大规模（或无限）状态空间的 RMGs 在很大程度上尚未得到探索。现有唯一超越表格设置的工作专注于使用消失最小价值假设的一类受限 RMGs 的线性函数近似（LFA），且其样本复杂度仍受多智能体诅咒的影响。在这项工作中，我们专注于具有 LFA 的通用 RMGs。对于由全变分距离定义的不确定性集，我们开发了可证明数据高效的算法，在生成模型设置和新提出的在线交互设置中都打破了多智能体诅咒。据我们所知，无论不确定性集如何构建，我们的结果都是首次打破了具有大（可能是无限）状态空间的 RMGs 样本复杂度的多智能体诅咒。

## Abstract
Multi-agent reinforcement learning (MARL) holds great potential but faces robustness challenges due to environmental uncertainty. To address this, distributionally robust Markov games (RMGs) optimize worst-case performance when the environment deviates from the nominal model within a uncertainty set. Beyond robustness, an equally urgent goal for MARL is data efficiency -- sampling from vast state and action spaces that grow exponentially with the number of agents potentially leads to the curse of multiagency. However, current provably data-efficient algorithms for RMGs are limited to tabular settings with finite state and action spaces, which are only computationally manageable for small-scale problems, leaving RMGs with large-scale (or infinite) state spaces largely unexplored. The only existing work beyond tabular settings focuses on linear function approximation (LFA) for a restrictive class of RMGs using vanish minimal value assumption and still suffers from sample complexity with the curse of multiagency. In this work, we focuses on general RMGs with LFA. For uncertainty sets defined by total variation distance, we develop provably data-efficient algorithms that break the curse of multiagency in both the generative model setting and a newly proposed online interactive setting. To our knowledge, our results are the first to break the curse of multiagency of sample complexity for RMGs with large (possibly infinite) state spaces, regardless of the uncertainty set construction.

---

## 论文详细总结（自动生成）

这是一份关于学术论文《Taming the Curses of Multiagency in Robust Markov Games with Large State Space through Linear Function Approximation》的结构化分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：多智能体强化学习（MARL）在实际应用中面临两大核心挑战：一是**环境不确定性**（需要鲁棒性），二是**维度灾难**（状态空间巨大且样本复杂度随智能体数量指数级增长，即“多智能体诅咒”）。
*   **核心问题**：现有的鲁棒马尔可夫博弈（RMG）算法大多局限于小规模的“表格设置”（Tabular Setting）。在具有大规模甚至无限状态空间的环境下，如何设计既能保证鲁棒性，又能通过函数近似（Function Approximation）打破多智能体诅咒的算法，是一个尚未解决的难题。
*   **整体含义**：本文首次证明了在大型状态空间中，利用**线性函数近似（LFA）**可以实现数据高效的鲁棒 MARL，使样本复杂度与智能体数量解耦。

### 2. 论文提出的方法论
论文针对两种数据获取机制提出了不同的算法：

*   **核心思想**：将分布鲁棒优化（DRO）集成到线性马尔可夫博弈框架中，利用线性结构的叠加性来抵消智能体数量带来的指数增长。
*   **生成模型设置（L-Robust-Q-FTRL 算法）**：
    *   **无限到有限约简**：利用基元分布（Lemma 1）在特征空间中选取有限的代表性状态-动作对进行采样。
    *   **名义模型估计**：通过脊回归（Ridge Regression）估计名义转移核和奖励函数。
    *   **鲁棒 Bellman 更新**：利用鲁棒 Bellman 方程的对偶形式更新 Q 函数，并结合 FTRL（跟随正则化领导者）策略更新算法。
*   **在线交互设置（Online-L-Robust-Q-FTRL 算法）**：
    *   **混合采样策略（Hybrid-Sampling）**：这是本文的关键创新。由于对抗环境未知，算法同时维护**乐观**（用于策略更新）和**悲观**（用于模拟对抗环境采样）两套鲁棒价值估计。
    *   **采样流程**：前 $h-1$ 步在近似对抗环境下采样，第 $h$ 步在名义环境下采样，从而在不满足线性假设的扰动环境中提取出符合线性结构的名义模型信息。

### 3. 实验设计
*   **性质说明**：本论文属于**纯理论证明型论文**（Theoretical Paper），未包含基于特定数据集（如 MuJoCo 或 Atari）的实证实验。
*   **Benchmark（基准）**：其对比对象是现有的理论界限（见论文 Table 1），包括：
    *   表格设置下的 RMG 算法（如 P2MPO, DR-NVI 等）。
    *   现有的唯一线性 RMG 工作（Zheng & Lin, 2025），指出其仍受多智能体诅咒影响且需要更严苛的假设。
*   **验证方式**：通过严密的数学推导（收敛性分析、样本复杂度分析、遗憾值分析）来验证算法的有效性。

### 4. 资源与算力
*   **算力说明**：论文中**未提及**任何 GPU/CPU 硬件型号、数量或训练时长。
*   **原因**：由于是理论研究，其核心产出是数学定理（Theorem 1, 2, 3）及其证明过程，不涉及大规模数值计算或深度学习训练。

### 5. 实验数量与充分性
*   **理论充分性**：论文提供了长达 40 页的详细推导，涵盖了从鲁棒纳什均衡的存在性证明到生成模型下的样本复杂度界限，再到在线设置下的遗憾值分析。
*   **逻辑严密性**：作者针对 TV（全变分）不确定性集进行了深入讨论，并对算法在不同设置下的表现给出了明确的数学界限（如生成模型下为 $\tilde{O}(H^9 d^3 / \epsilon^4)$），从理论逻辑角度看是非常充分且客观的。
*   **缺乏实证**：从工程角度看，缺乏在模拟环境中的数值验证，这在纯理论强化学习论文中是常见现象。

### 6. 论文的主要结论与发现
*   **打破诅咒**：证明了在鲁棒线性马尔可夫博弈中，样本复杂度可以只随单个智能体的最大动作空间（而非联合动作空间）多项式级增长。
*   **理论界限**：
    *   在生成模型下，实现了首个不随智能体数量指数增长的样本复杂度保证。
    *   在在线交互下，实现了次线性遗憾值 $\tilde{O}(H^2 d \max A_i \sqrt{T})$。
*   **均衡存在性**：扩展了 Glicksberg 固定点定理，证明了在无限状态空间的 R-LMG 中鲁棒纳什均衡的存在性。

### 7. 优点：方法或实验设计上的亮点
*   **理论突破**：填补了鲁棒 MARL 在大规模状态空间下理论保证的空白。
*   **采样创新**：提出的“混合采样”机制巧妙地解决了“扰动环境不满足线性结构”与“名义模型需要线性估计”之间的矛盾。
*   **通用性**：算法设计不依赖于特定的不确定性集构造（虽然重点讨论了 TV 距离），具有较强的理论迁移潜力。

### 8. 不足与局限
*   **假设限制**：研究主要基于**线性函数近似**，虽然比表格设置进步，但仍无法完全涵盖深度强化学习中高度非线性的神经网络近似。
*   **不确定性集单一**：核心结论主要针对全变分（TV）距离，对于 KL 散度或 Wasserstein 距离等其他常见的鲁棒集，其复杂度和算法设计可能需要调整。
*   **缺乏实证支持**：没有在实际的多智能体基准环境（如 SMARTS 自动驾驶模拟器）中验证算法的实际运行效率和鲁棒性能。
*   **计算开销**：虽然样本效率高，但每一轮迭代中的脊回归和对偶优化在处理极高维特征 $d$ 时可能存在计算瓶颈。

（完）
