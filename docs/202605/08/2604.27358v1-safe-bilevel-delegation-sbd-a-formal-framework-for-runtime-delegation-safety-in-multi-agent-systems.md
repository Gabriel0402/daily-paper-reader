---
title: "Safe Bilevel Delegation (SBD): A Formal Framework for Runtime Delegation Safety in Multi-Agent Systems"
title_zh: 安全双层授权 (SBD)：多智能体系统中运行时授权安全的形式化框架
authors: Yuan Sun
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27358v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 大语言模型多智能体系统运行时委派安全的正式框架
tldr: 针对大语言模型智能体在关键任务中的授权安全问题，本文提出安全双层授权（SBD）框架。该框架利用双层优化动态平衡安全与效率，引入连续授权度实现人工干预到自主执行的平滑过渡，并给出了安全性单调性、收敛性及责任传播边界的理论证明，为多智能体系统提供了运行时安全保障。
source: arxiv
selection_source: fresh_fetch
motivation: 现有的多智能体架构缺乏在执行过程中根据任务上下文动态调整安全与效率权衡的运行时机制。
method: 采用双层优化模型，外层学习上下文相关的安全权重，内层在概率安全约束下优化授权策略。
result: 理论证明了安全单调性、内层策略的线性收敛性以及多跳授权链中的责任传播边界。
conclusion: SBD框架为高风险环境下多智能体系统的动态授权提供了严谨的数学基础和安全保障机制。
---

## 摘要
随着大语言模型 (LLM) 智能体被部署在关键任务环境中，如何安全地将子任务授权给专门的子智能体变得至关重要。现有工作主要在设计阶段解决多智能体架构选择问题，或提供广泛的经验性指导，但两者都未能提供一种在执行过程中随任务上下文变化而动态调整安全与效率权衡的运行时机制。我们提出了安全双层授权 (SBD)，这是一个用于分层多智能体系统中运行时授权安全的形式化框架。SBD 将任务授权建模为一个双层优化问题：外层元权重网络 phi 学习与上下文相关的安全-效率权重 lambda(s) 在 [0,1] 之间；内层循环在满足概率安全约束 P(safe) >= 1-delta 的前提下优化授权策略 pi。连续授权程度 alpha 在 [0, 1] 之间控制转移给每个子智能体的决策权限，在完全人工干预 (alpha=0) 和完全自主执行 (alpha=1) 之间进行平滑插值。我们确立了三个理论结果：(1) 安全单调性——更高的外层安全权重会产生弱更安全的内层策略；(2) 内层策略收敛性——在标准平滑性假设下，内层问题的投影梯度下降法呈线性收敛；(3) 问责传播界限，该界限在多跳授权链中分配责任，并具有可证明的单智能体上限。我们在三个高风险领域——医疗 AI (MIMIC-III)、金融风险控制 (S&P 500) 和教育智能体监管 (ASSISTments) 中实例化了 SBD，并指定了数据集、安全约束集、基准模型和评估协议。本手稿完整展示了形式化框架和理论结果；按照文中所述协议进行的实证验证正在计划中，并将在后续修订版中报告。

## Abstract
As large language model (LLM) agents are deployed in high-stakes environments, the question of how safely to delegate subtasks to specialized sub-agents becomes critical. Existing work addresses multi-agent architecture selection at design time or provides broad empirical guidelines, but neither provides a runtime mechanism that dynamically adjusts the safety-efficiency trade-off as task context changes during execution.   We propose Safe Bilevel Delegation (SBD), a formal framework for runtime delegation safety in hierarchical multi-agent systems. SBD formulates task delegation as a bilevel optimization problem: an outer meta-weight network phi learns context-dependent safety-efficiency weights lambda(s) in [0,1]; an inner loop optimizes the delegation policy pi subject to a probabilistic safety constraint P(safe) >= 1-delta. The continuous delegation degree alpha in [0, 1] controls how much decision authority is transferred to each sub-agent, interpolating smoothly between full human override (alpha=0) and fully autonomous execution (alpha=1).   We establish three theoretical results: (1) Safety Monotonicity--higher outer safety weight produces a weakly safer inner policy; (2) Inner Policy Convergence--projected gradient descent on the inner problem converges linearly under standard smoothness assumptions; (3) an Accountability Propagation bound that distributes responsibility across multi-hop delegation chains with a provable per-agent ceiling. We instantiate SBD in three high-stakes domains--medical AI (MIMIC-III), financial risk control (S and P 500), and educational agent supervision (ASSISTments)--specifying datasets, safety constraint sets, baselines, and evaluation protocols. This manuscript presents the formal framework and theoretical results in full; empirical validation following the protocols described herein is planned and will be reported in a forthcoming revision.

---

## 论文详细总结（自动生成）

这是一份关于论文《Safe Bilevel Delegation (SBD): A Formal Framework for Runtime Delegation Safety in Multi-Agent Systems》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体被广泛应用于医疗、金融等高风险领域，如何安全地将子任务授权给专门的子智能体（Sub-agents）成为核心挑战。
*   **研究动机**：现有的多智能体安全机制大多在“设计时”固定（如预设权限边界或固定的人工检查点），无法根据执行过程中的实时风险上下文动态调整。
*   **核心问题**：主智能体应该向子智能体转移多少决策权？这种转移如何随风险环境的变化而动态演进？
*   **整体含义**：论文提出了 **SBD（安全双层授权）** 框架，将授权问题形式化为双层优化问题，旨在实现运行时（Runtime）安全与效率的最优平衡。

### 2. 方法论：核心思想与技术细节
SBD 的核心是将授权权限视为一个连续的标量 $\alpha \in [0, 1]$（授权度），并利用双层优化结构进行动态调节：
*   **双层优化模型**：
    *   **外层（元权重学习）**：学习一个元权重网络 $\phi(s)$，它根据当前状态 $s$ 输出安全权重 $\lambda$。当环境风险升高时，$\lambda$ 增大，迫使系统优先考虑安全性。
    *   **内层（策略优化）**：在给定的 $\lambda$ 下，优化授权策略 $\pi$，同时满足概率安全约束 $P(safe) \ge 1 - \delta$。
*   **关键技术细节**：
    *   **连续授权度 $\alpha$**：$\alpha=0$ 代表完全人工干预，$\alpha=1$ 代表完全自主执行，中间值代表决策融合。
    *   **超梯度计算**：使用隐式微分（Implicit Differentiation）处理外层对内层最优解的依赖。
    *   **责任传播机制**：定义了问责权重 $w_j$，用于在多级授权链中量化每个智能体应承担的责任比例。
*   **算法流程**：采用投影梯度下降（PGD）处理内层约束，结合基于 `higher` 库的自动微分处理外层元梯度更新。

### 3. 实验设计与基准测试
**注意：该版本（V1）论文为预注册（Pre-registered）版本，详细说明了实验协议，但尚未包含最终实验数据。**
*   **应用场景与数据集**：
    1.  **医疗 AI**：使用 MIMIC-III 数据集，根据患者病情严重程度（APACHE-II 评分）调整授权。
    2.  **金融风控**：使用 S&P 500 数据，在 2020 年新冠市场崩溃等极端压力测试下评估。
    3.  **教育监管**：使用 ASSISTments 数据集，针对“风险学生”群体调整教学智能体的授权。
*   **对比方法（Baselines）**：
    *   **Fixed-$\alpha$**：固定授权比例。
    *   **Safe-RL (CPO)**：传统的受限策略优化。
    *   **MARL-vanilla**：无安全机制的多智能体强化学习。
    *   **MaAS**：静态架构搜索方法。
    *   **SBD-NoOuter**：消融实验，移除外层动态权重学习。

### 4. 资源与算力
*   **硬件配置**：单张 **NVIDIA A100 (80GB)** GPU。
*   **训练时长**：在 Tout=500（外层迭代）和 Tin=50（内层步数）的配置下，每个领域的完整训练运行时间约为 **7 分钟**。
*   **计算效率**：单次外层迭代耗时约 0.8 秒，显示出该框架具有较好的运行时效率。

### 5. 实验数量与充分性
*   **实验规模**：计划在 3 个不同领域进行测试，每个实验使用 3 个随机种子。
*   **消融实验**：设计了四种变体（无外层、离散授权、固定 Lambda、无约束）来验证各组件的必要性。
*   **理论验证实验**：专门设计了针对“安全单调性”、“线性收敛性”和“责任边界”的直接实证测试。
*   **客观性评价**：论文预先设定了**证伪标准（Falsifiability Criteria）**，明确了在何种实验结果下该框架将被视为无效，这种“先声明协议再执行”的方法极大地提高了研究的客观性和科学性。

### 6. 主要结论与发现（基于理论证明）
由于实验结果待报，目前的主要结论基于数学证明：
*   **安全单调性**：证明了外层安全权重的增加必然导致内层策略在统计上更加安全。
*   **线性收敛性**：在标准平滑性假设下，内层 PGD 算法能够线性收敛到最优授权策略。
*   **责任上限**：证明了在多跳授权链中，没有任何单一智能体可以承担全部责任，责任会随授权链条自然稀释。

### 7. 优点与亮点
*   **动态适应性**：突破了静态安全规则的限制，实现了“风险越高，授权越谨慎”的闭环控制。
*   **理论严谨**：不仅提出了算法，还提供了关于安全性、收敛性和问责制的数学保证。
*   **连续性控制**：引入连续授权度 $\alpha$，比传统的“二选一（自主 vs 停机）”授权更精细，支持平滑的人机协作。
*   **协议规范**：采用预注册机制，有效防止了事后挑选实验数据（Cherry-picking）的风险。

### 8. 不足与局限
*   **实验缺失**：V1 版本暂无实证数据支持，理论假设是否能在复杂神经网络中完美契合仍待验证。
*   **强凸性假设**：理论证明依赖于内层损失函数的强凸性，但在深度学习实践中，这一条件往往难以严格满足。
*   **安全集依赖**：SBD 假设安全约束集 $C$ 是预先给定的专家知识，但在某些领域，定义完美的“安全边界”本身就是一个难题。
*   **单层安全模型**：目前只关注“授权决策”的安全性，尚未建模子智能体在执行过程中的内部故障。

（完）
