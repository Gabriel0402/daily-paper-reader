---
title: Taming the Curses of Multiagency in Robust Markov Games with Large State Space through Linear Function Approximation
title_zh: 通过线性函数近似克服具有大状态空间的鲁棒马尔可夫博弈中的多智能体诅咒
authors: "Jingchu Gai, Laixi Shi"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03125v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 马尔可夫博弈中的多智能体强化学习（MARL）
tldr: 本研究针对多智能体强化学习中的鲁棒性与数据效率挑战，探讨了具有大规模状态空间的鲁棒马尔可夫博弈（RMG）。针对现有算法在处理大规模状态时面临的“多智能体诅咒”问题，本文引入线性函数近似，并针对全变分距离定义的偏差集合，提出了在生成模型和在线交互设置下均能打破该诅咒的高效算法，实现了在大规模状态空间下的样本复杂度优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.03125v2/fig-001.webp\", \"caption\": \"Table 1: The existing results of sample complexity in the generative model setting (or regret in the online setting) for finding an ε-approximate equilibrium in finite-horizon, multi-agent, general-sum robust Markov games (RMGs), omitting logarithmic factors. To our knowledge, this work provides the first algorithms that break the curse of multiagency for large (possibly infinite) state spaces using linear function approximation.\", \"page\": 3, \"index\": 1, \"width\": 941, \"height\": 360}]"
motivation: 现有的鲁棒马尔可夫博弈算法大多局限于表格设置，且在处理大规模状态空间时面临样本复杂度随智能体数量指数增长的“多智能体诅咒”。
method: 采用线性函数近似处理大规模状态空间，并针对基于全变分距离的鲁棒不确定性集，设计了适用于生成模型和在线交互场景的数据高效算法。
result: 成功开发出首个在大规模（甚至无限）状态空间下打破多智能体诅咒的RMG算法，显著降低了样本复杂度。
conclusion: 该研究证明了通过线性函数近似可以有效解决鲁棒马尔可夫博弈中的扩展性问题，为大规模多智能体系统的鲁棒决策提供了理论支撑。
---

## 摘要
多智能体强化学习（MARL）具有巨大潜力，但由于环境的不确定性，面临着鲁棒性挑战。为了解决这一问题，分布鲁棒马尔可夫博弈（RMGs）在环境偏离不确定性集内的名义模型时，优化最坏情况下的性能。除了鲁棒性，MARL 的另一个同样紧迫的目标是数据效率——从随智能体数量呈指数增长的巨大状态和动作空间中采样，可能会导致“多智能体诅咒”。然而，目前可证明数据高效的 RMGs 算法仅限于具有有限状态和动作空间的表格设置，这仅在小规模问题上具有计算可行性，使得具有大规模（或无限）状态空间的 RMGs 在很大程度上尚未得到探索。现有在表格设置之外的唯一工作集中在针对一类受限 RMGs 的线性函数近似（LFA），该工作使用了消失最小值的假设，且其样本复杂度仍受多智能体诅咒的影响。在这项工作中，我们关注具有 LFA 的一般 RMGs。对于由全变分距离定义的不确定性集，我们开发了可证明数据高效的算法，在生成模型设置和新提出的在线交互设置中都打破了多智能体诅咒。据我们所知，无论不确定性集如何构建，我们的结果都是首次打破了具有大（可能是无限）状态空间的 RMGs 在样本复杂度上的多智能体诅咒。

## Abstract
Multi-agent reinforcement learning (MARL) holds great potential but faces robustness challenges due to environmental uncertainty. To address this, distributionally robust Markov games (RMGs) optimize worst-case performance when the environment deviates from the nominal model within a uncertainty set. Beyond robustness, an equally urgent goal for MARL is data efficiency -- sampling from vast state and action spaces that grow exponentially with the number of agents potentially leads to the curse of multiagency. However, current provably data-efficient algorithms for RMGs are limited to tabular settings with finite state and action spaces, which are only computationally manageable for small-scale problems, leaving RMGs with large-scale (or infinite) state spaces largely unexplored. The only existing work beyond tabular settings focuses on linear function approximation (LFA) for a restrictive class of RMGs using vanish minimal value assumption and still suffers from sample complexity with the curse of multiagency. In this work, we focuses on general RMGs with LFA. For uncertainty sets defined by total variation distance, we develop provably data-efficient algorithms that break the curse of multiagency in both the generative model setting and a newly proposed online interactive setting. To our knowledge, our results are the first to break the curse of multiagency of sample complexity for RMGs with large (possibly infinite) state spaces, regardless of the uncertainty set construction.

---

## 论文详细总结（自动生成）

这篇论文《Taming the Curses of Multiagency in Robust Markov Games with Large State Space through Linear Function Approximation》由卡内基梅隆大学（CMU）和约翰霍普金斯大学的研究人员撰写。该研究聚焦于多智能体强化学习（MARL）在面对环境不确定性时的鲁棒性与扩展性问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：多智能体强化学习在自动驾驶、金融市场等领域潜力巨大，但面临两大挑战：
    1.  **环境不确定性**：学习到的策略对环境动态的微小扰动非常敏感。
    2.  **多智能体诅咒（Curse of Multiagency）**：样本复杂度随智能体数量呈指数级增长。
*   **核心问题**：现有的鲁棒马尔可夫博弈（RMG）算法大多局限于“表格设置”（有限状态空间），无法处理大规模或无限状态空间。唯一涉及线性函数近似（LFA）的现有工作仍受多智能体诅咒影响，且依赖于严苛的假设。
*   **整体含义**：本文旨在探索是否能在具有大规模状态空间的鲁棒马尔可夫博弈中，通过线性函数近似同时实现**鲁棒性**和**数据效率**，并打破多智能体诅咒。

### 2. 方法论：核心思想与关键技术
论文提出了**鲁棒线性马尔可夫博弈（R-LMG）**框架，并针对两种数据采集场景设计了算法：

*   **核心思想**：利用线性函数近似（LFA）来表示名义转移内核和奖励函数，并针对全变分（TV）距离定义的不确定性集进行优化。
*   **关键技术细节**：
    *   **无限到有限的归约（Infinite-to-Finite Reduction）**：在生成模型设置下，通过 Lemma 1（基于 Kiefer-Wolfowitz 定理）选取有限的特征点集进行采样，从而推导至整个无限状态空间。
    *   **L-Robust-Q-FTRL 算法（生成模型）**：结合岭回归（Ridge Regression）估计名义模型，并利用鲁棒贝尔曼方程的对偶形式更新 Q 函数，最后通过 FTRL（跟随正则化领导者）更新策略。
    *   **混合采样策略（Hybrid-Sampling）**：在在线交互设置中，由于最坏情况环境未知，算法使用**悲观鲁棒价值估计**来模拟对抗环境进行采样，同时在特定步骤切换回名义模型以维持线性结构的估计。
    *   **双重估计（在线设置）**：同时维护**乐观估计**（用于策略更新以促进探索）和**悲观估计**（用于近似对抗环境）。

### 3. 实验设计
*   **研究性质**：这是一篇**纯理论证明型论文**，不包含基于特定数据集（如 MuJoCo 或 Atari）的数值实验。
*   **Benchmark（基准）**：其对比对象是现有的理论复杂度上界，包括：
    *   表格设置下的鲁棒马尔可夫博弈算法（如 DR-NVI, Robust-Q-FTRL）。
    *   标准线性马尔可夫博弈算法（非鲁棒设置）。
    *   Zheng & Lin (2025) 提出的现有线性鲁棒算法。
*   **对比维度**：样本复杂度（Sample Complexity）和遗憾值（Regret）是否随智能体数量指数增长，以及是否支持无限状态空间。

### 4. 资源与算力
*   **说明**：论文中**未提及任何 GPU/CPU 算力资源或训练时长**。
*   **原因**：该研究通过数学分析和概率统计推导（如集中不等式、矩阵 Bernstein 不等式等）来证明算法的收敛性和复杂度，而非通过实验运行得出结论。

### 5. 实验数量与充分性
*   **理论验证**：论文提供了长达 40 页的严谨数学证明（附录 A-D）。
*   **充分性**：
    *   证明了鲁棒纳什均衡（Robust NE）在无限状态空间下的**存在性**（定理 1）。
    *   给出了生成模型下的样本复杂度界限（定理 2）。
    *   给出了在线交互设置下的遗憾值界限（定理 3）。
*   **客观性**：分析涵盖了从生成模型到更具挑战性的在线对抗环境，逻辑严密，符合理论计算机科学的标准。

### 6. 主要结论与发现
*   **打破诅咒**：本文算法是首个在具有大规模（甚至无限）状态空间的 RMG 中，证明样本复杂度不随智能体数量指数增长的工作。
*   **复杂度界限**：
    *   **生成模型**：达到 $\epsilon$-近似粗略相关均衡（CCE）所需的样本量为 $\tilde{O}(H^9 d^3 / \epsilon^4)$。
    *   **在线设置**：遗憾值为 $\tilde{O}(d \max A_i H^2 \sqrt{T})$，其中 $d$ 是特征维度，$A_i$ 是单个智能体的动作空间大小。
*   **理论贡献**：提出的混合采样机制解决了“如何在不确定性集内采样以估计名义模型”的难题。

### 7. 优点
*   **理论突破**：填补了鲁棒 MARL 在函数近似领域的空白，解决了长期存在的扩展性难题。
*   **设置务实**：提出的在线交互协议（允许与近似对抗环境交互）比传统的仅名义环境交互更符合安全强化学习的实际需求。
*   **技术创新**：引入悲观估计来辅助采样，这一设计在鲁棒 RL 领域具有启发性。

### 8. 不足与局限
*   **缺乏实证**：虽然理论完备，但未在实际仿真环境（如智能电网调度或多机器人协作）中验证算法的实际表现。
*   **线性假设**：算法依赖于线性函数近似，对于具有高度非线性特征的复杂环境（需深度神经网络处理），该理论的适用性尚待验证。
*   **不确定性集限制**：目前主要针对全变分（TV）距离，对于 KL 散度或 Wasserstein 距离等其他类型的鲁棒集，其结论是否成立仍需进一步研究。

（完）
