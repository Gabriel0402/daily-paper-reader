---
title: "Bridging MARL to SARL: An Order-Independent Multi-Agent Transformer via Latent Consensus"
title_zh: 连接 MARL 与 SARL：一种基于潜在共识的顺序无关多智能体 Transformer
authors: "Zijian Zhao, Jing Gao, Sen Li"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13472v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 使用中心化Transformer编码器进行协作强化学习的多智能体Transformer。
tldr: 针对协作式多智能体强化学习（MARL）中存在的非平稳性和协调性弱等问题，本文提出了共识多智能体Transformer（CMAT）。该框架通过将MARL转化为分层单智能体强化学习（SARL）形式，利用Transformer编码器处理联合观测，并通过解码器生成潜在共识向量，实现了智能体间的无序并行决策。实验证明，CMAT在StarCraft II等多个基准任务上优于现有主流方法，提升了训练稳定性和协调效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13472v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison between CMAT and Conventional Decentralized MARL Methods.\", \"page\": 2, \"index\": 1, \"width\": 748, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2604.13472v1/fig-002.webp\", \"caption\": \"Figure 4: Training Curves under 5 Random Seeds: The shadow parts represent the standard deviation.\", \"page\": 8, \"index\": 2, \"width\": 822, \"height\": 871}, {\"url\": \"assets/figures/arxiv/2604.13472v1/fig-003.webp\", \"caption\": \"Figure 5: Ablation Study under 5 Random Seeds\", \"page\": 9, \"index\": 3, \"width\": 822, \"height\": 870}, {\"url\": \"assets/figures/arxiv/2604.13472v1/fig-004.webp\", \"caption\": \"Figure 3: Network Architecture: The Transformer encoder first extracts the features and relationships among the observations of all agents, compressing them into a single initial consensus vector. This vector is used for V-value estimation and iterated by the Transformer decoder to produce the final consensus vector. The final consensus vector is then combined with the extracted features of each agent’s observation from the encoder to generate actions.\", \"page\": 5, \"index\": 4, \"width\": 748, \"height\": 507}]"
motivation: 旨在解决传统多智能体强化学习中由于动作空间分解导致的非平稳性、训练不稳定以及对动作生成顺序敏感等挑战。
method: 提出一种基于Transformer的分层架构，通过生成潜在共识向量来指导所有智能体同时生成动作，从而将MARL问题转化为单智能体PPO可优化的形式。
result: 在StarCraft II、Multi-Agent MuJoCo和Google Research Football等基准测试中，CMAT的性能显著优于现有的集中式方案和序列化MARL方法。
conclusion: CMAT通过潜在共识机制成功桥接了多智能体与单智能体强化学习，提供了一种高效、顺序无关且具备强协调能力的通用多智能体决策框架。
---

## 摘要
协作式多智能体强化学习 (MARL) 广泛用于通过将中心化控制问题分解为多个相互作用的智能体，来处理庞大的联合观测和动作空间。然而，这种分解通常会引入额外的挑战，包括非平稳性、训练不稳定、协调性弱以及理论保证有限。在本文中，我们提出了共识多智能体 Transformer (CMAT)，这是一个将协作式 MARL 连接到分层单智能体强化学习 (SARL) 表述的中心化框架。CMAT 将所有智能体视为一个统一的实体，并采用 Transformer 编码器来处理庞大的联合观测空间。为了处理广阔的联合动作空间，我们引入了一种分层决策机制，其中 Transformer 解码器自回归地生成一个高层共识向量，模拟智能体在潜在空间中就其策略达成一致的过程。在该共识的条件下，所有智能体同时生成其动作，从而实现顺序无关的联合决策，并避免了传统多智能体 Transformer (MAT) 中对动作生成顺序的敏感性。这种分解允许使用单智能体 PPO 优化联合策略，同时通过潜在共识保持极具表现力的协调。为了评估所提出的方法，我们在 StarCraft II、Multi-Agent MuJoCo 和 Google Research Football 的基准任务上进行了实验。结果表明，CMAT 的性能优于近期的中心化解决方案、顺序 MARL 方法以及传统的 MARL 基准。本文代码可在 https://github.com/RS2002/CMAT 获取。

## Abstract
Cooperative multi-agent reinforcement learning (MARL) is widely used to address large joint observation and action spaces by decomposing a centralized control problem into multiple interacting agents. However, such decomposition often introduces additional challenges, including non-stationarity, unstable training, weak coordination, and limited theoretical guarantees. In this paper, we propose the Consensus Multi-Agent Transformer (CMAT), a centralized framework that bridges cooperative MARL to a hierarchical single-agent reinforcement learning (SARL) formulation. CMAT treats all agents as a unified entity and employs a Transformer encoder to process the large joint observation space. To handle the extensive joint action space, we introduce a hierarchical decision-making mechanism in which a Transformer decoder autoregressively generates a high-level consensus vector, simulating the process by which agents reach agreement on their strategies in latent space. Conditioned on this consensus, all agents generate their actions simultaneously, enabling order-independent joint decision making and avoiding the sensitivity to action-generation order in conventional Multi-Agent Transformers (MAT). This factorization allows the joint policy to be optimized using single-agent PPO while preserving expressive coordination through the latent consensus. To evaluate the proposed method, we conduct experiments on benchmark tasks from StarCraft II, Multi-Agent MuJoCo, and Google Research Football. The results show that CMAT achieves superior performance over recent centralized solutions, sequential MARL methods, and conventional MARL baselines. The code for this paper is available at:https://github.com/RS2002/CMAT .

---

## 论文详细总结（自动生成）

以下是对论文《Bridging MARL to SARL: An Order-Independent Multi-Agent Transformer via Latent Consensus》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的协作式多智能体强化学习（MARL）通常将中心化问题分解为多个独立或半独立的智能体，但这带来了非平稳性、训练不稳定和协调性弱等挑战。
*   **现有技术局限**：代表性方法 Multi-Agent Transformer (MAT) 虽然利用了 Transformer 架构，但其采用自回归方式顺序生成动作，导致性能对智能体动作生成的顺序高度敏感。优化顺序（如 PMAT）又会带来巨大的搜索空间（$n!$）。
*   **核心目标**：建立 MARL 与单智能体强化学习（SARL）之间的桥梁，提出一种顺序无关（Order-Independent）的架构，使多智能体系统能像单个实体一样进行高效、协调的决策。

### 2. 方法论：共识多智能体 Transformer (CMAT)
*   **核心思想**：将 MARL 重新表述为分层 SARL 问题。通过引入“潜在共识（Latent Consensus）”向量，模拟智能体在潜在空间达成一致的过程，随后所有智能体基于该共识并行生成动作。
*   **关键技术细节**：
    *   **编码器（Encoder）**：使用双向自注意力机制处理所有智能体的观测，提取特征并压缩为初始共识向量 $e_0$。
    *   **解码器（Decoder）**：自回归地迭代生成共识向量序列 $\{e_1, ..., e_m\}$。与 MAT 不同，这里迭代的是“策略共识”而非具体动作。
    *   **共识压缩（Actor-Compressor）**：将迭代过程中的所有向量混合，生成最终共识向量 $c$，以防止迭代过程中的信息丢失。
    *   **并行动作生成**：每个智能体的 Actor-MLP 同时接收自身观测特征和全局共识 $c$，独立但协调地输出动作。
*   **算法流程**：
    *   联合策略分解为：$\pi(A|O) = \pi_c(c|O) \prod_{i=1}^n \pi_i(a_i|O, c)$。
    *   由于共识生成是确定性的，该模型可以直接使用单智能体 PPO 算法进行端到端优化。
    *   **微调阶段**：提供“共识增强”或“动作策略增强”两种可选的微调模式，通过冻结部分参数进一步提升性能。

### 3. 实验设计
*   **实验场景**：
    *   **StarCraft II (SMAC)**：选取了极高难度的任务，如 `MMM2`、`6h vs 8z`、`3s5z vs 3s6z`。
    *   **Multi-Agent MuJoCo**：连续控制任务，如 `8x1-Agent Ant`、`6x1-Agent HalfCheetah`。
    *   **Google Research Football (GRF)**：复杂的足球协作场景。
*   **Benchmark 与对比方法**：
    *   **MAT / PMAT**：基于 Transformer 的序列化决策基准。
    *   **Triple-BERT**：现有的 SARL 风格 MARL 方法。
    *   **HAPPO / MAPPO**：经典的强力 MARL 基准方法。

### 4. 资源与算力
*   **硬件环境**：使用单台工作站，配置为 Intel i7-14700KF CPU 和 **NVIDIA RTX 4080 GPU**。
*   **算力消耗**：训练期间 GPU 显存占用仅为 **0.5-1.5 GB**，显示出该方法对显存极其友好。
*   **特别说明**：由于硬件限制，作者将 rollout 线程数设为 8（低于 MAT 官方推荐值），但在相同受限条件下与所有基准方法进行了公平对比。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流领域的 9 个复杂子任务。
*   **重复性**：所有实验均使用 **5 个随机种子**运行，并绘制了带标准差阴影的训练曲线。
*   **消融实验**：
    *   对比了“混合共识”与“仅使用最后一次迭代共识”的效果。
    *   分析了共识迭代次数 $m$ 对性能的影响（结论是 $m=n$ 效果最佳）。
*   **客观性**：实验设计考虑了微调前后的对比，并针对 MAT 的失败案例（纳什均衡陷阱）进行了专门的理论与实验分析，论证较为充分。

### 6. 主要结论与发现
*   **性能卓越**：CMAT 在所有测试场景中均优于 MAT、PMAT 和 MAPPO 等强基准，尤其在微调后达到了 SOTA 性能。
*   **顺序无关性**：通过潜在共识机制，CMAT 成功消除了动作生成顺序对结果的影响。
*   **全局优化能力**：相比于只能保证收敛到纳什均衡（NE）的序列化方法，CMAT 更有潜力收敛到全局最优解（Pareto 最优）。

### 7. 优点（亮点）
*   **架构优雅**：将复杂的 MARL 协调问题简化为分层 SARL 问题，使得单智能体优化技术能直接应用。
*   **理论支撑**：从协作式 Stackelberg 博弈的角度提供了理论解释，证明了其策略空间的表达能力强于 MAT。
*   **计算高效**：并行生成动作显著提高了推理效率，且显存占用极低。

### 8. 不足与局限
*   **全观测假设**：目前主要针对全观测（Fully Observable）环境设计，在部分观测（POMDP）或通信受限的去中心化执行场景下的表现尚待验证。
*   **硬件限制偏差**：作者坦承因硬件限制未能达到理论性能上限（线程数受限），虽然对比是公平的，但可能掩盖了某些方法在大规模并行下的潜力。
*   **通信开销**：作为中心化执行框架，在实际物理系统中可能面临中心节点单点故障或通信延迟的风险。

（完）
