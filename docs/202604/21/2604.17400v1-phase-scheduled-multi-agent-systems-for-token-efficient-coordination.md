---
title: Phase-Scheduled Multi-Agent Systems for Token-Efficient Coordination
title_zh: 用于 Token 高效协作的相位调度多智能体系统
authors: Mohit Dubey
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17400v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 用于高效令牌协作的相位调度多智能体系统
tldr: 针对大模型多智能体系统在并行执行和上下文共享中存在的 Token 效率低下问题，本文提出 PSMAS 框架。该方法将智能体激活重新构想为圆环流形上的相位调度，通过全局扫描信号按需激活智能体并压缩非活跃上下文。实验表明，PSMAS 在保持高性能的同时显著降低了 Token 成本，为多智能体协调提供了时间维度的新视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17400v1/fig-001.webp\", \"caption\": \"Figure 2: Sweep-field analysis. Top: four operating regimes; the velocity-failure boundary (red band) is sharp, confirming Theorem 7.3. Bottom: theory–empirical gap at small ε reflects summarisation overhead, which does not affect the scheduling gain.\", \"page\": 6, \"index\": 1, \"width\": 407, \"height\": 445}, {\"url\": \"assets/figures/arxiv/2604.17400v1/fig-002.webp\", \"caption\": \"Figure 1: PSMAS overview. (a) Agents on S1 by topological phase; sweep φ(t) (red) activates only A3 (within ε). (b) Dependency DAG with TPA phase labels derived by eq. (2). (c) Active agents receive full context; idle agents receive a compressed summary of length αL. Scheduling and compression are decoupled (§6): scheduling controls the idle fraction; compression controls the residual idle cost.\", \"page\": 4, \"index\": 2, \"width\": 1049, \"height\": 415}]"
motivation: 现有系统因无序并行和全量上下文共享导致 Token 浪费，且缺乏对协调过程中时间维度的有效建模。
method: 采用基于圆环流形的相位调度机制，根据任务拓扑分配相位并利用全局扫描信号控制智能体激活与上下文压缩。
result: "在多个结构化基准上实现平均 27.3% 的 Token 削减，性能仅下降 2.1%，优于现有的路由和剪枝基准。"
conclusion: 调度与压缩是独立的效率增益源，通过相位调度可实现更稳健、高效的多智能体协作。
---

## 摘要
由大语言模型驱动的多智能体系统（MAS）面临严重的 Token 效率低下问题，这源于两个叠加因素：(i) 非结构化的并行执行，即所有智能体不顾输入就绪状态同时激活；(ii) 无限制的上下文共享，即每个智能体都会接收完整的累积上下文，而不管其相关性。现有的缓解策略——静态剪枝、分层分解和学习型路由——将协作视为结构分配问题，从根本上忽略了其时间维度。我们提出了相位调度多智能体系统（PSMAS），该框架将智能体激活重新概念化为对基于圆流形建模的共享注意力空间的连续控制。每个智能体 i 被分配一个固定角相位 theta_i（范围在 [0, 2*pi] 内），该相位源自任务依赖拓扑；全局扫描信号 phi(t) 以速度 omega 旋转，仅激活角窗口 epsilon 内的智能体。空闲智能体接收压缩的上下文摘要，从而减少每步的 Token 消耗。我们在 LangGraph 上实现了 PSMAS，在四个结构化基准测试（HotPotQA-MAS、HumanEval-MAS、ALFWorld-Multi、WebArena-Coord）和两个非结构化对话场景中进行了评估，并证明了扫描动力学的稳定性、收敛性和最优性结果。PSMAS 实现了 27.3% 的平均 Token 削减（范围为 21.4-34.8%），同时将任务性能保持在全激活基准的 2.1 个百分点以内（p < 0.01，每种配置 n = 500），并且在 Token 削减方面比最强的学习型路由基准高出 5.6 个百分点，且性能下降减少了 2.0 个百分点。至关重要的是，我们表明调度和压缩是独立的增益来源：仅调度一项就贡献了 18-20 个百分点的削减，且在压缩退化高达 alpha = 0.40 时仍保持稳健。

## Abstract
Multi-agent systems (MAS) powered by large language models suffer from severe token inefficiency arising from two compounding sources: (i) unstructured parallel execution, where all agents activate simultaneously irrespective of input readiness; and (ii) unrestricted context sharing, where every agent receives the full accumulated context regardless of relevance. Existing mitigation strategies - static pruning, hierarchical decomposition, and learned routing - treat coordination as a structural allocation problem and fundamentally ignore its temporal dimension. We propose Phase-Scheduled Multi-Agent Systems (PSMAS), a framework that reconceptualizes agent activation as continuous control over a shared attention space modeled on a circular manifold.   Each agent i is assigned a fixed angular phase theta_i in the range [0, 2*pi], derived from the task dependency topology; a global sweep signal phi(t) rotates at velocity omega, activating only agents within an angular window epsilon. Idle agents receive compressed context summaries, reducing per-step token consumption. We implement PSMAS on LangGraph, evaluate on four structured benchmarks (HotPotQA-MAS, HumanEval-MAS, ALFWorld-Multi, WebArena-Coord) and two unstructured conversational settings, and prove stability, convergence, and optimality results for the sweep dynamics. PSMAS achieves a mean token reduction of 27.3 percent (range 21.4-34.8 percent) while maintaining task performance within 2.1 percentage points of a fully activated baseline (p < 0.01, n = 500 per configuration), and outperforms the strongest learned routing baseline by 5.6 percentage points in token reduction with 2.0 percentage points less performance drop. Crucially, we show that scheduling and compression are independent sources of gain: scheduling alone accounts for 18-20 percentage points of reduction, robust to compression degradation up to alpha = 0.40.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **PSMAS（相位调度多智能体系统）** 的创新框架，旨在解决大语言模型（LLM）多智能体协作中严重的 Token 浪费问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：当前多智能体系统（MAS）在执行任务时存在极大的 Token 冗余。这种低效主要源于两个方面：
    1.  **无序并行**：所有智能体往往同时激活，即使某些智能体尚未获得必要的输入。
    2.  **全量共享**：每个智能体在每一步都会接收完整的历史上下文，其中包含大量与其当前任务无关的信息。
*   **核心问题**：现有的优化方法（如剪枝、分层、路由）仅将协作视为“结构分配”问题，忽略了**时间维度**上的控制。论文试图通过引入连续的时间调度机制来降低 Token 消耗。

### 2. 方法论：PSMAS 框架
*   **核心思想**：将智能体的激活状态建模在**圆环流形 $S^1$** 上。通过一个全局的“扫描信号”像雷达一样旋转，只有当信号扫过某个智能体的“相位”时，该智能体才被激活。
*   **关键技术细节**：
    *   **相位分配（Phase Assignment）**：根据任务依赖的有向无环图（DAG），为每个智能体分配一个角度 $\theta_i$。
        *   *拓扑相位分配 (TPA)*：基于拓扑排序均匀分布相位。
        *   *加权相位分配 (WPA)*：根据智能体的预期推理延迟按比例拉开相位间距。
    *   **扫描信号与激活窗口**：全局信号 $\phi(t) = (\omega t) \mod 2\pi$ 以速度 $\omega$ 旋转。若智能体相位与信号的距离在窗口 $\epsilon$ 内，则激活。
    *   **上下文交付策略**：
        *   **活跃智能体**：接收完整上下文。
        *   **空闲智能体**：仅接收由轻量级模型（如 Mistral-7B）生成的压缩摘要（压缩率 $\alpha \approx 0.12$），从而大幅节省 Token。
*   **理论保证**：论文通过数学证明了该系统在延迟噪声下的稳定性、对全激活状态的收敛性，并推导出了最优激活窗口 $\epsilon^*$ 的解析解。

### 3. 实验设计
*   **数据集与场景**：
    *   **结构化基准**：HotPotQA-MAS（多跳问答）、HumanEval-MAS（代码生成）、ALFWorld-Multi（具身智能任务）、WebArena-Coord（网页导航协作）。
    *   **非结构化场景**：AgentDebate（自由辩论）、ResearchBench（开放式研究助理）。
*   **对比方法（Baselines）**：
    *   全激活（Full Activation）、静态剪枝（Static Pruning）、随机调度（Random Sched.）。
    *   **强力竞争对手**：HuggingGPT（基于 LLM 的路由）、RouterLLM（基于 MLP 的自适应路由）。

### 4. 资源与算力
*   **算力说明**：论文提到使用 **vLLM** 部署 **Mistral-7B-Instruct-v0.2** 作为摘要后端，并采用了**投机采样（Speculative Decoding）**技术。
*   **具体细节**：文中未明确给出具体的 GPU 型号（如 A100 或 H100）及总训练/运行小时数，但提到摘要生成的平均延迟为 1.18 秒，且 PSMAS 的代码实现仅需约 80 行 Python 代码（基于 LangGraph）。

### 5. 实验数量与充分性
*   **实验规模**：每种配置进行了 **n=500** 次实验，确保了统计显著性（$p < 0.01$）。
*   **充分性**：
    *   涵盖了从线性链到复杂 DAG 的多种任务拓扑。
    *   进行了详尽的**消融实验**，验证了相位分配策略、压缩率 $\alpha$、扫描速度 $\omega$ 对结果的影响。
    *   特别探讨了“调度”与“压缩”的解耦关系，证明了即使压缩效果变差，调度机制本身依然能贡献约 20% 的 Token 削减。
*   **客观性**：在非结构化场景下进行了“诚实评估”，承认了在依赖关系不明确时增益会减半，表现出较好的学术客观性。

### 6. 主要结论与发现
*   **高效性**：PSMAS 实现了平均 **27.3%** 的 Token 削减，最高可达 **34.8%**。
*   **性能保持**：在大幅减支的情况下，任务成功率仅下降了 **2.1%**，显著优于 RouterLLM 等现有最强基准。
*   **延迟优化**：由于减少了 Token 处理量，尽管增加了摘要生成的开销，总推理延迟反而降低了约 **20-28%**。
*   **独立增益**：证明了调度和压缩是独立的增益源，调度控制“空闲比例”，压缩控制“残余成本”。

### 7. 优点与亮点
*   **视角独特**：首次将多智能体协调建模为圆环流形上的**连续控制问题**，而非简单的离散调度。
*   **数学严谨**：提供了完备的稳定性证明和失效模式分类（F1-F3），使系统具备可解释性和预测性。
*   **易于集成**：作为 LangGraph 的中间件实现，侵入性小，易于在现有生产环境中部署。
*   **解耦分析**：清晰地回答了“究竟是压缩起作用还是调度起作用”的质疑。

### 8. 不足与局限
*   **结构依赖**：在任务依赖图（DAG）完全未知或高度动态变化的场景下，Token 削减效果会从 ~30% 下降到 ~15%。
*   **摘要模型开销**：虽然 Token 总数减少，但需要额外运行一个摘要模型，对于极小规模的 MAS 系统，这部分计算开销可能抵消收益。
*   **循环依赖挑战**：对于存在循环依赖的任务，系统可能会出现收敛停滞（Failure Mode F3），需要额外的状态聚合机制来破解。
*   **超参数敏感性**：扫描速度 $\omega$ 和窗口 $\epsilon$ 的选择对性能有一定影响，虽然有理论指导，但在极端多变的延迟环境下仍需动态调优。

（完）
