---
title: Provable Coordination for LLM Agents via Message Sequence Charts
title_zh: 基于消息序列图的大语言模型智能体可证明协调
authors: "Benedikt Bollig, Matthias Függer, Thomas Nowak"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17612v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 通过消息序列图实现大模型智能体的可证明协作
tldr: 针对大语言模型多智能体系统难以推理且易出现死锁等协调错误的问题，本文提出了一种基于消息序列图（MSC）的领域特定语言。该方法将消息传递结构与不可预测的LLM动作分离，通过语法导向投影生成无死锁的局部程序，并支持动态工作流生成，为多智能体协作提供了可证明的结构保障。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17612v1/fig-001.webp\", \"caption\": \"Table 5. Projection rule for P = while c@B do Pbody exit Pexit .\", \"page\": 15, \"index\": 1, \"width\": 551, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2604.17612v1/fig-002.webp\", \"caption\": \"Table 1. Inductive MSC semantics of global workflows.\", \"page\": 10, \"index\": 2, \"width\": 599, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.17612v1/fig-003.webp\", \"caption\": \"Table 4. Projection rule for P = if c@B then P⊤ else P⊥.\", \"page\": 14, \"index\": 3, \"width\": 715, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.17612v1/fig-004.webp\", \"caption\": \"Table 2. Local trace semantics for ⟨⟨S⟩⟩.\", \"page\": 12, \"index\": 4, \"width\": 699, \"height\": 495}, {\"url\": \"assets/figures/arxiv/2604.17612v1/fig-005.webp\", \"caption\": \"Fig. 1. MSC for reviewed execution. Filled dots are local actions (opaque computations), arrows are messages labelled by the value sent. Planner owns the conditional. The true branch involves Reviewer; in the false branch, Planner records that review was skipped. After the conditional, Planner forwards the plan to Executor, whose result is finalized by Orchestrator.\", \"page\": 5, \"index\": 5, \"width\": 525, \"height\": 413}]"
motivation: 解决大语言模型多智能体系统中难以通过测试检测的死锁和消息类型不匹配等协调错误。
method: 引入基于消息序列图的领域特定语言，将全局协调规范投影为无死锁的局部代理程序。
result: 成功实现了诊断共识协议，并展示了如何独立于LLM的不确定性来确立协调属性。
conclusion: 该框架（ZipperGen）为LLM智能体提供了可证明的协调保障，并支持动态生成的安全工作流。
---

## 摘要
基于大语言模型（LLM）构建的多智能体系统难以进行推理。死锁或消息类型不匹配等协调错误通常很难通过测试检测到。我们引入了一种基于消息序列图（MSCs）的领域特定语言，用于规范智能体间的协调。该语言将消息传递结构与 LLM 动作分离，后者的输出仍然具有不可预测性。我们定义了该语言的语法和语义，并提出了一种语法导向的投影方法，能够从全局协调规范中生成无死锁的本地智能体程序。我们通过一个诊断共识协议展示了该方法，并说明了如何独立于 LLM 的非确定性来建立协调属性。我们还描述了一个运行时规划扩展，其中 LLM 动态生成协调工作流，且同样的结构性保证依然适用。我们框架的开源 Python 实现已作为 ZipperGen 发布。

## Abstract
Multi-agent systems built on large language models (LLMs) are difficult to reason about. Coordination errors such as deadlocks or type-mismatched messages are often hard to detect through testing. We introduce a domain-specific language for specifying agent coordination based on message sequence charts (MSCs). The language separates message-passing structure from LLM actions, whose outputs remain unpredictable. We define the syntax and semantics of the language and present a syntax-directed projection that generates deadlock-free local agent programs from global coordination specifications. We illustrate the approach with a diagnosis consensus protocol and show how coordination properties can be established independently of LLM nondeterminism. We also describe a runtime planning extension in which an LLM dynamically generates a coordination workflow for which the same structural guarantees apply. An open-source Python implementation of our framework is available as ZipperGen.

---

## 论文详细总结（自动生成）

这是一份关于论文《Provable Coordination for LLM Agents via Message Sequence Charts》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）多智能体系统的普及，开发者面临一个严峻挑战：**LLM 输出的随机性与分布式系统的复杂性交织，导致系统极难调试和验证**。现有的框架（如 LangGraph, AutoGen）虽能构建复杂系统，但无法在理论上保证系统不会陷入死锁（Deadlock）或出现消息类型不匹配。
论文的核心动机是：**如何为不可预测的 LLM 智能体提供确定性的协调保障？** 作者试图通过形式化方法，将“不确定的 LLM 逻辑”与“可证明的通信结构”分离，确保无论 LLM 输出什么，系统在结构上始终是安全的。

### 2. 方法论：核心思想与关键技术
论文提出了一种基于**消息序列图（Message Sequence Charts, MSCs）**的领域特定语言（DSL）及配套框架 **ZipperGen**。
*   **核心思想**：采用“全局规范到局部投影”的思路。开发者编写一个全局工作流（Global Workflow），描述所有智能体的交互逻辑，然后通过算法自动将其“投影”为每个智能体各自运行的局部程序。
*   **关键技术细节**：
    *   **语法分离**：将 LLM 调用视为不透明的“动作块”（Action Blocks），而将控制流（if/while）和消息传递（msg）作为可形式化推理的骨架。
    *   **拥有者控制流（Owned Control Flow）**：在全局规范中明确指定谁是决策者（如 `if c@Planner`），投影时决策者会自动向其他参与者广播控制信号，确保所有智能体步调一致。
    *   **语法导向投影（Syntax-directed Projection）**：通过结构递归直接生成局部代码，无需构建复杂的自动机，并从数学上证明了生成的分布式程序是**无死锁（Deadlock-free）**的。
    *   **运行时规划（Runtime Planning）**：支持 LLM 动态生成 DSL 代码。只要生成的全局代码符合语法，投影后的执行依然享有无死锁保证。

### 3. 实验设计与 Benchmark
论文主要通过**典型场景模拟**和**协议实现**来验证框架的有效性，而非传统意义上的大规模数据集对比：
*   **场景一：评审执行工作流（Review-before-action）**：展示了 Planner、Reviewer、Executor 和 Orchestrator 之间的复杂异步协作。
*   **场景二：医疗诊断共识协议（Medical Diagnosis Consensus）**：两个 LLM 独立分析病例，通过多轮迭代交换意见直至达成共识。
*   **对比对象**：文中对比了现有的多智能体框架（LangGraph, AutoGen, CrewAI），指出这些框架依赖运行时监控和测试，而 ZipperGen 提供静态的形式化保证。
*   **Benchmark**：主要关注协议的正确性、终止性（在给定轮次内）以及在异步环境下的执行一致性。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因**：该研究侧重于**编程语言理论（PL）和形式化验证**，而非训练新的模型。其核心贡献在于协调层逻辑。实验部分使用了现有的 LLM API（如 OpenAI 或 Mock 后端）进行逻辑验证。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了 3-4 个核心工作流示例，包括静态定义的复杂工作流和由 LLM 动态生成的规划工作流。
*   **充分性评价**：从**形式化验证**的角度看，实验是充分的，因为作者提供了完备的数学证明（见附录 20 页证明），证明了算法对任何符合语法的程序都有效。从**经验性能**角度看，实验属于概念验证（PoC）性质，展示了框架在处理并发、循环和分支时的鲁棒性。

### 6. 主要结论与发现
*   **可证明性**：通过将 MSC 理论引入 LLM 领域，可以构建出在数学上保证无死锁的多智能体系统。
*   **解耦价值**：将协调逻辑从 LLM 提示词中抽离出来，不仅提高了系统的可靠性，还降低了 LLM 规划任务的难度（LLM 只需生成简单的全局 DSL）。
*   **异步优势**：投影生成的局部程序支持 FIFO 异步通信，允许智能体在不违反因果关系的前提下并行工作，提升了系统效率。

### 7. 优点（亮点）
*   **理论严谨**：将经典的分布式系统理论（MSC, MPST）成功应用于前沿的 AI 智能体领域。
*   **由构造而正确（Correct-by-construction）**：不需要事后检测死锁，而是通过投影算法确保生成的代码天生免疫死锁。
*   **易于集成**：ZipperGen 作为一个 Python 库，允许开发者用熟悉的语法编写具备形式化保障的代码。
*   **动态安全**：即使是 LLM 临时起意生成的复杂计划，只要通过了 DSL 语法检查，就能安全执行。

### 8. 不足与局限
*   **LLM 动作的黑盒性**：虽然协调结构是安全的，但如果 LLM 动作本身陷入死循环或拒绝回答（Non-returning），系统仍会停滞。论文提到未来需引入超时（Timeout）机制。
*   **并发灵活性受限**：目前采用全序接收，虽然保证了简单性，但在某些极高并发场景下，可能限制了消息处理的灵活性（需引入“核心区域/Coregions”概念）。
*   **应用门槛**：开发者需要学习一套特定的 DSL 编写规范（如明确指定决策者），这与完全自由的自然语言交互相比有一定约束。

（完）
