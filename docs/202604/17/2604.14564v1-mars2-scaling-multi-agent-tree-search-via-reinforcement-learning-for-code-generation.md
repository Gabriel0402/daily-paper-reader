---
title: "MARS$^2$: Scaling Multi-Agent Tree Search via Reinforcement Learning for Code Generation"
title_zh: "MARS$^2$: 通过强化学习实现多智能体树搜索的扩展，用于代码生成"
authors: "Pengfei Li, Shijie Wang, Fangyuan Li, Yikun Fu, Kaifeng Liu, Kaiyan Zhang, Dazhi Zhang, Yuqiang Li, Biqing Qi, Bowen Zhou"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14564v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 具有多个独立优化智能体的多智能体强化学习框架
tldr: 本文提出MARS²框架，通过在共享树状搜索环境中引入多智能体强化学习，实现智能体之间的协作探索与候选方案精炼，并通过树一致的奖励塑形改进信用分配机制，从而在代码生成任务中显著提升强化学习效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14564v1/fig-001.webp\", \"caption\": \"Table 1: Overall performance across models and systems under different training methods. Single-model performance is measured by Pass@1, while system-level performance is evaluated using Pass@1 (MCTS) and Pass@N under a fixed inference budget of 60. Results compare Base, GRPO, RS2, and MARS2 across both individual models and multi-model systems. Notation: “Q” and “A” abbreviate Qwen3 and AReaL respectively; “+” (e.g., Q+A) denotes a model combination used at training time, while “&” (e.g., Q&A) denotes a multi-model combination used at inference time.\", \"page\": 6, \"index\": 1, \"width\": 887, \"height\": 597}, {\"url\": \"assets/figures/arxiv/2604.14564v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of the MARS2 framework. Multiple agents collaboratively expand a shared search tree via Thompson-sampling-based agent–node selection, with node-level rewards refined by tree-consistent reward shaping over parent and sibling signals. Each agent is then independently optimized with a tree-level group-relative advantage over the shared tree.\", \"page\": 2, \"index\": 2, \"width\": 950, \"height\": 544}, {\"url\": \"assets/figures/arxiv/2604.14564v1/fig-003.webp\", \"caption\": \"Figure 3: Ablation on introducing a weaker agent in 14B-scale ensembles. We augment the 14B two-agent setting (Qwen3-14B & AReaL-14B) with a weaker model, DeepCoder-14B, to test robustness under imbalanced agent strength.\", \"page\": 8, \"index\": 3, \"width\": 931, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2604.14564v1/fig-004.webp\", \"caption\": \"Table 9: Training configurations and hyperparameters for MARS2.\", \"page\": 16, \"index\": 4, \"width\": 475, \"height\": 850}]"
motivation: 传统强化学习方法在代码生成任务中因探索多样性受限而性能受阻，多智能体与结构化搜索的结合被认为可以突破这一瓶颈。
method: 提出MARS²框架，让多个独立优化的智能体在共享的树状搜索环境中协作探索，并引入基于路径的群体优势函数进行奖励分配。
result: 在多个代码生成基准上，MARS²在不同模型组合和训练环境下均取得了稳定的性能提升。
conclusion: MARS²有效结合多智能体协作与树搜索，显著提升了代码生成任务中的强化学习性能。
---

## 摘要
强化学习（Reinforcement Learning, RL）范式在代码生成等推理密集型任务中展现出强大的性能。然而，受限的轨迹多样性常导致收益递减，从而限制了性能的上限。引入结构化探索的搜索增强型强化学习可以缓解这一问题，但仍受单智能体策略先验的限制。与此同时，利用多个交互式策略能够获得更为多样化的探索信号，但现有方法通常与结构化搜索相脱节。我们提出了 \textbf{MARS$^2$}（Multi-Agent Reinforced Tree-Search Scaling），这是一个统一的强化学习框架，其中多个独立优化的智能体在共享的树状搜索环境中协作。MARS$^2$ 将搜索树建模为一个可学习的多智能体交互环境，使得异质智能体能够在共享的搜索拓扑中协作生成并优化候选解。为了支持高效学习，我们引入了一种基于树一致奖励塑形的路径级群体优势（path-level group advantage）公式，以促进在复杂搜索轨迹中的有效信用分配。在代码生成基准测试上的实验结果表明，MARS$^2$ 在多种模型组合和训练设置下均持续提升性能，展示了多智能体协作与树搜索结合在增强强化学习中的有效性。我们的代码已在 https://github.com/TsinghuaC3I/MARTI 上公开。

## Abstract
Reinforcement learning (RL) paradigms have demonstrated strong performance on reasoning-intensive tasks such as code generation. However, limited trajectory diversity often leads to diminishing returns, which constrains the achievable performance ceiling. Search-enhanced RL alleviates this issue by introducing structured exploration, which remains constrained by the single-agent policy priors. Meanwhile, leveraging multiple interacting policies can acquire more diverse exploratory signals, but existing approaches are typically decoupled from structured search. We propose \textbf{MARS$^2$} (Multi-Agent Reinforced Tree-Search Scaling), a unified RL framework in which multiple independently-optimized agents collaborate within a shared tree-structured search environment. MARS$^2$ models the search tree as a learnable multi-agent interaction environment, enabling heterogeneous agents to collaboratively generate and refine candidate solutions within a shared search topology. To support effective learning, we introduce a path-level group advantage formulation based on tree-consistent reward shaping, which facilitates effective credit assignment across complex search trajectories. Experiments on code generation benchmarks show that MARS$^2$ consistently improves performance across diverse model combinations and training settings, demonstrating the effectiveness of coupling multi-agent collaboration with tree search for enhancing reinforcement learning. Our code is publicly available at https://github.com/TsinghuaC3I/MARTI.

---

## 论文详细总结（自动生成）

# MARS²：通过强化学习扩展多智能体树搜索以提升代码生成性能 — 中文综合总结

---

## 一、研究背景与核心问题

- **研究动机**：  
  强化学习（RL）在代码生成和数学推理等复杂推理任务上取得显著成效，但其探索能力受到单一策略（policy）分布的限制，导致：
  - 轨迹多样性不足；
  - 搜索空间狭窄，易陷入局部最优；
  - 性能提升呈递减趋势。  

- **现有方法局限性**：
  1. **搜索增强型RL**（如 TreeRL、TreePO）通过引入树状搜索扩展探索空间，但仍依赖单一智能体的策略先验；
  2. **多智能体RL**（MARL）能提升探索多样性，但其交互多为“对话、投票、辩论”等弱结构形式，缺乏与树搜索的结构化结合。

- **核心问题**：  
  如何在统一的框架中结合多智能体协同与结构化树搜索，使探索具备深度、多样性和可学习性，从而突破单一策略的探索边界？

---

## 二、方法论：MARS² 框架

### 1. 核心思想

> **MARS²（Multi-Agent Reinforced Tree-Search Scaling）**  
> 将多智能体强化学习嵌入到树状搜索过程中，让多个策略在**共享的可学习搜索树**上进行协作、竞争与互相精炼。

关键创新点：
- 搜索树被建模为一个**多智能体交互环境**；
- 多个独立优化的智能体在共享拓扑中**协作探索与候选方案生成**；
- 引入**路径级群体优势函数**（path-level group advantage）和**树一致奖励塑形**（tree-consistent reward shaping）来完善信用分配。

---

### 2. 算法概要与核心机制

**(1) 多智能体树搜索流程**  
- 每个节点表示一个候选解；多智能体共同扩展搜索树。  
- 通过 **Thompson Sampling** 进行两级选择：  
  - 选取智能体；
  - 选取该智能体负责的扩展节点。

节点类型分两类：
- **生成节点 (generation node)**：产生新的候选解；
- **精化节点 (refinement node)**：对父节点候选进行修订。

此设计形成“水平探索 + 垂直精化”的动态平衡。

**(2) 奖励与优势建模**

- **树级群体相对优势 (Tree-Level Group Advantage)**：  
  拓展 GRPO（Group Relative Policy Optimization）的思想，将原本在平行采样上的组优势扩展到树节点层级。  
  \[
  \hat{A}_{k,j} = \frac{r_{k,j} - \text{mean}(r)}{\text{std}(r)}
  \]

- **树一致奖励塑形 (Tree-Consistent Reward Shaping)**：  
  对每个非根节点 v：
  - 定义基线：
    \[
    b(v) = (1-\lambda)r_{p(v)} + \lambda \mu_{C(p(v))\setminus v}
    \]
  - 奖励调整：
    \[
    \hat{r}_v = r_v + \gamma(r_v - b(v))
    \]  
  通过综合**父节点改进信号（vertical）**和**兄弟节点竞争信号（horizontal）**，实现稳定的层级信用分配。

**(3) 优化目标**

- 以 GRPO 为基础（方程 6–7），将单轨道优化扩展到多树节点维度；
- 每个智能体独立优化但共享全局搜索动态；
- 保持 KL 正则与 reward normalization 保证训练稳定。

---

## 三、实验设计

### 1. 数据集与基准

- **训练集**：DeepCoder (Luo et al., 2025) 精选 7,992 个代码生成提示；
- **评测基准**：LiveCodeBench v6 (2025/01–05)，确保和训练数据无交叉污染；
- **外部验证任务**：MATH 数据集用于通用化测试。

### 2. 对比方法

| 类型 | 方法 | 说明 |
|------|------|------|
| 单智能体RL | GRPO | 基准加强型 RL |
| 搜索增强RL | RS² | 单策略 + 树搜索 |
| 多智能体树搜索 | **MARS²（本文）** | 多策略 + 共享树结构 |

所有方法在**同等训练配置**下比较，排除优化技巧带来的偏差。

### 3. 模型规模与系统设置

- 模型：Qwen3（8B/14B）、AReaL（8B/14B）、DeepCoder-14B；
- 组合设置：
  - Qwen3-8B + AReaL-8B；
  - Qwen3-14B + AReaL-14B；
  - Qwen3-14B + AReaL-14B + DeepCoder-14B（不平衡多智能体）。

### 4. 评估指标

- **单模型能力**：Pass@1  
- **系统级推理性能**：Pass@1 (MCTS)、Pass@N  
- **多样性指标**：AEC, DA@K, EA, NAUADC, G‑Vendi（用于探索覆盖与策略差异分析）。

---

## 四、资源与算力

- 文中提供的训练配置（Table 9）：
  - **每模型**使用 8 张 GPU；
  - 并行引擎：vLLM × 8；
  - Precision：bfloat16；
  - ZeRO Stage 3；
  - 学习率 1×10⁻⁶；
  - 每步最大序列长度约 40k；
  - MCTS 节点预算：8 或 16；
  - 训练批次：256；
  - 具体 GPU 型号、训练总时长未明示。  
  ✅ **说明**：无法估算确切 FLOPs 或总训练时长，仅知计算规模与主流14B模型相当。

---

## 五、实验数量与充分性

- **主实验**：5 个基础模型在单/多代理、多树结构下比较；
- **综合评估**：代码生成基准（LiveCodeBench） + 数学推理任务（MATH）；
- **消融实验**：
  - 有/无奖励塑形；
  - λ 参数敏感性；
  - 不同搜索预算（4、8、16节点）；
  - 引入弱智能体对性能影响；
  - 多样性统计分析；
  - “latest‑wins”节点选择策略验证；
  - 训练稳定性与收敛曲线分析。
- **实验充分性**：  
  涵盖不同模型规模、异构组合及指标维度，实验设置公正且全面。

---

## 六、主要结论与发现

1. **整体性能提升**：
   - 在所有模型与系统配置下，MARS² 均优于 Base、GRPO 与 RS²；
   - 平均提升幅度：
     - 单模型 Pass@1 ↑4–8%；  
     - 系统级 Pass@1(MCTS) ↑4–6%。

2. **结构化多智能体优势显著**：
   - 树状环境促进异构策略互补；
   - 通过协作搜索显著扩大可探索前沿。

3. **稳定训练与更高多样性**：
   - 奖励塑形提升训练稳定性；
   - 多样性指标（AEC、DA@K）均为所有方法中最高。

4. **泛化能力确认**：
   - 在数学推理任务 MATH 上同样表现提升，说明方法可跨任务迁移。

---

## 七、方法与实验亮点

- **方法创新性**：
  - 首次将多智能体协作与树搜索融合为统一的RL机制；
  - 提出树级群体优势与树一致奖励塑形，解决多层次信用分配难题；
  - Thompson Sampling 用于高效的代理–节点选择。

- **实验设计严谨**：
  - 控制同等样本量与优化步；
  - 同步采用稳定化技术确保公平；
  - 使用未见数据的独立基准验证泛化。

- **实证亮点**：
  - 在弱智能体参与下仍保持系统性能优势；
  - 模型多样性分析表明改进源于探索范围扩大而非“重复高奖轨迹”。

---

## 八、不足与局限

- **计算效率**：  
  树搜索带来顺序性扩展，降低并行性，使训练时间增加。
- **资源消耗**：  
  虽未提高总算力预算，但交互频繁导致训练吞吐下降。
- **评测覆盖**：  
  仅在代码生成与少量数学推理任务上验证，尚未测试更开放任务（如复杂对话或符号推理）。
- **智能体规模与交互形式**：  
  实验最多包含3个智能体，未探索更大规模或竞争性多智能体设置。
- **可重现性代价高**：  
  需要多模型协同与树搜索同步机制，部署复杂。

---

**总体评价**：  
MARS² 在理论设计与实证验证上均具有创新性与系统性，是强化学习在推理任务中从“单策略探索”向“多智能体结构化探索”迈出的重要一步。

---

（完）
