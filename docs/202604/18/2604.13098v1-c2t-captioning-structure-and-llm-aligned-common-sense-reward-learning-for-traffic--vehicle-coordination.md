---
title: "C$^2$T: Captioning-Structure and LLM-Aligned Common-Sense Reward Learning for Traffic--Vehicle Coordination"
title_zh: C$^2$T：面向车路协同的描述结构化与大语言模型对齐的常识性奖励学习
authors: "Yuyang Chen, Kaiyan Zhao, Yiming Wang, Ming Yang, Bin Rao, Zhenning Li"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.13098v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 结合LLM蒸馏内在奖励的多智能体强化学习（MARL）
tldr: 针对城市交通控制中多智能体强化学习（MARL）奖励函数设计过于单一且缺乏长远目标的问题，本文提出C2T框架。该框架通过将大语言模型（LLM）中的“常识”知识蒸馏为内在奖励函数，引导交通信号灯与联网自动驾驶车辆的协同。在CityFlow基准测试中，C2T在效率、安全和节能方面均优于现有方法，并能通过提示词灵活调整策略偏好。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13098v1/fig-001.webp\", \"caption\": \"Figure 1. C2T pipeline. Stage 1 converts raw observations into schema-constrained captions and samples high-contrast/safety-contrast pairs. Stage 2 queries an LLM to label pairwise preferences and trains a scalar scorer rϕ with a Bradley–Terry loss (with frequency reweighting and score centering). Stage 3 freezes rϕ and injects it as an intrinsic component for TLC steps under a safety mask, mixing with external rewards; all streams are normalized before GAE+PPO. In our CityFlow instantiation, only TLCs are learning agents and vehicles remain part of the environment dynamics.\", \"page\": 5, \"index\": 1, \"width\": 1036, \"height\": 467}]"
motivation: 现有的交通控制强化学习方法依赖于手工设计的短视奖励，难以捕捉安全、流量稳定和舒适度等高层人类目标。
method: 提出C2T框架，利用LLM的常识推理能力生成对交通动态的评价，并将其蒸馏为内在奖励函数来指导多智能体协同策略。
result: 在多交叉口基准测试中，该方法在交通效率、安全性和能耗指标上显著超越了强基线模型。
conclusion: C2T证明了将LLM的常识知识引入交通控制奖励学习的有效性，并提供了通过调整提示词实现不同策略目标的灵活性。
---

## 摘要
尖端的城市交通控制越来越多地采用多智能体强化学习（MARL）来协调交通信号灯控制器（TLC）和联网自动驾驶车辆（CAV）。然而，这些系统的性能从根本上受限于其手工设计的、短视的奖励函数（如交叉口压力），这些奖励无法捕捉到安全性、流量稳定性和舒适性等高层级、以人为本的目标。为了克服这一局限性，我们提出了 C2T，这是一个从交通-车辆动力学中学习常识性协调模型的新型框架。C2T 将大语言模型（LLM）中的“常识”知识蒸馏到学习到的内在奖励函数中。随后，该新奖励被用于指导基于 CityFlow 的多交叉口基准测试中协作式多交叉口 TLC MARL 系统的协调策略。我们的框架在交通效率、安全性和能源相关指标方面显著优于强大的 MARL 基准模型。我们进一步强调了 C2T 在原理上的灵活性，即通过修改 LLM 提示词，可以实现截然不同的“效率导向”或“安全导向”策略。

## Abstract
State-of-the-art (SOTA) urban traffic control increasingly employs Multi-Agent Reinforcement Learning (MARL) to coordinate Traffic Light Controllers (TLCs) and Connected Autonomous Vehicles (CAVs). However, the performance of these systems is fundamentally capped by their hand-crafted, myopic rewards (e.g., intersection pressure), which fail to capture high-level, human-centric goals like safety, flow stability, and comfort. To overcome this limitation, we introduce C2T, a novel framework that learns a common-sense coordination model from traffic-vehicle dynamics. C2T distills "common-sense" knowledge from a Large Language Model (LLM) into a learned intrinsic reward function. This new reward is then used to guide the coordination policy of a cooperative multi-intersection TLC MARL system on CityFlow-based multi-intersection benchmarks. Our framework significantly outperforms strong MARL baselines in traffic efficiency, safety, and an energy-related proxy. We further highlight C2T's flexibility in principle, allowing distinct "efficiency-focused" versus "safety-focused" policies by modifying the LLM prompt.

---

## 论文详细总结（自动生成）

这是一份关于论文《C$^2$T: Captioning-Structure and LLM-Aligned Common-Sense Reward Learning for Traffic–Vehicle Coordination》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的城市交通控制强化学习（RL）系统主要依赖手工设计的奖励函数（如最小化排队长度、交叉口压力等）。这些指标虽然能提升局部效率，但过于“短视”，忽略了安全性、流量稳定性、驾驶舒适度等高层级的人类常识目标，导致交通流虽然在数据上高效，但在实际部署中表现得脆弱且不可预测（如频繁急刹、相位切换振荡）。
*   **研究背景**：随着联网自动驾驶车辆（CAV）和智能交通信号灯（TLC）的发展，如何实现两者的深度协同并使其符合人类驾驶常识，成为提升城市交通质量的关键。

### 2. 论文提出的方法论：C$^2$T 框架
C$^2$T 框架的核心思想是将大语言模型（LLM）中的“常识”知识蒸馏为一个可学习的内在奖励函数，并将其离线集成到多智能体强化学习（MARL）中。
*   **阶段 1：结构化描述与偏好对采样**：
    *   定义了一个确定性的、带单位的描述符（Captioner），将模拟器的原始观测状态转化为结构化文本（包含信号相位、排队压力、延迟、TTC 安全指标、急刹车计数等）。
    *   根据交通拥堵对比、安全性对比和时间偏移采样观测对，构建偏好数据集。
*   **阶段 2：学习常识性内在奖励**：
    *   利用 LLM（作为评判者）对上述描述对进行两两比较，判断哪种状态更符合“好的交通”。
    *   使用 Bradley-Terry 模型训练一个轻量级的评分器 $r_\phi$，将 LLM 的偏好蒸馏为标量奖励值。
*   **阶段 3：奖励塑形与 RL 集成**：
    *   **非对称混合**：内在奖励仅混合到 TLC（信号灯）的奖励流中，避免车辆端产生额外的非平稳性。
    *   **安全掩码（Safety Mask）**：当检测到低 TTC（碰撞风险）或频繁急刹时，自动抑制内在奖励，强制回归安全基准。
    *   **优化策略**：采用多流奖励归一化、软裁剪（Soft Clipping）和动态权重调度，配合 PPO 算法进行训练。

### 3. 实验设计
*   **数据集/场景**：基于 CityFlow 模拟器，使用了三个真实城市网络：济南（12 交叉口）、杭州（16 交叉口）、纽约（196 交叉口）。此外还设计了“极端流量激增”和“24 小时昼夜循环”两个压力测试场景。
*   **Benchmark 与对比方法**：
    *   **传统方法**：Random, FixedTime, MaxPressure。
    *   **强化学习基线**：MPLight, CoLight, Efficient-CoLight, Advanced-CoLight 等。
    *   **LLM 智能体**：LLMLight 报告的 GPT、Qwen、Llama 系列变体。
*   **评估指标**：平均旅行时间（ATT）、平均排队长度（AQL）、平均等待时间（AWT）、安全性指标（TTC 分位数、每公里急刹次数）以及能耗代理指标。

### 4. 资源与算力
*   **论文提及情况**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **推断**：由于采用了 PPO 算法且奖励模型是离线训练的（推理时不调用 LLM），其在线训练阶段的算力需求应与标准 MARL 相当，但离线标注阶段需要调用 LLM API 或运行本地大模型进行偏好标注。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型（12 节点）到超大型（196 节点）的城市网络，并进行了跨城市迁移实验（在济南训练，在杭州零样本测试）。
*   **消融实验**：非常充分。分别验证了内在奖励、安全掩码、奖励归一化、调度计划以及描述符结构（纯数字 vs 纯文本 vs 混合）对性能的影响。
*   **客观性**：所有实验均基于 5 个随机种子取平均值，并遵循了 LLMLight 的标准协议，确保了与基线方法对比的公平性。

### 6. 论文的主要结论与发现
*   **性能提升**：C$^2$T 在所有城市网络中均显著降低了平均旅行时间（ATT），同时提升了安全性（TTC 显著增加，急刹车减少）。
*   **灵活性**：通过修改 LLM 的 Prompt，可以轻松切换“效率优先”或“安全优先”的控制策略，无需重新设计复杂的奖励公式。
*   **可扩展性**：在小规模网络上学习到的常识奖励模型可以有效泛化到大规模网络（如纽约），表现优于许多专门为大规模设计的 RL 基线。

### 7. 优点：亮点与创新
*   **解耦设计**：将 LLM 的推理能力与 RL 的控制能力解耦。在线控制阶段无需调用 LLM，解决了 LLM 在多智能体环境中延迟高、成本贵、不可靠的问题。
*   **结构化描述**：提出了一种确定性的、带单位的 Schema，克服了以往方法中自由文本描述模糊、不稳定的缺点，使 LLM 的判断更具一致性。
*   **安全保障**：引入安全掩码机制，确保了在追求“常识性优化”时不会牺牲底线的交通安全。

### 8. 不足与局限
*   **离线依赖**：虽然在线不调用 LLM，但离线阶段仍需要大量的 LLM 标注，这在初始阶段存在一定的计算或 API 成本。
*   **车辆端简化**：在当前的 CityFlow 实例化中，车辆主要遵循内置的微观动力学，虽然讨论了 CAV 扩展，但实验核心仍集中在 TLC（信号灯）的控制上。
*   **环境偏差**：实验完全基于仿真环境（CityFlow），真实世界中传感器噪声、通信延迟对结构化描述（Captioning）准确性的影响尚未深入探讨。

（完）
