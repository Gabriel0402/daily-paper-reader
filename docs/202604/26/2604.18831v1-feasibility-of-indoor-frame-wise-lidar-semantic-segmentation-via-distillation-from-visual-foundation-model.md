---
title: Feasibility of Indoor Frame-Wise Lidar Semantic Segmentation via Distillation from Visual Foundation Model
title_zh: 基于视觉基础模型蒸馏的室内逐帧激光雷达语义分割可行性研究
authors: "Haiyang Wu, Juan J. Gonzales Torres, George Vosselman, Ville Lehtola"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18831v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 通过视觉基础模型蒸馏进行激光雷达语义分割
tldr: 针对室内激光雷达（Lidar）语义分割标注成本高昂的问题，本文探讨了利用视觉基础模型（VFM）进行跨模态知识蒸馏的可行性。研究通过将室内Lidar扫描与经过VFM处理的相机图像耦合，实现了无需人工标注的逐帧语义分割训练。实验在室内SLAM数据集上验证了该方法，证明了从2D视觉模型向3D Lidar模型迁移知识在室内环境下的有效性与潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18831v1/fig-001.webp\", \"caption\": \"Figure 1. Workflow of ScaLR adaptation to indoor datasets.\", \"page\": 3, \"index\": 1, \"width\": 490, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.18831v1/fig-002.webp\", \"caption\": \"Figure 5. Qualitative comparison of segmentation outputs from DINOv2 (with Mask2Former head) and OneFormer on the NTU-VIRAL dataset.\", \"page\": 6, \"index\": 2, \"width\": 490, \"height\": 277}]"
motivation: 室内Lidar逐帧语义分割的人工标注成本极高，限制了其在3D场景理解和制图应用中的发展。
method: 提出一种2D到3D的蒸馏框架，利用视觉基础模型对相机图像生成的分割结果作为伪标签来训练Lidar分割模型。
result: "蒸馏模型在伪标签评估下达到56% mIoU，在真实标注评估下达到约36% mIoU。"
conclusion: 实验证明了在无需人工标注的情况下，通过跨模态蒸馏实现室内Lidar语义分割是切实可行的。
---

## 摘要
室内激光雷达扫描的逐帧语义分割是实现更高级别3D场景理解和地图构建应用的基础步骤。然而，获取用于训练深度学习模型的逐帧真值（ground truth）既昂贵又耗时。对于图像而言，这一挑战在很大程度上已通过能够分割图像帧的视觉基础模型（VFMs）得到解决。同样的VFMs可以通过2D到3D的蒸馏流水线来训练激光雷达扫描帧分割模型。这种蒸馏方法的成功已在自动驾驶场景中得到证实，但在室内场景中尚未得到验证。在本文中，我们研究了在室内场景中复制这一成功的可行性，采用逐帧蒸馏的方式，将每次激光雷达扫描与经VFM处理的相机图像耦合。评估工作使用室内SLAM数据集完成，其中伪标签用于下游评估。此外，由于目前尚无其他带有语义信息的室内激光雷达逐帧数据集，我们提供了一个小型的人工标注激光雷达数据集用于验证。结果表明，蒸馏模型在伪标签评估下达到了高达56%的mIoU，在真实标签评估下达到了约36%的mIoU，证明了在无需人工标注的情况下，跨模态蒸馏用于室内激光雷达语义分割的可行性。

## Abstract
Frame-wise semantic segmentation of indoor lidar scans is a fundamental step toward higher-level 3D scene understanding and mapping applications. However, acquiring frame-wise ground truth for training deep learning models is costly and time-consuming. This challenge is largely addressed, for imagery, by Visual Foundation Models (VFMs) which segment image frames. The same VFMs may be used to train a lidar scan frame segmentation model via a 2D-to-3D distillation pipeline. The success of such distillation has been shown for autonomous driving scenes, but not yet for indoor scenes. Here, we study the feasibility of repeating this success for indoor scenes, in a frame-wise distillation manner by coupling each lidar scan with a VFM-processed camera image. The evaluation is done using indoor SLAM datasets, where pseudo-labels are used for downstream evaluation. Also, a small manually annotated lidar dataset is provided for validation, as there are no other lidar frame-wise indoor datasets with semantics. Results show that the distilled model achieves up to 56% mIoU under pseudo-label evaluation and around 36% mIoU with real-label, demonstrating the feasibility of cross-modal distillation for indoor lidar semantic segmentation without manual annotations.

---

## 论文详细总结（自动生成）

这篇论文探讨了在室内环境下，利用视觉基础模型（VFM）通过跨模态蒸馏实现激光雷达（Lidar）逐帧语义分割的可行性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：室内 Lidar 逐帧语义分割缺乏大规模标注数据集。现有的室内数据集（如 S3DIS）多为完整场景重建后的点云，而非原始的逐帧扫描，且人工标注成本极高。
*   **研究动机**：在室外自动驾驶领域，利用 2D 视觉模型（如 DINOv2）向 3D 模型蒸馏知识已取得成功。本文旨在验证这种“2D 到 3D 蒸馏”方案在室内复杂环境、不同传感器配置下的可行性，实现无需人工标注的室内 Lidar 语义分割。

### 2. 方法论
*   **核心思想**：采用教师-学生架构（基于 ScaLR 框架）。利用预训练的 2D VFM 作为教师模型提取图像特征，引导 3D 学生模型学习点云的语义表示。
*   **关键技术细节**：
    *   **跨模态蒸馏**：通过内参和外参将 3D 点投影到 2D 图像平面，建立点与像素的对应关系。
    *   **损失函数**：使用余弦相似度损失（Cosine Similarity Loss），最小化 3D 学生模型提取的点特征与 2D 教师模型提取的像素特征之间的距离。
    *   **数据适配**：开发了专门的流水线，处理不同室内 SLAM 数据集（如 NTU-VIRAL、TIERS 等）中异构传感器的同步与投影问题。
    *   **伪标签生成**：利用 OneFormer 等模型对图像进行分割，并将结果投影到点云上生成伪标签，用于下游的线性探测（Linear Probing）和微调（Fine-tuning）。

### 3. 实验设计
*   **数据集**：
    *   公开数据集：**NTU-VIRAL**、**TIERS**、**M2DGR**（涵盖不同 Lidar 型号如 Ouster, Velodyne）。
    *   自建数据集：**ITC 数据集**（使用 Hesai XT-32 手持设备采集，包含 1,720 帧人工标注的真值用于验证）。
*   **Benchmark 与对比方法**：
    *   由于缺乏室内逐帧 Lidar 分割的基准，作者以**伪标签评估**为主。
    *   对比了在 S3DIS 上预训练的 **RandLA-Net**（作为跨域监督学习的参考）。
    *   对比了不同的 2D 教师模型（DINOv2 vs. OneFormer）。

### 4. 资源与算力
*   **硬件环境**：服务器配备了 **2 台 NVIDIA A40 GPU**（每台 46 GB 显存）。
*   **训练时长**：
    *   蒸馏阶段：25 个 epoch。
    *   微调阶段：根据模型深度（WaffleIron 网络的层数）不同，耗时在 **3.4 小时到 8.4 小时**之间。
    *   推理速度：24 层模型可达 **15.4 Hz**，满足实时性要求。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了跨数据集的线性探测实验，验证特征的通用性。
    *   在四个数据集上分别进行了微调实验。
    *   针对 VFM 模型敏感度、模型深度（24/36/48 层）进行了消融实验。
*   **充分性评价**：实验设计较为全面，涵盖了从特征提取到下游任务的完整流程。通过引入人工标注的 ITC 数据集进行“真实标签 vs. 伪标签”对比，增强了结论的客观性。

### 6. 主要结论与发现
*   **可行性证实**：蒸馏模型在伪标签评估下达到约 **56% mIoU**，在真实标签评估下达到约 **36% mIoU**，远超未经领域适配的监督模型 RandLA-Net（仅 10.7%）。
*   **领域依赖性**：室内环境受传感器配置（线数、视场角）影响极大，跨数据集表现会有所下降，但通过混合数据集训练可以提升泛化能力。
*   **模型稳定性**：3D 模型对 2D 教师模型的选择（DINOv2 或 OneFormer）不敏感，证明了蒸馏框架的鲁棒性。

### 7. 优点（亮点）
*   **零标注依赖**：成功证明了在完全没有人工 3D 标注的情况下，仅靠视觉模型即可训练出可用的室内 Lidar 分割模型。
*   **实时性**：选用的 WaffleIron 骨干网络在保证精度的同时，推理速度超过 15Hz，适合集成到语义 SLAM 系统中。
*   **填补空白**：针对室内“逐帧”Lidar 分割这一细分领域提供了初步的基准和数据处理经验。

### 8. 不足与局限
*   **伪标签噪声**：由于投影对齐误差和 2D 分割的不完美，伪标签存在噪声，导致模型在真实标签上的表现与伪标签评估存在约 20% 的 mIoU 差距。
*   **缺乏时间连续性**：目前仅在单帧层面处理，未利用视频流或点云序列的时间上下文信息来平滑分割结果。
*   **类别覆盖有限**：实验主要集中在墙壁、地板、天花板等结构化类别，对室内复杂小物体（如家具细节）的分割精度仍有待提升。

（完）
