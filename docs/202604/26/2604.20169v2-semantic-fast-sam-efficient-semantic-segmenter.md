---
title: "Semantic-Fast-SAM: Efficient Semantic Segmenter"
title_zh: Semantic-Fast-SAM：高效语义分割器
authors: Byunghyun Kim
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20169v2"
tags: ["query:mmseg"]
score: 10.0
evidence: 结合FastSAM与标注流水线的高效语义分割
tldr: 本研究提出Semantic-Fast-SAM (SFS)，旨在解决原始SAM模型推理缓慢且缺乏语义标签的问题。通过将基于CNN的高效FastSAM与语义标注流水线（SSA）结合，SFS在保持高精度的同时实现了实时语义分割。该模型在Cityscapes和ADE20K基准上表现优异，推理速度比SSA快约20倍，并支持开放词汇分割，为机器人等实时应用场景提供了高效的基础分割方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20169v2/fig-001.webp\", \"caption\": \"TABLE III SEMANTIC SEGMENTATION PERFORMANCE (MIOU). BEST WITHIN EACH SUPERVISION REGIME IS BOLDED; HIGHER IS BETTER.\", \"page\": 4, \"index\": 1, \"width\": 529, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-002.webp\", \"caption\": \"TABLE IV ABLATION RESULTS ON OPEN-VOCABULARY FUSION AND MASK COUNT. BEST WITHIN EACH ABLATION BLOCK IS BOLDED.\", \"page\": 4, \"index\": 2, \"width\": 529, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-003.webp\", \"caption\": \"TABLE II GPU MEMORY USAGE FOR DIFFERENT METHODS. BEST WITHIN EACH COMPARABLE BLOCK IS BOLDED; LOWER IS BETTER.\", \"page\": 3, \"index\": 3, \"width\": 529, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.20169v2/fig-004.webp\", \"caption\": \"Fig. 2. Overall architecture of Semantic-Fast-SAM.\", \"page\": 2, \"index\": 4, \"width\": 529, \"height\": 254}]"
motivation: 原始SAM模型基于Transformer架构，计算开销巨大且缺乏语义类别信息，难以满足实时语义分割的需求。
method: 结合FastSAM的快速掩码生成能力与语义标注策略，并利用CLIP模型实现闭集与开放词汇的类别分配。
result: 在Cityscapes和ADE20K上取得了与领先方法相当的精度，且推理速度比传统SSA方法快约20倍。
conclusion: SFS成功实现了高效且具备语义识别能力的“分割一切”框架，显著提升了基础分割模型在实际工程中的可用性。
---

## 摘要
我们提出了 Semantic-Fast-SAM (SFS)，这是一个将 Fast Segment Anything 模型与语义标注流水线相结合的语义分割框架，在不牺牲准确性的情况下实现了实时性能。FastSAM 是 Segment Anything Model (SAM) 的一种基于 CNN 的高效重新实现，其运行速度远快于原始的基于 Transformer 的 SAM。基于 FastSAM 的快速掩码生成能力，我们集成了一种语义分割一切 (SSA) 标注策略，为每个掩码分配具有实际意义的类别。由此产生的 SFS 模型能够生成高质量的语义分割图，而其计算成本和内存占用仅为原始基于 SAM 方法的一小部分。在 Cityscapes 和 ADE20K 基准测试上的实验表明，SFS 的准确率与之前的基于 SAM 的方法相当（Cityscapes 上的 mIoU 约为 70.33，ADE20K 上约为 48.01），同时在闭集设置下的推理速度比 SSA 快约 20 倍。我们还展示了 SFS 通过利用基于 CLIP 的语义头，能够有效处理开放词汇分割，在广泛类别标注方面优于近期的开放词汇模型。这项工作实现了具有“分割一切”能力的实用实时语义分割，扩展了基础分割模型在机器人场景中的适用性。代码实现已在 https://github.com/KBH00/Semantic-Fast-SAM 开源。

## Abstract
We propose Semantic-Fast-SAM (SFS), a semantic segmentation framework that combines the Fast Segment Anything model with a semantic labeling pipeline to achieve real-time performance without sacrificing accuracy. FastSAM is an efficient CNN-based re-implementation of the Segment Anything Model (SAM) that runs much faster than the original transformer-based SAM. Building upon FastSAM's rapid mask generation, we integrate a Semantic-Segment-Anything (SSA) labeling strategy to assign meaningful categories to each mask. The resulting SFS model produces high-quality semantic segmentation maps at a fraction of the computational cost and memory footprint of the original SAM-based approach. Experiments on Cityscapes and ADE20K benchmarks demonstrate that SFS matches the accuracy of prior SAM-based methods (mIoU ~ 70.33 on Cityscapes and 48.01 on ADE20K) while achieving approximately 20x faster inference than SSA in the closed-set setting. We also show that SFS effectively handles open-vocabulary segmentation by leveraging CLIP-based semantic heads, outperforming recent open-vocabulary models on broad class labeling. This work enables practical real-time semantic segmentation with the "segment-anything" capability, broadening the applicability of foundation segmentation models in robotics scenarios. The implementation is available at https://github.com/KBH00/Semantic-Fast-SAM.

---

## 论文详细总结（自动生成）

### Semantic-Fast-SAM: 高效语义分割器论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于解决基础分割模型（如 SAM）在实际应用中的两大瓶颈：**计算开销巨大**和**缺乏语义标签**。
*   **背景**：SAM 虽然具备强大的“分割一切”能力，但其基于 Transformer 的架构导致推理速度慢，且仅生成无类别的掩码。
*   **动机**：现有的语义标注扩展（如 SSA）虽然能为 SAM 掩码添加标签，但由于依赖重型 SAM 模型和多个大型视觉模型，无法满足机器人或自动驾驶等场景的实时性需求。本研究旨在结合 FastSAM 的速度优势与 SSA 的语义标注能力，构建一个既快又准的通用语义分割框架。

#### 2. 方法论：核心思想与技术细节
Semantic-Fast-SAM (SFS) 采用两阶段架构，无需对模型进行微调：
*   **阶段 A：快速掩码生成**
    *   使用 **FastSAM**（基于 YOLOv8-seg 的 CNN 架构）代替原始 SAM。它通过单次前向传播生成全图掩码，速度比 SAM 快约 50 倍。
*   **阶段 B：多分支语义标注**
    *   **闭集分支 (Closed-Set Branch)**：利用预训练的 OneFormer 模型（在 COCO 和 ADE20K 上训练）生成语义图，通过对掩码区域进行“多数投票”来分配已知类别标签。
    *   **开放词汇分支 (Open-Vocabulary Branch)**：针对未知物体，利用 **BLIP** 生成图像区域描述，提取名词短语作为候选标签，再通过 **CLIP** 计算图像嵌入与文本嵌入的余弦相似度进行重排序。
*   **融合与决策**：结合两个分支的结果。若闭集预测置信度高则保留；若开放词汇分支发现置信度更高的非闭集类别，则进行替换。

#### 3. 实验设计
*   **数据集**：主要在 **Cityscapes**（城市景观）和 **ADE20K**（通用场景）验证集上进行评估。
*   **Benchmark（基准）**：
    *   **速度与内存**：对比 SSA (Semantic-SAM)、FastSAM 原型、OneFormer。
    *   **分割精度 (mIoU)**：对比 SSA、OneFormer、Mask2Former、SegFormer。
    *   **开放词汇能力**：对比 CLIPSeg、GroupViT、MaskCLIP。
*   **对比维度**：涵盖了推理延迟（秒/帧）、峰值显存占用（GB）以及平均交并比（mIoU）。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 3090 GPU (24 GB)** 的工作站上完成。
*   **训练时长**：论文明确指出 SFS 是一个**推理时流水线 (Inference-time pipeline)**，直接组合预训练组件，**不需要重新训练或反向传播**。因此，文中未提及训练时长，重点在于推理阶段的资源消耗。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了零样本 (Zero-shot) 性能测试。
    *   进行了**消融实验**，分别验证了“仅闭集”与“全模型”模式的差异，以及掩码保留数量（Top-100, 50, 25）对速度和精度的影响。
*   **充分性评价**：实验设计较为客观，涵盖了从端到端速度到显存占用的全方位对比。通过与多种监督学习模型和开放词汇模型的横向对比，证明了其在“零样本”前提下的竞争优势。

#### 6. 主要结论与发现
*   **速度飞跃**：在闭集设置下，SFS 的推理速度（0.08s）比基于 SAM 的 SSA（1.65s）快约 **20 倍**。
*   **内存优化**：显存占用从 SSA 的约 19GB 降至 **4.5GB**，降低了约 4 倍，使其能在消费级 GPU 上运行。
*   **精度持平**：在 Cityscapes (70.33 mIoU) 和 ADE20K (48.01 mIoU) 上的表现与重型 SSA 几乎持平，证明更换轻量级掩码生成器对精度的损失极小。
*   **开放词汇有效性**：SFS 在处理未知类别时优于早期的开放词汇模型（如 GroupViT），得益于其更精细的掩码粒度。

#### 7. 优点（亮点）
*   **实用性极强**：通过工程化组合现有模型，解决了基础模型“落地难”的速度问题。
*   **即插即用**：无需训练，能够直接利用现有的分割和多模态模型成果。
*   **灵活性**：支持在闭集（极速）和开放词汇（广度）模式间切换，适应不同算力预算。

#### 8. 不足与局限
*   **开放词汇模式瓶颈**：虽然闭集模式极快，但在开启开放词汇分支时，由于 BLIP 和 CLIP 需要对每个掩码进行处理，耗时大幅增加（从 0.08s 升至 10.24s），尚未达到真正的实时。
*   **掩码精度上限**：FastSAM 在处理极小物体时，掩码质量偶尔逊于原始 SAM。
*   **依赖性**：性能高度依赖于预训练的 OneFormer 或 CLIP 模型的质量，存在模型偏差传递的风险。

（完）
