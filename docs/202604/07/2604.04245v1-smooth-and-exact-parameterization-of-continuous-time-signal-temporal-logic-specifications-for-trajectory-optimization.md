---
title: Smooth and Exact Parameterization of Continuous-time Signal Temporal Logic Specifications for Trajectory Optimization
title_zh: 用于轨迹优化的连续时间信号时序逻辑规约的平滑且精确参数化
authors: "Samet Uzun, Behçet Açıkmeşe"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04245v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于轨迹优化的信号时序逻辑规范
tldr: 本研究提出了一种针对非凸轨迹优化的连续时间信号时序逻辑（CT-STL）平滑参数化方法。该方法在密集时间内评估规范，确保了“始终”谓词的连续满足并消除了“最终”谓词的离散保守性。通过解决梯度掩蔽问题并允许使用更粗略的时间网格，该方法在保证安全性和逻辑保真度的同时，显著降低了优化问题的维度。在无人机复杂任务中的实验证明了其数值有效性和语义准确性。
selection_source: fresh_fetch
motivation: 现有的轨迹优化方法通常仅在离散采样点处强制执行时序逻辑规范，无法保证连续时间内的安全性且存在离散化导致的保守性。
method: 提出一种平滑且精确的参数化方案，通过密集时间评估来处理复杂的CT-STL公式，并优化了梯度景观以利于求解器运行。
result: 该方法在保持逻辑严谨性的前提下，允许使用更稀疏的离散化网格，从而减小了非凸规划问题的规模并提高了计算效率。
conclusion: 该框架为处理具有复杂时空约束的机器人轨迹优化提供了一种兼具连续时间保证和高效数值性能的解决方案。
---

## 摘要
本文提出了一种用于非凸轨迹优化的连续时间信号时序逻辑（CT-STL）规约的平滑参数化方法，该方法在底层数值积分方案的精度范围内是可靠且完备的。CT-STL 为编码丰富的时序和逻辑任务需求提供了一个自然框架，但现有的轨迹优化公式通常仅在离散采样节点处强制执行此类规约。相比之下，所提出的方法在稠密时间内评估规约，从而保证了“总是（always）”谓词的连续时间满足，这对于避障等路径约束至关重要；同时，通过允许在规定时间间隔内的任何时间满足，消除了“最终（eventually）”谓词由节点引起的保守性。这两个稠密时间构造也作为处理更通用 CT-STL 公式（包括复杂的“直到（until）”规约）的主要构建模块。此外，所提出的参数化解决了标准定量语义中固有的局部性和梯度掩蔽问题，为基于梯度的求解器提供了更有利的优化景观。虽然稠密时间评估在离散化过程中引入了额外的函数评估，但它也允许使用大幅变粗的时间网格，而不会牺牲安全性或逻辑保真度。这反过来又降低了所得非凸规划的维度，而维度通常是轨迹生成成本的主导因素。通过一个受复杂连续时间“直到”规约约束的敏捷四旋翼飞行问题，证明了所提出框架的数值有效性和语义精确性。实现代码可在 https://github.com/UW-ACL/TrajOpt_CT-STL 获取。

## Abstract
This paper presents a smooth parameterization of continuous-time Signal Temporal Logic (CT-STL) specifications for nonconvex trajectory optimization that is sound and complete up to the accuracy of the underlying numerical integration scheme. CT-STL provides a natural framework for encoding rich temporal and logical task requirements, but existing trajectory-optimization formulations typically enforce such specifications only at discrete sampling nodes. In contrast, the proposed method evaluates specifications in dense time, thereby guaranteeing continuous-time satisfaction of always predicates, which is critical for path constraints such as obstacle avoidance, while eliminating the node-induced conservatism of eventually predicates by allowing satisfaction at any time within the prescribed interval. These two dense-time constructions also serve as the main building blocks for handling more general CT-STL formulas, including complex until specifications. Furthermore, the proposed parameterization resolves the locality and gradient-masking issues inherent in standard quantitative semantics, yielding a more favorable landscape for gradient-based solvers. Although dense-time evaluation introduces additional function evaluations during discretization, it also permits substantially coarser temporal grids without sacrificing safety or logical fidelity. This, in turn, reduces the dimension of the resulting nonconvex program, which is often the dominant factor in trajectory-generation cost. The numerical effectiveness and semantic exactness of the proposed framework are demonstrated on an agile quadrotor flight problem subject to a complex continuous-time until specification. The implementation is available at https:// github.com/UW-ACL/TrajOpt_CT-STL

---

## 论文详细总结（自动生成）

以下是对论文《Smooth and Exact Parameterization of Continuous-time Signal Temporal Logic Specifications for Trajectory Optimization》的结构化深度分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在非凸轨迹优化中，既能保证**连续时间（Continuous-time）**下的信号时序逻辑（STL）规约满足，又能保持优化过程的**平滑性**和**计算效率**。
*   **研究动机**：
    *   **离散化的局限性**：现有的 STL 轨迹优化大多在离散采样点（DT-STL）上强制执行约束，这会导致采样点之间的行为失控（如碰撞），或者为了保证安全而引入过度的保守性。
    *   **数值挑战**：标准的 STL 鲁棒性语义包含 `min/max` 算子，是非光滑的。现有的平滑近似方法（如 LSE 或 Softmax）存在“梯度掩蔽（Masking）”和“局部性（Locality）”问题，导致基于梯度的求解器容易陷入局部最优或收敛缓慢。
    *   **语义保真度**：需要一种在数值积分精度范围内既“可靠（Sound）”又“完备（Complete）”的参数化方法。

### 2. 论文提出的方法论
*   **核心思想**：结合**广义均值平滑鲁棒性（GMSR）**与**稠密时间（Dense-time）评估**。
*   **关键技术细节**：
    *   **稠密时间评估**：不只是在优化的决策节点上评估 STL，而是在动力学数值积分（如 RK4）产生的内部子节点（Sub-nodes）上进行评估。这确保了在整个连续轨迹上捕捉规约的违反情况。
    *   **GMSR 参数化**：利用广义几何均值和算术均值的组合来替代 `min/max`。
        *   **逻辑算子**：通过平滑函数 $h_c^\wedge$ 和 $h_c^\vee$ 实现与、或、蕴含等逻辑。
        *   **时序算子**：将“总是（G）”、“最终（F）”和“直到（U）”转化为在稠密时间序列上的平滑聚合。
    *   **梯度分布特性**：GMSR 的数学结构确保梯度信息能分布到所有相关的子公式和时间点上，有效解决了梯度消失和掩蔽问题。
    *   **轨迹优化公式**：采用多重射击法（Multiple Shooting）离散化动力学，并将 CT-STL 规约作为平滑约束或惩罚项整合进非凸优化问题中。

### 3. 实验设计
*   **实验场景**：敏捷四旋翼无人机飞行任务。
*   **任务规约**：一个复杂的“直到（Until）”规约——无人机必须保持低速行驶，**直到**到达充电站（Charging Station），同时全程需满足倾斜角、推力和全局速度限制。
*   **Benchmark 与对比**：
    *   实验主要验证了该框架处理复杂 CT-STL 的能力。
    *   对比对象主要是传统的离散时间评估方法（指出其在节点间可能违反约束）。
    *   使用了 **Prox-convex** 方法作为底层非凸优化算法，并与 **JAX**（自动微分）和 **QOCO**（锥优化求解器）集成。

### 4. 资源与算力
*   **硬件环境**：2023 款 MacBook Pro，搭载 **Apple M2** 处理器。
*   **计算性能**：
    *   求解 10 次 Prox-convex 迭代。
    *   凸子问题求解总耗时约 **15.35 ms**。
    *   动力学离散化与 CT-STL 评估耗时约 **0.97 ms**。
*   **算力说明**：该方法不涉及深度学习训练，因此未动用大规模 GPU 集群，强调的是嵌入式实时优化的潜力。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了一个具有代表性的复杂四旋翼飞行案例。
*   **充分性评价**：
    *   **逻辑覆盖**：实验涵盖了 G（总是）和 U（直到）等核心算子，验证了方法对复杂时序逻辑的处理能力。
    *   **数值验证**：通过绘制速度曲线和充电站边际距离图，直观证明了连续时间下的规约满足（而非仅在节点处满足）。
    *   **局限性**：实验组数相对较少，主要集中在可行性验证，缺乏在大规模随机场景下的统计性能分析（如成功率、随机初始化的鲁棒性等）。

### 6. 论文的主要结论与发现
*   **连续时间保证**：该方法能有效消除离散采样导致的安全性漏洞，确保轨迹在整个时间轴上满足 STL 规约。
*   **降低维度**：由于可以在稠密时间评估规约，优化问题可以使用更粗略的决策网格（更少的决策变量），从而显著降低非凸规划的计算负担。
*   **优化景观友好**：GMSR 参数化提供了高质量的梯度，使得基于梯度的求解器（如 SCP）能够高效收敛。

### 7. 优点
*   **语义精确性**：在数值精度范围内实现了 CT-STL 的精确参数化，解决了符号一致性问题。
*   **平滑性与梯度质量**：克服了传统平滑算子的梯度掩蔽缺陷，对复杂逻辑规约的优化非常友好。
*   **实用性强**：允许使用粗网格决策，极大地提升了轨迹生成的效率，适合实时应用。

### 8. 不足与局限
*   **固定终端时间**：目前的公式主要针对固定终端时间（Fixed-final-time）问题，对于终端时间可变的优化问题尚需进一步扩展。
*   **计算开销**：虽然减少了决策变量，但稠密时间评估增加了每次迭代中函数评估和雅可比矩阵计算的次数。
*   **应用范围**：目前仅在四旋翼点质量模型上进行了验证，对于更高维、更复杂的非线性系统（如全状态动力学或多机协同）的扩展性有待观察。

（完）
