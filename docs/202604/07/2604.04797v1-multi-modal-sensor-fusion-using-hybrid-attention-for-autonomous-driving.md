---
title: Multi-Modal Sensor Fusion using Hybrid Attention for Autonomous Driving
title_zh: 自动驾驶中基于混合注意力的多模态传感器融合
authors: "Mayank Mayank, Bharanidhar Duraisamy, Florian Geiß, Abhinav Valada"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04797v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 用于自动驾驶感知的雷达-相机BEV融合
tldr: 针对自动驾驶中相机深度感知不准和雷达几何信息稀疏的挑战，本文提出MMF-BEV融合框架。该方法基于BEV视图，结合了BEVDepth相机分支与RadarBEVNet雷达分支，并利用可变形自注意力和交叉注意力机制实现跨模态特征的精确对齐与融合。通过在VoD数据集上的验证，该框架不仅在检测精度上显著优于单模态基准，还通过传感器贡献分析量化了不同距离下模态间的互补性。
selection_source: fresh_fetch
motivation: 利用相机丰富的语义信息与4D毫米波雷达精确的测距及测速能力，解决单模态传感器在3D目标检测中的局限性。
method: 提出一种基于混合可变形注意力的BEV融合架构，并采用先预训练相机深度分支再进行联合训练的两阶段策略。
result: 在View-of-Delft数据集上，MMF-BEV在所有目标类别上均优于单模态模型，并展现出与现有先进融合方法相当的竞争力。
conclusion: 研究证明了可变形注意力在多模态特征对齐中的有效性，并揭示了相机与雷达在不同探测距离下的协同贡献规律。
---

## 摘要
自动驾驶中准确的3D目标检测需要互补的传感器。摄像头提供密集的语义信息但深度信息不可靠，而毫米波雷达提供精确的距离和速度测量，但几何信息稀疏。我们提出了 MMF-BEV，这是一种雷达-摄像头 BEV 融合框架，在 View-of-Delft (VoD) 4D 雷达数据集上利用可变形注意力进行跨模态特征对齐。MMF-BEV 构建了一个 BEVDepth 摄像头分支和一个 RadarBEVNet 雷达分支，每个分支都通过可变形自注意力进行了增强，并通过一个可变形交叉注意力模块进行融合。我们评估了三种配置：仅摄像头、仅雷达以及混合融合。传感器贡献分析量化了不同距离下的模态权重，为传感器互补性提供了可解释的证据。一种两阶段训练策略——先利用深度监督预训练摄像头分支，然后联合训练雷达和融合模块——稳定了学习过程。在 VoD 数据集上的实验表明，MMF-BEV 在全标注区域和近距离感兴趣区域的所有目标类别中，均一致优于单模态基准模型，并与之前的融合方法相比取得了具有竞争力的结果。

## Abstract
Accurate 3D object detection for autonomous driving requires complementary sensors. Cameras provide dense semantics but unreliable depth, while millimeter-wave radar offers precise range and velocity measurements with sparse geometry. We propose MMF-BEV, a radar-camera BEV fusion framework that leverages deformable attention for cross-modal feature alignment on the View-of-Delft (VoD) 4D radar dataset [1]. MMF-BEV builds a BEVDepth [2] camera branch and a RadarBEVNet [3] radar branch, each enhanced with Deformable Self-Attention, and fuses them via a Deformable Cross-Attention module. We evaluate three configurations: camera-only, radar-only, and hybrid fusion. A sensor contribution analysis quantifies per-distance modality weighting, providing interpretable evidence of sensor complementarity. A two-stage training strategy - pre-training the camera branch with depth supervision, then jointly training radar and fusion modules stabilizes learning. Experiments on VoD show that MMF-BEV consistently outperforms unimodal baselines and achieves competitive results against prior fusion methods across all object classes in both the full annotated area and near-range Region of Interest.

---

## 论文详细总结（自动生成）

### 论文总结：基于混合注意力的多模态传感器融合自动驾驶框架 (MMF-BEV)

#### 1. 核心问题与研究动机
在自动驾驶的3D目标检测任务中，单一传感器存在固有局限：**摄像头**提供丰富的语义和纹理，但单目深度估计极不可靠；**毫米波雷达**能提供精确的距离和多普勒速度，但点云几何信息稀疏且缺乏语义。
本文的研究动机在于：
*   如何在统一的鸟瞰图（BEV）空间内有效对齐和融合这两种异构特征。
*   探索**可变形自注意力（DSA）**对单模态特征增强的作用。
*   提供一种可解释的手段，量化分析相机与雷达在不同距离和类别下的贡献度。

#### 2. 方法论
论文提出了 **MMF-BEV** 框架，核心思想是利用混合注意力机制进行特征提取与融合：
*   **相机分支（BEVDepth + DSA）**：采用 BEVDepth 架构，利用显式深度监督将图像特征投影到 BEV 空间，并引入可变形自注意力（DSA）来捕捉长程空间依赖并细化特征。
*   **雷达分支（RadarBEVNet + DSA）**：利用 4D 毫米波雷达（包含高度和多普勒信息），通过双流骨干网络提取特征，同样使用 DSA 进行空间增强。
*   **多层混合融合模块（MultiLayer Hybrid Fusion）**：
    *   **双向交叉注意力**：相机特征作为查询（Query）去检索雷达特征（Key/Value）以获取精确位置；雷达特征作为查询去检索相机特征以获取语义。
    *   **特征聚合**：将自注意力增强的原始特征与交叉注意力融合后的特征拼接，通过连续的卷积-归一化-激活（CBR）层进行空间和通道维度的自适应融合。
*   **两阶段训练策略**：第一阶段预训练相机分支（带深度监督）；第二阶段冻结相机参数，联合训练雷达分支和融合模块，以确保训练稳定性。

#### 3. 实验设计
*   **数据集**：使用 **View-of-Delft (VoD)** 4D 雷达数据集，该数据集专注于城市环境中的弱势路路使用者（行人和骑行者）。
*   **评估场景**：分别在“全标注区域（50m内）”和“驾驶走廊/感兴趣区域（ROI，近距离）”进行评估。
*   **对比方法（Benchmark）**：
    *   单模态基准：PointPillars (雷达), BEVDepth (相机)。
    *   融合方法基准：RCFusion, RCBEVDet（当前最先进的方法之一）。
*   **消融实验**：对比了仅相机、仅雷达、朴素拼接（Naive Concatenation）以及完整的 MMF-BEV 融合模型。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量及训练总时长。
*   **实现细节**：提到基于 MMDetection3D 框架开发，使用了 AdamW 优化器和余弦退火学习率策略。

#### 5. 实验数量与充分性
*   **实验规模**：论文针对三个主要类别（汽车、行人、骑行者）进行了详尽的定量分析，并提供了多组 BEV 特征图的可视化对比。
*   **充分性与客观性**：
    *   实验涵盖了从单模态到多模态的完整对比，证明了融合的必要性。
    *   通过“朴素拼接”与“注意力融合”的对比，客观证明了可变形交叉注意力在解决几何失配方面的有效性。
    *   **传感器贡献分析**是一大亮点，通过数千帧数据的统计，量化了模态权重随距离变化的规律，增强了实验的说服力。

#### 6. 主要结论与发现
*   **融合优势**：MMF-BEV 在所有指标上均显著优于单模态模型。在 ROI 区域，融合模型比仅相机模型 mAP 提升了约 31%。
*   **模态互补性**：
    *   **近距离（<15m）**：相机贡献占主导（约 70%），因为此时图像分辨率高且深度估计相对准确。
    *   **远距离（>30m）**：雷达贡献显著增加（>55%），弥补了单目相机深度不确定性随距离增加而剧增的缺陷。
*   **类别差异**：行人的检测更依赖相机语义；汽车和骑行者的检测则更多受益于雷达的 RCS（雷达散射截面）返回。

#### 7. 优点
*   **结构灵活性**：每个分支均可作为独立检测器运行，且 DSA 的引入提升了单模态的鲁棒性。
*   **可解释性强**：提出的传感器贡献图（Sensor Contribution Maps）为理解多模态融合逻辑提供了直观的证据，符合自动驾驶对安全可解释性的需求。
*   **4D 雷达应用**：充分利用了 4D 雷达的高度和速度信息，相比传统 3D 雷达在复杂城市环境下的表现更佳。

#### 8. 不足与局限
*   **单目限制**：该框架目前主要针对单前视摄像头，缺乏多摄像头环视场景的验证，这限制了其在全向感知中的应用参考价值。
*   **远距离数据稀疏**：VoD 数据集 50m 以外的标注较少，导致模型在极远距离的融合性能未能得到充分验证。
*   **实时性分析缺失**：论文未提供推理延迟（Inference Latency）数据，对于自动驾驶这种实时性要求极高的场景，缺乏算力开销的评估。

（完）
