---
title: Precise Robot Command Understanding Using Grammar-Constrained Large Language Models
title_zh: 基于语法约束大语言模型的精确机器人指令理解
authors: "Xinyun Huo, Raghav Gnanasambandam, Xinyao Zhang"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04233v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 语法约束的LLM用于机器人指令理解
tldr: 针对工业人机协作中大语言模型生成指令不够精确的问题，本文提出一种语法约束的混合模型。该方法结合了微调后的LLM与语法驱动的自然语言理解系统，通过两阶段处理和自动纠错机制，将自然语言指令转化为标准化的机器人可读JSON格式，显著提升了指令的准确性和安全性。
selection_source: fresh_fetch
motivation: 工业机器人需要极高的指令精确度，而通用大语言模型在处理特定领域指令时往往缺乏必要的确定性和安全性。
method: 采用微调LLM进行高层推理，并结合结构化语言模型与语法解析器，通过验证反馈循环强制输出符合预定义规则的JSON指令。
result: 在HuRIC数据集上的实验表明，该混合模型在指令有效性方面优于纯微调模型和传统语法驱动模型。
conclusion: 语法约束与大语言模型的结合为工业环境下安全、高效的人机协作提供了一种鲁棒且精确的指令理解方案。
---

## 摘要
工业环境中的人机协作需要精确且可靠的通信，以提高运行效率。虽然大语言模型（LLMs）能够理解通用语言，但它们往往缺乏安全且可执行的工业指令所需的领域特定严谨性。为填补这一空白，本文介绍了一种新型的语法约束 LLM，它将语法驱动的自然语言理解（NLU）系统与微调后的 LLM 相结合，从而兼顾了对话的灵活性和机器人技术所需的确定性精度。我们的方法采用了一个两阶段过程：首先，微调后的 LLM 对自然语言输入进行高层上下文推理和参数推断；其次，结构化语言模型（SLM）和基于语法的规范化器对 LLM 的输出进行约束，强制其转换为由有效动作帧和指令元素组成的标准化符号格式。该过程保证了生成的指令是有效的，并以机器人可读的 JSON 格式进行结构化。该模型的一个关键特征是验证和反馈循环。语法解析器根据预定义的机器人可执行动作列表对输出进行验证。如果指令无效，系统会自动生成纠正性提示并重新调用 LLM。这种迭代自纠错机制使模型能够从初始解释错误中恢复，从而提高系统的鲁棒性。我们将语法约束混合模型与两个基准模型（基于 API 的微调 LLM 和独立的语法驱动 NLU 模型）进行了评估。通过使用人机交互语料库（HuRIC）数据集，我们证明了该混合方法实现了卓越的指令有效性，从而促进了更安全、更有效的工业人机协作。

## Abstract
Human-robot collaboration in industrial settings requires precise and reliable communication to enhance operational efficiency. While Large Language Models (LLMs) understand general language, they often lack the domain-specific rigidity needed for safe and executable industrial commands. To address this gap, this paper introduces a novel grammar-constrained LLM that integrates a grammar-driven Natural Language Understanding (NLU) system with a fine-tuned LLM, which enables both conversational flexibility and the deterministic precision required in robotics. Our method employs a two-stage process. First, a fine-tuned LLM performs high-level contextual reasoning and parameter inference on natural language inputs. Second, a Structured Language Model (SLM) and a grammar-based canonicalizer constrain the LLM's output, forcing it into a standardized symbolic format composed of valid action frames and command elements. This process guarantees that generated commands are valid and structured in a robot-readable JSON format. A key feature of the proposed model is a validation and feedback loop. A grammar parser validates the output against a predefined list of executable robotic actions. If a command is invalid, the system automatically generates corrective prompts and re-engages the LLM. This iterative self-correction mechanism allows the model to recover from initial interpretation errors to improve system robustness. We evaluate our grammar-constrained hybrid model against two baselines: a fine-tuned API-based LLM and a standalone grammar-driven NLU model. Using the Human Robot Interaction Corpus (HuRIC) dataset, we demonstrate that the hybrid approach achieves superior command validity, which promotes safer and more effective industrial human-robot collaboration.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种结合大语言模型（LLM）与形式语法的混合架构，旨在解决工业环境下机器人指令理解的精确性与鲁棒性问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：通用大语言模型（LLM）虽然具备强大的自然语言处理能力，但在工业机器人场景下存在**非确定性**和**缺乏结构化约束**的问题。工业环境要求指令必须极度精确且符合特定的机器人动作规范（Action Frames），而 LLM 容易产生幻觉或输出格式错误的指令，导致安全隐患。
*   **研究背景**：智能制造（Industry 5.0）强调以人为本的人机协作（HRC），需要机器人能理解人类自然的、动态的语言输入，并将其转化为可执行的、标准化的 JSON 格式指令。

### 2. 方法论
该论文提出了一种**两阶段处理架构**，核心思想是将“语义推理”与“结构约束”解耦：
*   **第一阶段：语义理解（前端）**
    *   使用 **Meta-Llama-3-8B-Instruct** 作为基础模型。
    *   应用 **LoRA（低秩自适应）** 技术进行参数高效微调，使模型学习工业领域的动作框架（如 Taking, Bringing 等）。
    *   通过**提示工程（Prompt Engineering）**定义系统角色和用户角色，引导模型提取语义元素。
*   **第二阶段：语法约束与规范化（后端）**
    *   **语法解析器**：利用 Python 的 Lark 库和扩展巴科斯范式（EBNF）定义严格的机器人指令语法。
    *   **结构化验证**：将 LLM 的输出通过规范化器（Canonicalizer）转换为抽象语法树（AST），并验证其是否符合预定义的动作元素规则（如 "Bringing" 必须包含 "Theme" 和 "Goal"）。
*   **关键技术：反馈纠错循环**
    *   如果语法解析失败，系统不会直接报错，而是生成包含错误信息的**纠正性提示（Feedback Prompt）**，反馈给 LLM 进行迭代自纠错，直到生成合规指令或达到尝试上限。

### 3. 实验设计
*   **数据集**：使用 **HuRIC（人机交互语料库）**，包含 656 条标注了动作帧的自然语言指令，涵盖 16 种不同的动作类型。
*   **评估指标**：
    *   **JSON Exact Match (EM)**：预测的动作帧和语义元素是否与参考标注完全一致。
    *   **JSON Similarity**：基于 Jaccard 指数计算预测结果与参考结果的结构和语义重合度。
*   **对比方法（Baselines）**：
    1.  未微调的 API 模式 LLM。
    2.  微调后的 API 模式 LLM（仅靠 Prompt 和微调约束）。
    3.  纯语法驱动的传统 NLU 模型。
    4.  **本论文方法**：混合语法约束 LLM。

### 4. 资源与算力
*   **硬件环境**：本地工作站，配备 **NVIDIA RTX 3080 GPU (10GB VRAM)**，AMD Ryzen 7 5800X CPU，32GB RAM。
*   **软件环境**：Ubuntu 24.04 (WSL 2), PyTorch 2.3, Hugging Face Transformers 库。
*   **训练细节**：使用了 PEFT（参数高效微调）框架下的 LoRA 技术，允许在单块消费级 GPU 上完成 8B 参数模型的微调。

### 5. 实验数量与充分性
*   **实验规模**：数据集按 90%/10% 划分，测试集包含 66 个样本。
*   **充分性评价**：
    *   **优点**：实验对比了四种不同的技术路径，涵盖了从纯概率模型到纯规则模型的完整光谱，对比维度客观。
    *   **局限**：测试样本量（66条）相对较小，且仅在一个数据集（HuRIC）上进行了验证，缺乏跨领域或更大规模工业数据集的泛化性测试。

### 6. 主要结论与发现
*   **混合模型表现最优**：混合模型在 **Exact Match (48.48%)** 和 **JSON Similarity (88.07%)** 上均显著高于其他方法。
*   **LLM 与语法的互补性**：纯 LLM 语义理解好但格式乱（EM 仅 36.3%）；纯语法模型格式严谨但无法处理非标准输入（EM 仅 21.21%）。混合模型通过反馈循环有效结合了两者的优势。
*   **自纠错有效性**：反馈循环显著降低了失败输出率，提高了指令的最终可执行性。

### 7. 优点与亮点
*   **解耦设计**：将灵活的语义推理与刚性的语法约束分开，既保留了 LLM 的自然语言处理能力，又确保了机器人执行的安全性。
*   **反馈机制**：引入自动纠错循环，增强了系统在面对模糊输入时的鲁棒性。
*   **轻量化部署**：通过 LoRA 微调，证明了在有限算力下实现高性能工业指令理解的可行性。

### 8. 不足与局限
*   **数据集局限性**：HuRIC 数据集包含较多礼貌用语和疑问句，容易诱导 LLM 进入“对话模式”而非“指令模式”。
*   **语法歧义**：当所有语法规则合并为一个统一语法时，随着动作类型的增加，可能会出现结构歧义，导致 EM 率下降。
*   **模型依赖**：目前仅验证了 Llama-3-8B，不同基础模型的风格偏好可能会影响语法约束的效果。
*   **实时性挑战**：迭代反馈循环虽然提高了准确率，但可能会增加推理延迟，论文未详细讨论其在实时工业控制中的表现。

（完）
