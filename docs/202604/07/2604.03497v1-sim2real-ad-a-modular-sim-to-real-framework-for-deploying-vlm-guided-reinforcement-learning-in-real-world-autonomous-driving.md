---
title: "Sim2Real-AD: A Modular Sim-to-Real Framework for Deploying VLM-Guided Reinforcement Learning in Real-World Autonomous Driving"
title_zh: Sim2Real-AD：一种用于在真实世界自动驾驶中部署 VLM 引导强化学习的模块化 Sim-to-Real 框架
authors: "Zilin Huang, Zhengyang Wan, Zihao Sheng, Boyue Wang, Junwei You, Yue Leng, Sikai Chen"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03497v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于自动驾驶的VLM引导强化学习
tldr: 本研究针对VLM引导的强化学习策略在自动驾驶中从仿真到现实迁移难的问题，提出了Sim2Real-AD模块化框架。该框架通过几何观测桥接将单目图像转为BEV，利用物理感知映射统一动作空间，并采用两阶段渐进式训练稳定迁移过程。这是首个实现将CARLA训练的VLM-RL策略零样本部署到全尺寸真实车辆上的工作，无需任何真实世界训练数据，显著提升了自动驾驶策略的实用性。
selection_source: fresh_fetch
motivation: 旨在解决VLM引导的强化学习策略因仿真与现实间观测数据和动作语义的不一致而难以在真实车辆上部署的挑战。
method: 提出了包含几何观测桥接(GOB)、物理感知动作映射(PAM)、两阶段渐进式训练(TPT)和实时部署流水线(RDP)的模块化框架。
result: "在全尺寸真实车辆的零样本部署中，跟车、避障和停车标志交互场景分别取得了90%、80%和75%的成功率。"
conclusion: 该框架成功验证了在无真实训练数据的情况下，将仿真训练的复杂强化学习策略直接应用于真实自动驾驶场景的可行性。
---

## 摘要
将仿真中训练的强化学习（RL）策略部署到真实的自动驾驶车辆上仍然是一个根本性的挑战，特别是对于 VLM 引导的 RL 框架，其策略通常是利用仿真器原生的观测值和仿真器耦合的动作语义学习的，而这些在物理平台上是无法获得的。本文提出了 Sim2Real-AD，这是一个模块化框架，用于将 CARLA 训练的 VLM 引导 RL 策略零样本（zero-shot）迁移到全尺寸车辆，且无需任何真实世界的 RL 训练数据。该框架将迁移问题分解为四个组件：几何观测桥接（GOB），将单目前视图图像转换为仿真器兼容的鸟瞰图（BEV）观测；物理感知动作映射（PAM），将策略输出转换为与平台无关的物理指令；两阶段渐进式训练（TPT）策略，通过分离动作空间和观测空间的迁移来稳定适配过程；以及实时部署流水线（RDP），集成了感知、策略推理、控制转换和安全监控以实现闭环执行。仿真实验表明，该框架在不同的奖励范式下保留了代表性 RL 算法的相对性能排序，并验证了各模块的贡献。在全尺寸 Ford E-Transit 上的零样本部署在跟车、避障和停车标志交互场景中分别实现了 90%、80% 和 75% 的成功率。据我们所知，本研究是首批展示在没有任何真实世界 RL 训练数据的情况下，将 CARLA 训练的 VLM 引导 RL 策略零样本闭环部署到全尺寸真实车辆上的研究之一。演示视频和代码可在以下网址获取：https://zilin-huang.github.io/Sim2Real-AD-website/。

## Abstract
Deploying reinforcement learning policies trained in simulation to real autonomous vehicles remains a fundamental challenge, particularly for VLM-guided RL frameworks whose policies are typically learned with simulator-native observations and simulator-coupled action semantics that are unavailable on physical platforms. This paper presents Sim2Real-AD, a modular framework for zero-shot sim-to-real transfer of CARLA-trained VLM-guided RL policies to full-scale vehicles without any real-world RL training data. The framework decomposes the transfer problem into four components: a Geometric Observation Bridge (GOB) that converts monocular front-view images into simulator-compatible bird's-eye-view (BEV) observations, a Physics-Aware Action Mapping (PAM) that translates policy outputs into platform-agnostic physical commands, a Two-Phase Progressive Training (TPT) strategy that stabilizes adaptation by separating action-space and observation-space transfer, and a Real-time Deployment Pipeline (RDP) that integrates perception, policy inference, control conversion, and safety monitoring for closed-loop execution. Simulation experiments show that the framework preserves the relative performance ordering of representative RL algorithms across different reward paradigms and validate the contribution of each module. Zero-shot deployment on a full-scale Ford E-Transit achieves success rates of 90%, 80%, and 75% in car-following, obstacle avoidance, and stop-sign interaction scenarios, respectively. To the best of our knowledge, this study is among the first to demonstrate zero-shot closed-loop deployment of a CARLA-trained VLM-guided RL policy on a full-scale real vehicle without any real-world RL training data. The demo video and code are available at: https://zilin-huang.github.io/Sim2Real-AD-website/.

---

## 论文详细总结（自动生成）

### Sim2Real-AD 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶领域，利用视觉语言模型（VLM）引导的强化学习（RL）在仿真环境（如 CARLA）中取得了显著进展。然而，将这些策略迁移到真实车辆面临两大核心挑战：
*   **观测间隙（Observation Gap）：** 仿真策略通常依赖于“特权”信息（如完美的 360° 语义鸟瞰图 BEV），而真实车辆仅能获取嘈杂、有遮挡且视角受限的相机图像。
*   **动力学间隙（Dynamics Gap）：** 仿真中的动作指令（如直接的油门/转向）与真实车辆的物理反馈（受轴距、执行器延迟、轮胎摩擦等影响）存在严重失配。
**Sim2Real-AD** 的研究动机在于建立一个模块化框架，实现从仿真到真实全尺寸车辆的**零样本（Zero-shot）**闭环迁移，无需任何真实世界的 RL 训练数据。

#### 2. 方法论：核心思想与关键技术
该框架将迁移问题分解为四个相互解耦的模块：
*   **几何观测桥接（GOB）：** 
    *   使用预训练的 SegFormer-B0 进行语义分割，再通过逆透视变换（IPM）将单目前视图图像投影为 14 通道的 BEV 语义张量。
    *   这种方法消除了光照、纹理等外观差异，使策略仅处理统一的几何语义表示。
*   **物理感知动作映射（PAM）：** 
    *   重新定义策略输出为平台无关的物理量：**曲率 ($\kappa$)** 和 **期望速度 ($v_d$)**。
    *   通过运动学自行车模型将曲率转换为转向角，利用 PID 控制器跟踪期望速度。这使得迁移到新车型时仅需简单的物理参数校准（约 30 分钟）。
*   **两阶段渐进式训练（TPT）：** 
    *   **阶段 1：** 在完美 BEV 下训练策略以适应新的物理动作空间。
    *   **阶段 2：** 切换到 GOB 生成的嘈杂 BEV 进行微调。这种课程学习方式避免了同时处理观测和动力学剧变导致的训练不稳定。
*   **实时部署流水线（RDP）：** 
    *   集成了感知、推理、控制转换和多层安全监控（如速度限制、地理围栏、人工接管检测）。
*   **理论保证：** 论文推导了迁移误差界限，证明性能损失由分割误差、PID 跟踪误差和观测分布间隙共同决定，且均可独立优化。

#### 3. 实验设计
*   **仿真环境：** 使用 CARLA 0.9.13。在 Town 2 进行训练，在 Town 1, 3, 4, 5 进行泛化性测试。
*   **对比算法（Benchmark）：** 验证了框架的**奖励无关性**，对比了三种不同奖励范式的算法：
    1.  **ChatScene：** 手写奖励。
    2.  **VLM-RL：** 基于 CLIP 的静态语义奖励。
    3.  **DriveVLM-RL（本文基座）：** 基于双路径（静态+动态 LVLM 推理）的语义奖励。
*   **真实世界场景：** 在全尺寸 Ford E-Transit 货车上进行了三个场景的测试：
    1.  **S1 跟车：** 评估纵向控制。
    2.  **S2 静态避障：** 评估横向绕行能力。
    3.  **S3 停车标志交互：** 评估对交通规则和行人的语义理解。

#### 4. 资源与算力
*   **硬件：** 真实车辆搭载 **NVIDIA RTX A6000 GPU**。
*   **推理延迟：** 端到端闭环延迟平均为 **17.2 ms**（其中分割占 8.7ms，策略推理仅 1.8ms），完全满足 20Hz（50ms 周期）的实时控制要求。
*   **训练时长：** 阶段 1 训练 100 万步，阶段 2 微调 50 万步。文中未详细说明具体训练总耗时（小时数），但强调了其训练-部署解耦特性，部署时无需运行昂贵的 VLM 推理。

#### 5. 实验数量与充分性
*   **仿真实验：** 涵盖了 5 个不同的城镇地图，进行了详尽的消融实验（验证 GOB, PAM, TPT 各自的贡献）。
*   **真实实验：** 每个场景（S1-S3）进行了 **20 次独立重复实验**，总计 60 次真实路测。
*   **充分性评价：** 实验设计非常充分且客观。通过“影子模式”分析了奖励信号在真实世界的演变，并对比了 VLM 直接控制（VLM-as-Control）的延迟，证明了本文方法的实用性。

#### 6. 主要结论与发现
*   **零样本迁移可行：** 在无真实训练数据下，DriveVLM-RL 在跟车、避障、停车标志场景分别取得 **90%、80%、75%** 的成功率。
*   **性能排序保持：** 仿真中表现最好的算法（DriveVLM-RL）在真实世界依然最强，证明框架保留了算法的原始优势。
*   **语义理解的保留：** 实验证明，Sim2Real-AD 不仅迁移了运动控制，还成功保留了 VLM 赋予策略的语义推理能力（如识别行人并提前减速）。
*   **安全性提升：** 相比于 VLM-RL，DriveVLM-RL 在复杂场景下的干预率降低了 75%。

#### 7. 优点
*   **模块化与通用性：** 框架不绑定特定 RL 算法，可作为通用插件使用。
*   **部署效率极高：** 仅需 30 分钟物理校准，无需采集真实数据进行微调。
*   **实时性强：** 通过训练-部署解耦，解决了大模型无法实时控制的痛点。
*   **理论支撑：** 提供了严谨的数学证明来解释迁移误差的来源。

#### 8. 不足与局限
*   **速度限制：** 实验中真实车辆限速 15km/h，尚未验证高速场景下的动力学稳定性。
*   **视角局限：** 仅使用单目前视相机，导致 BEV 在侧向和后方的感知缺失，影响大角度转弯或复杂路口的表现。
*   **环境依赖：** 依赖 GPS 路径点提供全局导航，尚未实现完全基于视觉的端到端路径规划。
*   **平面假设：** IPM 转换依赖地面平坦假设，在坡度较大的道路上可能产生几何畸变。

（完）
