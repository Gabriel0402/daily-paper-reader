---
title: Visual Prompt Based Reasoning for Offroad Mapping using Multimodal LLMs
title_zh: 基于视觉提示推理的多模态大语言模型越野地图构建
authors: "Abdelmoamen Nasser, Yousef Baba'a, Murad Mebrahtu, Nadya Abdel Madjid, Jorge Dias, Majid Khonji"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04564v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用SAM2和VLM进行环境分割的零样本方法
tldr: 本研究针对传统越野自主导航依赖多个独立模型且需大量标注数据的问题，提出了一种基于视觉提示推理的零样本方法。该方法结合SAM2进行环境分割，并利用视觉语言模型（VLM）对带有数字标签的分割区域进行可行驶性推理。这种统一框架消除了对特定地形模型的依赖，不仅在高清分割数据集上超越了现有的SOTA可训练模型，还在Isaac Sim仿真环境中成功实现了全栈导航，展示了多模态大模型在复杂越野环境感知中的强大潜力。
selection_source: fresh_fetch
motivation: 传统越野导航系统依赖多个独立训练的地形分析模型，导致系统复杂且对特定任务数据集依赖性强。
method: 采用SAM2进行环境分割，并结合视觉语言模型对标注有数字标签的掩码区域进行零样本可行驶性推理。
result: 该方法在高清分割数据集上优于现有的最先进可训练模型，并支持在Isaac Sim越野环境中的全栈导航。
conclusion: 研究证明了利用多模态大模型的内在推理能力可以有效替代传统的多模型地形感知框架，实现高效的越野建图。
---

## 摘要
传统的越野自主导航方法依赖于独立的模型来进行地形分类、高度估计以及侧滑或坡度条件的量化。使用多个模型需要分别训练每个组件，并需要特定任务的数据集和微调。在这项工作中，我们提出了一种零样本（zero-shot）方法，利用 SAM2 进行环境分割，并使用视觉语言模型（VLM）来推理可行驶区域。我们的方法包括向 VLM 同时输入原始图像和带有每个掩码数字标签注释的分割图像。然后提示 VLM 识别由这些数字标签表示的哪些区域是可行驶的。结合规划和控制模块，这一统一框架消除了对显式地形特定模型的需求，转而依赖 VLM 固有的推理能力。我们的方法在高分辨率分割数据集上超越了最先进的可训练模型，并在 Isaac Sim 越野环境中实现了全栈导航。

## Abstract
Traditional approaches to off-road autonomy rely on separate models for terrain classification, height estimation, and quantifying slip or slope conditions. Utilizing several models requires training each component separately, having task specific datasets, and fine-tuning. In this work, we present a zero-shot approach leveraging SAM2 for environment segmentation and a vision-language model (VLM) to reason about drivable areas. Our approach involves passing to the VLM both the original image and the segmented image annotated with numeric labels for each mask. The VLM is then prompted to identify which regions, represented by these numeric labels, are drivable. Combined with planning and control modules, this unified framework eliminates the need for explicit terrain-specific models and relies instead on the inherent reasoning capabilities of the VLM. Our approach surpasses state-of-the-art trainable models on high resolution segmentation datasets and enables full stack navigation in our Isaac Sim offroad environment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Visual Prompt Based Reasoning for Offroad Mapping using Multimodal LLMs》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的越野自主导航系统通常依赖于多个独立训练的模型（如地形分类、高度估计、侧滑预测等），这导致系统复杂、需要大量特定任务的标注数据且泛化能力有限。
*   **核心问题**：如何在无需针对特定地形进行大规模训练（零样本/Zero-shot）的情况下，实现对复杂越野环境的可行驶区域识别与导航。
*   **整体含义**：论文提出了一种结合基础视觉模型（SAM2）和多模态大语言模型（VLM）的统一框架，利用大模型的推理能力替代传统的硬编码或专用分类模型。

### 2. 方法论
该框架包含四个核心模块：
*   **分割模块（SAM2）**：利用 SAM2 生成环境掩码。为了提高实时性，采用网格点提示（Point Prompting）而非内置的自动掩码生成。通过 IoU 阈值和面积阈值对掩码进行过滤和合并。
*   **视觉提示推理（VLM Inference）**：
    *   **核心思想**：将原始图像与带有数字标签的分割图像拼接成“拼贴图（Collage）”。
    *   **流程**：向 VLM 输入拼贴图和文本提示（如“识别所有可行驶区域，包括泥土、沙地、碎石等”），VLM 输出代表可行驶区域的数字索引。
*   **建图与规划**：将 VLM 识别的二进制掩码投影到 2D 占据网格中。
    *   **全局规划**：使用 D* Lite 算法计算最短路径。
    *   **局部规划**：使用 Hybrid A* 算法考虑车辆运动学约束生成可行轨迹。
*   **控制模块**：采用 Stanley 控制器进行横向控制，PID 控制器进行纵向速度跟踪。

### 3. 实验设计
*   **仿真环境**：使用 NVIDIA Isaac Sim 和 Unreal Engine 构建了包含草地、沙地、岩石等多种地形的越野场景。
*   **数据集**：
    *   **仿真数据**：采集了 2,957 张 RGB 图像并标注。
    *   **真实数据集**：在 ORFD、RUGD 和 O2DTD 三个公开越野数据集上进行基准测试。
*   **对比方法**：对比了 PathFormer, Mask2Former, CLIPSeg, CGNet, PSPNet 等最先进的可训练模型。
*   **VLM 测试对象**：GPT-4o, GPT-5 Mini (注：文中提及), Aquila-VL-2B, Ivy-VL, MiniCPM-V-2.6。

### 4. 资源与算力
*   **硬件设备**：
    *   **分割测试**：NVIDIA RTX 4070 (16GB GPU), i9-9900K CPU, 32GB RAM。
    *   **实时仿真与全栈评估**：NVIDIA RTX 3090 (24GB GPU)。
*   **训练时长**：由于是零样本（Zero-shot）方法，该框架**无需针对特定任务进行训练**，主要开销在于推理延迟。

### 5. 实验数量与充分性
*   **实验维度**：
    *   **实时性测试**：对比了 SAM2 不同提示方式的耗时。
    *   **消融/参数研究**：测试了 12 种不同的视觉/文本提示组合（如拼贴图 vs 仅标注图，单数字输出 vs 多数字输出，不同详细程度的指令）。
    *   **可达性测试**：在仿真中对三个目标点进行了多次导航测试（每组重复 5 次）。
    *   **跨模型对比**：对比了 5 种不同规模的 VLM。
*   **充分性评价**：实验设计较为全面，涵盖了从感知精度到端到端导航性能的评估，且在真实数据集上进行了验证，具有较强的说服力。

### 6. 主要结论与发现
*   **推理能力**：VLM 能够通过视觉提示有效地识别复杂地形，GPT 系列模型在 IoU 表现上最佳（GPT-5 Mini 达 0.727）。
*   **提示策略**：对于较小的模型（如 Aquila），简单的指令和单数字输出效果更好；而对于大模型（如 GPT-4o），提供完整的上下文信息能显著提升性能。
*   **性能表现**：该方法在高清图像数据集上表现优异，甚至在某些指标上超越了需要大量训练的 SOTA 模型（如在 ORFD 上 IoU 达 0.9141）。
*   **实时性**：本地运行的小型 VLM（如 Aquila-VL-2B）推理时间仅需约 0.59 秒，具备初步的实时应用潜力。

### 7. 优点
*   **零样本泛化**：无需地形特定的标注数据训练，能够适应未见过的越野环境。
*   **视觉提示创新**：通过给分割掩码编号并让 VLM “做选择题”的方式，巧妙地解决了 VLM 难以直接输出精确坐标的问题。
*   **全栈集成**：不仅停留在感知层面，还集成了完整的规划与控制链路，并在仿真中实现了闭环导航。

### 8. 不足与局限
*   **低分辨率限制**：在低分辨率数据集（如 O2DTD）上表现较差，说明 VLM 对细微地形特征的识别依赖于图像清晰度。
*   **非确定性风险**：VLM 的生成特性可能导致输出不一致，存在安全隐患。
*   **延迟问题**：尽管使用了 SAM2 跟踪技术减少 VLM 调用频率，但 VLM 的推理延迟对于高速越野场景仍是一个挑战。
*   **语义模糊**：VLM 有时难以区分障碍物的物理属性（例如深坑与阴影），导致在某些极端地形（如仿真中的沟壑）导航失败。

（完）
