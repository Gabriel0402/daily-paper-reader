---
title: Temporal Logic Control of Nonlinear Stochastic Systems with Online Performance Optimization
title_zh: 具有在线性能优化的非线性随机系统时序逻辑控制
authors: "Alessandro Riccardi, Thom Badings, Luca Laurenti, Alessandro Abate, Bart De Schutter"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01372v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 非线性随机系统的时态逻辑控制
tldr: 针对安全关键环境下的非线性随机系统，传统基于区间马尔可夫决策过程（IMDP）的抽象方法通常仅生成单一策略，限制了在线性能优化。本文提出一种新型抽象技术，可生成满足时序逻辑规范的策略集合，并结合模型预测控制（MPC）在保证满足概率的前提下实现在线成本最优化。实验证明该方法在保持高可靠性的同时，显著提升了系统的运行性能。
selection_source: fresh_fetch
motivation: 现有的形式化抽象方法通常只生成单一控制策略，缺乏在运行时针对能耗等性能指标进行动态优化的灵活性。
method: 提出一种新的IMDP抽象技术以获取满足规范的策略集合，并利用MPC在该集合内在线搜索最优控制动作以最小化成本函数。
result: 实验结果显示，该方法在性能优化方面优于现有的单策略抽象技术，且仅伴随极小的安全性保证下降。
conclusion: 该研究成功结合了形式化验证与在线优化，为复杂随机系统在保证逻辑规范的前提下实现高效控制提供了新方案。
---

## 摘要
在安全关键型环境中部署自主系统需要能够保证满足复杂控制规范的控制策略。这些系统通常被建模为非线性离散时间随机系统。计算可证明满足复杂控制规范的策略的一种流行方法是构建有限状态抽象，通常表示为具有转移概率区间的马尔可夫决策过程（MDP），即区间马尔可夫决策过程（IMDP）。然而，现有的抽象技术仅计算单一策略，因此没有为在线成本或性能优化（例如能耗优化）留出空间。为了克服这一局限性，我们提出了一种新颖的 IMDP 抽象技术，该技术可以产生一组策略，其中每个策略都以一定的最小概率满足控制规范。因此，我们可以使用任何在线控制算法在这组经过验证的策略中进行搜索，同时保留整个策略集的保证满足概率。特别地，我们采用模型预测控制（MPC）来最小化一个与抽象中考虑的控制规范无关的目标代价函数。我们的实验表明，与最先进的单策略抽象技术相比，我们的方法具有更好的控制性能，且保证程度仅有轻微下降。

## Abstract
The deployment of autonomous systems in safety-critical environments requires control policies that guarantee satisfaction of complex control specifications. These systems are commonly modeled as nonlinear discrete-time stochastic systems. A~popular approach to computing a policy that provably satisfies a complex control specification is to construct a finite-state abstraction, often represented as a Markov decision process (MDP) with intervals of transition probabilities, i.e., an interval MDP (IMDP). However, existing abstraction techniques compute a \emph{single policy}, thus leaving no room for online cost or performance optimization, e.g., of energy consumption. To overcome this limitation, we propose a novel IMDP abstraction technique that yields a \emph{set of policies}, each of which satisfies the control specification with a certain minimum probability. We can thus use any online control algorithm to search through this set of verified policies while retaining the guaranteed satisfaction probability of the entire policy set. In particular, we employ model predictive control (MPC) to minimize a desired cost function that is independent of the control specification considered in the abstraction. Our experiments demonstrate that our approach yields better control performance than state-of-the-art single-policy abstraction techniques, with a small degradation of the guarantees.

---

## 论文详细总结（自动生成）

### 论文总结：具有在线性能优化的非线性随机系统时序逻辑控制

#### 1. 核心问题与整体含义（研究动机和背景）
在安全关键型（Safety-critical）领域，自主系统（如无人机、机器人）需要同时满足两个目标：
*   **形式化逻辑规范**：必须以高概率满足复杂的任务要求（如“到达目标点 A 且避开障碍物 B”），这通常通过离散抽象（如区间马尔可夫决策过程 IMDP）来实现。
*   **在线性能优化**：在执行任务时，需要实时最小化能耗、时间或控制代价，这通常是模型预测控制（MPC）的强项。

**现有挑战**：传统的抽象技术通常只生成**单一**的离散策略，一旦确定便无法在运行时进行调整。而 MPC 虽然擅长优化，却难以在非线性随机环境下提供长时序逻辑（如 LTL）的硬性概率保证。本文旨在弥合这两者之间的鸿沟。

#### 2. 方法论：核心思想与关键技术
论文提出了一种将**离线形式化抽象**与**在线 MPC 优化**相结合的框架：

*   **核心思想**：不再将抽象动作映射到单一控制输入，而是映射到一个**输入集合**（Lp-球体）。这产生了一个“允许的策略集”，为在线优化留出了自由度。
*   **关键技术细节**：
    1.  **集合值接口函数（Set-Valued Interface）**：定义了状态空间分区与输入空间子集（如 $L_\infty$ 球体）之间的映射。
    2.  **概率交替仿真关系（PASR）**：证明了只要在线选取的控制输入落在该集合内，系统满足逻辑规范的概率下界（$\lambda$）依然受离线计算的 IMDP 保证。
    3.  **混合整数二次规划（MIQP）**：在线阶段，将非线性动力学进行分段仿射（PWA）近似，并将“输入必须属于特定集合”的逻辑约束转化为线性约束，通过求解 MIQP 来最小化代价函数 $J$。
    4.  **安全回退机制**：即使 MPC 求解失败，系统仍可回退到抽象策略提供的任意可行输入，确保安全性。

#### 3. 实验设计
论文在三个经典的控制基准场景上进行了验证：
*   **双积分器（Double Integrator）**：线性动力学，用于验证基础框架。
*   **登山车（Mountain Car）**：非线性动力学，测试在复杂地形下的能效优化。
*   **Dubins 车（Dubins Car）**：非线性、非完整约束系统，模拟现实中的车辆导航。
*   **Benchmark 与对比方法**：对比对象是**Vanilla IMDP 抽象技术**（即不具备在线优化能力的单策略方法，如 [6, 36] 中的方法）。

#### 4. 资源与算力
*   **硬件平台**：Intel i7-1185G7 CPU，16 GB RAM 笔记本电脑。
*   **软件工具**：使用 **Storm** 概率模型检查器计算离线策略，使用 **Gurobi** 求解在线 MIQP 优化问题。
*   **算力说明**：文中未提及 GPU 使用，主要依赖 CPU 进行数值优化。离线抽象时间从几秒到十几分钟不等（取决于状态空间规模），在线 MPC 每步求解时间在 0.18s 到 7s 之间。

#### 5. 实验数量与充分性
*   **实验规模**：针对每个基准场景，论文设置了不同的输入球体半径 $\epsilon$ 和预测时界 $N$。
*   **重复性**：每个配置下运行了 **100 次随机模拟**，并使用相同的噪声序列进行公平对比。
*   **充分性评价**：实验涵盖了从线性到非线性的多种动力学模型，并详细分析了性能提升与安全概率下降之间的权衡关系（Trade-off），实验设计较为客观、充分。

#### 6. 主要结论与发现
*   **性能显著提升**：相比单策略抽象，该方法在“登山车”场景中降低了高达 **52.8%** 的总成本，在“Dubins 车”中显著降低了控制努力（Control Effort）。
*   **“拐点”效应（Elbow Effect）**：增加输入集合的大小（$\epsilon$）起初只会导致安全概率 $\lambda$ 轻微下降，但超过某个临界值后，$\lambda$ 会剧烈下降。这为系统设计者提供了寻找最优平衡点的依据。
*   **实时性可行**：尽管 MIQP 复杂度较高，但在中等规模的分区下，在线计算时间仍能满足准实时要求。

#### 7. 优点（亮点）
*   **理论完备性**：通过扩展仿真关系（PASR），在理论上证明了在线优化不会破坏离线给出的形式化保证。
*   **灵活性**：将 MPC 的灵活性引入了原本僵化的形式化方法中，使得系统能够应对运行时的性能需求。
*   **鲁棒性**：即使 MPC 模型近似存在误差，只要输入在允许集合内，安全性依然有保障。

#### 8. 不足与局限
*   **计算扩展性**：MIQP 的求解复杂度随状态空间分区数量呈指数级增长，对于高维系统，构建全局 MIQP 可能变得不可行。
*   **参数敏感性**：输入球体半径 $\epsilon$ 的选择目前依赖于离线实验观察（寻找拐点），缺乏自动化的自适应调整机制。
*   **模型近似误差**：MPC 使用的 PWA 近似可能导致在某些极端非线性区域的优化效果不佳。

（完）
