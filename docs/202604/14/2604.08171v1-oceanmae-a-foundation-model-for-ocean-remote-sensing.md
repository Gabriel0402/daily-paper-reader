---
title: "OceanMAE: A Foundation Model for Ocean Remote Sensing"
title_zh: OceanMAE：一种海洋遥感基础模型
authors: "Viola-Joanna Stamer, Panagiotis Agrafiotis, Behnood Rasti, Begüm Demir"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08171v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用多光谱观测的海洋遥感基础模型
tldr: 针对海洋遥感领域标注数据稀缺及陆地预训练模型迁移性差的问题，本文提出了OceanMAE基础模型。该模型通过掩码自编码器框架，将Sentinel-2多光谱数据与物理海洋描述符相结合进行自监督预训练。实验证明，OceanMAE在海洋污染物分割和水深估计等任务中表现优异，展示了领域特定预训练在海洋遥感中的重要价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08171v1/fig-001.webp\", \"caption\": \"TABLE I BATHYMETRY REGRESSION RESULTS ON MAGICBATHYNET. LOWER IS BETTER.\", \"page\": 4, \"index\": 1, \"width\": 508, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.08171v1/fig-002.webp\", \"caption\": \"Fig. 2. Architecture of the modified UNet for downstream ocean tasks. The model follows an encoder-decoder design with skip connections to preserve spatial detail. The OceanMAE embedding is projected and fused at the bottleneck, enabling the downstream network to exploit pre-trained oceanaware representations for both dense and sparse prediction tasks.\", \"page\": 3, \"index\": 2, \"width\": 458, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.08171v1/fig-003.webp\", \"caption\": \"Fig. 1. Overview of the OceanMAE architecture adapted from [14]. Input patches are masked with a ratio of 90%. The visible patches are processed by the encoder and combined with learnable mask tokens to reconstruct the full image. In parallel, external ocean features are projected and concatenated with the [CLS] token to form a multimodal representation used during reconstruction.\", \"page\": 2, \"index\": 3, \"width\": 500, \"height\": 275}]"
motivation: 现有的地球观测预训练模型多基于陆地数据，难以有效迁移至海洋遥感任务，且海洋领域缺乏大规模标注数据。
method: 提出一种改进的掩码自编码器OceanMAE，在自监督学习中整合了多光谱观测数据与具有物理意义的海洋描述符。
result: 在MADOS和MARIDA数据集上的海洋分割任务中取得显著提升，并在MagicBathyNet水深回归任务中表现出竞争力。
conclusion: 结合物理信息和领域对齐的自监督预训练能显著增强海洋遥感模型在下游任务中的表征能力。
---

## 摘要
准确的海洋制图对于水深估计、海底特征描述、海洋垃圾检测和生态系统监测等应用至关重要。然而，海洋遥感（RS）仍受限于有限的标注数据，以及主要在以陆地为主的地球观测图像上预训练的模型迁移能力较弱的问题。在本文中，我们提出了 OceanMAE，这是一种针对海洋特定任务的掩码自编码器（MAE），它通过在自监督学习过程中将 Sentinel-2 多光谱观测数据与具有物理意义的海洋描述符相结合，扩展了标准的 MAE 预训练。通过整合这些辅助海洋特征，OceanMAE 旨在从大规模无标注数据中学习更具信息量且具备海洋感知能力的潜在表示。为了将这些表示迁移到下游应用，我们进一步采用了一个改进的基于 UNet 的框架，用于海洋分割和水深估计。OceanMAE 在 Hydro 数据集上进行预训练，并在 MADOS 和 MARIDA 数据集上进行了海洋污染物和碎片分割评估，在 MagicBathyNet 数据集上进行了水深回归评估。实验结果表明，OceanMAE 在海洋分割方面取得了最显著的提升，而在水深测量方面的收益具有竞争力且取决于具体任务。此外，在 MARIDA 数据集上针对标准 MAE 进行的消融实验表明，在预训练期间加入辅助海洋描述符可以提高下游分割质量。这些发现强调了物理信息引导和领域对齐的自监督预训练对于海洋遥感的重要性。代码和权重可在 https://git.tu-berlin.de/joanna.stamer/SSLORS2 公开获取。

## Abstract
Accurate ocean mapping is essential for applications such as bathymetry estimation, seabed characterization, marine litter detection, and ecosystem monitoring. However, ocean remote sensing (RS) remains constrained by limited labeled data and by the reduced transferability of models pre-trained mainly on land-dominated Earth observation imagery. In this paper, we propose OceanMAE, an ocean-specific masked autoencoder that extends standard MAE pre-training by integrating multispectral Sentinel-2 observations with physically meaningful ocean descriptors during self-supervised learning. By incorporating these auxiliary ocean features, OceanMAE is designed to learn more informative and ocean-aware latent representations from large- scale unlabeled data. To transfer these representations to downstream applications, we further employ a modified UNet-based framework for marine segmentation and bathymetry estimation. Pre-trained on the Hydro dataset, OceanMAE is evaluated on MADOS and MARIDA for marine pollutant and debris segmentation, and on MagicBathyNet for bathymetry regression. The experiments show that OceanMAE yields the strongest gains on marine segmentation, while bathymetry benefits are competitive and task-dependent. In addition, an ablation against a standard MAE on MARIDA indicates that incorporating auxiliary ocean descriptors during pre-training improves downstream segmentation quality. These findings highlight the value of physically informed and domain-aligned self-supervised pre- training for ocean RS. Code and weights are publicly available at https://git.tu-berlin.de/joanna.stamer/SSLORS2.

---

## 论文详细总结（自动生成）

以下是对论文《OceanMAE: A Foundation Model for Ocean Remote Sensing》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的遥感基础模型（Foundation Models）大多在以陆地为主的数据集上进行预训练，难以捕捉海洋环境特有的动态现象（如波浪、太阳闪光、季节性水体变化）。此外，海洋遥感领域面临标注数据极度匮乏、人工标注成本高昂的挑战。
*   **研究动机**：利用自监督学习（SSL）在大规模无标注海洋数据上构建领域特定的基础模型，通过整合物理海洋描述符（如叶绿素含量、水深等）来增强模型对海洋特征的表征能力。

### 2. 方法论
*   **核心思想**：提出 **OceanMAE**，一种改进的掩码自编码器（MAE），在预训练阶段不仅重建掩码图像，还融合了外部物理海洋特征。
*   **关键技术细节**：
    *   **预训练阶段（OceanMAE）**：采用 ViT-Base 架构，对 Sentinel-2 图像进行 90% 的高比例掩码。创新点在于引入了辅助海洋特征 $F_{ocean}$（包括水深、叶绿素水平、透明度等），将其线性投影后与编码器的 [CLS] 标记拼接，共同输入解码器进行图像重建。
    *   **微调阶段（改进的 UNet）**：设计了一个带有“并行嵌入流”的 UNet/Bathy-UNet 架构。预训练的 OceanMAE 提取的全局特征（Embedding）通过线性投影和卷积处理后，在 UNet 的瓶颈处（Bottleneck）与卷积编码器提取的局部空间特征进行融合。
    *   **初始化策略**：研究了随机初始化、冻结嵌入（FE）和全模型微调（FF）三种策略对下游任务的影响。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 Hydro 数据集，包含 10 万张全球范围内的 Sentinel-2 海洋与海岸带切片。
    *   **下游任务**：
        1.  **MARIDA**：多光谱海洋碎片与水面特征分割。
        2.  **MADOS**：海洋污染物（溢油、碎片等）分割。
        3.  **MagicBathyNet**：浅水水深回归（包含 Agia Napa 和 Puck Lagoon 两个区域）。
*   **对比方法（Benchmark）**：对比了多种主流遥感自监督模型，包括 **SatMAE**、**FGMAE** 和 **SSL4EO**，以及任务特定的基准模型（如 MariNeXt）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100-SXM4-80GB** GPU。
*   **训练时长**：OceanMAE 的预训练总耗时约为 **15 至 17.5 小时**。
*   **超参数**：预训练 100 个 epoch，batch size 为 64，输入尺寸 224×224，patch size 为 16×16。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了回归（水深）和分割（污染物、碎片）两大类任务，并在三个权威基准数据集上进行了验证。
*   **消融实验**：
    *   对比了“标准 MAE”与“加入物理描述符的 OceanMAE”，验证了物理信息的增益。
    *   进行了敏感性分析，探讨了不同 Patch Size（16 vs 4）和初始化策略（FE vs FF）的影响。
*   **评价**：实验设计较为全面，对比了当前最先进的（SOTA）遥感基础模型，客观地展示了 OceanMAE 在不同任务下的优劣。

### 6. 主要结论与发现
*   **分割任务表现优异**：在 MADOS 数据集上，OceanMAE (FE) 的 mIoU 达到 70.8%，比之前的 SOTA 模型 MariNeXt 提升了 6.5 个百分点。
*   **物理信息有效性**：消融实验证明，在预训练中加入物理海洋描述符能显著提升下游分割任务的像素准确率（PA）和 Macro F1 分数。
*   **水深回归的复杂性**：在水深估计任务中，OceanMAE 表现出竞争力，但提升幅度受数据集规模和区域特性影响，表现出较强的任务依赖性。
*   **策略选择**：对于稀疏目标分割，冻结嵌入（FE）往往优于全微调；而对于数据量极小的任务，预训练模型的增益最为明显。

### 7. 优点
*   **领域对齐**：针对海洋遥感特性定制，弥补了通用遥感模型在水体表征上的不足。
*   **多模态融合**：巧妙地在自监督框架中整合了物理参数，使模型学习到的特征更具物理意义。
*   **架构灵活**：改进的 UNet 瓶颈融合机制能够有效结合全局上下文和局部细节。

### 8. 不足与局限
*   **水深回归提升有限**：在某些特定区域（如 Puck Lagoon），其绝对误差（MAE）并未超越专门设计的基准模型，说明模型在处理复杂海底地形时仍有提升空间。
*   **数据源单一**：目前主要基于 Sentinel-2 数据，尚未验证在更高分辨率或合成孔径雷达（SAR）数据上的泛化能力。
*   **物理特征依赖**：预训练依赖于外部海洋物理参数，在缺乏这些辅助数据的场景下，模型的优势可能会减弱。

（完）
