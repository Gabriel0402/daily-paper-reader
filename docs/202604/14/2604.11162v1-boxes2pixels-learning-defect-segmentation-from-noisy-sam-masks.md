---
title: "Boxes2Pixels: Learning Defect Segmentation from Noisy SAM Masks"
title_zh: Boxes2Pixels：从噪声 SAM 掩码中学习缺陷分割
authors: "Camile Lendering, Erkut Akdag, Egor Bondarev"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11162v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 从噪声 SAM 掩码中学习缺陷分割
tldr: 针对工业缺陷检测中像素级标注匮乏的问题，本文提出Boxes2Pixels框架。该方法不直接将SAM生成的伪掩码视为真值，而是将其作为噪声教师进行蒸馏。通过结合DINOv2特征、辅助定位头及在线自纠正机制，有效解决了伪标签中的背景幻觉和漏检问题。在风力涡轮机基准测试中，该模型在显著提升分割精度的同时，大幅减少了参数量，为弱监督工业检测提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11162v1/fig-001.webp\", \"caption\": \"Figure 1. Overview of the proposed framework, Boxes2Pixels, for sparse defect segmentation. From left to right, top to bottom: (i) an input wind turbine image, (ii) legacy YOLO bounding boxes used as coarse supervision, (iii) SAM-generated pseudo-masks exhibiting both spurious regions and missed defects, and (iv) prediction from Boxes2Pixels. Colors indicate correct detections (green), defects recovered by the student but missed by the teacher (blue), and omitted or erroneous regions (red).\", \"page\": 1, \"index\": 1, \"width\": 483, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2604.11162v1/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison on wind turbine blade inspection images. From left to right: YOLO bounding boxes used for supervision, SAM-generated pseudo-masks, and predictions of the proposed Boxes2Pixels\", \"page\": 8, \"index\": 2, \"width\": 521, \"height\": 868}, {\"url\": \"assets/figures/arxiv/2604.11162v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of the hierarchical student architecture. The global semantic branch employs DINOv2 ViT-S/14 with BitFit adaptation to extract intermediate features from layers {7, 4, 2, 1}. Projected features F (l) are fused top-down through residual fusion blocks to produce Fglobal. In parallel, a lightweight CNN detail branch extracts local features at the same resolution, yielding Flocal. Both branches are concatenated and processed by a feature mixer to obtain Ffusion, followed by a Binary Defect Head and a Fine-grained Semantic Head for (K+1)-class segmentation. Training relies on offline SAM pseudo-masks generated from bounding boxes with objectives designed to mitigate systematic teacher errors.\", \"page\": 4, \"index\": 3, \"width\": 1032, \"height\": 425}]"
motivation: 工业缺陷检测中缺乏像素级标注，而使用SAM将边界框转换为伪标签时会产生严重的背景噪声和漏检。
method: 提出一种噪声鲁棒的蒸馏框架，利用DINOv2特征、辅助定位头和单侧在线自纠正机制来优化从SAM获取的伪标签。
result: "在风力涡轮机检测任务中，该方法显著提升了mIoU和召回率，且比基准模型减少了80%的可训练参数。"
conclusion: Boxes2Pixels证明了通过合理的自纠正和特征提取机制，可以从带噪声的弱监督信号中学习到高精度的缺陷分割模型。
---

## 摘要
精确的缺陷分割对于工业检测至关重要，但密集的像素级标注往往难以获得。一种常见的权宜之计是利用 Segment Anything Model (SAM) 等基础分割模型将低成本的边界框转换为伪掩码。然而，这些伪标签在工业表面上存在系统性噪声，经常将背景结构误认为缺陷（产生幻觉），同时遗漏稀疏的缺陷。针对这一局限性，本文提出了一种具有噪声鲁棒性的“框到像素”蒸馏框架 Boxes2Pixels，该框架将 SAM 视为有噪声的教师，而非地面真值（ground-truth）监督的来源。边界框由 SAM 离线转换为伪掩码，并训练一个紧凑的学生模型，该模型包含：(i) 基于冻结 DINOv2 特征的层次化解码器以确保语义稳定性；(ii) 辅助二值定位头，用于将稀疏前景发现与类别预测解耦；(iii) 单侧在线自校正机制，在学生模型置信度较高时放宽背景监督，旨在解决教师模型的漏报（false negatives）问题。在手动标注的风力涡轮机检测基准上，在相同的弱监督条件下，所提出的 Boxes2Pixels 相比最强的基准模型，其异常 mIoU 提升了 6.97，二值 IoU 提升了 9.71。此外，在线自校正使二值召回率提高了 18.56，同时模型的可训练参数减少了 80%。代码可在 https://github.com/CLendering/Boxes2Pixels 获取。

## Abstract
Accurate defect segmentation is critical for industrial inspection, yet dense pixel-level annotations are rarely available. A common workaround is to convert inexpensive bounding boxes into pseudo-masks using foundation segmentation models such as the Segment Anything Model (SAM). However, these pseudo-labels are systematically noisy on industrial surfaces, often hallucinating background structure while missing sparse defects.   To address this limitation, a noise-robust box-to-pixel distillation framework, Boxes2Pixels, is proposed that treats SAM as a noisy teacher rather than a source of ground-truth supervision. Bounding boxes are converted into pseudo-masks offline by SAM, and a compact student is trained with (i) a hierarchical decoder over frozen DINOv2 features for semantic stability, (ii) an auxiliary binary localization head to decouple sparse foreground discovery from class prediction, and (iii) a one-sided online self-correction mechanism that relaxes background supervision when the student is confident, targeting teacher false negatives.   On a manually annotated wind turbine inspection benchmark, the proposed Boxes2Pixels improves anomaly mIoU by +6.97 and binary IoU by +9.71 over the strongest baseline trained under identical weak supervision. Moreover, online self-correction increases the binary recall by +18.56, while the model employs 80\% fewer trainable parameters. Code is available at https://github.com/CLendering/Boxes2Pixels.

---

## 论文详细总结（自动生成）

### 论文总结：Boxes2Pixels —— 从噪声 SAM 掩码中学习缺陷分割

#### 1. 核心问题与整体含义（研究动机和背景）
在工业视觉检测（如风力涡轮机叶片检查）中，精确的像素级缺陷分割对于评估损伤程度至关重要。然而，获取高质量的像素级标注成本极高且需要领域专家知识。
*   **现状与挑战**：目前常用的做法是利用 Segment Anything Model (SAM) 将廉价的边界框（Bounding Box）转换为伪掩码（Pseudo-masks）。但 SAM 在工业场景下表现不佳，容易产生“背景幻觉”（将阴影、纹理误判为缺陷）或“漏检”（遗漏细长、低对比度的裂纹）。
*   **核心目标**：提出一种名为 **Boxes2Pixels** 的蒸馏框架，将 SAM 视为一个“有噪声的教师”而非绝对真值，通过鲁棒的学习机制，从带有噪声的弱监督信号中训练出高性能、轻量化的学生分割模型。

#### 2. 方法论：核心思想与关键技术
Boxes2Pixels 的核心思想是利用自监督预训练特征的稳定性来抵御伪标签噪声，并通过自纠正机制修复教师模型的错误。

*   **层次化学生架构**：
    *   **全局语义分支**：采用冻结的 **DINOv2 (ViT-S/14)** 作为骨干网络。为了适应特定领域，使用了 **BitFit** 策略（仅微调 Bias 和 Norm 层），这能保持语义稳定性并防止模型过拟合伪标签中的高频噪声。
    *   **局部细节分支**：一个轻量级的 CNN 分支，用于提取高分辨率的局部特征，弥补 Transformer 补丁化（Patch-based）处理导致的细小缺陷信息丢失。
*   **双头预测设计**：
    *   **二值缺陷头**：专门负责定位缺陷（前景 vs 背景），使用**不对称 Dice Loss**，通过降低对假阳性（FP）的惩罚来提升召回率。
    *   **细粒度语义头**：负责具体的缺陷分类（如损伤、污垢）。
*   **在线自纠正机制（Online Self-Correction）**：
    *   这是解决教师漏检的关键。当 SAM 伪标签标注为“背景”，但学生模型以极高置信度（阈值 $\tau=0.9$）预测为“缺陷”时，系统会暂时放宽背景监督，允许学生模型“覆盖”教师的错误标签。

#### 3. 实验设计
*   **数据集**：使用公开的 **DTU 风力涡轮机检测数据集**，包含约 13,000 张真实户外环境下的 RGB 图像。
*   **Benchmark 与对比方法**：
    *   训练阶段仅使用 YOLO 格式的边界框。
    *   对比了主流的分割架构：**U-Net**、**DeepLabV3-B2** 和 **SegFormer-B2**。
    *   评估基准：为了保证定量评价的准确性，研究者手动标注了一组测试集的像素级真值（Ground Truth）。

#### 4. 资源与算力
*   **硬件支持**：论文在推理性能测试中提到了使用 **NVIDIA H100 GPU**。
*   **训练细节**：使用 AdamW 优化器，采用余弦学习率调度（初始 $5 \times 10^{-4}$），并应用了指数移动平均（EMA）来稳定训练。
*   **未明确信息**：文中未明确给出训练总时长、具体的 GPU 数量以及完整的训练能耗数据。

#### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的定量对比实验、详细的消融实验（验证局部细节分支、二值头、自纠正机制的有效性）以及效率分析。
*   **客观性与公平性**：所有对比模型均在相同的弱监督（SAM 伪标签）条件下训练。通过手动标注的测试集进行最终评估，避免了“用噪声评估噪声”的逻辑陷阱，实验设计较为严谨、客观。

#### 6. 主要结论与发现
*   **性能提升**：Boxes2Pixels 在异常 mIoU 上比最强基准（SegFormer）提升了 **6.97%**，二值 IoU 提升了 **9.71%**。
*   **召回率突破**：通过自纠正机制，二值召回率（Binary Recall）大幅提升了 **18.56%**，证明了该方法能有效找回被教师模型遗漏的微小缺陷。
*   **极高效率**：模型的可训练参数量减少了 **80%**（仅 5.6M），但在 H100 上实现了 **161.4 FPS** 的超高速推理，远超对比方法。

#### 7. 优点（亮点）
*   **数据驱动的鲁棒性**：不迷信基础模型（SAM），而是将其视为噪声源进行处理，更符合工业实际。
*   **参数高效适配**：BitFit 的应用证明了在工业小样本/噪声场景下，限制可训练参数量反而能获得更好的泛化性能。
*   **端到端实用性**：推理时不再需要昂贵的 SAM 或 DINOv2 全量计算，适合实时工业部署。

#### 8. 不足与局限
*   **场景单一性**：实验主要集中在风力涡轮机叶片这一特定工业场景，尚未在其他类型的工业缺陷（如金属表面、织物等）上验证泛化能力。
*   **自纠正风险**：虽然设置了高置信度阈值，但在极端复杂的背景下，自纠正机制仍存在诱发语义漂移（Semantic Drift）的潜在风险。
*   **依赖预训练质量**：模型高度依赖 DINOv2 的特征表达能力，若工业图像与 DINOv2 的预训练分布差异过大，性能可能会受限。

（完）
