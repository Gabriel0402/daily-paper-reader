---
title: Bootstrapping Video Semantic Segmentation Model via Distillation-assisted Test-Time Adaptation
title_zh: 通过蒸馏辅助的测试时自适应自举视频语义分割模型
authors: "Jihun Kim, Hoyong Kwon, Hyeokjun Kweon, Kuk-Jin Yoon"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.10950v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用SAM2在视频语义分割中进行时序分割蒸馏
tldr: 针对全监督视频语义分割（VSS）依赖大量标注数据的问题，本文提出DiTTA框架。该方法无需视频标注，通过测试时自适应（TTA）将预训练图像分割模型（ISS）转化为具备时序感知能力的VSS模型。DiTTA在初始化阶段将SAM2的时序分割知识蒸馏至ISS模型，并结合轻量化时序融合模块。实验证明，DiTTA在仅使用少量视频片段进行自适应的情况下，性能优于零样本精细化方法，在VSPW和Cityscapes上达到了与全监督方法相当的水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10950v1/fig-001.webp\", \"caption\": \"Figure 1. VSS using ISS model and SAM2. I and S are frames and their semantic segmentation results, respectively. In (C), DiTTA adapts the ISS model to the VSS model at test time by distilling temporal knowledge from SAM2 over a few initial frames. The pink arrow denotes an temporal fusion add-on.\", \"page\": 1, \"index\": 1, \"width\": 491, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2604.10950v1/fig-002.webp\", \"caption\": \"Table 1. Performance comparison of ISS, VSS, ISS+TTA, zero-shot refinement, and DiTTA under the W2F protocol. DiTTA does not involve SAM2 inference during evaluation, unlike zero-shot refinement.\", \"page\": 6, \"index\": 2, \"width\": 966, \"height\": 744}, {\"url\": \"assets/figures/arxiv/2604.10950v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of our DiTTA (Distillation-assisted Test-Time Adaptation) framework. It comprises a lightweight add-on for temporal fusion (left blue box) and a mask-based contrastive alignment (right green block).\", \"page\": 4, \"index\": 3, \"width\": 1025, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.10950v1/fig-004.webp\", \"caption\": \"Figure 3. Qualitative comparison of VSS results across various methods under W2F protocol. Only the initial 10% of frames are used for distillation, with evaluation on the remaining. The frames shown are entirely unseen.\", \"page\": 5, \"index\": 4, \"width\": 931, \"height\": 383}]"
motivation: 旨在解决全监督视频语义分割标注成本高昂以及现有图像模型在处理视频时缺乏时序一致性的挑战。
method: 提出一种蒸馏辅助的测试时自适应框架，将SAM2的时序知识迁移至图像分割模型，并引入轻量化模块进行跨帧上下文融合。
result: 在VSPW和Cityscapes数据集上，DiTTA仅需少量视频片段即可实现优于零样本方法的性能，甚至媲美全监督模型。
conclusion: DiTTA为现实世界中的视频语义分割任务提供了一种高效、无需标注且具备强泛化能力的实用解决方案。
---

## 摘要
全监督视频语义分割（VSS）严重依赖密集标注的视频数据，这限制了其实际应用。另一种选择是逐帧应用预训练的图像语义分割（ISS）模型，虽然避免了标注成本，但忽略了关键的时间相干性。近期如 SAM2 等基础模型虽然能实现高质量的掩码传播，但由于语义理解有限且计算开销大，直接用于 VSS 仍不现实。本文提出了 DiTTA（蒸馏辅助的测试时自适应），这是一个通过高效的测试时自适应（TTA）在无需标注视频的情况下将 ISS 模型转换为时间感知型 VSS 模型的新颖框架。DiTTA 在简短的单次初始化阶段将 SAM2 的时间分割知识蒸馏到 ISS 模型中，并配合轻量级时间融合模块来聚合跨帧上下文。关键在于，即使仅利用极有限的部分视频片段（如前 10%）进行自适应，DiTTA 也能实现鲁棒的泛化，显著优于推理时需反复调用 SAM2 的零样本细化方法。在 VSPW 和 Cityscapes 上的广泛实验证明了 DiTTA 的有效性，其性能可与全监督 VSS 方法相媲美甚至更优，为现实世界的 VSS 任务提供了一种实用且无需标注的解决方案。

## Abstract
Fully supervised Video Semantic Segmentation (VSS) relies heavily on densely annotated video data, limiting practical applicability. Alternatively, applying pre-trained Image Semantic Segmentation (ISS) models frame-by-frame avoids annotation costs but ignores crucial temporal coherence. Recent foundation models such as SAM2 enable high-quality mask propagation yet remain impractical for direct VSS due to limited semantic understanding and computational overhead. In this paper, we propose DiTTA (Distillation-assisted Test-Time Adaptation), a novel framework that converts an ISS model into a temporally-aware VSS model through efficient test-time adaptation (TTA), without annotated videos. DiTTA distills SAM2's temporal segmentation knowledge into the ISS model during a brief, single-pass initialization phase, complemented by a lightweight temporal fusion module to aggregate cross-frame context. Crucially, DiTTA achieves robust generalization even when adapting with highly limited partial video snippets (e.g., initial 10%), significantly outperforming zero-shot refinement approaches that repeatedly invoke SAM2 during inference. Extensive experiments on VSPW and Cityscapes demonstrate DiTTA's effectiveness, achieving competitive or superior performance relative to fully-supervised VSS methods, thus providing a practical and annotation-free solution for real-world VSS tasks.

---

## 论文详细总结（自动生成）

### 论文总结：通过蒸馏辅助的测试时自适应自举视频语义分割模型 (DiTTA)

#### 1. 核心问题与研究动机
*   **核心问题**：全监督视频语义分割（VSS）依赖极其昂贵的逐帧像素级标注。虽然可以直接使用图像语义分割（ISS）模型逐帧推理，但由于缺乏时序一致性，会导致预测结果在帧间剧烈闪烁或在遮挡时失效。
*   **研究动机**：
    *   **标注成本**：视频标注成本远高于静态图像。
    *   **现有模型局限**：SAM2 等基础模型虽有时序传播能力，但缺乏细粒度语义类别知识，且推理开销巨大，不适合直接作为 VSS 模型。
    *   **目标**：在**无需任何视频标注**的情况下，通过测试时自适应（TTA），将现有的 ISS 模型转化为具备时序感知能力的 VSS 模型。

#### 2. 方法论
DiTTA 框架的核心思想是在视频推理的初始阶段（Warm-up），利用 SAM2 作为“临时导师”，将其时序传播知识蒸馏给 ISS 模型。
*   **轻量化时序融合模块 (Temporal Fusion Add-on)**：在 ISS 模型中引入基于交叉注意力机制的插件，通过查询当前帧特征并键入前一帧特征，实现跨帧上下文聚合。
*   **蒸馏目标生成**：
    1.  利用 ISS 模型在初始帧生成高置信度采样点作为 Prompt。
    2.  调用 SAM2 进行双向掩码传播，生成时序一致的物体掩码。
    3.  结合 ISS 的语义预测和频率补偿机制，为这些掩码分配类别标签，形成伪标签。
*   **掩码对比对齐 (Mask-based Contrastive Alignment)**：采用动量编码器（Momentum Encoder）架构，通过对比损失函数强制同一物体在不同帧之间的特征表示保持一致。
*   **W2F 协议 (Warm-Up then Freeze)**：仅在视频前 X%（如 10%）的帧上进行自适应训练，随后冻结模型参数，对剩余帧进行纯推理，以保证部署效率。

#### 3. 实验设计
*   **数据集**：
    *   **VSPW**：大规模野外视频场景解析数据集（主要 Benchmark）。
    *   **Cityscapes**：用于验证跨域（Cross-dataset）泛化能力。
    *   **ADE20K**：用于验证在纯静态图像预训练模型上的有效性。
*   **对比方法**：
    *   **ISS 基准**：SegFormer, OCRNet。
    *   **全监督 VSS**：CFFM, CFFM++, TV3S 等。
    *   **现有 TTA 方法**：TENT, CoTTA, AuxAdapt。
    *   **零样本精细化**：直接使用 SAM2 进行后处理的方法。
*   **评价指标**：mIoU（精度）、wIoU、mVC（时序一致性）、FPS（推理速度）。

#### 4. 资源与算力
*   **硬件**：实验中提到使用单张 **NVIDIA GeForce RTX 3090 GPU** 进行推理速度（FPS）测试。
*   **训练细节**：在自适应阶段，每帧进行 5 次迭代更新，学习率为 0.001。
*   **算力开销**：DiTTA 仅在初始阶段调用一次 SAM2，推理阶段无需 SAM2，因此推理速度（约 13.45 FPS）远高于需要逐帧调用 SAM2 的方法（约 1.41 FPS）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括不同 warm-up 比例（10%, 25%, 50%）、全视频自适应、跨数据集验证（VSPW → Cityscapes）、以及从纯静态图像模型（ADE20K）开始的迁移实验。
*   **消融实验**：针对时序插件、蒸馏目标、对比损失三个核心组件分别进行了消融分析。
*   **充分性评价**：实验设计较为全面，涵盖了精度、一致性和效率的权衡，对比了从传统 TTA 到最新基础模型精细化的多种路径，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能卓越**：DiTTA 在仅使用 10% 视频片段进行自适应的情况下，mIoU 提升显著（+2.1%p），甚至超过了部分全监督 VSS 模型。
*   **时序一致性强**：在 mVC 指标上大幅领先 ISS 模型，证明了蒸馏 SAM2 知识能有效抑制帧间闪烁。
*   **高效实用**：相比于推理时依赖 SAM2 的方法，DiTTA 推理速度快 10 倍，且不依赖视频标注，具有极高的工程实践价值。

#### 7. 优点
*   **无需标注**：完全摆脱了对视频像素级标注的依赖。
*   **模型无关性**：可应用于 SegFormer、OCRNet 等多种主流分割架构。
*   **效率平衡**：通过 W2F 协议，在极短的自适应时间后即可获得高性能的视频专用模型，兼顾了精度与速度。
*   **知识融合**：巧妙地结合了 ISS 的语义能力和 SAM2 的时序传播能力。

#### 8. 不足与局限
*   **初始依赖**：自适应的效果高度依赖于初始帧 ISS 预测的质量，如果初始 Prompt 错误，可能会导致错误的知识蒸馏。
*   **Warm-up 开销**：虽然推理快，但在视频开始时仍需短暂的计算资源进行自适应训练，对于极短视频或实时性要求极高的瞬时任务可能存在延迟。
*   **场景覆盖**：对于物体类别极多且频繁进出画面的复杂长视频，仅靠初始段的自适应可能无法覆盖后期出现的新类别物体。

（完）
