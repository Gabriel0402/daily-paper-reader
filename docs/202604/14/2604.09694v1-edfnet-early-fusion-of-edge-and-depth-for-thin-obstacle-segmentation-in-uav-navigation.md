---
title: "EDFNet: Early Fusion of Edge and Depth for Thin-Obstacle Segmentation in UAV Navigation"
title_zh: EDFNet：边缘与深度的早期融合用于无人机导航中的细小障碍物分割
authors: Negar Fathi
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.09694v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 无人机导航中用于障碍物分割的多模态早期融合（RGB、深度、边缘）
tldr: 针对无人机导航中细长障碍物（如电线、树枝）难以识别的问题，本文提出EDFNet框架。该方法通过早期融合RGB、深度和边缘信息，增强了对低对比度和极小像素目标的感知能力。实验表明，EDFNet在保持实时性的同时，显著提升了边界敏感指标，为无人机避障提供了一个实用且模块化的多模态分割基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09694v1/fig-001.webp\", \"caption\": \"Fig. 1: Overview of the EDFNET framework.\", \"page\": 3, \"index\": 1, \"width\": 1022, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2604.09694v1/fig-002.webp\", \"caption\": \"TABLE II: Per-class IoU for all modality–backbone configurations on the DDOS test split. Higher values indicate better classspecific segmentation performance.\", \"page\": 6, \"index\": 2, \"width\": 1064, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.09694v1/fig-003.webp\", \"caption\": \"TABLE I: Quantitative results for all modality–backbone configurations on the DDOS test split. Higher values indicate better performance for mIoU, bIoU, Recall, FPS, and TSE, whereas lower values are better for FPR and latency.\", \"page\": 6, \"index\": 3, \"width\": 826, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2604.09694v1/fig-004.webp\", \"caption\": \"Fig. 2: Qualitative comparison between ground truth and predictions from five high-performing modality–backbone configurations on the DDOS test split.\", \"page\": 7, \"index\": 4, \"width\": 488, \"height\": 829}]"
motivation: 现有的分割方法难以有效识别无人机飞行路径中像素占比极低且视觉对比度弱的细长障碍物。
method: 提出EDFNet早期融合框架，将RGB、深度和边缘信息整合，并利用U-Net和DeepLabV3进行多模态特征提取。
result: 预训练的RGBDE U-Net在细长结构评估得分和平均交并比上表现最优，且运行速度达到19.62 FPS。
conclusion: 早期RGB-深度-边缘融合是提升细长障碍物分割性能的有效手段，但极细类别的识别仍是未来挑战。
---

## 摘要
自主无人机（UAV）必须可靠地检测电线、电线杆和树枝等细小障碍物，以便在现实环境中安全导航。由于这些结构占据的像素极少，通常表现出微弱的视觉对比度，并受到类别不平衡的严重影响，因此仍然难以感知。现有的分割方法主要针对较粗的障碍物，未能充分利用细小结构感知所需的互补多模态线索。我们提出了 EDFNet，这是一个模块化的早期融合分割框架，它集成了 RGB、深度和边缘信息，用于复杂空中场景中的细小障碍物感知。我们在无人机深度与障碍物分割（DDOS）数据集上，通过 U-Net 和 DeepLabV3 在预训练和非预训练设置下的 16 种模态-骨干网络配置对 EDFNet 进行了评估。结果表明，RGB-深度-边缘（RGB-Depth-Edge）早期融合提供了一个具有竞争力且均衡的基准，在边界敏感和召回导向的指标中提升最为一致。预训练的 RGBDE U-Net 实现了最佳的综合性能，具有最高的细小结构评估分数（0.244）、平均交并比（mIoU, 0.219）和边界交并比（boundary IoU, 0.234），同时在我们的评估硬件上保持了具有竞争力的运行性能（19.62 FPS）。然而，所有模型在最罕见的极细类别上的表现仍然较低，这表明可靠的极细结构分割仍然是一个开放性挑战。总体而言，这些研究结果将 RGB-深度-边缘早期融合定位为无人机导航中细小障碍物分割的一个实用且模块化的基准。

## Abstract
Autonomous Unmanned Aerial Vehicles (UAVs) must reliably detect thin obstacles such as wires, poles, and branches to navigate safely in real-world environments. These structures remain difficult to perceive because they occupy few pixels, often exhibit weak visual contrast, and are strongly affected by class imbalance. Existing segmentation methods primarily target coarser obstacles and do not fully exploit the complementary multimodal cues needed for thin-structure perception. We present EDFNet, a modular early-fusion segmentation framework that integrates RGB, depth, and edge information for thin-obstacle perception in cluttered aerial scenes. We evaluate EDFNet on the Drone Depth and Obstacle Segmentation (DDOS) dataset across sixteen modality-backbone configurations using U-Net and DeepLabV3 in pretrained and non-pretrained settings. The results show that early RGB-Depth-Edge fusion provides a competitive and well-balanced baseline, with the most consistent gains appearing in boundary-sensitive and recall-oriented metrics. The pretrained RGBDE U-Net achieves the best overall performance, with the highest Thin-Structure Evaluation Score (0.244), mean IoU (0.219), and boundary IoU (0.234), while maintaining competitive runtime performance (19.62 FPS) on our evaluation hardware. However, performance on the rarest ultra-thin categories remains low across all models, indicating that reliable ultra-thin segmentation is still an open challenge. Overall, these findings position early RGB-Depth-Edge fusion as a practical and modular baseline for thin-obstacle segmentation in UAV navigation.

---

## 论文详细总结（自动生成）

### 论文总结：EDFNet——边缘与深度的早期融合用于无人机导航中的细小障碍物分割

#### 1. 核心问题与整体含义（研究动机和背景）
无人机（UAV）在自主飞行中面临的最严峻挑战之一是感知**细小障碍物**（如电线、细树枝、电线杆）。这些物体在图像中占据的像素极少，视觉对比度微弱，且在数据集中存在严重的类别不平衡问题。传统的语义分割模型往往在下采样过程中丢失这些细微结构。本文的研究动机在于通过整合多模态信息（RGB、深度、边缘），利用它们在几何和结构上的互补性，提升无人机对这类安全关键目标的感知能力。

#### 2. 论文提出的方法论
论文提出了 **EDFNet (Edge-Depth Fusion Network)**，其核心思想是**早期融合（Early Fusion）**：
*   **多模态输入构建**：
    *   **RGB增强**：使用 CLAHE（对比度受限的自适应直方图均衡化）提升低对比度区域的可见性。
    *   **边缘提取**：利用 Sobel 算子从灰度图中提取水平和垂直梯度，生成归一化的边缘图。
    *   **深度处理**：对深度图进行逐帧归一化，减少传感器量程差异的影响。
*   **融合策略**：将 RGB（3通道）、深度（1通道）和边缘（1通道）在输入层进行通道拼接（Concatenation），形成 3、4 或 5 通道的输入张量。
*   **骨干网络**：支持 **U-Net**（侧重空间细节恢复）和 **DeepLabV3**（侧重多尺度上下文聚合），并分为随机初始化和 ImageNet 预训练两种模式。
*   **损失函数**：采用**类别加权交叉熵损失（Class-weighted Cross-entropy Loss）**，通过计算像素频率的倒数来增加稀有细小障碍物类别的权重。
*   **评价指标**：引入了自定义的**细小结构评估得分（TSE）**：
    $TSE = 0.45 \times bIoU + 0.30 \times Recall - 0.15 \times FPR + 0.10 \times mIoU$
    该指标通过加重边界 IoU 和召回率的权重，更客观地衡量安全敏感目标的检测效果。

#### 3. 实验设计
*   **数据集**：使用 **DDOS (Drone Depth and Obstacle Segmentation)** 数据集，包含对齐的 RGB、深度图及 10 类精细标注（包括电线、极细结构等）。
*   **Benchmark 设置**：
    *   **16 种配置**：4 种模态组合（RGB, RGBD, RGBE, RGBDE） $\times$ 2 种模型（U-Net, DeepLabV3） $\times$ 2 种初始化方式（Pretrained, Non-pretrained）。
*   **对比维度**：涵盖了平均交并比（mIoU）、边界交并比（bIoU）、召回率（Recall）、误报率（FPR）以及推理速度（FPS）。

#### 4. 资源与算力
*   论文提到了在“评估硬件（evaluation hardware）”上进行了推理速度测试，结果显示模型运行在 **17.59 至 20.81 FPS** 之间。
*   **具体算力说明缺失**：文中**未明确指出**具体的 GPU 型号（如 RTX 系列或嵌入式平台 Jetson 系列）、GPU 数量以及具体的训练总时长。

#### 5. 实验数量与充分性
*   **实验数量**：共进行了 16 组核心对比实验，并提供了详细的每类 IoU（Per-class IoU）分析。
*   **充分性与公平性**：
    *   实验设计涵盖了消融研究（通过增减 D 和 E 模态），验证了各模态的贡献。
    *   所有模型均在统一的 DDOS 官方划分集上训练和测试，保证了对比的客观性。
    *   **不足之处**：实验主要集中在单一数据集上，缺乏在其他无人机数据集（如 TTPLA）上的泛化性验证。

#### 6. 主要结论与发现
*   **最优配置**：**预训练的 RGBDE + U-Net** 表现最佳，TSE 达到 0.244，mIoU 为 0.219，bIoU 为 0.234。
*   **融合效果**：早期融合能显著提升边界敏感指标和召回率，相比单一 RGB 模态，多模态模型生成的障碍物区域更连贯，碎片化更少。
*   **瓶颈问题**：尽管引入了多模态，但对于**“极细（Ultra-thin）”**类别的 IoU 依然极低（最高仅 0.007），说明极细目标的可靠分割仍是未解决的挑战。
*   **实时性**：增加深度和边缘通道对推理延迟的影响较小，证明了早期融合在计算上的可行性。

#### 7. 优点
*   **模块化设计**：框架简单且易于扩展，只需修改第一层卷积即可适配不同的骨干网络。
*   **指标创新**：提出的 TSE 指标比传统的 mIoU 更符合无人机避障的实际安全需求。
*   **实用性强**：通过 CLAHE 和 Sobel 等传统图像处理技术增强输入，不增加复杂的注意力机制，易于在算力受限的无人机上部署。

#### 8. 不足与局限
*   **极细目标感知受限**：对于像素宽度极小的电线，模型仍存在严重的漏检和断裂现象。
*   **环境鲁棒性未知**：实验未考虑无人机实际飞行中的运动模糊、相机抖动及极端光照变化。
*   **硬件评估不完整**：缺乏在嵌入式边缘计算设备（如 NVIDIA Orin）上的功耗和性能分析，FPS 数据仅代表桌面端表现。
*   **深度图依赖**：模型性能高度依赖深度图的质量，而现实中细小目标的深度测量往往噪声极大或不可用。

（完）
