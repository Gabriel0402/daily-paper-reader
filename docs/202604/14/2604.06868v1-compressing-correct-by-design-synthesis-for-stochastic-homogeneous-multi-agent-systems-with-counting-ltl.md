---
title: Compressing Correct-by-Design Synthesis for Stochastic Homogeneous Multi-Agent Systems with Counting LTL
title_zh: 针对带有计数 LTL 的随机同质多智能体系统的压缩构造即正确合成
authors: "Xinyuan Qiu, Ruohan Wang, Siyuan Liu, Sofie Haesaert"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06868v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 具有计数线性时态逻辑规范的多智能体系统
tldr: 针对随机同构多智能体系统（MAS）在计数线性时序逻辑（cLTL）下的安全保障问题，本文提出了一种压缩合成框架。针对传统方法在智能体数量增加时面临的计算爆炸问题，研究利用确定性有限自动机（DFA）揭示了满意概率的结构化张量分解特性，并据此开发了双树值迭代算法。该方法显著减少了动态规划中的冗余计算，在处理大规模系统和复杂任务时表现出优异的扩展性和效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06868v1/fig-001.webp\", \"caption\": \"Fig. 1: The multi-agent tree Gm (top left) and single-agent tree Gs (top right) grown simultaneously via Alg. 1. As shown in \\\"Node Values per Agent\\\" (bottom), vertices sharing an identical value vector are indicated using the same color and are mapped to a unique vertex κ ∈ K in Gs via R.\", \"page\": 5, \"index\": 1, \"width\": 512, \"height\": 279}]"
motivation: 传统基于有限抽象的合成方法在处理大规模多智能体系统和复杂时序逻辑时，面临严重的计算瓶颈。
method: 利用DFA揭示满意概率的结构化张量分解特性，并提出一种基于双树的值迭代框架以减少动态规划中的冗余计算。
result: 数值实验证明，该方法在处理复杂规格和大规模多智能体系统时具有极高的计算效率和良好的可扩展性。
conclusion: 本研究通过张量分解和双树优化，为大规模同构随机多智能体系统的形式化安全合成提供了一种高效的解决方案。
---

## 摘要
构造即正确合成（Correct-by-design synthesis）为建立随机多智能体系统（MAS）的形式化安全保证提供了一个原则性框架。然而，随着智能体数量和时序逻辑规范复杂度的增加，基于有限抽象的传统方法往往会产生极高的计算成本。在这项工作中，我们研究了计数线性时序逻辑（cLTL）规范下的同质随机多智能体系统，并表明通过利用确定性有限自动机（DFA），相应的满足概率可以进行结构化张量分解。基于这一结构，我们开发了一个基于双树（dual-tree）的值迭代框架，减少了动态规划过程中的冗余计算。数值结果证明了所提方法在处理复杂规范和大规模多智能体系统时的有效性和可扩展性。

## Abstract
Correct-by-design synthesis provides a principled framework for establishing formal safety guarantees for stochastic multi-agent systems (MAS). However, conventional approaches based on finite abstractions often incur prohibitive computational costs as the number of agents and the complexity of temporal logic specifications increase. In this work, we study homogeneous stochastic MAS under counting linear temporal logic (cLTL) specifications, and show that the corresponding satisfaction probability admits a structured tensor decomposition via leveraging deterministic finite automata (DFA). Building on this structure, we develop a dual-tree-based value iteration framework that reduces redundant computation in the process of dynamic programming. Numerical results demonstrate the proposed approach's effectiveness and scalability for complex specifications and large-scale MAS.

---

## 论文详细总结（自动生成）

### 论文总结：针对带有计数 LTL 的随机同质多智能体系统的压缩构造即正确合成

#### 1. 核心问题与研究动机
*   **核心问题**：如何在保证形式化安全（Correct-by-design）的前提下，为大规模**随机同质多智能体系统（MAS）**合成满足**计数线性时序逻辑（cLTL）**规范的控制器。
*   **研究动机**：
    *   **维度灾难**：传统的基于有限抽象的合成方法在智能体数量增加时，状态空间呈指数级增长，导致计算和内存成本不可行。
    *   **表达能力限制**：标准 LTL 在描述大规模群体行为（如“至少有 $m$ 个智能体到达目标区”）时过于复杂，而 cLTL 能更简洁地表达这类不依赖个体身份的集体任务。
    *   **随机性挑战**：现有 cLTL 研究多集中在确定性系统，而实际应用中环境和动力学往往具有随机性。

#### 2. 方法论
该论文提出了一种基于**张量分解**和**双树结构（Dual-tree）**的近似值迭代框架：
*   **核心思想**：利用系统的同质性（所有智能体动力学相同）和解耦性，将全局满足概率分解为多个**秩-1（Rank-1）张量**的和。每个张量代表一条 DFA（确定性有限自动机）接受路径，且可进一步分解为各智能体独立概率的乘积。
*   **关键技术细节**：
    *   **双树结构**：
        1.  **多智能体树 ($G_m$)**：维护与 DFA 状态转换对应的见证序列（Witness sequences）。
        2.  **单智能体树 ($G_s$)**：仅存储和计算独特的单智能体概率向量。通过建立 $G_m$ 与 $G_s$ 之间的映射关系，避免了对具有相同行为模式的智能体进行重复计算。
    *   **BDD 压缩**：利用二元决策图（BDD）对 DFA 的转换条件进行符号化表示和捆绑，减少树的分支宽度。
    *   **树剪枝**：通过设定阈值移除概率贡献极低的叶节点，在保证概率下界的同时控制树的规模。
*   **算法流程**：交替进行树的扩展（Algorithm 1）和值迭代更新（Algorithm 2），并在此基础上进行策略优化，寻找最大化满足概率的解。

#### 3. 实验设计
*   **实验场景**：考虑一个一维随机同质 MAS，智能体具有随机扰动动力学。状态空间和动作空间均进行了离散化抽象。
*   **对比基准（Benchmark）**：
    *   **单体动态规划（Monolithic DP）**：直接在全状态空间上求解。
    *   **见证树方法（Witness-tree, [23]）**：该团队之前提出的针对 scLTL 的张量合成方法。
*   **测试规范**：设计了四种 cLTL 公式（$\mu_1$ 到 $\mu_4$），涵盖了“直到（Until）”、“最终（Eventually）”、“下一步（Next）”以及不同的计数阈值（如 $N/2, N/3$ 等）。

#### 4. 资源与算力
*   **硬件配置**：实验在一台配备 **13th Gen Intel Core i9-13900HX** 处理器和 **16.0 GB RAM** 的笔记本电脑上运行。
*   **算力说明**：论文未提及使用 GPU，所有计算均基于 CPU 完成。实验主要关注内存消耗和运行时间随智能体数量 $N$ 增加的变化趋势。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   对比实验：在 $\mu_1$ 规范下，对比了 $N=2$ 到 $N=8$ 的性能。
    *   扩展性实验：在 $\mu_2, \mu_3$ 规范下，测试了智能体数量从 3 增加到 18 的情况。
    *   复杂性实验：在 $\mu_4$ 规范下测试了更复杂的计数阈值组合。
*   **充分性评价**：实验设计较为充分，涵盖了从简单到复杂的多种逻辑规范，并直观展示了内存溢出（OOM）的临界点。对比实验客观地证明了新方法在处理大规模系统时的优越性。

#### 6. 主要结论与发现
*   **内存与时间优化**：双树结构显著减少了冗余计算。在 $N=8$ 时，内存占用比前代方法降低了近一个数量级。
*   **线性扩展性**：对于某些 cLTL 规范（如 $\mu_2, \mu_3$），内存和运行时间随智能体数量 $N$ 呈近似**线性增长**，成功缓解了维度灾难。
*   **复杂规范处理能力**：即使对于中间计数阈值（如 $N/2$）导致的组合爆炸问题，该方法通过 BDD 压缩仍能保持计算可行性。

#### 7. 优点
*   **高效性**：通过识别并消除同质智能体间的重复计算，极大地提升了合成效率。
*   **理论完备性**：提供了满足概率的下界保证，确保了合成结果的“构造即正确”。
*   **创新性**：这是首篇针对随机 MAS 处理 cLTL 规范的构造即正确合成论文。

#### 8. 不足与局限
*   **解耦假设**：该方法假设智能体之间的动力学是解耦的（即个体运动互不影响），这限制了其在具有强物理交互（如避障、编队控制）场景中的直接应用。
*   **DFA 规模风险**：虽然压缩了值迭代过程，但 cLTL 转换成的 DFA 状态数仍可能随规范复杂度的增加而剧增。
*   **同质性依赖**：方法高度依赖智能体的同质性，若系统中存在多种不同类型的智能体，计算优势将减弱。

（完）
