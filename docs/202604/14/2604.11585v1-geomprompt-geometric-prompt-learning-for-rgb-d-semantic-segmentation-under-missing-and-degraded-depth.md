---
title: "GeomPrompt: Geometric Prompt Learning for RGB-D Semantic Segmentation Under Missing and Degraded Depth"
title_zh: GeomPrompt：深度缺失和退化情况下的 RGB-D 语义分割几何提示学习
authors: "Krishna Jaganathan, Patricio Vela"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11585v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 针对机器人多模态感知的RGB-D语义分割，处理深度缺失问题
tldr: 针对机器人感知中RGB-D数据常面临深度信息缺失或退化的问题，本文提出GeomPrompt。这是一种轻量级跨模态适配模块，能仅从RGB图像合成任务驱动的几何提示，用于增强冻结的RGB-D分割模型。该方法无需深度监督，通过下游分割任务训练，在显著提升分割精度的同时保持了极低的推理延迟，为处理不完整多模态输入提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11585v1/fig-001.webp\", \"caption\": \"Figure 3. Degraded depth vs. our recovered prompt across severities and degradation types.\", \"page\": 7, \"index\": 1, \"width\": 1012, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2604.11585v1/fig-002.webp\", \"caption\": \"Figure 2. Model architecture diagrams. (a) Overview of the GeomPrompt architecture. (b) Depth handling in GeomPrompt-Recovery.\", \"page\": 4, \"index\": 2, \"width\": 1036, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2604.11585v1/fig-003.webp\", \"caption\": \"Figure 5. Qualitative comparison of segmentation outputs and geometric representations.\", \"page\": 8, \"index\": 3, \"width\": 489, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.11585v1/fig-004.webp\", \"caption\": \"Figure 7. GeomPrompt-Recovery under simulated depth failures.\", \"page\": 8, \"index\": 4, \"width\": 461, \"height\": 280}]"
motivation: 现有的RGB-D语义分割模型在深度信息缺失、噪声或损坏的实际场景下表现不佳，且传统的单目深度估计方法计算开销较大。
method: 提出GeomPrompt模块，通过RGB图像生成几何提示作为冻结分割模型的补充输入，并利用GeomPrompt-Recovery修正退化的深度数据，全程仅需分割任务监督。
result: 在SUN RGB-D数据集上，该方法比纯RGB推理提升了高达6.1 mIoU，且推理延迟仅为7.8ms，远优于传统的单目深度估计基准。
conclusion: 任务驱动的几何提示学习是一种高效的跨模态补偿机制，能有效提升RGB-D感知系统在深度输入不理想时的鲁棒性和性能。
---

## 摘要
机器人和具身智能的多模态感知系统通常假设具有可靠的 RGB-D 传感，但在实践中，深度信息经常缺失、存在噪声或损坏。因此，我们提出了 GeomPrompt，这是一个轻量级的跨模态适配模块，它仅从 RGB 图像中合成任务驱动的几何提示，作为冻结的 RGB-D 语义分割模型的第四通道，且无需深度监督。我们进一步引入了 GeomPrompt-Recovery，这是一个适配模块，通过预测与冻结分割器相关的第四通道修正，来补偿退化的深度信息。这两个模块仅使用下游分割任务的监督进行训练，从而能够恢复对分割有用的几何先验，而不是估计深度信号。在 SUN RGB-D 数据集上，GeomPrompt 在 DFormer 上比仅使用 RGB 的推理提高了 +6.1 mIoU，在 GeminiFusion 上提高了 +3.0 mIoU，同时与强大的单目深度估计器相比仍具竞争力。对于退化的深度信息，GeomPrompt-Recovery 持续提高了鲁棒性，在严重的深度损坏情况下获得了高达 +3.6 mIoU 的提升。GeomPrompt 的效率也显著高于单目深度基准模型，延迟仅为 7.8 ms，而后者分别为 38.3 ms 和 71.9 ms。这些结果表明，任务驱动的几何提示是在 RGB-D 感知中，针对深度输入缺失和退化进行跨模态补偿的一种有效机制。

## Abstract
Multimodal perception systems for robotics and embodied AI often assume reliable RGB-D sensing, but in practice, depth is frequently missing, noisy, or corrupted. We thus present GeomPrompt, a lightweight cross-modal adaptation module that synthesizes a task-driven geometric prompt from RGB alone for the fourth channel of a frozen RGB-D semantic segmentation model, without depth supervision. We further introduce GeomPrompt-Recovery, an adaptation module that compensates for degraded depth by predicting the fourth channel correction relevant for the frozen segmenter. Both modules are trained solely with downstream segmentation supervision, enabling recovery of the geometric prior useful for segmentation, rather than estimating depth signals. On SUN RGB-D, GeomPrompt improves over RGB-only inference by +6.1 mIoU on DFormer and +3.0 mIoU on GeminiFusion, while remaining competitive with strong monocular depth estimators. For degraded depth, GeomPrompt-Recovery consistently improves robustness, yielding gains up to +3.6 mIoU under severe depth corruptions. GeomPrompt is also substantially more efficient than monocular depth baselines, reaching 7.8 ms latency versus 38.3 ms and 71.9 ms. These results suggest that task-driven geometric prompting is an efficient mechanism for cross-modal compensation under missing and degraded depth inputs in RGB-D perception.

---

## 论文详细总结（自动生成）

这是一份关于论文《GeomPrompt: Geometric Prompt Learning for RGB-D Semantic Segmentation Under Missing and Degraded Depth》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在机器人和具身智能领域，RGB-D 语义分割依赖可靠的深度感知。然而，在实际部署中，深度传感器常因材质（如反射、透明表面）、环境光照或硬件限制，导致深度信息**缺失、多噪或损坏**。
*   **核心问题**：当预训练好的强力 RGB-D 分割模型在推理时遇到深度输入不可用或质量极差的情况，如何高效地补偿几何信息？
*   **整体含义**：论文提出了一种“几何提示（Geometric Prompt）”的学习机制，旨在不依赖显式深度监督的前提下，仅从 RGB 图像中合成对分割任务有用的几何信号，从而实现跨模态的鲁棒感知。

### 2. 方法论
*   **核心思想**：将深度估计问题重构为“任务驱动的提示生成”。通过一个轻量级模块生成“类深度”的信号（即提示），填充到冻结（Frozen）的 RGB-D 分割器的深度通道中。
*   **关键技术细节**：
    *   **GeomPrompt 架构**：采用 ViT-S/16 作为编码器提取 RGB 特征，配合轻量级 CNN 解码器生成低分辨率残差图。
    *   **抗锯齿上采样与参数化**：使用抗锯齿技术将残差图提升至全分辨率，并通过 `s · tanh(·)` 函数将其限制在有界空间内，中心化为中性灰（127.5），以符合分割器的输入分布。
    *   **PromptAdapter**：一个零初始化的轻量级残差卷积模块，用于学习特定分割器的微调修正。
    *   **GeomPrompt-Recovery**：针对深度退化场景，该变体通过双路径融合 RGB 和损坏的深度图，预测一个有界的残差修正值来修复原始深度。
*   **训练流程**：**完全冻结**下游 RGB-D 分割器，仅使用语义分割损失（如 OHEM Cross-Entropy）训练提示模块。引入全变分（TV）正则化和 L1 残差惩罚来保证生成信号的平滑性。

### 3. 实验设计
*   **数据集**：SUN RGB-D（标准的室内场景理解基准）。
*   **Benchmark 与对比方法**：
    *   **冻结主干**：DFormer-Base 和 GeminiFusion (MiT-B3)。
    *   **对比基准**：RGB-only（深度设为0）、Ground Truth 深度（性能上限）。
    *   **单目深度估计器**：Depth Anything V2 (DA2)、Metric3Dv2。
    *   **手工特征对照**：灰度图、Canny 边缘、Laplacian 边缘、Scharr 梯度等。
*   **测试场景**：深度完全缺失场景，以及模拟的量化、丢弃（Dropout）、高斯噪声等传感器故障场景。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A40 GPU** 进行训练。
*   **训练参数**：采用 AdamW 优化器，训练 300 个 epoch，有效 Batch Size 为 32。
*   **推理效率**：GeomPrompt 的延迟仅为 **7.8 ms**，参数量 23.4M，计算量 44.0 GFLOPs。

### 5. 实验数量与充分性
*   **实验规模**：论文在两种架构迥异的分割模型（层次化 Transformer vs. 像素级融合 Transformer）上验证了有效性。
*   **消融实验**：针对残差缩放课程学习、PromptAdapter、低通投影、正则化项以及编码器是否冻结等关键变量进行了 6 组对比实验。
*   **鲁棒性测试**：在 3 种主要退化类型、9 种不同严重程度（0.1-0.9）下进行了详尽的性能曲线分析。
*   **充分性评价**：实验设计客观且公平，通过与 SOTA 单目深度估计模型的对比，证明了该方法在保持高性能的同时具有极高的计算效率。

### 6. 主要结论与发现
*   **性能显著提升**：在深度缺失时，GeomPrompt 使 DFormer 的 mIoU 提升了 **+6.1**，GeminiFusion 提升了 **+3.0**。
*   **任务驱动优于通用重建**：仅靠分割监督生成的几何提示，在分割任务上的表现优于或等同于一些专门训练的单目深度估计模型。
*   **高效性**：推理速度比单目深度估计基准快 **5 到 9 倍**，非常适合实时机器人系统。
*   **鲁棒性**：GeomPrompt-Recovery 在严重噪声下能带来高达 +3.6 mIoU 的增益，有效缓冲了传感器失效带来的性能崩溃。

### 7. 优点
*   **轻量化设计**：极低的推理延迟和计算开销，解决了传统深度估计模型难以在嵌入式端实时运行的问题。
*   **无需深度标注**：完全依赖现有的分割标注进行训练，降低了数据获取成本。
*   **即插即用**：无需修改或重新训练复杂的多模态分割主干，具有良好的适配性。
*   **参数化创新**：通过有界残差和抗锯齿设计，确保了生成的提示信号稳定且符合物理直觉。

### 8. 不足与局限
*   **场景覆盖**：实验主要集中在室内场景，对于室外大尺度场景或极端天气下的表现尚待验证。
*   **跨任务泛化**：目前仅针对语义分割任务，尚未探讨该几何提示是否能直接辅助导航或抓取等下游操作任务。
*   **RGB 依赖性**：作为一种跨模态补偿方案，其性能上限受限于 RGB 图像的质量，在极低光照等 RGB 同样失效的场景下可能无法工作。

（完）
