---
title: "IMPACT-CYCLE: A Contract-Based Multi-Agent System for Claim-Level Supervisory Correction of Long-Video Semantic Memory"
title_zh: IMPACT-CYCLE：一种基于契约的多智能体系统，用于长视频语义记忆的断言级监督修正
authors: "Weitong Kong, Di Wen, Kunyu Peng, David Schneider, Zeyun Zhong, Alexander Jaus, Zdravko Marinov, Jiale Wei, Ruiping Liu, Junwei Zheng, Yufan Chen, Lei Qi, Rainer Stiefelhagen"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20136v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 使用时态逻辑进行长视频理解的多智能体系统
tldr: 针对长视频理解中错误修正成本高昂且缺乏中间状态的问题，本文提出IMPACT-CYCLE系统。该系统采用基于合同的多智能体架构，将视频理解转化为对共享语义记忆的声明级维护。通过结构化存储声明及其依赖关系，智能体能自动验证一致性，并在必要时引入人工仲裁。该方法显著降低了人工干预成本，并提升了下游推理任务的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20136v1/fig-001.webp\", \"caption\": \"Fig. 2. NASA-TLX workload comparison (lower is better).\", \"page\": 6, \"index\": 1, \"width\": 437, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2604.20136v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of IMPACT-CYCLE. Starting from keyframe-grounded scene graphs, the system decomposes graph elements into typed atomic claims and instantiates structured verification probes. Three verification agents—local grounding, temporal consistency, and global semantic audit—provide complementary evidence over a shared, revisable semantic memory. Their outputs are fused by the Arbitration Agent; unresolved conflicts are escalated to the human supervisor, whose edits trigger dependency-closure re-verification rather than a full pipeline rerun.\", \"page\": 3, \"index\": 2, \"width\": 838, \"height\": 462}]"
motivation: 现有的长视频理解模型输出不透明，导致修正错误时必须重新审视原始视频，造成极高的人工成本。
method: 建立一个包含声明依赖图和溯源日志的结构化语义记忆系统，由具备明确授权合同的多智能体进行迭代验证与修正。
result: 实验表明该系统在VidOR数据集上将VQA性能从0.71提升至0.79，同时减少了4.8倍的人工仲裁工作量。
conclusion: 通过声明级的监督修正机制，IMPACT-CYCLE实现了理解精度与人工成本之间的有效平衡。
---

## 摘要
纠正长视频理解中的错误成本极高：现有的多模态流水线产生不透明的端到端输出，不暴露任何可供检查的中间状态，迫使标注者重新查看原始视频并从头重构时序逻辑。核心瓶颈不仅在于生成质量，还在于缺乏一种监督界面，使人类的投入能与每个错误的范围成比例。我们提出了 IMPACT-CYCLE，这是一个监督式多智能体系统，它将长视频理解重新表述为对共享语义记忆的迭代断言级维护——这是一种结构化的、带版本的状态，编码了类型化断言、断言依赖图和溯源日志。在明确的权限契约下运行的角色专业化智能体将验证分解为局部对象关系正确性、跨时序一致性和全局语义连贯性，且修正仅限于结构相关的断言。当自动化证据不足时，系统会升级到作为具有最终覆盖权的监督机构的人工仲裁；随后，依赖闭包重新验证确保修正成本与错误范围保持比例。在 VidOR 上的实验表明，下游推理能力显著提高（VQA：0.71 提升至 0.79），人工仲裁成本降低了 4.8 倍，工作量显著低于手动标注。代码将在 https://github.com/MKong17/IMPACT_CYCLE 发布。

## Abstract
Correcting errors in long-video understanding is disproportionately costly: existing multimodal pipelines produce opaque, end-to-end outputs that expose no intermediate state for inspection, forcing annotators to revisit raw video and reconstruct temporal logic from scratch. The core bottleneck is not generation quality alone, but the absence of a supervisory interface through which human effort can be proportional to the scope of each error. We present IMPACT-CYCLE, a supervisory multi-agent system that reformulates long-video understanding as iterative claim-level maintenance of a shared semantic memory -- a structured, versioned state encoding typed claims, a claim dependency graph, and a provenance log. Role-specialized agents operating under explicit authority contracts decompose verification into local object-relation correctness, cross-temporal consistency, and global semantic coherence, with corrections confined to structurally dependent claims. When automated evidence is insufficient, the system escalates to human arbitration as the supervisory authority with final override rights; dependency-closure re-verification then ensures correction cost remains proportional to error scope. Experiments on VidOR show substantially improved downstream reasoning (VQA: 0.71 to 0.79) and a 4.8x reduction in human arbitration cost, with workload significantly lower than manual annotation. Code will be released at https://github.com/MKong17/IMPACT_CYCLE.

---

## 论文详细总结（自动生成）

这是一份关于论文《IMPACT-CYCLE: A Contract-Based Multi-Agent System for Claim-Level Supervisory Correction of Long-Video Semantic Memory》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
长视频理解目前面临一个核心瓶颈：**错误修正成本与错误范围不成比例**。现有的多模态大模型（MLLM）流水线通常是端到端的“黑盒”，一旦输出结果（如视频问答或描述）出错，用户无法定位中间逻辑错误，只能被迫重新审视原始视频并从头标注。
*   **研究动机**：将长视频理解从“一次性生成”转变为“可维护的语义记忆”。
*   **核心目标**：建立一个结构化、可编辑的中间表示（断言级语义记忆），使人类监督者能够以极小的工作量（仅针对受影响的局部断言）修正模型生成的错误。

### 2. 核心思想与方法论
IMPACT-CYCLE 提出了一个基于契约的多智能体系统，其核心思想是将视频理解转化为对**共享语义记忆（Semantic Memory）**的迭代维护。

*   **语义记忆结构**：定义为四元组 $M = (G, C, D, \Pi)$。
    *   $G$：视频级场景图状态。
    *   $C$：从图中分解出的原子化、类型化断言（如实体存在、标签、属性、关系）。
    *   $D$：断言依赖图，记录断言间的结构约束。
    *   $\Pi$：溯源日志，记录所有修改历史。
*   **五大角色智能体**：
    1.  **记忆构建者（Memory Constructor）**：负责初始化场景图和断言。
    2.  **局部接地智能体（Local Grounding Agent）**：验证单帧内的对象和关系。
    3.  **时序一致性智能体（Temporal Consistency Agent）**：验证跨帧的动作和状态连贯性。
    4.  **全局语义审计智能体（Global Semantic Audit Agent）**：通过视频摘要检测全局逻辑矛盾。
    5.  **仲裁智能体（Arbitration Agent）**：融合多方证据，决定接受、重写断言或升级给人类。
*   **关键技术细节**：
    *   **角色感知证据融合**：根据断言类型（如标签 vs 关系）为不同智能体分配不同的权重矩阵 $\lambda$。
    *   **依赖闭包重新验证**：当某个断言被修正后，系统仅重新验证与其有结构依赖的邻近断言，而非重启整个流水线。
    *   **人类作为最高权威**：当自动验证冲突严重或置信度低时，系统发起仲裁请求，人类仅需进行简单的二选一或填空操作。

### 3. 实验设计
*   **数据集**：主要在 **VidOR**（长视频对象关系数据集）上进行评估。
*   **Benchmark 与对比方法**：
    *   **Baseline**：直接使用 MLLM（无场景图）、初始生成的场景图。
    *   **对比维度**：结构化质量（实体准确率、场景图编辑距离 GED）、下游任务表现（VQA 准确率）、验证行为（Resolve Score、人工查询频率）。
*   **用户研究**：对比了三种模式：全手动标注、基于 SAM 的手动精炼、以及 IMPACT-CYCLE 辅助下的监督修正。

### 4. 资源与算力
*   **模型后端**：使用了 **GPT-4V** (gpt-4-vision-preview) 作为主要的验证后端，并对比了 **Gemini Pro**。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。由于该系统主要基于现有的闭源大模型 API 进行多智能体协作，其开销主要体现在 API 调用次数和推理延迟上，而非传统的模型训练算力。

### 5. 实验数量与充分性
*   **实验规模**：在 VidOR 数据集的 240 个视频上进行了测试，VQA 增益在统计学上显著（p < 0.05）。
*   **消融实验**：非常充分。分别验证了多智能体角色、角色感知权重、迭代精炼机制对系统性能的贡献。
*   **客观性**：通过对比不同后端（GPT-4V vs Gemini Pro）和不同断言密度（低、中、高）下的表现，证明了系统的鲁棒性。同时引入了 NASA-TLX 量表进行用户主观工作量评估，增加了实验的多维度客观性。

### 6. 主要结论与发现
*   **性能提升**：IMPACT-CYCLE 将 VQA 准确率从 0.71 提升至 **0.79**。
*   **成本降低**：通过依赖闭包机制，人工仲裁成本比全量重新标注降低了 **4.8 倍**。
*   **验证行为**：Resolve Score（自动解决率）与下游 VQA 表现强相关（r=0.72），证明了断言级修正的有效性。
*   **工作量**：用户研究显示，IMPACT-CYCLE 显著降低了人类在长视频标注中的认知负荷（NASA-TLX 各维度得分最低）。

### 7. 优点与亮点
*   **架构创新**：将“生成”问题转化为“维护”问题，引入了“断言依赖图”概念，实现了精准的局部修正。
*   **人机协作范式**：不再将人类视为简单的标注员，而是作为具有“最终覆盖权”的监督者，且系统主动筛选高价值、高冲突的断言请求人类干预。
*   **透明度**：溯源日志和结构化记忆使视频理解过程可审计、可回滚。

### 8. 不足与局限
*   **评估偏差风险**：VQA 评估使用了与验证相同的 MLLM 进行问题生成和评分，可能存在“自我评估偏差”。
*   **实时性限制**：多轮智能体交互和 API 调用可能导致处理长视频时的延迟较高，难以满足实时应用需求。
*   **人类仲裁模拟**：主实验中的人类仲裁使用了 Oracle（真值）模拟，实际应用中不同标注者的水平差异可能影响最终一致性。
*   **应用范围**：目前主要针对场景图和关系理解，对于更高级的抽象情感或复杂叙事逻辑的覆盖尚不明确。

（完）
