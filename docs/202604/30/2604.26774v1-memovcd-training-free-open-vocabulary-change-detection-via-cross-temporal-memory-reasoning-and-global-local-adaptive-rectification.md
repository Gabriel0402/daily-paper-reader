---
title: "MemOVCD: Training-Free Open-Vocabulary Change Detection via Cross-Temporal Memory Reasoning and Global-Local Adaptive Rectification"
title_zh: MemOVCD：基于跨时空记忆推理与全局-局部自适应修正的无需训练的开放词汇变化检测
authors: "Zuzheng Kuang, Honghao Chang, Boqiang Liang, Haoqian Wang, Lijun He, Fan Li, Haixia Bi"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26774v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用SAM和基础模型进行遥感图像的开放词汇变化检测
tldr: 本研究针对开放词汇变化检测中时空耦合不足和高分辨率图像推理碎片化的问题，提出了MemOVCD框架。该框架无需训练，将双时相变化检测重新定义为两帧跟踪问题，通过跨时相记忆推理和加权双向传播聚合语义证据。同时，引入直方图对齐的过渡帧和全局-局部自适应校正策略，显著提升了语义一致性与细节保留能力。在五个基准数据集上的实验证明了其在多样化开放词汇设置下的优越性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26774v1/fig-001.webp\", \"caption\": \"Figure 2: Overall pipeline of the proposed method. Built on a training-free Identify-Mask-Compare framework, MemOVCD performs open-vocabulary change detection via cross-temporal memory reasoning and global-local adaptive rectification.\", \"page\": 4, \"index\": 1, \"width\": 1052, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.26774v1/fig-002.webp\", \"caption\": \"Table 1: Comparison on building change detection benchmarks. “*” denotes our re-implemented results under the best reproducible settings. “-” denotes unavailable results. The best and second-best results are marked in red and blue respectively.\", \"page\": 5, \"index\": 2, \"width\": 494, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2604.26774v1/fig-003.webp\", \"caption\": \"Figure 1: Illustration of our motivation.\", \"page\": 1, \"index\": 3, \"width\": 500, \"height\": 281}]"
motivation: 现有方法在处理双时相图像时缺乏足够的时空耦合，且高分辨率下的局部推理易导致语义不连续和结果碎片化。
method: 将变化检测建模为跟踪问题，利用加权双向传播、直方图对齐过渡帧以及全局-局部自适应校正策略进行语义推理与结果优化。
result: 在五个基准数据集上的实验结果显示，该方法在两项变化检测任务中均取得了优异表现，验证了其有效性。
conclusion: MemOVCD通过创新的记忆推理和校正机制，为无需训练的开放词汇变化检测提供了一种高效且泛化性强的解决方案。
---

## 摘要
开放词汇变化检测旨在识别双时相遥感图像中的语义变化，而无需预定义类别。最近的方法结合了 SAM、DINO 和 CLIP 等基础模型，但通常独立处理每个时间戳，或者仅在最后的比较阶段进行交互。这种范式在语义推理过程中存在时空耦合不足的问题，限制了其区分真实语义变化与非语义外观差异的能力。此外，在高分辨率图像上以分块为主的推理往往会削弱全局语义连续性，并产生碎片化的变化区域。为了解决这些问题，我们提出了 MemOVCD，这是一个基于跨时空记忆推理和全局-局部自适应修正的无需训练的开放词汇变化检测框架。具体而言，我们将双时相变化检测重新表述为双帧跟踪问题，并引入加权双向传播来聚合来自两个时间方向的语义证据。为了稳定跨越较大时间间隔的记忆传播，我们构建了直方图对齐的过渡帧，以平滑突变的外观变化。此外，全局-局部自适应修正策略自适应地融合了局部和全局视图的预测，在保持细粒度细节的同时提高了空间一致性。在五个基准数据集上的实验表明，MemOVCD 在两个变化检测任务上均取得了良好的性能，验证了其在多种开放词汇设置下的有效性和泛化能力。

## Abstract
Open-vocabulary change detection aims to identify semantic changes in bi-temporal remote sensing images without predefined categories. Recent methods combine foundation models such as SAM, DINO and CLIP, but typically process each timestamp independently or interact only at the final comparison stage. Such paradigms suffer from insufficient temporal coupling during semantic reasoning, which limits their ability to distinguish genuine semantic changes from non-semantic appearance discrepancies. In addition, patch-dominant inference on high-resolution images often weakens global semantic continuity and produces fragmented change regions. To address these issues, we propose MemOVCD, a training-free open-vocabulary change detection framework based on cross-temporal memory reasoning and global-local adaptive rectification. Specifically, we reformulate bi-temporal change detection as a two-frame tracking problem and introduce weighted bidirectional propagation to aggregate semantic evidence from both temporal directions. To stabilize memory propagation across large temporal gaps, we construct histogram-aligned transition frames to smooth abrupt appearance changes. Moreover, a global-local adaptive rectification strategy adaptively fuses local and global-view predictions, improving spatial consistency while preserving fine-grained details. Experiments on five benchmarks demonstrate that MemOVCD achieves favorable performance on two change detection tasks, validating its effectiveness and generalization under diverse open-vocabulary settings.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《MemOVCD: Training-Free Open-Vocabulary Change Detection via Cross-Temporal Memory Reasoning and Global-Local Adaptive Rectification》** 的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：开放词汇变化检测（OVCD）旨在利用自然语言查询识别遥感图像中的语义变化，无需预定义类别。
*   **核心痛点**：
    1.  **时空耦合不足**：现有方法多采用“独立处理+后期比较”的范式，两个时间戳的图像在语义推理阶段缺乏交互，难以区分真实的语义变化与光照、季节等引起的外观差异。
    2.  **大时间跨度挑战**：时间间隔较长时，外观突变会削弱特征交互。
    3.  **空间不连续性**：高分辨率图像通常采用分块（Patch-based）推理，导致全局语义不连续，产生碎片化的检测结果。
*   **整体含义**：本文提出 **MemOVCD** 框架，通过将变化检测重新定义为“两帧跟踪”任务，利用基础模型的记忆机制增强时空耦合，并结合全局-局部修正策略提升结果的完整性。

### 2. 论文提出的方法论
核心思想是**无需训练（Training-free）**，直接利用预训练视觉基础模型（如 SAM 3）的能力。
*   **跨时空记忆推理（CTMR）**：
    *   将双时相检测建模为视频跟踪问题。利用 SAM 3 的记忆模块，让每个时间戳的解码都基于另一个时间戳积累的证据。
    *   **直方图对齐过渡帧桥接**：为解决外观突变，在 $T_1$ 和 $T_2$ 之间构建 $K$ 个过渡帧。首先对 $T_1$ 进行直方图匹配以对齐 $T_2$ 的风格，再通过线性插值生成中间帧，从而平滑记忆传播过程，减少“鬼影”伪影。
    *   **加权双向传播**：执行从 $T_1 \to T_2$ 和 $T_2 \to T_1$ 的双向掩码传播，提取稳定的区域作为“视觉示例（Visual Exemplar）”，并将其作为 Prompt 注入分割模型。
*   **全局-局部自适应修正（Rectification）**：
    *   **双路推理**：同时进行局部切片推理（保留细节）和全局视图推理（保持连续性）。
    *   **自适应融合**：利用 8-连通域分析，计算全局组件与局部预测的覆盖率。若局部预测覆盖不足，则通过加权融合引入全局预测的逻辑值（Logits），实现残差补偿。
*   **推理流程**：全局初始化 $\to$ 跨时空 Prompt 生成 $\to$ Logit 计算 $\to$ 修正与变化解码 $\to$ 实例匹配。

### 3. 实验设计
*   **数据集**：
    *   **建筑变化检测**：LEVIR-CD, DSIFN, S2Looking, BANDON。
    *   **语义变化检测**：SECOND（多类别土地覆盖变化）。
*   **Benchmark 与对比方法**：
    *   **传统无监督方法**：IRMAD, PCA-Kmeans, ISFA, DCVA, DSFA。
    *   **需训练的 OVCD 方法**：OV-CD, UniVCD, OpenDPR-W。
    *   **无需训练的 OVCD 方法**：DynamicEarth 变体（SAM/DINO/CLIP 组合）、AdaptOVCD、OpenDPR、OmniOVCD。

### 4. 资源与算力
*   **硬件**：使用了一台 **NVIDIA RTX A100 GPU**。
*   **模型基础**：采用了 **SAM 3.1** 作为视觉基础模型。
*   **训练时长**：由于该方法是 **Training-free（无需训练）** 的，因此不存在训练阶段，仅涉及推理过程。文中未详细说明单对图像的具体推理耗时。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流遥感基准数据集上进行了测试，涵盖了二值建筑变化和多类别语义变化。
*   **消融实验**：针对 CTMR（记忆推理）、Recti（自适应修正）、GR（全局细化）分别进行了消融，并细化到了双向传播的方向性以及视觉示例的加权策略。
*   **敏感性分析**：对过渡帧数量 $K$ 进行了参数敏感性测试（$K=0, 1, 3, 5$）。
*   **评价**：实验设计较为全面，对比了从传统到最前沿的各类方法，且在不同类型的遥感场景下验证了泛化性，实验结果具有较高的客观性和说服力。

### 6. 论文的主要结论与发现
*   **性能领先**：MemOVCD 在 LEVIR-CD 上达到了 72.5% 的 IoU，显著超过了之前的 SOTA 方法 OmniOVCD（67.2%）和需训练的方法 OV-CD（66.0%）。
*   **时空耦合的重要性**：实验证明，将 CD 视为动态跟踪过程比静态的后期比较更能有效区分语义变化与环境干扰。
*   **过渡帧的有效性**：引入 3 个过渡帧能显著提升大跨度图像下的推理稳定性。
*   **全局修正的价值**：全局-局部自适应融合能有效解决分块推理带来的碎片化问题，尤其对不规则或大型地物（如森林、水体）效果明显。

### 7. 优点
*   **无需训练**：极大地降低了部署成本，能够直接应用于各种未见过的遥感类别。
*   **创新的视角**：将变化检测转化为视频跟踪任务，巧妙利用了 SAM 3 的原生记忆机制。
*   **鲁棒性强**：通过直方图对齐和过渡帧设计，克服了遥感图像中常见的季节和光照剧变问题。
*   **细节与全局兼顾**：自适应修正策略平衡了小目标的精细结构和大目标的空间一致性。

### 8. 不足与局限
*   **计算开销**：虽然无需训练，但引入过渡帧和双向传播意味着需要多次调用基础模型进行推理，可能会增加单次检测的时间延迟。
*   **依赖基础模型**：性能高度依赖于 SAM 3 等基础模型的预训练质量，若基础模型在特定极端遥感场景下失效，该框架也难以弥补。
*   **超参数依赖**：如过渡帧数量 $K$、覆盖率阈值 $\tau$ 等仍需经验设置，不同传感器数据可能需要微调。

（完）
