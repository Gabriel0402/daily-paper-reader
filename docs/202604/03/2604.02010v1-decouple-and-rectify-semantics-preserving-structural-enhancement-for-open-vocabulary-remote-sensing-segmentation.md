---
title: "Decouple and Rectify: Semantics-Preserving Structural Enhancement for Open-Vocabulary Remote Sensing Segmentation"
title_zh: 解耦与纠正：面向开放词汇遥感分割的语义保持结构增强
authors: "Jie Feng, Fengze Li, Junpeng Zhang, Siyu Chen, Yuping Liang, Junying Chen, Ronghua Shang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02010v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用CLIP和DINO进行开放词汇遥感图像分割
tldr: 针对遥感图像开放词汇语义分割中CLIP特征缺乏结构细节且易受干扰的问题，本文提出DR-Seg框架。该方法基于CLIP特征通道的异构性，将其解耦为语义和结构子空间，利用DINO特征进行针对性结构增强。通过图整流模块注入结构先验，并结合不确定性引导的自适应融合，在保持语义一致性的同时提升了边界分割精度，在八个基准测试中达到SOTA。
selection_source: fresh_fetch
motivation: 现有方法将CLIP特征视为统一整体，难以在不破坏其语言对齐语义的前提下，精准定位并增强遥感图像的细粒度结构细节。
method: 提出解耦与整流框架，将CLIP特征分为语义和结构子空间，利用DINO引导的图整流模块注入结构先验，并通过不确定性引导模块进行自适应融合。
result: 在八个遥感图像分割基准测试上的实验结果表明，DR-Seg显著提升了分割精度，刷新了开放词汇遥感分割的最先进性能纪录。
conclusion: 通过解耦CLIP特征并进行针对性的结构修复，可以在保留强大语义泛化能力的同时，实现高精度的遥感地物边界刻画。
---

## 摘要
遥感（RS）领域的开放词汇语义分割既需要语言对齐的识别，也需要细粒度的空间轮廓描绘。尽管 CLIP 提供了强大的语义泛化能力，但其全局对齐的视觉表示在捕捉结构细节方面存在固有困难。最近的方法尝试通过引入遥感预训练的 DINO 特征来弥补这一不足。然而，这些方法将 CLIP 表示视为单一的语义空间，无法定位需要结构增强的区域，导致无法有效描绘边界，并存在破坏 CLIP 语义完整性的风险。为了解决这一局限性，本文提出了 DR-Seg，一种新颖的解耦与纠正框架。我们的方法源于一个关键观察：CLIP 特征通道表现出明显的功能异质性，而非形成统一的语义空间。基于这一见解，DR-Seg 将 CLIP 特征解耦为语义主导和结构主导的子空间，从而在不扭曲语言对齐语义的情况下，利用 DINO 进行有针对性的结构增强。随后，先验驱动的图纠正模块在 DINO 的引导下注入高保真结构先验以形成精细化分支，而一个不确定性引导的自适应融合模块则动态地将该精细化分支与原始 CLIP 分支整合，以进行最终预测。在八个基准测试集上的全面实验表明，DR-Seg 达到了新的最先进水平。

## Abstract
Open-vocabulary semantic segmentation in the remote sensing (RS) field requires both language-aligned recognition and fine-grained spatial delineation. Although CLIP offers robust semantic generalization, its global-aligned visual representations inherently struggle to capture structural details. Recent methods attempt to compensate for this by introducing RS-pretrained DINO features. However, these methods treat CLIP representations as a monolithic semantic space and cannot localize where structural enhancement is required, failing to effectively delineate boundaries while risking the disruption of CLIP's semantic integrity. To address this limitation, we propose DR-Seg, a novel decouple-and-rectify framework in this paper. Our method is motivated by the key observation that CLIP feature channels exhibit distinct functional heterogeneity rather than forming a uniform semantic space. Building on this insight, DR-Seg decouples CLIP features into semantics-dominated and structure-dominated subspaces, enabling targeted structural enhancement by DINO without distorting language-aligned semantics. Subsequently, a prior-driven graph rectification module injects high-fidelity structural priors under DINO guidance to form a refined branch, while an uncertainty-guided adaptive fusion module dynamically integrates this refined branch with the original CLIP branch for final prediction. Comprehensive experiments across eight benchmarks demonstrate that DR-Seg establishes a new state-of-the-art.

---

## 论文详细总结（自动生成）

这是一份关于论文《Decouple and Rectify: Semantics-Preserving Structural Enhancement for Open-Vocabulary Remote Sensing Segmentation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在遥感（RS）图像的开放词汇语义分割中，如何平衡**语言对齐的语义识别**与**精细的空间结构刻画**。
*   **研究动机**：
    *   **CLIP的局限性**：CLIP虽然具备强大的零样本语义泛化能力，但其特征缺乏空间保真度，难以处理遥感图像中密集的物体和复杂的边界。
    *   **现有方法的缺陷**：目前常引入RS预训练的DINO特征作为结构先验，但通常采用“全盘融合”策略。作者发现CLIP特征通道具有**功能异质性**（有的通道负责语义识别，有的则对结构敏感），盲目融合会破坏CLIP原有的语义完整性，导致分割结果模糊。

### 2. 方法论：DR-Seg 框架
核心思想是将CLIP特征进行“解耦”处理，仅对结构相关的部分进行增强，最后通过不确定性引导进行融合。
*   **语义保持子空间解耦 (SPSD)**：
    *   通过离线计算训练集中各通道的语义重要性得分。
    *   利用**跨类别激活熵**（衡量通道是否只对特定类响应）和**类间相似度**（衡量通道区分不同类的能力）对通道排序。
    *   将特征划分为“语义主导子空间”（保留以维持语言对齐）和“结构主导子空间”（送入后续增强）。
*   **先验驱动图整流 (PDGR)**：
    *   利用DINO特征构建稀疏亲和图，结合空间邻近性和特征相似性。
    *   在结构主导子空间上进行可学习的图传播，利用DINO的几何先验修复物体边界和细微结构。
*   **不确定性引导自适应融合 (UGAF)**：
    *   计算原始CLIP分支预测的像素级熵（不确定性）。
    *   在语义确定的区域偏向原始分支，在不确定区域（如边界、小物体）加强整流分支的权重，实现动态融合。

### 3. 实验设计
*   **数据集**：涵盖了8个主流遥感基准测试集，包括 DLRSD, iSAID, Potsdam, Vaihingen, LoveDA, UAVid, UDD5, VDD。
*   **实验场景**：在 DLRSD 或 iSAID 上训练，在所有8个数据集上进行测试，以验证**域内精度**和**跨数据集泛化能力**。
*   **对比方法**：
    *   通用开放词汇分割方法：SAN, Cat-Seg, SED, SCAN 等。
    *   遥感专用方法：OVRS, GSNet, RSKT-Seg 等。
*   **评价指标**：mIoU（平均交并比）、mACC（平均准确率）和 fwIoU（频率加权交并比）。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA RTX 5090 GPU**（每台 32GB 显存）。
*   **训练细节**：
    *   迭代次数：30,000 次。
    *   Batch Size：8。
    *   优化器：AdamW，初始学习率 $2 \times 10^{-4}$。
    *   输入分辨率：ViT-B 为 384×384，ViT-L 为 336×336。

### 5. 实验数量与充分性
*   **实验规模**：论文在8个不同的遥感数据集上进行了详尽测试，这在同类研究中属于非常广泛的验证。
*   **消融实验**：
    *   验证了三个核心组件（SPSD, PDGR, UGAF）的各自贡献。
    *   分析了不同语义比例 $\rho$、图稀疏参数 $k$ 以及不同结构先验（DINOv1 vs DINOv3）的影响。
    *   通过可视化通道激活图和零样本分割性能，客观证明了“通道异质性”假设的成立。
*   **公平性**：在相同预训练权重（如匹配 DINO 先验）的情况下与 SOTA 方法对比，确保了性能提升源于架构设计而非单纯的骨干网络升级。

### 6. 主要结论
*   **解耦是关键**：CLIP特征并非同质的语义空间，通过解耦可以有效防止结构增强对语义对齐的干扰。
*   **性能领先**：DR-Seg 在多个基准上刷新了纪录，例如在 DLRSD 训练设置下，相比之前的 SOTA (RSKT-Seg)，mIoU 提升了 **2.83%**。
*   **泛化性强**：在跨数据集测试（如 UDD5 和 VDD）中表现优异，证明该方法能很好地适应不同的传感器和拍摄视角。

### 7. 优点
*   **理论洞察深刻**：通过实验揭示了 CLIP 通道的异质性，为后续研究提供了新的视角。
*   **针对性增强**：避免了传统方法中“一刀切”的特征融合，通过图整流精准修复边界。
*   **即插即用潜力**：解耦思想和不确定性融合机制具有较好的通用性，可推广至其他多模态任务。

### 8. 不足与局限
*   **离线计算依赖**：SPSD 模块的通道排序是基于训练集离线计算并固定的，如果测试场景与训练场景差异极端巨大，预设的通道划分可能不再最优。
*   **计算复杂度**：引入图整流模块和多分支融合会增加推理时的计算开销，尽管论文提到使用了轻量化设计，但在超大规模遥感影像实时处理中可能仍面临挑战。
*   **超参数敏感性**：如语义比例 $\rho$ 和图稀疏度 $k$ 需要针对不同任务进行微调以达到最佳效果。

（完）
