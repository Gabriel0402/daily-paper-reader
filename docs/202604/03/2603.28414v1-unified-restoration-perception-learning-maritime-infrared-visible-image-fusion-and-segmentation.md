---
title: "Unified Restoration-Perception Learning: Maritime Infrared-Visible Image Fusion and Segmentation"
title_zh: 统一恢复-感知学习：海事红外-可见光图像融合与分割
authors: "Weichao Cai, Weiliang Huang, Biao Xue, Chao Huang, Fei Yuan, Bob Zhang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28414v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 多模态红外-可见光图像融合与分割
tldr: 本研究针对海事场景中雾气和强反射导致的图像退化问题，提出了红外-可见光海事船舶数据集（IVMSD）。同时，设计了一个多任务互补学习框架（MCLF），通过频率-空间增强互补模块（FSEC）和语义-视觉一致性注意力模块（SVCA），实现了图像修复、多模态融合与语义分割的统一学习。该方法在复杂海况下显著提升了语义感知的稳定性和图像质量，达到了领域领先水平。
selection_source: fresh_fetch
motivation: 现有方法缺乏能同时处理海事环境特有退化并兼顾视觉修复与语义感知的端到端协作机制。
method: 提出了多任务互补学习框架（MCLF），集成频率-空间增强、语义一致性引导及跨模态融合机制。
result: 在IVMSD数据集上的实验表明，该方法在复杂海事条件下实现了最先进的分割性能和图像感知质量。
conclusion: 通过统一修复与感知学习，该研究有效提升了海事监测中多模态图像处理的鲁棒性与语义理解能力。
---

## 摘要
海洋场景理解与分割在海事监控和航行安全中发挥着至关重要作用。然而，海事环境中普遍存在的雾气和强反射等因素会导致严重的图像退化，显著损害了语义感知的稳定性。现有的恢复和增强方法通常针对特定的退化类型，或仅关注视觉质量，缺乏能够同时提高结构恢复和语义有效性的端到端协作机制。此外，公开可用的红外-可见光数据集主要采集自城市场景，无法捕捉海洋环境中耦合退化的真实特征。为应对这些挑战，本文提出了红外-可见光海事船舶数据集（IVMSD），涵盖了多种天气和光照条件下的各种海事场景。基于该数据集，本文提出了一种多任务互补学习框架（MCLF），在统一架构内协同执行图像恢复、多模态融合和语义分割。该框架包括用于退化抑制和结构增强的频域-空间增强互补（FSEC）模块、用于语义一致性引导的语义-视觉一致性注意力（SVCA）模块，以及用于选择性融合的跨模态引导注意力机制。在 IVMSD 上的实验结果表明，所提方法实现了最先进的分割性能，显著增强了复杂海事条件下的鲁棒性和感知质量。

## Abstract
Marine scene understanding and segmentation plays a vital role in maritime monitoring and navigation safety. However, prevalent factors like fog and strong reflections in maritime environments cause severe image degradation, significantly compromising the stability of semantic perception. Existing restoration and enhancement methods typically target specific degradations or focus solely on visual quality, lacking end-to-end collaborative mechanisms that simultaneously improve structural recovery and semantic effectiveness. Moreover, publicly available infrared-visible datasets are predominantly collected from urban scenes, failing to capture the authentic characteristics of coupled degradations in marine environments. To address these challenges, the Infrared-Visible Maritime Ship Dataset (IVMSD) is proposed to cover various maritime scenarios under diverse weather and illumination conditions. Building upon this dataset, a Multi-task Complementary Learning Framework (MCLF) is proposed to collaboratively perform image restoration, multimodal fusion, and semantic segmentation within a unified architecture. The framework includes a Frequency-Spatial Enhancement Complementary (FSEC) module for degradation suppression and structural enhancement, a Semantic-Visual Consistency Attention (SVCA) module for semantic-consistent guidance, and a cross-modality guided attention mechanism for selective fusion. Experimental results on IVMSD demonstrate that the proposed method achieves state-of-the-art segmentation performance, significantly enhancing robustness and perceptual quality under complex maritime conditions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Unified Restoration-Perception Learning: Maritime Infrared-Visible Image Fusion and Segmentation》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：海事环境（如海雾、强反射、低光照）会导致严重的图像退化，影响船舶识别与分割的准确性。
*   **研究动机**：
    *   **现有方法脱节**：传统的图像增强（低级视觉）与语义分割（高级视觉）通常是级联或独立的，缺乏端到端的协同优化机制，导致增强后的图像未必有利于语义感知。
    *   **数据集缺失**：现有的红外-可见光融合数据集（如 MFNet）多集中于城市道路，缺乏真实海事场景下复杂退化（如海浪反射、盐雾）的数据支持。
*   **整体含义**：本文旨在通过构建专用数据集并提出统一的学习框架，打破“恢复”与“感知”之间的壁垒，提升复杂海况下多模态感知的鲁棒性。

### 2. 论文提出的方法论：MCLF 框架
论文提出了**多任务互补学习框架（MCLF）**，其核心思想是将图像恢复、融合与分割整合在统一架构中，利用语义信息反向指导图像增强。
*   **核心模块**：
    1.  **频率-空间增强互补模块 (FSEC)**：
        *   **技术细节**：利用离散小波变换（DWT）将特征分解为不同频段，结合 Fast Fourier Transform (FFT) 进行全局频率细化，并使用局部 2D 选择性扫描（SS2D/Mamba 机制）在空间域增强结构信息。
        *   **作用**：针对性地抑制高频噪声（海浪、热噪声）和低频退化（海雾）。
    2.  **门控模态交互注意力 (GMIA)**：
        *   **技术细节**：采用双向门控交叉注意力机制，在红外与可见光特征之间进行选择性融合。
        *   **作用**：抑制模态间的冗余信息和噪声传播，确保特征的一致性。
    3.  **语义-视觉一致性注意力 (SVCA)**：
        *   **技术细节**：采用轴解耦（Axis-decoupled）空间注意力和门控通道注意力，利用分割任务的语义先验来引导视觉增强。
        *   **作用**：使模型更关注任务相关的区域（如船舶），而非背景干扰。
*   **多任务损失函数**：结合了分割损失（Focal Loss + IoU Loss）和融合损失（强度对齐 + 梯度约束），形成闭环优化。

### 3. 实验设计
*   **数据集与场景**：
    *   **IVMSD (本文构建)**：包含 2,313 对对齐的红外-可见光图像，涵盖正常、雾天、低光、强反射四种场景，标注了货船、渔船、挖沙船、快艇四类目标。
    *   **跨场景验证集**：MFNet (城市)、PST900 (地下)、FMB (全天候城市)。
*   **Benchmark 与对比方法**：
    *   对比了 10 种 SOTA 方法，包括 SegFormer、SeAFusion、LASNet、MDRNet+、SGFNet、EAEFNet、MRFS、StitchFusion、UniRGB-IR 等。
*   **评估指标**：主要使用平均交并比 (mIoU)。

### 4. 资源与算力
*   **硬件环境**：使用了一台 **NVIDIA GeForce RTX 4090 GPU (24 GB)**。
*   **训练细节**：
    *   训练 200 个 Epoch。
    *   Batch size 为 4。
    *   优化器为 Adam，初始学习率为 $1 \times 10^{-4}$。
    *   特征编码器（Backbone）采用了预训练的 **DINOv3**，且在训练过程中保持**冻结**状态，以验证所提模块的有效性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个不同的数据集上进行了定量对比。
    *   针对 IVMSD 进行了详细的消融实验（包括模块有效性、不同 Backbone 的影响、门控机制的作用、频率-空间增强的必要性等）。
    *   提供了丰富的定性视觉对比图和特征图可视化。
*   **充分性评价**：实验设计较为充分且客观。通过冻结 Backbone 并在多个异构数据集上测试，证明了性能提升源于架构创新而非单纯的参数堆砌。消融实验涵盖了从宏观模块到微调机制（如门控、混合注意力）的各个层面。

### 6. 论文的主要结论与发现
*   **统一学习的优越性**：将恢复与感知任务耦合，比单纯的级联处理能获得更高的分割精度（IVMSD 上 mIoU 达到 60.2%）。
*   **频率-空间结合的必要性**：海事退化在频域有明显特征，结合 DWT 和 FFT 能更有效地恢复被雾气和反射遮蔽的结构。
*   **语义引导的作用**：语义先验能显著减少背景噪声（如海浪误报）对特征提取的干扰。
*   **泛化能力**：该模型在非海事数据集（如 PST900, FMB）上也取得了 SOTA 结果，证明了其模块设计的通用性。

### 7. 优点（亮点）
*   **数据集贡献**：填补了高质量海事红外-可见光多模态分割数据集的空白，且采用了半自动标注（SAM2）结合专家修正的高效流程。
*   **架构创新**：巧妙地将 Mamba (SS2D) 机制引入频率-空间增强，处理长程依赖的同时保持了计算效率。
*   **任务解耦与耦合**：通过轴解耦注意力处理海事目标的长条状结构特征，设计合理。

### 8. 不足与局限
*   **小目标挑战**：尽管整体性能领先，但在“渔船”和“快艇”等小目标上的 IoU 仍然较低（约 28%-32%），说明极小目标的特征恢复仍是瓶颈。
*   **实时性说明不足**：论文未详细讨论推理延迟（FPS），对于需要快速响应的海事导航任务，实时性是一个关键指标。
*   **Backbone 依赖**：虽然冻结 Backbone 证明了模块有效性，但未探讨如果全参数微调是否会带来进一步增益或导致过拟合。
*   **应用限制**：目前主要针对船舶，对于海面其他障碍物（如浮标、礁石）的覆盖可能不足。

（完）
