---
title: Temporal Logic Control of Nonlinear Stochastic Systems with Online Performance Optimization
title_zh: 具有在线性能优化的非线性随机系统时序逻辑控制
authors: "Alessandro Riccardi, Thom Badings, Luca Laurenti, Alessandro Abate, Bart De Schutter"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01372v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 随机系统的时态逻辑控制与性能优化
tldr: 针对非线性随机系统在安全关键环境下的复杂任务控制，本文提出一种基于区间马尔可夫决策过程（IMDP）的新型抽象技术。不同于传统方法仅生成单一策略，该方法能生成一组满足最小概率保证的策略集合。通过结合模型预测控制（MPC），系统可在运行时动态优化性能指标（如能耗），在保持时序逻辑约束的同时显著提升控制效率。
selection_source: fresh_fetch
motivation: 现有的基于抽象的控制方法通常只生成单一策略，无法在运行时针对能耗等性能指标进行在线优化。
method: 提出一种新型IMDP抽象技术以获取满足时序逻辑约束的策略集合，并利用MPC在该集合内在线搜索最优控制动作。
result: 实验表明，该方法在保持高概率安全保证的前提下，比现有的单策略抽象技术具有更好的控制性能和更低的运行成本。
conclusion: 该研究成功结合了形式化验证的严谨性与在线优化的灵活性，为非线性随机系统的复杂任务控制提供了更高效的方案。
---

## 摘要
在安全关键环境中部署自主系统需要能够保证满足复杂控制规范的控制策略。这些系统通常被建模为非线性离散时间随机系统。计算可证明满足复杂控制规范的策略的一种常用方法是构建有限状态抽象，通常表示为具有转移概率区间的马尔可夫决策过程（MDP），即区间马尔可夫决策过程（IMDP）。然而，现有的抽象技术仅计算单一策略，因此没有为在线成本或性能优化（例如能耗优化）留出空间。为了克服这一局限性，我们提出了一种新型 IMDP 抽象技术，该技术可产生一组策略，其中每个策略都以一定的最小概率满足控制规范。因此，我们可以使用任何在线控制算法在这组经过验证的策略中进行搜索，同时保留整个策略集的保证满足概率。特别地，我们采用模型预测控制（MPC）来最小化一个与抽象中考虑的控制规范无关的目标代价函数。我们的实验表明，与最先进的单一策略抽象技术相比，我们的方法在保证性能略有下降的情况下，获得了更好的控制性能。

## Abstract
The deployment of autonomous systems in safety-critical environments requires control policies that guarantee satisfaction of complex control specifications. These systems are commonly modeled as nonlinear discrete-time stochastic systems. A~popular approach to computing a policy that provably satisfies a complex control specification is to construct a finite-state abstraction, often represented as a Markov decision process (MDP) with intervals of transition probabilities, i.e., an interval MDP (IMDP). However, existing abstraction techniques compute a \emph{single policy}, thus leaving no room for online cost or performance optimization, e.g., of energy consumption. To overcome this limitation, we propose a novel IMDP abstraction technique that yields a \emph{set of policies}, each of which satisfies the control specification with a certain minimum probability. We can thus use any online control algorithm to search through this set of verified policies while retaining the guaranteed satisfaction probability of the entire policy set. In particular, we employ model predictive control (MPC) to minimize a desired cost function that is independent of the control specification considered in the abstraction. Our experiments demonstrate that our approach yields better control performance than state-of-the-art single-policy abstraction techniques, with a small degradation of the guarantees.

---

## 论文详细总结（自动生成）

以下是对论文《Temporal Logic Control of Nonlinear Stochastic Systems with Online Performance Optimization》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**非线性离散时间随机系统**在执行复杂任务（如时序逻辑规范）时的控制问题。
*   **研究动机**：现有的形式化抽象技术（如将系统抽象为区间马尔可夫决策过程 IMDP）虽然能提供逻辑规范满足概率的严谨保证，但通常只能生成单一的离散策略，缺乏灵活性。在实际应用中，系统往往还需要在满足逻辑任务的同时，在线优化其他性能指标（如最小化能耗或控制努力）。
*   **核心问题**：如何在保证时序逻辑规范（如 Reach-Avoid 任务）满足概率不低于给定阈值 $\lambda$ 的前提下，实现控制性能的在线优化？

### 2. 方法论：核心思想与关键技术
论文提出了一种结合**离线形式化抽象**与**在线模型预测控制（MPC）**的分层框架：
*   **离线阶段（新型 IMDP 抽象）**：
    *   **集合值接口（Set-Valued Interface）**：这是本文的核心创新。传统方法将抽象动作映射为单一控制输入，而本文将其映射为输入空间中的一个集合（如 $L_p$ 球体）。
    *   **概率交替仿真关系（PASR）**：证明了这种基于集合的抽象与原始系统之间存在 PASR 关系。这意味着只要在线控制器从该集合中挑选动作，就能保留 IMDP 计算出的概率保证。
    *   **策略合成**：利用鲁棒价值迭代（Robust Value Iteration）在 IMDP 上计算出一个“允许策略集”，该集合定义了每个状态下可选的输入范围。
*   **在线阶段（抽象驱动的 MPC）**：
    *   **逻辑驱动的 MPC**：将 MPC 的搜索空间限制在离线阶段确定的允许输入集合内。
    *   **混合整数规划（MIQP）**：为了处理非线性动力学和分段常数策略带来的非凸性，利用分段线性（PWA）近似动力学，将在线优化问题转化为混合整数二次规划（MIQP）进行高效求解。

### 3. 实验设计
论文在三个经典的控制基准场景上进行了验证：
*   **实验场景**：
    1.  **双积分器（Double Integrator）**：基础线性随机系统。
    2.  **登山车（Mountain Car）**：具有非线性动力学的经典强化学习/控制基准。
    3.  **Dubins 车（Dubins Car）**：具有非线性转向动力学和随机扰动的复杂模型。
*   **Benchmark（对比方法）**：
    *   **Vanilla IMDP**：最先进的单策略抽象技术（不具备在线优化能力）。
*   **评估指标**：逻辑规范满足概率的下界 $\lambda$、平均运行代价 $J$（包括状态误差和控制努力）、计算耗时。

### 4. 资源与算力
*   **硬件环境**：Intel i7-1185G7 CPU，16 GB RAM 笔记本电脑。
*   **软件工具**：使用 **Storm** 概率模型检查器进行离线策略合成，使用 **Gurobi** 求解在线 MIQP 优化问题。
*   **耗时**：
    *   离线抽象时间：从几秒到 2500 秒不等（取决于状态空间划分精度）。
    *   在线 MPC 单步求解时间：双积分器约 0.2-0.7s，登山车约 7s，Dubins 车约 4-5s。

### 5. 实验数量与充分性
*   **实验规模**：针对每个基准场景，论文测试了不同的输入集合半径 $\epsilon$ 和 MPC 预测时域 $N$。
*   **模拟次数**：每种配置均进行了 **100 次随机闭环模拟**，以获取统计意义上的平均代价。
*   **充分性评价**：实验设计较为充分，涵盖了从简单线性到复杂非线性的系统。通过改变 $\epsilon$ 的大小，客观地展示了“性能优化”与“安全保证”之间的权衡（Trade-off），验证了方法的鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：与单策略基准相比，该方法显著降低了控制代价。例如在登山车实验中，控制努力（能耗）降低了 **61.4%**。
*   **概率保证**：在线 MPC 优化虽然会略微降低逻辑规范的满足概率下界 $\lambda$，但这种降低是可控且可预测的。
*   **“拐点”效应**：实验发现存在一个最优的集合半径 $\epsilon$。在此半径内，性能优化收益巨大，而安全概率损失极小；一旦超过该阈值，安全概率会剧烈下降。

### 7. 优点
*   **灵活性与严谨性结合**：首次在基于抽象的随机控制框架中引入了如此高自由度的在线优化机制。
*   **理论完备**：扩展了 PASR 理论，为集合值接口提供了坚实的数学证明，确保了形式化保证在优化过程中不丢失。
*   **通用性**：该框架不依赖于特定的代价函数，可以根据需求灵活更换优化目标（如能耗、时间、平滑度等）。

### 8. 不足与局限
*   **计算复杂度**：在线求解 MIQP 具有挑战性。随着状态空间划分变细，二进制变量增加，单步求解时间（如登山车的 7s）可能无法满足高频实时控制的需求。
*   **近似误差**：MPC 使用了 PWA 近似动力学，虽然论文提到即使 MIQP 无解也可以退回到安全动作，但这种近似可能导致在线性能无法达到理论最优。
*   **维数灾难**：基于网格划分的抽象方法依然受限于状态空间维度，难以直接扩展到极高维度的系统（如 10 维以上）。

（完）
