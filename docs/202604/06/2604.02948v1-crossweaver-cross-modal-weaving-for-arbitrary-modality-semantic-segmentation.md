---
title: "CrossWeaver: Cross-modal Weaving for Arbitrary-Modality Semantic Segmentation"
title_zh: CrossWeaver：面向任意模态语义分割的跨模态编织
authors: "Zelin Zhang, Kedi Li, Huiqi Liang, Tao Zhang, Chuanzhi Xu"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02948v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 适用于任意模态语义分割的多模态融合框架
tldr: 针对多模态语义分割中融合策略灵活性差、跨模态协调效率低的问题，本文提出CrossWeaver框架。该框架通过模态交互块（MIB）在编码器中实现选择性且感知可靠性的跨模态交互，并利用轻量级的缝隙对齐融合（SAF）模块聚合增强特征。CrossWeaver在多个基准测试上达到了SOTA性能，具有极少的额外参数量，且对未见过的模态组合表现出强大的泛化能力。
selection_source: fresh_fetch
motivation: 现有方法往往依赖特定模态的适配或松散的耦合策略，难以在高效信息交换与保留各模态独特特征之间取得平衡。
method: 提出包含模态交互块（MIB）和轻量级缝隙对齐融合（SAF）模块的框架，实现编码器内的可靠性感知交互与特征聚合。
result: 在多个多模态语义分割基准上取得了最先进的性能，同时保持了极低的参数增量和对未知模态组合的强泛化性。
conclusion: CrossWeaver为任意模态语义分割提供了一个简单、高效且通用的融合方案，显著提升了跨模态协作的效果。
---

## 摘要
多模态语义分割在利用不同传感模态间的互补信息方面展现出巨大潜力。然而，现有方法通常依赖于精心设计的融合策略，这些策略要么使用特定模态的自适应，要么依赖于松耦合的交互，从而限制了灵活性并导致跨模态协调效果欠佳。此外，这些方法往往难以在不同模态组合中平衡高效的信息交换与保留各模态的独特特征。为应对这些挑战，我们提出了 CrossWeaver，这是一个简单而有效的面向任意模态语义分割的多模态融合框架。其核心是一个模态交互块（MIB），它能够在编码器内实现选择性且具备可靠性感知的跨模态交互，同时通过轻量级的缝隙对齐融合（SAF）模块进一步聚合增强后的特征。在多个多模态语义分割基准测试上的广泛实验表明，我们的框架以极少的额外参数实现了最先进的性能，并对未见过的模态组合具有强大的泛化能力。

## Abstract
Multimodal semantic segmentation has shown great potential in leveraging complementary information across diverse sensing modalities. However, existing approaches often rely on carefully designed fusion strategies that either use modality-specific adaptations or rely on loosely coupled interactions, thereby limiting flexibility and resulting in less effective cross-modal coordination. Moreover, these methods often struggle to balance efficient information exchange with preserving the unique characteristics of each modality across different modality combinations. To address these challenges, we propose CrossWeaver, a simple yet effective multimodal fusion framework for arbitrary-modality semantic segmentation. Its core is a Modality Interaction Block (MIB), which enables selective and reliability-aware cross-modal interaction within the encoder, while a lightweight Seam-Aligned Fusion (SAF) module further aggregates the enhanced features. Extensive experiments on multiple multimodal semantic segmentation benchmarks demonstrate that our framework achieves state-of-the-art performance with minimal additional parameters and strong generalization to unseen modality combinations.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CrossWeaver** 的新型多模态语义分割框架，旨在解决现有方法在处理任意模态组合时的灵活性不足、鲁棒性差以及对 RGB 模态过度依赖的问题。

以下是对该论文的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：语义分割是自动驾驶和机器人感知的核心任务。虽然引入深度、红外、激光雷达（LiDAR）等辅助模态能提升性能，但现有融合策略（如基于映射、提示或交换的方法）往往采用固定的融合规则或均匀的信息交换。
*   **核心问题**：
    1.  **缺乏选择性**：现有方法难以区分有益信息与噪声，尤其是在传感器质量波动或模态缺失时。
    2.  **RGB 中心化**：设计往往偏向 RGB 流，限制了模型在非 RGB 组合下的灵活性。
    3.  **模态不一致性**：空间对齐不良和异构特征分布会导致物体边缘分割模糊。
*   **研究意义**：提出一种轻量级、模块化且能感知可靠性的框架，实现对任意模态组合的高效编织（Weaving）。

### 2. 方法论：核心思想与技术细节
CrossWeaver 采用共享权重的分层编码器（基于 SegFormer），并插入两个关键模块：

*   **模态交互块 (MIB, Modality Interaction Block)**：
    *   **可靠性校准**：利用全局描述符和 MLP 生成模态权重和 Token 覆盖率，通过 Top-p 掩码过滤掉不可信的区域。
    *   **跨模态交互**：在过滤后的特征上执行跨注意力（Cross-Attention），并引入多尺度网格池化以捕捉不同尺度的依赖关系。
    *   **一致性过滤**：计算目标模态与交互信息之间的余弦相似度，动态抑制不一致或嘈杂的跨模态消息。
    *   **Token 混合与细化**：学习 Token 级别的混合系数，并通过源条件亲和（SCA）适配器进行残差细化。
*   **缝隙对齐融合 (SAF, Seam-Aligned Fusion)**：
    *   **局部一致性增强**：使用多分支深度卷积（3x3, 5x5, 7x7）和通道注意力，增强模态交界处的空间连贯性。
    *   **模态加权残差**：通过自适应权重平衡融合特征与原始模态特征，保留细粒度的模态特有细节。

### 3. 实验设计
*   **数据集**：
    *   **MCubeS**：多模态材料分割数据集，包含 RGB、近红外（NIR）、偏振度（DoLP/AoLP）。
    *   **DeLiVER**：大规模仿真数据集，包含 RGB、深度、事件流、LiDAR，涵盖多种恶劣天气和传感器故障场景。
*   **Benchmark 与对比方法**：
    *   对比了 **CMNeXt**、**MMSFormer**、**StitchFusion** 等当前最先进（SOTA）的多模态融合模型。
    *   测试场景包括标准全模态输入以及**任意模态子集**（测试缺失模态下的鲁棒性）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA RTX 4090 GPU**。
*   **训练细节**：采用 AdamW 优化器，Batch Size 为每张显卡 4（总计 32），训练 200 个 Epoch，包含 10 个 Epoch 的预热期。
*   **主干网络**：统一使用 MiT-B0 作为 Encoder，以确保对比的公平性。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的定量对比实验（表 1-3）和消融实验（表 4-5）。
*   **充分性**：
    *   **多模态组合测试**：在 MCubeS 上测试了 15 种模态组合，在 DeLiVER 上测试了 15 种组合，验证了模型在任意输入下的稳定性。
    *   **消融实验**：分别验证了 MIB、SAF 模块的有效性，以及 MIB 插入不同编码器阶段的影响。
    *   **定性分析**：提供了特征图可视化和不同天气条件下的分割效果图。
*   **客观性**：所有对比方法均在相同的主干网络和数据增强条件下运行，实验设计严谨。

### 6. 主要结论与发现
*   **性能领先**：在 MCubeS 上达到 48.76% mIoU，在 DeLiVER 上达到 63.85% mIoU，均刷新了 SOTA 记录。
*   **极强鲁棒性**：在缺失模态测试中，CrossWeaver 的平均 mIoU 显著高于竞争对手（在 DeLiVER 上比 StitchFusion 高出 3.74%，比 CMNeXt 高出 19.69%）。
*   **优雅降级**：可视化结果显示，当某些传感器失效时，模型能自动利用剩余模态维持全局语义结构，表现出“优雅降级”特性。

### 7. 优点与亮点
*   **选择性交互**：MIB 模块通过可靠性评估解决了“盲目融合”的问题，能有效抑制噪声。
*   **参数高效**：作为轻量级插件，CrossWeaver 在提升性能的同时，额外增加的参数量和计算开销极小。
*   **通用性强**：不依赖于特定的 RGB 模态，能够处理从单模态到四模态的任意组合，具有很强的实际应用潜力。

### 8. 不足与局限
*   **主干网络限制**：实验主要集中在轻量级的 MiT-B0 上，虽然证明了效率，但尚未充分展示在大规模模型（如 ViT-L 或 InternVideo）上的扩展性表现。
*   **实时性未详述**：虽然提到了轻量级，但未给出具体的推理延迟（Latency）数据，这对于自动驾驶等实时应用至关重要。
*   **动态场景挑战**：尽管在 DeLiVER 仿真数据上表现良好，但在真实世界极端动态环境（如高速运动下的传感器异步）中的表现仍需进一步验证。

（完）
