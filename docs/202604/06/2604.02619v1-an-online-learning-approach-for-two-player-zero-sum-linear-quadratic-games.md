---
title: An Online Learning Approach for Two-Player Zero-Sum Linear Quadratic Games
title_zh: 两人零和线性二次博弈的一种在线学习方法
authors: "Shanting Wang, Weihao Sun, Andreas A. Malikopoulos"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02619v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 具有未知动力学的双人零和线性二次博弈
tldr: 本文研究动力学未知的双人零和线性二次博弈问题，提出一种结合正则化最小二乘估计、高概率置信集与代理模型选择的在线学习框架。通过在每轮引入收缩步骤，确保代理模型在广义代数Riccati方程下存在稳定的鞍点解，从而实现策略的持续更新。研究不仅提供了算法收敛性的遗憾分析，还通过数值仿真验证了其在未知环境下的收敛性能与理论分析的一致性。
selection_source: fresh_fetch
motivation: 旨在解决动力学参数未知情况下，双人零和线性二次博弈的在线策略优化与系统稳定性保障问题。
method: 采用正则化最小二乘法估计模型，并利用收缩步骤在置信区间内寻找能使广义代数Riccati方程产生稳定鞍点解的代理模型。
result: 理论上推导出了算法的遗憾界，并通过数值实验展示了良好的收敛表现。
conclusion: 该方法为未知动力学下的零和博弈提供了有效的在线学习方案，平衡了模型估计精度与控制策略的稳定性。
---

## 摘要
本文针对动力学未知的两人零和线性二次博弈，提出了一种在线学习方法。我们开发了一个结合正则化最小二乘模型估计、高概率置信集和代理模型选择的框架，以维持用于策略更新的正规模型。我们在每一轮中应用收缩步骤，在广义代数Riccati方程存在稳定鞍点解的区域内识别代理模型。随后，我们对算法收敛性进行了遗憾分析，并通过数值算例说明了收敛性能并验证了遗憾分析的结果。

## Abstract
In this paper, we present an online learning approach for two-player zero-sum linear quadratic games with unknown dynamics. We develop a framework combining regularized least squares model estimation, high probability confidence sets, and surrogate model selection to maintain a regular model for policy updates. We apply a shrinkage step at each episode to identify a surrogate model in the region where the generalized algebraic Riccati equation admits a stabilizing saddle point solution. We then establish regret analysis on algorithm convergence, followed by a numerical example to illustrate the convergence performance and verify the regret analysis.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：两人零和线性二次博弈的在线学习方法

#### 1. 核心问题与整体含义（研究动机和背景）
本文探讨了在**系统动力学未知**的情况下，如何解决**两人零和线性二次（LQ）博弈**的在线学习问题。
*   **研究动机**：线性二次博弈是建模网络实体系统（如多机器人协作、自动驾驶、安全关键控制）中竞争行为的标准框架。传统方法依赖于精确的动力学模型（$A, B_1, B_2$ 矩阵），但在实际应用中这些参数往往不可知。
*   **核心挑战**：与单智能体 LQR 问题不同，零和博弈涉及对抗性交互，且两个玩家的策略通过平衡结构耦合。在学习过程中，必须同时保证**系统稳定性**、**博弈可解性**（GARE 方程有解）以及**低遗憾度（Regret）**。

#### 2. 方法论：核心思想与技术细节
论文提出了一种**认证在线学习算法（Certified Online Learning Algorithm）**，其核心流程如下：
*   **参数估计与置信区间**：利用正则化最小二乘法（Ridge Regression）从观测到的状态和控制数据中估计系统参数 $\Theta = [A, B_1, B_2]$。基于自归一化鞅集中不等式，构建一个高概率包含真实参数的置信椭球 $\mathcal{C}_k(\delta)$。
*   **认证代理模型选择（核心创新点）**：由于原始估计值可能无法保证广义代数 Riccati 方程（GARE）有稳定解，算法引入了**收缩步骤（Shrinkage Step）**。在当前估计值与前一轮“安全”模型之间进行线性搜索，寻找一个既在置信区间内、又满足稳定性边缘和 GARE 可解性条件的代理模型 $\tilde{\theta}_k$。
*   **策略更新与倍增策略**：仅在设计矩阵的行列式翻倍时（Doubling Trick）才更新策略，以减少计算开销。通过求解代理模型的 GARE 获得反馈增益 $(K_k, L_k)$。
*   **探索机制**：在控制输入中注入衰减的扰动信号（探索噪声），以满足持续激发（Persistent Excitation）条件，确保数据能够持续改进模型估计。
*   **遗憾分析**：通过将遗憾分解为瞬态成本、探索成本和策略偏差，证明了该算法可实现 **$O(\sqrt{T})$** 的次线性遗憾界。

#### 3. 实验设计
*   **场景设定**：采用了一个离散时间线性系统数值算例，状态维度为 3，控制输入维度分别为 1（玩家 1）和 1（玩家 2）。
*   **Benchmark（基准）**：以已知真实动力学参数时计算出的真实纳什均衡（NE）解和最优平均成本 $J^*$ 作为基准。
*   **对比指标**：
    1.  模型估计误差 $\|\tilde{\theta}_k - \theta^*\|_2$。
    2.  反馈增益误差 $\|K_k - K^*\|_F$ 和 $\|L_k - L^*\|_F$。
    3.  归一化遗憾值 $Reg(T)/\sqrt{T}$ 的演变趋势。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件资源（如 GPU/CPU 型号）或训练时长。由于该算法涉及的是矩阵运算和代数 Riccati 方程求解，通常在标准工作站或个人电脑上即可运行，不涉及大规模深度学习所需的算力。

#### 5. 实验数量与充分性
*   **实验规模**：论文提供了一组详尽的数值仿真实验，运行步长为 $T=50,000$。
*   **充分性评价**：实验展示了参数估计、增益收敛以及遗憾度随时间变化的曲线。虽然实验场景较为单一（仅一个 3 维系统），但其主要目的是**验证理论推导的正确性**（如 $O(\sqrt{T})$ 遗憾界和稳定性保证），而非测试算法在复杂环境下的泛化性。对于理论贡献型论文，该实验设计是基本充分且客观的。

#### 6. 主要结论与发现
*   **理论验证**：证明了在未知动力学下，通过认证的代理模型选择，可以保证在线学习过程中的系统稳定性。
*   **收敛性**：实验结果证实模型估计误差和反馈增益误差随时间显著下降，最终趋于真实纳什均衡。
*   **遗憾表现**：实测的归一化遗憾曲线趋于平稳，验证了理论上的次线性遗憾界 $O(\sqrt{T})$。
*   **安全性**：收缩步骤有效地在探索（寻找更好模型）和利用（保持系统稳定）之间取得了平衡。

#### 7. 优点
*   **理论严谨性**：提供了完整的收敛性证明和遗憾界分析，填补了零和 LQ 博弈在线学习理论的部分空白。
*   **稳定性保障**：通过“认证代理模型”机制，解决了在线学习中可能出现的 GARE 无解或系统失稳的风险。
*   **计算效率**：采用倍增策略（Doubling Trick）触发更新，避免了频繁的矩阵求逆和 Riccati 方程求解。

#### 8. 不足与局限
*   **持续激发假设**：算法依赖于“持续激发”假设（Assumption 2），即要求系统状态和输入具有足够的变异性，这在某些受限的实际场景中可能难以完全满足。
*   **实验复杂度较低**：仅在低维线性系统上进行了验证，未在具有高维状态空间或非线性扰动的复杂网络实体系统（如大规模无人机群）中进行测试。
*   **离散时间限制**：目前框架仅限于离散时间系统，扩展到连续时间系统可能需要处理更复杂的微分 Riccati 方程。

（完）
