---
title: "ExploreVLA: Dense World Modeling and Exploration for End-to-End Autonomous Driving"
title_zh: ExploreVLA：面向端到端自动驾驶的稠密世界建模与探索
authors: "Zihao Sheng, Xin Ye, Jingru Luo, Sikai Chen, Liu Ren"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02714v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于自动驾驶和强化学习探索的视觉-语言-动作 (VLA) 架构
tldr: 本研究针对端到端自动驾驶中模仿学习泛化性差的问题，提出了ExploreVLA框架。该框架结合了视觉语言动作（VLA）架构与稠密世界模型，通过生成未来RGB和深度图像来增强环境理解。利用预测不确定性作为内在奖励，引导模型在安全前提下探索分布外场景，并采用GRPO算法优化策略。实验证明，该方法在NAVSIM和nuScenes基准上达到了SOTA水平，显著提升了驾驶策略的鲁棒性。
selection_source: fresh_fetch
motivation: 传统的模仿学习限制了自动驾驶模型在分布外场景的探索能力，导致其在面对新环境时表现脆弱。
method: 提出一种统一的理解与生成框架，通过预测未来图像和深度图进行稠密世界建模，并利用预测不确定性作为内在奖励，结合GRPO算法进行策略优化。
result: 在NAVSIM基准测试中取得了93.7的PDMS评分和88.8的EPDMS评分，刷新了当前最先进的性能记录。
conclusion: 结合稠密世界建模与主动探索机制能有效提升VLA模型在复杂驾驶环境中的表征能力和决策鲁棒性。
---

## 摘要
基于视觉-语言-动作（VLA）架构的端到端自动驾驶模型通过对专家演示进行行为克隆来学习驾驶策略，已取得了显著成果。然而，模仿学习本质上限制了模型仅能复制观察到的行为，而无法探索多样化的驾驶策略，导致其在面对新颖或分布外（OOD）场景时表现脆弱。强化学习（RL）通过允许策略在专家分布之外进行探索，提供了一种自然的补救方案。然而，通常在离线数据集上训练的 VLA 模型缺乏直接可观测的状态转移，因此需要一个学习到的世界模型来预测动作后果。在这项工作中，我们提出了一个统一的理解与生成框架，利用世界建模同时实现有意义的探索并提供稠密监督。具体而言，我们将轨迹预测与未来的 RGB 和深度图像生成相结合，作为稠密世界建模目标，要求模型学习细粒度的视觉和几何表示，从而显著增强规划骨干网络。除了作为监督信号外，世界模型还进一步作为策略探索的内在奖励来源：其图像预测的不确定性自然地衡量了轨迹相对于训练分布的新颖性，其中高不确定性表示分布外场景，如果安全，则代表了宝贵的学习机会。我们将这种探索信号整合到安全门控奖励中，并通过群体相对策略优化（GRPO）对策略进行优化。在 NAVSIM 和 nuScenes 基准测试上的实验证明了我们方法的有效性，在 NAVSIM 上实现了 93.7 的 SOTA PDMS 分数和 88.8 的 EPDMS 分数。代码和演示将在 https://zihaosheng.github.io/ExploreVLA/ 公开。

## Abstract
End-to-end autonomous driving models based on Vision-Language-Action (VLA) architectures have shown promising results by learning driving policies through behavior cloning on expert demonstrations. However, imitation learning inherently limits the model to replicating observed behaviors without exploring diverse driving strategies, leaving it brittle in novel or out-of-distribution scenarios. Reinforcement learning (RL) offers a natural remedy by enabling policy exploration beyond the expert distribution. Yet VLA models, typically trained on offline datasets, lack directly observable state transitions, necessitating a learned world model to anticipate action consequences. In this work, we propose a unified understanding-and-generation framework that leverages world modeling to simultaneously enable meaningful exploration and provide dense supervision. Specifically, we augment trajectory prediction with future RGB and depth image generation as dense world modeling objectives, requiring the model to learn fine-grained visual and geometric representations that substantially enrich the planning backbone. Beyond serving as a supervisory signal, the world model further acts as a source of intrinsic reward for policy exploration: its image prediction uncertainty naturally measures a trajectory's novelty relative to the training distribution, where high uncertainty indicates out-of-distribution scenarios that, if safe, represent valuable learning opportunities. We incorporate this exploration signal into a safety-gated reward and optimize the policy via Group Relative Policy Optimization (GRPO). Experiments on the NAVSIM and nuScenes benchmarks demonstrate the effectiveness of our approach, achieving a state-of-the-art PDMS score of 93.7 and an EPDMS of 88.8 on NAVSIM. The code and demo will be publicly available at https://zihaosheng.github.io/ExploreVLA/.

---

## 论文详细总结（自动生成）

### ExploreVLA：面向端到端自动驾驶的稠密世界建模与探索

#### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的端到端自动驾驶模型（如 VLA 架构）主要依赖**模仿学习（行为克隆）**。这种方式存在两个核心瓶颈：
    1.  **缺乏探索能力**：模型仅能复制专家行为，在面对分布外（OOD）或新颖场景时表现脆弱。
    2.  **监督信号稀疏**：仅依靠文本指令和轨迹坐标点进行训练，无法捕捉驾驶场景中复杂的空间几何和细粒度视觉特征。
*   **核心目标**：通过引入**稠密世界建模（Dense World Modeling）**，利用预测未来图像的不确定性作为内在奖励，引导模型在安全的前提下进行主动探索，并增强其对环境的表征能力。

#### 2. 论文提出的方法论
*   **统一架构**：基于 **Show-o** 多模态模型，结合了 Phi-1.5（语言模型）和 MAGVIT-v2（图像分词器）。模型同时处理文本、图像和车辆状态，输出轨迹预测及未来图像。
*   **稠密世界建模（监督增强）**：
    *   模型不仅预测轨迹，还被要求生成未来的 **RGB 图像**和**深度图（Depth Maps）**。
    *   RGB 生成提供视觉语义监督，深度生成提供 3D 几何结构监督，从而迫使骨干网络学习更丰富的环境特征。
*   **基于不确定性的探索机制（内在奖励）**：
    *   **新颖性度量**：利用世界模型预测未来图像时的**信息熵（Entropy）**来衡量轨迹的新颖性。高熵意味着该轨迹导致的后果是模型未曾见过的（分布外）。
    *   **安全门控奖励**：设计复合奖励函数 $R = PDMS + \lambda \cdot \text{bonus}$。只有当轨迹满足基本安全阈值（PDMS 分数达标）时，才会发放基于新颖性的探索奖励，避免模型探索危险行为。
*   **策略优化**：采用**群体相对策略优化（GRPO）**算法。通过对同一场景采样多条轨迹并进行组内相对排名，优化策略以发现既安全又具有新颖性的驾驶方案。

#### 3. 实验设计
*   **数据集**：
    *   **NAVSIM (v1 & v2)**：目前最先进的非反应式自动驾驶仿真基准，强调闭环指标的关联性。
    *   **nuScenes**：标准的开环预测基准。
*   **Benchmark 与指标**：
    *   主要指标为 **PDMS**（预测驾驶模型分数）和 **EPDMS**（扩展 PDMS），综合考量碰撞、舒适度、进度等。
    *   对比方法包括：TransFuser、Hydra-MDP、DriveSuprim、AutoVLA、DriveVLA-W0 等 SOTA 模型。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA H200 GPU**。
*   **训练阶段**：
    1.  **第一阶段（模仿学习）**：预训练 10 个 epoch（仅图像生成），随后进行 15 个 epoch 的联合监督微调（SFT）。
    2.  **第二阶段（强化学习）**：使用 LoRA 技术进行 5 个 epoch 的 GRPO 后训练。
*   **超参数**：GRPO 组大小 $G=8$，LoRA rank=32。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流数据集上进行了详尽测试，并提供了大量的定性可视化结果。
*   **消融实验**：
    *   验证了 RGB 生成和深度生成各自对性能的提升作用。
    *   验证了 PDMS 奖励与图像探索奖励结合的有效性。
*   **充分性评价**：实验设计较为全面，涵盖了从开环到闭环指标的对比，且通过消融实验清晰地展示了各模块的贡献，实验结果具有较高的说服力和客观性。

#### 6. 论文的主要结论与发现
*   **性能突破**：ExploreVLA 在 NAVSIM v1 上达到了 **93.7 PDMS**，在 v2 上达到了 **88.8 EPDMS**，均刷新了 SOTA 记录。
*   **探索的价值**：通过强化学习阶段的探索，模型能够纠正模仿学习中常见的安全隐患（如闯红灯、碰撞风险），学习到比专家演示更稳健的策略。
*   **表征增强**：引入深度图生成作为辅助任务，显著提升了模型对空间布局的理解，从而提高了规划精度。

#### 7. 优点（亮点）
*   **创新的探索信号**：巧妙地将世界模型的“预测不确定性”转化为强化学习的“内在奖励”，解决了自动驾驶中 OOD 探索难的问题。
*   **稠密监督**：通过 RGB+Depth 的双重生成任务，弥补了传统 VLA 模型监督信号稀疏的缺陷。
*   **高效优化**：引入 GRPO 算法，相比传统的 PPO 更加高效且适合大模型后训练。

#### 8. 不足与局限
*   **传感器覆盖**：目前主要基于单目（前视）相机，尚未完全扩展到全景多相机融合，这可能限制其在复杂路口转弯时的感知范围。
*   **计算开销**：尽管使用了 LoRA，但生成未来图像和深度图在推理/训练时仍会增加额外的计算负担。
*   **仿真局限**：NAVSIM 虽然是强相关的非反应式仿真，但仍与真实的闭环动态环境存在一定差距。

（完）
