---
title: "Open-Loop Planning, Closed-Loop Verification: Speculative Verification for VLA"
title_zh: 开环规划，闭环验证：针对 VLA 的投机验证
authors: "Zihua Wang, Zhitao Lin, Ruibo Li, Yu Zhang, Xu Yang, Siya Mi, Xiu-Shen Wei"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02965v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 机器人领域视觉-语言-动作（VLA）模型的投机验证
tldr: 针对VLA模型推理成本高且开环动作分块执行缺乏鲁棒性的问题，本文提出SV-VLA框架。该框架利用重型VLA作为宏观规划器生成动作序列，并引入轻量级验证器进行实时闭环监控。验证器根据当前观测和规划上下文，仅在必要时触发重新规划。这种投机验证机制在保持高效推理的同时，显著提升了系统在动态环境中的可靠性。
selection_source: fresh_fetch
motivation: 现有的VLA动作分块方法虽能降低计算开销，但因缺乏闭环反馈，在动态环境中容易出现误差累积和执行失败。
method: 提出一种投机验证框架，由大型VLA生成动作块及上下文，并由轻量级验证器实时对比计划动作与参考动作以决定是否重规划。
result: 实验结果表明，SV-VLA在动态任务中成功结合了分块预测的高效性与闭环控制的鲁棒性，实现了更可靠的机器人操控。
conclusion: SV-VLA通过按需重规划的策略，有效解决了VLA模型在效率与闭环稳定性之间的权衡问题。
---

## 摘要
视觉-语言-动作（VLA）模型作为具身控制的大型基础模型，在操作任务中展现出强大的性能。然而，其性能是以高昂的推理成本为代价的。为了提高效率，最近的方法采用了动作分块（action chunking），即预测一系列未来动作以进行开环执行。尽管开环执行在减少计算量方面行之有效，但由于缺乏闭环反馈，它对环境变化非常敏感，且容易产生误差累积。为了解决这一局限性，我们提出了针对 VLA 控制的投机验证（SV-VLA），该框架将高效的开环长程规划与轻量级闭环在线验证相结合。具体而言，SV-VLA 使用重型 VLA 作为低频宏观规划器，生成动作分块及规划上下文，而轻量级验证器则根据最新观测结果持续监控执行过程。在当前观测和规划上下文的约束下，验证器将规划动作与闭环参考动作进行对比，并仅在必要时触发重新规划。实验表明，SV-VLA 结合了分块预测的高效性与闭环控制的鲁棒性，能够在动态环境中实现高效且可靠的基于 VLA 的控制。代码见：https://github.com/edsad122/SV-VLA。

## Abstract
Vision-Language-Action (VLA) models, as large foundation models for embodied control, have shown strong performance in manipulation tasks. However, their performance comes at high inference cost. To improve efficiency, recent methods adopt action chunking, which predicts a sequence of future actions for open-loop execution. Although effective for reducing computation, open-loop execution is sensitive to environmental changes and prone to error accumulation due to the lack of close-loop feedback. To address this limitation, we propose Speculative Verification for VLA Control (SV-VLA), a framework that combines efficient open-loop long-horizon planning with lightweight closed-loop online verification. Specifically, SV-VLA uses a heavy VLA as a low-frequency macro-planner to generate an action chunk together with a planning context, while a lightweight verifier continuously monitors execution based on the latest observations. Conditioned on both the current observation and the planning context, the verifier compares the planned action against a closed-loop reference action and triggers replanning only when necessary. Experiments demonstrate that SV-VLA combines the efficiency of chunked prediction with the robustness of closed-loop control, enabling efficient and reliable VLA-based control in dynamic environments. Code is available: https://github.com/edsad122/SV-VLA.

---

## 论文详细总结（自动生成）

这是一份关于论文《Open-Loop Planning, Closed-Loop Verification: Speculative Verification for VLA》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）虽然强大，但推理延迟极高，难以实现高频闭环控制。
*   **研究背景**：
    *   **动作分块（Action Chunking）**：现有方法通过一次预测多个动作（开环执行）来降低推理频率，但在动态环境中容易因环境变化或误差累积导致任务失败。
    *   **投机采样（Speculative Decoding）**：NLP 中的投机采样依赖于固定的文本上下文，而机器人任务中未来的“上下文”（即环境观测）取决于动作的执行结果，因此无法直接套用 NLP 的并行验证机制。
*   **研究动机**：开发一种既能享受动作分块的高效性，又能具备闭环控制鲁棒性的框架。

### 2. 方法论：SV-VLA 框架
核心思想是“**昂贵但低频的规划，廉价但高频的验证**”。
*   **宏观规划器（Macro-Planner）**：使用重型 VLA 模型（如 OpenVLA-OFT）。在规划边界，它接收当前观测、指令和状态，输出一个动作块（Action Chunk）以及一个**规划上下文特征（Context Feature）**。
*   **轻量级验证器（Lightweight Verifier）**：由微型视觉骨干网络（如 ViT-Tiny）和浅层 MLP 组成。它在每个控制步运行，融合当前最新的视觉观测和宏观规划器提供的上下文特征，预测一个“参考动作”。
*   **基于偏差的重规划机制（Deviation-based Replanning）**：
    1.  计算当前计划执行的动作与验证器预测的参考动作之间的 L1 距离。
    2.  如果偏差超过预设阈值 $\tau$，则认为当前计划已失效。
    3.  **触发重规划**：丢弃剩余动作块，调用重型 VLA 重新生成规划。
    4.  如果偏差在范围内，则继续执行原计划，保持高效。

### 3. 实验设计
*   **数据集与场景**：使用 **LIBERO** 机器人操控基准测试，包含三个子任务库：
    *   **LIBERO-Goal**：考察目标达成能力。
    *   **LIBERO-Spatial**：考察空间关系处理。
    *   **LIBERO-Object**：考察对不同物体的操作。
*   **对比方法（Baselines）**：
    *   **BASE (K=8)**：短分块开环基准（高性能、低速度）。
    *   **BASE (K=64)**：长分块开环基准（低性能、高速度）。
    *   **Speculative Decoding (SpecVLA)**：传统的投机解码 VLA 变体。
*   **评估指标**：任务成功率（Success Rate）和推理加速比（Speed-up）。

### 4. 资源与算力
*   **训练算力**：使用了 **4 台 NVIDIA A100 GPU** 进行模型训练。
*   **推理/评估算力**：所有在线测试均在 **单台 NVIDIA V100 GPU** 上完成，以确保推理延迟对比的公平性。
*   **优化器**：使用 AdamW，学习率为 $8 \times 10^{-4}$，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：在 LIBERO 的三个子库上进行了广泛的在线测试。
*   **消融实验**：非常充分，涵盖了：
    *   **输入消融**：验证了“规划上下文特征”和“当前观测”对验证器的必要性。
    *   **机制消融**：对比了“无重规划”的情况（性能大幅下降）。
    *   **敏感度分析**：测试了不同重规划阈值 $\tau$ 对性能和速度的平衡影响。
*   **客观性**：通过对比不同分块长度的基准，客观展示了 SV-VLA 在效率与鲁棒性之间的折中优势。

### 6. 主要结论与发现
*   **性能提升**：相比于长分块开环基准（K=64），SV-VLA 将平均成功率从 **79.5% 提升至 90.9%**（提升了 11.4%）。
*   **效率优势**：SV-VLA 实现了 **2.17 倍** 的加速（相对于 K=8 基准），且成功率接近高频重规划的效果。
*   **验证器作用**：实验证明，验证器必须同时结合“全局规划意图”和“实时局部观测”才能有效检测执行偏差。
*   **重规划触发**：在复杂任务（如 LIBERO-Spatial）中，SV-VLA 能在检测到抓取失败等异常时及时止损并重规划。

### 7. 优点与亮点
*   **解耦设计**：将长程规划与实时监控解耦，解决了大模型推理慢与机器人实时性要求高的矛盾。
*   **训练高效**：重型 VLA 和视觉编码器在训练验证器时保持冻结，仅需训练极轻量的验证头，适配成本低。
*   **即插即用**：该框架理论上可以应用于任何现有的动作分块 VLA 模型。

### 8. 不足与局限
*   **阈值敏感性**：重规划阈值 $\tau$ 是手工设定的固定值，对于不同难度的任务可能需要手动调优，缺乏自适应性。
*   **纠错能力有限**：目前的验证器仅负责“触发重规划”，而不能直接提供微小的局部动作修正（Local Correction）。
*   **环境覆盖**：实验主要在模拟器（LIBERO）中进行，尚未在具有更复杂光照和物理干扰的真实世界硬件上进行大规模验证。

（完）
