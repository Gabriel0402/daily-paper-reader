---
title: Improving Role Consistency in Multi-Agent Collaboration via Quantitative Role Clarity
title_zh: 通过定量角色清晰度提升多智能体协作中的角色一致性
authors: "Guoling Zhou, Wenpei Han, Fengqin Yang, Li Wang, Yingcong Zhou, Zhiguo Fu"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02770v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 提升LLM驱动的多智能体系统中的角色一致性
tldr: 针对大语言模型多智能体协作中角色越权和职责不清的问题，本文提出了一种量化角色清晰度的方法。通过构建角色分配矩阵并计算行为轨迹与角色描述的对齐度，将其作为正则项引入轻量化微调过程。实验表明，该方法在ChatDev等系统中显著降低了角色越权率，提升了角色一致性及最终任务的成功率。
selection_source: fresh_fetch
motivation: 解决多智能体系统中智能体不遵循角色规范、容易产生角色混淆导致协作效率低下的核心痛点。
method: 提出角色清晰度矩阵，利用行为轨迹与角色描述的语义相似度构建正则项，在轻量化微调中优化角色一致性。
result: "实验显示角色越权率大幅下降（如Llama从43.4%降至0.2%），同时角色清晰度评分和任务成功率均有显著提升。"
conclusion: 量化角色清晰度并将其作为微调约束，是提升多智能体协作一致性和端到端任务性能的有效途径。
---

## 摘要
在大语言模型（LLM）驱动的多智能体系统中，违背角色规范（即未能遵守所分配角色的定义职责和约束，可能导致智能体表现得像另一个角色）是一种主要的失败模式 \cite{DBLP:journals/corr/abs-2503-13657}。为解决这一问题，本文提出了一种定量角色清晰度方法以提升角色一致性。首先，我们构建了一个角色分配矩阵 $S(φ)=[s_{ij}(φ)]$，其中 $s_{ij}(φ)$ 是第 $i$ 个智能体的行为轨迹与第 $j$ 个智能体的角色描述之间的语义相似度。随后，我们将角色清晰度矩阵 $M(φ)$ 定义为 $\text{softmax}(S(φ))-I$，其中 $\text{softmax}(S(φ))$ 是 $S(φ)$ 的行 Softmax，而 $I$ 是单位矩阵。$M(φ)$ 的 Frobenius 范数量化了智能体角色描述与其行为轨迹之间的对齐程度。此外，我们在轻量化微调过程中将角色清晰度矩阵作为正则化项，以提升角色一致性，从而提高端到端任务性能。在 ChatDev 多智能体系统上的实验表明，我们的方法显著提升了角色一致性和任务性能：在使用 Qwen 和 Llama 时，角色越权率分别从 $46.4\%$ 降至 $8.4\%$ 以及从 $43.4\%$ 降至 $0.2\%$；角色清晰度得分分别从 $0.5328$ 提升至 $0.9097$ 以及从 $0.5007$ 提升至 $0.8530$；任务成功率则分别从 $0.6769$ 提升至 $0.6909$ 以及从 $0.6174$ 提升至 $0.6763$。

## Abstract
In large language model (LLM)-driven multi-agent systems, disobey role specification (failure to adhere to the defined responsibilities and constraints of an assigned role, potentially leading to an agent behaving like another) is a major failure mode \cite{DBLP:journals/corr/abs-2503-13657}. To address this issue, in the present paper, we propose a quantitative role clarity to improve role consistency. Firstly, we construct a role assignment matrix $S(φ)=[s_{ij}(φ)]$, where $s_{ij}(φ)$ is the semantic similarity between the $i$-th agent's behavior trajectory and the $j$-th agent's role description. Then we define role clarity matrix $M(φ)$ as $\text{softmax}(S(φ))-I$, where $\text{softmax}(S(φ))$ is a row-wise softmax of $S(φ)$ and $I$ is the identity matrix. The Frobenius norm of $M(φ)$ quantifies the alignment between agents' role descriptions and their behaviors trajectory. Moreover, we employ the role clarity matrix as a regularizer during lightweight fine-tuning to improve role consistency, thereby improving end-to-end task performance. Experiments on the ChatDev multi-agent system show that our method substantially improves role consistency and task performance: with Qwen and Llama, the role overstepping rate decreases from $46.4\%$ to $8.4\%$ and from $43.4\%$ to $0.2\%$, respectively, and the role clarity score increases from $0.5328$ to $0.9097$ and from $0.5007$ to $0.8530$, respectively, the task success rate increases from $0.6769$ to $0.6909$ and from $0.6174$ to $0.6763$, respectively.

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Role Consistency in Multi-Agent Collaboration via Quantitative Role Clarity》的结构化分析报告：

### 1. 核心问题与研究背景
*   **核心问题**：在由大语言模型（LLM）驱动的多智能体系统（如 ChatDev）中，智能体经常出现**“角色不一致”**或**“角色越权”**（Role Overstepping）的现象。即智能体未能遵守分配给它的职责约束，转而执行了属于其他智能体的任务，这会导致协作混乱并降低任务成功率。
*   **研究背景**：现有的多智能体系统主要通过自然语言 Prompt 定义角色，这种方式是定性的、不可微的，且在长文本对话中容易失效。受社会心理学中“角色清晰度”概念的启发，作者试图将其引入 AI 领域，通过数学手段量化并优化智能体的行为边界。

### 2. 方法论：定量角色清晰度与正则化微调
*   **核心思想**：将智能体的“行为轨迹”与“角色描述”映射到同一语义空间，通过计算两者的对齐程度来量化角色清晰度，并将其作为损失函数的一部分进行微调。
*   **关键技术细节**：
    1.  **角色分配矩阵 $S(\phi)$**：计算第 $i$ 个智能体的行为轨迹（Behavior Trajectory）与第 $j$ 个智能体的角色描述（Role Description）之间的余弦相似度 $s_{ij}$。
    2.  **角色清晰度矩阵 $M(\phi)$**：对 $S(\phi)$ 进行行归一化（Softmax）后减去单位矩阵 $I$。即 $M(\phi) = \text{softmax}(S(\phi)) - I$。
    3.  **量化指标**：使用 $M(\phi)$ 的 **Frobenius 范数** 来衡量偏差。理想状态下，智能体只应与自己的角色描述相似（对角线为1），与其他角色相似度为0。
    4.  **优化目标**：在标准的最大似然估计（MLE）损失基础上，引入**角色清晰度正则项** $L_{CE\_RC}$（基于交叉熵），鼓励模型在生成回复时保持角色边界。
    5.  **微调技术**：采用 **LoRA（低秩自适应）** 进行轻量化微调，仅更新少量参数。

### 3. 实验设计
*   **实验框架**：基于 **ChatDev** 协作开发框架。
*   **基座模型**：Qwen2.5-7B-Instruct 和 Llama-3.1-8B-Instruct。
*   **数据集/场景**：
    *   **SWE-Dev**：包含 14,000 个训练实例和 500 个测试实例，用于评估角色一致性。
    *   **SRDD (Software Requirement Description Dataset)**：包含 1,200 个任务 Prompt，用于评估端到端软件生成的质量。
*   **对比设置**：
    *   **Base-Base**：原始未微调模型。
    *   **单角色微调**：仅微调 CEO 或 CPO。
    *   **联合微调 (FT)**：同时微调 CEO 和 CPO。
    *   **消融实验**：对比开启和关闭“角色清晰度正则项”的效果。

### 4. 资源与算力
*   **训练细节**：Batch size 为 32，学习率为 $5 \times 10^{-5}$，训练 10 个 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体训练时长。但考虑到使用了 LoRA 微调 7B/8B 规模的模型，通常单张或少量消费级/企业级 GPU 即可完成。

### 5. 实验数量与充分性
*   **实验规模**：针对两个主流开源模型系列（Qwen 和 Llama）进行了完整测试，涵盖了 Easy 和 Hard 不同难度的任务子集。
*   **充分性评价**：实验设计较为充分。作者不仅验证了角色一致性的提升（越权率下降），还通过 SRDD 数据集验证了这种提升能转化为实际的业务价值（软件生成的完整性、可执行性等）。消融实验清晰地展示了正则化项在维持角色清晰度评分方面的关键作用。

### 6. 主要结论与发现
*   **角色一致性显著提升**：Llama 模型的角色越权率从 43.4% 惊人地降至 0.2%；Qwen 从 46.4% 降至 8.4%。
*   **任务性能增强**：角色清晰度的提高直接带动了端到端任务的成功率。例如，Llama 在 SRDD 上的综合质量得分（Quality）从 0.6174 提升至 0.6763。
*   **正则化的必要性**：如果不加角色清晰度正则项，单纯的微调虽然能降低越权率，但无法有效提升角色清晰度评分，甚至可能导致角色边界模糊。

### 7. 优点与亮点
*   **首创性**：据作者称，这是首次提出**定量且可微**的角色清晰度定义，填补了多智能体角色管理从“提示工程”向“参数优化”跨越的空白。
*   **数学严谨性**：利用 Frobenius 范数和单位矩阵偏差来定义角色清晰度，逻辑简洁且易于集成到现有的深度学习优化框架中。
*   **实用性**：通过拒绝采样（Rejection Sampling）构建高质量轨迹数据集，为解决 LLM 幻觉和指令遵循问题提供了新思路。

### 8. 不足与局限
*   **角色覆盖范围**：实验主要集中在 ChatDev 中的 CEO 和 CPO 两个核心角色，对于更多角色（如 Coder, Tester, Reviewer）同时协作时的复杂冲突研究较少。
*   **框架依赖**：目前仅在 ChatDev 框架下验证，尚未在 AutoGen 或 MetaGPT 等其他主流框架上进行交叉验证。
*   **Embedding 依赖**：该方法高度依赖于 Embedding 模型对“行为轨迹”和“角色描述”的语义捕捉能力，若 Embedding 模型存在偏差，量化结果可能失真。
*   **动态角色支持**：该方法适用于预定义角色，对于在任务过程中动态生成或演化的角色，其适用性有待商榷。

（完）
