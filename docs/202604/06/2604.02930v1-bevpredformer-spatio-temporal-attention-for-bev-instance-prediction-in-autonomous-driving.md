---
title: "BEVPredFormer: Spatio-temporal Attention for BEV Instance Prediction in Autonomous Driving"
title_zh: BEVPredFormer：自动驾驶中用于 BEV 实例预测的时空注意力机制
authors: "Miguel Antunes-García, Santiago Montiel-Marín, Fabio Sánchez-García, Rodrigo Gutiérrez-Moreno, Rafael Barea, Luis M. Bergasa"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02930v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 自动驾驶中的鸟瞰图分割与运动估计
tldr: BEVPredFormer是一个针对自动驾驶BEV实例预测的纯视觉架构，旨在解决传统模块化感知系统中的误差累积和延迟问题。该模型采用无循环设计，结合门控Transformer层、分立的时空注意力机制以及多尺度任务头，并引入差分引导特征提取模块以增强时间表征。在nuScenes数据集上的实验表明，该方法在捕捉细粒度运动模式和长程依赖方面表现出色，性能达到或超越了现有最先进水平。
selection_source: fresh_fetch
motivation: 传统模块化感知系统存在误差累积和高延迟，且现有模型在处理复杂动态场景时难以兼顾细粒度运动捕捉与实时性能。
method: 提出一种基于注意力的无循环架构，利用分立的时空注意力、门控Transformer层及差分引导特征提取模块来优化BEV下的实例预测。
result: 在nuScenes数据集上的评估结果显示，BEVPredFormer的性能与当前最先进的方法持平或更优。
conclusion: BEVPredFormer通过高效的时空建模提升了自动驾驶系统的场景理解能力，证明了其在鲁棒且高效的感知任务中的潜力。
---

## 摘要
对动态场景演变的鲁棒感知对于自动驾驶系统至关重要，因为系统必须准确地检测、跟踪并预测周围障碍物的行为。依赖模块化架构的传统感知流水线往往会受到累积误差和延迟的影响。实例预测模型提供了一种统一的解决方案，利用直接从不同传感器获取的信息，在当前和未来帧中执行鸟瞰图（BEV）分割和运动估计。然而，这些模型面临的一个关键挑战在于如何有效处理动态驾驶环境中固有的密集时空信息。这种复杂程度要求架构能够在不牺牲实时性能的情况下，捕捉细粒度的运动模式和长程依赖关系。我们提出了 BEVPredFormer，这是一种用于 BEV 实例预测的新型纯摄像头架构，它使用基于注意力的时间处理来提高对场景的时空理解，并依赖于基于注意力的摄像头信息 3D 投影。BEVPredFormer 采用无循环设计，结合了门控 Transformer 层、分立式时空注意力机制和多尺度头部任务。此外，我们还加入了一个差分引导的特征提取模块，以增强时间表征。广泛的消融实验验证了每个架构组件的有效性。在 nuScenes 数据集上的评估结果表明，BEVPredFormer 与现有最先进（SOTA）方法持平或更优，凸显了其在鲁棒且高效的自动驾驶感知方面的潜力。

## Abstract
A robust awareness of how dynamic scenes evolve is essential for Autonomous Driving systems, as they must accurately detect, track, and predict the behaviour of surrounding obstacles. Traditional perception pipelines that rely on modular architectures tend to suffer from cumulative errors and latency. Instance Prediction models provide a unified solution, performing Bird's-Eye-View segmentation and motion estimation across current and future frames using information directly obtained from different sensors. However, a key challenge in these models lies in the effective processing of the dense spatial and temporal information inherent in dynamic driving environments. This level of complexity demands architectures capable of capturing fine-grained motion patterns and long-range dependencies without compromising real-time performance. We introduce BEVPredFormer, a novel camera-only architecture for BEV instance prediction that uses attention-based temporal processing to improve temporal and spatial comprehension of the scene and relies on an attention-based 3D projection of the camera information. BEVPredFormer employs a recurrent-free design that incorporates gated transformer layers, divided spatio-temporal attention mechanisms, and multi-scale head tasks. Additionally, we incorporate a difference-guided feature extraction module that enhances temporal representations. Extensive ablation studies validate the effectiveness of each architectural component. When evaluated on the nuScenes dataset, BEVPredFormer was on par or surpassed State-Of-The-Art methods, highlighting its potential for robust and efficient Autonomous Driving perception.

---

## 论文详细总结（自动生成）

这是一份关于论文《BEVPredFormer: Spatio-temporal Attention for BEV Instance Prediction in Autonomous Driving》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的自动驾驶感知系统通常采用模块化架构（检测、跟踪、预测分步进行），这容易导致**误差累积**和**处理延迟**。
*   **研究背景**：实例预测（Instance Prediction）模型旨在通过统一的架构，直接从传感器数据生成鸟瞰图（BEV）下的语义分割和运动估计。然而，如何在保证实时性的前提下，有效处理密集的时空信息并捕捉长程依赖关系，是当前该领域的主要挑战。
*   **整体含义**：本文提出 BEVPredFormer，旨在通过一种**无循环（Recurrent-free）**的注意力机制架构，提升纯视觉方案在 BEV 空间内的时空理解能力，实现更精准的未来场景预测。

### 2. 方法论：核心思想与关键技术
BEVPredFormer 的核心是一个基于 Transformer 的端到端预测框架，主要包含以下关键组件：
*   **BEV 投影与细化**：利用 **BEVFormer** 的注意力机制将多摄像头图像投影到 BEV 空间，随后通过 Sparse UNet 模块进行空间特征细化。
*   **差分引导模块（Difference Module）**：通过计算连续帧 BEV 特征之间的差异来捕捉显式的运动线索，并利用权重机制强化发生显著变化的区域。
*   **PredFormer 时间块**：
    *   **无循环设计**：放弃了传统的 RNN/LSTM，采用并行化更好的 Transformer 结构。
    *   **分立式时空注意力（Divided Attention）**：将注意力机制分解为独立的时间注意力和空间注意力（如 Triplet-TST 模式），以降低计算复杂度并提高学习效率。
    *   **门控 Transformer 层（GTB）**：引入门控线性单元（GLU）替代传统的 MLP，以更有效地控制时空信息的流动，减少长程预测中的噪声。
*   **多尺度预测头**：采用多尺度编码器-解码器结构，输出未来帧的**反向光学流（Backwards Flow）**和**语义分割图**，最终通过后处理合成实例预测结果。

### 3. 实验设计
*   **数据集**：使用自动驾驶主流数据集 **nuScenes**。
*   **场景与任务**：针对车辆（Vehicles）类别，输入当前及过去 1 秒的帧（3 帧），预测未来 2 秒的状态（4 帧预测 + 2 帧初始化，共 6 帧输出）。
*   **Benchmark 与对比方法**：
    *   **对比模型**：包括 FIERY、PowerBEV、StretchBEV、BEVerse、ST-P3、FaE 和 DMP 等 SOTA 模型。
    *   **评估指标**：IoU（分割精度）和 VPQ（视频全景质量，衡量实例跟踪的一致性）。
    *   **范围设定**：分为短程（15m）和长程（50m）两种评估环境。

### 4. 资源与算力
*   **硬件环境**：使用了多张 **NVIDIA A100 (80GB VRAM)** GPU 进行训练。
*   **训练策略**：采用两阶段训练法。
    *   第一阶段：预训练图像提取器和 BEV 投影模块（50 epochs）。
    *   第二阶段：冻结前期权重，训练时间模块和预测头（50 epochs）。
*   **推理延迟**：在 448x800 分辨率下，端到端延迟约为 **220ms**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的定量对比和定性分析。
*   **消融实验**：针对时间注意力类型（Binary-TS, Triplet-TST, Quad-TSST）、时间块的数量（1, 2, 4, 6 个）、输入图像分辨率（从 224x480 到 640x1600）以及差分模块的有效性进行了多组对比实验。
*   **公平性与客观性**：实验遵循了 nuScenes 的官方分割标准，并使用了开源社区公认的指标。通过在相同硬件和设置下重新运行部分 SOTA 模型（如 PowerBEV、BEVerse），确保了对比的基准一致。

### 6. 主要结论与发现
*   **性能领先**：BEVPredFormer 在长程 IoU（40.9）上显著超越了所有对比方法，VPQ 指标（33.3）也达到了顶尖水平。
*   **注意力模式的影响**：实验发现先进行时间注意力、再进行空间注意力的组合（如 Triplet-TST）在处理 BEV 序列时最为有效。
*   **效率平衡**：增加时间块数量能提升精度，但边际收益在 2 个块之后递减，而计算量显著增加。
*   **分辨率关键性**：图像分辨率对 BEV 投影质量影响巨大，448x800 被证明是精度与延迟之间的最佳平衡点。

### 7. 优点（亮点）
*   **高效的时空建模**：通过无循环的 Transformer 设计，克服了 RNN 在处理长序列时的梯度问题，且更易于硬件并行加速。
*   **显式运动感知**：差分引导模块为模型提供了直观的运动特征，增强了对动态障碍物的捕捉能力。
*   **鲁棒性**：在雨天、复杂路口等挑战性场景下表现出良好的分割和预测一致性。

### 8. 不足与局限
*   **长程预测衰减**：定性分析显示，对于距离 ego 车辆较远且处于长预测步长（2秒末尾）的目标，模型仍存在预测失效或运动趋势偏差的情况。
*   **计算开销**：虽然采用了分立注意力，但相比于极轻量化的模型（如 PowerBEV），其在最高分辨率下的推理延迟（360ms）仍难以满足极高实时性的需求。
*   **类别局限**：实验主要集中在“车辆”类别，未充分展示对行人、骑行者等更小、运动更不规则目标的预测效果。

（完）
