---
title: Smooth and Exact Parameterization of Continuous-time Signal Temporal Logic Specifications for Trajectory Optimization
title_zh: 用于轨迹优化的连续时间信号时序逻辑规范的平滑且精确参数化
authors: "Samet Uzun, Behçet Açıkmeşe"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04245v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于轨迹优化的连续时间信号时序逻辑参数化
tldr: 本文提出了一种针对非凸轨迹优化的连续时间信号时序逻辑（CT-STL）平滑参数化方法。该方法通过在稠密时间内评估规范，解决了传统离散采样导致的安全性缺失和逻辑保守性问题，并克服了梯度掩盖现象。该方案在保证安全性的前提下允许使用更粗的采样网格，从而降低了优化问题的维度。实验在四旋翼无人机复杂任务中验证了其数值有效性和语义精确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04245v1/fig-001.webp\", \"caption\": \"Fig. 1. Optimized point-mass quadrotor trajectory together with the charging station. The vehicle reaches the charging region before proceeding to the terminal state, consistent with the continuous-time until specification.\", \"page\": 7, \"index\": 1, \"width\": 473, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2604.04245v1/fig-002.webp\", \"caption\": \"TABLE II STANDARD QUANTITATIVE ROBUSTNESS SEMANTICS FOR CONTINUOUS-TIME STL.\", \"page\": 3, \"index\": 2, \"width\": 504, \"height\": 281}]"
motivation: 现有的轨迹优化方法通常仅在离散采样点强制执行时序逻辑规范，无法保证节点间的连续时间安全性且存在逻辑保守性。
method: 提出一种在稠密时间内评估CT-STL规范的平滑参数化方案，通过构建“始终”和“最终”谓词的连续时间表示来处理复杂的时序逻辑公式。
result: 在敏捷四旋翼飞行实验中，该方法成功处理了复杂的“直到”规范，并在大幅减少采样节点的情况下保持了极高的逻辑保真度。
conclusion: 该研究通过提升连续时间满足性的评估精度和优化景观的平滑度，为复杂任务下的高效轨迹生成提供了强有力的理论支撑。
---

## 摘要
本文提出了一种用于非凸轨迹优化的连续时间信号时序逻辑（CT-STL）规范的平滑参数化方法，该方法在底层数值积分方案的精度范围内是可靠且完备的。CT-STL 为编码丰富的时序和逻辑任务需求提供了一个自然框架，但现有的轨迹优化公式通常仅在离散采样节点处强制执行此类规范。相比之下，所提出的方法在稠密时间内评估规范，从而保证了“始终（always）”谓词的连续时间满足性（这对于避障等路径约束至关重要），同时通过允许在规定间隔内的任何时间满足，消除了“最终（eventually）”谓词由节点引起的保守性。这两个稠密时间构建块还可用于处理更通用的 CT-STL 公式，包括复杂的“直到（until）”规范。此外，所提出的参数化解决了标准定量语义中固有的局部性和梯度掩蔽问题，为基于梯度的求解器提供了更有利的优化景观。尽管稠密时间评估在离散化过程中引入了额外的函数评估，但它也允许使用大幅粗糙的时间网格，而不会牺牲安全性或逻辑保真度。这反过来又降低了所得非凸规划的维度，而维度通常是轨迹生成成本的主导因素。通过一个受复杂连续时间“直到”规范约束的敏捷四旋翼飞行问题，证明了所提出框架的数值有效性和语义精确性。代码实现见：https://github.com/UW-ACL/TrajOpt_CT-STL

## Abstract
This paper presents a smooth parameterization of continuous-time Signal Temporal Logic (CT-STL) specifications for nonconvex trajectory optimization that is sound and complete up to the accuracy of the underlying numerical integration scheme. CT-STL provides a natural framework for encoding rich temporal and logical task requirements, but existing trajectory-optimization formulations typically enforce such specifications only at discrete sampling nodes. In contrast, the proposed method evaluates specifications in dense time, thereby guaranteeing continuous-time satisfaction of always predicates, which is critical for path constraints such as obstacle avoidance, while eliminating the node-induced conservatism of eventually predicates by allowing satisfaction at any time within the prescribed interval. These two dense-time constructions also serve as the main building blocks for handling more general CT-STL formulas, including complex until specifications. Furthermore, the proposed parameterization resolves the locality and gradient-masking issues inherent in standard quantitative semantics, yielding a more favorable landscape for gradient-based solvers. Although dense-time evaluation introduces additional function evaluations during discretization, it also permits substantially coarser temporal grids without sacrificing safety or logical fidelity. This, in turn, reduces the dimension of the resulting nonconvex program, which is often the dominant factor in trajectory-generation cost. The numerical effectiveness and semantic exactness of the proposed framework are demonstrated on an agile quadrotor flight problem subject to a complex continuous-time until specification. The implementation is available at https:// github.com/UW-ACL/TrajOpt_CT-STL

---

## 论文详细总结（自动生成）

以下是对论文《Smooth and Exact Parameterization of Continuous-time Signal Temporal Logic Specifications for Trajectory Optimization》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在非凸轨迹优化中，既能保证**连续时间（Continuous-time）**下的逻辑规范满足性，又能保持优化过程的**平滑性**和**计算效率**。
*   **研究背景**：
    *   **离散时间 STL (DT-STL) 的缺陷**：现有方法大多在离散采样点强制执行信号时序逻辑（STL），这会导致“采样间隙”的安全风险（如避障失效）以及逻辑上的保守性（如“最终”谓词必须在节点处满足）。
    *   **现有平滑方法的不足**：传统的平滑近似（如 Log-Sum-Exp 或 Softmax）存在“梯度掩盖”和“局部性”问题，导致求解器容易陷入局部最优或收敛缓慢。
    *   **研究动机**：开发一种在数值积分精度范围内精确、且对梯度求解器友好的连续时间 STL（CT-STL）参数化框架。

### 2. 方法论
*   **核心思想**：将基于广义均值的平滑鲁棒性（GMSR）扩展到稠密时间域，利用动力学离散化过程中的**积分副节点（Integration Sub-nodes）**来评估 STL 规范。
*   **关键技术细节**：
    *   **GMSR 参数化**：使用一种特殊的 $C^1$ 连续函数来替代非光滑的 `min/max` 算子。该算子在保证符号精确性（即鲁棒值大于 0 等价于逻辑满足）的同时，能将梯度信息分布到所有相关的子公式和时间点上。
    *   **稠密时间评估（Dense-time Evaluation）**：在多重射击法（Multiple Shooting）中，利用 RK4 等积分器产生的中间子节点构建稠密时间网格。CT-STL 的算子（Always, Eventually, Until）直接在该网格上定义。
    *   **算子构建**：
        *   `Always (G)`：要求稠密网格上所有点的鲁棒值之和/积满足特定平滑条件。
        *   `Eventually (F)`：允许在网格中任意一点满足即可，消除了对特定节点的依赖。
        *   `Until (U)`：通过嵌套的平滑算子处理复杂的时序依赖。
*   **算法流程**：将非凸轨迹优化问题转化为精确惩罚形式，利用 **Prox-convex 方法**（一种近端复合优化算法）进行迭代求解，并使用 JAX 进行自动微分。

### 3. 实验设计
*   **实验场景**：四旋翼无人机（点质量模型）的敏捷飞行任务。
*   **任务目标**：
    *   **边界条件**：固定的初始和终点状态。
    *   **路径约束**：倾斜角限制、推力限制、全局速度限制（使用 `Always` 算子）。
    *   **核心任务**：满足一个复杂的 `Until` 规范——“在到达充电站（目标区域）之前，飞行速度必须保持在安全阈值以下”。
*   **对比与基准**：
    *   实验重点展示了该方法处理连续时间 `Until` 算子的能力，这是此前平滑优化方法难以精确处理的。
    *   通过可视化鲁棒值（Robustness Value）和充电站边际距离（Margin）来验证语义的精确性。

### 4. 资源与算力
*   **硬件环境**：2023款 MacBook Pro，搭载 **Apple M2** 处理器。
*   **软件框架**：使用 **JAX** 进行动力学离散化和 CT-STL 评估的自动微分，**CVXPY** 进行建模，**QOCO**（一种锥凸优化求解器）求解子问题。
*   **计算耗时**：
    *   凸子问题求解耗时：约 **15.35 ms**。
    *   动力学离散化与 STL 评估耗时：约 **0.97 ms**。
    *   总迭代次数：10 次 Prox-convex 迭代。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了一个具有代表性的复杂四旋翼飞行案例。
*   **充分性评价**：
    *   **逻辑覆盖**：实验涵盖了 STL 中最难处理的 `Until` 算子，以及常见的 `Always` 约束，证明了框架的通用性。
    *   **数值验证**：通过展示速度曲线和充电站进入时间的精确匹配，客观证明了“连续时间满足性”优于传统的离散节点检查。
    *   **局限性**：实验主要集中在单一复杂场景的验证，缺乏大规模蒙特卡洛模拟或与多种不同求解器（如传统 MIP 求解器）的大规模横向对比。

### 6. 主要结论与发现
*   **语义精确性**：该方法在数值积分精度内实现了 CT-STL 的精确参数化，消除了离散化带来的逻辑偏差。
*   **优化优势**：GMSR 成功解决了梯度掩盖问题，使得基于梯度的求解器能够高效处理复杂的时序逻辑约束。
*   **效率提升**：由于可以在稠密时间评估规范，优化问题可以使用**更粗的节点网格**（如本例仅用 5 个优化节点），从而显著降低决策变量的维度和计算成本。

### 7. 优点
*   **理论完备**：在符号上与原始 STL 语义保持一致，且具备 $C^1$ 平滑性。
*   **安全性高**：通过稠密时间检查，确保了轨迹在采样点之间不会违反安全约束。
*   **梯度友好**：梯度分布机制避免了优化过程中的停滞，提高了非凸问题的求解成功率。
*   **计算灵活**：复用了动力学积分的副节点，没有引入显著的额外计算负担。

### 8. 不足与局限
*   **时间跨度限制**：目前主要针对固定终点时间（Fixed-final-time）问题，尚未完全扩展到自由终点时间问题。
*   **实现依赖**：高性能表现依赖于高效的自动微分工具（如 JAX）和底层凸求解器。
*   **应用范围**：虽然理论上支持所有 CT-STL，但在极高维系统或极长时域任务下的扩展性仍需进一步验证。

（完）
