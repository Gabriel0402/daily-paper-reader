---
title: "Differentiable SpaTiaL: Symbolic Learning and Reasoning with Geometric Temporal Logic for Manipulation Tasks"
title_zh: 可微 SpaTiaL：基于几何时序逻辑的操纵任务符号学习与推理
authors: "Licheng Luo, Kaier Liang, Cristian-Ioan Vasile, Mingyu Cai"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02643v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于操作任务的可微时空逻辑
tldr: 针对复杂环境下机器人操作任务中几何与时序约束耦合的问题，本文提出了首个端到端可微的符号时空逻辑工具箱 Differentiable SpaTiaL。该工具通过对多边形集合构建平滑且兼容自动求导的几何原语，实现了从高层语义规范到底层几何配置的可微映射，支持大规模并行轨迹优化并能从演示中学习逻辑参数。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02643v1/fig-001.webp\", \"caption\": \"Fig. 6: Qualitative spatio-temporal trajectory optimization using differentiable spatial semantics. From left to right: initial trajectory (violates), iteration 30, final trajectory (satisfies), and robustness analysis.\", \"page\": 6, \"index\": 1, \"width\": 1052, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.02643v1/fig-002.webp\", \"caption\": \"Fig. 1: Overview of Differentiable SpaTiaL. Our framework replaces discrete geometry engines with a fully tensorized architecture, enabling end-to-end trajectory optimization under formal specifications.\", \"page\": 2, \"index\": 2, \"width\": 1052, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.02643v1/fig-003.webp\", \"caption\": \"Fig. 7: Learning spatial specification parameters from demonstrations via robustness backpropagation. From left to right: oblique, xy, and xz views.\", \"page\": 7, \"index\": 3, \"width\": 1054, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2604.02643v1/fig-004.webp\", \"caption\": \"Fig. 8: Geometric accuracy of differentiable predicates. We compare distance, signed distance, penetration, and enclosure robustness against Shapely across randomized convex polygons and transforms.\", \"page\": 8, \"index\": 4, \"width\": 512, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2604.02643v1/fig-005.webp\", \"caption\": \"Fig. 3: Differentiable penetration depth via Smooth SAT. Exact discrete overlap is relaxed into a smooth, differentiable repulsion field.\", \"page\": 4, \"index\": 5, \"width\": 512, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2604.02643v1/fig-006.webp\", \"caption\": \"Fig. 5: Geometric interpretation of compositional predicates (left: EnclIn, right: leftOf). Spatial relations are resolved as differentiable algebraic operations over vertex tensors.\", \"page\": 5, \"index\": 6, \"width\": 512, \"height\": 291}]"
motivation: 现有的时空逻辑方法因依赖离散几何引擎而不可微，阻碍了其在梯度优化和端到端学习中的应用。
method: 提出一种全张量化工具箱，通过解析推导空间谓词的可微松弛，构建了支持自动求导的几何原语。
result: 实验验证了该框架在处理严苛时空约束下的轨迹优化效率，以及从演示数据中学习逻辑参数的有效性。
conclusion: Differentiable SpaTiaL 填补了符号逻辑与梯度优化之间的空白，为复杂机器人操作任务提供了高效的推理与学习范式。
---

## 摘要
在凌乱环境中执行复杂的操纵任务需要满足耦合的几何与时序约束。尽管时空逻辑（SpaTiaL）提供了一个原则性的规范框架，但由于不可微的几何操作，其在基于梯度的优化中的应用受到了限制。现有的可微时序逻辑主要关注机器人的内部状态，忽略了交互式的物体-环境关系；而捕捉此类交互的空间逻辑方法则依赖于离散几何引擎，这会破坏计算图并阻碍精确的梯度传播。为了克服这一局限性，我们提出了 Differentiable SpaTiaL，这是一个全张量化的工具箱，直接在多边形集合上构建平滑且兼容自动微分的几何原语。据我们所知，这是首个端到端可微的符号时空逻辑工具箱。通过解析推导关键空间谓词（包括有符号距离、相交、包含和方向关系）的可微松弛，我们实现了从高层语义规范到低层几何配置的端到端可微映射，而无需调用外部离散求解器。这种全可微的表述开启了两项核心能力：(i) 在严格的时空约束下进行大规模并行轨迹优化；(ii) 通过反向传播直接从演示中学习空间逻辑参数。实验结果验证了所提框架的有效性和可扩展性。

## Abstract
Executing complex manipulation in cluttered environments requires satisfying coupled geometric and temporal constraints. Although Spatio-Temporal Logic (SpaTiaL) offers a principled specification framework, its use in gradient-based optimization is limited by non-differentiable geometric operations. Existing differentiable temporal logics focus on the robot's internal state and neglect interactive object-environment relations, while spatial logic approaches that capture such interactions rely on discrete geometry engines that break the computational graph and preclude exact gradient propagation. To overcome this limitation, we propose Differentiable SpaTiaL, a fully tensorized toolbox that constructs smooth, autograd-compatible geometric primitives directly over polygonal sets. To the best of our knowledge, this is the first end-to-end differentiable symbolic spatio-temporal logic toolbox. By analytically deriving differentiable relaxations of key spatial predicates--including signed distance, intersection, containment, and directional relations--we enable an end-to-end differentiable mapping from high-level semantic specifications to low-level geometric configurations, without invoking external discrete solvers. This fully differentiable formulation unlocks two core capabilities: (i) massively parallel trajectory optimization under rigorous spatio-temporal constraints, and (ii) direct learning of spatial logic parameters from demonstrations via backpropagation. Experimental results validate the effectiveness and scalability of the proposed framework.Code Available: https://github.com/plen1lune/DiffSpaTiaL

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Differentiable SpaTiaL** 的工具箱，旨在解决机器人操纵任务中高层逻辑规范与底层梯度优化之间的断层问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在复杂、拥挤的环境中，机器人操纵需要同时满足几何（空间）和时序约束。时空逻辑（SpaTiaL）虽然能提供严谨的规范框架，但其空间谓词（如“在左侧”、“包含在内”、“距离多远”）通常依赖离散的几何引擎（如 Shapely、FCL）。
*   **核心问题**：传统的几何引擎涉及离散的碰撞检测算法和布尔运算，这会破坏计算图，导致梯度无法回传。现有的可微逻辑（如 STLCG）仅处理一维信号，无法直接处理多维物体的几何形状和复杂的空间拓扑关系。
*   **研究目标**：开发一个全张量化、端到端可微的符号时空逻辑工具箱，使高层语义规范能够直接参与基于梯度的轨迹优化和参数学习。

### 2. 方法论：核心思想与技术细节
该方法的核心是将离散的几何查询转化为平滑、可微的张量运算：
*   **平滑近似算子**：使用 **LogSumExp (LSE)** 函数作为 `min` 和 `max` 的平滑近似（通过温度参数 $\tau$ 控制平滑度），确保在整个空间域内梯度非零且连续。
*   **可微穿透深度（Smooth SAT）**：基于分离轴定理（SAT），将多边形的投影重叠量公式化为全张量操作。通过平滑投影边界，计算出两个凸多边形之间的可微穿透深度，从而在物体重叠时产生排斥梯度。
*   **可微有符号距离场（SDF）**：通过对多边形边界进行**软采样（Soft Boundary Sampling）**，结合平滑半空间约束，计算点到多边形以及多边形到多边形的有符号距离。
*   **组合空间谓词**：利用上述原语构建复杂的空间关系，如 `enclIn`（包含）、`LeftOf`（在左侧）、`Between`（在中间）等。
*   **语义保证**：论文在理论上证明了该平滑松弛具有**保守性**（Conservative Approximation），即平滑后的鲁棒性值通常不会高估实际的满足程度，确保了优化过程的安全性。

### 3. 实验设计
论文设计了三个核心实验来验证框架的能力：
*   **任务 I：轨迹优化**。在平面操纵场景中，要求末端执行器避开障碍物并最终进入目标区域。对比了初始可行性差的轨迹在梯度驱动下的收敛过程。
*   **任务 II：规范参数学习**。给定 30 组 3D 拾取-放置任务的专家演示，通过反向传播优化空间谓词的连续参数（如安全距离 $\epsilon$），从而从演示中推断出隐含的逻辑规范。
*   **任务 III：几何精度分析**。将 Differentiable SpaTiaL 计算的距离、穿透深度和包含关系鲁棒性与标准几何引擎 **Shapely** 进行对比，验证其数值准确性。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备单块 **NVIDIA RTX 4090 GPU** 的工作站上运行。
*   **软件框架**：基于 **PyTorch** 实现，利用其自动求导（Autograd）和张量并行能力。
*   **算力细节**：文中强调了该工具箱是“批处理原生”（Batch-native）的，支持在 GPU 上进行大规模并行轨迹优化，但未具体给出单次训练或优化的总时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   轨迹优化展示了从违规到满足规范的动态演变过程。
    *   参数学习使用了 30 条演示轨迹，涵盖了 3D 空间中的多个障碍物。
    *   精度分析通过随机生成的凸多边形对进行了大量采样对比。
*   **充分性评价**：实验设计较为全面，涵盖了“从逻辑到动作（优化）”和“从动作到逻辑（学习）”两个闭环。对比 Shapely 的精度实验证明了该平滑方法在逼近真实几何值方面的可靠性。不过，实验主要集中在凸多边形（2D/2.5D），对于更复杂的 3D 网格或非凸形状的讨论较少。

### 6. 主要结论与发现
*   **端到端可微性**：成功实现了从高层逻辑鲁棒性到底层机器人状态的完整梯度流。
*   **优化效率**：平滑的几何原语提供了密集的梯度信息，能够有效引导轨迹跳出局部最优（如碰撞状态），比稀疏的碰撞信号更易于优化。
*   **可解释性学习**：能够从人类演示中提取出具有物理意义的几何参数（如具体的安全边界值），而不仅仅是黑盒策略。
*   **精度平衡**：通过调节温度参数 $\tau$ 和采样密度，可以在梯度平滑度和几何准确性之间取得平衡。

### 7. 优点与亮点
*   **首创性**：据称是首个全张量化、端到端可微的符号时空逻辑工具箱。
*   **数学严谨性**：不仅给出了实现，还通过引理和定理证明了平滑算子的保守性质，这对安全关键型任务至关重要。
*   **高效并行**：完全摒弃了外部离散求解器，利用 GPU 并行性显著提升了处理大规模轨迹簇的速度。

### 8. 不足与局限
*   **形状限制**：目前主要针对**凸多边形**。虽然可以通过分解处理非凸形状，但会增加计算复杂度和梯度不连续的风险。
*   **维度扩展**：虽然在任务 II 中涉及了 3D 坐标，但核心几何原语仍基于多边形投影，对于全 3D 复杂网格（Mesh）的直接支持尚待加强。
*   **局部最优风险**：尽管梯度是连续的，但复杂的时空逻辑景观仍然高度非凸，优化过程可能陷入局部最小值，需要良好的初始化或启发式搜索配合。

（完）
