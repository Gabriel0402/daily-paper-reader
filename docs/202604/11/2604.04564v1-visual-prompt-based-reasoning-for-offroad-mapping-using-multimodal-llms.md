---
title: Visual Prompt Based Reasoning for Offroad Mapping using Multimodal LLMs
title_zh: 基于多模态大语言模型的越野建图视觉提示推理
authors: "Abdelmoamen Nasser, Yousef Baba'a, Murad Mebrahtu, Nadya Abdel Madjid, Jorge Dias, Majid Khonji"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04564v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用SAM2和VLM进行环境分割和自动驾驶推理
tldr: 针对越野自动驾驶中传统方法依赖多个独立模型且需大量训练的问题，本文提出一种基于视觉提示推理的零样本框架。该方法结合 SAM2 进行环境分割，并利用多模态大模型（VLM）对带编号的分割区域进行可通行性推理。该方案无需特定地形训练，在分割精度上超越了现有 SOTA 模型，并成功实现了仿真环境下的全栈导航。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04564v1/fig-001.webp\", \"caption\": \"Figure 3: The pre-processing interface at three states from left to right. (i) The initial state. (ii) After addition of drivable masks 4, 5 and 6. (iii) After subtraction of masks 6 and 7.\", \"page\": 6, \"index\": 1, \"width\": 960, \"height\": 364}, {\"url\": \"assets/figures/arxiv/2604.04564v1/fig-002.webp\", \"caption\": \"Figure 4: Qualitative comparison of Mask Generation vs Point Prompting on frame 88.\", \"page\": 7, \"index\": 2, \"width\": 976, \"height\": 301}, {\"url\": \"assets/figures/arxiv/2604.04564v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of the off-road framework: (i) Simulation Module using NVIDIA Isaac Sim, (ii) Segmentation Module with SAM2 to for mask generation and tracking, (iii) VLM Inference Module to detect drivable area based on segmented masks and textual prompts, (iv) Mapping, planning, and control for environment mapping, path generation, and path following and velocity tracking respectively.\", \"page\": 3, \"index\": 3, \"width\": 974, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2604.04564v1/fig-004.webp\", \"caption\": \"Figure 5: Samples of drivable areas detected by evaluated VLMs. GT refers to Ground Truth, while the remaining columns display the outputs of each model: ChatGPT-4o, Aquila, Ivy-VL and MiniCPM.\", \"page\": 8, \"index\": 4, \"width\": 946, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.04564v1/fig-005.webp\", \"caption\": \"Figure 6: Examples from the scoring process: in the first column, a VLM selected mask indices that perfectly aligned with the Ground Truth (GT) drivable area, earning a score of 1; in the second column, a model selected indices corresponding to both a drivable area (an open trail) and a non-drivable area (a small hill of rocks), resulting in a score of 0.5; and in the third column, a model selected only mask indices corresponding to clouds, leading to a score of 0.\", \"page\": 9, \"index\": 5, \"width\": 979, \"height\": 596}, {\"url\": \"assets/figures/arxiv/2604.04564v1/fig-006.webp\", \"caption\": \"Figure 2: Simulation Setup: Bottom left: Simulation environment created in Unreal Engine. Top left: Simulation environment imported into Isaac Sim. Center: Top view of the open trail created using the spline creation tools, where the letters (A,B and C) indicate the goals in the reachability test. Right: Polaris RZR Sport 2022 imported using PhysX vehicle api.\", \"page\": 5, \"index\": 6, \"width\": 968, \"height\": 318}]"
motivation: 传统越野导航依赖多个特定任务模型，存在训练复杂、数据需求量大且难以泛化的问题。
method: 利用 SAM2 生成环境分割掩码并标注数字，引导多模态大模型通过视觉提示推理出可行驶区域。
result: 在多项高分辨率分割数据集上超越了最先进的可训练模型，并在 Isaac Sim 仿真中实现了完整的越野导航。
conclusion: 该研究证明了利用大模型的推理能力可以替代传统的多模型流水线，实现高效且通用的越野环境感知与制图。
---

## 摘要
传统的越野自主导航方法依赖于独立的模型来进行地形分类、高度估计以及侧滑或坡度条件的量化。使用多个模型需要分别训练每个组件，并具备特定任务的数据集和进行微调。在这项工作中，我们提出了一种零样本（zero-shot）方法，利用 SAM2 进行环境分割，并使用视觉语言模型（VLM）来推理可行驶区域。我们的方法包括向 VLM 同时输入原始图像和带有每个掩码数字标签注释的分割图像。随后，通过提示 VLM 识别由这些数字标签表示的哪些区域是可行驶的。结合规划和控制模块，这一统一框架消除了对显式特定地形模型的需求，转而依赖 VLM 固有的推理能力。我们的方法在高分辨率分割数据集上超越了最先进的可训练模型，并在 Isaac Sim 越野环境中实现了全栈导航。

## Abstract
Traditional approaches to off-road autonomy rely on separate models for terrain classification, height estimation, and quantifying slip or slope conditions. Utilizing several models requires training each component separately, having task specific datasets, and fine-tuning. In this work, we present a zero-shot approach leveraging SAM2 for environment segmentation and a vision-language model (VLM) to reason about drivable areas. Our approach involves passing to the VLM both the original image and the segmented image annotated with numeric labels for each mask. The VLM is then prompted to identify which regions, represented by these numeric labels, are drivable. Combined with planning and control modules, this unified framework eliminates the need for explicit terrain-specific models and relies instead on the inherent reasoning capabilities of the VLM. Our approach surpasses state-of-the-art trainable models on high resolution segmentation datasets and enables full stack navigation in our Isaac Sim offroad environment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Visual Prompt Based Reasoning for Offroad Mapping using Multimodal LLMs》的结构化总结：

### 1. 论文的核心问题与整体含义
越野环境（Off-road）的自主导航比城市道路更复杂，因为缺乏高精度地图、地形多样且边界模糊。传统方法通常依赖多个独立模型（如地形分类、高度估计、侧滑预测等），这需要大量特定任务的标注数据和繁琐的微调。
**本文核心动机**是提出一种**统一的、零样本（Zero-shot）的感知框架**。它利用大模型的通用推理能力，通过将环境分割与视觉语言模型（VLM）结合，直接识别可行驶区域，从而消除对特定地形训练模型的依赖。

### 2. 论文提出的方法论
该框架包含四个主要模块，形成一个闭环系统：
*   **分割模块（SAM2）**：利用 SAM2 模型生成环境掩码。为了提高实时性，研究者采用点提示（Point Prompting）而非内置的全局掩码生成。通过 IoU 阈值合并重叠掩码，并过滤掉过小的区域。
*   **视觉提示推理（VLM Inference）**：
    *   **拼图创建**：将原始图像与带有数字编号的分割掩码图拼接成一张“拼图”（Collage）。
    *   **推理提示**：向 VLM 输入图像和文本指令（如“识别所有可行驶区域，包括泥地、沙地、碎石等，输出数字编号”）。
    *   **掩码过滤**：根据 VLM 返回的编号提取对应的二进制掩码。
*   **建图与规划**：将掩码投影到 2D 占据网格（Occupancy Grid）中。
    *   **全局规划**：使用 **D* Lite** 算法计算最短路径。
    *   **局部规划**：使用 **Hybrid A*** 算法考虑车辆运动学约束，生成平滑轨迹。
*   **控制模块**：采用 **Stanley 控制器**进行横向控制，**PID 控制器**进行纵向速度控制。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真环境**：基于 NVIDIA Isaac Sim 和 Unreal Engine 构建的自定义越野场景，包含草地、小径、岩石等，采集了 2,957 张图像。
    *   **真实数据集**：在 **ORFD**（越野自由空间检测）、**RUGD**（非结构化地面）和 **O2DTD**（越野小径检测）三个公开数据集上进行基准测试。
*   **对比方法**：
    *   **VLM 对比**：测试了 GPT-5 Mini、ChatGPT-4o、Aquila-VL-2B、Ivy-VL (3B) 和 MiniCPM-V-2.6。
    *   **SOTA 对比**：与 PathFormer、Mask2Former、CLIPSeg、PSPNet 等可训练的先进模型进行 IoU 指标对比。

### 4. 资源与算力
*   **硬件设备**：
    *   **分割测试**：NVIDIA RTX 4070 (16GB GPU), i9-9900K CPU, 32GB RAM。
    *   **实时仿真与全栈运行**：NVIDIA RTX 3090 (24GB GPU)。
*   **训练时长**：由于是零样本（Zero-shot）框架，**无需针对特定任务进行训练**，主要开销在于推理延迟。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了不同 VLM 配置的消融实验（如“拼图模式”vs“仅注释模式”、“单数字输出”vs“多数字输出”）。
    *   在仿真环境中进行了 3 个目标的到达率测试（每个重复 5 次以确保鲁棒性）。
    *   在 3 个真实数据集上进行了定量评估。
*   **充分性评价**：实验设计较为全面，涵盖了从感知精度（IoU）到端到端导航成功率的评估。通过对比不同参数规模的 VLM，客观展示了模型规模与推理性能之间的权衡。

### 6. 论文的主要结论与发现
*   **零样本能力强**：该框架在无需针对越野数据微调的情况下，性能与 SOTA 可训练模型持平，甚至在某些高分辨率数据集上表现更优。
*   **VLM 表现差异**：GPT 系列模型在推理准确性上最高（mIoU 0.727），但推理延迟大（约 4-11 秒）；本地模型如 **Aquila-VL-2B** 在准确性（mIoU 0.515）与速度（0.59 秒）之间达到了最佳平衡。
*   **提示词工程影响**：对于小参数模型，简单的指令和较少的上下文反而能提高准确率；而大模型（如 GPT-4o）则能更好地处理复杂的上下文信息。

### 7. 优点
*   **通用性与灵活性**：无需重新训练即可适应不同的越野地形，极大地降低了部署成本。
*   **推理透明化**：通过数字编号和视觉提示，人类可以直观地理解 VLM 为什么选择某块区域作为可行驶路径。
*   **全栈集成**：不仅停留在感知层面，还成功集成了规划与控制，实现了闭环导航。

### 8. 不足与局限
*   **推理延迟**：VLM 的推理速度（尤其是高性能模型）目前难以满足高速越野导航的实时性需求。
*   **非确定性风险**：VLM 作为生成式模型，有时会产生不一致的输出（幻觉），可能导致导航指令错误。
*   **细小障碍物识别**：实验发现 VLM 难以识别某些物理特征（如模拟环境中的深沟），这可能导致车辆陷入困境。
*   **分辨率敏感**：在低分辨率图像（如 O2DTD 数据集）上表现较差，说明该方法依赖于清晰的视觉特征。

（完）
