---
title: Feasibility of Indoor Frame-Wise Lidar Semantic Segmentation via Distillation from Visual Foundation Model
title_zh: 基于视觉基础模型蒸馏的室内逐帧激光雷达语义分割的可行性研究
authors: "Haiyang Wu, Juan J. Gonzales Torres, George Vosselman, Ville Lehtola"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18831v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 从视觉基础模型蒸馏用于激光雷达语义分割
tldr: 室内激光雷达逐帧语义分割对3D场景理解至关重要，但获取标注数据成本极高。本文探讨了利用视觉基础模型（VFM）通过2D到3D蒸馏技术，在无需人工标注的情况下实现室内激光雷达分割的可行性。研究通过将激光雷达扫描与VFM处理的图像耦合，在SLAM数据集和新标注的验证集上进行了评估，证明了跨模态蒸馏在室内环境中的有效性，为缺乏标注的室内场景提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18831v1/fig-001.webp\", \"caption\": \"Figure 1. Workflow of ScaLR adaptation to indoor datasets.\", \"page\": 3, \"index\": 1, \"width\": 490, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.18831v1/fig-002.webp\", \"caption\": \"Figure 5. Qualitative comparison of segmentation outputs from DINOv2 (with Mask2Former head) and OneFormer on the NTU-VIRAL dataset.\", \"page\": 6, \"index\": 2, \"width\": 490, \"height\": 277}]"
motivation: 针对室内激光雷达逐帧语义分割中人工标注成本高昂且缺乏数据集的问题，探索利用视觉基础模型进行知识迁移的可行性。
method: 采用2D到3D蒸馏框架，将视觉基础模型生成的图像语义伪标签传递给对应的激光雷达扫描帧进行模型训练。
result: "实验显示该模型在伪标签评估下达到56% mIoU，在真实标注评估下达到36% mIoU。"
conclusion: 证明了在无需人工标注的情况下，通过跨模态蒸馏实现室内激光雷达语义分割是切实可行的。
---

## 摘要
室内激光雷达扫描的逐帧语义分割是实现更高级别3D场景理解和地图构建应用的基础步骤。然而，获取用于训练深度学习模型的逐帧真值（ground truth）既昂贵又耗时。对于图像而言，这一挑战在很大程度上已通过能够对图像帧进行分割的视觉基础模型（VFMs）得到解决。同样的VFMs可以通过2D到3D的蒸馏流水线来训练激光雷达扫描帧分割模型。这种蒸馏方法的成功已在自动驾驶场景中得到证实，但在室内场景中尚未实现。在本文中，我们研究了在室内场景中复制这一成功的可行性，采用逐帧蒸馏的方式，将每次激光雷达扫描与经VFM处理的相机图像耦合。评估工作使用室内SLAM数据集完成，其中伪标签用于下游评估。此外，由于目前还没有其他带有语义信息的激光雷达逐帧室内数据集，我们提供了一个小型的人工标注激光雷达数据集用于验证。结果表明，蒸馏模型在伪标签评估下达到了高达56%的mIoU，在真实标签评估下达到了约36%的mIoU，证明了在无需人工标注的情况下，跨模态蒸馏用于室内激光雷达语义分割的可行性。

## Abstract
Frame-wise semantic segmentation of indoor lidar scans is a fundamental step toward higher-level 3D scene understanding and mapping applications. However, acquiring frame-wise ground truth for training deep learning models is costly and time-consuming. This challenge is largely addressed, for imagery, by Visual Foundation Models (VFMs) which segment image frames. The same VFMs may be used to train a lidar scan frame segmentation model via a 2D-to-3D distillation pipeline. The success of such distillation has been shown for autonomous driving scenes, but not yet for indoor scenes. Here, we study the feasibility of repeating this success for indoor scenes, in a frame-wise distillation manner by coupling each lidar scan with a VFM-processed camera image. The evaluation is done using indoor SLAM datasets, where pseudo-labels are used for downstream evaluation. Also, a small manually annotated lidar dataset is provided for validation, as there are no other lidar frame-wise indoor datasets with semantics. Results show that the distilled model achieves up to 56% mIoU under pseudo-label evaluation and around 36% mIoU with real-label, demonstrating the feasibility of cross-modal distillation for indoor lidar semantic segmentation without manual annotations.

---

## 论文详细总结（自动生成）

这是一份关于论文《Feasibility of Indoor Frame-Wise Lidar Semantic Segmentation via Distillation from Visual Foundation Model》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：室内激光雷达（Lidar）的逐帧语义分割是3D场景理解和语义SLAM的基础。然而，获取室内逐帧Lidar标注数据极度耗时且昂贵，目前缺乏像室外自动驾驶（如SemanticKITTI）那样的大规模标注数据集。
*   **核心问题**：在自动驾驶领域，利用视觉基础模型（VFM）通过“2D到3D蒸馏”来训练Lidar模型已取得成功，但这种方法在**室内环境**中的可行性尚未得到验证。
*   **整体含义**：本文旨在探索是否可以通过现有的视觉基础模型（如DINOv2）将知识迁移到室内Lidar模型中，从而在**无需人工标注**的情况下实现有效的室内逐帧语义分割。

### 2. 论文提出的方法论
*   **核心思想**：采用教师-学生架构的跨模态蒸馏。利用预训练的2D VFM作为教师模型提取图像特征，引导3D学生模型学习点云的语义表示。
*   **关键技术细节**：
    *   **ScaLR框架适配**：将原本用于室外的ScaLR框架适配到室内SLAM数据集。通过传感器外参和内参，将3D点 $p_i$ 投影到2D像素 $\rho(i)$。
    *   **特征对齐**：最小化3D学生模型提取的特征 $\tilde{f}_i$ 与2D教师模型提取的特征 $g_{\rho(i)}$ 之间的距离。损失函数采用归一化描述符的平方差（等效于余弦相似度）：
        $$L_{sim} = \frac{1}{N} \sum_{i=1}^{N} ||\tilde{f}_i - g_{\rho(i)}||^2$$
    *   **伪标签生成**：使用OneFormer等模型对图像进行分割，将结果投影到点云上生成伪标签，用于下游的线性探测（Linear Probing）和全模型微调（Fine-tuning）。
    *   **3D骨干网络**：选用WaffleIron，这是一种基于投影的2D密集卷积处理3D点云的高效架构。

### 3. 实验设计
*   **数据集与场景**：
    *   **公开数据集**：NTU-VIRAL、TIERS、M2DGR（均为包含同步Lidar和相机的室内SLAM数据集）。
    *   **自建数据集**：ITC数据集（使用Hesai XT-32手持设备采集）。
*   **Benchmark（基准）**：
    *   由于缺乏现成的室内逐帧标注，作者手动标注了ITC数据集中的1720帧（约15%）作为**真实标签（Real-label）**验证集。
    *   对比基准包括：基于伪标签的评估、基于真实标签的评估，以及在S3DIS（场景级数据集）上预训练的监督模型RandLA-Net。
*   **对比维度**：跨数据集泛化性、不同VFM（OneFormer vs DINOv2）的影响、模型深度（24/36/48层）的消融实验。

### 4. 资源与算力
*   **硬件环境**：使用一台配备两块 **NVIDIA A40 GPU**（每块46GB显存）的服务器。
*   **训练时长**：
    *   蒸馏预训练：25个epoch。
    *   线性探测：20个epoch。
    *   微调：以24层WaffleIron为例，微调耗时约 **3.41小时**。
*   **推理速度**：24层模型在测试时达到 **15.41 Hz**，满足实时处理需求。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了跨四个数据集的交叉验证（表2）。
    *   对比了三种不同深度的模型性能（表7）。
    *   针对VFM的选择做了对比实验（表6）。
*   **充分性评价**：实验设计较为全面，涵盖了从特征提取能力（线性探测）到最终分割性能（微调）的完整链路。通过手动标注小规模真实数据集来校验伪标签评估的可靠性，增强了实验的客观性和说服力。

### 6. 论文的主要结论与发现
*   **可行性确认**：跨模态蒸馏在室内场景切实可行。在伪标签评估下达到 **56.5% mIoU**，在真实标签评估下达到 **35.8% mIoU**。
*   **超越监督迁移**：蒸馏模型在ITC数据集上的表现远优于直接从S3DIS迁移过来的监督模型RandLA-Net（仅10.7% mIoU），证明了蒸馏能有效缩小领域差距（Domain Gap）。
*   **模型稳定性**：3D特征的学习对具体的VFM（DINOv2或OneFormer）不敏感，两者提供的监督信号质量相当。
*   **效率平衡**：增加模型深度（从24层到48层）对精度的提升非常有限（仅约1%），但会显著增加计算开销，因此24层是室内任务的最优选择。

### 7. 优点（亮点）
*   **填补空白**：首次系统研究了室内逐帧Lidar语义分割的蒸馏可行性。
*   **实用性强**：提出了一套完整的从原始SLAM数据到训练就绪数据的处理流水线，无需任何人工标注即可启动模型训练。
*   **验证严谨**：尽管缺乏基准，但作者通过手动标注部分数据提供了硬性的性能指标，而非仅仅依赖伪标签。

### 8. 不足与局限
*   **领域依赖性**：实验显示模型在不同室内数据集之间仍存在明显的性能下降（如在NTU-VIRAL训练，在M2DGR上表现较差），说明传感器配置（线数、FOV）对特征迁移影响较大。
*   **伪标签噪声**：伪标签评估与真实标签评估之间存在约20%的mIoU差距，说明2D-to-3D投影过程中的对齐误差和2D分割错误仍是主要瓶颈。
*   **缺乏时序利用**：目前仅是纯粹的逐帧处理，未利用SLAM序列中的时序连续性或多视图几何约束来平滑分割结果。

（完）
