---
title: "SYMBOLIZER: Symbolic Model-free Task Planning with VLMs"
title_zh: "SYMBOLIZER: 基于视觉语言模型（VLM）的符号化无模型任务规划"
authors: "Sami Azirar, Zlatan Ajanovic, Hermann Blum"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17830v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 将VLM与机器人符号任务规划相结合
tldr: 针对传统任务与运动规划（TAMP）中符号模型难以构建且视觉语言模型（VLM）规划能力有限的问题，本文提出SYMBOLIZER框架。该方法利用VLM将谓词关系从图像映射为符号状态，并结合领域无关的启发式搜索进行长程推理，无需预定义的动作模型。实验表明，该方法在ProDG和ViPlan基准测试中达到了SOTA水平，显著提升了机器人在未知环境下的泛化与规划能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17830v1/fig-001.webp\", \"caption\": \"Fig. 1. SYMBOLIZER grounds visual observations into well-typed symbolic states using structured VLM outputs, enabling domain-independent planning without handcrafted action models.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-002.webp\", \"caption\": \"Fig. 6. Example observations and goals from the VIPLAN benchmark. BLOCKSWORLD uses photorealistic rendering, HOUSEHOLD provides goals as natural language.\", \"page\": 7, \"index\": 2, \"width\": 529, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-003.webp\", \"caption\": \"Fig. 2. Example of planning with a simulator. From an initial state, the planner expands successor states generated by the simulator, without requiring explicit knowledge of the underlying actions.\", \"page\": 3, \"index\": 3, \"width\": 526, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-004.webp\", \"caption\": \"Fig. 3. Flowchart of grounding from images or text to symbolic states.\", \"page\": 4, \"index\": 4, \"width\": 1076, \"height\": 381}]"
motivation: 传统TAMP依赖难以手工构建的符号模型，而现有VLM方法在复杂长程规划中缺乏通用性。
method: 利用VLM将提升谓词从图像中实例化为符号状态，并采用目标计数和基于宽度的领域无关启发式搜索进行规划。
result: 在ProDG和ViPlan基准测试中取得了最先进的结果，性能优于直接使用VLM规划或基于VLM启发式的方法。
conclusion: 结合VLM的视觉理解与领域无关的符号搜索，可以有效解决具有大规模组合状态空间的跨领域规划问题。
---

## 摘要
传统的任务与运动规划（TAMP）系统依赖物理模型进行运动规划，并依赖离散符号模型进行任务规划。尽管物理模型通常是现成的，但符号模型（由符号状态解释和动作模型组成）必须经过精心的人工设计或从标注数据中学习。这一过程既耗费资源，又将解决方案限制在特定领域，从而限制了可扩展性和适应性。另一方面，视觉语言模型（VLM）由于在异构数据上进行了广泛训练，展现出理想的零样本视觉理解能力，但其规划能力仍然有限。因此，将 VLM 与经典规划相结合，用于 TAMP 问题中的长程推理，具有巨大的潜力。该方向的近期工作仍缺乏通用性，且依赖于手工设计的特定任务解决方案，例如预先描述所有可能的物体，或使用符号动作模型。我们提出了一个能够很好地泛化到未见问题实例的框架。该方法仅需要描述物体间关系的提升谓词（lifted predicates），并利用 VLM 将其从图像中锚定（grounding）以获取符号状态。规划是通过使用目标计数（goal-count）和基于宽度的启发式搜索进行的，无需动作模型，且与领域无关。在基于 VLM 锚定的状态空间上进行的符号搜索优于直接基于 VLM 的规划，且与使用 VLM 衍生启发式的方法表现相当。这表明领域无关的搜索可以有效解决具有大规模组合状态空间的跨领域问题。我们对该方法进行了广泛评估，并在 ProDG 和 ViPlan 基准测试中取得了最先进的结果。

## Abstract
Traditional Task and Motion Planning (TAMP) systems depend on physics models for motion planning and discrete symbolic models for task planning. Although physics model are often available, symbolic models (consisting of symbolic state interpretation and action models) must be meticulously handcrafted or learned from labeled data. This process is both resource-intensive and constrains the solution to the specific domain, limiting scalability and adaptability. On the other hand, Visual Language Models (VLMs) show desirable zero-shot visual understanding (due to their extensive training on heterogeneous data), but still achieve limited planning capabilities. Therefore, integrating VLMs with classical planning for long-horizon reasoning in TAMP problems offers high potential. Recent works in this direction still lack generality and depend on handcrafted, task-specific solutions, e.g. describing all possible objects in advance, or using symbolic action models. We propose a framework that generalizes well to unseen problem instances. The method requires only lifted predicates describing relations among objects and uses VLMs to ground them from images to obtain the symbolic state. Planning is performed with domain-independent heuristic search using goal-count and width-based heuristics, without need for action models. Symbolic search over VLM-grounded state-space outperforms direct VLM-based planning and performs on par with approaches that use a VLM-derived heuristic. This shows that domain-independent search can effectively solve problems across domains with large combinatorial state spaces. We extensively evaluate on extensively evaluate our method and achieve state-of-the-art results on the ProDG and ViPlan benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《SYMBOLIZER: Symbolic Model-free Task Planning with VLMs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了机器人**任务与运动规划（TAMP）**中的一个关键瓶颈：传统方法高度依赖于人工精心设计的符号模型（如 PDDL 动作模型和状态谓词），这限制了系统在非结构化、开放世界环境中的泛化能力。
*   **研究动机**：虽然视觉语言模型（VLM）具备强大的零样本感知能力，但其直接进行长程逻辑规划（Long-horizon reasoning）时容易产生幻觉且缺乏严谨性。
*   **核心目标**：结合 VLM 的语义理解优势与经典规划算法的严谨性，构建一个**无需预定义动作模型**、仅需少量谓词定义的通用规划框架。

### 2. 方法论：核心思想与关键技术
论文提出了 **SYMBOLIZER** 框架，其核心思想是将 VLM 作为“符号传感器”，将非结构化观测转化为结构化状态，再利用模拟器进行搜索。
*   **结构化输出约束**：为了避免 VLM 产生幻觉或不一致的命名，SYMBOLIZER 将谓词和对象定义为 Python 类。通过强制 VLM 生成符合特定 Schema 的 JSON/Python 对象，确保了符号状态的类型安全和逻辑一致性。
*   **两阶段锚定（Grounding）**：
    1.  **对象提取**：从图像或文本中识别物体及其类型。
    2.  **谓词实例化**：基于提取的对象列表，推断物体间的关系（如 `on(block1, block2)`）。
*   **无模型规划（Model-free Planning）**：该框架不要求已知的 PDDL 动作效果定义。它将物理模拟器视为“黑盒”转移模型，通过在模拟器中尝试动作来生成后继状态。
*   **启发式搜索**：采用领域无关的搜索策略，主要使用**目标计数（Goal-count）**启发式，并辅以**基于宽度的搜索（Width-based Search）**作为破局机制，以处理复杂的组合状态空间。

### 3. 实验设计
研究者在多个仿真和真实场景中进行了评估：
*   **数据集/场景**：
    *   **自定义域**：包括 Blocksworld（积木世界）、Hanoi（汉诺塔，含颜色变体）、Kitchen-Worlds（厨房场景）。
    *   **外部基准**：**ProDG**（包含烹饪和汉诺塔的真实图像/视觉任务）和 **ViPlan**（包含光照真实的积木世界和部分可观测的家庭任务）。
*   **对比方法**：
    *   **ViLaIn**：现有的基于 VLM 的 PDDL 生成框架。
    *   **Direct VLM Planning**：直接让 VLM 输出动作序列。
    *   **VLM-guided Search**：使用 VLM 作为启发式函数指导搜索。
    *   **DREAM-VL**：结合 VLM 锚定与学习型世界模型的规划方法。

### 4. 资源与算力
*   **模型使用**：实验使用了多种 VLM，包括 **Gemini 1.5 Pro**、**Gemini 1.5 Flash Lite**（作为成本效益平衡点）以及开源模型 **Mistral Small 2503**。
*   **算力说明**：文中**未明确提及**具体的 GPU 训练时长或硬件配置。由于该方法主要依赖于现成 VLM 的推理（In-context Learning）而非从头训练，其算力消耗主要集中在 API 调用或本地模型推理上。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括对象提取准确率、谓词提取准确率、目标提取准确率以及端到端的规划成功率。
*   **消融与对比**：针对不同规模的模型（Pro vs. Flash vs. Small）进行了对比，并对“直接规划”与“符号搜索”进行了消融实验。
*   **充分性评价**：实验设计较为充分，涵盖了从简单 2D 仿真到 3D 物理渲染，再到真实图像的跨度。通过在外部基准（ProDG, ViPlan）上取得 SOTA 结果，证明了其客观性和竞争力。

### 6. 主要结论与发现
*   **搜索胜过直接推理**：在 VLM 锚定的状态空间上进行符号搜索，其表现远优于 VLM 直接生成计划。
*   **结构化约束的必要性**：通过 Python 类约束 VLM 输出，可以显著减少解析错误和逻辑不一致，使小型模型（如 Mistral Small）也能在复杂任务中发挥作用。
*   **领域无关性**：SYMBOLIZER 无需针对特定任务调整 Prompt，仅靠少量上下文示例（In-context examples）即可在不同模拟器和任务间泛化。
*   **性能领先**：在 ViPlan 等基准测试中，即使使用较小的模型（Flash Lite），其表现也优于使用 GPT-4 的现有方法。

### 7. 优点与亮点
*   **解耦感知与推理**：将 VLM 定位为“传感器”而非“大脑”，充分利用了 VLM 的视觉理解力，同时规避了其逻辑推理不稳的弱点。
*   **无需动作模型**：解决了 TAMP 中最难自动化的部分（动作效果定义），仅需模拟器即可运行。
*   **极高的泛化性**：一套框架可同时处理图像输入和自然语言指令，且能适应完全不同的物理环境。

### 8. 不足与局限
*   **依赖模拟器**：作为“无模型”方法，它高度依赖于一个可交互的黑盒模拟器来进行状态转移预测，这在纯现实世界（无数字孪生）中难以直接应用。
*   **静态谓词瓶颈**：实验发现，对于一些不随动作改变的静态谓词（如汉诺塔中的盘子大小关系），VLM 有时难以仅凭单张图像准确推断。
*   **感知不确定性**：目前将 VLM 视为确定性传感器，尚未充分处理感知噪声或维护概率性的信念状态（Belief State）。
*   **长程规划开销**：虽然搜索比直接推理更准，但在状态空间极大时，黑盒模拟器的调用次数可能会导致规划速度变慢。

（完）
