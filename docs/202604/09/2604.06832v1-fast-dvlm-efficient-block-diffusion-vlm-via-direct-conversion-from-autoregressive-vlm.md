---
title: "Fast-dVLM: Efficient Block-Diffusion VLM via Direct Conversion from Autoregressive VLM"
title_zh: Fast-dVLM：通过从自回归 VLM 直接转换实现的高效块扩散 VLM
authors: "Chengyue Wu, Shiyi Lan, Yonggan Fu, Sensen Gao, Jin Wang, Jincheng Yu, Jose M. Alvarez, Pavlo Molchanov, Ping Luo, Song Han, Ligeng Zhu, Enze Xie"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06832v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 通过块扩散实现用于机器人和自动驾驶的高效VLM
tldr: Fast-dVLM 针对自回归视觉语言模型（VLM）推理速度慢的问题，提出了一种通过直接转换将 AR VLM 变为块扩散（Block-Diffusion）模型的方法。该方法支持并行解码和投机块解码，通过引入块大小退火、因果上下文注意力和视觉高效拼接等技术，在保持多模态生成质量的同时，实现了超过 6 倍的推理加速，显著提升了边缘设备上的推理效率。
selection_source: fresh_fetch
motivation: 自回归解码在边缘设备上受限于内存带宽，导致硬件并行度利用不足，限制了 VLM 在机器人和自动驾驶等实时场景的应用。
method: 提出一种直接将预训练 AR VLM 转换为块扩散模型的高效方案，并引入块大小退火、因果上下文注意力和自动截断掩码等优化技术。
result: 在 11 个多模态基准测试中，Fast-dVLM 的生成质量与自回归模型相当，且在集成 SGLang 和 FP8 量化后实现了 6 倍以上的端到端推理加速。
conclusion: 通过直接转换策略和块扩散机制，Fast-dVLM 成功解决了 VLM 推理效率瓶颈，为高效多模态智能体部署提供了新路径。
---

## 摘要
视觉语言模型 (VLM) 主要依赖自回归解码，这种方式逐个生成 token，从根本上限制了推理吞吐量。这种限制在机器人和自动驾驶等物理 AI 场景中尤为突出，在这些场景中，VLM 以 batch size 为 1 的形式部署在边缘设备上，使得自回归 (AR) 解码受限于内存带宽，且硬件并行性未得到充分利用。虽然分块离散扩散在并行文本生成方面展现出潜力，但由于需要同时处理连续的视觉表示和离散的文本 token，并保持预训练的多模态能力，将其扩展到 VLM 仍具挑战性。我们提出了 Fast-dVLM，这是一种基于块扩散的 VLM，支持与 KV 缓存兼容的并行解码和投机块解码，以实现推理加速。我们系统地比较了两种从 AR 到扩散的转换策略：一种是两阶段方法，即在多模态训练之前先通过纯文本扩散微调来适配 LLM 主干网络；另一种是直接方法，即在一个阶段内转换完整的 AR VLM。在相当的训练预算下，通过利用已经实现多模态对齐的 VLM，直接转换被证明效率显著更高；因此，我们将其作为推荐方案。我们引入了一系列多模态扩散适配技术，包括块大小退火、因果上下文注意力、自动截断掩码和视觉高效拼接，这些技术共同实现了 VLM 环境下的有效块扩散。在 11 个多模态基准测试上的广泛实验表明，Fast-dVLM 在生成质量上与其自回归对应模型相当。通过 SGLang 集成和 FP8 量化，Fast-dVLM 相比 AR 基线实现了超过 6 倍的端到端推理加速。

## Abstract
Vision-language models (VLMs) predominantly rely on autoregressive decoding, which generates tokens one at a time and fundamentally limits inference throughput. This limitation is especially acute in physical AI scenarios such as robotics and autonomous driving, where VLMs are deployed on edge devices at batch size one, making AR decoding memory-bandwidth-bound and leaving hardware parallelism underutilized. While block-wise discrete diffusion has shown promise for parallel text generation, extending it to VLMs remains challenging due to the need to jointly handle continuous visual representations and discrete text tokens while preserving pretrained multimodal capabilities. We present Fast-dVLM, a block-diffusion-based VLM that enables KV-cache-compatible parallel decoding and speculative block decoding for inference acceleration. We systematically compare two AR-to-diffusion conversion strategies: a two-stage approach that first adapts the LLM backbone with text-only diffusion fine-tuning before multimodal training, and a direct approach that converts the full AR VLM in one stage. Under comparable training budgets, direct conversion proves substantially more efficient by leveraging the already multimodally aligned VLM; we therefore adopt it as our recommended recipe. We introduce a suite of multimodal diffusion adaptations, block size annealing, causal context attention, auto-truncation masking, and vision efficient concatenation, that collectively enable effective block diffusion in the VLM setting. Extensive experiments across 11 multimodal benchmarks show Fast-dVLM matches its autoregressive counterpart in generation quality. With SGLang integration and FP8 quantization, Fast-dVLM achieves over 6x end-to-end inference speedup over the AR baseline.

---

## 论文详细总结（自动生成）

### Fast-dVLM 论文深度总结

#### 1. 核心问题与研究背景
*   **研究动机**：当前的视觉语言模型（VLM）普遍采用自回归（AR）解码，即逐个生成 token。这种串行方式在机器人、自动驾驶等“物理 AI”场景中面临严重瓶颈。
*   **核心矛盾**：在边缘设备上，VLM 通常以 Batch Size = 1 的形式运行，此时 AR 解码受限于内存带宽（Memory-bandwidth-bound），导致硬件并行计算能力（如 GPU Tensor Cores）被大量闲置。
*   **研究目标**：探索如何将预训练的 AR VLM 高效转换为支持并行解码的“块扩散”（Block-Diffusion）模型，在不损失多模态理解能力的前提下，大幅提升推理吞吐量。

#### 2. 方法论：核心思想与关键技术
Fast-dVLM 的核心是将离散扩散模型（Masked Diffusion）与块状自回归结构结合，支持 KV 缓存复用。
*   **直接转换策略（Direct Conversion）**：研究发现，相比于先将 LLM 转换为扩散模型再进行多模态训练的“两阶段法”，直接从预训练好的 AR VLM 进行单阶段多模态扩散微调效率更高，能更好地继承已有的多模态对齐能力。
*   **关键训练技术**：
    *   **因果上下文注意力（Causal Context Attention）**：在块内使用双向注意力进行并行去噪，但对历史块保持因果注意力，以保留预训练模型的序列推理能力。
    *   **块大小退火（Block-size Annealing）**：训练初期使用小块，随进度逐渐增加块大小（最高至 32），帮助模型平滑学习大规模并行去噪。
    *   **自动截断掩码（Auto-truncation Masking）**：针对多模态对话中常见的短回答，自动在回答边界截断块，防止去噪过程泄露下一轮对话的 Prompt 信息。
    *   **视觉高效拼接（Vision-efficient Concatenation）**：由于视觉 token 不会被掩码（Mask），在训练时仅在“清洁流”中保留视觉嵌入，避免在“噪声流”中重复计算，节省约 15% 的显存。
*   **推理加速技术**：
    *   **自投机块解码（Self-speculative Block Decoding）**：利用同一套权重，先用扩散模式生成草稿（Draft），再用因果模式进行验证（Verify），实现无损或近无损的加速。
    *   **系统级集成**：集成至 SGLang 推理引擎，支持 FP8 量化和 CUDA Graph 优化。

#### 3. 实验设计
*   **数据集**：使用了约 200 万条多模态指令微调数据，涵盖通用对话（ShareGPT-4V）、图表理解（ChartQA）、文档理解（DocVQA）、科学推理（AI2D）等。
*   **基准测试（Benchmarks）**：在 11 个主流榜单上进行评估，包括短评测（MMBench, MMMU, POPE, RealWorldQA 等）和长链条推理评测（MMMU-Pro-V）。
*   **对比对象**：
    *   **AR 基线**：Qwen2.5-VL-3B, Intern-VL-2.5-4B, MiniCPM-V-2 等。
    *   **扩散 VLM 基线**：LLaDA-V, LaViDa, Dimple 等。

#### 4. 资源与算力
*   **硬件环境**：使用了 **64 张 NVIDIA H100 GPU**（8 个节点）。
*   **训练细节**：采用 DeepSpeed ZeRO-2 优化器，BF16 混合精度。
*   **训练时长**：在 200 万数据上训练 **1 个 epoch**，以防止模型过拟合。
*   **推理测试**：所有吞吐量（TPS）均在单张 H100 GPU 上以 Batch Size = 1 测得。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 11 个多模态任务，实验覆盖面广。
*   **消融实验**：系统性地验证了因果上下文、块大小退火、自动截断掩码等每个组件的贡献，证明了技术路径的合理性。
*   **公平性**：在对比“直接转换”与“两阶段转换”时，保持了相同的训练数据、计算预算和模型主干，对比结果具有说服力。

#### 6. 主要结论与发现
*   **性能持平**：Fast-dVLM 在绝大多数短回答任务上达到了与 AR 基线相当的准确率。
*   **加速显著**：通过扩散并行解码结合投机采样、SGLang 优化及 FP8 量化，实现了 **6.18 倍** 的端到端推理加速。
*   **转换效率**：直接从 AR VLM 转换比从 AR LLM 开始转换在多模态任务上平均高出 13.1 分，证明了预训练多模态对齐的重要性。

#### 7. 优点
*   **工程落地性强**：不仅提出了算法，还完成了 SGLang 系统集成和 FP8 量化支持，直接面向生产环境。
*   **训练配方科学**：引入的退火策略和视觉高效拼接技术解决了扩散模型在多模态领域训练难、开销大的痛点。
*   **推理灵活**：支持在 MDM（扩散）模式和投机验证模式之间切换，平衡速度与精度。

#### 8. 不足与局限
*   **长文本推理差距**：在需要极长链条推理的任务（如 MMMU-Pro-V）中，Fast-dVLM 与 AR 基线仍有约 1.7% 的微小准确率差距，表明块并行去噪在极长序列的连贯性上仍有提升空间。
*   **二次方投机开销**：提出的“二次方投机解码”变体虽然减少了前向传播次数，但由于 $O(B^2)$ 的计算复杂度，在当前算子优化下实际墙钟时间（Wall-clock time）反而不如线性变体。
*   **应用限制**：主要针对 Batch Size = 1 的边缘推理场景，在云端大 Batch 场景下的优势可能不及其在边缘端的表现。

（完）
