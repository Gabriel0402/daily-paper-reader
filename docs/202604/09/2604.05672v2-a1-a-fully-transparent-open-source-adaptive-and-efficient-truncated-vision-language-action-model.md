---
title: "A1: A Fully Transparent Open-Source, Adaptive and Efficient Truncated Vision-Language-Action Model"
title_zh: A1：一个完全透明开源、自适应且高效的截断式视觉-语言-动作模型
authors: "Kaidong Zhang, Jian Zhang, Rongtao Xu, Yu Sun, Shuoshuo Xue, Youpeng Wen, Xiaoyu Guo, Minghao Guo, Weijia Liufu, Liu Zihou, Kangyi Ji, Yangsong Zhang, Jiarun Zhu, Jingzhi Liu, Zihang Li, Ruiyi Chen, Meng Cao, Jingming Zhang, Shen Zhao, Xiaojun Chang, Feng Zheng, Ivan Laptev, Xiaodan Liang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05672v2"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于机器人操作的高效截断式视觉-语言-动作模型
tldr: A1是一个开源透明的视觉-语言-动作（VLA）模型框架，旨在解决现有模型推理成本高、延迟大的问题。它通过利用预训练VLM的先验知识，并引入预算感知的自适应推理方案，包括中间层动作一致性监测触发的早期停止机制，以及跨层截断流匹配技术。在多个仿真和真实机器人基准测试中，A1在保持甚至超越SOTA成功率的同时，显著降低了计算开销和推理延迟，为低成本、高吞吐量的实时机器人控制提供了有效方案。
selection_source: fresh_fetch
motivation: 现有的VLA模型因庞大的VLM骨干网络和复杂的动作生成头导致推理成本高、延迟大，难以在普通硬件上实现实时控制。
method: 提出A1框架，通过监测中间层动作一致性实现骨干网络早期停止，并利用跨层截断流匹配技术减少去噪迭代次数。
result: "在RoboChallenge等多个基准测试中取得SOTA成功率（29.00%），同时最高减少了72%的推理延迟和76.6%的骨干网络计算量。"
conclusion: A1证明了通过自适应推理和模型截断，可以在不牺牲操作性能的前提下大幅提升VLA模型的效率和实时性。
---

## 摘要
视觉-语言-动作（VLA）模型已成为开放世界机器人操控的一种强大范式，但其实际部署往往受限于成本：十亿级参数规模的 VLM 骨干网络以及基于扩散/流匹配（diffusion/flow-based）的迭代式动作头会产生高延迟和高计算量，使得在通用硬件上进行实时控制变得昂贵。我们提出了 A1，这是一个完全开源且透明的 VLA 框架，旨在不牺牲操控成功率的前提下实现低成本、高吞吐量的推理。我们的方法利用预训练 VLM 为动作生成提供隐式的可操作性先验（affordance priors）。我们发布了完整的训练栈（包括训练代码、数据/数据处理流水线、中间检查点和评估脚本），以实现端到端的复现。除了仅优化 VLM 之外，A1 还通过引入一种预算感知的自适应推理方案来针对整个推理流水线，该方案能够同时加速骨干网络和动作头。具体而言，我们通过监控 VLM 中间层的动作一致性来触发提前终止，并提出了层间截断流匹配（Inter-Layer Truncated Flow Matching），该方法可以在不同层之间热启动去噪过程，从而以显著减少的有效去噪迭代次数实现精确的动作生成。在仿真基准测试（LIBERO、VLABench）和真实机器人（Franka、AgiBot）上，A1 在显著降低推理成本的同时（例如，流匹配推理的单回合延迟降低高达 72%，骨干网络计算量减少高达 76.6% 且性能下降极小），实现了最先进的成功率。在 RoboChallenge 上，A1 达到了 29.00% 的平均成功率，优于包括 pi0 (28.33%)、X-VLA (21.33%) 和 RDT-1B (15.00%) 在内的基准模型。

## Abstract
Vision-Language-Action (VLA) models have emerged as a powerful paradigm for open-world robot manipulation, but their practical deployment is often constrained by cost: billion-scale VLM backbones and iterative diffusion/flow-based action heads incur high latency and compute, making real-time control expensive on commodity hardware. We present A1, a fully open-source and transparent VLA framework designed for low-cost, high-throughput inference without sacrificing manipulation success; Our approach leverages pretrained VLMs that provide implicit affordance priors for action generation. We release the full training stack (training code, data/data-processing pipeline, intermediate checkpoints, and evaluation scripts) to enable end-to-end reproducibility. Beyond optimizing the VLM alone, A1 targets the full inference pipeline by introducing a budget-aware adaptive inference scheme that jointly accelerates the backbone and the action head. Specifically, we monitor action consistency across intermediate VLM layers to trigger early termination, and propose Inter-Layer Truncated Flow Matching that warm-starts denoising across layers, enabling accurate actions with substantially fewer effective denoising iterations. Across simulation benchmarks (LIBERO, VLABench) and real robots (Franka, AgiBot), A1 achieves state-of-the-art success rates while significantly reducing inference cost (e.g., up to 72% lower per-episode latency for flow-matching inference and up to 76.6% backbone computation reduction with minor performance degradation). On RoboChallenge, A1 achieves an average success rate of 29.00%, outperforming baselines including pi0(28.33%), X-VLA (21.33%), and RDT-1B (15.00%).

---

## 论文详细总结（自动生成）

以下是对论文《A1: A Fully Transparent Open-Source, Adaptive and Efficient Truncated Vision-Language-Action Model》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型虽然在开放世界机器人操控中表现出色，但其**部署成本极高**。主要瓶颈在于：1) 十亿级参数的 VLM 骨干网络计算量巨大；2) 基于扩散或流匹配（Flow Matching）的动作头需要多次迭代去噪，导致高延迟，难以在普通硬件上实现实时控制。
*   **研究动机**：作者观察到动作生成存在冗余性（流匹配轨迹在极少步数内即可收敛、连续控制步骤间动作变化平滑、VLM 中间层已包含足够空间特征）。因此，研究目标是开发一个**完全开源透明、低延迟、高吞吐量**且不牺牲成功率的 VLA 框架。

### 2. 方法论
A1 框架由预训练 VLM 骨干网络（基于 Molmo-7B）和动作头（支持 MLP 或流匹配 FM）组成，核心创新在于**预算感知的自适应推理方案**：
*   **动作一致性阈值早期停止（Early-Exit）**：在推理时，模型逐层计算 VLM 隐藏状态并生成动作预测。通过对比当前层与前一层动作的差异（如 L2 距离），若差异低于预设阈值 $\eta_i$，则触发早期停止，无需运行后续 VLM 层。
*   **层间截断流匹配（Inter-Layer Truncated Flow Matching, IL-TFM）**：
    *   针对 FM 动作头迭代耗时的问题，A1 在每个 VLM 层仅运行极少量的去噪步数（如 $\delta=2$）。
    *   **热启动机制**：下一层的去噪过程不再从随机噪声开始，而是从上一层的预测结果开始。这种跨层的信息传递使得模型能以极少的总迭代次数获得精确动作。
*   **多机器人预训练**：利用大规模开源数据集（DROID, AgiBot 等）结合 1.5 万条内部采集轨迹，增强模型的跨平台泛化能力。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：LIBERO（长程操控任务）、VLABench（考察常识与推理）。
    *   **真实机器人**：涵盖 Franka, AgiBot, OpenArm, Dobot-Arm 四种平台，涉及擦桌子、放杯子、烹饪蔬菜等 7 类任务。
    *   **大规模评测**：RoboChallenge Table30 基准测试（包含 30 个真实机器人任务）。
*   **对比方法**：包括 $\pi_0$、$\pi_{0.5}$、OpenVLA、RDT-1B、X-VLA、SmolVLA 等当前最先进的开源或半开源模型。

### 4. 资源与算力
*   **计算量分析**：文中详细对比了 GFLOPs。例如，Molmo-7B 全量推理约需 11,074 GFLOPs；A1-FM 在 $\delta=10$ 时推理时间为 1.15s，而通过 IL-TFM（$\delta=2$）可降至 0.73s。
*   **硬件说明**：文中提到了在“通用硬件（commodity hardware）”上运行的目标，并提及了云端服务器与本地机器人间的网络延迟问题。
*   **训练细节**：**未明确说明具体的 GPU 型号和数量**，但给出了超参数：预训练 Batch Size 为 1024，总步数 200K；微调阶段根据任务不同 Batch Size 在 32 到 128 之间。

### 5. 实验数量与充分性
*   **实验规模**：进行了超过 3000 条真实轨迹的测试，每个任务重复 10 次；仿真实验中 LIBERO 每类任务测试 500 次，VLABench 每类 50 次。
*   **消融实验**：针对不同的退出准则（指数、高斯、伽马分布）、不同的去噪步数 $\delta$、以及热启动机制的有效性进行了详尽的对比。
*   **客观性**：实验覆盖了从仿真到多种不同形态真实机器人的迁移，并参与了第三方基准测试（RoboChallenge），结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：在 RoboChallenge 上以 29.00% 的成功率超越了 $\pi_0$ 和 RDT-1B 等知名模型。
*   **效率极高**：通过自适应推理，在仅损失极小性能的情况下，骨干网络计算量最高可减少 **76.6%**；流匹配推理延迟降低了 **72%**（从 37.8s 降至 10.5s 每回合）。
*   **冗余性证实**：实验证明 VLM 的深层计算在许多简单动作中是冗余的，早期停止机制能有效识别任务复杂度并分配算力。

### 7. 优点
*   **全栈开源**：不仅提供权重，还提供完整的训练代码、数据处理流水线和评估脚本，极大地促进了社区复现。
*   **联合优化**：不同于以往只优化 VLM 或只优化动作头的研究，A1 实现了骨干网络与动作头的协同加速。
*   **热启动创新**：IL-TFM 巧妙地解决了扩散类动作头在早期停止框架下的计算倒挂问题（即动作头计算量反而超过 VLM 的问题）。

### 8. 不足与局限
*   **数据依赖**：预训练仍依赖于带标签的可操作性（affordance）数据，数据规模的扩展受限。
*   **模仿学习通病**：主要基于模仿学习，存在复合误差（cumulative errors）问题，导致在某些极高精度要求的任务中表现尚有提升空间。
*   **部署延迟**：尽管推理加速显著，但在实际部署中，云端推理与本地执行间的网络同步仍可能导致操作不连贯。
*   **阈值标定**：早期停止的阈值 $\eta_i$ 需要在训练集上预先运行一遍来校准，增加了额外的前期准备步骤。

（完）
