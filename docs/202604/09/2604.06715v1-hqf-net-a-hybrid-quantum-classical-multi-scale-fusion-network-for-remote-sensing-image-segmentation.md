---
title: "HQF-Net: A Hybrid Quantum-Classical Multi-Scale Fusion Network for Remote Sensing Image Segmentation"
title_zh: HQF-Net：一种用于遥感图像分割的量子-经典混合多尺度融合网络
authors: "Md Aminur Hossain, Ayush V. Patel, Siddhant Gole, Sanjay K. Singh, Biplab Banerjee"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06715v1"
tags: ["query:mmseg"]
score: 10.0
evidence: 使用DINOv3骨干网络的遥感图像分割
tldr: HQF-Net是一种针对遥感图像分割的混合量子-经典多尺度融合网络。它结合了预训练DINOv3提取的高级语义与定制化U-Net的细节捕获能力，通过可变形多尺度交叉注意力融合模块（DMCAF）进行特征整合。此外，引入了量子增强跳跃连接（QSkip）和量子专家混合瓶颈（QMoE）来优化特征细化。在LandCover.ai等三个基准数据集上表现优异，证明了量子-经典混合架构在提升遥感语义分割性能方面的潜力。
selection_source: fresh_fetch
motivation: 传统的遥感图像分割模型在处理复杂场景时，难以同时兼顾精细的空间细节和全局语义上下文。
method: 提出HQF-Net架构，利用DINOv3提取多尺度语义，并通过DMCAF模块、量子增强跳跃连接（QSkip）以及量子专家混合瓶颈（QMoE）实现特征的高效融合与细化。
result: 在LandCover.ai、OpenEarthMap和SeasoNet三个遥感基准数据集上，HQF-Net均取得了显著的mIoU和总体准确率提升。
conclusion: 结构化的量子-经典混合特征处理是提升近期量子约束下遥感语义分割性能的一个极具前景的研究方向。
---

## 摘要
遥感语义分割要求模型能够同时捕捉复杂场景中的精细空间细节和高层语义上下文。虽然像 U-Net 这样的经典编码器-解码器架构仍然是强大的基准模型，但它们往往难以充分利用全局语义和结构化特征交互。在这项工作中，我们提出了 HQF-Net，一种用于遥感图像分割的量子-经典混合多尺度融合网络。HQF-Net 通过可变形多尺度交叉注意力融合（DMCAF）模块，将来自冻结的 DINOv3 ViT-L/16 骨干网络的多尺度语义引导与定制的 U-Net 架构相结合。为了增强特征细化，该框架进一步引入了量子增强跳跃连接（QSkip）和带有混合专家机制的量子瓶颈（QMoE），后者在自适应路由机制中结合了互补的局部、全局和方向性量子电路。在三个遥感基准数据集上的实验表明，所提出的设计带来了持续的性能提升。HQF-Net 在 LandCover.ai 上实现了 0.8568 的 mIoU 和 96.87% 的总体准确率，在 OpenEarthMap 上实现了 71.82% 的 mIoU，在 SeasoNet 上实现了 55.28% 的 mIoU 和 99.37% 的总体准确率。架构消融研究进一步证实了每个主要组件的贡献。这些结果表明，在近期量子约束下，结构化的量子-经典混合特征处理是提高遥感语义分割性能的一个有前景的方向。

## Abstract
Remote sensing semantic segmentation requires models that can jointly capture fine spatial details and high-level semantic context across complex scenes. While classical encoder-decoder architectures such as U-Net remain strong baselines, they often struggle to fully exploit global semantics and structured feature interactions. In this work, we propose HQF-Net, a hybrid quantum-classical multi-scale fusion network for remote sensing image segmentation. HQF-Net integrates multi-scale semantic guidance from a frozen DINOv3 ViT-L/16 backbone with a customized U-Net architecture through a Deformable Multiscale Cross-Attention Fusion (DMCAF) module. To enhance feature refinement, the framework further introduces quantum-enhanced skip connections (QSkip) and a Quantum bottleneck with Mixture-of-Experts (QMoE), which combines complementary local, global, and directional quantum circuits within an adaptive routing mechanism. Experiments on three remote sensing benchmarks show consistent improvements with the proposed design. HQF-Net achieves 0.8568 mIoU and 96.87% overall accuracy on LandCover.ai, 71.82% mIoU on OpenEarthMap, and 55.28% mIoU with 99.37% overall accuracy on SeasoNet. An architectural ablation study further confirms the contribution of each major component. These results show that structured hybrid quantum-classical feature processing is a promising direction for improving remote sensing semantic segmentation under near-term quantum constraints.

---

## 论文详细总结（自动生成）

### HQF-Net：一种用于遥感图像分割的量子-经典混合多尺度融合网络总结

#### 1. 核心问题与整体含义（研究动机和背景）
遥感图像（RS）语义分割面临着巨大的挑战：图像通常具有极大的空间跨度、显著的类内差异、精细的边界（如道路、建筑物边缘）以及复杂的多分辨率结构。传统的编码器-解码器架构（如 U-Net）虽然表现稳健，但在捕捉全局语义和处理复杂的结构化特征交互方面存在局限。
与此同时，量子机器学习（QML）利用量子叠加和纠缠特性，在处理高维希尔伯特空间变换方面展现出潜力。然而，受限于当前的“含噪声中等规模量子”（NISQ）设备，直接处理高维图像并不现实。因此，论文的研究动机在于探索如何将**自监督视觉表征（DINOv3）**、**经典卷积网络**与**混合量子电路**有机结合，以提升遥感图像在复杂场景下的分割精度。

#### 2. 论文提出的方法论
HQF-Net 采用了一种混合架构，核心思想是在经典的 U-Net 框架中嵌入量子增强模块和多尺度语义引导。
*   **多尺度语义引导与 DMCAF 模块**：利用预训练且冻结的 DINOv3 ViT-L/16 提取高层语义特征。通过**可变形多尺度交叉注意力融合（DMCAF）**模块，将 DINOv3 的语义特征与 U-Net 编码器的空间特征进行对齐。该模块使用可变形注意力机制来降低计算复杂度，并通过 FiLM 门控残差注入实现特征融合。
*   **量子增强跳跃连接（QSkip）**：在跳跃连接路径中引入参数化量子电路（PQC）。它通过量子特征变换提取全局通道间的相关性，对编码器特征进行自适应调制，从而优化传递给解码器的信息。
*   **量子混合专家瓶颈（QMoE）**：在 U-Net 的瓶颈层（特征最压缩处），设计了一个混合专家系统。它包含三个专门的量子专家电路：
    *   **Localist（局部专家）**：捕捉细微的空间模式（如边缘、纹理）。
    *   **Globalist（全局专家）**：通过深度纠缠捕捉长程依赖和全局上下文。
    *   **Diagonal（对角专家）**：捕捉结构化的方向性特征。
    *   通过一个经典的门控网络（Gating Network）根据输入特征动态分配这些专家的权重。

#### 3. 实验设计
*   **数据集**：
    *   **LandCover.ai**：波兰航拍影像，分辨率 25–50 cm/pixel，侧重土地利用制图。
    *   **OpenEarthMap**：全球高分辨率卫星影像基准，涵盖多种地理区域。
    *   **SeasoNet**：德国多时相、多源无人机（UAV）农业影像，包含 33 个语义类别。
*   **基准（Benchmark）与对比方法**：
    *   **经典 CNN/Transformer**：U-Net, U-Net++, DeepLabv3, SegFormer, UNetFormer, ConvNeXt, PyramidMamba 等。
    *   **混合量子模型**：FQCNN, MQCNN, COQCNN 等。

#### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100 80GB PCIe GPU** 进行训练。
*   **软件框架**：基于 PyTorch 和 **PennyLane**（量子微分编程库）。
*   **训练细节**：使用 Adam 优化器，学习率为 0.0001，Batch Size 为 32，训练 100 个 Epoch。量子模拟使用了 `lightning.gpu` 后端和伴随状态（Adjoint）微分方法以加速梯度计算。

#### 5. 实验数量与充分性
论文进行了多维度的实验验证：
*   **定量对比**：在三个主流遥感数据集上分别进行了性能测试，指标包括 mIoU 和总体准确率（OA）。
*   **消融实验**：针对 DINOv3 引导、DMCAF 融合策略、QSkip 和 QMoE 模块分别进行了剥离测试，验证了每个组件的增量贡献。
*   **定性分析**：提供了大量的分割掩码可视化对比图。
实验设计较为充分，涵盖了从航拍到卫星、从单时相到多时相的不同场景，对比对象既包含了最新的经典 SOTA 模型，也包含了早期的量子混合模型，体现了评价的客观性。

#### 6. 主要结论与发现
*   **性能领先**：HQF-Net 在所有三个数据集上均刷新了记录。在 LandCover.ai 上达到 0.8568 mIoU，在 OpenEarthMap 上达到 71.82% mIoU。
*   **量子模块的有效性**：消融实验证明，量子增强的跳跃连接和瓶颈层专家系统显著提升了模型对复杂纹理和细小边界的识别能力。
*   **混合架构的优势**：将大规模预训练模型（DINOv3）的语义能力与量子电路的特征细化能力结合，是解决遥感图像复杂性的一种有效途径。

#### 7. 优点
*   **创新性结合**：首次将 DINOv3 的自监督表征与量子混合专家系统（QMoE）结合用于密集预测任务。
*   **结构化设计**：量子电路的设计具有明确的物理/几何含义（局部、全局、方向性），而非盲目堆叠。
*   **效率优化**：通过 DMCAF 的稀疏采样和瓶颈层的量子处理，在保持量子优势的同时规避了高维数据直接输入的难题。

#### 8. 不足与局限
*   **计算成本**：尽管使用了 GPU 加速模拟，但量子电路的模拟耗时仍远高于纯经典模型，这限制了其在大规模实时任务中的应用。
*   **硬件鸿沟**：所有实验均在经典硬件的模拟器上完成，尚未在真实的 NISQ 量子计算机上验证，真实硬件的噪声和退相干效应可能会影响性能。
*   **参数敏感性**：量子电路的初始化和梯度稳定性（如贫瘠高原问题）在更深的网络中可能仍是潜在风险。
*   **类别不平衡**：在 SeasoNet 等数据集上，虽然 OA 极高（99.37%），但 mIoU 相对较低，反映出模型在处理极少数类时仍有提升空间。

（完）
