---
title: "KD-MARL: Resource-Aware Knowledge Distillation in Multi-Agent Reinforcement Learning"
title_zh: KD-MARL：多智能体强化学习中的资源感知知识蒸馏
authors: "Monirul Islam Pavel, Siyi Hu, Muhammad Anwar Masum, Mahardhika Pratama, Ryszard Kowalczyk, Zehong Jimmy Cao"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06691v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多智能体强化学习中从大规模专家模型到轻量级智能体的知识蒸馏
tldr: "针对多智能体强化学习（MARL）在边缘设备部署时面临的计算和内存限制，本文提出KD-MARL框架。该框架通过两阶段知识蒸馏，将中心化专家的协调行为转移到轻量化、去中心化的异构学生智能体中。通过蒸馏优势信号和结构化策略监督，在减少高达28.6倍计算量的同时，保留了专家90%以上的性能，实现了资源受限环境下的高效协作。"
selection_source: fresh_fetch
motivation: 现有的多智能体强化学习模型因计算开销大而难以在嵌入式平台部署，且传统蒸馏方法往往忽略了智能体间的协调结构和异构性。
method: 提出一种两阶段蒸馏框架，通过蒸馏优势信号和结构化策略监督，将中心化专家的协调模式转移给轻量化的去中心化异构学生智能体。
result: "在SMAC和MPE测试中，该方法在降低计算量高达28.6倍的同时，保留了专家模型90%以上的性能。"
conclusion: KD-MARL证明了通过结构化蒸馏可以在极低资源消耗下维持高水平的多智能体协作，为实际硬件部署提供了有效途径。
---

## 摘要
多智能体强化学习（MARL）系统在现实世界中的部署受到计算内存和推理时间的根本限制。虽然专家策略能够实现高性能，但它们依赖于昂贵的决策周期和大规模模型，这对于边缘设备或嵌入式平台来说是不切实际的。知识蒸馏（KD）为实现资源感知执行提供了一条充满前景的途径，但现有的 MARL 知识蒸馏方法仅局限于动作模仿，往往忽略了协作结构，并假设智能体能力是统一的。我们提出了用于多智能体强化学习的资源感知知识蒸馏（KD-MARL），这是一个两阶段框架，旨在将协作行为从中心化专家转移到轻量级去中心化学生智能体。学生策略在没有评论家（critic）的情况下进行训练，而是依靠蒸馏的优势信号和结构化策略监督，以在异构和受限观测下保持协作。我们的方法从专家策略中转移了动作级行为和结构化协作模式，同时支持异构学生架构，允许每个智能体的模型容量与其观测复杂度相匹配，这对于在部分或受限可观测性以及有限机载资源下的高效执行至关重要。在 SMAC 和 MPE 基准测试上的广泛实验表明，KD-MARL 在大幅降低计算成本的同时，实现了极高的性能保留。在标准多智能体基准测试中，KD-MARL 保留了超过 90% 的专家性能，同时将计算成本（FLOPs）降低了高达 28.6 倍。所提出的方法实现了专家级的协作，并通过结构化蒸馏将其保留，从而实现了 MARL 在资源受限的机载平台上的实际部署。

## Abstract
Real world deployment of multi agent reinforcement learning MARL systems is fundamentally constrained by limited compute memory and inference time. While expert policies achieve high performance they rely on costly decision cycles and large scale models that are impractical for edge devices or embedded platforms. Knowledge distillation KD offers a promising path toward resource aware execution but existing KD methods in MARL focus narrowly on action imitation often neglecting coordination structure and assuming uniform agent capabilities. We propose resource aware Knowledge Distillation for Multi Agent Reinforcement Learning KD MARL a two stage framework that transfers coordinated behavior from a centralized expert to lightweight decentralized student agents. The student policies are trained without a critic relying instead on distilled advantage signals and structured policy supervision to preserve coordination under heterogeneous and limited observations. Our approach transfers both action level behavior and structural coordination patterns from expert policies while supporting heterogeneous student architectures allowing each agent model capacity to match its observation complexity which is crucial for efficient execution under partial or limited observability and limited onboard resources. Extensive experiments on SMAC and MPE benchmarks demonstrate that KD MARL achieves high performance retention while substantially reducing computational cost. Across standard multi agent benchmarks KD MARL retains over 90 percent of expert performance while reducing computational cost by up to 28.6 times FLOPs. The proposed approach achieves expert level coordination and preserves it through structured distillation enabling practical MARL deployment across resource constrained onboard platforms.

---

## 论文详细总结（自动生成）

以下是对论文《KD-MARL: Resource-Aware Knowledge Distillation in Multi-Agent Reinforcement Learning》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：多智能体强化学习（MARL）在机器人、分布式传感和自动驾驶等领域表现优异，但其模型通常规模庞大、计算开销高，难以部署在资源受限的边缘设备、卫星网络或嵌入式平台上。
*   **核心问题**：现有的知识蒸馏（KD）方法在 MARL 中大多仅关注动作模仿（Action Imitation），忽略了智能体间的**协调结构（Coordination Structure）**，且通常假设智能体是同构的，无法处理实际应用中观测能力受限或异构的场景。
*   **研究意义**：提出一种资源感知的蒸馏框架，旨在在极低算力消耗下保留专家级的协作能力。

### 2. 方法论
KD-MARL 采用**两阶段训练框架**，核心思想是将中心化专家的协调知识转移给轻量级的去中心化学生。
*   **第一阶段（专家训练）**：使用标准 MAPPO 算法在全观测（Full Observation）下训练一个高容量的教师模型（包含 Actor 和中心化 Critic）。
*   **第二阶段（教师引导的优势蒸馏）**：
    *   **无评论家训练（Critic-Free）**：学生智能体仅包含轻量级 Actor，不学习 Critic。通过冻结的教师 Critic 计算广义优势估计（GAE）作为信号，直接引导学生策略优化。
    *   **异构架构支持**：根据观测复杂度为不同智能体分配不同容量的学生模型（如隐藏层维度从 256 缩减至 16 或 32）。
*   **复合蒸馏损失函数 ($L_{KD}$)**：
    1.  **动作策略保真度**：通过 KL 散度和交叉熵损失，使学生模仿教师的动作分布。
    2.  **结构关系损失**：利用余弦相似度对齐智能体间的潜在嵌入（Latent Embeddings），保留教师模型中的空间/逻辑协作几何关系。
    3.  **协调角色损失**：通过角色投影矩阵对齐教师与学生的角色表示，防止在模型压缩过程中发生角色缺失。

### 3. 实验设计
*   **实验场景**：
    *   **SMAC (StarCraft Multi-Agent Challenge)**：测试地图包括 3m, 8m, 5m vs 6m, 3s5z。
    *   **MPE (Multi-Agent Particle Environment)**：包括 Speaker-Listener, Simple Spread, Adversary。
*   **基准测试 (Benchmarks)**：对比了 MAPPO、QMIX 和 VDN 在三种设置下的表现：全观测 (FO)、受限异构观测 (LH)、受限异构观测+异构架构 (LH+A)。
*   **评估指标**：胜率 (Win Rate)、奖励值 (Return)、每步推理时间 (TPS)、计算量 (FLOPs)。

### 4. 资源与算力
*   **软件环境**：使用了 EPyMARL 和 PyMARL2 库进行实验。
*   **硬件说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但详细记录了推理阶段的计算成本，例如学生模型的隐藏层维度被压缩至 16 或 32。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流 MARL 基准（SMAC 和 MPE）的 8 个不同任务上进行了测试。
*   **充分性评价**：实验设计较为充分。通过对比 FO、LH 和 LH+A 三种模式，验证了模型在极端资源受限（观测掩码+模型压缩）下的鲁棒性。此外，通过热力图分析了动作选择的协调性，并与其他蒸馏方法（如 CTDS, PTDE）进行了横向对比，证明了其在性能与 FLOPs 之间的平衡能力。

### 6. 主要结论与发现
*   **高性能保留**：KD-MARL 在大幅压缩模型的情况下，保留了专家模型 90% 以上的性能（在 MPE 中性能损失仅为 4-6%）。
*   **极高的压缩比**：相比教师模型，KD-MARL 在 MPE 任务中减少了高达 **28.6 倍** 的 FLOPs，在 SMAC 中减少了 **3.3 到 11.7 倍**。
*   **推理加速**：推理吞吐量显著提升，每步推理时间（TPS）在某些场景下缩短了约 40%。
*   **协作保持**：结构关系损失和角色对齐损失有效地防止了智能体在资源受限时出现“协作崩溃”现象。

### 7. 优点
*   **资源感知性强**：通过“无 Critic 训练”和“异构架构”，极大地降低了机载部署的门槛。
*   **结构化蒸馏**：不只是简单的动作模仿，而是通过潜在空间对齐保留了多智能体间的协作逻辑。
*   **实用性高**：针对部分可观测和异构硬件设计，非常贴合无人机群、卫星星座等真实物理系统的需求。

### 8. 不足与局限
*   **教师依赖性**：学生性能高度依赖于预训练教师模型的质量，若教师模型存在偏差，蒸馏过程会继承该偏差。
*   **离线限制**：目前的框架主要基于两阶段离线蒸馏，对于环境动态剧烈变化、需要在线持续学习的场景，其适应性尚待验证。
*   **算力细节缺失**：缺乏训练阶段的具体资源消耗记录，难以评估从头训练专家再到蒸馏的总时间成本。

（完）
