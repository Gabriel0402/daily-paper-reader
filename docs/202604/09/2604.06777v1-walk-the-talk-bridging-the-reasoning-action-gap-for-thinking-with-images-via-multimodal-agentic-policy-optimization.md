---
title: "Walk the Talk: Bridging the Reasoning-Action Gap for Thinking with Images via Multimodal Agentic Policy Optimization"
title_zh: 言行一致：通过多模态智能体策略优化弥合结合图像思考中的推理-行动差距
authors: "Wenhao Yang, Yu Xia, Jinlong Huang, Shiyin Lu, Qing-Guo Chen, Zhao Xu, Weihua Luo, Kaifu Zhang, Yuchen Zhou, Xiaobo Xia, Yuanyu Wan, Lijun Zhang, Tat-Seng Chua"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06777v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多模态智能体策略优化，弥合多模态大模型的推理与行动差距
tldr: 本研究针对多模态大模型在利用视觉工具推理时存在的“言行不一”问题，即文本推理与实际视觉操作不匹配，提出了多模态智能体策略优化（MAPO）。MAPO要求模型对工具获取的视觉内容生成文本描述，并将描述与实际观察的语义一致性与任务奖励相结合，从而缩小推理与行动间的差距。实验证明，该方法能有效降低梯度方差，显著提升模型在多个视觉推理基准测试中的表现。
selection_source: fresh_fetch
motivation: 现有的强化学习方法仅依赖结果奖励，忽略了模型在推理过程中可能出现的文本逻辑合理但视觉操作错误的问题。
method: 提出MAPO方法，通过要求模型生成视觉内容的文本描述，并将描述与实际观察的语义对齐度融入优势估计中。
result: 实验结果显示，MAPO在多个视觉推理基准测试上均取得了优于现有方法的性能。
conclusion: MAPO通过缩小推理与行动之间的差距，有效提升了多模态大模型在复杂视觉任务中的推理能力和训练稳定性。
---

## 摘要
多模态大语言模型（MLLMs）的最新进展促使模型在多轮推理过程中通过主动调用视觉工具来“结合图像思考”。强化学习（RL）中依赖基于结果的奖励的常见做法忽略了一个事实，即文本上的合理性往往掩盖了执行上的失败，这意味着模型在智能体推理轨迹中可能会表现出直观的文本推理，但执行的视觉动作却不精确或不相关。这种推理与行动之间的差异引入了在多轮推理过程中不断累积的噪声，严重降低了模型的多模态推理能力，并可能导致训练崩溃。在本文中，我们提出了多模态智能体策略优化（MAPO），旨在弥合模型在多模态思维链（MCoT）中生成的文本推理与视觉动作之间的差距。具体而言，MAPO 要求模型为通过工具使用获取的视觉内容生成显式的文本描述。随后，我们采用了一种新颖的优势估计方法，将这些描述与实际观测之间的语义一致性与任务奖励相结合。理论研究证明了 MAPO 背后的合理性，它本质上降低了梯度的方差；广泛的实验表明，我们的方法在多个视觉推理基准测试中均取得了优异的性能。

## Abstract
Recent advancements in Multimodal Large Language Models (MLLMs) have incentivized models to ``think with images'' by actively invoking visual tools during multi-turn reasoning. The common Reinforcement Learning (RL) practice of relying on outcome-based rewards ignores the fact that textual plausibility often masks executive failure, meaning that models may exhibit intuitive textual reasoning while executing imprecise or irrelevant visual actions within their agentic reasoning trajectories. This reasoning-action discrepancy introduces noise that accumulates throughout the multi-turn reasoning process, severely degrading the model's multimodal reasoning capabilities and potentially leading to training collapse. In this paper, we introduce Multimodal Agentic Policy Optimization (MAPO), bridging the gap between textual reasoning and visual actions generated by models within their Multimodal Chain-of-Thought (MCoT). Specifically, MAPO mandates the model to generate explicit textual descriptions for the visual content obtained via tool usage. We then employ a novel advantage estimation that couples the semantic alignment between these descriptions and the actual observations with the task reward. Theoretical findings are provided to justify the rationale behind MAPO, which inherently reduces the variance of gradients, and extensive experiments demonstrate that our method achieves superior performance across multiple visual reasoning benchmarks.

---

## 论文详细总结（自动生成）

## 论文分析报告：Walk the Talk (MAPO)

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文关注的是多模态大语言模型（MLLM）在执行“结合图像思考”（Thinking with Images）任务时的**推理-行动差距（Reasoning-Action Gap）**。
*   **背景**：目前的模型（如 OpenAI o3/o4-mini）能够通过调用视觉工具（如缩放、裁剪）进行多轮推理。
*   **核心问题**：现有的强化学习（RL）训练主要依赖“结果奖励”（Outcome-based Reward），即只看最终答案是否正确。这导致模型可能产生“言行不一”的现象：文本推理逻辑看起来很合理（例如：“我要看左边的红盒子”），但实际执行的视觉动作却是错误的（例如：裁剪了右边的区域）。
*   **后果**：如果模型通过“瞎猜”碰巧得到了正确答案，这种错误的视觉操作会被错误地强化，导致训练过程中噪声累积，甚至引发训练崩溃（Training Collapse）。

### 2. 论文提出的方法论：MAPO
论文提出了**多模态智能体策略优化（Multimodal Agentic Policy Optimization, MAPO）**，核心思想是强制模型实现“言行一致”。

*   **核心思想**：通过一个“验证-强化”循环，确保模型的视觉行动与其文本意图在语义上对齐。
*   **关键技术细节**：
    1.  **Talk（显式意图）**：要求模型在调用工具时，必须同时生成一个简短的文本标签（Label），描述它预期在观察到的新图像中看到什么。
    2.  **Walk（语义对齐）**：使用预训练的轻量级 **CLIP 模型** 作为语义验证器，计算模型生成的“标签”与工具返回的“实际观察图像”之间的余弦相似度，得到**语义得分（Semantic Score）**。
    3.  **轨迹感知折扣因子**：引入折扣因子 $\lambda$，在推理早期给予一定的容错空间（鼓励探索），而在轨迹变长时加大惩罚，防止模型通过冗余操作刷分（Reward Hacking）。
    4.  **优势估计（Advantage Estimation）**：采用类似 GRPO 的组内聚合方式，将语义得分与最终的结果奖励结合，计算优势函数。
*   **理论支撑**：论文从数学上证明了 MAPO 具有**双重方差缩减（Dual Variance Reduction）**效应：
    *   **空间方差缩减**：通过组内聚合过滤掉不同视觉提示带来的随机不确定性。
    *   **信号方差缩减**：通过稠密的语义得分平滑了稀疏结果奖励带来的噪声。

### 3. 实验设计
*   **数据集/场景**：专注于需要精细视觉推理的高分辨率基准测试：
    *   **V***：视觉搜索基准。
    *   **HR-Bench**：高分辨率视觉理解测试。
    *   **MME-Realworld-Lite**：真实世界复杂场景测试。
*   **对比方法**：
    *   **闭源前沿模型**：GPT-5（预测版）、Gemini 2.5 Pro。
    *   **开源智能体模型**：DeepEyes、Thyme、Mini-o3、DeepEyesV2。
    *   **RL 算法基准**：PPO、GRPO、DAPO、GSPO。
*   **基座模型**：Ovis2.5-9B。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：
    *   框架：基于 `verl` 强化学习框架和 `vLLM` 推理引擎。
    *   训练技术：使用了 FSDP（全分片数据并行）和梯度检查点（Gradient Checkpointing）以节省显存。
    *   超参数：全局 Batch Size 为 96，学习率 1e-6，最大交互轮数为 6 轮。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流高分辨率基准上进行了全面测试，涵盖了从 2K 到 8K 分辨率的图像。
*   **消融与对比**：对比了 4 种主流 RL 算法（PPO, GRPO 等），并展示了完整的训练奖励曲线。
*   **充分性评价**：实验设计较为充分。论文不仅提供了量化指标，还通过**可视化推理轨迹**直观展示了 MAPO 如何修正“言行不一”的问题。此外，论文通过长时训练曲线证明了 MAPO 在防止训练崩溃方面的优越性，这在智能体 RL 研究中是非常关键的。

### 6. 论文的主要结论与发现
1.  **性能领先**：MAPO 在所有评估的基准测试中均优于现有的开源智能体模型（如 Mini-o3），并显著超过了传统的 GRPO 和 PPO 算法。
2.  **训练稳定性**：传统的 GRPO 在长时训练中容易出现性能震荡甚至崩溃，而 MAPO 保持了稳定的上升趋势，证明了其良好的可扩展性（Scalability）。
3.  **弥合差距**：可视化证明 MAPO 能引导模型执行更精确的视觉缩放动作，使其“所看即所想”。

### 7. 优点
*   **简单高效**：不需要昂贵的人工中间步骤标注，利用预训练 CLIP 即可实现稠密监督。
*   **理论扎实**：提供了详细的收敛性分析和方差缩减证明，为算法的有效性提供了数学依据。
*   **通用性强**：虽然本文针对视觉工具，但其“意图标签-执行验证”的框架可以推广到网页浏览、代码执行等其他智能体领域。

### 8. 不足与局限
*   **依赖外部模型**：语义对齐的准确性高度依赖于 CLIP 模型的质量，如果 CLIP 无法识别某些细微特征，奖励信号可能会失效。
*   **工具范围有限**：实验主要集中在“缩放/裁剪”这类简单的视觉工具上，对于更复杂的组合工具调用（如同时调用搜索、检测和分割）尚未进行验证。
*   **算力细节缺失**：未公布具体算力消耗，不便于其他研究者评估复现成本。

（完）
