---
title: "ProVG: Progressive Visual Grounding via Language Decoupling for Remote Sensing Imagery"
title_zh: ProVG：基于语言解耦的遥感图像渐进式视觉定位
authors: "Ke Li, Ting Wang, Di Wang, Yongshan Zhu, Yiming Zhang, Tao Lei, Quan Wang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01893v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 遥感图像中的视觉定位与定位
tldr: 针对遥感图像视觉定位（RSVG）中细粒度语言线索利用不足的问题，本文提出ProVG框架。该框架通过将语言描述解耦为全局上下文、空间关系和物体属性，并采用“勘察-定位-验证”的渐进式跨模态调制方案，实现了从粗到精的视觉语言对齐。此外，结合跨尺度融合和语言引导校准，ProVG在多个基准测试上达到了SOTA性能，并支持目标检测与分割任务。
selection_source: fresh_fetch
motivation: 现有方法主要依赖句子级对齐，难以充分利用空间关系和物体属性等细粒度语言线索来区分遥感影像中特征相似的目标。
method: 提出一种渐进式跨模态调制器，通过解耦语言信息并结合跨尺度融合模块与语言引导校准解码器，实现动态的视觉注意力调节。
result: 在RRSIS-D和RISBench两个基准数据集上的实验表明，ProVG在定位和分割任务上均显著优于现有方法，达到了新的SOTA水平。
conclusion: 通过语言解耦和渐进式对齐策略，ProVG有效解决了遥感场景下目标尺度多变和特征相似带来的定位难题。
---

## 摘要
遥感视觉定位（RSVG）旨在根据自然语言表达式在遥感图像中定位目标。以往的方法通常依赖于句子级的视觉-语言对齐，难以利用细粒度的语言线索（如空间关系和目标属性），而这些线索对于区分具有相似特征的目标至关重要。重要的是，这些线索在不同的定位阶段发挥着不同的作用，应当据此加以利用，以提供更明确的引导。在这项工作中，我们提出了 ProVG，这是一种新型的 RSVG 框架，通过将语言表达式解耦为全局上下文、空间关系和目标属性来提高定位精度。为了整合这些语言线索，ProVG 采用了一个简单而有效的渐进式跨模态调制器，通过“勘察-定位-验证”（survey-locate-verify）方案动态调制视觉注意力，实现从粗到细的视觉-语言对齐。此外，ProVG 结合了一个跨尺度融合模块以缓解遥感图像中的大尺度变化，并配备了一个语言引导的校准解码器以在预测过程中细化跨模态对齐。统一的多任务头进一步使 ProVG 能够同时支持指代性表达理解和分割任务。在 RRSIS-D 和 RISBench 两个基准数据集上的广泛实验表明，ProVG 始终优于现有方法，达到了新的最先进性能。

## Abstract
Remote sensing visual grounding (RSVG) aims to localize objects in remote sensing imagery according to natural language expressions. Previous methods typically rely on sentence-level vision-language alignment, which struggles to exploit fine-grained linguistic cues, such as \textit{spatial relations} and \textit{object attributes}, that are crucial for distinguishing objects with similar characteristics. Importantly, these cues play distinct roles across different grounding stages and should be leveraged accordingly to provide more explicit guidance. In this work, we propose \textbf{ProVG}, a novel RSVG framework that improves localization accuracy by decoupling language expressions into global context, spatial relations, and object attributes. To integrate these linguistic cues, ProVG employs a simple yet effective progressive cross-modal modulator, which dynamically modulates visual attention through a \textit{survey-locate-verify} scheme, enabling coarse-to-fine vision-language alignment. In addition, ProVG incorporates a cross-scale fusion module to mitigate the large-scale variations in remote sensing imagery, along with a language-guided calibration decoder to refine cross-modal alignment during prediction. A unified multi-task head further enables ProVG to support both referring expression comprehension and segmentation tasks. Extensive experiments on two benchmarks, \textit{i.e.}, RRSIS-D and RISBench, demonstrate that ProVG consistently outperforms existing methods, achieving new state-of-the-art performance.

---

## 论文详细总结（自动生成）

以下是对论文《ProVG: Progressive Visual Grounding via Language Decoupling for Remote Sensing Imagery》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：遥感图像视觉定位（RSVG）任务面临目标尺度变化剧烈、分布密集及背景复杂的挑战。现有方法多依赖于**句子级**的整体特征对齐，忽略了语言中细粒度的线索（如空间关系、物体属性），导致在区分特征相似的目标时表现不佳。
*   **研究动机**：受人类认知心理学启发，人类定位目标遵循“先全局理解、再空间定位、最后属性验证”的阶段性过程。论文旨在通过**语言解耦**和**渐进式推理**，模拟这一过程以提升遥感场景下的定位精度。

### 2. 方法论
ProVG 框架的核心思想是将复杂的视觉-语言对齐任务分解为多个有序的子阶段：
*   **语言解耦（Language Decoupling）**：利用自然语言处理工具（NLTK）将输入的指代性表达式拆解为三种特征：**全局上下文（$L_c$）**、**空间线索（$L_s$）**和**属性线索（$L_a$）**。
*   **渐进式跨模态调制器（PCM）**：这是核心模块，采用“勘察-定位-验证”（Survey-Locate-Verify）方案：
    1.  **勘察（Survey）**：利用全局上下文建立语义感知，生成初步的注意力权重。
    2.  **定位（Locate）**：利用空间线索缩小候选区域，增强空间显著性。
    3.  **验证（Verify）**：利用属性线索进行通道级调制，确认目标身份并消除歧义。
*   **跨尺度融合模块（CFM）**：通过自顶向下和自底向上的双向交互，融合多尺度特征，应对遥感图像中目标尺度差异巨大的问题。
*   **语言引导校准解码器（LCD）**：在解码阶段引入全局语言引导，通过校准门控机制动态调整特征，并配合统一的多任务头同时输出边界框（REC）和分割掩码（RES）。
*   **损失函数**：结合了 Smooth L1、GIoU（定位）、交叉熵、Dice（分割）以及一致性约束损失（Lcons），确保预测框与掩码的几何一致性。

### 3. 实验设计
*   **数据集**：在两个主流遥感视觉定位基准上进行评估：
    *   **RRSIS-D**：包含丰富的遥感图像和对应的指代性标注。
    *   **RISBench**：更具挑战性，包含更长的表达式、更复杂的空间关系和更密集的场景。
*   **对比方法**：对比了 19 种先进方法，涵盖：
    *   **VLM 方法**：如 GeoGround, SegEarth-R1（基于大语言模型的方法）。
    *   **REC 专用方法**：如 LQVG, LPVA。
    *   **RES 专用方法**：如 RMISN, FIANet, CroBIM。
    *   **多任务方法**：如 RefTR, EEVG。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA GeForce RTX 4090 GPU**（每台 24GB 显存）。
*   **训练细节**：采用 AdamW 优化器，初始学习率为 $3 \times 10^{-5}$，总计训练 **70 个 epoch**。在第 50 和 60 个 epoch 时学习率衰减 0.1 倍。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的定量对比实验和定性可视化分析。
*   **消融实验**：非常充分，涵盖了：
    1.  **模块有效性**：分别验证了 PCM、CFM、LCD 等组件的作用。
    2.  **调制顺序**：对比了“勘察-定位-验证”不同排列组合的效果，证明了 S-L-V 顺序的最优性。
    3.  **损失权重**：探讨了不同损失项对最终性能的影响。
*   **客观性**：在两个不同难度的基准上均取得了 SOTA 性能，且对比了包括基于 7B 参数大模型在内的多种架构，实验设计公平且具有说服力。

### 6. 主要结论与发现
*   **渐进式优于并行式**：实验证明，按顺序注入解耦的语言线索比并行注入或仅使用整体句子特征效果更好。
*   **SOTA 性能**：ProVG 在 RRSIS-D 和 RISBench 上均刷新了纪录。例如在 RISBench 上，其 mIoU 比之前的最强竞争对手提升了约 2.75%~2.89%。
*   **高效性**：尽管没有使用超大规模参数的 LLM，ProVG 依然在精度上超越了部分基于大模型的遥感定位框架，证明了结构化推理的效率。

### 7. 优点（亮点）
*   **认知启发**：将人类的阶段性感知机制成功转化为神经网络的模块化设计。
*   **语言解耦策略**：通过显式拆分空间和属性线索，解决了遥感图像中“同类目标多、空间关系复杂”导致的定位漂移问题。
*   **多任务统一**：一个框架同时解决检测与分割，且通过一致性损失增强了两者之间的逻辑关联。

### 8. 不足与局限
*   **预处理依赖**：语言解耦依赖于 NLTK 等外部工具进行规则化拆分，对于语法极度不规范或口语化严重的表达式，解耦质量可能会影响后续性能。
*   **计算复杂度**：虽然比大模型轻量，但 PCM 模块在骨干网络的每个阶段都进行三次顺序注意力操作，相比单次融合的方法，推理延迟可能会略有增加（文中未详细给出推理耗时对比）。
*   **应用限制**：目前主要针对 RGB 遥感图像，对于多源数据（如 SAR、高光谱）的泛化能力尚待验证。

（完）
