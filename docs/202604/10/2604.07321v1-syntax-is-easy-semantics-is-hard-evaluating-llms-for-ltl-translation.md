---
title: "Syntax Is Easy, Semantics Is Hard: Evaluating LLMs for LTL Translation"
title_zh: 语法易，语义难：评估大语言模型在 LTL 翻译中的表现
authors: "Priscilla Kyei Danso, Mohammad Saqib Hasan, Niranjan Balasubramanian, Omar Chowdhury"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07321v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 评估大语言模型将自然语言翻译为线性时序逻辑公式的能力
tldr: 线性时序逻辑（LTL）在软件安全和隐私策略中至关重要，但其复杂的语义使得编写极具挑战。本文评估了大型语言模型（LLM）将自然语言翻译为LTL公式的能力。通过在人工和合成数据集上的测试，研究发现LLM在语法上表现良好但在语义准确性上仍有欠缺。研究提出通过详细提示词和将任务转化为Python代码补全形式可显著提升翻译性能，为未来开发更可靠的LTL翻译工具提供了指导。
selection_source: fresh_fetch
motivation: 由于LTL语义复杂且难以编写，本研究旨在评估LLM是否能有效降低非专家使用LTL进行安全和隐私分析的门槛。
method: 研究者利用人工和合成的基准数据集，从语法和语义维度对比了多种LLM在不同提示词策略及任务表述下的翻译表现。
result: 结果显示LLM在语法层面表现较好但语义处理较弱，且通过详细提示和重构为Python代码补全任务可显著提升性能。
conclusion: LLM在LTL翻译中具有潜力，但语义准确性仍是核心挑战，未来研究应关注更优的任务建模和公平的评估方法。
---

## 摘要
命题线性时序逻辑（LTL）是一种用于规定软件、网络和系统的理想需求以及安全与隐私策略的流行形式化方法。然而，由于其复杂的语义，使用 LTL 表达此类需求和策略仍然具有挑战性。由于许多安全和隐私分析工具需要 LTL 公式作为输入，这一困难使得许多开发人员和分析人员无法使用这些工具。大语言模型（LLMs）可以通过将自然语言片段翻译成 LTL 公式，从而扩大此类工具的使用范围。本文通过评估几种具有代表性的 LLM 将陈述性英语句子翻译为 LTL 公式的有效性，对这一前提进行了评估。利用人工生成和合成的基准真相数据，我们从语法和语义维度评估了其有效性。结果揭示了三个发现：（1）与之前的发现一致，LLM 在 LTL 的语法方面表现优于语义方面；（2）它们通常受益于更详细的提示词；（3）将任务重新表述为 Python 代码补全问题可显著提高整体性能。我们还讨论了在此任务上进行公平评估的挑战，并对未来的工作提出了建议。

## Abstract
Propositional Linear Temporal Logic (LTL) is a popular formalism for specifying desirable requirements and security and privacy policies for software, networks, and systems. Yet expressing such requirements and policies in LTL remains challenging because of its intricate semantics. Since many security and privacy analysis tools require LTL formulas as input, this difficulty places them out of reach for many developers and analysts. Large Language Models (LLMs) could broaden access to such tools by translating natural language fragments into LTL formulas. This paper evaluates that premise by assessing how effectively several representative LLMs translate assertive English sentences into LTL formulas. Using both human-generated and synthetic ground-truth data, we evaluate effectiveness along syntactic and semantic dimensions. The results reveal three findings: (1) in line with prior findings, LLMs perform better on syntactic aspects of LTL than on semantic ones; (2) they generally benefit from more detailed prompts; and (3) reformulating the task as a Python code-completion problem substantially improves overall performance. We also discuss challenges in conducting a fair evaluation on this task and conclude with recommendations for future work.

---

## 论文详细总结（自动生成）

这篇论文《Syntax Is Easy, Semantics Is Hard: Evaluating LLMs for LTL Translation》对大语言模型（LLM）在自然语言（NL）到线性时序逻辑（LTL）翻译任务中的表现进行了系统且深入的评估。以下是该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：线性时序逻辑（LTL）是形式化验证、安全策略和系统需求规范的核心工具。然而，LTL 的语义极其复杂且不直观，即使是专家也难以准确编写。
*   **核心问题**：LLM 是否能作为自然语言接口，将非专家的安全/隐私需求准确翻译为 LTL 公式？
*   **背景**：虽然 LLM 在代码生成和自然语言处理方面表现出色，但在逻辑严密性要求极高的时序逻辑翻译中，其可靠性尚未得到充分验证。

### 2. 方法论
论文设计了一个六层评估框架，从语法和语义两个维度考察 LLM：
*   **核心思想**：通过引入不同程度的结构化约束（提示词工程），观察模型在逻辑推理上的变化。
*   **关键技术细节（三种接口/提示策略）**：
    1.  **Minimal Interface（极简接口）**：仅提供任务描述和基本的符号映射。
    2.  **Detailed Interface（详细接口）**：提供完整的 BNF 语法、语义定义、操作符约束和示例。
    3.  **Python (AST) Interface（Python 抽象语法树接口）**：将 LTL 翻译任务重构为 Python 代码补全任务。利用 Python 的 `dataclass` 构建 AST，强制模型遵循结构化输出。
*   **语义评判标准**：不使用模糊的字符串匹配，而是使用 **NuSMV（符号模型检测器）** 作为外部语义 Oracle。通过双向蕴含（$\phi_{pred} \Rightarrow \phi_{gt}$ 且 $\phi_{gt} \Rightarrow \phi_{pred}$）来验证逻辑等价性。

### 3. 实验设计
*   **数据集**：
    *   `DS tricky`：源自 Little Tricky Logic，包含复杂的嵌套时序逻辑。
    *   `DS pastltl`：包含过去时态操作符的 LTL。
    *   `DS book`：从逻辑教科书中收集的经典案例。
    *   `DS verify`：专门针对安全领域（身份验证、访问控制等）的 56 个真实需求。
    *   此外还包括用于语法检查（`DS syntax`）和迹推理（`DS trace`）的合成数据集。
*   **对比方法/模型**：评估了 GPT-3.5-Turbo, GPT-4o, GPT-4o-Mini, Gemini-1.5-Flash/Pro, Gemini-2.5-Flash, 以及 Claude-3.5-Sonnet 和 Claude-4.5。
*   **任务类型**：原子命题（AP）提取、公式合法性检测、NL 到 Future/Past LTL 翻译、迹（Trace）分类与生成。

### 4. 资源与算力
*   论文主要使用商业模型的 API（OpenAI, Google, Anthropic）进行推理测试。
*   **算力说明**：文中未提及具体的 GPU 训练细节，因为研究重点在于评估现有的预训练模型（Off-the-shelf LLMs）在不同提示策略下的表现，而非从头训练或微调模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了数千个测试用例，跨越了从语法解析到复杂语义推理的多个维度。
*   **充分性**：实验设计非常全面，不仅对比了不同模型，还对比了 Zero-shot、Few-shot 和自我修正（Self-Refinement）策略。
*   **客观性**：引入 NuSMV 这一形式化工具作为客观标准，避免了人工评估的主观偏差，实验结果具有高度的可重复性和说服力。

### 6. 主要结论与发现
*   **语法易，语义难**：LLM 能够生成语法正确的公式，但在处理复杂的时序语义（如操作符嵌套作用域）时经常出错。
*   **Python 接口最优**：将任务表述为 Python 代码补全显著提升了性能（最高提升至 72% 以上的等价率），这表明 LLM 在代码语料上的训练有助于其理解结构化逻辑。
*   **安全领域更具挑战**：在安全需求（`DS verify`）中，原子命题（AP）的准确对应（Grounding）比通用基准测试更难，且操作符作用域错误仍是主要失败原因。
*   **提示词效应**：详细的语法说明能显著减少语法错误，但对解决深层语义误解（如混淆 G 和 F 操作符）效果有限。

### 7. 优点
*   **评估维度严谨**：通过逻辑等价性而非文本相似度进行评估，抓住了形式化方法的本质。
*   **创新性接口设计**：提出将逻辑公式映射为 Python AST，利用了 LLM 强大的代码处理能力来约束逻辑生成。
*   **安全相关性**：专门针对安全和隐私策略进行了端到端评估，具有很强的实际应用指导意义。

### 8. 不足与局限
*   **数据污染风险**：由于使用了公开的教科书和基准数据集，无法完全排除这些数据已存在于 LLM 训练集中的可能性（尽管研究者认为这代表了性能上限）。
*   **需求复杂度限制**：实验主要针对短小的自然语言片段，而现实中的工业级安全需求文档通常更长、上下文依赖更深。
*   **AP 映射依赖**：大多数实验假设已知 NL 片段到 AP 的映射，但在实际场景中，自动提取并对齐 AP 仍然是一个巨大的挑战。

（完）
