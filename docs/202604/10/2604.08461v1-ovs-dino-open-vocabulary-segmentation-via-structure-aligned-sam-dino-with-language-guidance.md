---
title: "OVS-DINO: Open-Vocabulary Segmentation via Structure-Aligned SAM-DINO with Language Guidance"
title_zh: OVS-DINO：通过语言引导的结构对齐 SAM-DINO 实现开放词汇分割
authors: "Haoxi Zeng, Qiankun Liu, Yi Bin, Haiyue Zhang, Yujuan Ding, Guoqing Wang, Deqiang Ouyang, Heng Tao Shen"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08461v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 使用SAM和DINO基础模型进行开放词汇分割
tldr: 本研究针对开放词汇分割（OVS）中语义泛化与空间感知难以兼顾的问题，提出 OVS-DINO 框架。研究发现 DINO 的边界感知能力随网络加深而衰减，为此引入结构感知编码器（SAE）和结构调制解码器（SMD），利用 SAM 的结构先验激活 DINO 的潜在边缘敏感性，并结合 SAM 伪掩码进行监督。该方法在多个基准测试中达到 SOTA，显著提升了复杂场景下的分割精度。
selection_source: fresh_fetch
motivation: 旨在解决 DINO 特征在深层网络中边界感知能力衰减的问题，以提升开放词汇分割的边缘精度。
method: 提出 OVS-DINO 框架，通过结构感知编码器和结构调制解码器将 SAM 的结构先验与 DINO 对齐，并利用伪掩码进行监督。
result: "在多个弱监督 OVS 基准上取得 SOTA 性能，平均得分提升 2.1%，在 Cityscapes 复杂场景下提升达 6.3%。"
conclusion: 通过将 SAM 的结构信息有效整合进 DINO 框架，显著增强了模型在复杂场景下的精细化分割能力。
---

## 摘要
开放词汇分割（OVS）旨在利用语义描述来分割超出预定义类别集的图像区域。虽然基于 CLIP 的方法在语义泛化方面表现出色，但它们往往缺乏密集预测所需的细粒度空间感知。最近的研究引入了 DINO 等视觉基础模型（VFM）来缓解这些局限性。然而，这些方法在实现高保真分割所需的精确边缘感知方面仍然面临挑战。在本文中，我们分析了 DINO 的内部表示，并发现其固有的边界意识并非不存在，而是随着特征向更深的 Transformer 块传递而逐渐衰减。为了解决这一问题，我们提出了 OVS-DINO，这是一个通过与 Segment Anything Model (SAM) 进行结构对齐来激活 DINO 潜在边缘敏感性的新颖框架。具体而言，我们引入了结构感知编码器（SAE）和结构调制解码器（SMD），利用 SAM 的结构先验有效激活 DINO 的边界特征，并辅以利用 SAM 生成的伪掩码的监督策略。大量实验表明，我们的方法在多个弱监督 OVS 基准测试中达到了最先进的性能，平均得分提高了 2.1%（从 44.8% 提升至 46.9%）。值得注意的是，我们的方法显著提高了复杂、凌乱场景下的分割精度，在 Cityscapes 数据集上获得了 6.3% 的提升（从 36.6% 提升至 42.9%）。

## Abstract
Open-Vocabulary Segmentation (OVS) aims to segment image regions beyond predefined category sets by leveraging semantic descriptions. While CLIP based approaches excel in semantic generalization, they frequently lack the fine-grained spatial awareness required for dense prediction. Recent efforts have incorporated Vision Foundation Models (VFMs) like DINO to alleviate these limitations. However, these methods still struggle with the precise edge perception necessary for high fidelity segmentation. In this paper, we analyze internal representations of DINO and discover that its inherent boundary awareness is not absent but rather undergoes progressive attenuation as features transition into deeper transformer blocks. To address this, we propose OVS-DINO, a novel framework that revitalizes latent edge-sensitivity of DINO through structural alignment with the Segment Anything Model (SAM). Specifically, we introduce a Structure-Aware Encoder (SAE) and a Structure-Modulated Decoder (SMD) to effectively activate boundary features of DINO using SAM's structural priors, complemented by a supervision strategy utilizing SAM generated pseudo-masks. Extensive experiments demonstrate that our method achieves state-of-the-art performance across multiple weakly-supervised OVS benchmarks, improving the average score by 2.1% (from 44.8% to 46.9%). Notably, our approach significantly enhances segmentation accuracy in complex, cluttered scenarios, with a gain of 6.3% on Cityscapes (from 36.6% to 42.9%).

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **OVS-DINO** 的新型开放词汇分割（Open-Vocabulary Segmentation, OVS）框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机与背景）
*   **核心问题**：现有的 OVS 方法主要依赖 CLIP 等视觉语言模型，虽然语义泛化能力强，但缺乏密集的像素级空间感知。虽然引入 DINO 等视觉基础模型（VFM）有所改善，但在处理复杂场景的精确边界（如物体轮廓）时仍然乏力。
*   **研究发现**：作者通过频域分析发现，DINO 并非没有边界意识，而是其边界感知能力随着网络层数加深而逐渐衰减（表现为一种“低通滤波器”效应），深层特征更偏向抽象语义。
*   **整体含义**：旨在通过引入 **SAM（Segment Anything Model）** 的结构先验来“激活” DINO 深层特征中流失的边缘敏感性，同时保持其原有的语义对齐能力。

### 2. 方法论
核心思想是利用 SAM 的结构化知识作为教师，引导 DINO 特征进行结构对齐。
*   **结构感知编码器 (SAE)**：聚合 DINO 的多层特征（浅层到深层），通过卷积层进行线性重组，并将其投影到 SAM 的潜在空间进行对齐。
*   **结构对齐损失 ($L_{Align}$)**：包含余弦相似度损失和均方误差（MSE）损失，强制 SAE 输出的特征在语义和结构上接近 SAM 的特征。
*   **结构调制解码器 (SMD)**：将增强后的特征映射回 DINO 的语义流。
*   **保存门 (Preservation Gate, PG)**：这是关键技术细节，通过残差连接和 Tanh 激活函数，确保在注入结构信息时不会破坏 DINO 原有的跨模态语义空间，防止语义退化。
*   **弱监督训练流程**：
    1.  使用 NLTK 从图像标题中提取名词。
    2.  利用 Grounding DINO 定位物体并结合 SAM 生成伪掩码（Pseudo-masks）。
    3.  使用 Dice 损失和交叉熵损失对预测掩码进行监督。

### 3. 实验设计
*   **数据集**：涵盖了 8 个主流基准测试，包括 PASCAL VOC 20/21、PASCAL Context 59/60、COCO-Stuff、COCO-Object、Cityscapes 和 ADE20K。
*   **Benchmark**：弱监督开放词汇分割（仅使用图像-文本对训练）。
*   **对比方法**：对比了 GroupViT、TCL、CoDe、CLIP-DINOiser、Talk2DINO、DSGS 等十余种最先进（SOTA）的方法。

### 4. 资源与算力
*   **硬件说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：Batch size 为 64，学习率为 $1 \times 10^{-4}$（模块）和 $1 \times 10^{-5}$（DINO 微调），共训练 25 个 epoch，使用 AdamW 优化器。输入图像尺寸为 $1024 \times 1024$（SAM 端）和 $448 \times 448$（DINO 端）。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同难度的标准数据集上进行了全面测试，涵盖了从简单物体到复杂城市街道场景。
*   **消融实验**：非常充分。分别验证了 SAM 引导的有效性、SAE/SMD 模块的作用、保存门（PG）的必要性、不同层选择的影响以及微调策略。
*   **客观性**：通过 CKA 相似度分析和频域能量分布图（DFT）从理论上支撑了实验动机，实验对比均采用标准 mIoU 指标，结果具有较高的说服力。

### 6. 主要结论与发现
*   **性能提升**：OVS-DINO 在所有基准上均达到 SOTA，平均 mIoU 提升 2.1%。在复杂场景（Cityscapes）中提升尤为显著，达 **6.3%**。
*   **知识迁移**：通过训练，DINO 成功内化了 SAM 的结构感知能力。
*   **推理效率**：在推理阶段，**不需要运行 SAM 模型**，仅需 DINO 和轻量级的 SAE/SMD 模块，这使得模型在保持高精度的同时具备较高的推理效率。

### 7. 优点（亮点）
*   **理论支撑扎实**：通过频域分析揭示了 DINO 特征衰减的本质原因，为方法论提供了科学依据。
*   **结构设计巧妙**：引入“保存门”解决了强行对齐可能导致的语义空间崩溃问题。
*   **实用性强**：推理时摆脱了对巨大模型 SAM 的依赖，降低了部署成本。
*   **复杂场景表现优异**：在物体密集、边界模糊的场景下展现了极强的鲁棒性。

### 8. 不足与局限
*   **伪标签依赖**：模型性能高度依赖于 Grounding DINO 和 SAM 生成的伪掩码质量，若初始检测出错，会产生错误传播。
*   **细小物体挑战**：在失败案例分析中提到，对于极小物体（如路灯、旗帜）或视觉特征极不明显的区域，分割精度仍有提升空间。
*   **语义混淆**：在极其凌乱的室内场景中，语义相近的类别（如墙壁与柱子）仍存在分类混淆的风险。

（完）
