---
title: "Tarot-SAM3: Training-free SAM3 for Any Referring Expression Segmentation"
title_zh: Tarot-SAM3：面向任意指代性表达分割的免训练 SAM3
authors: "Weiming Zhang, Dingwen Xiao, Songyue Guo, Guangyu Xiang, Shiqi Wen, Minwei Zhao, Lei Chen, Lin Wang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07916v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 将SAM3应用于指代性表达分割并结合多模态大模型
tldr: Tarot-SAM3是一个无需训练的指代分割（RES）框架，旨在解决现有模型在处理复杂或隐式表达时的局限性。该框架结合了SAM3的分割能力与多模态大语言模型的推理能力，通过“表达式推理转换器”生成结构化提示，并利用“掩码自精炼”阶段结合DINOv3特征进行结果修正。它在显式、隐式及开放世界RES任务中均表现出色，显著提升了分割的准确性和泛化性。
selection_source: fresh_fetch
motivation: 现有RES方法依赖大量标注且难以处理复杂表达，而SAM3在处理长文本或隐式查询时存在推理不足及缺乏结果精炼的问题。
method: 提出包含表达式推理转换器（ERI）和掩码自精炼（MSR）的双阶段框架，通过结构化解析生成提示并利用DINOv3特征关系修正分割掩码。
result: 实验证明Tarot-SAM3在显式和隐式RES基准测试以及开放世界场景中均取得了强劲的性能表现。
conclusion: 该研究通过免训练的方式成功将SAM3扩展至任意指代分割任务，为视觉语言理解提供了一种高效且鲁棒的新方案。
---

## 摘要
指代性表达分割 (RES) 旨在分割由自然语言表达式描述的图像区域，作为视觉与语言理解之间的桥梁。然而，现有的 RES 方法严重依赖大规模标注数据集，且局限于显式或隐式表达，阻碍了其泛化到任意指代性表达的能力。最近，Segment Anything Model 3 (SAM3) 在可提示概念分割中展现了出色的鲁棒性。尽管如此，将其应用于 RES 仍面临挑战：(1) SAM3 难以处理较长或隐式的表达式；(2) 将 SAM3 与多模态大语言模型 (MLLM) 简单耦合会导致最终结果过度依赖 MLLM 的推理能力，且无法对 SAM3 的分割输出进行精细化处理。为此，我们提出了 Tarot-SAM3，这是一种新颖的免训练框架，能够根据任意指代性表达进行精确分割。具体而言，Tarot-SAM3 包含两个关键阶段。首先，表达式推理阐释器 (ERI) 阶段引入了推理辅助提示选项，以支持结构化表达式解析和评估感知重写。这能将任意查询转换为鲁棒的异构提示，从而利用 SAM3 生成可靠的掩码。其次，掩码自精细化 (MSR) 阶段从不同提示类型中选择最佳掩码，并利用 DINOv3 丰富的特征关系来比较 ERI 输出中的判别性区域，从而进行自精细化。随后，它推断区域与目标的归属关系，从而纠正过度分割和欠分割问题。广泛的实验表明，Tarot-SAM3 在显式和隐式 RES 基准测试以及开放世界场景中均取得了强大的性能。消融研究进一步验证了每个阶段的有效性。

## Abstract
Referring Expression Segmentation (RES) aims to segment image regions described by natural-language expressions, serving as a bridge between vision and language understanding. Existing RES methods, however, rely heavily on large annotated datasets and are limited to either explicit or implicit expressions, hindering their ability to generalize to any referring expression. Recently, the Segment Anything Model 3 (SAM3) has shown impressive robustness in Promptable Concept Segmentation. Nonetheless, applying it to RES remains challenging: (1) SAM3 struggles with longer or implicit expressions; (2) naive coupling of SAM3 with a multimodal large language model (MLLM) makes the final results overly dependent on the MLLM's reasoning capability, without enabling refinement of SAM3's segmentation outputs. To this end, we present Tarot-SAM3, a novel training-free framework that can accurately segment from any referring expression. Specifically, Tarot-SAM3 consists of two key phases. First, the Expression Reasoning Interpreter (ERI) phase introduces reasoning-assisted prompt options to support structured expression parsing and evaluation-aware rephrasing. This transforms arbitrary queries into robust heterogeneous prompts for generating reliable masks with SAM3. Second, the Mask Self-Refining (MSR) phase selects the best mask across prompt types and performs self-refinement by leveraging rich feature relationships from DINOv3 to compare discriminative regions among ERI outputs. It then infers region affiliation to the target, thereby correcting over- and under-segmentation. Extensive experiments demonstrate that Tarot-SAM3 achieves strong performance on both explicit and implicit RES benchmarks, as well as open-world scenarios. Ablation studies further validate the effectiveness of each phase.

---

## 论文详细总结（自动生成）

以下是对论文《Tarot-SAM3: Training-free SAM3 for Any Referring Expression Segmentation》的结构化总结：

### 1. 核心问题与整体含义
指代性表达分割（RES）要求模型根据自然语言描述精准分割图像区域。现有的 RES 方法面临两大瓶颈：一是**过度依赖大规模标注数据**，导致泛化性差；二是**难以统一处理显式（直接描述）和隐式（需推理）的表达**。虽然最新的 SAM3 模型具备强大的分割能力，但它在处理长文本或复杂逻辑时表现不佳，且现有的“MLLM + SAM3”耦合方式往往是单向的，缺乏对分割结果的反馈与精炼机制。

### 2. 方法论
论文提出了 **Tarot-SAM3**，一个无需任何任务特定训练（Training-free）的框架，包含两个核心阶段：
*   **表达式推理阐释器 (ERI) 阶段**：
    *   **结构化解析**：利用 MLLM（如 Qwen2.5-VL）将复杂查询分解为 6 个维度的推理辅助提示（如显/隐式识别、单/多目标判定等）。
    *   **提示增强与重写**：通过 MLLM 生成增强的目标名称（同义词扩展）和重写的长/短表达式，从而为 SAM3 提供更稳健的文本和边界框（BBox）提示。
    *   **一致性过滤**：通过计算文本提示生成的掩码与 BBox 提示的重叠度（IoU），剔除不一致的候选掩码。
*   **掩码自精炼 (MSR) 阶段**：
    *   **跨类型择优**：在文本、BBox 和点提示生成的掩码中，由 MLLM 选出全局最佳掩码。
    *   **特征相干性分析**：利用 DINOv3 提取图像特征，计算判别性区域（即不同提示生成的掩码之间的差异区）与高置信度区域的特征相似度。
    *   **提示修正**：根据区域归属感判定是否存在过度分割或欠分割，通过移动或增加正/负点提示（Point Prompts）来重新驱动 SAM3 生成更精确的边界。

### 3. 实验设计
*   **数据集**：
    *   **显式 RES**：RefCOCO、RefCOCO+、RefCOCOg。
    *   **隐式 RES**：ReasonSeg（包含 LISA 提出的推理型任务）。
    *   **开放世界**：使用在线抓取和自拍图像构建的测试集。
*   **对比方法**：
    *   **全监督方法**：LISA、GLaMM、X-SAM、SAM-Veteran 等。
    *   **免训练/零样本方法**：SAM3 Agent、EVOL-SAM3、LGD+DINO 等。
*   **评价指标**：gIoU（全局交并比）和 cIoU（累积交并比）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A800-80G GPU**。
*   **模型骨干**：MLLM 采用 Qwen2.5-VL（3B 和 7B 版本），DINOv3 采用 ViT-B 架构，分割器为 SAM3。
*   **训练时长**：由于是**免训练（Training-free）**框架，该方法不需要训练阶段，直接进行推理测试。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准数据集的多个子集（val, testA, testB）上进行了详尽测试。
*   **消融实验**：针对 ERI 阶段的推理选项、文本增强、BBox 增强，以及 MSR 阶段的择优机制和提示修改模块（OPM）分别做了消融分析。
*   **超参数敏感性**：测试了重叠阈值 $\tau$ 和负样本相似度阈值 $s_{neg}$ 的影响。
*   **充分性评价**：实验设计非常全面，不仅对比了同类免训练方法，还挑战了许多经过大量数据微调的有监督模型，证明了其在不同逻辑复杂度下的鲁棒性，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：Tarot-SAM3 在免训练方法中达到了 SOTA 水平。例如在 RefCOCO testA 上达到 75.5 gIoU，在 ReasonSeg 测试集上达到 74.3 gIoU。
*   **超越微调模型**：在隐式推理任务中，Tarot-SAM3 甚至超过了一些在特定数据集上微调过的模型（如 SAM-Veteran）。
*   **推理效率**：相比于之前的 EVOL-SAM3 和 SAM3 Agent，Tarot-SAM3 在保证高精度的同时，推理速度更快（ReasonSeg 测试集上单例耗时 15.2s）。

### 7. 优点
*   **无需训练**：极大地降低了部署成本，且不存在训练数据的分布偏差问题。
*   **闭环精炼**：引入了基于 DINOv3 特征的自精炼机制，解决了以往方法中 MLLM 与分割器之间“断层”的问题。
*   **通用性强**：能够同时处理简单描述和复杂的常识推理查询，在动漫、自拍等开放世界场景下表现出极强的泛化能力。

### 8. 不足与局限
*   **空间歧义处理**：在处理如“最左边的箱子”这类具有空间相对关系的描述时，模型有时会产生误解。
*   **目标层级偏差**：对于区域级查询（如“叶子上颜色异常的部分”），模型有时会错误地分割整个物体（整片叶子）而非特定局部。
*   **推理延迟**：虽然比同类方法快，但由于涉及 MLLM 多次调用和 DINOv3 特征计算，其实时性仍有待提升。

（完）
