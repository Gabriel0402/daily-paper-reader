---
title: Lightweight Spatiotemporal Highway Lane Detection via 3D-ResNet and PINet with ROI-Aware Attention
title_zh: 基于 3D-ResNet 和带有 ROI 感知注意力的 PINet 的轻量级时空高速公路车道线检测
authors: "Sorna Shanmuga Raja, Abdelhafid Zenati"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02188v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 自动驾驶场景下的时空车道线检测
tldr: "本研究提出一种轻量级端到端高速公路车道线检测架构，通过结合3D-ResNet编码器与PINet解码器来同时捕获时空信息。研究设计了两种模型：一种利用FPN和自注意力机制增强多尺度特征，另一种引入ROI检测头以聚焦关键区域。在TuSimple数据集上，该架构以更少的参数和更低延迟实现了93.40%的准确率，显著降低了漏检率，适用于实时高级驾驶辅助系统（ADAS）。"
selection_source: fresh_fetch
motivation: 旨在解决现有车道线检测方法在处理复杂驾驶场景时，难以兼顾时空特征提取效率与计算实时性的挑战。
method: 采用3D-ResNet提取时空特征，并结合PINet解码器与ROI感知注意力机制，通过ROI检测头优化计算资源分配。
result: "在TuSimple数据集上达到93.40%的准确率，且在参数量、推理延迟和降低假阴性率方面均优于传统2D和3D基准模型。"
conclusion: 该轻量化架构在实时推理中表现优异，为自动驾驶中的车道辅助系统提供了一种高效、精准且可扩展的解决方案。
---

## 摘要
本文提出了一种轻量级、端到端的高速公路车道线检测架构，该架构能够同时捕获空间和时间信息，从而在真实驾驶场景中实现稳健的性能。基于 3D 卷积神经网络和实例分割的优势，我们提出了两种将 3D-ResNet 编码器与点实例网络 (PINet) 解码器相结合的模型。第一种模型利用特征金字塔网络 (FPN) 和自注意力机制增强多尺度特征表示，以细化空间依赖关系。第二种模型引入了感兴趣区域 (ROI) 检测头，以选择性地关注与车道相关的区域，从而在提高精度的同时降低计算复杂度。在 TuSimple 数据集（高速公路驾驶场景）上进行的实验表明，所提出的第二种模型达到了 93.40% 的准确率，并显著减少了漏检。与现有的 2D 和 3D 基准模型相比，我们的方法以更少的参数和更低的延迟实现了性能提升。该架构已在伦敦大学城市圣乔治学院的自主系统实验室通过离线训练和实时推理得到了验证。研究结果表明，所提出的模型非常适合集成到高级驾驶辅助系统 (ADAS) 中，并具有向完整车道辅助系统 (LAS) 扩展的潜力。

## Abstract
This paper presents a lightweight, end-to-end highway lane detection architecture that jointly captures spatial and temporal information for robust performance in real-world driving scenarios. Building on the strengths of 3D convolutional neural networks and instance segmentation, we propose two models that integrate a 3D-ResNet encoder with a Point Instance Network (PINet) decoder. The first model enhances multi-scale feature representation using a Feature Pyramid Network (FPN) and Self-Attention mechanism to refine spatial dependencies. The second model introduces a Region of Interest (ROI) detection head to selectively focus on lane-relevant regions, thereby improving precision and reducing computational complexity.   Experiments conducted on the TuSimple dataset (highway driving scenarios) demonstrate that the proposed second model achieves 93.40% accuracy while significantly reducing false negatives. Compared to existing 2D and 3D baselines, our approach achieves improved performance with fewer parameters and reduced latency. The architecture has been validated through offline training and real-time inference in the Autonomous Systems Laboratory at City, St George's University of London. These results suggest that the proposed models are well-suited for integration into Advanced Driver Assistance Systems (ADAS), with potential scalability toward full Lane Assist Systems (LAS).

---

## 论文详细总结（自动生成）

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源受限的嵌入式环境下，实现既能捕获时间连续性（视频流信息）又具备高实时性的轻量级车道线检测。
*   **研究背景**：传统的2D CNN方法通常独立处理每一帧图像，忽略了帧间的时空相干性，导致在遮挡、光照突变或车道线磨损时表现不稳定。而现有的3D CNN模型虽然能提取时空特征，但往往计算量巨大，难以在实际的自动驾驶辅助系统（ADAS）中部署。

### 2. 方法论
论文提出了两种基于 **3D-ResNet（编码器）** 和 **PINet（解码器）** 的端到端架构：
*   **核心思想**：利用3D卷积提取视频序列中的时空特征，并结合点实例网络（PINet）将车道线建模为离散的关键点。
*   **关键技术细节**：
    *   **3D-ResNet 编码器**：通过在高度、宽度和时间三个维度上进行卷积，学习车辆行驶过程中的动态模式。
    *   **模型 1（自注意力 + FPN）**：引入自注意力机制（Self-Attention）捕捉长程空间依赖，并利用特征金字塔网络（FPN）融合多尺度语义信息。
    *   **模型 2（ROI 感知 + 损失优化）**：引入 **感兴趣区域（ROI）** 检测头，将计算资源集中在图像下方的道路区域，减少背景噪声。
    *   **损失函数**：结合了 **Focal Loss**（处理类别不平衡）、**MSE**（偏移回归）和 **LineIoU Loss**（优化车道线几何对齐）。
    *   **后处理**：采用 **RANSAC** 算法进行多项式曲线拟合，并使用 **图平滑算法（Graph-based smoothing）** 消除帧间预测的突变。

### 3. 实验设计
*   **数据集**：使用 **TuSimple** 官方数据集，包含 3,626 个训练视频切片和 2,782 个测试视频切片，涵盖高速公路的各种光照和天气条件。
*   **Benchmark**：以 TuSimple 官方评价指标（Accuracy, False Positive, False Negative）为基准。
*   **对比方法**：
    *   基础 3D-ResNet + PINet 架构。
    *   消融实验对比了：Unet + 曲线拟合、3D-ResNet + CABM（注意力模块）、3D-ResNet + LSTM、3D-ResNet + Line Prior 等组合。

### 4. 资源与算力
*   **硬件环境**：
    *   初步开发与调优：NVIDIA **GTX 1650** GPU (笔记本平台)。
    *   最终实验与长周期训练：在 **HPC（高性能计算）集群** 上完成。
*   **训练细节**：模型最高训练了 200 个 epoch，最终选定的最佳模型训练时长为 162 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文设计了 3 组核心对比实验（Base vs. Model 1 vs. Model 2）以及 4 组针对不同组件的消融实验。
*   **充分性评价**：实验设计较为系统，通过对比不同注意力机制和回归头验证了 ROI 模块的有效性。但在数据集多样性上略显不足，仅依赖 TuSimple 单一数据集，缺乏在 CULane 等更复杂城市道路数据集上的验证。

### 6. 主要结论与发现
*   **性能提升**：提出的 ROI 感知模型（模型 2）在 TuSimple 上达到了 **93.40% 的准确率**，F1-score 为 93.23%。
*   **效率优势**：通过轻量化 3D-ResNet 和 ROI 约束，模型在减少参数量的同时显著降低了推理延迟，适合实时 ADAS 应用。
*   **鲁棒性**：时空特征的引入显著降低了假阴性（FN）率，使模型在车道线被遮挡或弯道场景下比纯 2D 模型更稳健。

### 7. 优点
*   **时空建模**：成功将 3D 卷积应用于轻量化车道线检测，解决了单帧检测易受干扰的问题。
*   **ROI 策略**：通过简单的空间先验（ROI）有效提升了计算效率并抑制了天空、路牌等背景干扰。
*   **端到端优化**：整合了从特征提取到几何拟合的完整流水线，后处理逻辑（RANSAC + 图平滑）非常务实。

### 8. 不足与局限
*   **场景覆盖**：研究集中于高速公路（TuSimple 数据集），对于城市复杂路口、大曲率急转弯及极端恶劣天气（如暴雨、大雪）的泛化能力尚待验证。
*   **对比基准**：未与近年来最先进的（SOTA）基于 Transformer 的 2D 车道线检测模型（如 CLRNet）进行直接的功耗与精度对比。
*   **硬件适配**：虽然提到轻量化，但未给出在特定嵌入式芯片（如 Jetson 或车规级芯片）上的具体 FPS 数据。

（完）
