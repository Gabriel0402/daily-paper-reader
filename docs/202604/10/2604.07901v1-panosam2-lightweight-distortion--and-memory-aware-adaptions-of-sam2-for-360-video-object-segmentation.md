---
title: "PanoSAM2: Lightweight Distortion- and Memory-aware Adaptions of SAM2 for 360 Video Object Segmentation"
title_zh: PanoSAM2：针对360度视频目标分割的轻量级畸变与记忆感知SAM2适配方案
authors: "Dingwen Xiao, Weiming Zhang, Shiqi Wen, Lin Wang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07901v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 针对360度视频对象分割对SAM2进行适配
tldr: 针对360度视频目标分割（360VOS）中存在的投影畸变、左右边界不一致及内存中目标信息稀疏等挑战，本文提出了PanoSAM2。该框架在SAM2的基础上引入了轻量级的畸变与内存感知适配策略，包括具有缝隙一致性感受野的全景感知解码器、畸变引导的掩码损失函数以及长短期记忆模块。实验证明，PanoSAM2在保持SAM2交互式提示设计的同时，显著提升了全景视频分割的准确性与时序一致性。
selection_source: fresh_fetch
motivation: 现有的SAM2模型在处理360度视频时，因投影畸变、边界语义不连续及内存信息稀疏导致分割效果不佳。
method: 提出了包含全景感知解码器、畸变引导掩码损失和长短期记忆模块的轻量级适配方案，以解决畸变和时序一致性问题。
result: 在360VOTS和PanoVOS数据集上，PanoSAM2相比SAM2分别实现了5.6和6.7个百分点的性能提升。
conclusion: PanoSAM2通过有效的轻量级适配，成功将SAM2的强大分割能力扩展到了具有挑战性的全景视频领域。
---

## 摘要
360度视频目标分割（360VOS）旨在预测360度视频中时间一致的掩码，提供全场景覆盖，从而使VR/AR和具身智能等应用受益。由于缺乏高质量的标注数据集，训练360VOS模型并非易事。最近，Segment Anything Models (SAMs)，特别是具有记忆模块设计的SAM2，展现了强大的、可提示的VOS能力。然而，直接将SAM2用于360VOS会产生不理想的结果，因为360度视频存在投影畸变、左右两侧语义不一致以及SAM2记忆中目标掩码信息稀疏的问题。为此，我们提出了PanoSAM2，这是一个基于轻量级畸变与记忆感知适配策略的新型360VOS框架，在保留SAM2用户友好提示设计的同时，实现了可靠的360VOS。具体而言，为了解决投影畸变和语义不一致问题，我们提出了一个全景感知解码器（Pano-Aware Decoder），该解码器具有接缝一致的感受野和迭代畸变细化功能，以保持0/360度边界的连续性。同时，引入了畸变引导掩码损失（Distortion-Guided Mask Loss），根据畸变大小对像素进行加权，强调拉伸区域和边界。为了解决目标稀疏性问题，我们提出了一个长短期记忆模块（Long-Short Memory Module），用于维护一个紧凑的长期目标指针，以重新实例化并对齐短期记忆，从而增强时间连贯性。广泛的实验表明，PanoSAM2相比SAM2取得了显著提升：在360VOTS上提升了5.6，在PanoVOS上提升了6.7，证明了我们方法的有效性。

## Abstract
360 video object segmentation (360VOS) aims to predict temporally-consistent masks in 360 videos, offering full-scene coverage, benefiting applications, such as VR/AR and embodied AI. Learning 360VOS model is nontrivial due to the lack of high-quality labeled dataset. Recently, Segment Anything Models (SAMs), especially SAM2 -- with its design of memory module -- shows strong, promptable VOS capability. However, directly using SAM2 for 360VOS yields implausible results as 360 videos suffer from the projection distortion, semantic inconsistency of left-right sides, and sparse object mask information in SAM2's memory. To this end, we propose PanoSAM2, a novel 360VOS framework based on our lightweight distortion- and memory-aware adaptation strategies of SAM2 to achieve reliable 360VOS while retaining SAM2's user-friendly prompting design. Concretely, to tackle the projection distortion and semantic inconsistency issues, we propose a Pano-Aware Decoder with seam-consistent receptive fields and iterative distortion refinement to maintain continuity across the 0/360 degree boundary. Meanwhile, a Distortion-Guided Mask Loss is introduced to weight pixels by distortion magnitude, stressing stretched regions and boundaries. To address the object sparsity issue, we propose a Long-Short Memory Module to maintain a compact long-term object pointer to re-instantiate and align short-term memories, thereby enhancing temporal coherence. Extensive experiments show that PanoSAM2 yields substantial gains over SAM2: +5.6 on 360VOTS and +6.7 on PanoVOS, showing the effectiveness of our method.

---

## 论文详细总结（自动生成）

### 论文总结：PanoSAM2 —— 针对 360 度视频目标分割的轻量级畸变与记忆感知适配方案

#### 1. 论文的核心问题与整体含义
360 度全景视频目标分割（360VOS）在 VR/AR、自动驾驶和具身智能领域具有重要应用价值。然而，现有的视频分割模型（如 SAM2）直接应用于全景视频时效果不佳，主要面临三大挑战：
*   **投影畸变**：等距柱状投影（ERP）导致极点区域严重拉伸。
*   **边界不连续性**：0°/360° 接缝处的语义不一致，导致跨越边界的目标被切断。
*   **记忆稀疏与漂移**：全景场景中目标往往较小且易受遮挡，SAM2 的短期记忆机制容易导致目标丢失或身份切换。
本文提出了 **PanoSAM2**，旨在通过轻量级的适配策略，在保留 SAM2 强大的交互式提示能力的同时，解决全景视频的几何与时序一致性问题。

#### 2. 论文提出的方法论
PanoSAM2 引入了三个核心组件来增强 SAM2：
*   **全景感知解码器（Pano-Aware Decoder）**：
    *   **Pano-Consistency (PC) Block**：在卷积层中使用“环绕填充”（Wrap Padding），使左边界特征能感知右边界特征，确保接缝处的语义连续。
    *   **迭代畸变细化**：融合前一帧的掩码特征，辅助解码器在处理逐渐变化的畸变时保持稳定。
*   **长短期记忆模块（LSMM）**：
    *   为了不显著增加显存负担，LSMM 仅存储长期的“目标指针”（Object Pointers，高维向量总结）而非稠密特征。
    *   **遮挡采样策略**：根据预测的遮挡分数选择高质量的历史帧作为长期参考。
    *   使用 FiLM（特征线性调制）技术将长期上下文注入短期记忆，防止在目标稀疏或遮挡时发生跟踪漂移。
*   **畸变引导掩码损失（Distortion-Guided Mask Loss）**：
    *   根据 ERP 投影的畸变程度对像素进行加权。
    *   结合 BFoV（边界视场）投影，对前景像素和靠近边界的背景像素赋予更高权重，使模型更关注被拉伸的区域和目标轮廓。

#### 3. 实验设计
*   **数据集**：
    *   **360VOTS**：大规模全景视频数据集（290 个序列，约 24.2 万帧）。
    *   **PanoVOS**：包含 150 个视频，侧重于实例级别的全景分割。
*   **Benchmark 与对比方法**：
    *   对比了传统的 2D VOS 模型（如 STCN, XMem, AOT）。
    *   对比了专门的全景模型（如 PSCFormer）。
    *   对比了基础模型 SAM2 及其变体（如 SAM2Long）。
*   **评估指标**：采用标准的 $J$（区域重叠度）、$F$（边界准确度）以及 $J\&F$ 综合得分。

#### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA A800-80GB GPU**。
*   **训练时长**：以 Hiera-T 为骨干网络时，训练大约需要 **50 小时**。
*   **效率**：模型保持了较高的推理速度（Hiera-T 版本约 34.8 FPS），显存占用仅比原始 SAM2 略微增加（从 3.6GB 增至 3.7GB）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流全景数据集上进行了详尽的测试，并进行了跨数据集的零样本（Zero-shot）泛化实验。
*   **消融实验**：针对 PA Decoder、LSMM、畸变损失函数、长期记忆大小 $L$、以及损失权重 $\lambda_{occ}$ 等进行了多组消融实验。
*   **客观性**：实验涵盖了“已见类别”和“未见类别”的对比，并提供了自采真实场景的定性分析，证明了模型在不同分布数据下的鲁棒性，实验设计较为客观公平。

#### 6. 论文的主要结论与发现
*   **性能提升**：PanoSAM2 在 360VOTS 测试集上比 SAM2 提升了 **5.6** 个 $J\&F$ 点；在 PanoVOS 验证集上提升了 **6.7** 个点。
*   **几何感知的必要性**：通过简单的环绕填充和畸变加权损失，可以显著改善全景视频中的边界断裂问题。
*   **长期记忆的价值**：LSMM 模块在处理长视频和目标消失再现时，比 SAM2 原生的短期记忆更具稳定性。
*   **泛化能力**：即使在未经过微调的开放世界全景视频中，PanoSAM2 也展现出比原始 SAM2 更强的鲁棒性。

#### 7. 优点
*   **轻量级适配**：不需要从头训练庞大的基础模型，通过轻量级模块即可实现显著的性能飞跃。
*   **保留交互性**：完美继承了 SAM2 的点、框、掩码提示功能，用户体验友好。
*   **几何一致性**：从架构层面解决了全景视频特有的接缝连续性问题，而非简单的后处理。

#### 8. 不足与局限
*   **极端运动失效**：当目标发生剧烈的纬度跨越（如从赤道快速移动到极点）时，由于畸变剧烈变化，模型仍可能出现预测碎片化。
*   **远距离小目标**：在目标极小且存在外观相似的干扰物时，模型偶尔会发生身份切换（Identity Swap）。
*   **多目标交互**：目前主要侧重于单目标或简单多目标，在极度拥挤、频繁遮挡的复杂场景下仍有提升空间。

（完）
