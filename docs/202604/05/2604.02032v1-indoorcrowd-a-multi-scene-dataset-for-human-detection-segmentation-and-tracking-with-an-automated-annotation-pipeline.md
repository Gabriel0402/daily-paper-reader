---
title: "IndoorCrowd: A Multi-Scene Dataset for Human Detection, Segmentation, and Tracking with an Automated Annotation Pipeline"
title_zh: IndoorCrowd：一个用于人体检测、分割与追踪的多场景数据集及自动化标注流水线
authors: "Sebastian-Ion Nae, Radu Moldoveanu, Alexandra Stefania Ghita, Adina Magda Florea"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02032v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 基于SAM的自动标注器在实例分割中的基准测试
tldr: IndoorCrowd是一个针对室内拥挤场景的多任务数据集，涵盖人体检测、实例分割和多目标跟踪。该研究在四个校园场景采集了31个视频（近万帧），并提供经人工验证的分割掩码。研究还评估了基于基础模型的自动标注流程，并利用YOLO和RT-DETR等模型建立了性能基准，揭示了室内复杂环境（如高密度和遮挡）对算法性能的挑战。
selection_source: fresh_fetch
motivation: 现有的室内数据集难以捕捉真实世界中大规模且复杂的拥挤环境，限制了智能监控和人机交互的发展。
method: 提出IndoorCrowd数据集，包含多场景视频及人工验证的掩码，并利用SAM3等基础模型构建自动标注流水线。
result: 通过对不同场景的分析，发现人群密度、目标尺度和遮挡显著影响算法表现，其中ACS-EC场景最具挑战性。
conclusion: 该数据集和自动标注流程为室内人体行为理解提供了高质量的基准，有助于推动复杂室内场景下的视觉算法研究。
---

## 摘要
理解拥挤室内环境中的人类行为对于监控、智能建筑和人机交互至关重要，然而现有数据集很少能大规模捕捉真实世界的室内复杂性。我们推出了 IndoorCrowd，这是一个用于室内人体检测、实例分割和多目标追踪的多场景数据集，采集自四个校园地点（ACS-EC、ACS-EG、IE-Central、R-Central）。该数据集包含 $31$ 个视频（共 $9{,}913$ 帧，帧率为 $5$fps），并带有经人工验证的逐实例分割掩码。一个包含 $620$ 帧的对照子集利用 Cohen's $κ$、AP、精确率、召回率和掩码 IoU，将三种基础模型自动标注器（SAM3、GroundingSAM 和 EfficientGroundingSAM）与人工标签进行了基准测试对比。另一个包含 $2{,}552$ 帧的子集支持多目标追踪，并提供 MOTChallenge 格式的连续身份轨迹。我们使用 YOLOv8n、YOLOv26n 和 RT-DETR-L，结合 ByteTrack、BoT-SORT 和 OC-SORT，建立了检测、分割和追踪的基准模型。逐场景分析显示，受人群密度、尺度和遮挡的影响，场景难度存在显著差异：ACS-EC 场景中 $79.3\%$ 为密集帧，平均实例尺度为 $60.8$px，是挑战性最大的场景。项目页面见 https://sheepseb.github.io/IndoorCrowd/。

## Abstract
Understanding human behaviour in crowded indoor environments is central to surveillance, smart buildings, and human-robot interaction, yet existing datasets rarely capture real-world indoor complexity at scale. We introduce IndoorCrowd, a multi-scene dataset for indoor human detection, instance segmentation, and multi-object tracking, collected across four campus locations (ACS-EC, ACS-EG, IE-Central, R-Central). It comprises $31$ videos ($9{,}913$ frames at $5$fps) with human-verified, per-instance segmentation masks. A $620$-frame control subset benchmarks three foundation-model auto-annotators: SAM3, GroundingSAM, and EfficientGroundingSAM, against human labels using Cohen's $κ$, AP, precision, recall, and mask IoU. A further $2{,}552$-frame subset supports multi-object tracking with continuous identity tracks in MOTChallenge format. We establish detection, segmentation, and tracking baselines using YOLOv8n, YOLOv26n, and RT-DETR-L paired with ByteTrack, BoT-SORT, and OC-SORT. Per-scene analysis reveals substantial difficulty variation driven by crowd density, scale, and occlusion: ACS-EC, with $79.3\%$ dense frames and a mean instance scale of $60.8$px, is the most challenging scene. The project page is available at https://sheepseb.github.io/IndoorCrowd/.

---

## 论文详细总结（自动生成）

以下是对论文《IndoorCrowd: A Multi-Scene Dataset for Human Detection, Segmentation, and Tracking with an Automated Annotation Pipeline》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于**室内拥挤环境下高质量人体感知数据集的匮乏**。
*   **研究动机**：现有的行人数据集（如 CrowdHuman, MOTChallenge）多侧重于室外、街道或车载视角，而室内环境（如走廊、中庭）具有独特的挑战：复杂的建筑遮挡（柱子、家具）、剧烈波动的光照、极高的人群密度以及多变的摄像头视角。
*   **整体含义**：为了推动智能监控、智能建筑和人机交互的发展，作者推出了 **IndoorCrowd** 数据集。它不仅提供了检测和追踪标签，还包含了昂贵的实例分割掩码，并探索了利用基础模型（Foundation Models）构建自动化标注流水线的可行性。

### 2. 论文提出的方法论
论文提出了一套**人机协作的半自动标注流水线**，旨在降低标注成本并保证质量：
*   **核心思想**：利用大模型进行预标注，人工仅负责验证和修正。
*   **关键技术细节**：
    *   **自动标注器评估**：对比了 SAM3、GroundingSAM 和 EfficientGroundingSAM 三种模型。
    *   **检测与分割标注**：使用基础模型生成候选掩码，人工通过 SAM 2.1 进行交互式修正（添加缺失掩码、调整边界、删除误报）。
    *   **追踪标注**：基于 SAM3 生成初始轨迹，人工修正身份切换（ID Switch）、合并断裂轨迹、删除“幽灵”轨迹并进行线性插值填补缺失帧。
    *   **质量度量**：引入 **Cohen’s κ（柯恩系数）** 来量化自动标注器与人工标注之间的一致性，确保基准的客观性。

### 3. 实验设计
*   **数据集构成**：采集自校园内 4 个不同场景（ACS-EC, ACS-EG, IE-Central, R-Central），共 31 个视频，9,913 帧（5 fps）。
*   **子集划分**：
    *   **控制子集（620 帧）**：用于评估自动标注器的准确性。
    *   **追踪子集（2,552 帧）**：提供连续的身份轨迹（MOTChallenge 格式）。
*   **对比方法（Benchmark）**：
    *   **检测与分割**：对比了 YOLOv8n, YOLOv26n, RT-DETR-L。
    *   **多目标追踪**：对比了上述检测器与 ByteTrack, BoT-SORT, OC-SORT 的组合。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA RTX 4060Ti (16 GB)** 显卡上完成。
*   **训练细节**：
    *   模型初始化使用 COCO 预训练权重。
    *   训练 30 个 epoch，Batch Size 为 16，输入分辨率 640x640。
    *   使用了线性学习率调度和 Mosaic 数据增强。
    *   推理延迟（Latency）在相同硬件上测得，RT-DETR-L 约为 27ms，YOLOv8n 仅需约 2ms。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括 3 种自动标注器的性能对比、5 种检测/分割模型的基准测试、以及 6 种“检测器+追踪器”组合的性能评估。
*   **充分性与公平性**：
    *   **场景多样性**：涵盖了从稀疏走廊到极拥挤中庭的多种室内布局。
    *   **评估指标全面**：使用了 mAP, MOTA, IDF1, Cohen’s κ 等标准指标。
    *   **实验设计客观**：采用了“在 2 个场景训练，在其余场景测试”的策略，验证了模型的泛化能力。

### 6. 主要结论与发现
*   **场景难度差异**：ACS-EC 是最难的场景，其拥挤帧占比达 79.3%，且目标尺度小（平均 60.8px），导致所有模型在该场景下的性能大幅下降。
*   **自动标注器表现**：**SAM3 具有最高的召回率（0.88-0.98）**，虽然在拥挤场景下精确率较低，但非常适合作为人工修正的起点。EfficientGroundingSAM 在保持精度的同时显著降低了计算成本。
*   **模型基准**：**RT-DETR-L + OC-SORT** 组合在追踪精度上表现最优，而 **YOLOv8n + ByteTrack** 在实时性（>100 FPS）和精度之间取得了最佳平衡。
*   **瓶颈分析**：在室内拥挤场景中，检测质量（而非关联算法）是多目标追踪性能的主要瓶颈。

### 7. 优点
*   **多任务集成**：同时提供检测、实例分割和追踪标签，在室内行人数据集中较为罕见。
*   **标注流程创新**：系统性地评估了基础模型在自动标注中的表现，并提供了可复用的半自动流水线。
*   **真实性强**：数据采集自真实的校园公共空间，捕捉到了复杂的建筑遮挡和自然的人群流动。

### 8. 不足与局限
*   **样本规模有限**：完全由人工从零标注的控制子集仅 620 帧，对于深度学习训练而言规模较小。
*   **场景单一性**：数据仅来自单一机构（大学校园），可能无法完全代表医院、商场或机场等其他室内环境的特征。
*   **环境局限**：目前尚未涵盖夜间、极低光照或紧急情况（如火灾烟雾）等极端室内场景。
*   **隐私处理**：虽然进行了人脸模糊，但未深入讨论步态等其他生物识别信息的隐私风险。

（完）
