---
title: Multi-AUV Ad-hoc Networks-Based Multi-Target Tracking Based on Scene-Adaptive Embodied Intelligence
title_zh: 基于场景自适应具身智能的多 AUV 自组网多目标跟踪
authors: "Kai Tian, Jialun Wang, Chuan Lin, Guangjie Han, Shengchao Zhu, Ying Liu, Qian Zhu"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27194v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: AUV的多智能体协作与具身智能
tldr: 针对水下多目标跟踪中动态拓扑和通信受限的挑战，本文提出一种基于场景自适应具身智能（EI）的多AUV自组织网络架构。该架构将感知、决策与执行整合为统一认知环路，并引入SA-MARL算法，通过双路径Critic机制实现任务跟踪与安全约束的解耦，显著提升了复杂水下环境中的跟踪精度与鲁棒性。
selection_source: fresh_fetch
motivation: 传统数据中心架构难以应对水下通信带宽受限及动态拓扑波动带来的操作一致性挑战。
method: 提出一种三层具身智能架构，并设计了包含场景与通用双路径Critic机制的SA-MARL算法，实现策略的自主演化。
result: 实验表明，该方案在收敛速度和跟踪精度上均优于主流MARL方法，且在强干扰和拓扑变化下表现稳健。
conclusion: 具身智能架构与场景自适应强化学习的结合，为复杂海洋环境下的多AUV协同任务提供了高效且可靠的解决方案。
---

## 摘要
随着水下联网和多智能体协作技术的飞速发展，自主水下航行器（AUV）自组网已成为执行复杂海洋任务（如多目标跟踪）的关键框架。然而，传统的数据中心架构在高度动态的拓扑波动和严重受限的水声通信带宽下，难以维持运行的一致性。本文提出了一种用于多 AUV 自组网的场景自适应具身智能（EI）架构，通过将感知、决策和物理执行集成到统一的认知闭环中，将 AUV 重新构想为具身实体。为了实现这些层级之间的功能交互，我们定义了一种基于信标的通信与控制模型，将通信链路视为动态约束感知信道，有效地弥合了高层策略推理与分布式物理执行之间的鸿沟。具体而言，所提架构采用了三层功能框架，并引入了一种具有双路径 Critic 机制的场景自适应多智能体强化学习（SA-MARL）算法。通过基于权重的动态融合过程集成场景 Critic 网络和通用 Critic 网络，SA-MARL 有效地将专门的跟踪任务与全局安全约束解耦，促进了策略的自主演化。评估结果表明，与主流的 MARL 方法相比，该方案显著加速了策略收敛并实现了更高的跟踪精度，即使在剧烈的环境干扰和流动的拓扑变化下也能保持稳健的性能。

## Abstract
With the rapid advancement of underwater net-working and multi-agent coordination technologies, autonomous underwater vehicle (AUV) ad-hoc networks have emerged as a pivotal framework for executing complex maritime missions, such as multi-target tracking. However, traditional data-centricarchitectures struggle to maintain operational consistency under highly dynamic topological fluctuations and severely constrained acoustic communication bandwidth. This article proposes a scene-adaptive embodied intelligence (EI) architecture for multi-AUV ad-hoc networks, which re-envisions AUVs as embodied entities by integrating perception, decision-making, and physical execution into a unified cognitive loop. To materialize the functional interaction between these layers, we define a beacon-based communication and control model that treats the communication link as a dynamic constraint-aware channel, effectively bridging the gap between high-level policy inference and decentralized physical actuation. Specifically, the proposed architecture employs a three-layer functional framework and introduces a Scene-Adaptive MARL (SA-MARL) algorithm featuring a dual-path critic mechanism. By integrating a scene critic network and a general critic network through a weight-based dynamic fusion process, SA-MARL effectively decouples specialized tracking tasks from global safety constraints, facilitating autonomous policy evolution. Evaluation results demonstrate that the proposedscheme significantly accelerates policy convergence and achieves superior tracking accuracy compared to mainstream MARL approaches, maintaining robust performance even under intense environmental interference and fluid topological shifts.

---

## 论文详细总结（自动生成）

### 论文总结：基于场景自适应具身智能的多 AUV 自组网多目标跟踪

#### 1. 核心问题与研究动机
*   **核心问题**：在复杂的水下环境中，多自主水下航行器（AUV）自组网面临**水声通信带宽极低、延迟高**以及**网络拓扑动态剧烈波动**的挑战。传统以数据为中心的架构难以在这些限制下保持任务执行的一致性和高效协作。
*   **研究背景**：为了解决抽象决策指令与物理执行之间的脱节，论文引入了**具身智能（Embodied Intelligence, EI）**概念，将 AUV 视为集感知、决策、执行于一体的认知实体，旨在通过场景自适应能力提升多目标跟踪的鲁棒性。

#### 2. 方法论
*   **核心思想**：构建一个三层功能架构（感知层、决策层、执行层），通过**场景自适应多智能体强化学习（SA-MARL）**算法，实现从环境感知到物理动作的闭环控制。
*   **关键技术细节**：
    *   **三层架构**：感知层负责多模态数据融合；决策层利用 SA-MARL 进行策略演化；执行层通过 PID 控制将抽象指令映射为物理推力和转矩。
    *   **信标通信模型**：设计了基于信标（Beacon）的通信与控制模型，在 USV（无人水面艇）全局控制、局部控制 AUV 与跟随 AUV 之间进行高效的任务分配与状态反馈。
    *   **SA-MARL 算法流程**：
        1.  **状态表示**：利用 MLP 压缩高维观测数据以适应低带宽通信。
        2.  **场景识别**：通过权重分配自主识别当前任务场景（如跟踪、避障、围捕）。
        3.  **双路径 Critic 机制**：引入“场景 Critic”（负责特定任务优化）和“通用 Critic”（负责全局安全与稳定性），解决梯度冲突。
        4.  **动态融合**：通过加权公式 $Q = (1-w)Q_{general} + wQ_{scene}$ 融合双路径评价，更新 Actor 网络。
    *   **多目标奖励函数**：整合了跟踪精度、编队间距（安全性）、运动平滑度及速度一致性四个维度。

#### 3. 实验设计
*   **实验场景**：设置了三种不同规模的任务：
    1.  4 AUV 跟踪 2 目标。
    2.  6 AUV 跟踪 3 目标。
    3.  12 AUV 跟踪 4 目标（大规模场景）。
*   **对比基准（Benchmarks）**：对比了 7 种主流 MARL 算法：DSBM, MA-A3C, MAAC, MADDPG, MAPPO, MASAC, MATD3。
*   **评估指标**：收敛速度（平均回报）、跟踪准确率（径向指标）、环境干扰下的鲁棒性。
*   **可视化**：使用 **Unity 引擎** 进行高保真 3D 物理仿真，验证算法在复杂海底地形下的表现。

#### 4. 资源与算力
*   **硬件环境**：实验在 **MacBook Air (2024)** 上运行，配置为 **Apple M3 处理器** 和 **16 GB 内存**。
*   **训练时长**：文中未明确说明具体的训练总时长，但展示了算法在 1000 个 Episode 内即可快速趋于收敛。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小规模到大规模（12 台 AUV）的扩展性测试。
*   **干扰测试**：专门设置了“环境干扰（Interfered）”对比实验，模拟水下噪声对策略稳定性的影响。
*   **充分性评价**：实验设计较为全面，包含了收敛性曲线、准确率定量分析以及 3D 物理仿真定性展示。对比算法涵盖了当前主流的强化学习框架，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能优越**：SA-MARL 在所有规模下均实现了最快的收敛速度和最高的平均回报。
*   **高精度**：在 4 AUV 场景下准确率达 92%，在 12 AUV 大规模且有干扰的场景下仍能保持 84% 的准确率，远超 MASAC 和 DSBM 等方法。
*   **鲁棒性**：双路径 Critic 机制有效缓解了任务目标与安全约束之间的梯度冲突，使 AUV 集群在拓扑切换时能平滑过渡。

#### 7. 优点
*   **架构创新**：将具身智能引入水下自组网，打破了传统“网络仅作为数据管道”的局限。
*   **解耦设计**：双路径 Critic 机制巧妙地解决了多目标学习中的竞争问题，提升了训练稳定性。
*   **实用性强**：考虑了水下通信带宽受限的实际约束，通过信标模型和数据压缩优化了通信效率。

#### 8. 不足与局限
*   **算力评估局限**：实验仅在移动端处理器（M3）上完成，未讨论在嵌入式 AUV 硬件上的实时推理延迟。
*   **通信模型简化**：虽然考虑了带宽限制，但对水声通信中复杂的**多径效应、多普勒频移**等物理特性的建模可能仍做了简化处理。
*   **缺乏实测**：所有结论均基于仿真环境（含 Unity 3D），缺乏真实水域（如湖试或海试）的数据验证。

（完）
