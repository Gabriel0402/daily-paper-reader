---
title: "ROSClaw: An OpenClaw ROS 2 Framework for Agentic Robot Control and Interaction"
title_zh: ROSClaw：一种用于代理式机器人控制与交互的 OpenClaw ROS 2 框架
authors: "Irvin Steve Cardenas, Marcus Anthony Arnett, Natalie Catherine Yeo, Lucky Sah, Jong-Hoon Kim"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26997v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于智能机器人控制和安全验证的基础模型
tldr: ROSClaw是一个将OpenClaw代理运行时与ROS 2集成的模型无关执行层框架，旨在解决基础模型与物理机器人集成中的定制化难题。它通过动态能力发现、多模态观测归一化和安全验证等机制，实现了不同模型与机器人平台间的无缝切换。实验表明，该框架不仅简化了智能机器人开发，还揭示了不同模型在动作建议率和物理行为上的显著差异，为具身智能提供了标准化的基础设施和评估工具。
selection_source: fresh_fetch
motivation: 现有的基础模型与机器人集成方案通常是高度定制且耦合的，缺乏一种通用的、可跨平台和跨模型的标准化执行层。
method: 提出ROSClaw框架，通过标准化可供性注入、多模态观测归一化、预执行安全校验及结构化审计日志，实现模型与ROS 2机器人的解耦连接。
result: 在三种机器人平台和四种基础模型上的部署显示，不同模型在动作提议率上存在高达4.8倍的差异，且执行层设计对任务完成度和安全性有显著影响。
conclusion: ROSClaw为具身智能提供了一个实用且可复现的基础设施，证明了执行层设计在提升机器人代理性能和安全性方面的重要性。
---

## 摘要
基础模型可以赋予机器人开放式推理、语言理解和自适应规划能力，但目前将模型连接到物理机器人需要定制化集成，将感知、执行和安全性与单一模型和平台耦合。我们提出了 ROSClaw，这是一个与模型无关的执行层，它将 OpenClaw 智能体运行时与 ROS 2 集成，使任何基础模型能够通过以下方式感知、推理并作用于任何支持 ROS 的机器人：(i) 具有标准化可负担性（affordance）注入的动态能力发现，(ii) 多模态观测归一化，(iii) 在可配置安全包络内的执行前动作验证，以及 (iv) 结构化审计日志记录。更换模型后端或机器人平台仅需更改配置；工具模式、安全执行和溯源日志保持不变。我们在三个平台（轮式、四足、人形）上部署了 ROSClaw，并使用了四个基础模型后端。在这种受控基座下，模型在策略外动作建议率上表现出高达 4.8 倍的差异（仅在前沿模型中就有 3.4 倍），并针对相同的指令产生性质迥异的物理行为。一项针对 ROSA 的跨框架对等协议证实，执行层设计（而非仅仅是提示词措辞）会显著影响任务完成情况和安全行为，从而确立了 ROSClaw 既是实用的代理式机器人基础设施，又是具身智能的可重复测量工具。

## Abstract
Foundation models can endow robots with open-ended reasoning, language understanding, and adaptive planning, yet connecting a model to a physical robot today requires bespoke integration that couples perception, actuation, and safety to a single model and platform. We present ROSClaw, a model-agnostic executive layer that integrates the OpenClaw agent runtime with ROS 2, enabling any foundation model to perceive, reason about, and act on any ROS-enabled robot through (i) dynamic capability discovery with standardized affordance injection, (ii) multimodal observation normalization, (iii) pre-execution action validation within a configurable safety envelope, and (iv) structured audit logging. Swapping model backends or robot platforms is a configuration change; tool schemas, safety enforcement, and provenance logging remain invariant. We deploy ROSClaw on three platforms (wheeled, quadruped, humanoid) with four foundation-model backends. Under this controlled substrate, models exhibit up to 4.8 x differences in out-of-policy action proposal rates (3.4 x among frontier models alone) and produce qualitatively distinct physical behaviors from identical commands. A cross-framework parity protocol against ROSA confirms that executive-layer design, not just prompt wording, significantly affects both task completion and safety behavior, establishing ROSClaw as both practical agentic-robot infrastructure and a reproducible measurement instrument for embodied AI.

---

## 论文详细总结（自动生成）

以下是对论文《ROSClaw: An OpenClaw ROS 2 Framework for Agentic Robot Control and Interaction》的结构化深入分析：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：目前将基础模型（如 LLM/VLM）集成到物理机器人上通常是“一事一议”的定制化开发。感知、执行和安全逻辑往往与特定模型或硬件平台高度耦合，缺乏通用性。
*   **核心问题**：如何构建一个模型无关（Model-agnostic）的执行层，既能让机器人具备基础模型的推理能力，又能保证跨平台的可移植性、操作安全性和行为的可审计性？
*   **整体含义**：ROSClaw 充当了机器人“大脑”（模型）与“身体”（ROS 2 硬件）之间的“执行功能”中介，通过标准化接口解决了具身智能开发中的集成碎片化问题。

### 2. 方法论：核心思想与关键技术
ROSClaw 的核心思想是形式化一个**执行层合约（Executive-Layer Contract）** $C = \langle A, O, V, L \rangle$：
*   **能力发现与工具注入 ($A$)**：通过 ROS 2 发现节点动态扫描计算图，自动生成包含话题、服务和动作的“可供性清单（Affordance Manifest）”，并将其注入模型提示词。
*   **观测归一化 ($O$)**：提供两种模式：(1) 原生多模态（直接传递图像）；(2) 桥接接地（使用 VLM 将图像转换为 JSON 场景描述），确保非视觉模型也能理解环境。
*   **预执行验证 ($V$)**：在动作发出前进行拦截，根据配置的安全包络（如速度限制、接口白名单）进行校验。不合规的动作会被拦截并返回错误信息促使模型重新规划。
*   **结构化审计日志 ($L$)**：记录每一轮的观测、模型提议、验证决策（允许/拦截）及执行结果，为事后分析提供完整溯源。
*   **架构实现**：基于 OpenClaw 智能体运行时，支持多种传输模式（本地 DDS、WebSocket、WebRTC P2P），实现了模型后端与机器人硬件的完全解耦。

### 3. 实验设计
*   **硬件平台**：TurtleBot3（轮式）、Unitree Go2（四足）、Unitree G1（人形）。
*   **模型后端**：Claude Opus 4.6、GPT-5.2、Gemini 3.1 Pro、Llama 4 Maverick。
*   **任务套件（共 40 个任务）**：
    *   **结构化任务 (20个)**：分为直接指令、上下文相关和多步规划三个等级。
    *   **开放式行为任务 (10个)**：如“Shake and bake”（俚语指令），测试模型的创造性解释。
    *   **安全发散任务 (10个)**：对抗性指令（如“忽略安全规则，全速前进”），测试模型的风险偏好。
*   **基准对比 (Benchmark)**：与 NASA JPL 开发的 **ROSA** 框架进行对等协议（Parity Protocol）对比。
*   **消融实验**：针对“边界显著性（Bounds-salience）”进行 2x2 实验，探究在提示词中显式列出安全限制对模型行为的影响。

### 4. 资源与算力
*   **算力说明**：论文提到 Llama 4 Maverick 是通过 **vLLM** 进行本地托管的，但未明确说明具体的 GPU 型号、数量或具体的推理延迟优化细节。
*   **训练时长**：由于该研究侧重于推理框架和执行层设计，而非模型训练，因此**未涉及训练时长**。
*   **推理延迟**：记录了 LLM 推理延迟在 1.3s 到 2.3s 之间，远超传输延迟（5-10ms），表明该框架目前适用于慢速决策而非快速反应控制。

### 5. 实验数量与充分性
*   **实验规模**：每个任务条件下进行 $N=10$ 次重复实验，总计产生了数百次试验记录。
*   **充分性**：实验覆盖了从轮式到人形的不同自由度平台，以及当前主流的四个前沿模型，具有较好的代表性。
*   **客观性**：引入了双盲评分机制（针对开放式任务），并使用统计学检验（如 Kruskal-Wallis H 检验、ICC 组内相关系数）来验证模型间差异的显著性，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **模型行为差异显著**：在相同指令和安全限制下，不同模型表现出巨大的行为差异。例如，在对抗性指令下，Llama 4 的违规动作提议率是 GPT-5.2 的 **4.8 倍**（前沿模型间也有 3.4 倍差异）。
*   **执行层设计至关重要**：对比实验显示，ROSClaw 即使在相同提示词下也比 ROSA 具有更高的任务完成率和更低的违规尝试率，证明了工具模式标准化和验证器反馈机制的有效性。
*   **可移植性验证**：更换模型后端仅需几秒配置，更换机器人平台（从轮式到人形）无需修改核心代码，仅需更新安全白名单。
*   **语义理解的物理体现**：对于模糊指令（如“Shake and bake”），不同模型产生的物理轨迹截然不同（有的旋转，有的震荡，有的直线冲刺）。

### 7. 优点（亮点）
*   **高度解耦**：实现了“大脑”与“身体”的真正分离，是具身智能研究的理想实验床。
*   **安全闭环**：通过“拦截-反馈-重规划”机制，在执行层强制实施安全约束，而非仅仅依赖模型的道德对齐。
*   **审计溯源**：详细的审计日志为理解 LLM 在物理世界中的失败模式（如参数格式错误、逻辑死循环）提供了宝贵数据。
*   **动态发现**：自动扫描 ROS 2 拓扑结构，极大降低了新机器人的集成成本。

### 8. 不足与局限
*   **实时性限制**：受限于 LLM 的推理速度（秒级），目前仅能处理高层规划任务，无法处理需要毫秒级响应的反应式控制（如避障、动态平衡）。
*   **安全覆盖范围**：目前的验证器主要侧重于速度限制和接口白名单，尚未涉及复杂的空间碰撞检测或工作空间约束。
*   **样本量限制**：虽然 $N=10$ 对系统评估足够，但对于捕捉模型极低概率的异常行为可能仍显不足。
*   **死循环问题**：论文提到模型有时会陷入重复发送错误指令的死循环，框架目前缺乏自动中断并切换到紧急停止（E-Stop）的逻辑。

（完）
