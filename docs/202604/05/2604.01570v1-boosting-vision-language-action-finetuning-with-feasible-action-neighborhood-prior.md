---
title: Boosting Vision-Language-Action Finetuning with Feasible Action Neighborhood Prior
title_zh: 利用可行动作邻域先验提升视觉-语言-动作模型微调
authors: "Haochen Niu, Kanyu Zhang, Shuyu Yin, Qinghai Guo, Peilin Liu, Fei Wen"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01570v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于机器人操作的视觉-语言-动作（VLA）模型微调
tldr: 针对机器人操作中存在“可行动作邻域（FAN）”这一物理特性，即多个相近动作均能达成目标，本文提出了一种FAN引导的正则化方法。该方法通过引入高斯先验，使模型输出分布在局部保持平滑和单峰，从而克服了传统VLA训练忽略动作容差导致的泛化性差和样本效率低的问题。实验证明，该方法在强化微调和监督微调中均显著提升了任务成功率和样本效率。
selection_source: fresh_fetch
motivation: 现有的VLA训练直接沿用语言模型方法，忽略了机器人操作中存在多个等效可行动作的物理特性，导致模型泛化能力和样本效率受限。
method: 引入一种FAN引导的正则化项，利用高斯先验约束模型输出分布，使其在首选动作的方向和幅度周围保持局部平滑与单峰性。
result: 在监督微调和强化微调实验中，该方法显著提高了模型在分布内及分布外场景下的成功率和样本利用率。
conclusion: 通过对齐物理操作中的动作容差特性，FAN引导的正则化为提升VLA模型的适应能力和泛化性提供了一种原则性且实用的方法。
---

## 摘要
在现实世界的机器人操纵中，状态通常允许一个近乎等效动作的邻域。也就是说，对于每个状态，存在一个可行动作邻域（FAN）而非单一的正确动作，在该邻域内的运动所产生的进展是难以区分的。然而，现有的 VLA 训练方法直接继承自语言设置，未能利用 FAN 特性，从而导致泛化能力差和样本效率低。为了解决这一局限性，我们引入了一种 FAN 引导的正则化器，通过塑造模型的输出分布使其与 FAN 的几何结构对齐。具体而言，我们引入了一个高斯先验，在首选方向和幅度周围促进局部平滑且单峰的预测。在强化微调（RFT）和监督微调（SFT）的大量实验中，我们的方法在样本效率以及分布内和分布外（OOD）场景的成功率方面均取得了显著提升。通过与物理操纵固有的动作容差相对齐，FAN 引导的正则化为实现高样本效率且具泛化性的 VLA 适配提供了一种有原则且实用的方法。

## Abstract
In real-world robotic manipulation, states typically admit a neighborhood of near-equivalent actions. That is, for each state, there exist a feasible action neighborhood (FAN) rather than a single correct action, within which motions yield indistinguishable progress. However, prevalent VLA training methodologies are directly inherited from linguistic settings and do not exploit the FAN property, thus leading to poor generalization and low sample efficiency. To address this limitation, we introduce a FAN-guided regularizer that shapes the model's output distribution to align with the geometry of FAN. Concretely, we introduce a Gaussian prior that promotes locally smooth and unimodal predictions around the preferred direction and magnitude. In extensive experiments across both reinforced finetuning (RFT) and supervised finetuning (SFT), our method achieves significant improvement in sample efficiency, and success rate in both in-distribution and out-of-distribution (OOD) scenarios. By aligning with the intrinsic action tolerance of physical manipulation, FAN-guided regularization provides a principled and practical method for sample-efficient, and generalizable VLA adaptation.

---

## 论文详细总结（自动生成）

以下是对论文《Boosting Vision-Language-Action Finetuning with Feasible Action Neighborhood Prior》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型训练方法（如 OpenVLA）直接沿用了语言模型的“预测下一个 Token”范式，将连续动作空间离散化并使用交叉熵损失。这种方法忽略了机器人操作中的**物理容差（Physical Tolerance）**。
*   **研究动机**：在物理世界中，一个状态往往对应一个“可行动作邻域（FAN）”，即多个相近的动作都能产生几乎相同的任务进展。传统的训练方法强迫模型只拟合演示中的单一动作，导致模型输出分布过于“尖锐（Spiky）”，从而引发严重的过拟合、泛化性差以及强化学习微调（RFT）时样本效率低下的问题。

### 2. 方法论
*   **核心思想**：引入一种 **FAN 引导的正则化器（FAN-guided regularizer）**，通过高斯先验（Gaussian Prior）来塑造模型的输出分布，使其符合物理操作的局部平滑性和单峰性。
*   **关键技术细节**：
    *   **FAN 定义**：定义为在给定状态下，与最优动作相比其 Q 值损失在一定阈值（$\delta$）内的连通动作集合。
    *   **正则化项**：计算模型预测分布 $\pi$ 与目标高斯分布 $N(\mu, \Sigma)$ 之间的 KL 散度。
    *   **监督微调（FAN-SFT）**：使用动态协方差矩阵。均值 $\mu$ 为模型预测的最优动作，协方差 $\Sigma$ 根据模型当前分布的方差动态调整，引导模型从“排他性正确”转向“加权容差”。
    *   **强化微调（FAN-PPO）**：在 PPO 的目标函数中加入 FAN 正则化项。为了稳定训练，此处使用固定的协方差 $\Sigma = \sigma^2 I$。
*   **算法流程**：在训练过程中，除了标准的负对数似然（SFT）或优势函数（RFT）损失外，额外计算并最小化与高斯先验的 KL 散度，强制模型在预测动作周围保留一定的概率质量。

### 3. 实验设计
*   **数据集与场景**：
    *   **ManiSkill**：使用 `PutOnPlateInScene25Main-v3` 任务，包含 25 种多样的拾取与放置原语。
    *   **LIBERO**：涵盖 Spatial、Object、Goal 和 Long 四个子套件。
    *   **分布外（OOD）评估**：设计了 15 种扰动，包括视觉（桌子、纹理、噪声）、语义（未见物体、指令、干扰物）和执行（机器人姿态、物体重新定位）层面的变化。
*   **Benchmark 与对比方法**：
    *   **基准模型**：OpenVLA（单步动作）和 OpenVLA-OFT（动作块）。
    *   **对比方法**：标准 SFT、RL4VLA（基于 PPO 的微调）、标签平滑（Label Smoothing）、熵最大化（Entropy Maximization）。
*   **真实世界实验**：使用 JAKA 7-DoF 机械臂进行 4 类任务测试，验证空间鲁棒性。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **SFT 阶段**：ManiSkill 实验使用 4 张 NVIDIA A100 (80GB)；LIBERO 实验根据模型不同使用 2 张或 4 张 A100。
    *   **RFT 阶段**：使用 1 张 NVIDIA A100 (80GB)。
*   **训练时长**：SFT 训练步数最高达 60K 步；RFT 训练 390-650 个 Episode。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的仿真实验（ManiSkill 和 LIBERO 多个子集）和 150 次真实世界试验。
*   **充分性分析**：
    *   **消融实验**：对正则化系数 $\alpha$ 和高斯标准差 $\sigma$ 进行了详细的灵敏度分析。
    *   **对比充分**：不仅对比了标准基准，还对比了标签平滑等类似思路的方法，证明了 FAN 这种“结构化先验”的优越性。
    *   **客观性**：在不同架构（单步 vs 块预测）和不同微调范式（SFT vs RFT）下均验证了有效性，实验设计较为公平。

### 6. 主要结论与发现
*   **泛化性提升**：在 ManiSkill OOD 任务中，FAN-SFT 比标准 SFT 平均提升了 5.2% 的成功率；在执行类 OOD 任务中提升尤为显著（>10%）。
*   **样本效率**：在 RFT 过程中，FAN-PPO 达到 90% 成功率所需步数仅为基准方法的 1/3。
*   **分布形态**：实验证实，标准 SFT 会导致分布“尖锐化”，而 FAN 正则化能保持分布的平滑性，这与模型的鲁棒性直接正相关。
*   **真实世界验证**：在存在空间扰动的情况下，FAN-SFT 表现出更强的适应能力，而标准 SFT 容易陷入训练数据的空间偏差中。

### 7. 优点
*   **物理启发式创新**：深刻洞察了机器人动作空间与语言空间的本质区别（动作的连续容差性）。
*   **即插即用**：无需修改模型架构或推理流程，仅通过损失函数即可实现性能提升。
*   **多范式适用**：同时解决了 SFT 的过拟合问题和 RFT 的探索效率问题。

### 8. 不足与局限
*   **超参数依赖**：引入了 $\alpha$ 和 $\sigma$ 两个关键超参数，虽然论文给出了推荐值，但在不同任务中可能仍需微调。
*   **先验形状假设**：目前主要使用单峰高斯分布作为先验。对于某些本质上具有高度多峰性（Multimodal）的复杂任务，简单的单峰高斯先验可能无法完美拟合。
*   **任务复杂度限制**：实验主要集中在拾取-放置类任务，对于更复杂的长程逻辑推理或精细力控任务的适用性有待进一步验证。

（完）
