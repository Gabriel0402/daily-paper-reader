---
title: "Semantic-Fast-SAM: Efficient Semantic Segmenter"
title_zh: Semantic-Fast-SAM：高效语义分割器
authors: Byunghyun Kim
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20169v1"
tags: ["query:mmseg"]
score: 10.0
evidence: 结合Fast-SAM与语义标注实现高效分割
tldr: 本研究提出 Semantic-Fast-SAM (SFS)，旨在解决原始 SAM 模型在语义分割任务中计算开销大、难以实时应用的问题。SFS 将基于 CNN 的高效 FastSAM 与语义标注流水线相结合，通过集成语义分割一切（SSA）策略为掩码分配类别。该模型在保持高精度的同时，显著降低了内存占用，推理速度提升约 20 倍，并支持基于 CLIP 的开放词汇分割，为机器人等实时场景提供了实用的基础分割方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20169v1/fig-001.webp\", \"caption\": \"TABLE III SEMANTIC SEGMENTATION PERFORMANCE (MIOU). BEST WITHIN EACH SUPERVISION REGIME IS BOLDED; HIGHER IS BETTER.\", \"page\": 4, \"index\": 1, \"width\": 529, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2604.20169v1/fig-002.webp\", \"caption\": \"TABLE IV ABLATION RESULTS ON OPEN-VOCABULARY FUSION AND MASK COUNT. BEST WITHIN EACH ABLATION BLOCK IS BOLDED.\", \"page\": 4, \"index\": 2, \"width\": 529, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.20169v1/fig-003.webp\", \"caption\": \"TABLE II GPU MEMORY USAGE FOR DIFFERENT METHODS. BEST WITHIN EACH COMPARABLE BLOCK IS BOLDED; LOWER IS BETTER.\", \"page\": 3, \"index\": 3, \"width\": 529, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.20169v1/fig-004.webp\", \"caption\": \"Fig. 2. Overall architecture of Semantic-Fast-SAM.\", \"page\": 2, \"index\": 4, \"width\": 529, \"height\": 254}]"
motivation: 原始基于 Transformer 的 SAM 模型及其语义标注扩展计算成本极高，无法满足实时语义分割的需求。
method: 结合 FastSAM 的快速掩码生成能力与 SSA 标注策略，并集成 CLIP 语义头以支持开放词汇分类。
result: 在 Cityscapes 和 ADE20K 基准上精度与 SAM 相当，但推理速度比 SSA 快 20 倍，且在开放词汇任务中表现优异。
conclusion: SFS 成功实现了高效且实时的“分割一切”语义分割框架，拓宽了基础分割模型在实际工程中的应用前景。
---

## 摘要
我们提出了 Semantic-Fast-SAM (SFS)，这是一个将 Fast Segment Anything 模型与语义标注流水线相结合的语义分割框架，在不牺牲准确性的情况下实现了实时性能。FastSAM 是 Segment Anything Model (SAM) 的一种基于 CNN 的高效重新实现，其运行速度远快于原始基于 Transformer 的 SAM。基于 FastSAM 的快速掩码生成能力，我们集成了一种语义分割一切 (SSA) 标注策略，为每个掩码分配具有实际意义的类别。由此产生的 SFS 模型能够生成高质量的语义分割图，而其计算成本和内存占用仅为原始基于 SAM 方法的一小部分。在 Cityscapes 和 ADE20K 基准测试上的实验表明，SFS 的准确率与之前的基于 SAM 的方法相当（Cityscapes 上的 mIoU 约为 70.33，ADE20K 上约为 48.01），同时在闭集设置下的推理速度比 SSA 快约 20 倍。我们还展示了 SFS 通过利用基于 CLIP 的语义头，能够有效处理开放词汇分割，在广泛类别标注方面优于近期的开放词汇模型。这项工作实现了具有“分割一切”能力的实用实时语义分割，扩展了基础分割模型在机器人场景中的适用性。代码实现可在 https://github.com/KBH00/Semantic-Fast-SAM 获取。

## Abstract
We propose Semantic-Fast-SAM (SFS), a semantic segmentation framework that combines the Fast Segment Anything model with a semantic labeling pipeline to achieve real-time performance without sacrificing accuracy. FastSAM is an efficient CNN-based re-implementation of the Segment Anything Model (SAM) that runs much faster than the original transformer-based SAM. Building upon FastSAM's rapid mask generation, we integrate a Semantic-Segment-Anything (SSA) labeling strategy to assign meaningful categories to each mask. The resulting SFS model produces high-quality semantic segmentation maps at a fraction of the computational cost and memory footprint of the original SAM-based approach. Experiments on Cityscapes and ADE20K benchmarks demonstrate that SFS matches the accuracy of prior SAM-based methods (mIoU ~ 70.33 on Cityscapes and 48.01 on ADE20K) while achieving approximately 20x faster inference than SSA in the closed-set setting. We also show that SFS effectively handles open-vocabulary segmentation by leveraging CLIP-based semantic heads, outperforming recent open-vocabulary models on broad class labeling. This work enables practical real-time semantic segmentation with the "segment-anything" capability, broadening the applicability of foundation segmentation models in robotics scenarios. The implementation is available at https://github.com/KBH00/Semantic-Fast-SAM.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic-Fast-SAM: Efficient Semantic Segmenter》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：原始的 Segment Anything Model (SAM) 虽然具备强大的零样本分割能力，但存在两个致命缺陷：一是**计算开销巨大**（基于 ViT-H 架构，难以实时运行）；二是**缺乏语义标签**（仅生成无类别的掩码）。
*   **研究背景**：现有的改进方案如 Semantic-Segment-Anything (SSA) 虽然通过外挂分类器解决了标签问题，但由于底层仍依赖 SAM，推理速度极慢（处理一张图需数秒）。
*   **研究目标**：开发一种既能保持“分割一切”能力，又能实现实时推理且具备语义识别能力的框架，以满足机器人和自动驾驶等实际应用需求。

### 2. 方法论：核心思想与关键技术
SFS 采用了一种**两阶段、多分支**的架构，核心思想是“用高效的 CNN 替代沉重的 Transformer，用多源知识融合进行标注”：
*   **第一阶段：快速掩码生成 (Fast Mask Generation)**
    *   采用 **FastSAM** 作为骨干网络。它基于 YOLOv8-seg 架构，利用卷积神经网络（CNN）和 YOLACT 风格的实例分割分支，在单次前向传播中生成全图掩码，速度比 SAM 快约 50 倍。
*   **第二阶段：语义标注模块 (Semantic Labeling Module)**
    *   **闭集语义分支 (Closed-Set Branch)**：利用预训练的 OneFormer（基于 ConvNeXt-L）在 COCO 和 ADE20K 数据集上生成粗略语义图，通过区域内“多数投票法”为掩码分配已知类别。
    *   **开放词汇分支 (Open-Vocabulary Branch)**：针对未知物体，利用 **BLIP** 生成图像区域描述，提取名词短语作为候选标签，再通过 **CLIP** 计算图像嵌入与文本嵌入的余弦相似度进行重排序。
    *   **决策融合 (Fusion)**：通过启发式规则合并两个分支的结果。若闭集预测置信度高则保留；若 CLIP 发现更匹配的开放词汇标签则替换。

### 3. 实验设计：数据集、Benchmark 与对比方法
*   **数据集**：主要在 **Cityscapes**（城市景观）和 **ADE20K**（室内外通用场景）验证集上进行零样本（Zero-shot）评估。
*   **Benchmark**：
    *   **精度对比**：对比了 SSA (SAM-based)、OneFormer、Mask2Former、SegFormer 等。
    *   **开放词汇对比**：对比了 CLIPSeg、GroupViT、MaskCLIP 等。
*   **对比维度**：平均交并比 (mIoU)、推理延迟 (Latency)、GPU 显存占用 (Memory)。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 3090 GPU (24 GB)** 的工作站上完成。
*   **训练时长**：文中明确指出 SFS 是一个**推理时流水线 (Inference-time pipeline)**，直接组合了预训练组件，**不需要额外的反向传播或模型微调**。因此，不存在训练时长的概念，其优势在于即插即用。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了精度测试。
    *   进行了推理速度和显存占用的定量对比。
    *   **消融实验**：针对“是否使用开放词汇分支”以及“保留掩码数量（25/50/100）”对性能和速度的影响进行了测试。
*   **充分性评价**：实验设计较为客观，涵盖了速度、精度、显存三个关键工程指标。对比对象既包含了原始 SAM 方案，也包含了全监督的 SOTA 模型，能够清晰展示该方法的定位（即：以极小的精度损失换取巨大的速度提升）。

### 6. 主要结论与发现
*   **速度飞跃**：在闭集设置下，SFS 的推理速度（0.08s）比基于 SAM 的 SSA（1.65s）快了约 **20 倍**。
*   **精度持平**：SFS 在 Cityscapes (70.33 mIoU) 和 ADE20K (48.01 mIoU) 上的表现与 SSA 非常接近，证明 FastSAM 生成的掩码质量足以支撑语义标注任务。
*   **显存优化**：峰值显存从 SSA 的 19GB 降至 **4.5GB**，使得该模型可以在 8-12GB 显存的消费级显卡上运行。
*   **开放词汇能力**：通过 CLIP 增强，SFS 在处理未见过的类别时优于传统的 GroupViT 等模型。

### 7. 优点（亮点）
*   **极高的工程实用性**：解决了 SAM 落地最核心的“慢”和“贵”的问题。
*   **无需重训**：巧妙组合了现有的 FastSAM、OneFormer、BLIP 和 CLIP，展示了强大的模块化集成能力。
*   **灵活可调**：用户可以根据算力需求，通过调整掩码预算（Mask Budget）或开关开放词汇分支来平衡速度与精度。

### 8. 不足与局限
*   **开放词汇模式下的瓶颈**：虽然闭集模式很快，但在开启开放词汇模式（BLIP+CLIP）时，推理时间仍需约 10 秒，这主要是受限于 BLIP 对每个掩码区域的逐一处理，尚未达到完全的实时性。
*   **掩码精度风险**：FastSAM 在处理极小物体时，掩码精度偶尔逊色于原始 SAM，这可能导致细碎目标的语义分配错误。
*   **依赖预训练模型**：SFS 的上限受限于所使用的闭集分割器（如 OneFormer）和视觉语言模型（CLIP）的质量。

（完）
