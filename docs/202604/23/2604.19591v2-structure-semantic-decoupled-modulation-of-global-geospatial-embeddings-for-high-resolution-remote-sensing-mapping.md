---
title: Structure-Semantic Decoupled Modulation of Global Geospatial Embeddings for High-Resolution Remote Sensing Mapping
title_zh: 结构-语义解耦调制的全球地理空间嵌入用于高分辨率遥感制图
authors: "Jienan Lyu, Miao Yang, Jinchen Cai, Yiwen Hu, Guanyi Lu, Junhao Qiu, Runmin Dong"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19591v2"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用全球地理空间基础模型和解耦调制进行遥感制图
tldr: 针对高分辨率遥感制图中局部特征导致的预测破碎和泛化性差问题，本文提出结构-语义解耦调制（SSDM）框架。该框架通过将全局地理空间嵌入解耦为结构先验和全局语义两个分支，分别引导局部特征提取并增强语义一致性，有效解决了跨模态融合中的特征干扰与空间降级问题，显著提升了复杂地物的制图精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19591v2/fig-001.webp\", \"caption\": \"Table 1: Comparison results on the GID24 (4 m) and GID24 (2 m) subsets. All models are trained exclusively on the 4 m training set, and the 2 m test set is utilized to evaluate generalization. The best results are highlighted in bold.\", \"page\": 6, \"index\": 1, \"width\": 1056, \"height\": 184}, {\"url\": \"assets/figures/arxiv/2604.19591v2/fig-002.webp\", \"caption\": \"Figure 3: Qualitative comparison of segmentation results. The colored bounding boxes highlight complex regions where existingmethods suffer from severe fragmentation or boundary inconsistency, whereas our SSDMmaintains superior structural coherence and precise delineation.\", \"page\": 6, \"index\": 2, \"width\": 1056, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2604.19591v2/fig-003.webp\", \"caption\": \"Figure 2: Overview of the proposed SSDM framework. (a) Overall framework. The adapted global embeddings are functionally decoupled to guide the encoder via SMM and complement semantic information via SeMM. (b) Architecture of the SMM block. (c) Structural-aware self-attention mechanism.\", \"page\": 4, \"index\": 3, \"width\": 1056, \"height\": 665}, {\"url\": \"assets/figures/arxiv/2604.19591v2/fig-004.webp\", \"caption\": \"Table 2: Per-class performance on the GID24 (4 m) subset.\", \"page\": 7, \"index\": 4, \"width\": 440, \"height\": 548}]"
motivation: 传统高分辨率遥感制图过度依赖局部特征且泛化性差，而直接融合全局地理空间嵌入时存在严重的语义-空间鸿沟。
method: 提出SSDM框架，通过结构先验分支引入宏观感受野约束抑制预测碎片化，并通过全局语义注入分支增强深层特征的类别辨别力。
result: 实验证明该方法在多种复杂场景下均达到了最先进的性能，显著提升了高分辨率遥感制图的准确性和语义一致性。
conclusion: SSDM为将地理空间大模型有效集成到高分辨率视觉任务中提供了一种通用且高效的解耦调制范式。
---

## 摘要
细粒度高分辨率遥感制图通常依赖于局部视觉特征，这限制了跨域泛化能力，并常导致大尺度地物的预测结果出现碎片化。虽然全球地理空间基础模型提供了强大且泛化性强的表示，但由于严重的语义-空间鸿沟，直接将其高维隐式嵌入与高分辨率视觉特征融合，往往会引发特征干扰和空间结构退化。为克服这些局限性，我们提出了一种结构-语义解耦调制（SSDM）框架，将全球地理空间表示解耦为两条互补的跨模态注入路径。首先，结构先验调制分支将全球表示中的宏观感受野先验引入高分辨率编码器的自注意力模块中。通过利用整体结构约束引导局部特征提取，该分支有效抑制了由高频细节噪声和过大的类内方差导致的预测碎片化。其次，全球语义注入分支将整体上下文与深层高分辨率特征空间进行显式对齐，并通过跨模态集成直接补充全球语义，从而显著增强了复杂地物的语义一致性和类别级辨别力。大量实验表明，与现有的跨模态融合方法相比，我们的方法达到了最先进的性能。通过释放全球嵌入的潜力，SSDM 在多种场景下持续提升了高分辨率制图的精度，为将地理空间基础模型集成到高分辨率视觉任务中提供了一种通用且有效的范式。

## Abstract
Fine-grained high-resolution remote sensing mapping typically relies on localized visual features, which restricts cross-domain generalizability and often leads to fragmented predictions of large-scale land covers. While global geospatial foundation models offer powerful, generalizable representations, directly fusing their high-dimensional implicit embeddings with high-resolution visual features frequently triggers feature interference and spatial structure degradation due to a severe semantic-spatial gap. To overcome these limitations, we propose a Structure-Semantic Decoupled Modulation (SSDM) framework, which decouples global geospatial representations into two complementary cross-modal injection pathways. First, the structural prior modulation branch introduces the macroscopic receptive field priors from global representations into the self-attention modules of the high-resolution encoder. By guiding local feature extraction with holistic structural constraints, it effectively suppresses prediction fragmentation caused by high-frequency detail noise and excessive intra-class variance. Second, the global semantic injection branch explicitly aligns holistic context with the deep high-resolution feature space and directly supplements global semantics via cross-modal integration, thereby significantly enhancing the semantic consistency and category-level discrimination of complex land covers. Extensive experiments demonstrate that our method achieves state-of-the-art performance compared to existing cross-modal fusion approaches. By unleashing the potential of global embeddings, SSDM consistently improves high-resolution mapping accuracy across diverse scenarios, providing a universal and effective paradigm for integrating geospatial foundation models into high-resolution vision tasks.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **SSDM（Structure-Semantic Decoupled Modulation，结构-语义解耦调制）** 的框架，旨在将全球地理空间基础模型（GFMs）的宏观表示有效地集成到高分辨率遥感语义分割任务中。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：高分辨率遥感制图通常依赖局部视觉特征，这导致在处理大尺度连续地物时容易产生“预测碎片化”现象，且跨地域泛化能力差。
*   **研究背景**：虽然现有的地理空间基础模型（如 AlphaEarth, TESSERA）能提供全球尺度的宏观上下文，但由于其与高分辨率图像之间存在巨大的**语义-空间鸿沟**（高维隐式嵌入 vs. 细粒度空间特征），直接进行特征融合（如拼接或双编码器）往往会引起特征干扰，破坏空间结构，导致性能下降。
*   **整体含义**：论文试图寻找一种通用的范式，将全球尺度的“先验知识”以非侵入、高效的方式注入到局部视觉模型中。

### 2. 方法论：核心思想与关键技术
SSDM 的核心思想是将全球嵌入的功能**解耦**为两个独立的路径，分别在网络的不同阶段进行调制：

*   **结构调制模块 (SMM)**：
    *   **位置**：作用于编码器（Encoder）阶段。
    *   **技术细节**：将全球嵌入投影为“结构亲和矩阵”，作为**加性偏置（Additive Bias）**注入到自注意力机制的 Logits 中。
    *   **作用**：通过宏观空间约束引导局部特征提取，利用分解注意力（行/列方向）提升效率，有效抑制由局部噪声引起的预测碎片化。
*   **语义调制模块 (SeMM)**：
    *   **位置**：作用于解码器（Decoder）之前的深层特征空间。
    *   **技术细节**：通过轻量级卷积编码全球语义，并以残差连接的方式注入到掩码特征（Mask Features）中。
    *   **作用**：在不破坏空间组织的前提下，补充全局类别上下文，增强复杂地物的语义一致性。
*   **基础架构**：以 Mask2Former 为基准模型，采用 ResNet-50 作为骨干网络。

### 3. 实验设计
*   **数据集**：
    *   **GID24**：包含24类精细地物。使用 4m 分辨率数据进行训练和域内评估。
    *   **跨分辨率测试**：使用 2m 分辨率的 GaoFen/ZiYuan 卫星数据评估模型的跨尺度泛化性。
*   **全球嵌入源**：主要使用 AEF (10m)，并测试了 TESSERA (10m) 和 ESD (30m) 以验证通用性。
*   **对比方法 (Benchmarks)**：
    *   Baseline (仅 HR 图像)。
    *   Dual-Encoder (双编码器融合)。
    *   SAM-based (基于 SAM 模型的结构先验融合)。
    *   DFormerv2* (针对 RGB-D 任务的几何注意力模型适配版)。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA RTX 4090 GPU**。
*   **软件框架**：基于 Detectron2 和 Mask2Former 官方实现。
*   **训练细节**：采用 AdamW 优化器，基础学习率 $1 \times 10^{-4}$，权重衰减 0.05，输入尺寸固定为 $512 \times 512$。
*   **时长**：文中未明确给出具体的总训练小时数，但提到了推理延迟（SSDM 约为 33.74ms/张）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在 4m 和 2m 两个分辨率上进行了全指标对比。
    *   **消融实验**：分别验证了 SMM 和 SeMM 的增益，证明了解耦设计的必要性。
    *   **泛化实验**：在不微调的情况下，直接将模型应用于 TESSERA 和 ESD 嵌入，验证了框架对不同基础模型的适配性。
    *   **效率分析**：对比了参数量、FLOPs、推理延迟和显存占用。
*   **评价**：实验设计较为充分，涵盖了精度、泛化性、鲁棒性和计算效率四个维度。通过跨分辨率和跨嵌入源的测试，客观地证明了方法的通用性。

### 6. 主要结论与发现
*   **性能提升**：SSDM 在 GID24 (4m) 上比 Baseline 提升了约 **10.93% 的 mIoU**，在跨分辨率测试中也保持了领先。
*   **解耦优势**：结构调制对大面积地物（如农田、草地）提升最明显，而语义调制有助于稳定类别判断。
*   **鲁棒性**：即使全球嵌入与高分辨率图像之间存在时间不一致性（如 2015 年图像配 2017 年嵌入），SSDM 依然能提取有效先验，表现出极强的噪声容忍度。

### 7. 优点（亮点）
*   **轻量高效**：相比于引入 SAM 等大型外部模型，SSDM 增加的计算开销较小，且推理速度更快。
*   **解耦范式**：成功解决了跨模态融合中的“特征干扰”问题，为地理空间大模型的下游应用提供了新思路。
*   **通用性强**：不依赖特定的基础模型，能够适配多种全球嵌入场。

### 8. 不足与局限
*   **细小物体挑战**：对于边界极度模糊或尺度极小的类别（如雪地、灌木、池塘），提升幅度相对有限。
*   **过度平滑风险**：结构调制虽然增强了连通性，但在某些极端情况下可能会导致细微边缘的过度平滑。
*   **时间对齐**：虽然实验证明了其鲁棒性，但在地物变化剧烈的区域，时间跨度较大的嵌入可能会引入错误的先验引导。

（完）
