---
title: Dynamic Class-Aware Active Learning for Unbiased Satellite Image Segmentation
title_zh: 针对无偏卫星图像分割的动态类别感知主动学习
authors: "Gadi Hemanth Kumar, Athira Nambiar, Pankaj Bodani"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08965v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 卫星图像语义分割与土地覆盖制图
tldr: 针对卫星图像分割中大规模标注成本高及类别不平衡导致模型偏差的问题，本文提出了一种动态类别感知的主动学习方法（DCAU-AL）。该方法通过实时监控各类别性能差异，动态调整采样权重，优先选择性能较差或稀有类别的样本进行标注。在OpenEarth数据集上的实验证明，该方法在处理严重类别不平衡时显著优于现有主动学习策略，提高了标注效率和分割精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08965v1/fig-001.webp\", \"caption\": \"Fig. 3: Segmentation results for Fully Supervised, Naive-AL, and DCAU-AL models across AL cycles, with corresponding mIoU values in parentheses.(Better Viewed in Colour)\", \"page\": 11, \"index\": 1, \"width\": 725, \"height\": 862}, {\"url\": \"assets/figures/arxiv/2604.08965v1/fig-002.webp\", \"caption\": \"Fig. 2: Progression of mIoU scores across acquisition cycles for Fully Supervised, Naive AL, and DCAU-Based AL models.(Better viewed in colour)\", \"page\": 10, \"index\": 2, \"width\": 473, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2604.08965v1/fig-003.webp\", \"caption\": \"Fig. 1: Proposed Dynamic Class Aware Uncertainty based Active Learning (DCAU-AL) framework for semantic segmentation with a human-in-the-loop.\", \"page\": 5, \"index\": 3, \"width\": 725, \"height\": 308}]"
motivation: 传统主动学习策略依赖全局不确定性，难以应对卫星图像中常见的类别不平衡问题，导致模型对稀有类别产生偏差。
method: 提出DCAU-AL机制，通过实时追踪各类别分割性能并动态调整采样权重，使模型在主动学习过程中聚焦于表现欠佳的类别。
result: 在OpenEarth土地覆盖数据集上的实验表明，DCAU-AL在各类别的交并比（IoU）和标注效率上均显著优于现有方法。
conclusion: 动态类别感知采样能有效缓解卫星图像分割中的类别偏差，是资源受限下实现高精度土地覆盖制图的有效方案。
---

## 摘要
卫星图像的语义分割在土地覆盖制图和环境监测中发挥着至关重要的作用。然而，标注大规模、高分辨率的卫星数据集既昂贵又耗时，尤其是在覆盖广阔地理区域时。主动学习（AL）提供了一种高效的替代方案，它并非随机标记数据或详尽标注整个数据集，而是在人机回环（HITL）的帮助下，智能地选择最具信息量的样本进行标注，从而在保持高模型性能的同时降低标注成本。AL 对于大规模或资源受限的卫星应用特别有益，因为它能以显著更少的标注样本实现高分割精度。尽管具有这些优势，但标准的主动学习策略通常依赖于全局不确定性或多样性度量，且缺乏随着训练推进而针对性能欠佳或稀有类别的适应性，从而导致系统产生偏差。为了克服这些局限性，我们提出了一种新型自适应采集函数——基于动态类别感知不确定性的主动学习（DCAU-AL），该函数根据实时的类别性能差距优先选择样本，从而解决类别不平衡问题。所提出的 DCAU-AL 机制在整个主动学习过程中持续跟踪每个类别的分割性能，并动态调整采样权重，以关注性能较差或代表性不足的类别。在 OpenEarth 土地覆盖数据集上的广泛实验表明，DCAU-AL 显著优于现有的主动学习方法，特别是在严重的类别不平衡情况下，提供了更优的每类 IoU 并提高了标注效率。

## Abstract
Semantic segmentation of satellite imagery plays a vital role in land cover mapping and environmental monitoring. However, annotating large-scale, high-resolution satellite datasets is costly and time consuming, especially when covering vast geographic regions. Instead of randomly labeling data or exhaustively annotating entire datasets, Active Learning (AL) offers an efficient alternative by intelligently selecting the most informative samples for annotation with the help of Human-in-the-loop (HITL), thereby reducing labeling costs while maintaining high model performance. AL is particularly beneficial for large-scale or resource-constrained satellite applications, as it enables high segmentation accuracy with significantly fewer labeled samples. Despite these advantages, standard AL strategies typically rely on global uncertainty or diversity measures and lack the adaptability to target underperforming or rare classes as training progresses, leading to bias in the system. To overcome these limitations, we propose a novel adaptive acquisition function, Dynamic Class-Aware Uncertainty based Active learning (DCAU-AL) that prioritizes sample selection based on real-time class-wise performance gaps, thereby overcoming class-imbalance issue. The proposed DCAU-AL mechanism continuously tracks the performance of the segmentation per class and dynamically adjusts the sampling weights to focus on poorly performing or underrepresented classes throughout the active learning process. Extensive experiments on the OpenEarth land cover dataset show that DCAU-AL significantly outperforms existing AL methods, especially under severe class imbalance, delivering superior per-class IoU and improved annotation efficiency.

---

## 论文详细总结（自动生成）

### 针对无偏卫星图像分割的动态类别感知主动学习 (DCAU-AL) 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
卫星图像的语义分割（如土地覆盖制图）面临两大挑战：
*   **标注成本极高**：高分辨率卫星图像的像素级标注需要领域专家耗费大量时间。
*   **严重的类别不平衡**：自然场景中，植被、农田等多数类占据主导，而道路、裸地、水体等少数类样本稀少。
*   **传统主动学习（AL）的偏差**：现有的 AL 策略（如基于全局不确定性或多样性的采样）往往会优先选择多数类的样本，导致模型在少数类上的表现持续低迷，产生严重的系统偏差。

本文的研究动机是开发一种能够**实时感知类别性能差异**的主动学习框架，通过动态调整采样策略来缓解类别不平衡，实现更公平、更高效的分割。

#### 2. 论文提出的方法论：DCAU-AL
核心思想是引入**动态类别感知不确定性（Dynamic Class-Aware Uncertainty）**，将模型在各类别上的实时性能反馈引入采样决策中。

*   **性能差距评估**：在每个 AL 周期，计算验证集上每个类别的交并比（IoU）。定义性能差距 $Gap_c = 1 - IoU_c$，差距越大表示模型在该类表现越差。
*   **动态权重分配**：利用幂律公式计算类别权重 $w_c$，通过可调参数 $\alpha$（实验设定为 0.5）非线性地放大表现欠佳类别的权重，避免权重分配过于极端导致训练不稳定。
*   **动态像素级不确定性**：将传统的预测熵（Entropy）与类别权重结合。如果一个像素被预测为表现较差的类别，其不确定性得分会被加权放大。
*   **自适应阈值机制**：引入 $\theta(t) = \mu(t) + \gamma \cdot \sigma(t)$ 过滤噪声样本。通过缩放因子 $\gamma$ 平衡“探索”（采样新数据）与“利用”（聚焦困难类别），确保选出的样本既具有信息量又相对可靠。
*   **人机回环（HITL）流程**：模型预测 -> DCAU 模块评分 -> 选择 Top-K 样本 -> 专家标注 -> 更新训练集并重训。

#### 3. 实验设计
*   **数据集**：使用 **OpenEarth Land Cover** 数据集，包含 4300 张 1024×1024 的高分辨率卫星图像，涵盖 9 个语义类别（背景、裸地、道路、建筑、水体等）。
*   **Benchmark（基准）**：
    *   **全监督模型**：使用 100% 标注数据训练，作为性能上限。
    *   **Naive AL**：朴素的主动学习（基于标准熵采样）。
*   **对比方法**：
    *   **Entropy-based Uncertainty Sampling**（基于熵的不确定性采样）。
    *   **Coreset**（基于多样性的核心集方法）。

#### 4. 资源与算力
*   **硬件配置**：使用单张 **NVIDIA GeForce RTX 4070 Ti GPU** (32 GB VRAM)。
*   **训练时长**：完整的 20 个主动学习周期（每个周期包含模型重训）大约需要 **30 到 36 小时**。
*   **软件框架**：基于 PyTorch 实现，使用 U-Net 作为基础分割网络。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 20 轮迭代采样，每轮新增 50 个样本。
*   **消融实验**：针对**标注预算**（每轮 50 vs 100 个样本）和**学习率**（1e-3 vs 1e-4）进行了交叉验证。
*   **充分性评价**：实验设计较为客观。通过展示 mIoU 随采样周期的增长曲线，清晰地对比了不同方法在相同标注预算下的性能。同时，提供了详细的**每类 IoU（Per-class IoU）**数据，直接证明了该方法在处理少数类（如 Bareland, Road）上的优越性。

#### 6. 主要结论与发现
*   **高效性**：DCAU-AL 仅使用 **40% 的标注数据**，其 mIoU（0.664）就已非常接近全监督模型（0.670）的水平。
*   **纠偏能力**：相比 Naive AL，DCAU-AL 在少数类上的提升显著。例如，裸地（Bareland）的 IoU 从 0.201 提升至 0.248，道路（Road）从 0.547 提升至 0.582。
*   **收敛速度**：在初始迭代阶段，DCAU-AL 的性能提升斜率明显高于对比方法，说明其采样策略在数据匮乏时更具针对性。

#### 7. 优点
*   **动态反馈闭环**：不同于静态权重方法，DCAU 能根据模型当前的“短板”实时调整采样重心。
*   **鲁棒的权重公式**：采用幂律公式而非指数公式，有效平衡了对困难类的关注与训练稳定性。
*   **自适应阈值**：通过统计学手段（均值+标准差）过滤潜在的离群值或噪声样本，提高了标注效率。

#### 8. 不足与局限
*   **数据集单一**：实验仅在 OpenEarth 一个数据集上完成，缺乏在其他不同传感器或地理区域数据集上的泛化性验证。
*   **超参数依赖**：参数 $\alpha$ 和 $\gamma$ 对结果有影响，虽然文中给出了经验值，但在不同任务中可能需要重新调优。
*   **计算开销**：由于每个周期都需要在验证集上评估所有类别的 IoU 并重新计算 pool 中所有样本的得分，在大规模未标注池中可能会产生一定的计算延迟。
*   **模型架构单一**：仅使用了基础的 U-Net，未探讨该策略在更先进的 Transformer-based 分割模型上的表现。

（完）
