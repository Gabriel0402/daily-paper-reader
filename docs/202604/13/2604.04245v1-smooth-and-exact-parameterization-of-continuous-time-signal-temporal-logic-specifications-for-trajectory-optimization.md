---
title: Smooth and Exact Parameterization of Continuous-time Signal Temporal Logic Specifications for Trajectory Optimization
title_zh: 用于轨迹优化的连续时间信号时序逻辑规范的平滑且精确参数化
authors: "Samet Uzun, Behçet Açıkmeşe"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04245v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于轨迹优化的连续时间信号时序逻辑 (STL) 的平滑参数化
tldr: 本文针对非凸轨迹优化提出了一种平滑且精确的连续时间信号时序逻辑（CT-STL）参数化方法。该方法通过在稠密时间内评估逻辑规范，解决了传统离散采样导致的安全性缺失和保守性问题，并克服了梯度掩蔽现象。研究表明，该方法在保证连续时间安全性的同时，允许使用更粗略的时间网格，从而显著降低了轨迹生成的计算维度和成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04245v1/fig-001.webp\", \"caption\": \"Fig. 1. Optimized point-mass quadrotor trajectory together with the charging station. The vehicle reaches the charging region before proceeding to the terminal state, consistent with the continuous-time until specification.\", \"page\": 7, \"index\": 1, \"width\": 473, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2604.04245v1/fig-002.webp\", \"caption\": \"TABLE II STANDARD QUANTITATIVE ROBUSTNESS SEMANTICS FOR CONTINUOUS-TIME STL.\", \"page\": 3, \"index\": 2, \"width\": 504, \"height\": 281}]"
motivation: 现有的轨迹优化方法通常仅在离散节点处强制执行逻辑规范，难以保证节点间的连续安全性，且面临梯度消失和局部最优的挑战。
method: 提出一种平滑的CT-STL参数化方案，利用稠密时间评估来处理“始终”、“最终”及“直到”等复杂时序谓词，并优化了梯度景观。
result: 在敏捷无人机飞行实验中，该方法在大幅减少时间网格节点的情况下，依然实现了对复杂时序逻辑规范的精确遵循和高效求解。
conclusion: 该框架成功实现了计算效率与逻辑保真度的平衡，为复杂任务需求下的连续时间轨迹优化提供了稳健的数值解决方案。
---

## 摘要
本文提出了一种用于非凸轨迹优化的连续时间信号时序逻辑（CT-STL）规范的平滑参数化方法，该方法在底层数值积分方案的精度范围内是可靠且完备的。CT-STL 为编码丰富的时序和逻辑任务需求提供了一个自然的框架，但现有的轨迹优化方案通常仅在离散采样节点处强制执行此类规范。相比之下，所提出的方法在密集时间内评估规范，从而保证了“总是（always）”谓词的连续时间满足，这对于避障等路径约束至关重要；同时，通过允许在规定间隔内的任何时间满足，消除了“最终（eventually）”谓词由节点引起的保守性。这两种密集时间构造也作为处理更通用 CT-STL 公式（包括复杂的“直到（until）”规范）的主要构建模块。此外，所提出的参数化解决了标准定量语义中固有的局部性和梯度掩蔽问题，为基于梯度的求解器提供了更有利的优化景观。尽管密集时间评估在离散化过程中引入了额外的函数评估，但它也允许使用显著变粗的时间网格，而不会牺牲安全性或逻辑保真度。这反过来降低了所得非凸规划的维度，而维度通常是轨迹生成成本的主导因素。通过一个受复杂连续时间“直到”规范约束的敏捷四旋翼飞行问题，证明了所提出框架的数值有效性和语义精确性。代码实现见 https://github.com/UW-ACL/TrajOpt_CT-STL。

## Abstract
This paper presents a smooth parameterization of continuous-time Signal Temporal Logic (CT-STL) specifications for nonconvex trajectory optimization that is sound and complete up to the accuracy of the underlying numerical integration scheme. CT-STL provides a natural framework for encoding rich temporal and logical task requirements, but existing trajectory-optimization formulations typically enforce such specifications only at discrete sampling nodes. In contrast, the proposed method evaluates specifications in dense time, thereby guaranteeing continuous-time satisfaction of always predicates, which is critical for path constraints such as obstacle avoidance, while eliminating the node-induced conservatism of eventually predicates by allowing satisfaction at any time within the prescribed interval. These two dense-time constructions also serve as the main building blocks for handling more general CT-STL formulas, including complex until specifications. Furthermore, the proposed parameterization resolves the locality and gradient-masking issues inherent in standard quantitative semantics, yielding a more favorable landscape for gradient-based solvers. Although dense-time evaluation introduces additional function evaluations during discretization, it also permits substantially coarser temporal grids without sacrificing safety or logical fidelity. This, in turn, reduces the dimension of the resulting nonconvex program, which is often the dominant factor in trajectory-generation cost. The numerical effectiveness and semantic exactness of the proposed framework are demonstrated on an agile quadrotor flight problem subject to a complex continuous-time until specification. The implementation is available at https:// github.com/UW-ACL/TrajOpt_CT-STL

---

## 论文详细总结（自动生成）

以下是对论文《Smooth and Exact Parameterization of Continuous-time Signal Temporal Logic Specifications for Trajectory Optimization》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：在自主系统（如无人机、火箭）的轨迹优化中，信号时序逻辑（STL）常用于编码复杂的任务需求。然而，现有的方法大多依赖**离散时间评估（DT-STL）**，这导致两个主要问题：
    1.  **安全性缺失**：仅在离散采样点强制执行约束，无法保证采样点之间的连续时间安全性（如避障失效）。
    2.  **数值挑战**：传统的鲁棒性语义（基于 min/max）是非平滑的，且存在“局部性”和“梯度掩蔽”问题，导致基于梯度的优化算法难以收敛。
*   **核心目标**：提出一种**平滑且精确的连续时间 STL（CT-STL）参数化方法**，在保证连续时间满足性的同时，提供有利于梯度求解器的优化景观。

### 2. 方法论
*   **核心思想**：将基于广义均值的平滑鲁棒性（GMSR）从离散时间扩展到**稠密时间（Dense-time）**。该方法在动力学数值积分的细分网格上评估逻辑规范，而非仅在优化的决策节点上评估。
*   **关键技术细节**：
    *   **控制参数化**：使用一阶保持（FOH）参数化控制输入，确保节点间的控制信号连续。
    *   **多重射击法（Multiple Shooting）**：将轨迹分为 $K$个优化节点，但在每个节点区间内使用 $N$个积分步长生成稠密子节点。
    *   **GMSR 算子**：利用专门设计的广义均值函数（结合几何均值和算术均值）来近似逻辑算子（与、或、非）。该算子在 $c > 0$ 时是 $C^1$ 平滑的，且在符号上与原始逻辑完全一致（即精确性）。
    *   **时序算子参数化**：
        *   **Always ($G$)**：在稠密网格上应用平滑“与”运算，强化最弱的满足点。
        *   **Eventually ($F$)**：在稠密网格上应用平滑“或”运算，允许在区间内任意时刻满足。
        *   **Until ($U$)**：通过递归组合上述算子，处理复杂的时序依赖。
    *   **梯度分布机制**：不同于 min/max 只关注单一最差样本，GMSR 将梯度分布到所有相关的时间样本和子公式上，有效缓解了梯度消失和局部最优问题。

### 3. 实验设计
*   **实验场景**：敏捷四旋翼无人机引导问题。
*   **任务需求**：
    *   **Until 规范**：无人机在到达充电站（目标区域）之前，其飞行速度必须始终保持在安全阈值以下。
    *   **路径约束**：包括倾斜角限制、推力大小限制和全局速度上限（均通过 Always 算子实现）。
*   **Benchmark 与对比**：论文主要通过该复杂案例展示方法的**语义精确性**（即在稠密网格上完全满足逻辑）和**数值效率**。实验对比了优化节点（Nodes）与实际连续轨迹之间的关系，证明了即使在粗略的优化网格下也能保证连续安全性。

### 4. 资源与算力
*   **硬件环境**：2023 MacBook Pro，搭载 Apple M2 处理器。
*   **软件框架**：使用 Python 实现，利用 **JAX** 进行自动微分和硬件加速，**CVXPY** 进行建模，**QOCO** 作为底层圆锥凸优化求解器。
*   **计算耗时**：
    *   凸子问题求解时间：约 **15.35 ms**。
    *   动力学离散化与 STL 评估时间：约 **0.97 ms**。
    *   总迭代次数：10 次 Prox-convex 迭代。
*   **算力评价**：该方法表现出极高的计算效率，适合嵌入式实时应用。

### 5. 实验数量与充分性
*   **实验规模**：论文重点展示了一个具有代表性的四旋翼复杂任务案例。
*   **充分性评价**：
    *   **逻辑覆盖**：实验涵盖了 $G$（总是）和 $U$（直到）这两个最具挑战性的算子，证明了框架的通用性。
    *   **数值验证**：通过可视化速度曲线和充电站边际距离（Margin），清晰地验证了逻辑算子的“见证时间（Witness time）”和连续满足性。
    *   **局限性**：缺乏大规模的随机初始化统计实验，也未在不同动力学模型（如固定翼、行走机器人）上进行广泛迁移测试。

### 6. 主要结论与发现
*   **连续时间保证**：该方法在数值积分精度范围内实现了 CT-STL 的可靠且完备的参数化，消除了离散采样导致的安全性盲区。
*   **网格解耦**：由于在稠密时间评估逻辑，优化算法可以使用**更粗的临时网格**（较小的 $K$ 值），从而显著降低非凸优化问题的维度和计算成本。
*   **求解器友好**：GMSR 参数化提供的平滑梯度景观使得基于梯度的连续优化算法（如 SCvx）能够稳定且快速地收敛。

### 7. 优点
*   **语义精确性**：解决了平滑近似通常带来的近似误差问题，符号逻辑严格匹配。
*   **消除保守性**：相比于传统的缩减可行域方法，该方法允许在节点间任何位置满足“最终”算子，减少了不必要的性能损失。
*   **高效性**：通过 JAX 加速和高效的凸化策略，达到了毫秒级的求解速度。

### 8. 不足与局限
*   **实验覆盖面**：缺乏与 SOTA 方法（如基于混合整数规划 MILP 或控制屏障函数 CBF 的方法）在相同基准下的量化对比。
*   **参数调节**：GMSR 中的平滑参数 $c$ 需要根据具体问题调节，虽然论文给出了建议值，但缺乏自动调参机制。
*   **应用限制**：目前主要针对固定终点时间的问题，对于自由终点时间（Free-final-time）问题的扩展仍处于未来工作阶段。

（完）
