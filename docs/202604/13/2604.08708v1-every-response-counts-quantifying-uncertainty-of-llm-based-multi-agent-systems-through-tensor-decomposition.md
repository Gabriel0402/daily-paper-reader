---
title: "Every Response Counts: Quantifying Uncertainty of LLM-based Multi-Agent Systems through Tensor Decomposition"
title_zh: 响应皆关键：通过张量分解量化基于大语言模型的多智能体系统的不确定性
authors: "Tiejin Chen, Huaiyuan Yao, Jia Chen, Evangelos E. Papalexakis, Hua Wei"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08708v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 基于大语言模型的多智能体系统不确定性量化
tldr: 针对大语言模型多智能体系统（MAS）在复杂交互中存在的可靠性评估难题，本文提出了MATU框架。该框架通过张量分解技术，将多轮推理轨迹转化为高阶张量，从而量化多步推理、通信路径及拓扑结构带来的不确定性。MATU不仅能有效识别不同来源的不确定性，还在多种任务和通信拓扑中展现了优于传统方法的鲁棒性和泛化能力，为提升多智能体系统的可靠性提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08708v1/fig-001.webp\", \"caption\": \"Table 2: Comparison of our methods with different baselines on various datasets and LLMs on AutoGen (Wu et al., 2024), highlighted with best performance\", \"page\": 6, \"index\": 1, \"width\": 466, \"height\": 502}, {\"url\": \"assets/figures/arxiv/2604.08708v1/fig-002.webp\", \"caption\": \"Table 5: Case Study on Mathematical Reasoning. Despite diverse communication patterns, all agents consistently reach the correct solution ( √ 10). Baselines like SAUP fail due to sensitivity to trajectory length (step count), and Eigv-Whole struggles with long-context entailment. MATU effectively disentangles surface-level variations from semantic stability, correctly assigning low uncertainty.\", \"page\": 15, \"index\": 2, \"width\": 948, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2604.08708v1/fig-003.webp\", \"caption\": \"Table 3: Comparison of our methods with different baselines on various datasets and LLMs on AnyMac (Wang et al., 2025), highlighted with best performance.\", \"page\": 7, \"index\": 3, \"width\": 461, \"height\": 494}, {\"url\": \"assets/figures/arxiv/2604.08708v1/fig-004.webp\", \"caption\": \"Figure 2: Results for ablation study and sensitivity study. The results show that our design for MATU and our choices of the hyperparameter are well-suited.\", \"page\": 8, \"index\": 4, \"width\": 934, \"height\": 224}, {\"url\": \"assets/figures/arxiv/2604.08708v1/fig-005.webp\", \"caption\": \"Figure 1: The overall pipeline of MATU. As shown in the figure, MATU could be applied to multi-agent systems with different communication topologies. We first collect trajectories for a fixed system and task, and then obtain embedding matrices for each trajectory. Then, we form a ragged tensor by stacking all embedding matrices and obtain the reconstructed tensor by conducting CP-2 decomposition. Finally, we use the reconstruction losses from reconstructed tensors with different ranks as the final uncertainty.\", \"page\": 4, \"index\": 5, \"width\": 927, \"height\": 449}]"
motivation: 现有的不确定性量化方法主要针对单轮输出，难以应对多智能体系统中复杂的通信动态、角色依赖及级联不确定性问题。
method: 提出MATU框架，将多次执行的推理轨迹表示为嵌入矩阵并构建高阶张量，利用张量分解技术解耦并量化不同来源的不确定性。
result: 实验结果表明，MATU在多种任务和通信拓扑下均能有效且鲁棒地估计多智能体系统的整体不确定性。
conclusion: 通过张量分解量化推理轨迹，MATU为评估和提升复杂多智能体系统的可靠性提供了一种通用且高效的量化手段。
---

## 摘要
尽管基于大语言模型（LLM）的多智能体系统（MAS）在处理复杂任务时表现优于单智能体系统，但其复杂的交互作用由于通信动态和角色依赖性，带来了严峻的可靠性挑战。现有的不确定性量化（Uncertainty Quantification）方法通常针对单轮输出设计，难以应对 MAS 特有的复杂性。具体而言，这些方法面临三个主要挑战：多步推理中的级联不确定性、智能体间通信路径的可变性以及通信拓扑的多样性。为了填补这一空白，我们提出了 MATU，这是一个通过张量分解（tensor decomposition）量化不确定性的新颖框架。MATU 不再局限于分析最终的文本输出，而是将整个推理轨迹表示为嵌入矩阵，并将多次执行运行组织成高阶张量。通过应用张量分解，我们解耦并量化了不同的不确定性来源，提供了一种可推广至不同智能体结构的全面可靠性度量。实验结果表明，MATU 能够有效地估计各种任务和通信拓扑下的整体且稳健的不确定性。

## Abstract
While Large Language Model-based Multi-Agent Systems (MAS) consistently outperform single-agent systems on complex tasks, their intricate interactions introduce critical reliability challenges arising from communication dynamics and role dependencies. Existing Uncertainty Quantification methods, typically designed for single-turn outputs, fail to address the unique complexities of the MAS. Specifically, these methods struggle with three distinct challenges: the cascading uncertainty in multi-step reasoning, the variability of inter-agent communication paths, and the diversity of communication topologies. To bridge this gap, we introduce MATU, a novel framework that quantifies uncertainty through tensor decomposition. MATU moves beyond analyzing final text outputs by representing entire reasoning trajectories as embedding matrices and organizing multiple execution runs into a higher-order tensor. By applying tensor decomposition, we disentangle and quantify distinct sources of uncertainty, offering a comprehensive reliability measure that is generalizable across different agent structures. We provide comprehensive experiments to show that MATU effectively estimates holistic and robust uncertainty across diverse tasks and communication topologies.

---

## 论文详细总结（自动生成）

### 论文总结：MATU —— 基于张量分解的多智能体系统不确定性量化框架

#### 1. 核心问题与整体含义（研究动机和背景）
随着基于大语言模型（LLM）的多智能体系统（MAS）在复杂任务中展现出超越单智能体的性能，其**可靠性问题**也日益凸显。MAS 的不确定性不仅源于单个智能体的错误，还源于复杂的通信动态、角色依赖和共识构建过程。
现有的不确定性量化（UQ）方法主要针对单轮输出设计，难以应对 MAS 的三大挑战：
*   **多步推理的级联效应**：早期的微小错误会随推理步骤放大。
*   **通信路径的多样性**：同一问题在不同运行中可能产生不同的交互序列。
*   **通信拓扑的多样性**：系统可能采用星型、链型或动态拓扑，现有方法缺乏通用性。

#### 2. 方法论：核心思想与技术细节
论文提出了 **MATU (Multi-Agent Tensor Uncertainty)** 框架，其核心思想是将多智能体交互的推理轨迹视为一个高阶张量，通过张量分解提取“共识”结构，并将偏离共识的程度（重构误差）定义为不确定性。

*   **多步推理嵌入**：将每个智能体在每一步生成的文本或工具调用结果转化为高维嵌入向量（Embedding），形成嵌入矩阵 $E \in \mathbb{R}^{T \times d}$，从而处理变长且异构的推理步骤。
*   **构建不规则张量（Ragged Tensor）**：将多次运行（Runs）、多个智能体（Agents）和变长推理步骤（Steps）组织成一个三维不规则张量。
*   **张量分解（PARAFAC2/CP-2）**：针对步骤长度不一致的问题，采用 CP-2 分解。该技术能对齐不同长度的潜在因子，捕捉跨运行、跨智能体的共享模式。
*   **不确定性计算**：通过不同秩（Rank）下的重构损失（Reconstruction Loss）之和来衡量不确定性。重构损失越高，说明推理轨迹越难以被低秩结构解释，即系统内部的一致性越差，不确定性越高。

#### 3. 实验设计
*   **数据集**：MATH（数学推理）、MoreHopQA（多跳问答）、MMLU（通用知识）、HumanEval（代码生成/工具集成）。
*   **MAS 框架/拓扑**：
    *   **静态设计**：Camel（轮询对话）、AutoGen（星型拓扑）。
    *   **动态设计**：AnyMac（动态选择下一个智能体）。
*   **对比基准（Benchmarks）**：
    *   **P(true)**：让模型自我评估正确性。
    *   **Eigv(Agre)**：基于蕴含图特征值的语义一致性方法（分为 Answer 和 Whole 两个版本）。
    *   **SAUP**：针对单智能体轨迹设计的状态感知不确定性传播方法（分为 Single 和 Multiple 版本）。
*   **评估指标**：AUROC（曲线下面积）和 AUARC（准确率拒绝曲线面积），用于衡量不确定性得分预测答案错误的能力。

#### 4. 资源与算力
*   **硬件**：实验在一台 **Nvidia A100-80GB GPU** 上运行，或通过 OpenAI API 调用。
*   **模型**：使用了 Qwen2.5-7B、Llama3.1-8B（开源）以及 GPT-4o（闭源）。
*   **嵌入模型**：默认使用 Qwen3-Task-Embedding-0.6B 以平衡速度与精度。
*   **训练/推理时长**：文中未明确给出总训练时长，但提到每项任务收集了 10 条轨迹，推理成本随采样次数线性增加。

#### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **覆盖面广**：涵盖了 4 种任务类型、3 种主流 MAS 框架、多种开源与闭源模型。
*   **消融与敏感性分析**：测试了不同嵌入模型（GPT vs Qwen）、不同嵌入维度（32 到 512）以及不同输入变体（如余弦相似度矩阵）的影响。
*   **应用验证**：除了基础的 UQ 评估，还验证了 MATU 在 **OOD（分布外）检测**和**下游任务（模型选择/路由）**中的有效性。
*   **公平性**：对比方法均经过了适配 MAS 环境的调整（如 SAUP-Multiple），确保了对比的基准线具有竞争力。

#### 6. 主要结论与发现
*   **性能领先**：MATU 在所有数据集和拓扑结构上均显著优于现有基准，尤其在处理动态拓扑和长推理轨迹时表现稳健。
*   **整体性优势**：实验证明，仅分析最终答案（Eigv-final）或独立分析步骤（SAUP）会丢失大量系统级交互信息，而 MATU 的张量表示能捕捉到这些隐藏的不稳定性。
*   **可解释性**：通过分析张量分解的潜在因子，MATU 能够定位特定的推理失败模式（例如在数学题中对求和目标的误解）。
*   **实用价值**：在模型选择任务中，利用 MATU 得分进行路由选择，能显著提升系统的最终准确率。

#### 7. 优点
*   **创新性**：首次系统性地定义并解决了 LLM 多智能体系统特有的不确定性量化问题。
*   **黑盒友好**：不需要访问模型的 Token 概率（Logits），仅需文本嵌入，适用于闭源 API。
*   **结构通用**：利用 CP-2 分解处理不规则张量，使其能够无缝适配任何复杂的智能体通信拓扑。
*   **鲁棒性**：对嵌入模型的规模不敏感，使用轻量级嵌入模型即可达到优异效果。

#### 8. 不足与局限
*   **计算成本**：由于需要多次采样（如 10 次运行）来构建张量，推理开销较大，难以满足极高实时性的需求。
*   **嵌入依赖**：系统的精度受限于嵌入模型的质量。在极度专业的领域（如法律、前沿生物），通用嵌入模型可能无法捕捉细微的语义偏差。
*   **采样敏感性**：虽然 10 次采样在实验中效果良好，但在极复杂任务中，可能需要更多采样才能稳定构建低秩空间。

（完）
