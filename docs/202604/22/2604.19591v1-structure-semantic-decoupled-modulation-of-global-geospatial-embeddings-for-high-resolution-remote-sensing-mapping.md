---
title: Structure-Semantic Decoupled Modulation of Global Geospatial Embeddings for High-Resolution Remote Sensing Mapping
title_zh: 结构-语义解耦调制的全球地理空间嵌入用于高分辨率遥感制图
authors: "Jienan Lyu, Miao Yang, Jinchen Cai, Yiwen Hu, Guanyi Lu, Junhao Qiu, Runmin Dong"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19591v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 使用全球地理空间基础模型进行高分辨率遥感制图
tldr: 针对高分辨率遥感制图中局部特征导致的泛化性差和预测破碎问题，本文提出结构-语义解耦调制（SSDM）框架。该框架将全球地理空间基础模型的嵌入解耦为结构先验和全局语义两个路径，分别通过宏观感受野约束和跨模态语义对齐来增强高分辨率视觉特征。实验证明，SSDM有效抑制了预测碎片化，提升了语义一致性，在多场景下达到了SOTA性能，为融合地理空间大模型提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19591v1/fig-001.webp\", \"caption\": \"Table 1: Comparison results on the GID24 (4 m) and GID24 (2 m) subsets. All models are trained exclusively on the 4 m training set, and the 2 m test set is utilized to evaluate generalization. The best results are highlighted in bold.\", \"page\": 6, \"index\": 1, \"width\": 1056, \"height\": 184}, {\"url\": \"assets/figures/arxiv/2604.19591v1/fig-002.webp\", \"caption\": \"Figure 3: Qualitative comparison of segmentation results. The colored bounding boxes highlight complex regions where existingmethods suffer from severe fragmentation or boundary inconsistency, whereas our SSDMmaintains superior structural coherence and precise delineation.\", \"page\": 6, \"index\": 2, \"width\": 1056, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2604.19591v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of the proposed SSDM framework. (a) Overall framework. The adapted global embeddings are functionally decoupled to guide the encoder via SMM and complement semantic information via SeMM. (b) Architecture of the SMM block. (c) Structural-aware self-attention mechanism.\", \"page\": 4, \"index\": 3, \"width\": 1056, \"height\": 669}, {\"url\": \"assets/figures/arxiv/2604.19591v1/fig-004.webp\", \"caption\": \"Table 2: Per-class performance on the GID24 (4 m) subset.\", \"page\": 7, \"index\": 4, \"width\": 440, \"height\": 548}]"
motivation: 高分辨率遥感制图过度依赖局部特征导致泛化性受限，且直接融合全球地理空间嵌入时存在严重的语义-空间鸿沟。
method: 提出SSDM框架，通过结构先验分支引入宏观感受野约束，并通过全局语义注入分支实现跨模态上下文对齐。
result: 实验证明该方法在多项任务中达到了当前最先进水平，显著提升了复杂地物的语义一致性和制图精度。
conclusion: SSDM为集成地理空间基础模型到高分辨率视觉任务提供了一种通用范式，有效解决了特征干扰和空间结构退化问题。
---

## 摘要
细粒度高分辨率遥感制图通常依赖于局部视觉特征，这限制了跨域泛化能力，并常导致大规模地物的预测结果破碎化。虽然全球地理空间基础模型提供了强大且泛化性强的表示，但由于严重的语义-空间鸿沟，直接将其高维隐式嵌入与高分辨率视觉特征融合，往往会引发特征干扰和空间结构退化。为了克服这些局限性，我们提出了一个结构-语义解耦调制（SSDM）框架，该框架将全球地理空间表示解耦为两个互补的跨模态注入路径。首先，结构先验调制分支将全球表示中的宏观感受野先验引入高分辨率编码器的自注意力模块中。通过利用整体结构约束指导局部特征提取，该分支有效地抑制了由高频细节噪声和过大的类内方差导致的预测破碎化。其次，全球语义注入分支将整体上下文与深层高分辨率特征空间显式对齐，并通过跨模态集成直接补充全球语义，从而显著增强了复杂地物的语义一致性和类别级区分度。大量实验表明，与现有的跨模态融合方法相比，我们的方法达到了最先进的性能。通过释放全球嵌入的潜力，SSDM 在不同场景下持续提高了高分辨率制图的准确性，为将地理空间基础模型集成到高分辨率视觉任务中提供了一种通用且有效的范式。

## Abstract
Fine-grained high-resolution remote sensing mapping typically relies on localized visual features, which restricts cross-domain generalizability and often leads to fragmented predictions of large-scale land covers. While global geospatial foundation models offer powerful, generalizable representations, directly fusing their high-dimensional implicit embeddings with high-resolution visual features frequently triggers feature interference and spatial structure degradation due to a severe semantic-spatial gap. To overcome these limitations, we propose a Structure-Semantic Decoupled Modulation (SSDM) framework, which decouples global geospatial representations into two complementary cross-modal injection pathways. First, the structural prior modulation branch introduces the macroscopic receptive field priors from global representations into the self-attention modules of the high-resolution encoder. By guiding local feature extraction with holistic structural constraints, it effectively suppresses prediction fragmentation caused by high-frequency detail noise and excessive intra-class variance. Second, the global semantic injection branch explicitly aligns holistic context with the deep high-resolution feature space and directly supplements global semantics via cross-modal integration, thereby significantly enhancing the semantic consistency and category-level discrimination of complex land covers. Extensive experiments demonstrate that our method achieves state-of-the-art performance compared to existing cross-modal fusion approaches. By unleashing the potential of global embeddings, SSDM consistently improves high-resolution mapping accuracy across diverse scenarios, providing a universal and effective paradigm for integrating geospatial foundation models into high-resolution vision tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Structure-Semantic Decoupled Modulation of Global Geospatial Embeddings for High-Resolution Remote Sensing Mapping》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：高分辨率（HR）遥感制图通常依赖局部视觉特征，容易受到高频噪声和类内方差干扰，导致大面积地物预测结果“破碎化”，且跨区域泛化能力差。
*   **研究背景**：全球地理空间基础模型（GFMs，如 AlphaEarth, TESSERA）能提供宏观的地理分布先验，但这些高维隐式嵌入与 HR 图像的细粒度空间特征之间存在巨大的“语义-空间鸿沟”。直接融合（如拼接或双编码器）往往会引起特征干扰，破坏空间结构。
*   **整体含义**：本文旨在探索如何将全球尺度的宏观地理先验有效地注入到局部高分辨率视觉任务中，以提升制图的结构一致性和语义准确性。

### 2. 方法论：结构-语义解耦调制（SSDM）
SSDM 的核心思想是将全球嵌入的功能进行**解耦**，分为结构引导和语义补充两个独立路径：
*   **结构调制模块 (SMM)**：
    *   **核心逻辑**：在编码器阶段注入宏观空间约束。
    *   **技术细节**：将全球嵌入投影为结构感知的亲和矩阵（Affinity Matrix），捕捉宏观结构关联。采用**分解注意力机制**，将结构先验作为“软加性偏置”引入自注意力图的行和列中，引导模型在提取局部特征时遵循全局结构约束，从而抑制碎片化。
*   **语义调制模块 (SeMM)**：
    *   **核心逻辑**：在解码器之前的深层特征空间注入类别语义。
    *   **技术细节**：通过轻量级卷积编码器提取全球嵌入的语义特征，在像素解码器输出的掩码特征（Mask Features）上进行残差融合。这种“晚期注入”避免了早期对空间结构的干扰，增强了类别判别力。
*   **整体流程**：输入 HR 图像和对应的全球嵌入 $\rightarrow$ SMM 引导编码器学习 $\rightarrow$ SeMM 补充深层语义 $\rightarrow$ Transformer 解码器输出最终分割结果。

### 3. 实验设计
*   **数据集**：
    *   **GID24 (4m)**：包含 24 类精细地物的 4 米分辨率数据集，用于训练和域内评估。
    *   **GID24 (2m)**：2 米分辨率的跨分辨率测试集，用于评估模型的泛化性。
*   **全球嵌入源**：使用了 AEF (10m)、TESSERA (10m) 和 ESD (30m) 三种不同的地理空间基础模型嵌入。
*   **对比方法 (Benchmarks)**：
    *   Baseline (仅 HR 图像，Mask2Former)。
    *   Dual-Encoder (双编码器融合)。
    *   SAM-based (基于 SAM 模型的边界先验融合)。
    *   DFormerv2 (针对 RGB-D 融合的 SOTA 方法适配版)。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA RTX 4090 GPU** 进行训练。
*   **软件框架**：基于 Detectron2 和 Mask2Former 实现。
*   **训练细节**：输入尺寸 512x512，使用 AdamW 优化器，学习率 $1 \times 10^{-4}$，权重衰减 0.05。文中未明确说明具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在 4m 和 2m 两个分辨率上进行了对比，涵盖了 OA、mIoU、mAcc 等多项指标。
    *   **消融实验**：分别验证了 SMM 和 SeMM 的有效性。
    *   **泛化性实验**：在不微调的情况下，将模型应用于三种不同的全球嵌入源（AEF, TESSERA, ESD），验证了方法的通用性。
    *   **效率分析**：对比了参数量、FLOPs、推理延迟和显存占用。
*   **评价**：实验设计较为充分且客观。特别是引入了**时间不一致性**（2015/16 年的图像配对 2017 年的嵌入）和**跨分辨率测试**，模拟了真实的遥感应用挑战，增强了结论的说服力。

### 6. 主要结论与发现
*   **性能领先**：SSDM 在 GID24 (4m) 上 mIoU 达到 50.01%，显著优于 Baseline (39.08%) 和 SAM-based 方法 (44.12%)。
*   **结构一致性**：SMM 模块对大面积连续地物（如灌溉地、自然草地）的提升最为明显，有效解决了预测图的“椒盐噪声”和破碎问题。
*   **解耦的必要性**：消融实验证明，结构和语义的解耦比单一路径融合效果更好，能有效缓解跨模态特征干扰。
*   **嵌入无关性**：SSDM 对不同来源、不同分辨率（10m 到 30m）的全球嵌入均表现出稳定的增益，具有很强的通用性。

### 7. 优点与亮点
*   **轻量化与高效**：相比于引入 SAM 等重型视觉基础模型，SSDM 通过轻量级适配器实现了更好的效果，推理延迟更低。
*   **创新的解耦视角**：首次提出将地理空间嵌入的功能划分为结构先验和语义补偿，为多模态遥感融合提供了新思路。
*   **鲁棒性强**：能够处理图像与嵌入之间的时间偏差和分辨率差异，体现了极强的实用价值。

### 8. 不足与局限
*   **细小物体挑战**：在灌木丛、雪地、广场等小尺度或边界极度模糊的类别上，提升幅度相对有限。
*   **过度平滑风险**：结构调制分支在增强全局一致性的同时，可能会对某些极细微的局部结构产生过度平滑（Oversmoothing）的影响。
*   **任务覆盖**：目前仅在语义分割任务上验证，尚未扩展到实例分割或变化检测等更复杂的遥感任务。

（完）
