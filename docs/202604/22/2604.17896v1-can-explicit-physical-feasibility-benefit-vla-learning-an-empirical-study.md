---
title: Can Explicit Physical Feasibility Benefit VLA Learning? An Empirical Study
title_zh: 显式物理可行性是否能使 VLA 学习受益？一项实证研究
authors: "Yubai Wei, Chen Wu, Hashem Haghbayan"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17896v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: VLA学习中的显式物理可行性监督
tldr: 本研究探讨了显式物理可行性监督对视觉-语言-动作（VLA）模型学习的影响。针对现有VLA模型仅通过模仿学习隐式推断物理约束的问题，本文提出了一种基于几何的物理可行性目标函数，并将其应用于扩散式VLA策略。通过避障操作任务的系统评估，证明了该方法能显著提升物理可靠性、任务成功率及低数据环境下的学习效率，为构建更可靠的机器人策略提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17896v1/fig-001.webp\", \"caption\": \"Fig. 1. Illustration on learning VLA with explicit physical feasibility supervision. The policy is trained to match demonstrated action chunks via LMSE. We additionally map predicted actions through forward kinematics and compute signed distances to the obstacle, producing a feasibility loss Lgeo. The combined objective is Ltotal = LMSE+λLgeo. Obstacle geometry and kinematic computations are used only during training; at inference the policy acts solely on RGB observations and language.\", \"page\": 1, \"index\": 1, \"width\": 512, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2604.17896v1/fig-002.webp\", \"caption\": \"TABLE III EFFECT OF TRAINING DATA SIZE UNDER LARGE PERTURBATIONS (δ=0.10, λ=1.0). THE BENEFIT OF FEASIBILITY SUPERVISION IS LARGEST IN THE LOW-DATA REGIME AND DIMINISHES AS DATA INCREASES.\", \"page\": 7, \"index\": 2, \"width\": 529, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2604.17896v1/fig-003.webp\", \"caption\": \"TABLE IV ABLATION OF SUPERVISION STRENGTH (δ, λ) UNDER LARGE PERTURBATIONS (40 TRAINING EPISODES). MODERATE SUPERVISION ACHIEVES THE BEST BALANCE IN SSR, WHILE OVERLY STRONG SUPERVISION INTRODUCES A SAFETY–ACCURACY TRADE-OFF.\", \"page\": 7, \"index\": 3, \"width\": 529, \"height\": 271}]"
motivation: 现有的视觉-语言-动作（VLA）模型主要依赖模仿学习，缺乏对避障和运动学可行性等物理约束的显式监督。
method: 提出一种基于几何的物理可行性目标函数，并将其集成到基于扩散模型的VLA策略训练过程中。
result: 实验表明，显式可行性监督显著提升了机器人在避障任务中的物理可靠性、整体性能以及在低数据量下的学习效率。
conclusion: 显式物理可行性信号能有效补充基于模仿的VLA学习，是开发更可靠机器人策略的潜力方向。
---

## 摘要
视觉-语言-动作（VLA）模型将多模态输入直接映射到机器人动作，通常通过大规模模仿学习进行训练。虽然这种范式表现出了强大的性能，但现有的 VLA 训练过程并未显式监督诸如避障或运动学可行性等硬性物理约束。因此，物理可行行为背后的几何结构只能从演示中隐式推断。在本文中，我们研究了引入显式可行性监督是否能为 VLA 策略提供有效的结构化引导。我们制定了一个简单的基于几何的可行性目标，并将其集成到基于扩散的 VLA 策略的训练阶段。为了系统地评估这一想法，我们使用避障操纵作为几何相关物理可行性的受控探针。实证结果表明，通过可行性监督增强 VLA 训练不仅提高了物理可靠性和整体任务性能，还提升了低数据量情况下的学习效率。这些发现表明，显式可行性信号可以有效补充基于模仿的 VLA 学习，突显了其在开发更可靠 VLA 策略方面的潜力。

## Abstract
Vision-Language-Action (VLA) models map multimodal inputs directly to robot actions and are typically trained through large-scale imitation learning. While this paradigm has shown strong performance, prevailing VLA training procedures do not explicitly supervise hard physical constraints such as obstacle avoidance or kinematic feasibility. As a result, the geometric structure underlying physically feasible behavior must be inferred only implicitly from demonstrations. In this paper, we study whether introducing explicit feasibility supervision can provide effective structured guidance for VLA policies. We formulate a simple geometry-grounded feasibility objective and integrate it into the training stage of a diffusion-based VLA policy. To evaluate this idea systematically, we use obstacle-aware manipulation as a controlled probe of geometry-dependent physical feasibility. Empirical results show that augmenting VLA training with feasibility supervision improves both physical reliability and overall task performance, while also enhancing learning efficiency in the low-data regime. These findings indicate that explicit feasibility signals can effectively complement imitation-based VLA learning, highlighting their potential for developing more reliable VLA policies.

---

## 论文详细总结（自动生成）

这是一份关于论文《Can Explicit Physical Feasibility Benefit VLA Learning? An Empirical Study》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型主要依赖大规模模仿学习，将多模态输入直接映射为动作。然而，这种范式缺乏对物理约束（如避障、运动学可行性）的显式监督，模型必须从演示中隐式推断几何结构。
*   **核心问题**：在 VLA 训练中引入**显式的物理可行性监督信号**，是否能提供有效的结构化引导，从而提升模型的可靠性和学习效率？
*   **背景**：传统机器人学通过配置空间推理和碰撞检测来确保可行性，而端到端 VLA 往往忽略了这些硬约束。

### 2. 方法论：核心思想与技术细节
论文提出在基于扩散模型（Diffusion-based）的 VLA 训练阶段集成一个**几何感知可行性目标函数**：
*   **核心思想**：在训练时利用机器人运动学模型和环境几何信息（障碍物位置），对预测出的“不可行”动作序列进行惩罚，引导模型学习符合物理规律的特征。
*   **关键技术流程**：
    1.  **正向运动学映射（FK）**：将 VLA 预测的关节空间动作序列映射到工作空间中机器人各连杆的位姿。
    2.  **有向距离计算（SDF）**：选取机器人关键连杆的代表点，计算其与障碍物（建模为有向包围盒 OBB）之间的有向距离。
    3.  **几何可行性损失 ($L_{geo}$)**：采用平方铰链损失（Squared Hinge Loss）。当机器人表面与障碍物的距离小于预设的安全裕度 $\delta$ 时，激活该损失。
    4.  **联合优化**：总损失函数为 $L_{total} = L_{MSE} + \lambda L_{geo}$。其中 $L_{MSE}$ 负责模仿专家演示，$\lambda$ 控制物理监督的强度。
*   **推理阶段**：该方法具有“训练时引导，推理时脱离”的特点。推理时模型仅需 RGB 图像和语言指令，无需障碍物几何信息或 FK 计算。

### 3. 实验设计
*   **实验场景**：在 NVIDIA Isaac Sim 仿真环境中设计了“近距离避障到达（Close-obstacle Reaching）”任务，使用 Franka 机械臂。
*   **数据集生成**：通过运动规划器（OMPL）生成 120 个包含障碍物的专家演示片段。
*   **Benchmark 与对比方法**：
    *   **基座模型**：RDT-1B（12 亿参数的扩散基础模型）。
    *   **对比方法**：标准的 MSE 模仿学习（Baseline） vs. 增加可行性监督的 MSE+Feasibility（本文方法）。
*   **评估指标**：
    *   **安全成功率 (SSR)**：同时满足“最小间距大于阈值”和“末端误差小于阈值”。
    *   **安全度**：最小有向距离 $d_{min}$。
    *   **准确度**：末端到目标的距离 $dtgt$。
*   **扰动测试**：设置了“小扰动”（分布内）和“大扰动”（分布外障碍物重置）来测试模型的泛化能力。

### 4. 资源与算力
*   **硬件**：使用了 **2 张 NVIDIA A100 GPU**。
*   **训练时长**：
    *   40 个 episode 训练约 8 小时。
    *   80 个 episode 训练约 15 小时。
    *   120 个 episode 训练约 23 小时。
*   **模型规模**：对 1.2B 参数的 RDT-1B 模型进行了全参数微调。

### 5. 实验数量与充分性
*   **实验组数**：
    *   **数据效率实验**：对比了 40、80、120 个不同训练样本量下的表现。
    *   **消融实验**：针对安全裕度 $\delta$ 和损失权重 $\lambda$ 进行了多组组合测试（共 7 组配置）。
    *   **泛化实验**：在两种不同强度的障碍物扰动下进行了多次 Rollout。
*   **充分性评价**：实验设计较为客观，通过 Bootstrap 方法估计了 95% 置信区间，增强了结果的可信度。虽然场景集中在避障任务，但作为“实证研究（Empirical Study）”，其对核心变量（数据量、监督强度）的控制变量分析是充分的。

### 6. 主要结论与发现
*   **性能提升**：显式可行性监督显著提高了 SSR，且同时改善了安全性和任务精度，而非通过牺牲精度换取安全。
*   **低数据效率**：在仅有 40 个样本的低数据量下，本文方法提升最明显，甚至超过了使用 120 个样本的基座模型，证明了物理先验能有效弥补数据不足。
*   **监督强度平衡**：存在最优的监督强度。过强的监督（过大的 $\delta$ 或 $\lambda$）会导致模型过于保守，反而降低任务成功率。

### 7. 优点（亮点）
*   **即插即用**：该方法不改变 VLA 的模型架构，仅在训练损失函数上做文章，易于集成到现有的扩散式 VLA 框架中。
*   **推理高效**：推理时不需要额外的感知模块（如不需要实时获取障碍物的精确几何参数），保持了端到端模型的简洁性。
*   **结构化引导**：成功证明了将机器人学的传统几何约束转化为深度学习的辅助损失是提升具身智能可靠性的有效路径。

### 8. 不足与局限
*   **任务单一性**：实验仅限于单臂避障任务，尚未在双臂协作、复杂接触力学或长程规划任务中验证。
*   **几何表示简化**：将障碍物简化为 OBB（包围盒），对于形状极度复杂的物体，简单的 SDF 计算可能不够精确。
*   **依赖真值**：训练阶段需要环境的 Ground Truth 几何信息（如障碍物位置），这在纯真实世界数据采集（非仿真）中可能难以获取。
*   **偏差风险**：如果专家演示本身与物理约束存在冲突，强行引入可行性损失可能会导致训练不稳定。

（完）
