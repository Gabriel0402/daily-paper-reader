---
title: "SYMBOLIZER: Symbolic Model-free Task Planning with VLMs"
title_zh: SYMBOLIZER：基于 VLM 的无符号模型任务规划
authors: "Sami Azirar, Zlatan Ajanovic, Hermann Blum"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17830v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 将VLM与经典符号规划结合用于长程推理
tldr: 本研究针对传统任务与运动规划（TAMP）中符号模型构建困难的问题，提出了SYMBOLIZER框架。该方法利用视觉语言模型（VLM）的零样本理解能力，将抽象谓词从图像中实例化为符号状态，并结合领域无关的启发式搜索（如目标计数和基于宽度的启发式）进行长程推理，无需预定义的动作模型。实验证明，该方法在ProDG和ViPlan基准上取得了SOTA性能，显著提升了机器人在未知环境中的规划通用性与效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17830v1/fig-001.webp\", \"caption\": \"Fig. 1. SYMBOLIZER grounds visual observations into well-typed symbolic states using structured VLM outputs, enabling domain-independent planning without handcrafted action models.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-002.webp\", \"caption\": \"Fig. 6. Example observations and goals from the VIPLAN benchmark. BLOCKSWORLD uses photorealistic rendering, HOUSEHOLD provides goals as natural language.\", \"page\": 7, \"index\": 2, \"width\": 529, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-003.webp\", \"caption\": \"Fig. 2. Example of planning with a simulator. From an initial state, the planner expands successor states generated by the simulator, without requiring explicit knowledge of the underlying actions.\", \"page\": 3, \"index\": 3, \"width\": 526, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.17830v1/fig-004.webp\", \"caption\": \"Fig. 3. Flowchart of grounding from images or text to symbolic states.\", \"page\": 4, \"index\": 4, \"width\": 1076, \"height\": 381}]"
motivation: 传统任务规划依赖难以手工构建或学习的符号模型，而现有结合VLM的方法往往缺乏通用性且依赖特定任务的动作定义。
method: 提出一种无需动作模型的框架，利用VLM将关系谓词从图像映射为符号状态，并采用领域无关的启发式搜索进行规划。
result: 在ProDG和ViPlan基准测试中取得了最先进的结果，性能优于直接使用VLM进行规划的方法。
conclusion: 通过将VLM的视觉感知与领域无关的符号搜索相结合，可以有效解决跨领域的复杂长程规划问题。
---

## 摘要
传统的任务与运动规划（TAMP）系统依赖于用于运动规划的物理模型和用于任务规划的离散符号模型。尽管物理模型通常是可用的，但符号模型（由符号状态解释和动作模型组成）必须经过精心的人工设计或从标注数据中学习。这一过程既耗费资源，又将解决方案限制在特定领域，从而限制了可扩展性和适应性。另一方面，视觉语言模型（VLM）表现出理想的零样本视觉理解能力（归功于其在异构数据上的广泛训练），但其规划能力仍然有限。因此，将 VLM 与经典规划相结合，用于 TAMP 问题中的长时程推理，具有巨大的潜力。该方向的近期工作仍缺乏通用性，且依赖于手工设计的特定任务解决方案，例如预先描述所有可能的物体，或使用符号动作模型。我们提出了一个能够很好地泛化到未见问题实例的框架。该方法仅需要描述物体间关系的提升谓词（lifted predicates），并利用 VLM 从图像中对这些谓词进行接地（grounding），以获取符号状态。规划是通过使用目标计数（goal-count）和基于宽度（width-based）启发式的领域无关启发式搜索完成的，无需动作模型。在 VLM 接地的状态空间上进行的符号搜索优于直接基于 VLM 的规划，且与使用 VLM 衍生启发式的方法表现相当。这表明领域无关搜索可以有效解决具有大规模组合状态空间的跨领域问题。我们对该方法进行了广泛评估，并在 ProDG 和 ViPlan 基准测试中取得了最先进的结果。

## Abstract
Traditional Task and Motion Planning (TAMP) systems depend on physics models for motion planning and discrete symbolic models for task planning. Although physics model are often available, symbolic models (consisting of symbolic state interpretation and action models) must be meticulously handcrafted or learned from labeled data. This process is both resource-intensive and constrains the solution to the specific domain, limiting scalability and adaptability. On the other hand, Visual Language Models (VLMs) show desirable zero-shot visual understanding (due to their extensive training on heterogeneous data), but still achieve limited planning capabilities. Therefore, integrating VLMs with classical planning for long-horizon reasoning in TAMP problems offers high potential. Recent works in this direction still lack generality and depend on handcrafted, task-specific solutions, e.g. describing all possible objects in advance, or using symbolic action models. We propose a framework that generalizes well to unseen problem instances. The method requires only lifted predicates describing relations among objects and uses VLMs to ground them from images to obtain the symbolic state. Planning is performed with domain-independent heuristic search using goal-count and width-based heuristics, without need for action models. Symbolic search over VLM-grounded state-space outperforms direct VLM-based planning and performs on par with approaches that use a VLM-derived heuristic. This shows that domain-independent search can effectively solve problems across domains with large combinatorial state spaces. We extensively evaluate on extensively evaluate our method and achieve state-of-the-art results on the ProDG and ViPlan benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《SYMBOLIZER: Symbolic Model-free Task Planning with VLMs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了机器人**任务与运动规划（TAMP）**中的一个关键瓶颈：传统方法高度依赖手工设计的符号模型（如 PDDL 动作模型和状态谓词），这限制了系统在未知环境中的泛化能力。虽然视觉语言模型（VLM）具备强大的零样本感知能力，但其直接进行长程逻辑规划（Long-horizon Reasoning）时容易产生幻觉且缺乏严谨性。
**核心问题**：如何利用 VLM 的感知能力，在**无需预定义符号动作模型**的情况下，实现通用且鲁棒的开放世界任务规划？

### 2. 方法论：SYMBOLIZER 框架
SYMBOLIZER 的核心思想是将 VLM 作为“传感器模型”来提取符号状态，并利用物理模拟器作为“黑盒转移模型”进行经典启发式搜索。

*   **结构化接地（Structured Grounding）**：
    *   不直接让 VLM 输出自由文本，而是通过 Python 类定义（Object, Predicate）约束 VLM 输出。
    *   **两阶段提取**：首先从图像/文本中提取物体列表（Object Extraction），然后基于提取的物体和预定义的提升谓词（Lifted Predicates）生成关系状态（Predicate Grounding）。
    *   利用**上下文学习（In-context Learning）**，仅需少量示例即可让 VLM 理解如何将视觉场景转化为符号逻辑。
*   **无模型规划（Model-free Planning）**：
    *   **模拟器驱动**：不依赖 PDDL 中的 `Action: Precondition/Effect` 定义，而是直接在模拟器中执行动作并观察状态变化。
    *   **启发式搜索**：采用领域无关的搜索策略，以“目标计数（Goal-count）”为主启发式，以“宽度搜索（Width-based Search）”作为打破僵局的机制，探索状态空间。
*   **状态预测**：VLM 同样可用于预测动作后的继发状态，作为模拟器的补充或替代。

### 3. 实验设计
论文在多个仿真环境和真实图像数据集上进行了评估：
*   **数据集/场景**：
    *   **自定义域**：Blocksworld（积木世界）、Hanoi（汉诺塔）、Kitchen-Worlds（厨房场景）。
    *   **模拟器**：PDDLGym（2D 渲染）、PyBullet（3D 物理渲染）。
    *   **外部基准**：**ProDG**（真实图像接地评估）和 **ViPlan**（光照真实感渲染的长程规划评估）。
*   **对比方法**：
    *   **ViLaIn**：现有的基于 VLM 的 PDDL 生成框架。
    *   **Direct VLM Planning**：直接让 VLM 输出动作序列。
    *   **VLM-guided Search**：使用 VLM 作为启发式函数指导搜索。
    *   **DREAM-VL**：结合 VLM 接地与学习型世界模型的规划方法。

### 4. 资源与算力
*   **模型使用**：实验使用了 **Gemini 3.1 Pro**、**Gemini 3.1 Flash Lite** 和 **Mistral Small 2503**（开源模型代表）。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或训练时长。由于该方法主要依赖预训练 VLM 的推理和上下文学习，而非从头训练，其算力消耗主要集中在 VLM API 调用和模拟器运行上。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括物体提取 F1 分数、谓词提取 F1 分数、目标提取 F1 分数以及最终的规划成功率。
*   **消融与对比**：对比了不同参数规模的模型（Pro vs Flash），并针对不同难度的任务（Simple, Medium, Hard）进行了测试。
*   **充分性评价**：实验设计较为**充分且客观**。它不仅在合成数据上测试，还引入了真实图像（ProDG）和复杂的局部可观测场景（Household），并与当前最先进的（SOTA）基准进行了直接对比，证明了其在不同模拟器间的迁移能力。

### 6. 主要结论与发现
*   **搜索胜过直觉**：在 VLM 接地的状态空间上进行符号搜索，其表现远优于 VLM 直接生成规划。
*   **结构化约束是关键**：通过 Python 类约束 VLM 输出，可以有效消除幻觉和格式错误，使小型模型（如 Flash Lite）也能达到极高的接地精度。
*   **无需动作模型**：证明了仅靠“感知接地 + 模拟器转移”即可完成复杂的 TAMP 任务，无需昂贵的人工动作建模。
*   **SOTA 性能**：在 ViPlan 基准上，SYMBOLIZER 在所有难度级别上均刷新了纪录。

### 7. 优点
*   **通用性强**：不依赖特定领域的 Prompt 工程，仅需少量示例即可适配新环境。
*   **鲁棒性高**：解耦了感知与推理，感知的错误可以通过搜索过程中的状态验证得到一定程度的缓解。
*   **零样本/少样本**：利用 VLM 的先验知识，极大地降低了机器人任务部署的门槛。

### 8. 不足与局限
*   **依赖模拟器**：规划过程需要频繁调用模拟器作为转移模型，在缺乏高精度模拟器的现实场景中可能受限。
*   **静态谓词瓶颈**：在处理如汉诺塔中“盘子大小”等隐含的静态属性时，若上下文示例不足，VLM 仍可能出错。
*   **实时性挑战**：大规模搜索配合 VLM 推理可能会带来较高的计算延迟，难以直接应用于对实时性要求极高的动态环境。
*   **闭环反馈**：目前主要侧重于离线规划，对于执行过程中的动态重规划（Replanning）讨论较少。

（完）
