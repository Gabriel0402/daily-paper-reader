---
title: Targeted Exploration via Unified Entropy Control for Reinforcement Learning
title_zh: 强化学习中通过统一熵控制实现的针对性探索
authors: "Chen Wang, Lai Wei, Yanzhi Zhang, Chenyang Shao, Zedong Dan, Weiran Huang, Ge Lan, Yue Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14646v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 提升大语言模型和视觉语言模型推理能力的强化学习
tldr: 针对大语言模型强化学习中GRPO算法易出现熵塌陷、导致过早收敛和缺乏多样性的问题，本文提出统一熵控制强化学习（UEC-RL）框架。该框架通过对困难提示词进行针对性探索，并结合稳定器防止熵过度增长，在维持训练稳定性的同时扩展了搜索空间。实验证明，UEC-RL在LLM和VLM推理任务上显著优于基准算法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14646v2/fig-001.webp\", \"caption\": \"Table 1: Comparison of Pass@1 accuracy on text and multimodal reasoning benchmarks. UEC-RL consistently outperforms the RL baselines. For AIME24 and AIME25, each question is repeated 32 times.\", \"page\": 6, \"index\": 1, \"width\": 941, \"height\": 615}, {\"url\": \"assets/figures/arxiv/2604.14646v2/fig-002.webp\", \"caption\": \"Figure 1: Illustration of UEC-RL. UEC-RL balances exploration and stabilization, keeping entropy within an optimal operating range.\", \"page\": 2, \"index\": 2, \"width\": 950, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2604.14646v2/fig-003.webp\", \"caption\": \"Figure 4: Results on Geometry3K. UEC-RL achieves the best accuracy, exhibits more stable entropy and responselength dynamics, and also demonstrates excellent training efficiency with lower per-step cost than GRPO.\", \"page\": 7, \"index\": 3, \"width\": 943, \"height\": 232}, {\"url\": \"assets/figures/arxiv/2604.14646v2/fig-004.webp\", \"caption\": \"Figure 3: Pass@k performance on the AIME24 and AIME25 benchmarks. UEC-RL consistently improves the success rate across different values of k.\", \"page\": 7, \"index\": 4, \"width\": 714, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.14646v2/fig-005.webp\", \"caption\": \"Figure 2: Two prominent optimization issues of GRPO on Geometry3K: insufficient exploration (entropy collapse) and unstable training dynamics.\", \"page\": 3, \"index\": 5, \"width\": 867, \"height\": 216}, {\"url\": \"assets/figures/arxiv/2604.14646v2/fig-006.webp\", \"caption\": \"Figure 5: Ablation of parameter tuning: peak accuracy and average entropy under varying t′ and s′. Higher t′ boosts exploration (entropy↑), larger s′ aids stabilization (entropy↓), and best performance arises from the balance state of entropy.\", \"page\": 8, \"index\": 6, \"width\": 867, \"height\": 583}, {\"url\": \"assets/figures/arxiv/2604.14646v2/fig-007.webp\", \"caption\": \"Figure 6: Module ablation of UEC-RL. Removing either exploration or stabilizer consistently degrades performance, highlighting their complementary roles in improving accuracy.\", \"page\": 9, \"index\": 7, \"width\": 867, \"height\": 313}]"
motivation: 现有的强化学习算法在推理任务中常面临熵塌陷问题，导致模型过早收敛且探索不足。
method: 提出UEC-RL框架，通过针对性探索机制加强对困难任务的搜索，并利用稳定器控制熵的增长以确保训练稳定性。
result: "在Geometry3K等推理任务上，UEC-RL相比GRPO实现了37.9%的相对提升，并显著提高了Pass@1和Pass@k指标。"
conclusion: UEC-RL通过平衡探索与稳定性，为大规模模型在复杂推理任务中的强化学习提供了有效的扩展方案。
---

## 摘要
强化学习（RL）的最新进展提升了大语言模型（LLM）和视觉语言模型（VLM）的推理能力。然而，广泛使用的群体相对策略优化（GRPO）始终面临熵崩溃问题，导致策略过早收敛并失去多样性。现有的探索方法在探索过程中引入了额外的偏差或方差，难以维持优化稳定性。我们提出了强化学习统一熵控制（UEC-RL），这是一个为探索和稳定提供针对性机制的框架。UEC-RL 在困难提示词上激活更多探索，以搜索潜在且有价值的推理轨迹。与此同时，稳定器防止熵失控增长，从而在模型巩固可靠行为时保持训练稳定。这些组件共同在需要时扩展搜索空间，同时在整个训练过程中保持稳健的优化。在 LLM 和 VLM 推理任务上的实验表明，在 Pass@1 和 Pass@k 指标上，该方法均优于 RL 基准。在 Geometry3K 数据集上，UEC-RL 相比 GRPO 实现了 37.9% 的相对提升，表明它在不损害收敛性的情况下维持了有效的探索，并强调了 UEC-RL 是在大模型中扩展基于 RL 的推理的关键。我们的代码可在 https://github.com/597358816/UEC-RL 获取。

## Abstract
Recent advances in reinforcement learning (RL) have improved the reasoning capabilities of large language models (LLMs) and vision-language models (VLMs). However, the widely used Group Relative Policy Optimization (GRPO) consistently suffers from entropy collapse, causing the policy to converge prematurely and lose diversity. Existing exploration methods introduce additional bias or variance during exploration, making it difficult to maintain optimization stability. We propose Unified Entropy Control for Reinforcement Learning (UEC-RL), a framework that provides targeted mechanisms for exploration and stabilization. UEC-RL activates more exploration on difficult prompts to search for potential and valuable reasoning trajectories. In parallel, a stabilizer prevents entropy from growing uncontrollably, thereby keeping training stable as the model consolidates reliable behaviors. Together, these components expand the search space when needed while maintaining robust optimization throughout training. Experiments on both LLM and VLM reasoning tasks show consistent gains over RL baselines on both Pass@1 and Pass@$k$. On Geometry3K, UEC-RL achieves a 37.9\% relative improvement over GRPO, indicating that it sustains effective exploration without compromising convergence and underscoring UEC-RL as a key for scaling RL-based reasoning in large models. Our code is available at https://github.com/597358816/UEC-RL.

---

## 论文详细总结（自动生成）

这是一份关于论文《Targeted Exploration via Unified Entropy Control for Reinforcement Learning》（UEC-RL）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于大语言模型（LLM）和视觉语言模型（VLM）在强化学习（RL）后训练阶段的**探索效率**问题。
*   **背景**：目前主流的强化学习算法（如 GRPO）在处理复杂推理任务时，极易出现**熵塌陷（Entropy Collapse）**，即模型过早地收敛到某些特定的、可能并非最优的回答模式，导致搜索空间急剧缩小，无法发现更深层的推理路径。
*   **核心问题**：现有的改进方法（如 DAPO 或增加熵奖励）往往会引入额外的偏差或导致训练不稳定（方差过大）。论文旨在提出一种既能针对性增加探索，又能维持训练稳定性的统一熵控制框架。

### 2. 方法论：核心思想与技术细节
UEC-RL 的核心思想是**双向熵控制**：在需要时增加熵以促进探索，在模型掌握可靠行为时降低熵以稳定收敛。

*   **针对性探索机制（Targeted Exploration）**：
    *   **困难样本识别**：如果初始的 $G$ 个采样结果全部错误，则将该提示词标记为“困难”。
    *   **空间扩展**：对困难样本使用更高的温度系数 $t'$ 进行采样，以激活低概率但可能包含正确逻辑的推理轨迹。
    *   **选择性保留**：仅保留具有正优势值（Positive Advantage）的轨迹进入优化集，过滤掉无意义的噪声。
*   **可控熵稳定器（Entropy Stabilizer）**：
    *   **经验回放**：建立一个 Replay Buffer，存储近期发现的高质量（高优势值）轨迹。
    *   **理论支撑**：基于数学证明（定理 4.2），重复利用高优势轨迹的梯度更新会产生正协方差，从而自然地降低策略熵，引导模型向稳定解靠拢。
*   **算法流程**：1. 进行常规采样；2. 若失败则触发高温度探索采样；3. 将高质量样本存入回放池；4. 定期从回放池抽样进行稳定化更新。

### 3. 实验设计
*   **数据集**：
    *   **文本推理**：DAPO-17K（数学推理）。
    *   **多模态推理**：6k 规模的混合数据集（涵盖几何、图表、表格等）。
    *   **分析专用**：Geometry3K（用于深入的训练动力学分析）。
*   **Benchmark（基准测试）**：
    *   **文本**：AIME24/25、MATH、GSM8K、Minerva、ARC、MMLU Pro。
    *   **多模态**：MathVision、MathVerse、MathVista、We-Math。
*   **对比方法**：GRPO（基准）、DAPO（剪切优化）、KL-cov（协方差感知 KL 正则）、Entropy-Adv（优势值增强熵奖励）。

### 4. 资源与算力
*   **硬件配置**：使用了 **8 张 NVIDIA A800 GPU (40GB)**。
*   **模型初始化**：基于 Qwen2.5-VL-7B-Instruct 和 Qwen2.5-Math-7B。
*   **训练细节**：最大响应长度 8192，Batch Size 为 512。文中未明确给出总训练时长，但提到 UEC-RL 的单步训练耗时约为 GRPO 的 0.79 倍（由于采样效率优化），具有较好的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：论文在 11 个主流文本和多模态基准上进行了测试，涵盖了从小学数学到竞赛级数学的跨度。
*   **消融实验**：
    *   针对探索温度 $t'$ 和回放预算 $s'$ 进行了参数敏感性分析。
    *   分别去掉了“探索模块”和“稳定器模块”，验证了两者缺一不可的互补关系。
*   **充分性评价**：实验设计较为全面，不仅关注 Pass@1 准确率，还分析了 Pass@k（多样性指标）、响应长度变化以及熵的演变曲线。对比方法涵盖了近期该领域最前沿的改进方案，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能飞跃**：在 Geometry3K 任务上，UEC-RL 相比 GRPO 实现了 **37.9% 的相对性能提升**。
*   **双向控制的必要性**：单纯增加探索会导致训练发散，单纯抑制熵塌陷会导致搜索不足。UEC-RL 成功将熵维持在一个“最优操作区间”。
*   **多样性提升**：Pass@k 曲线显示，UEC-RL 生成的推理路径不仅更准确，而且更具多样性。
*   **通用性**：该方法在纯文本和视觉语言模型上均表现出一致的增益，证明了其作为通用强化学习框架的潜力。

### 7. 优点（亮点）
*   **理论与实践结合**：通过数学定理（熵变化与协方差的关系）指导算法设计，而非纯经验性的调参。
*   **针对性强**：不盲目增加全局随机性，而是只在模型“不会做”的困难题目上发力。
*   **效率优势**：在提升性能的同时，通过优化采样流程降低了单步训练的时间开销。

### 8. 不足与局限
*   **超参数敏感性**：探索温度 $t'$ 和回放池大小 $s'$ 的最优组合在不同难度的任务上可能不同，需要一定的任务特定调优（Task-specific tuning）。
*   **难度估计精度**：目前对“困难提示词”的定义较为简单（全错即困难），未来可能需要更精细的步级（Step-level）难度评估。
*   **长链推理限制**：虽然在数学任务上表现优异，但在需要极长步骤或非验证性奖励（Non-verifiable rewards）的开放式任务中效果尚待验证。

（完）
