---
title: "GOLD-BEV: GrOund and aeriaL Data for Dense Semantic BEV Mapping of Dynamic Scenes"
title_zh: GOLD-BEV：用于动态场景稠密语义鸟瞰图（BEV）制图的地面与航空数据
authors: "Joshua Niemeijer, Alaa Eddine Ben Zekri, Reza Bahmanyar, Philipp M. Schmälzle, Houda Chaabouni-Chouayakh, Franz Kurz"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19411v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 自动驾驶动态道路场景的稠密语义BEV地图构建
tldr: 本研究提出GOLD-BEV框架，旨在利用时间同步的航拍图像作为训练监督，从车载传感器生成稠密的鸟瞰图（BEV）语义地图。该方法通过航拍与地面数据的严格同步，有效解决了动态物体标注难题，并利用领域自适应的航拍教师模型生成伪标签。此外，系统还能合成伪航拍图像以辅助人工标注和不确定性感知标注，显著降低了BEV语义地图的构建成本并提升了场景理解的几何一致性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19411v1/fig-001.webp\", \"caption\": \"Fig. 6: Starting from two supervision sources—a labeled target subset of BEV-aligned helicopter crops and external supervised data—we train two domain-adapted aerial teachers: a structural Mask2Former for coarse scene layout and a pedestrian-specific Mask2Former for dynamic agents. Applied to each BEV crop, the structural teacher predicts per-pixel semantic labels and confidences, while the pedestrian teacher predicts pedestrian probability. The final dense pseudo-label is obtained by conservative fusion: confident pedestrian predictions overwrite the structural map, confident structural labels are retained where pedestrian evidence is low, and ambiguous pixels are mapped to ignore. Pixels predicted as tree/vegetation are always ignored.\", \"page\": 21, \"index\": 1, \"width\": 725, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2604.19411v1/fig-002.webp\", \"caption\": \"Fig. 7: Qualitative results on the GOLD-BEV dataset. Each row shows one scene. From left to right: front camera, LiDAR BEV raster, ground-truth BEV aerial crop, reconstructed BEV image, pseudo-label BEV segmentation, and predicted segmentation.\", \"page\": 22, \"index\": 2, \"width\": 708, \"height\": 1087}, {\"url\": \"assets/figures/arxiv/2604.19411v1/fig-003.webp\", \"caption\": \"Table 2: Main results on the Gold BEV Test split (dense BEV segmentation). L indicates an additional fine-tuning stage using sparse LiDAR semantic annotations. LiDAR Holdout mIoU is computed on supervised cells only. For camera-only models, all metrics are computed inside the BEV visibility cone (pixels outside are ignored).\", \"page\": 10, \"index\": 3, \"width\": 718, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2604.19411v1/fig-004.webp\", \"caption\": \"Fig. 4: Views used for BEV annotation and validation. Left: real BEV image used to create the Gold manual GT. Middle/right: pseudo-aerial BEV reconstructions from SegFormer and diffusion. LiDAR returns are overlaid (red) to indicate cells with direct sensor support. Annotators label SegFormer/diffusion views independently; the final mask is a fusion that retains only pixels where both views agree on the class.\", \"page\": 14, \"index\": 4, \"width\": 725, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.19411v1/fig-005.webp\", \"caption\": \"Fig. 2: Vehicle setup used for the campaign and sensor installations (a). Trajectory of the data and how it is split into a train val test split (b). Examples for the input (camera and LiDAR), the corresponding aerial crops with the centered ego vehicle and the derived labels from the aerial view (c).\", \"page\": 4, \"index\": 5, \"width\": 725, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2604.19411v1/fig-006.webp\", \"caption\": \"Fig. 5: Overview of our student model. The input consists of (i) a LiDAR point cloud rasterized into a BEV representation with occupancy, height, and density channels, and (ii) a front-facing ground-view RGB image. Each modality is first encoded by a SegFormer backbone. The resulting multi-scale features are fused by cross-attention, allowing the model to combine complementary information despite the different viewpoints and coordinate systems of camera and LiDAR. The fused representation is then used as conditioning for a ControlNet-based latent diffusion decoder (or, alternatively, a deterministic SegFormer decoder). During diffusion, the frozen pretrained denoiser is guided by trainable ControlNet residuals conditioned on the fused features. After denoising, the final latent is decoded by the VAE decoder to produce a pseudo-aerial reconstruction, as well as semantic segmentation outputs in parallel which can be compared against aerial supervision and LiDAR-based annotations.\", \"page\": 19, \"index\": 6, \"width\": 725, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.19411v1/fig-007.webp\", \"caption\": \"Fig. 3: Comparison of BEV semantic segmentation predictions. Each row shows a different scenario: a highway scene (top), a complex urban intersection (middle), and a zoomed-in crop highlighting a VRU prediction (bottom). From left to right: ground truth (GT), LiDAR-only prediction (L-only), camera+LiDAR prediction (C+L), and camera+LiDAR with additional sparse LiDAR fine-tuning (C+L + sparse).\", \"page\": 12, \"index\": 7, \"width\": 725, \"height\": 394}]"
motivation: 传统的车载BEV标注存在歧义且难以处理动态场景，而利用非同步航拍数据会导致严重的时空不一致问题。
method: 采用时间同步的航拍图像作为监督信号，结合领域自适应的教师模型生成稠密伪标签，并支持从车载传感器合成伪航拍图像。
result: 该框架实现了对动态交通参与者和静态环境的高效稠密BEV语义映射，并能通过伪标签技术在无标注数据上进行扩展。
conclusion: GOLD-BEV通过空地协同学习，为构建几何一致且包含动态目标的稠密BEV语义地图提供了一种可扩展且低成本的有效方案。
---

## 摘要
以几何一致、以场景为中心的表示方式理解道路场景，对于规划和制图至关重要。我们提出了 GOLD-BEV，这是一个从自我中心传感器学习稠密鸟瞰图（BEV）语义环境地图（包括动态代理）的框架，仅在训练期间使用时间同步的航空图像作为监督。BEV 对齐的航空裁剪图像提供了一个直观的目标空间，能够以极少的人工努力实现稠密语义标注，并避免了仅依靠自我中心传感器进行 BEV 标注的歧义。至关重要的是，严格的空地同步允许俯视观测来监督移动的交通参与者，并减轻了非同步俯视源固有的时间不一致性。为了获得可扩展的稠密目标，我们使用领域自适应的航空教师模型生成 BEV 伪标签，并将 BEV 分割与可选的伪航空 BEV 重建进行联合训练，以提高可解释性。最后，我们通过学习从自我中心传感器合成伪航空 BEV 图像，将应用范围扩展到航空覆盖范围之外，这支持了在未标记驾驶数据上的轻量化人工标注和不确定性感知伪标签生成。

## Abstract
Understanding road scenes in a geometrically consistent, scene-centric representation is crucial for planning and mapping. We present GOLD-BEV, a framework that learns dense bird's-eye-view (BEV) semantic environment maps-including dynamic agents-from ego-centric sensors, using time-synchronized aerial imagery as supervision only during training. BEV-aligned aerial crops provide an intuitive target space, enabling dense semantic annotation with minimal manual effort and avoiding the ambiguity of ego-only BEV labeling. Crucially, strict aerial-ground synchronization allows overhead observations to supervise moving traffic participants and mitigates the temporal inconsistencies inherent to non-synchronized overhead sources. To obtain scalable dense targets, we generate BEV pseudo-labels using domain-adapted aerial teachers, and jointly train BEV segmentation with optional pseudo-aerial BEV reconstruction for interpretability. Finally, we extend beyond aerial coverage by learning to synthesize pseudo-aerial BEV images from ego sensors, which support lightweight human annotation and uncertainty-aware pseudo-labeling on unlabeled drives.

---

## 论文详细总结（自动生成）

### GOLD-BEV 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
自动驾驶系统需要几何一致且包含语义信息的鸟瞰图（BEV）来辅助路径规划。然而，构建稠密的 BEV 语义地图面临两大挑战：
*   **标注困难**：仅依靠车载传感器（如 LiDAR 或相机）进行 BEV 标注非常困难，LiDAR 在远距离十分稀疏，相机存在视角转换歧义，且两者都受遮挡影响。
*   **时空不一致**：虽然现有研究尝试利用卫星或地图数据作为监督，但这些数据通常与车辆行驶时间不同步，无法提供动态交通参与者（如车辆、行人）的准确监督信号。

**GOLD-BEV** 的核心动机是利用**时间同步的航空图像**作为训练时的“完美老师”，为车载传感器提供稠密、无遮挡且包含动态目标的 BEV 监督信号。

#### 2. 方法论：核心思想与关键技术
该框架的核心思想是“航空监督，地面推理”。
*   **空地同步数据采集**：使用直升机搭载高分辨率相机与地面实验车（配备 LiDAR、相机、GNSS/INS）同步拍摄。通过严格的时间戳对齐（误差 < 100ms）和空间对齐（GNSS + 模板匹配），生成以车辆为中心的 BEV 对齐航空裁剪图。
*   **领域自适应教师模型**：
    *   使用两个基于 Mask2Former 的教师模型：一个负责静态结构（道路、建筑等），一个专门负责行人（VRU）。
    *   通过半监督领域自适应（SSDA）将模型从公开数据集迁移到直升机航拍域。
    *   **伪标签融合**：采用保守融合策略，高置信度的行人预测会覆盖结构预测，并自动忽略树木遮挡区域。
*   **学生模型架构**：
    *   **输入**：前视 RGB 图像 + 栅格化的 LiDAR BEV 特征（占用、高度、密度）。
    *   **融合**：采用 SegFormer 编码器提取特征，通过**交叉注意力机制（Cross-Attention）**融合多模态信息。
    *   **多任务输出**：同时预测 BEV 语义分割图和**伪航空 BEV 图像重建**。
*   **重建技术**：提供两种方案，一种是轻量级的 SegFormer 解码器，另一种是基于 **Stable Diffusion + ControlNet** 的生成式解码器，用于生成高保真度的伪航拍图以辅助人工标注。

#### 3. 实验设计
*   **数据集**：自研 GOLD 数据集，涵盖德国城市、郊区和高速公路（60公里，约5小时驾驶数据，8199个同步样本）。
*   **Benchmark 与对比**：
    *   **输入模态对比**：仅相机（C）、仅 LiDAR（L）、相机+LiDAR（C+L）。
    *   **监督源对比**：纯航空伪标签监督 vs. 航空伪标签 + 少量稀疏 LiDAR 标注微调。
    *   **重建质量评估**：对比 SegFormer 和 Diffusion 在 PSNR、SSIM、LPIPS 和 FID 指标上的表现。
*   **评估指标**：mIoU（全类别、静态类别、动态类别 IoU）。

#### 4. 资源与算力
*   **论文说明**：文中**未明确说明**具体的 GPU 型号、数量以及训练所需的总时长。
*   **推测**：考虑到使用了 Stable Diffusion v1.5 和 ControlNet 进行微调，且涉及多模态融合训练，该实验通常需要具备 24GB 以上显存的工业级 GPU（如 NVIDIA A100 或 RTX 3090/4090 级别）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验，包括不同传感器组合的效果、稀疏 LiDAR 监督比例（10% 到 100%）对性能的影响、以及人工标注一致性验证。
*   **充分性**：实验设计较为充分。作者不仅验证了模型在测试集上的表现，还专门设置了“LiDAR Holdout”验证集，利用人工标注的 LiDAR 点云语义来客观衡量模型在可见区域的几何准确性。
*   **公平性**：通过地理分区（而非随机采样）划分训练/验证/测试集，有效防止了空间数据泄露，保证了评估的客观性。

#### 6. 主要结论与发现
*   **航空监督的有效性**：时间同步的航拍图能提供极佳的静态布局监督，LiDAR+相机的融合模型在静态类别上表现稳健。
*   **动态目标的增益**：通过同步航拍，模型能够学习到动态车辆的 BEV 特征。
*   **稀疏监督的效率**：实验发现，仅需 **25% 的稀疏 LiDAR 标注**进行微调，即可显著提升动态物体（尤其是行人）的检测精度，达到边际收益递减点。
*   **重建辅助标注**：Diffusion 生成的伪航拍图虽然像素精度略低，但感知质量（FID/LPIPS）更高，非常适合作为人工快速标注的底图。

#### 7. 优点（亮点）
*   **创新的监督方式**：解决了 BEV 标注中“动态物体不同步”的痛点，利用直升机数据实现了高质量的稠密监督。
*   **多任务闭环**：将语义分割与图像重建结合，重建出的伪航拍图不仅提高了模型的可解释性，还为无航拍覆盖区域的数据标注提供了工具。
*   **标注效率**：提出的“两视图标注+严格一致性融合”协议，证明了在伪航拍图上进行轻量级标注的可行性。

#### 8. 不足与局限
*   **采集成本高昂**：依赖直升机进行同步采集，这种数据获取方式难以大规模推广到所有自动驾驶场景。
*   **小目标挑战**：行人在航拍图中仅占极少数像素，导致纯航空监督对行人的召回率有限，仍需依赖 LiDAR 信号补充。
*   **视角限制**：目前仅使用了前视相机，未实现全向 360° 的环视融合（虽然框架理论上支持）。
*   **应用限制**：在树木茂密的区域，航拍图的遮挡问题依然存在，导致部分区域必须被标记为“忽略”。

（完）
