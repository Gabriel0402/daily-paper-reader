---
title: A Controlled Benchmark of Visual State-Space Backbones with Domain-Shift and Boundary Analysis for Remote-Sensing Segmentation
title_zh: 遥感分割中视觉状态空间骨干网络的受控基准测试：领域偏移与边界分析
authors: "Nichula Wasalathilaka, Dineth Perera, Oshadha Samarakoon, Buddhi Wijenayake, Roshan Godaliyadda, Vijitha Herath, Parakrama Ekanayake"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18721v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 用于遥感语义分割的视觉状态空间模型基准
tldr: 本研究针对遥感语义分割任务，建立了一个严格受控的视觉状态空间模型（SSM）基准测试。通过统一解码器和训练协议，对比了VMamba、MambaVision和Spatial-Mamba等模型。研究发现SSM在精度与效率权衡上优于传统CNN和Transformer，但存在跨域泛化不对称及边界识别失效等问题，为未来Mamba类骨干网络的设计提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18721v1/fig-001.webp\", \"caption\": \"Fig. 2. Qualitative comparison under the controlled protocol. (a) LoveDA: Urban/Rural examples illustrating boundary delineation and robustness under domain shift. (b) ISPRS Potsdam: fine-grained structures (buildings, impervious surfaces, vegetation, cars) in very-high-resolution aerial imagery.\", \"page\": 4, \"index\": 1, \"width\": 990, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2604.18721v1/fig-002.webp\", \"caption\": \"Fig. 3. Efficiency analysis on LoveDA: A comparison of mIoU versus FPS across VSS backbones.\", \"page\": 4, \"index\": 2, \"width\": 502, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2604.18721v1/fig-003.webp\", \"caption\": \"TABLE I CONTROLLED BENCHMARK RESULTS ON LOVEDA AND ISPRS POTSDAM. U→R AND R→U DENOTE SOURCE-ONLY CROSS-DOMAIN MIOU. TOP-3 PER COLUMN: 1ST, 2ND, 3RD.\", \"page\": 3, \"index\": 3, \"width\": 879, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.18721v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of the controlled benchmark pipeline. To ensure fair comparison, all backbones including visual SSMs (VMamba, MambaVision, SpatialMamba) and baselines (CNNs, Transformers) are interfaced through a unified 4-stage feature pyramid and evaluated with the same lightweight U-Net decoder under an identical training and evaluation protocol.\", \"page\": 2, \"index\": 4, \"width\": 983, \"height\": 323}]"
motivation: 现有研究往往将编码器与解码器及训练策略混淆，导致无法公平评估视觉状态空间模型在遥感分割中的实际优势。
method: 在统一的四阶段特征接口和轻量级解码器下，仅改变编码器，对多种代表性视觉SSM家族在LoveDA和ISPRS Potsdam数据集上进行受控实验。
result: 实验表明SSM具有优异的精度-效率比，但家族内缩放收益有限，且在分布偏移下边界刻画是主要的失效模式。
conclusion: 未来Mamba类分割模型的研究应侧重于鲁棒性设计和边界感知解码，而非单纯依赖编码器的规模缩放。
---

## 摘要
视觉状态空间模型（SSM）正日益被推广为视觉 Transformer 的高效替代方案，但在公平比较下，其实际优势仍不明确，因为现有研究很少将编码器效应与解码器及训练选择隔离开来。我们针对遥感语义分割，提出了一个对代表性视觉 SSM 家族（包括 VMamba、MambaVision 和 Spatial-Mamba）进行严格受控的基准测试，其中实验间仅编码器发生变化。在 LoveDA 和 ISPRS Potsdam 数据集上，通过统一的 4 阶段特征接口和固定的轻量级解码器进行评估，该基准揭示了三个主要发现：家族内缩放仅带来适度收益，跨域泛化具有强烈的非对称性，且边界描绘是分布偏移下的主要失效模式。尽管与此处考虑的受控 CNN 和 Transformer 基准相比，视觉 SSM 实现了良好的准确率-效率权衡，但结果表明，未来的改进更有可能来自面向鲁棒性的设计和边界感知解码，而非仅仅依靠编码器缩放。通过在统一且可重复的协议下隔离编码器行为，本研究为未来基于 Mamba 的分割骨干网络的设计与评估建立了一个实用的参考基准。

## Abstract
Visual state-space models (SSMs) are increasingly promoted as efficient alternatives to Vision Transformers, yet their practical advantages remain unclear under fair comparison because existing studies rarely isolate encoder effects from decoder and training choices. We present a strictly controlled benchmark of representative visual SSM families, including VMamba, MambaVision, and Spatial-Mamba, for remote-sensing semantic segmentation, in which only the encoder varies across experiments. Evaluated on LoveDA and ISPRS Potsdam under a unified 4-stage feature interface and a fixed lightweight decoder, the benchmark reveals three main findings, intra-family scaling yields only modest gains, cross-domain generalization is strongly asymmetric, and boundary delineation is the dominant failure mode under distribution shift. Although visual SSMs achieve favorable accuracy-efficiency trade-offs relative to the controlled CNN and Transformer baselines considered here, the results suggest that future improvements are more likely to come from robustness-oriented design and boundary-aware decoding than from encoder scaling alone. By isolating encoder behavior under a unified and reproducible protocol, this study establishes a practical reference benchmark for the design and evaluation of future Mamba-based segmentation backbones

---

## 论文详细总结（自动生成）

这篇论文针对遥感图像语义分割任务，为新兴的视觉状态空间模型（SSM/Mamba）建立了一个严格受控的基准测试。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：视觉状态空间模型（如 Mamba）因其在线性复杂度下建模长程依赖的能力，被视为 Vision Transformer (ViT) 的强力竞争者。
*   **核心问题**：现有关于视觉 SSM 的研究往往将编码器（Encoder）的改进与解码器（Decoder）的选择、训练策略及数据增强混为一谈，导致无法客观评估 SSM 骨干网络（Backbone）在遥感任务中的真实效能。
*   **研究动机**：在遥感领域，模型需要同时处理高分辨率带来的计算压力和复杂的领域偏移（如城乡差异）。作者旨在通过“控制变量法”，隔离编码器效应，建立一个公平、可重复的基准。

### 2. 方法论
*   **核心思想**：采用**严格受控的编码器协议**。固定整个分割流水线的所有组件，仅更换编码器骨干网络。
*   **关键技术细节**：
    *   **统一特征接口**：所有编码器均通过一个统一的 4 阶段特征金字塔接口输出，步长分别为 {4, 8, 16, 32}。对于通道数不匹配的模型，使用 1×1 卷积进行投影标准化。
    *   **固定解码器**：统一使用轻量级的类 U-Net 卷积解码器，以确保性能差异完全源于编码器的表征能力。
    *   **损失函数设计**：组合了三种损失：$L = L_{Lovász} + L_{Focal} + 0.5 L_{Boundary}$。其中 Lovász 优化 Jaccard 指数，Focal 处理类别不平衡，Boundary 损失专门惩罚边缘附近的错误。
    *   **训练协议**：统一 50k 次迭代，使用 AdamW 优化器，并对骨干网络和解码器设置不同的学习率。

### 3. 实验设计
*   **数据集**：
    *   **LoveDA**：用于分析城乡之间的跨域泛化能力（Urban $\leftrightarrow$ Rural）。
    *   **ISPRS Potsdam**：超高分辨率（5cm）航空影像，用于验证精细物体和边界的解析能力。
*   **对比方法**：
    *   **SSM 家族**：VMamba (Tiny/Small/Base), MambaVision (T/T2/S/B/L/L2), Spatial-Mamba (T/S/B)。
    *   **基准参考**：CNN 代表（DeepLabv3 风格编码器）和 Transformer 代表（UNetFormer 风格编码器）。
*   **评估指标**：mIoU、OA、F1-score，以及专门的**边界诊断指标**（通过对边缘进行 2 像素膨胀处理来区分边界和内部区域）。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA Quadro GV100 (32 GB)** GPU。
*   **软件环境**：PyTorch 2.0.1, CUDA 11.5。
*   **训练细节**：Batch size 为 4，输入分辨率为 512×512。文中提供了详细的推理吞吐量（FPS）和峰值显存占用数据。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个 SSM 家族的 12 种不同规模的变体，并与 CNN 和 Transformer 进行了对比。
*   **公平性与客观性**：
    *   所有模型均使用 ImageNet-1K 预训练权重初始化。
    *   对表现最好的模型进行了 **3 次随机种子重复实验**并报告标准差（标准差均在 0.18-0.26 之间），证明了结果的稳定性。
    *   通过统一解码器和训练参数，消除了“训练技巧”带来的偏差。

### 6. 主要结论与发现
*   **精度-效率权衡**：视觉 SSM 在遥感分割中表现出优于受控 CNN 和 Transformer 的权衡关系。VMamba-Small 在复杂度较低的情况下表现极其稳健。
*   **家族内缩放收益递减**：简单地增加 SSM 编码器的参数量（如从 Small 到 Base）带来的 mIoU 提升非常有限，甚至在某些任务中出现退化。
*   **跨域泛化不对称性**：从“农村到城市”（R$\to$U）的迁移效果显著优于“城市到农村”（U$\to$R）。这可能是因为农村场景的多样性更高，有助于学习更具通用性的特征。
*   **边界失效模式**：边界精度普遍比内部区域精度低约 30 mIoU。边界模糊是分布偏移下的主要失效原因，而非内部纹理识别。

### 7. 优点
*   **高度可重复性**：提供了严格的协议和代码，为后续研究提供了标准化的测试床。
*   **深入的诊断分析**：不仅看 mIoU，还通过边界/内部对比、城乡跨域对比，深入探讨了模型“为什么失败”。
*   **实用性强**：针对遥感部署中关心的 FPS 和显存做了详尽测试，对实际工程选型有指导意义。

### 8. 不足与局限
*   **基准覆盖范围**：虽然对比了代表性模型，但未涵盖所有流行的遥感骨干网络（如 SegFormer 或 Swin-UNet）。
*   **解码器单一性**：仅测试了一种轻量级卷积解码器，未探讨 SSM 编码器与更复杂的 Transformer 解码器或边界感知解码器的协同效应。
*   **预训练依赖**：所有结论均基于 ImageNet 预训练，未探讨在大规模遥感基础模型（Foundation Models）预训练下的表现。

（完）
