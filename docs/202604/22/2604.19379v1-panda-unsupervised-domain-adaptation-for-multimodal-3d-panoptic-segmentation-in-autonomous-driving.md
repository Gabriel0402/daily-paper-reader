---
title: "PanDA: Unsupervised Domain Adaptation for Multimodal 3D Panoptic Segmentation in Autonomous Driving"
title_zh: PanDA：自动驾驶中多模态 3D 全景分割的无监督领域自适应
authors: "Yining Pan, Shijie Li, Yuchen Wu, Xulei Yang, Na Zhao"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19379v1"
tags: ["query:mmseg"]
score: 10.0
evidence: 自动驾驶中具有领域自适应的多模态3D全景分割
tldr: 本研究针对自动驾驶中的多模态3D全景分割（mm-3DPS）提出了首个无监督领域自适应（UDA）框架PanDA。针对现有方法在传感器退化时鲁棒性差以及伪标签不完整的问题，PanDA引入了非对称多模态增强技术来模拟领域偏移，并利用双专家伪标签细化模块结合2D和3D先验知识，显著提升了模型在不同天气、时间和传感器变化下的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19379v1/fig-001.webp\", \"caption\": \"Figure 3. Visualization of panoptic segmentation error maps across various domain shifts, comparing our method with xMUDA and UniDSeg. Point colors indicate error types: false positives and false negatives reflect recognition errors, while well-matched and mismatched points (within true positives) highlight segmentation inaccuracies. Raw point clouds, camera images, and semantic-colored ground truth (GT) are shown for reference. Best viewed in color.\", \"page\": 8, \"index\": 1, \"width\": 1031, \"height\": 662}, {\"url\": \"assets/figures/arxiv/2604.19379v1/fig-002.webp\", \"caption\": \"Figure 2. The architecture overview of our PanDA framework. Our model follows a teacher–student paradigm for UDA in multimodal 3D panoptic segmentation. On the labeled source domain (green block), Asymmetric Multi-modal Drop (AMD) selectively removes instance-interior and boundary regions from either the LiDAR or image modality. This encourages cross-modal recovery and improves robustness to modality imbalance and domain shifts. On the unlabeled target domain (purple block), Dual-Expert Pseudo-Label Refinement (DualRefine) leverages 3D geometric constraints to refine masks and exploits 2D semantic knowledge to correct category predictions.\", \"page\": 3, \"index\": 2, \"width\": 1024, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2604.19379v1/fig-003.webp\", \"caption\": \"Table 1. Comparative and ablative studies for cross-domain 3D multimodal panoptic segmentation. “Pano-xMUDA” and “Pano-UniDseg” represent adapting 3D semantic UDA work xMUDA [18] and UniDSeg [50] to panoptic segmentation. Our method is ablated by DualRefine, AMD modules or both (-base). Top and runner-up results are marked in bold and underline, respectively.\", \"page\": 7, \"index\": 3, \"width\": 1022, \"height\": 331}]"
motivation: 现有的多模态3D全景分割方法在面临传感器退化或领域偏移时，容易因过度依赖跨模态互补性而导致性能下降和伪标签破碎。
method: 提出了PanDA框架，包含通过选择性丢弃区域来增强鲁棒性的非对称多模态增强模块，以及利用2D和3D领域不变先验进行伪标签优化的双专家模块。
result: 在涵盖时间、天气、地点和传感器变化的多种领域偏移场景下，PanDA的性能显著优于现有的3D语义分割UDA基准方法。
conclusion: PanDA通过增强多模态鲁棒性和伪标签质量，为自动驾驶环境下复杂的多模态3D全景分割任务提供了有效的无监督领域自适应方案。
---

## 摘要
本文介绍了针对多模态 3D 全景分割（mm-3DPS）的无监督领域自适应（UDA）的首个研究，旨在提高在现实自动驾驶中常见的领域偏移下的泛化能力。一个直接的解决方案是采用伪标签策略（在 UDA 中广泛用于为未标记的目标数据生成监督信号），并结合 mm-3DPS 骨干网络。然而，现有的有监督 mm-3DPS 方法严重依赖 LiDAR 和 RGB 输入之间强大的跨模态互补性，这使得它们在一种模态退化（例如光照不良或恶劣天气）的领域偏移下变得脆弱。此外，传统的伪标签通常只保留高置信度区域，导致掩码碎片化和物体监督不完整，这些问题对全景分割尤为不利。为了应对这些挑战，我们提出了 PanDA，这是首个专门为多模态 3D 全景分割设计的 UDA 框架。为了提高对单传感器退化的鲁棒性，我们引入了一种非对称多模态增强，通过选择性地丢弃区域来模拟领域偏移并改进鲁棒的表示学习。为了增强伪标签的完整性和可靠性，我们进一步开发了一个双专家伪标签细化模块，从 2D 和 3D 模态中提取领域不变的先验知识。在跨越时间、天气、地点和传感器变化的各种领域偏移下进行的广泛实验表明，该方法显著超越了现有的 3D 语义分割 UDA 基准。

## Abstract
This paper presents the first study on Unsupervised Domain Adaptation (UDA) for multimodal 3D panoptic segmentation (mm-3DPS), aiming to improve generalization under domain shifts commonly encountered in real-world autonomous driving. A straightforward solution is to employ a pseudo-labeling strategy, which is widely used in UDA to generate supervision for unlabeled target data, combined with an mm-3DPS backbone. However, existing supervised mm-3DPS methods rely heavily on strong cross-modal complementarity between LiDAR and RGB inputs, making them fragile under domain shifts where one modality degrades (e.g., poor lighting or adverse weather). Moreover, conventional pseudo-labeling typically retains only high-confidence regions, leading to fragmented masks and incomplete object supervision, which are issues particularly detrimental to panoptic segmentation. To address these challenges, we propose PanDA, the first UDA framework specifically designed for multimodal 3D panoptic segmentation. To improve robustness against single-sensor degradation, we introduce an asymmetric multimodal augmentation that selectively drops regions to simulate domain shifts and improve robust representation learning. To enhance pseudo-label completeness and reliability, we further develop a dual-expert pseudo-label refinement module that extracts domain-invariant priors from both 2D and 3D modalities. Extensive experiments across diverse domain shifts, spanning time, weather, location, and sensor variations, significantly surpass state-of-the-art UDA baselines for 3D semantic segmentation.

---

## 论文详细总结（自动生成）

### PanDA: 自动驾驶中多模态 3D 全景分割的无监督领域自适应

#### 1. 核心问题与整体含义
论文针对自动驾驶中的**多模态 3D 全景分割（mm-3DPS）**任务，首次探讨了其在**无监督领域自适应（UDA）**场景下的应用。
*   **研究动机**：现有的多模态 3D 分割模型高度依赖 LiDAR 和图像的互补性，但在现实世界中，领域偏移（如昼夜交替、天气变化、地理差异）会导致某一模态性能严重退化（如雨天 LiDAR 噪点多、夜晚图像质量差），使模型失效。
*   **核心挑战**：传统的伪标签（Pseudo-labeling）策略在处理全景分割时，往往因置信度阈值过滤导致实例掩码破碎、边界模糊，无法提供完整的监督信号。

#### 2. 方法论
PanDA 采用了基于 **Mean-Teacher（教师-学生）** 的框架，并引入了两个核心模块：
*   **非对称多模态丢弃（Asymmetric Multimodal Drop, AMD）**：
    *   在源域训练时，主动模拟传感器退化。
    *   **边界丢弃**：利用 Canny 算子或几何不连续性识别图像/点云的边界区域并随机丢弃，增强模型对边界结构的鲁棒性。
    *   **实例丢弃**：针对“物（Thing）”类实例内部进行局部遮蔽，强迫模型通过另一模态恢复完整目标。
*   **双专家伪标签细化（Dual-Expert Pseudo-Label Refinement, DualRefine）**：
    *   **3D 几何专家**：利用 RANSAC 和 HDBSCAN 提取 3D 几何超点（Superpoints），通过“生长（Grow）”操作修复破碎的背景（Stuff）掩码。
    *   **2D 视觉专家**：利用视觉大模型（VFM，如 SAM 和 Grounding DINO）生成的 2D 提议，通过“重分配（Reassign）”操作纠正低置信度实例的类别错误。

#### 3. 实验设计
*   **数据集与场景**：
    *   **nuScenes 内部偏移**：昼 vs. 夜（Day $\rightarrow$ Night）、晴天 vs. 雨天（Sunny $\rightarrow$ Rainy）、波士顿 vs. 新加坡（USA $\rightarrow$ SG）。
    *   **跨数据集偏移**：SemanticKITTI $\rightarrow$ nuScenes（涉及 64 线到 32 线 LiDAR 的传感器变化）。
*   **基准（Benchmark）与对比方法**：
    *   以 **IAL** 作为基础分割网络。
    *   对比了 **Pano-xMUDA** 和 **Pano-UniDSeg**（将现有的 3D 语义分割 UDA 方法适配到全景分割任务作为 Baseline）。
    *   提供了 **Oracle**（目标域全监督）作为性能上限参考。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA A40 或 H100 GPU**。
*   **训练细节**：Batch size 为 2；采用 AdamW 优化器；根据任务不同，训练周期设定为目标域样本量的 15 或 30 倍迭代次数。
*   **预处理**：LiDAR 点云离散化为柱状体（Cylindrical voxels），图像缩放至 640×360。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 4 种主要的领域偏移场景下进行了全面测试，每种场景均包含 PQ、mIoU、PQ$_{th}$、PQ$_{st}$ 等多项指标。
*   **消融实验**：针对 AMD 模块（实例丢弃、边界丢弃、辅助损失）和 DualRefine 模块（Grow、Class Reassign）分别进行了详尽的消融研究。
*   **客观性**：实验涵盖了时间、天气、地理、传感器四大维度，对比了当前最先进的适配方法，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能提升显著**：PanDA 在所有偏移场景下均大幅超越 Baseline。在 USA $\rightarrow$ SG 场景下 PQ 提升了 13.2%，在最具挑战性的跨数据集任务中 PQ 提升了 53.3%。
*   **突破上限**：在“昼 $\rightarrow$ 夜”场景中，PanDA 的表现甚至超过了仅在目标域训练的 Oracle 模型，证明了其增强策略在极端环境下的有效性。
*   **协同效应**：AMD 提升了特征层的鲁棒性，而 DualRefine 保证了标签层的质量，两者结合对“物”类实例的完整性提升尤为明显。

#### 7. 优点
*   **首创性**：填补了多模态 3D 全景分割在无监督领域自适应领域的空白。
*   **先验融合**：巧妙地结合了 3D 几何连续性和 2D 视觉大模型的语义知识，解决了伪标签破碎的顽疾。
*   **鲁棒性强**：非对称丢弃策略不依赖特定领域的先验知识，具有很强的通用性。

#### 8. 不足与局限
*   **计算开销**：引入了视觉大模型（VFM）生成提议，虽然是在离线或伪标签生成阶段，但整体训练流水线可能较为复杂且耗时。
*   **依赖性**：DualRefine 模块的效果在一定程度上受限于 2D VFM 的准确性，若 2D 模型在某些极端场景下失效，可能会引入噪声。
*   **实时性讨论缺失**：论文未详细讨论推理阶段的延迟，对于自动驾驶这一实时性要求极高的场景，模型部署的效率尚待验证。

（完）
