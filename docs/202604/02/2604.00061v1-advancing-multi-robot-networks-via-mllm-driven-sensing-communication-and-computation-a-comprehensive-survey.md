---
title: "Advancing Multi-Robot Networks via MLLM-Driven Sensing, Communication, and Computation: A Comprehensive Survey"
title_zh: 通过 MLLM 驱动的感知、通信与计算推进多机器人网络：一项全面综述
authors: "Hyun Jong Yang, Howon Lee, Kyuhong Shim, Jeongho Kwak, Hyunsoo Kim, Donghoon Kim, Khoa Anh Ngo, Sehyun Ryu, Jaehyun Choi, Youbin Kim, Chanjun Moon, Michael Ryoo, Byonghyo Shim"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00061v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多机器人网络中基于多模态大模型的协调与感知
tldr: 本综述探讨了多机器人网络中由多模态大模型（MLLM）驱动的感知、通信与计算集成设计。针对多机器人协作中海量数据传输带来的网络压力，文章提出了“意图到资源”的编排框架，利用MLLM理解高层指令以优化资源分配。通过四个端到端实验展示了该架构在降低延迟和提升任务成功率方面的优势，为未来机器人协同系统提供了系统级指导。
selection_source: fresh_fetch
motivation: 传统多机器人系统在处理海量传感器数据时面临通信带宽瓶颈，且缺乏基于任务意图的动态资源优化机制。
method: 提出一种由MLLM驱动的R2X编排框架，将感知、通信与计算进行联合优化，并实现端云协同的推理分配。
result: 通过数字孪生导航、主动控制及硬件垃圾分类等四个实验，证明了该集成设计在负载、延迟和任务成功率上优于纯本地基准。
conclusion: MLLM驱动的意图感知资源编排是实现高效多机器人协同的关键，能有效平衡系统资源约束与复杂任务需求。
---

## 摘要
想象一下，由多模态大语言模型（MLLM）驱动的高级人形机器人，在仓库物流、制造业和安全救援等行业中协同执行任务。虽然单个机器人表现出局部自主性，但现实任务需要多个智能体之间进行协作，并共享海量的传感器数据流。通信是必不可少的，但传输全部数据可能会使网络过载，特别是当系统级编排器或基于云的 MLLM 融合多模态输入以进行路径规划或异常检测时。这些任务通常由高级自然语言指令发起。这种意图充当了资源优化的过滤器：通过 MLLM 理解目标，系统可以有选择地激活相关的感知模态，动态分配带宽，并确定计算位置。因此，R2X 从根本上是一个“意图到资源”的编排问题，其中感知、通信和计算被联合优化，以在资源受限的情况下最大限度地提高任务级成功率。本综述探讨了集成设计如何为 MLLM 指导下的多机器人协作铺平道路。我们回顾了最先进的感知模态、通信策略和计算方法，重点介绍了推理如何在设备端模型与强大的边缘/云服务器之间进行切分。我们展示了四个端到端演示（感知 -> 通信 -> 计算 -> 行动）：(i) 具有预测链路上下文的数字孪生仓库导航，(ii) 移动驱动的主动式 MCS 控制，(iii) 具有语义感知开关的 FollowMe 机器人，以及 (iv) 通过边缘辅助 MLLM 锚定的真实硬件开放词汇垃圾分类。我们强调了系统级指标——负载、延迟和成功率——以展示为什么 R2X 编排优于纯设备端的基准方案。

## Abstract
Imagine advanced humanoid robots, powered by multimodal large language models (MLLMs), coordinating missions across industries like warehouse logistics, manufacturing, and safety rescue. While individual robots show local autonomy, realistic tasks demand coordination among multiple agents sharing vast streams of sensor data. Communication is indispensable, yet transmitting comprehensive data can overwhelm networks, especially when a system-level orchestrator or cloud-based MLLM fuses multimodal inputs for route planning or anomaly detection.   These tasks are often initiated by high-level natural language instructions. This intent serves as a filter for resource optimization: by understanding the goal via MLLMs, the system can selectively activate relevant sensing modalities, dynamically allocate bandwidth, and determine computation placement. Thus, R2X is fundamentally an intent-to-resource orchestration problem where sensing, communication, and computation are jointly optimized to maximize task-level success under resource constraints.   This survey examines how integrated design paves the way for multi-robot coordination under MLLM guidance. We review state-of-the-art sensing modalities, communication strategies, and computing approaches, highlighting how reasoning is split between on-device models and powerful edge/cloud servers. We present four end-to-end demonstrations (sense -> communicate -> compute -> act): (i) digital-twin warehouse navigation with predictive link context, (ii) mobility-driven proactive MCS control, (iii) a FollowMe robot with a semantic-sensing switch, and (iv) real-hardware open-vocabulary trash sorting via edge-assisted MLLM grounding. We emphasize system-level metrics -- payload, latency, and success -- to show why R2X orchestration outperforms purely on-device baselines.

---

## 论文详细总结（自动生成）

### 论文总结：通过 MLLM 驱动的感知、通信与计算推进多机器人网络

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**多机器人网络（Multi-Robot Networks）在执行复杂协作任务时面临的资源瓶颈**。
*   **研究动机**：虽然单体机器人自主性已显著提升，但现实任务（如仓库物流、灾难救援）需要多机器人协同。这产生了海量的多模态传感器数据（LiDAR、RGB-D等），直接传输这些原始数据会使无线网络过载。
*   **核心概念**：提出了 **R2X（Robot-to-Everything）** 范式。其核心在于将多模态大语言模型（MLLM）作为“意图到资源”的编排器，通过理解人类的高层自然语言指令（如“寻找黄色箱子”），动态地决定“感知什么、传输什么、在哪里计算”，从而在有限的带宽、延迟和算力约束下最大化任务成功率。

#### 2. 方法论：核心思想与关键技术
论文提出了一个集成了感知、通信和计算的统一架构，关键技术包括：
*   **意图驱动的编排（Intent-to-Resource Orchestration）**：利用 MLLM 解析自然语言，将其转化为系统参数（如路径规划优先级、无线电发射功率、感知精度）。
*   **语义感知与特征压缩**：不再传输原始图像，而是利用视觉编码器（如 ViT、SigLIP）提取紧凑的语义特征向量，实现约 **1024:1** 的压缩比，显著降低上行链路负载。
*   **主动式通信控制**：利用射线追踪（Ray-tracing）预测未来的链路质量（SNR），在信号衰减前主动调整调制编码方案（MCS）或发射功率，减少因重传导致的延迟抖动。
*   **分层计算架构**：将简单反射动作保留在设备端，而将复杂的全局规划、多视图融合和开放词汇推理（Open-vocabulary reasoning）卸载到边缘或云端服务器。

#### 3. 实验设计
论文通过四个递进的端到端演示（Demo）验证了 R2X 框架：
*   **Demo I：数字孪生仓库导航**
    *   **场景**：NVIDIA Isaac Sim 模拟的仓库，两台机器人需避开动态行人。
    *   **对比方法**：Stop-and-Go（无通信）、LORC-P（原始流+预测）、LORC-SC（语义+反应式控制）、LORC-SC-P（语义+预测，本文方案）。
*   **Demo II：移动场景下的主动 MCS 控制**
    *   **场景**：基于 Sionna 射线追踪的城市移动环境。
    *   **Benchmark**：理想 MCS、延迟反馈基准（t-1 到 t-30）。
*   **Demo III：真实硬件 FollowMe 机器人**
    *   **场景**：室内走廊，机器人跟随人类。
    *   **对比方法**：固定 JPEG 流、固定 VQ（向量量化）特征、本文的 LLM 驱动动态切换方案。
*   **Demo IV：开放词汇垃圾分类**
    *   **场景**：真实移动机械臂，处理未见过的物体（如纸杯、耳机）。
    *   **对比方法**：闭集检测器（YOLOv8）及其重训练版本。

#### 4. 资源与算力
论文明确列出了部分实验的硬件配置：
*   **服务器端**：AMD Ryzen 5950X CPU，**2张 NVIDIA RTX 3090 GPU**（24GB VRAM）。
*   **模型分配**：GPU 0 运行感知流水线（YOLOv8n、语义编码器）；GPU 1 运行 MLLM 编排器（如 **LLaMA 3 8B**）。
*   **边缘/云端模型**：Demo IV 中使用了 Gemini 1.5 Flash 和 Qwen2.5-VL-7B 进行推理。
*   **训练时长**：文中未详细说明具体模型的训练总时长，但提到了推理延迟（如 LLaMA 3 8B 生成 JSON 配置约需 0.88 秒）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从高保真数字孪生（Isaac Sim）到物理层仿真（Sionna），再到真实硬件（移动底座+机械臂）的完整闭环。
*   **充分性**：实验设计较为全面，不仅测量了传统的通信指标（吞吐量、BLER、RSSI），还测量了任务层指标（任务完成时间、跟踪失败率 UTFR、指令到动作延迟 CTA）。
*   **客观性**：通过消融实验对比了“有无语义压缩”和“有无预测控制”的影响，证明了集成设计的必要性。

#### 6. 主要结论与发现
*   **语义通信的威力**：语义特征传输比原始图像流节省了三个数量级的带宽，且能保持足够的推理精度。
*   **预测优于反应**：在移动环境下，基于语义上下文预测链路质量比依赖过时反馈（CSI）能显著降低延迟抖动和重传。
*   **MLLM 的编排价值**：MLLM 能有效将模糊的语言指令转化为精确的 JSON 配置文件，协调多机器人避障和资源分配。
*   **性能提升**：在仓库场景中，R2X 方案比传统方案显著缩短了任务完成时间；在 FollowMe 任务中实现了 100% 的成功率。

#### 7. 优点（亮点）
*   **全栈集成**：打破了机器人学与通信工程的壁垒，实现了从物理层协议到高层语义推理的端到端联合优化。
*   **意图驱动**：提出了“意图到资源”的映射机制，使系统能根据任务紧急程度自动权衡安全与速度。
*   **软硬结合**：不仅有仿真，还有真实的硬件部署，验证了方案在非理想 WiFi 环境下的鲁棒性。

#### 8. 不足与局限
*   **扩展性挑战**：实验主要集中在 2-3 个机器人的小规模协作，尚未验证在数十甚至上百个机器人高密度竞争环境下的调度表现。
*   **环境泛化性**：语义编码器和链路预测模型可能对特定环境（如特定仓库布局）有依赖，跨场景的零样本迁移能力有待加强。
*   **安全风险**：MLLM 作为核心编排器，可能面临提示词注入（Prompt Injection）或对抗性感知攻击，论文虽有提及但未深入探讨防御机制。
*   **实时性瓶颈**：8B 规模的 LLM 推理仍需近 1 秒，对于极高频（>100Hz）的底层控制循环，仍需依赖本地反射逻辑。

（完）
