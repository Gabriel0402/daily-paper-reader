---
title: "StreamingVLA: Streaming Vision-Language-Action Model with Action Flow Matching and Adaptive Early Observation"
title_zh: StreamingVLA：结合动作流匹配与自适应早期观测的流式视觉-语言-动作模型
authors: "Yiran Shi, Dongqi Guo, Tianchen Zhao, Feng Gao, Liangzhi Shi, Chao Yu, ZhiJian Mo, Qihua Xiao, XiaoShuai Peng, Qingmin Liao, Yu Wang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28565v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于机器人控制的流式视觉-语言-动作模型
tldr: 针对视觉-语言-动作（VLA）模型在边缘设备上因顺序执行导致的延迟和停顿问题，本文提出了StreamingVLA。该模型通过异步并行化处理流程，引入动作流匹配（Action Flow Matching）替代传统分块，并设计了动作显著性感知自适应观测机制。在保持性能的同时，实现了2.4倍的延迟加速和6.5倍的停顿减少，显著提升了机器人执行的流畅度。
selection_source: fresh_fetch
motivation: 现有的VLA模型因观察、生成和执行阶段必须顺序进行，导致在资源受限平台上存在高延迟和频繁停顿。
method: 采用动作流匹配技术重叠生成与执行阶段，并利用自适应早期观测机制重叠执行与观察阶段，实现流式并行处理。
result: 实验表明，StreamingVLA在不损失性能的前提下，实现了2.4倍的延迟加速，并将执行停顿减少了6.5倍。
conclusion: StreamingVLA通过异步并行化架构有效解决了VLA模型的效率瓶颈，为实时、流畅的机器人控制提供了新方案。
---

## 摘要
视觉-语言-动作（VLA）模型在自然语言驱动的感知与控制方面展现出了卓越的性能。然而，VLA 模型的高计算成本带来了显著的效率挑战，特别是在实际部署中资源受限的边缘平台。由于 VLA 的不同阶段（观测、动作生成和执行）必须顺序进行，并等待前一阶段的完成，系统面临频繁的停顿和高延迟。为解决这一问题，我们进行了系统分析以识别快速且流畅生成的挑战，并提出赋予 VLA 以“流式”方式跨阶段异步并行的能力。首先，我们消除了对动作分块（action chunking）的依赖，并采用了动作流匹配（action flow matching），该方法学习动作流的轨迹，而非对分块动作进行去噪。它实现了动作生成与执行延迟的重叠。其次，我们设计了一种动作显著性感知的自适应观测机制，从而实现了执行与观测延迟的重叠。在不牺牲性能的情况下，StreamingVLA 实现了显著的加速并提高了执行的流畅度。它实现了 2.4 倍的延迟加速，并将执行停顿减少了 6.5 倍。

## Abstract
Vision-language-action (VLA) models have demonstrated exceptional performance in natural language-driven perception and control. However, the high computational cost of VLA models poses significant efficiency challenges, particularly for resource-constrained edge platforms in real-world deployments. However, since different stages of VLA (observation, action generation and execution) must proceed sequentially, and wait for the completion of the preceding stage, the system suffers from frequent halting and high latency. To address this, We conduct a systematic analysis to identify the challenges for fast and fluent generation, and propose enabling VLAs with the ability to asynchronously parallelize across VLA stages in a "streaming" manner. First, we eliminate the reliance on action chunking and adopt action flow matching, which learns the trajectory of action flows rather than denoising chunk-wise actions. It overlaps the latency of action generation and execution. Second, we design an action saliency-aware adaptive observation mechanism, thereby overlapping the latency of execution and observation. Without sacrificing performance, StreamingVLA achieves substantial speedup and improves the fluency of execution. It achieves a 2.4 $\times$ latency speedup and reduces execution halting by 6.5 $\times$.

---

## 论文详细总结（自动生成）

以下是对论文《StreamingVLA: Streaming Vision-Language-Action Model with Action Flow Matching and Adaptive Early Observation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在部署时面临严重的**高延迟**和**执行停顿（Halting）**问题。
*   **背景**：传统的 VLA 模型遵循“观测-生成-执行”的顺序循环。即使采用了动作分块（Action Chunking）技术，机器人仍需等待整个动作块生成完毕才能执行，且在执行完当前块、开始下一次观测时会产生明显的停顿，导致动作不连贯、不流畅。
*   **研究动机**：通过将 VLA 的各个阶段异步并行化，实现“流式（Streaming）”执行，从而在不损失性能的前提下提升机器人的反应速度和动作流畅度。

### 2. 方法论：核心思想与关键技术
论文提出了 **StreamingVLA** 框架，通过两个核心技术重叠（Overlap）不同阶段的延迟：

*   **基于状态建模的动作流匹配（Action Flow Matching, AFM）**：
    *   **核心思想**：放弃传统的动作分块去噪（Diffusion），改为学习动作流轨迹。将动作建模为对“物理状态”的更新。
    *   **技术细节**：引入“动作空间状态”，通过预测速度场（Velocity Field）来逐个生成动作。这使得动作生成与执行可以重叠：生成一个动作即可立即执行，无需等待整块生成。
    *   **架构调整**：为了在大型 VLA 模型中保持动作的“可加性”，修改了归一化层（移除偏移项）并引入状态对齐建模，确保训练稳定。
*   **动作显著性感知自适应早期观测（Adaptive Early Observation, AEO）**：
    *   **核心思想**：在当前动作序列执行完之前，提前启动下一轮的 VLM 观测处理。
    *   **关键技术**：并非所有动作对环境的影响都一致。论文设计了一个轻量级的**显著性预测器**（基于 Transformer），预测剩余动作对视觉特征的影响。如果预测的视觉变化较小（低显著性），则触发早期观测；反之则等待，以避免因环境剧变导致的感知错位。

### 3. 实验设计
*   **数据集/场景**：主要在 **LIBERO** 仿真基准上进行，涵盖四个子任务集：Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程），共 40 个不同任务。
*   **Benchmark**：以 **$\pi_{0.5}$**（先进的流匹配 VLA 模型）作为基础模型。
*   **对比方法**：
    *   **RTC**（实时分块）、**SmolVLA**（轻量级 VLA）、**VLASH**（异步推理优化）、**Temporal Ensembling**（时间集成）。
*   **真实世界验证**：在 Franka Panda 机器人上进行了“取放物体”任务的实机部署。

### 4. 资源与算力
*   **硬件**：使用 4 台 **NVIDIA A800-SXM4-80GB** GPU 进行训练。
*   **训练时长**：
    *   主模型训练：60,000 次迭代，耗时约 **10-20 GPU 小时**。
    *   自适应预测器训练：54,000 次迭代，耗时约 **3-4 GPU 小时**。
*   **推理开销**：自适应预测器带来的额外计算开销仅占模型推理时间的约 5%。

### 5. 实验数量与充分性
*   **实验规模**：每个任务进行 50 次 Rollout（总计 2000 次测试），涵盖了成功率、单步动作延迟（$T_{action}$）和停顿间隙（$T_{halt}$）等多个指标。
*   **消融实验**：系统地验证了动作流匹配（AFM）、归一化修改（NM）、状态对齐（SA）和自适应观测（EO）的有效性。
*   **评价**：实验设计较为充分，对比了当前主流的加速方案，且包含了仿真与实机的双重验证，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能保持**：StreamingVLA 在 LIBERO 上的平均成功率为 94.9%，与基准模型（95.1%）几乎持平，证明了异步并行化不会牺牲精度。
*   **显著加速**：实现了 **2.4 倍** 的端到端延迟加速。
*   **流畅度提升**：将执行停顿时间减少了 **6.5 倍**（从约 230ms 降至 36ms），使机器人动作更加丝滑。
*   **自适应的重要性**：相比于盲目的早期观测（成功率仅 86.2%），自适应机制能有效识别关键动作，维持高成功率。

### 7. 优点
*   **系统性分析**：论文对 VLA 运行时的瓶颈进行了深入的量化分析，找出了优化目标。
*   **创新性建模**：将动作流匹配引入 VLA 并解决了大模型下的训练稳定性问题。
*   **实用性强**：不依赖于复杂的模型压缩（如量化、剪枝），而是通过调度优化实现加速，且能与压缩技术叠加使用。

### 8. 不足与局限
*   **任务复杂度限制**：虽然在 LIBERO 上表现优异，但该基准仍属于相对受控的环境。在极度动态或不可预测的环境中，动作显著性预测器的准确性可能面临挑战。
*   **硬件依赖**：虽然旨在优化边缘部署，但实验主要在高性能 A800 上完成，在更低端的嵌入式设备（如 Jetson）上的实际表现仍需进一步验证。
*   **应用范围**：目前主要针对机械臂操纵任务，对于需要极高频率反馈的足式机器人等场景的适用性尚未讨论。

（完）
