---
title: "DFM-VLA: Iterative Action Refinement for Robot Manipulation via Discrete Flow Matching"
title_zh: DFM-VLA：通过离散流匹配实现机器人操纵的迭代动作细化
authors: "Jiayi Chen, Wenxuan Song, Shuai Chen, Jingbo Wang, Zhijun Li, Haoang Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26320v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于机器人操作中迭代动作细化的离散流匹配VLA
tldr: 针对现有视觉-语言-动作（VLA）模型在离散动作生成中无法修正早期错误的问题，本文提出了DFM-VLA。该模型利用离散流匹配技术，通过建模动作序列的概率速度场实现迭代优化。结合两阶段解码策略，DFM-VLA在CALVIN和LIBERO等基准测试中显著超越了自回归和扩散模型基准，在提升机器人操控准确性的同时保持了高效推理。
selection_source: fresh_fetch
motivation: 现有的离散动作解码范式（如自回归或离散扩散）一旦生成动作标记便无法修改，导致早期错误难以纠正。
method: 提出一种基于离散流匹配的VLA框架，通过概率速度场动态更新动作序列，并采用迭代细化与确定性验证的两阶段解码策略。
result: "在CALVIN上达到4.44的平均成功长度，在LIBERO上达到95.7%的成功率，性能显著优于自回归及各类扩散模型。"
conclusion: 离散流匹配为机器人操控中的动作细化提供了有效途径，在保持高推理效率的同时显著提升了任务成功率。
---

## 摘要
使用离散分词方案编码动作的视觉-语言-动作（VLA）模型正越来越多地被应用于机器人操纵，但现有的解码范式仍存在根本性的局限。无论是由自回归 VLA 顺序解码动作，还是由离散扩散 VLA 并行解码动作，一旦生成一个 token，它通常就是固定的，无法在后续迭代中修改，因此早期的 token 错误无法在后期得到有效纠正。我们提出了 DFM-VLA，一种用于动作 token 迭代细化的离散流匹配 VLA。DFM-VLA 建模了一个 token 级的概率速度场，在迭代细化过程中动态更新整个动作序列。我们研究了两种构建速度场的方法：辅助速度头公式化和动作嵌入引导公式化。我们的框架进一步采用了一种两阶段解码策略，包括一个迭代细化阶段，随后是用于稳定收敛的确定性验证。在 CALVIN、LIBERO 和真实世界操纵任务上的大量实验表明，DFM-VLA 在保持高推理效率的同时，在操纵性能上始终优于强大的自回归、离散扩散和连续扩散基准模型。特别是，DFM-VLA 在 CALVIN 上实现了 4.44 的平均成功长度，在 LIBERO 上实现了 95.7% 的平均成功率，突显了通过离散流匹配进行动作细化对机器人操纵的价值。我们的项目可在 https://chris1220313648.github.io/DFM-VLA/ 获取。

## Abstract
Vision--Language--Action (VLA) models that encode actions using a discrete tokenization scheme are increasingly adopted for robotic manipulation, but existing decoding paradigms remain fundamentally limited. Whether actions are decoded sequentially by autoregressive VLAs or in parallel by discrete diffusion VLAs, once a token is generated, it is typically fixed and cannot be revised in subsequent iterations, so early token errors cannot be effectively corrected later. We propose DFM-VLA, a discrete flow matching VLA for iterative refinement of action tokens. DFM-VLA~models a token-level probability velocity field that dynamically updates the full action sequence across refinement iterations. We investigate two ways to construct the velocity field: an auxiliary velocity-head formulation and an action-embedding-guided formulation. Our framework further adopts a two-stage decoding strategy with an iterative refinement stage followed by deterministic validation for stable convergence. Extensive experiments on CALVIN, LIBERO, and real-world manipulation tasks show that DFM-VLA consistently outperforms strong autoregressive, discrete diffusion, and continuous diffusion baselines in manipulation performance while retaining high inference efficiency. In particular, DFM-VLA achieves an average success length of 4.44 on CALVIN and an average success rate of 95.7\% on LIBERO, highlighting the value of action refinement via discrete flow matching for robotic manipulation. Our project is available https://chris1220313648.github.io/DFM-VLA/

---

## 论文详细总结（自动生成）

### DFM-VLA：通过离散流匹配实现机器人操纵的迭代动作细化

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型在处理离散动作 token 时，主要采用**自回归（AR）**或**离散扩散（DD）**两种范式。然而，这两种方法都面临“**不可逆承诺（Irreversible Commitment）**”问题：
*   **自回归模型**按顺序生成 token，一旦某个位置的 token 被确定，后续生成无法对其进行修正。
*   **离散扩散模型**虽然支持并行生成，但在推理过程中，一旦某个掩码位置被填充，通常在后续迭代中也会被固定。
这种局限性导致早期的解码错误会随时间累积，严重影响机器人长程任务的成功率。本文提出的 **DFM-VLA** 旨在通过**离散流匹配（Discrete Flow Matching）**技术，允许模型在迭代过程中对整个动作序列进行持续的修正和细化。

#### 2. 方法论：核心思想与关键技术
DFM-VLA 的核心是将动作生成建模为一个在离散空间上的概率流过程，通过学习“速度场”来指导 token 的演变。

*   **核心思想**：模型不再是一次性预测动作，而是学习一个 token 级的概率速度场（Probability Velocity Field），在推理时通过多次迭代动态更新完整的动作序列。
*   **速度场构建（两种变体）**：
    1.  **辅助速度头（Auxiliary Velocity Head）**：通过一个额外的预测头从模型隐藏状态中直接预测 token 替换的速度。
    2.  **动作嵌入引导（Action-Embedding-Guided）**：利用动作 token 在嵌入空间中的语义距离定义概率路径，推导出动力学最优速度。实验证明这种方式收敛更快、性能更强。
*   **两阶段解码策略**：
    1.  **迭代细化阶段**：利用连续时间马尔可夫链（CTMC）的欧拉离散化进行随机跳转更新，允许模型探索和修正。
    2.  **确定性验证阶段**：在最后几步切换为贪婪解码，以确保动作序列的稳定收敛。
*   **效率优化**：引入**自适应 KV 缓存（Adaptive KV Caching）**，根据动作 token 的变化程度动态更新缓存，在保持性能的同时实现 2.4 倍的推理加速。

#### 3. 实验设计
论文在仿真环境和真实世界中进行了广泛评估：
*   **数据集/场景**：
    *   **CALVIN**：评估长程操纵能力（ABCD→D 设置），关注连续完成子任务的平均长度。
    *   **LIBERO**：评估跨任务泛化能力，包含 Spatial、Object、Goal 和 Long 四个子集。
    *   **真实世界**：使用双臂协作平台，执行“协作抬锅”、“蔬菜入锅”、“方块放置”三个任务。
*   **对比方法（Benchmark）**：
    *   **自回归基准**：RT-1, OpenVLA, UniVLA, π0-FAST 等。
    *   **扩散模型基准**：Dream-VLA, RDT, Discrete Diffusion VLA 等。
    *   **其他**：ReconVLA, ThinkAct, FlowVLA 等增强感知或推理的模型。

#### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   仿真环境（CALVIN/LIBERO）：训练 20k 至 32k 步。
    *   真实世界任务：每个任务训练 5k 步。
    *   学习率：1e-4，Batch Size 为 8。
    *   初始化：从预训练的机器人视频模型权重开始微调。

#### 5. 实验数量与充分性
实验设计非常详尽且具有说服力：
*   **基准测试**：在 CALVIN 上进行了 1000 次 rollout，在 LIBERO 每个任务上进行了 50 次 rollout，确保了统计意义。
*   **消融实验**：针对超参数（c 和 α）、两阶段解码的步数分配、速度场构建方式、以及**训练数据规模（10%, 50%, 100%）**的影响进行了深入分析。
*   **公平性**：在对比实验中，确保了 DFM 与离散扩散模型在相同的解码步数（16步）下进行比较。

#### 6. 主要结论与发现
*   **性能领先**：DFM-VLA 在 CALVIN 上达到了 **4.44** 的平均成功长度，在 LIBERO 上达到了 **95.7%** 的平均成功率，均刷新了 SOTA 记录。
*   **纠错能力**：实验证实，DFM 的迭代细化机制能有效修正早期 token 错误，尤其在长程任务中表现出更强的动作一致性。
*   **数据效率**：在仅有 10% 训练数据的情况下，DFM-VLA 的表现显著优于自回归和扩散模型，显示出极强的学习效率。
*   **真实世界有效性**：在双臂协作等复杂任务中，DFM-VLA 的成功率（70.8%）大幅领先于连续扩散模型 RDT（60.0%）。

#### 7. 优点（亮点）
*   **理论创新**：首次将离散流匹配（DFM）引入 VLA 领域，解决了离散动作生成的“不可逆”痛点。
*   **设计优雅**：嵌入引导的速度场构建方法充分利用了动作空间的语义结构。
*   **兼顾效率与性能**：通过两阶段解码和自适应缓存，在提升准确性的同时，推理速度优于传统的自回归模型。

#### 8. 不足与局限
*   **迭代开销**：虽然比自回归快，但相比于单步预测模型，DFM 仍需要多次迭代（如 16 步），在极高频控制场景下可能存在延迟压力。
*   **任务复杂度**：真实世界实验虽然包含双臂协作，但任务种类（3种）相对较少，尚需在更广泛、更复杂的非结构化环境中验证。
*   **依赖预训练**：模型性能在很大程度上依赖于高质量的视觉-语言预训练权重。

（完）
