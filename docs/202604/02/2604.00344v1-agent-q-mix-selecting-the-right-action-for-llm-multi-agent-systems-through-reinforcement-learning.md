---
title: "Agent Q-Mix: Selecting the Right Action for LLM Multi-Agent Systems through Reinforcement Learning"
title_zh: Agent Q-Mix：通过强化学习为大语言模型多智能体系统选择正确的动作
authors: "Eric Hanchen Jiang, Levina Li, Rui Sun, Xiao Liang, Yubei Li, Yuchen Wu, Haozheng Luo, Hengli Li, Zhi Zhang, Zhaolu Kang, Kai-Wei Chang, Ying Nian Wu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00344v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: LLM多智能体系统中拓扑选择的强化学习
tldr: 针对大语言模型多智能体系统中的拓扑选择与协作难题，本文提出 Agent Q-Mix 框架。该方法将智能体间的通信决策建模为协作式多智能体强化学习问题，利用 QMIX 价值分解实现去中心化的通信拓扑优化。通过结合 GNN 编码器与 CTDE 范式，在平衡任务准确率与 Token 成本的同时，显著提升了系统在复杂推理任务中的表现与鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决多智能体系统中如何有效选择和互连智能体以应对复杂任务的挑战。
method: 将拓扑选择重构为多智能体强化学习问题，利用 QMIX 价值分解和 GNN 编码器学习去中心化的通信决策。
result: 在编程、推理和数学等七个基准测试中取得最高平均准确率，且在 Token 效率和抗故障鲁棒性方面表现优异。
conclusion: 研究证明了学习到的去中心化拓扑优化能有效提升多智能体系统的推理边界，是构建高效协作系统的关键。
---

## 摘要
大语言模型（LLMs）在完成各种任务方面表现出了卓越的性能。然而，解决复杂问题通常需要多个智能体的协作，这提出了一个根本性问题：如何有效地选择并互连这些智能体。在本文中，我们提出了 Agent Q-Mix，这是一个强化学习框架，它将拓扑选择重新表述为一个协作式多智能体强化学习（MARL）问题。我们的方法利用 QMIX 价值分解来学习去中心化的通信决策，其中每个智能体从一组通信动作中进行选择，这些动作共同诱导产生逐轮的通信图。Agent Q-Mix 的核心是在中心化训练与去中心化执行（CTDE）范式下，结合了拓扑感知的 GNN 编码器、GRU 记忆单元以及每个智能体的 Q 头。该框架优化了一个平衡任务准确率与 token 成本的奖励函数。在涵盖编程、推理和数学的七个核心基准测试中，与现有方法相比，Agent Q-Mix 实现了最高的平均准确率，同时展示了卓越的 token 效率以及针对智能体故障的鲁棒性。值得注意的是，在以 Gemini-3.1-Flash-Lite 为骨干网络的具有挑战性的 Humanity's Last Exam (HLE) 测试中，Agent Q-Mix 达到了 20.8% 的准确率，优于 Microsoft Agent Framework (19.2%) 和 LangGraph (19.2%)，随后是 AutoGen 和 OpenClaw 的 Lobster。这些结果强调了学习型去中心化拓扑优化在拓展多智能体推理边界方面的有效性。

## Abstract
Large Language Models (LLMs) have shown remarkable performance in completing various tasks. However, solving complex problems often requires the coordination of multiple agents, raising a fundamental question: how to effectively select and interconnect these agents. In this paper, we propose \textbf{Agent Q-Mix}, a reinforcement learning framework that reformulates topology selection as a cooperative Multi-Agent Reinforcement Learning (MARL) problem. Our method learns decentralized communication decisions using QMIX value factorization, where each agent selects from a set of communication actions that jointly induce a round-wise communication graph. At its core, Agent Q-Mix combines a topology-aware GNN encoder, GRU memory, and per-agent Q-heads under a Centralized Training with Decentralized Execution (CTDE) paradigm. The framework optimizes a reward function that balances task accuracy with token cost. Across seven core benchmarks in coding, reasoning, and mathematics, Agent Q-Mix achieves the highest average accuracy compared to existing methods while demonstrating superior token efficiency and robustness against agent failure. Notably, on the challenging Humanity's Last Exam (HLE) using Gemini-3.1-Flash-Lite as a backbone, Agent Q-Mix achieves 20.8\% accuracy, outperforming Microsoft Agent Framework (19.2\%) and LangGraph (19.2\%), followed by AutoGen and Lobster by OpenClaw. These results underscore the effectiveness of learned, decentralized topology optimization in pushing the boundaries of multi-agent reasoning.

---

## 论文详细总结（自动生成）

### 论文总结：Agent Q-Mix —— 通过强化学习优化大语言模型多智能体通信拓扑

#### 1. 核心问题与整体含义（研究动机和背景）
在解决复杂任务时，多智能体系统（MAS）通常优于单个大语言模型（LLM）。然而，**如何确定智能体之间的通信拓扑（即谁该和谁说话、何时说话）**是一个核心难题。
*   **现有局限：** 静态方法（如链式、全连接）缺乏灵活性，浪费 Token 或协作不足；现有的自适应方法多依赖中心化生成器，导致执行时缺乏灵活性，且难以量化单个智能体对全局目标的贡献。
*   **研究动机：** 提出一种去中心化的决策框架，让每个智能体根据当前状态自主选择通信动作，从而动态构建最优拓扑，平衡任务准确率、Token 成本和系统鲁棒性。

#### 2. 方法论：核心思想与关键技术
Agent Q-Mix 将拓扑选择建模为**联网多智能体马尔可夫决策过程（Networked MMDP）**，并采用 **QMIX** 算法进行求解。
*   **核心思想：** 采用“中心化训练、去中心化执行”（CTDE）范式。训练时利用全局信息优化，执行时每个智能体仅凭局部观察和历史记忆做出通信决策。
*   **关键技术细节：**
    *   **离散动作空间：** 定义了 6 种具有图论意义的动作：`solo process`（独立工作）、`broadcast all`（广播）、`selective query`（定向查询）、`aggregate refine`（聚合精炼）、`execute verify`（转发验证）、`debate check`（双向辩论）。
    *   **模型架构：** 采用 **GNN-GRU-MLP** 堆栈。GNN 编码当前拓扑结构，GRU 维护多轮通信的时间记忆，MLP 输出每个动作的 Q 值。
    *   **价值分解（QMIX）：** 使用单调混合网络将各智能体的局部 Q 值整合为全局 $Q_{tot}$，确保去中心化决策与全局最优的一致性。
    *   **奖励函数：** $R = w_{acc} \cdot \text{accuracy} - w_{tok} \cdot \min(\frac{\text{tokens}}{\text{budget}}, 1)$，显式惩罚过度通信。

#### 3. 实验设计
*   **数据集/场景：** 涵盖三大领域共 7 个核心基准测试：
    *   **编程：** LiveCodeBench (v6), HumanEval。
    *   **推理：** MMLU-Pro。
    *   **数学：** AIME 2025/2026, Beyond-AIME, HMMT 2025。
    *   **挑战性测试：** Humanity’s Last Exam (HLE)。
*   **骨干模型：** GPT-OSS:120B 和 Gemini-3.1-Flash-Lite。
*   **对比方法：** 包括单智能体基准、静态多智能体（LLM-Debate）、自适应拓扑方法（GPTSwarm, G-Designer, GTD, TopoDIM 等）以及商业框架（AutoGen, LangGraph, Microsoft Agent Framework）。

#### 4. 资源与算力
*   **训练规模：** 每个领域仅需 15 个训练样本，进行 50 个 Episode 的训练。
*   **算力消耗：** 论文明确提到，由于主要计算开销在于 LLM API 调用而非神经网络更新，在**单台 CPU** 上每个领域的训练时长仅需 **30-60 分钟**。
*   **GPU 使用：** 未提及具体 GPU 型号，暗示该框架对本地深度学习算力要求极低，主要依赖 API。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了跨 8 个数据集的性能对比、针对 MMLU-Pro 的鲁棒性实验（注入对抗性智能体）、Token 效率分析以及 4 组消融实验（智能体数量、训练数据量、奖励权重、通信轮数）。
*   **客观性与公平性：** 实验结果取 3 次独立运行的中位数；对比基准涵盖了当前最前沿的学术和工业框架；消融实验验证了模型在极小样本（15个）下的泛化能力，设计较为科学、客观。

#### 6. 主要结论与发现
*   **性能领先：** Agent Q-Mix 在所有基准测试中均取得最高平均准确率。在 HLE 挑战中以 20.8% 的准确率超越了微软框架和 LangGraph。
*   **Token 高效：** 相比其他多智能体方法，Token 消耗降低了 4 到 24 倍，证明了“按需通信”的有效性。
*   **强鲁棒性：** 当系统中存在故意提供错误答案的“恶意智能体”时，Agent Q-Mix 能通过学习到的拓扑自动隔离该智能体，准确率仅下降 2.86%，远低于其他方法。
*   **数学任务增益最大：** 在需要多轮严密推理的数学竞赛题目中，动态拓扑的优势最为显著。

#### 7. 优点：亮点总结
*   **去中心化决策：** 真正实现了智能体在执行阶段的自主通信选择，而非预设脚本。
*   **动作空间可解释：** 6 种动作直接对应图结构，易于理解智能体的协作逻辑。
*   **极高的样本效率：** 仅需 15 个样本即可训练出泛化性强的策略，降低了微调成本。
*   **CTDE 范式的成功应用：** 将经典的 MARL 算法（QMIX）成功引入 LLM 领域，解决了拓扑优化的信用分配问题。

#### 8. 不足与局限
*   **训练样本偏差：** 虽然 15 个样本表现良好，但在更极端或更多样化的任务分布下，极小样本训练可能存在过拟合风险。
*   **API 依赖：** 训练和评估高度依赖闭源模型 API，成本和速度受限于供应商。
*   **智能体角色固定：** 实验中智能体的角色（如 CodeWriter, MathSolver）是预设的，框架尚未实现智能体“角色/身份”的动态演化。
*   **通信轮数限制：** 实验中通信轮数（2-3轮）是手动设定的超参数，而非由智能体根据任务难度自动决定何时停止。

（完）
