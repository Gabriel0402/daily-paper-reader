---
title: Robust Multi-Agent Reinforcement Learning for Small UAS Separation Assurance under GPS Degradation and Spoofing
title_zh: 针对 GPS 性能下降与欺骗下小型无人机系统间隔保证的鲁棒多智能体强化学习
authors: "Alex Zongo, Filippos Fotiadis, Ufuk Topcu, Peng Wei"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28900v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 多智能体强化学习中代理与对手之间的零和博弈
tldr: "针对小型无人机系统（sUAS）在GPS受损或欺骗环境下的间隔保证问题，本文提出了一种鲁棒多智能体强化学习（MARL）方法。研究将状态观测污染建模为智能体与对手之间的零和博弈，推导出对抗扰动的闭式表达式，从而绕过对抗训练并实现线性时间评估。通过将该鲁棒策略集成到策略梯度算法中，在高达35%的污染水平下实现了近乎零的碰撞率，显著提升了高密度飞行环境下的安全性。"
selection_source: fresh_fetch
motivation: 旨在解决小型无人机在GPS信号受损或遭受欺骗时，因广播位置信息不可靠而导致的空中交通安全间隔失效问题。
method: 将状态污染建模为零和博弈，推导出对抗扰动的闭式表达式，并将其集成到MARL策略梯度算法中以训练鲁棒的反制策略。
result: "在高密度无人机仿真实验中，该方法在观测污染率高达35%的情况下仍能保持近乎零的碰撞率，性能显著优于传统策略。"
conclusion: 该研究通过数学推导简化了对抗训练过程，为受干扰环境下的多无人机协同安全控制提供了一种高效且具有理论保障的鲁棒方案。
---

## 摘要
我们通过多智能体强化学习（MARL）解决了在 GPS 性能下降和欺骗情况下小型无人机系统（sUAS）的鲁棒间隔保证问题。在协作监视中，每架飞机（或智能体）广播其基于 GPS 的位置；当此类位置广播受到干扰时，整个观测到的空中交通状态将变得不可靠。我们将这种状态观测干扰建模为智能体与对手之间的零和博弈：对手以概率 R 扰动观测状态，以最大限度地降低每个智能体的安全性能。我们推导出了这种对抗性扰动的闭式表达式，完全绕过了对抗性训练，并实现了在状态维度上的线性时间评估。我们证明了该表达式以二阶精度逼近真实的最佳对抗扰动。我们进一步限定了干净观测与受干扰观测之间的安全性能差距，证明在 Kullback-Leibler 正则化下，该差距随干扰概率的增加至多呈线性退化。最后，我们将闭式对抗策略集成到 MARL 策略梯度算法中，为智能体获得鲁棒的对抗策略。在高密度 sUAS 仿真中，我们观察到在干扰水平高达 35% 的情况下，碰撞率接近于零，优于未经对抗扰动训练的基准策略。

## Abstract
We address robust separation assurance for small Unmanned Aircraft Systems (sUAS) under GPS degradation and spoofing via Multi-Agent Reinforcement Learning (MARL). In cooperative surveillance, each aircraft (or agent) broadcasts its GPS-derived position; when such position broadcasts are corrupted, the entire observed air traffic state becomes unreliable. We cast this state observation corruption as a zero-sum game between the agents and an adversary: with probability R, the adversary perturbs the observed state to maximally degrade each agent's safety performance. We derive a closed-form expression for this adversarial perturbation, bypassing adversarial training entirely and enabling linear-time evaluation in the state dimension. We show that this expression approximates the true worst-case adversarial perturbation with second-order accuracy. We further bound the safety performance gap between clean and corrupted observations, showing that it degrades at most linearly with the corruption probability under Kullback-Leibler regularization. Finally, we integrate the closed-form adversarial policy into a MARL policy gradient algorithm to obtain a robust counter-policy for the agents. In a high-density sUAS simulation, we observe near-zero collision rates under corruption levels up to 35%, outperforming a baseline policy trained without adversarial perturbations.

---

## 论文详细总结（自动生成）

### 针对 GPS 性能下降与欺骗下小型无人机系统间隔保证的鲁棒多智能体强化学习

#### 1. 核心问题与整体含义（研究动机和背景）
随着小型无人机系统（sUAS）在城市物流和应急响应中的规模化应用，自主间隔保证（即自动避障）变得至关重要。然而，现有的自主导航高度依赖 GPS 进行定位和协作式位置共享（如 Remote ID）。在城市环境中，GPS 信号极易受到多路径干扰（误差达 10-60 米）或恶意欺骗（成功率可达 5-40%）。当 GPS 受损时，无人机不仅自身定位错误，获取的邻机位置信息也是错误的，导致整个交通态势感知失效。本文旨在研究如何在观测数据不可靠的情况下，通过鲁棒多智能体强化学习（MARL）确保高密度无人机群的飞行安全。

#### 2. 方法论：核心思想与关键技术
论文将该问题建模为智能体与对手之间的**零和博弈**，并引入了 **R-污染（R-contamination）模型**：
*   **核心思想**：假设观测状态以概率 $R$ 被对手篡改。对手的目标是在给定的扰动范围 $\kappa$ 内，找到能使智能体安全性能（价值函数）最小化的状态扰动。
*   **闭式对抗扰动（Closed-form Perturbation）**：研究者利用价值函数的一阶泰勒展开，推导出最优对抗扰动的解析解。这意味着无需额外训练一个复杂的对抗网络，即可在每一轮训练中快速计算出“最坏情况”的观测值，计算复杂度随状态维度线性增长。
*   **鲁棒 PPO 算法**：将上述闭式扰动集成到近端策略优化（PPO）算法中。
*   **正则化机制**：
    *   **扰动不变性正则化（Invariance）**：通过 KL 散度约束，要求策略在面对“干净”和“受污染”观测时做出相似的决策。
    *   **锚定正则化（Anchoring）**：将鲁棒策略与预训练的标称策略对齐，防止在追求鲁棒性时过度牺牲正常情况下的性能。
*   **理论保障**：证明了性能损失随污染概率 $R$ 呈线性退化，且一阶近似误差具有二阶精度。

#### 3. 实验设计
*   **场景设置**：使用 **BlueSky 空中交通模拟器** 构建了一个高密度城市无人机物流配送环境，包含多条交叉和合并的航线。
*   **无人机模型**：模拟亚马逊 MK30 无人机，具备非线性动力学特征，巡航速度 20 m/s。
*   **Benchmark（基准）**：对比了在无噪声环境下训练的**标称 PPO 策略（Nominal Policy）**。
*   **评估指标**：近距离空中碰撞（NMAC）次数、平均最小间隔距离。
*   **消融实验**：分别验证了“仅使用锚定”、“仅使用不变性”以及“全功能”模型的表现。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或总训练时长。
*   文中提到训练步数为 $2 \times 10^6$ 步，学习率为 $2.5 \times 10^{-4}$，采用 8 个 epoch 进行更新。由于采用了闭式解而非对抗训练，其计算开销应显著低于传统的对抗强化学习方法。

#### 5. 实验数量与充分性
*   **实验规模**：每个数据点均基于 **100 组独立模拟回合** 的平均值，并给出了标准差（阴影区域），具有较好的统计说服力。
*   **覆盖范围**：测试了污染率 $R$ 从 0 到 1.0 的全区间表现，并针对扰动范围 $\kappa$ 设定了符合城市 GPS 误差实际情况的参数（如位置误差 60m，速度误差 2m/s）。
*   **充分性评价**：实验设计较为充分，不仅对比了基准方法，还通过消融实验深入探讨了不同正则化项对系统稳定性的贡献，验证了理论推导的有效性。

#### 6. 主要结论与发现
*   **高鲁棒性**：在 GPS 污染率高达 **35%** 的情况下，鲁棒策略仍能保持**近乎零的碰撞率**。
*   **优雅降级**：当污染率进一步提升至 95% 时，标称策略的碰撞次数飙升至每回合 18 次，而鲁棒策略仅为 5 次左右，表现出更强的生存能力。
*   **性能权衡**：在完全无干扰（R=0）的情况下，鲁棒策略的性能与标称策略相当，证明该方法没有以牺牲正常性能为代价换取鲁棒性。
*   **正则化必要性**：消融实验显示，缺乏锚定正则化会导致训练不稳定，而缺乏不变性正则化则无法应对高强度攻击。

#### 7. 优点
*   **计算高效**：通过数学推导给出了对抗扰动的闭式解，避免了昂贵的对抗样本生成过程。
*   **理论支撑**：不仅有仿真实验，还提供了关于性能边界和近似精度的数学证明。
*   **去中心化执行**：虽然是集中式训练，但执行时每个无人机仅依赖局部（可能受损的）观测，符合实际部署需求。

#### 8. 不足与局限
*   **一阶近似限制**：对抗扰动的计算依赖于价值函数的一阶泰勒展开，如果价值函数在某些区域高度非线性，该近似可能失效。
*   **环境单一**：实验主要在结构化的航路点（Waypoints）环境下进行，对于完全随机、无航路约束的自由飞行场景验证不足。
*   **GPS 攻击模型简化**：假设 GPS 欺骗是随机发生的（R-污染），但在现实中，高级攻击者可能会根据无人机的轨迹进行有针对性的连续诱导，这种时序相关的攻击在文中未深入讨论。

（完）
