---
title: "GraSP-STL: A Graph-Based Framework for Zero-Shot Signal Temporal Logic Planning via Offline Goal-Conditioned Reinforcement Learning"
title_zh: GraSP-STL：一种通过离线目标条件强化学习实现零样本信号时序逻辑规划的基于图的框架
authors: "Ancheng Hou, Ruijia Liu, Xiang Yin"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29533v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 信号时态逻辑(STL)规范下的零样本规划
tldr: 本研究针对离线、零样本信号时序逻辑（STL）规划问题，提出了GraSP-STL框架。该方法仅利用任务无关的离线数据集，无需动力学模型或环境交互。通过学习目标条件价值函数构建状态空间图抽象，将STL规划转化为图搜索问题，并利用算术几何平均鲁棒性进行评估。该框架实现了长时程规划和对未知STL任务的零样本泛化，显著提升了离线强化学习在复杂逻辑任务中的表现。
selection_source: fresh_fetch
motivation: 旨在解决在无动力学模型且无法进行环境交互的情况下，如何利用离线数据实现对任意未知STL规范的零样本规划。
method: 通过离线数据学习目标条件价值函数以构建图抽象，并结合算术几何平均鲁棒性进行图搜索来生成路径点序列。
result: 实验结果表明，该框架在多种离线STL规划任务中表现出色，能够有效组合短时程行为以完成长时程复杂任务。
conclusion: GraSP-STL成功分离了可重用的可达性学习与任务相关的规划过程，为离线强化学习下的复杂逻辑任务提供了高效的零样本解决方案。
---

## 摘要
本文研究了信号时序逻辑（STL）规范下的离线零样本规划。我们假设仅能访问由任务无关的行为策略收集的状态-动作-状态转移离线数据集，且不具备解析动力学模型，不进行进一步的环境交互，也不进行针对特定任务的重训练。目标是合成一种控制策略，使其生成的轨迹满足任意未见的 STL 规范。为此，我们提出了 GraSP-STL，这是一个基于图搜索的框架，用于从离线轨迹中进行零样本 STL 规划。该方法从离线数据中学习目标条件价值函数，并利用其在状态空间上诱导出有限时域可达性度量。基于该度量，它构建了一个有向图抽象，其节点表示代表性状态，边编码了可行的短时域转移。规划随后被表述为对路标点序列的图搜索，使用算术-几何平均鲁棒性及其区间语义进行评估，并由学习到的目标条件策略执行。所提出的框架将可重用的可达性学习与任务条件规划分离，从而实现了对未见 STL 任务的零样本泛化，并通过组合离线数据中的短时域行为实现长时域规划。实验结果证明了该方法在一系列离线 STL 规划任务中的有效性。

## Abstract
This paper studies offline, zero-shot planning under Signal Temporal Logic (STL) specifications. We assume access only to an offline dataset of state-action-state transitions collected by a task-agnostic behavior policy, with no analytical dynamics model, no further environment interaction, and no task-specific retraining. The objective is to synthesize a control strategy whose resulting trajectory satisfies an arbitrary unseen STL specification. To this end, we propose GraSP-STL, a graph-search-based framework for zero-shot STL planning from offline trajectories. The method learns a goal-conditioned value function from offline data and uses it to induce a finite-horizon reachability metric over the state space. Based on this metric, it constructs a directed graph abstraction whose nodes represent representative states and whose edges encode feasible short-horizon transitions. Planning is then formulated as a graph search over waypoint sequences, evaluated using arithmetic-geometric mean robustness and its interval semantics, and executed by a learned goal-conditioned policy. The proposed framework separates reusable reachability learning from task-conditioned planning, enabling zero-shot generalization to unseen STL tasks and long-horizon planning through the composition of short-horizon behaviors from offline data. Experimental results demonstrate its effectiveness on a range of offline STL planning tasks.

---

## 论文详细总结（自动生成）

### 论文总结：GraSP-STL 框架

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了在**离线（Offline）**且**零样本（Zero-Shot）**设置下的信号时序逻辑（STL）规划问题。
*   **研究动机**：传统的 STL 规划通常依赖精确的动力学模型或大量的在线环境交互，但在实际应用中，模型往往难以获取，且在线试错成本极高。
*   **核心挑战**：如何在仅有任务无关（Task-agnostic）的离线数据集（即碎片化的状态-动作转移数据）的情况下，合成能够满足任意、未见过的复杂 STL 规范的控制策略，且无需针对新任务进行重新训练。

#### 2. 方法论：核心思想与技术细节
GraSP-STL 提出了一种将**可重用的可达性学习**与**任务条件规划**分离的分层架构：
*   **目标条件强化学习（GCRL）**：从离线数据中学习目标条件价值函数 $V(s, g)$ 和策略 $\pi(a|s, g)$。价值函数被转化为一种有限时域的可达性度量 $\hat{d}(s, g) = -V(s, g)$，用于估计状态间的转移步数。
*   **图抽象构建**：
    *   **节点**：通过对状态空间进行几何均匀采样和时间距离感知聚类，提取代表性状态作为图节点。
    *   **边**：若两个节点间的估计距离小于预设时域 $k$，则建立有向边，并采用方向多样性策略进行稀疏化处理。
*   **基于 AGM 鲁棒性的图搜索**：
    *   将规划表述为在增强状态空间（图节点 + STL 监控状态）上的搜索。
    *   引入**算术-几何平均（AGM）鲁棒性**及其区间语义，并将其扩展到支持任意嵌套的 STL 公式。
    *   利用鲁棒性区间的上下界进行剪枝（剔除无法满足规范的路径）和启发式引导。
*   **执行**：搜索生成的路标点（Waypoints）序列由底层的目标条件策略执行。

#### 3. 实验设计
*   **实验场景**：使用了 **OGBench** 中的 **ANTMAZE** 环境。这是一个具有 29 维状态空间的 8 自由度四足机器人，任务是在包含障碍物的 2D 迷宫中移动。
*   **数据集**：包含 10,000 条由任务无关策略收集的轨迹（每条长度 500 步）。
*   **Benchmark 与任务**：设计了 12 种 STL 公式模板（T1-T12），涵盖基础（到达、析取）、中级（顺序、安全约束）和高级（分支、持久性、嵌套逻辑）三个难度等级。
*   **对比维度**：主要评估规划成功率（PSR）、执行成功率（ESR）和规划时间（PT）。

#### 4. 资源与算力
*   论文中**未明确说明**具体的 GPU 型号、数量或训练总时长。通常此类基于离线 RL 和图搜索的实验在单张消费级 GPU（如 RTX 3090/4090）上即可完成训练，但文中缺乏具体参数。

#### 5. 实验数量与充分性
*   **实验规模**：进行了大规模评估，针对 12 个模板中的每一个生成了 200 个不同配置（改变区域位置、大小和起始状态），共计 **2400 个测试任务**。
*   **充分性**：实验涵盖了从简单到极高复杂度的逻辑规范，并提供了详细的案例研究（Case Study）来分析鲁棒性演化和剪枝效率。
*   **客观性**：通过 PSR 和 ESR 两个指标区分了“规划层”和“执行层”的成功，能够客观反映模型在不同阶段的瓶颈。

#### 6. 主要结论与发现
*   **高效性**：整体规划成功率达到 **95.58%**，执行成功率达到 **81.58%**。
*   **零样本泛化**：框架能够有效组合离线数据中的短时域行为，完成长达 140 个路标点的长时程任务，证明了对未见 STL 任务的强大泛化能力。
*   **复杂逻辑处理**：即使是带有嵌套操作符（如 GF 规范）的复杂任务，也能在合理时间内（平均 10 秒内）找到解。
*   **瓶颈发现**：对于时间约束极紧（Slack 较小）的任务（如 T6、T9），执行层更容易因跟踪误差而失败。

#### 7. 优点与亮点
*   **解耦设计**：成功将底层的动力学学习与高层的逻辑推理分离，使得模型无需针对新任务微调。
*   **区间语义扩展**：改进了 AGM 鲁棒性的区间监控算法，使其能处理复杂的嵌套时序逻辑，增强了搜索的剪枝能力。
*   **可解释性**：通过图搜索和鲁棒性曲线，规划过程具有高度的可视化和可解释性。

#### 8. 不足与局限
*   **数据依赖性**：规划性能高度依赖离线数据集对状态空间的覆盖程度，若数据存在严重空洞，图构建将不完整。
*   **离散化限制**：依赖离散的图抽象，可能无法找到连续空间中的最优路径。
*   **缺乏容错余量**：生成的计划有时过于紧凑，没有充分考虑执行过程中的不确定性（缺乏 Slack-aware 机制），导致执行层在扰动下鲁棒性不足。
*   **基准对比缺失**：实验主要展示了自身框架在不同难度下的表现，缺乏与其他最先进（SOTA）离线 STL 规划方法的直接数值对比。

（完）
