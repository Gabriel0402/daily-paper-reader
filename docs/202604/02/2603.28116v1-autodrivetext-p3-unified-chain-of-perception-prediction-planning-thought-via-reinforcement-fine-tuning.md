---
title: "$AutoDrive\\text{-}P^3$: Unified Chain of Perception-Prediction-Planning Thought via Reinforcement Fine-Tuning"
title_zh: "$AutoDrive\\text{-}P^3$：通过强化微调实现的感知-预测-规划统一思维链"
authors: "Yuqi Ye, Zijian Zhang, Junhong Lin, Shangkun Sun, Changhao Peng, Wei Gao"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28116v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 基于VLM的自动驾驶统一感知-预测-规划
tldr: 针对端到端自动驾驶中视觉语言模型（VLM）缺乏推理链或模块协同不足的问题，本文提出AutoDrive-P3框架。该框架通过P3-CoT数据集和P3-GRPO分层强化学习算法，实现了感知、预测与规划的统一推理链。此外，引入双重思考模式平衡效率与性能，在nuScenes和NAVSIM等基准测试中达到SOTA水平，显著提升了决策的安全性和可解释性。
selection_source: fresh_fetch
motivation: 现有VLM驱动的自动驾驶系统要么缺乏感知预测的中间推理过程，要么各模块间缺乏协同，导致决策能力受限。
method: 提出AutoDrive-P3框架，利用P3-CoT数据集构建结构化推理链，并采用P3-GRPO分层强化学习算法进行多任务渐进式监督。
result: 在nuScenes开环和NAVSIM闭环基准测试中均取得了最先进的规划性能，证明了统一推理链的有效性。
conclusion: 通过将感知、预测和规划整合进统一的思维链并结合强化学习优化，AutoDrive-P3显著增强了自动驾驶系统的决策能力和可解释性。
---

## 摘要
视觉语言模型（VLMs）因其在处理长尾场景方面的卓越表现，正越来越多地被应用于端到端自动驾驶系统。然而，目前基于 VLM 的方法存在两个主要局限性：1）部分 VLM 直接输出规划结果而缺乏思维链（CoT）推理，绕过了关键的感知和预测阶段，这造成了显著的领域差距并损害了决策能力；2）其他 VLM 虽然可以生成感知、预测和规划任务的输出，但采用了碎片化的决策方式，各模块独立运行，导致严重缺乏协同作用，从而削弱了真实的规划性能。为了解决这些局限性，我们提出了 $AutoDrive\text{-}P^3$，这是一个通过结构化推理将感知（\textbf{P}erception）、预测（\textbf{P}rediction）和规划（\textbf{P}lanning）无缝集成的创新框架。我们引入了 $P^3\text{-}CoT$ 数据集以促进连贯推理，并提出了 $P^3\text{-}GRPO$，这是一种分层强化学习算法，可为所有三项任务提供渐进式监督。具体而言，$AutoDrive\text{-}P^3$ 逐步生成感知、预测和规划的思维链推理及答案，其中感知为后续的预测和规划提供关键信息，而感知和预测共同助力最终的规划决策，从而实现更安全、更具可解释性的自动驾驶。此外，为了平衡推理效率与性能，我们引入了双重思考模式：深度思考（detailed thinking）和快速思考（fast thinking）。在开环（nuScenes）和闭环（NAVSIMv1/v2）基准测试上的广泛实验表明，我们的方法在规划任务中达到了最先进的性能。代码可在 https://github.com/haha-yuki-haha/AutoDrive-P3 获取。

## Abstract
Vision-language models (VLMs) are increasingly being adopted for end-to-end autonomous driving systems due to their exceptional performance in handling long-tail scenarios. However, current VLM-based approaches suffer from two major limitations: 1) Some VLMs directly output planning results without chain-of-thought (CoT) reasoning, bypassing crucial perception and prediction stages which creates a significant domain gap and compromises decision-making capability; 2) Other VLMs can generate outputs for perception, prediction, and planning tasks but employ a fragmented decision-making approach where these modules operate separately, leading to a significant lack of synergy that undermines true planning performance. To address these limitations, we propose ${AutoDrive\text{-}P^3}$, a novel framework that seamlessly integrates $\textbf{P}$erception, $\textbf{P}$rediction, and $\textbf{P}$lanning through structured reasoning. We introduce the ${P^3\text{-}CoT}$ dataset to facilitate coherent reasoning and propose ${P^3\text{-}GRPO}$, a hierarchical reinforcement learning algorithm that provides progressive supervision across all three tasks. Specifically, ${AutoDrive\text{-}P^3}$ progressively generates CoT reasoning and answers for perception, prediction, and planning, where perception provides essential information for subsequent prediction and planning, while both perception and prediction collectively contribute to the final planning decisions, enabling safer and more interpretable autonomous driving. Additionally, to balance inference efficiency with performance, we introduce dual thinking modes: detailed thinking and fast thinking. Extensive experiments on both open-loop (nuScenes) and closed-loop (NAVSIMv1/v2) benchmarks demonstrate that our approach achieves state-of-the-art performance in planning tasks. Code is available at https://github.com/haha-yuki-haha/AutoDrive-P3.

---

## 论文详细总结（自动生成）

### AutoDrive-P³：感知-预测-规划统一思维链强化微调框架总结

#### 1. 核心问题与研究动机
*   **研究背景**：视觉语言模型（VLM）因其强大的世界知识，被视为解决自动驾驶“长尾场景”的关键。
*   **核心问题**：
    1.  **缺乏推理链**：现有模型往往直接输出规划结果，跳过了感知和预测的中间逻辑，导致决策缺乏依据。
    2.  **模块碎片化**：虽然部分模型能输出感知和预测信息，但各模块独立运行，缺乏协同效应。
    3.  **监督不足**：现有的强化学习优化（如 GRPO）通常只针对最终的规划指标（如 L2 距离），忽略了对感知和预测准确性的直接引导。
*   **整体含义**：本文旨在通过构建一个统一的“感知-预测-规划”（P³）思维链，并利用分层强化学习进行全流程监督，提升自动驾驶的安全性和可解释性。

#### 2. 方法论：核心思想与技术细节
*   **AutoDrive-P³ 框架**：将自动驾驶任务建模为连续的思维过程。模型输入视频、车辆状态和指令，依次生成感知思考、感知答案、预测思考、预测答案、规划思考及规划轨迹。
*   **P³-CoT 数据集**：
    *   利用 Qwen2.5-VL-72B 生成高质量的推理链。
    *   专注于“关键对象”（Key Objects），避免冗余信息干扰。
    *   包含 nuScenes（约 2.5 万帧）和 NAVSIM（约 11.5 万帧）的结构化标注。
*   **P³-GRPO 算法（核心创新）**：
    *   基于组相对策略优化（GRPO），取消了价值函数，通过组内样本的相对奖励进行优化。
    *   **分层奖励函数**：
        *   **感知奖励 ($R_{perc}$)**：基于检测框的 IoU、精确率和召回率。
        *   **预测奖励 ($R_{pred}$)**：结合检测质量与行为预测标签的准确性。
        *   **规划奖励 ($R_{plan}$)**：基于轨迹的 L2 距离及闭环评估分数（如 PDMS）。
*   **双重思考模式**：
    *   **深度思考（Detailed）**：输出完整推理链，用于高精度决策。
    *   **快速思考（Fast）**：跳过推理步骤直接输出答案，提升推理频率（可达 1Hz）。

#### 3. 实验设计
*   **数据集与场景**：
    *   **nuScenes**：开环基准，评估 L2 位移误差和碰撞率。
    *   **NAVSIM (v1/v2)**：闭环/伪仿真基准，使用 PDMS 和 EPDMS 综合评分。
*   **对比方法**：
    *   **非自回归模型**：ST-P3, VAD, UniAD 等。
    *   **基于 VLM 的模型**：DriveVLM, OmniDrive, OpenDriveVLA, AutoVLA 等。
*   **评估指标**：L2 误差、碰撞率、安全性（NC）、舒适度（Comf）、交通规则依从性（DAC/TLC）等。

#### 4. 资源与算力
*   **训练算力**：使用 **8 台 NVIDIA A100 GPU** 进行训练。
*   **训练配置**：
    *   基座模型：Qwen2.5-VL-3B。
    *   SFT 阶段：训练 10 个 epoch。
    *   RL 阶段（P³-GRPO）：nuScenes 训练 5 个 epoch，NAVSIM 训练 10 个 epoch。
*   **推理算力**：在 **H100 GPU** 上使用 vLLM 加速，推理频率约为 0.5Hz（深度模式）至 1.0Hz（快速模式）。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准（nuScenes 和 NAVSIM）上进行了全面测试，涵盖了开环和闭环场景。
*   **消融实验**：
    *   验证了 P³-GRPO 相比于“仅规划监督”的优越性。
    *   测试了组大小（Group Size）、历史轨迹、传感器模态（图像 vs 视频）对性能的影响。
    *   分析了 KL 散度约束对训练稳定性的作用。
*   **客观性**：实验对比了不同参数规模的模型，证明了 3B 规模的 AutoDrive-P³ 在更少数据下能超越 7B 规模的 SOTA 模型，结果较为客观。

#### 6. 主要结论与发现
*   **协同效应显著**：同时监督感知、预测和规划任务，比单纯优化规划轨迹能显著降低碰撞率（在 nuScenes 上降低了约 40%）。
*   **强化学习的价值**：P³-GRPO 能够有效纠正 SFT 阶段的偏差，使模型生成的推理链与最终决策更加一致。
*   **性能领先**：在 NAVSIM 闭环测试中取得了 90.6 的 PDMS 高分，刷新了仅视觉输入的 SOTA 纪录。

#### 7. 优点与亮点
*   **全流程可解释性**：通过结构化的 CoT，用户可以清晰看到模型是因为看到了哪个物体、预测了什么行为才做出避让决策。
*   **分层监督机制**：解决了端到端模型中“中间过程不可控”的问题，确保规划是建立在正确的感知基础之上的。
*   **效率平衡**：双重思考模式的设计考虑到了自动驾驶对实时性的高要求。

#### 8. 不足与局限
*   **幻觉风险**：尽管有 CoT，VLM 仍可能产生逻辑自洽但与事实不符的“幻觉”推理。
*   **仿真依赖**：目前的强化学习是在离线模拟器中进行的，与真实世界的交互动态仍有差距。
*   **推理延迟**：即便在快速模式下，1Hz 的频率对于高速自动驾驶场景（通常需要 10Hz 以上）仍显不足。

（完）
