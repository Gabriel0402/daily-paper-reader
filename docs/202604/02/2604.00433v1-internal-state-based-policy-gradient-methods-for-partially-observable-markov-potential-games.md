---
title: Internal State-Based Policy Gradient Methods for Partially Observable Markov Potential Games
title_zh: 基于内部状态的部分可观测马尔可夫势博弈策略梯度方法
authors: "Wonseok Yang, Thinh T. Doan"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00433v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 部分可观测马尔可夫势博弈中的多智能体强化学习
tldr: 针对部分可观测马尔可夫势博弈（POMPGs）中的去中心化决策和维度灾难问题，本文提出了一种基于内部状态的自然策略梯度方法。通过引入公共信息框架和有限状态控制器来压缩历史信息，并建立了非渐近收敛界。该收敛界由统计误差和有限状态近似误差组成，仿真验证了该方法在提升多智能体协作性能方面的有效性。
selection_source: fresh_fetch
motivation: 解决部分可观测马尔可夫势博弈中由于信息不对称和历史信息无限增长导致的决策难题。
method: 结合公共信息框架与有限状态控制器（FSC）来压缩内部状态，并采用自然策略梯度算法寻找纳什均衡。
result: 理论上推导出了包含统计误差和近似误差的非渐近收敛界，仿真实验显示其性能优于仅依赖当前观测的方法。
conclusion: 该研究为处理具有部分可观测性的多智能体势博弈提供了一种具有理论收敛保证且计算可行的策略优化框架。
---

## 摘要
本文研究了部分可观测马尔可夫势博弈中的多智能体强化学习。由于部分可观测性、去中心化信息以及维数灾难，解决这一问题具有挑战性。首先，为了应对前两个挑战，我们利用了公共信息框架，该框架允许智能体基于共享信息和局部信息进行决策。其次，为了确保可处理性，我们研究了一种压缩累积信息的内部状态，防止其随时间无限增长。然后，我们实现了一种基于内部状态的自然策略梯度方法，以寻找马尔可夫势博弈的纳什均衡。我们的主要贡献是为该方法建立了一个非渐近收敛界。我们的理论界可以分解为两个可解释的部分：一个是在标准马尔可夫势博弈中也会出现的统计误差项，以及一个捕捉使用有限状态控制器的近似误差项。最后，在多个部分可观测环境下的仿真表明，与仅使用当前观测的设置相比，所提出的使用有限状态控制器的方法在性能上取得了持续的提升。

## Abstract
This letter studies multi-agent reinforcement learning in partially observable Markov potential games. Solving this problem is challenging due to partial observability, decentralized information, and the curse of dimensionality. First, to address the first two challenges, we leverage the common information framework, which allows agents to act based on both shared and local information. Second, to ensure tractability, we study an internal state that compresses accumulated information, preventing it from growing unboundedly over time. We then implement an internal state-based natural policy gradient method to find Nash equilibria of the Markov potential game. Our main contribution is to establish a non-asymptotic convergence bound for this method. Our theoretical bound decomposes into two interpretable components: a statistical error term that also arises in standard Markov potential games, and an approximation error capturing the use of finite-state controllers. Finally, simulations across multiple partially observable environments demonstrate that the proposed method using finite-state controllers achieves consistent improvements in performance compared to the setting where only the current observation is used.

---

## 论文详细总结（自动生成）

这篇论文针对部分可观测马尔可夫势博弈（POMPGs）提出了一种基于内部状态的自然策略梯度（NPG）方法，并提供了理论收敛性证明。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在多智能体强化学习中，处理部分可观测性（Partial Observability）和去中心化决策极其困难。虽然马尔可夫势博弈（MPGs）在全观测下已有成熟研究，但在部分可观测设定下，由于信念空间（Belief Space）是连续且不可数的，直接优化变得计算不可行。
*   **研究动机**：现有的公共信息框架虽能将问题转化为信念状态下的 MDP，但需要环境动力学知识且面临维度灾难。本文旨在开发一种**无需环境模型**、**计算可处理**且具有**理论收敛保证**的策略梯度算法。

### 2. 论文提出的方法论
*   **核心思想**：结合“公共信息共享”与“有限内部状态压缩”。通过引入有限状态控制器（FSC），将无限增长的历史信息压缩为有限的内部状态，从而在计算可处理性与最优性之间取得平衡。
*   **关键技术细节**：
    *   **公共信息框架**：智能体共享部分观测到公共内存，将去中心化 POMDP 转化为等效的 POMDP。
    *   **内部状态表示**：定义共享内部状态 $w$ 和局部内部状态 $l_i$，通过预定义的更新函数（如滑动窗口或 RNN 特征）压缩历史。
    *   **内部状态 NPG 算法**：采用 Softmax 参数化策略，利用自然策略梯度更新参数。更新规则考虑了策略空间的几何结构，而非简单的欧几里得梯度。
    *   **收敛界分解**：推导出的非渐近收敛界 $O(1/\sqrt{T}) + \epsilon_{FSC}$。其中第一项是标准势博弈的统计误差，第二项 $\epsilon_{FSC}$ 是由于使用有限状态近似信念状态而产生的近似误差。

### 3. 实验设计
*   **实验场景**：使用了 `POSGGym` 基准测试库中的三个环境：
    1.  **Multi-Agent Tiger (MATiger)**：双智能体听声辨虎，需协同开启宝箱门。
    2.  **Multi-Access Broadcast Channel (MABC)**：通信信道冲突避免，需协调发送时机。
    3.  **Level-Based Foraging (LBF)**：网格世界中的协同觅食，观测范围受限。
*   **对比基准（Benchmark）**：
    *   **有限内存策略（本文方法）**：使用长度为 2 的历史窗口作为内部状态。
    *   **反应式策略（Reactive Policy）**：仅根据当前时刻的观测进行决策。
*   **实现算法**：使用了独立信任区域策略优化（ITRPO），这是 NPG 的一种实用变体。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   **实现细节**：提到使用了基于神经网络的策略和价值函数，并采用了参数共享机制。

### 5. 实验数量与充分性
*   **实验规模**：在三个具有代表性的部分可观测环境中进行了测试。
*   **充分性评价**：
    *   实验展示了学习曲线的收敛过程，验证了理论中 $O(1/\sqrt{T})$ 的趋势。
    *   通过对比反应式策略，证明了“内存（内部状态）”在处理部分可观测性时的必要性。
    *   **局限性**：实验主要集中在小规模基准任务上，缺乏对不同内部状态长度（Memory Length）或不同压缩机制（如 RNN vs. 窗口）的消融实验。

### 6. 主要结论与发现
*   **性能提升**：在所有测试环境中，基于内部状态的策略显著优于仅依赖当前观测的策略。在 MABC 环境中，反应式策略甚至无法学习，而本文方法表现良好。
*   **理论验证**：实验结果支持了论文的理论发现，即内部状态能够有效近似信念状态，且算法能够收敛至纳什均衡（NE）的邻域。
*   **协作涌现**：在 LBF 环境中观察到，内部状态自然地促进了智能体之间的结构化协作行为。

### 7. 优点
*   **理论严谨**：为部分可观测势博弈这一复杂领域提供了首个（或少数之一）具有非渐近收敛保证的 NPG 分析。
*   **架构实用**：通过有限状态控制器解决了信念空间不可数的问题，使算法在实际应用中具有可扩展性。
*   **误差可解释**：将总误差分解为迭代误差和表示误差，为未来改进内部状态表示提供了理论指导。

### 8. 不足与局限
*   **近似误差持久性**：只要内部状态不能完美表示信念状态，$\epsilon_{FSC}$ 误差项就永远不会消失，这意味着算法只能收敛到近似纳什均衡。
*   **超参数敏感性**：收敛界依赖于初始分布的覆盖程度（参数 $a$），如果初始策略探索不足，收敛速度会极慢。
*   **实验规模有限**：尚未在大规模、高维动作空间的复杂多智能体环境（如星际争霸等）中验证其有效性。
*   **理论假设**：理论证明假设可以获得精确的优势函数（Advantage Function），但在强化学习实践中，优势函数的估计往往存在方差和偏差。

（完）
