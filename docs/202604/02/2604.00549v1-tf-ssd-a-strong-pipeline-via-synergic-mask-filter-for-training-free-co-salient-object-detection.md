---
title: "TF-SSD: A Strong Pipeline via Synergic Mask Filter for Training-free Co-salient Object Detection"
title_zh: TF-SSD：一种基于协同掩码过滤器的强流水线，用于无需训练的协同显著性目标检测
authors: "Zhijin He, Shuo Jin, Siyue Yu, Shuwei Wu, Bingfeng Zhang, Li Yu, Jimin Xiao"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00549v1"
tags: ["query:mmseg"]
score: 9.0
evidence: SAM与DINO在目标检测中的协同
tldr: 本研究针对协同显著性目标检测（CoSOD）中训练模型泛化性受限的问题，提出了一种名为 TF-SSD 的无需训练的新方法。该方法协同利用了 SAM 和 DINO 两种视觉基础模型：首先通过 SAM 生成候选掩码并进行质量过滤，随后利用 DINO 的注意力图进行单图显著性筛选，最后通过跨图像原型相似度选择共有的显著目标。TF-SSD 在性能上显著优于现有无监督方法，展现了基础模型在 CoSOD 领域的巨大潜力。
selection_source: fresh_fetch
motivation: 传统的 CoSOD 方法依赖于闭集数据集训练，导致泛化能力有限，而视觉基础模型在显著性理解上具有强大潜力。
method: 提出 TF-SSD 框架，通过 SAM 生成掩码，并结合 DINO 的注意力机制与跨图像原型匹配实现显著目标的精准筛选。
result: "实验表明 TF-SSD 性能优异，在多个指标上大幅领先，较近期无训练方法提升了 13.7%。"
conclusion: 该研究证明了通过协同过滤机制有效整合基础模型，可以在无需训练的情况下实现强大的协同显著性检测能力。
---

## 摘要
协同显著性目标检测（CoSOD）旨在分割出一组相关图像中一致出现的显著目标。尽管最近基于训练的方法取得了显著进展，但它们仍受限于闭集数据集，且泛化能力有限。然而，很少有研究探索视觉基础模型（VFMs）在解决 CoSOD 方面的潜力，而这些模型展现出了强大的泛化能力和稳健的显著性理解。在本文中，我们研究并利用 VFMs 进行 CoSOD，并进一步通过 SAM 和 DINO 之间的协同作用，提出了一种新颖的无需训练的方法 TF-SSD。具体而言，我们首先利用 SAM 生成全面的原始提议（proposals），作为候选掩码池。然后，我们引入了一个质量掩码生成器来过滤冗余掩码，从而获得精细化的掩码集。由于该生成器基于 SAM 构建，其本质上缺乏对显著性的语义理解。为此，我们采用了一种图像内显著性过滤器，利用 DINO 的注意力图来识别单张图像中视觉显著的掩码。此外，为了将显著性理解扩展到图像组，我们提出了一种图像间原型选择器，通过计算跨图像原型之间的相似度得分来选择得分最高的掩码。这些选定的掩码作为 CoSOD 的最终预测结果。广泛的实验表明，我们的 TF-SSD 优于现有方法（例如，比最近的无需训练的方法提升了 13.7%）。代码可在 https://github.com/hzz-yy/TF-SSD 获取。

## Abstract
Co-salient Object Detection (CoSOD) aims to segment salient objects that consistently appear across a group of related images. Despite the notable progress achieved by recent training-based approaches, they still remain constrained by the closed-set datasets and exhibit limited generalization. However, few studies explore the potential of Vision Foundation Models (VFMs) to address CoSOD, which demonstrate a strong generalized ability and robust saliency understanding. In this paper, we investigate and leverage VFMs for CoSOD, and further propose a novel training-free method, TF-SSD, through the synergy between SAM and DINO. Specifically, we first utilize SAM to generate comprehensive raw proposals, which serve as a candidate mask pool. Then, we introduce a quality mask generator to filter out redundant masks, thereby acquiring a refined mask set. Since this generator is built upon SAM, it inherently lacks semantic understanding of saliency. To this end, we adopt an intra-image saliency filter that employs DINO's attention maps to identify visually salient masks within individual images. Moreover, to extend saliency understanding across group images, we propose an inter-image prototype selector, which computes similarity scores among cross-image prototypes to select masks with the highest score. These selected masks serve as final predictions for CoSOD. Extensive experiments show that our TF-SSD outperforms existing methods (e.g., 13.7\% gains over the recent training-free method). Codes are available at https://github.com/hzz-yy/TF-SSD.

---

## 论文详细总结（自动生成）

以下是对论文《TF-SSD: A Strong Pipeline via Synergic Mask Filter for Training-free Co-salient Object Detection》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：协同显著性目标检测（CoSOD）旨在从一组相关图像中识别并分割出共同的显著物体。
*   **研究动机**：
    *   **现有方法局限性**：目前主流方法多基于有监督训练，受限于闭集数据集，泛化能力较差，容易过拟合于训练集的类别分布。
    *   **基础模型潜力**：视觉基础模型（VFM）如 SAM 具备极强的分割能力，但缺乏对“协同显著性”的语义理解；DINO 则具备优秀的自监督语义特征和显著性定位能力。
    *   **研究目标**：探索如何无需训练（Training-free），仅通过协同 SAM 的精确分割和 DINO 的语义引导来实现高性能的 CoSOD。

### 2. 方法论：核心思想与技术细节
TF-SSD 提出了一种渐进式的过滤流水线，核心思想是将 SAM 生成的海量掩码池通过 DINO 的语义信息进行层层筛选：
*   **质量掩码生成器 (QMG)**：
    *   利用 SAM 生成原始掩码。
    *   通过三阶段过滤：1) 面积比例过滤（剔除极小掩码）；2) 重叠过滤（基于 IoU 剔除冗余重复掩码）；3) 质量评估（结合 SAM 预测的 IoU 和自定义的面积惩罚函数，筛选出 Top-Tr 个高质量候选掩码）。
*   **图像内显著性过滤器 (ISF)**：
    *   利用 DINO 的 [CLS] token 注意力图作为显著性参考。
    *   计算掩码区域与注意力图的对齐得分，保留单张图像内视觉最显著的 Top-T 个掩码。
*   **图像间原型选择器 (IPS)**：
    *   利用 DINO 提取每个候选掩码区域的特征原型（Prototype）。
    *   计算跨图像的原型余弦相似度矩阵，通过最大相似度求和得出“协同显著性得分”，最终选出组内一致性最高的掩码作为预测结果。
*   **后处理机制**：引入了回退机制（当所有掩码得分过低时直接使用 DINO 注意力图生成掩码）和多目标合并机制。

### 3. 实验设计
*   **数据集**：在三个主流 CoSOD 基准测试集上进行评估：**CoCA**（最具挑战性）、**CoSal2015** 和 **CoSOD3k**。
*   **对比方法**：
    *   **有监督方法**：如 GCoNet+、VCP、CONDA 等。
    *   **无监督方法**：如 TokenCut、SCoSPARC 等。
    *   **无需训练方法**：如 ZS-CoSOD（主要竞争对手）。
*   **评价指标**：F-measure ($F_\beta^{max}$)、S-measure ($S_\alpha$)、MAE 和 E-measure ($E_\xi^{max}$)。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 4090 GPU** 上完成。
*   **训练时长**：由于该方法是 **Training-free（无需训练）** 的，因此不存在训练阶段，仅涉及推理过程。
*   **模型配置**：SAM 使用 ViT-H 主干，DINO 使用 ViT-B/8 架构。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个大型数据集的完整测试，并进行了定性（可视化）和定量分析。
*   **消融实验**：非常充分。分别验证了 QMG 的三个阶段、ISF 模块、IPS 模块的有效性；探讨了 IPS 中不同选择策略（Max vs Avg）的影响；测试了不同超参数（如掩码数量 T、面积阈值、DINO 主干网络版本等）对结果的灵敏度。
*   **客观性**：通过与 Oracle（理想上限）性能对比，客观展示了 SAM 掩码池的潜力以及 TF-SSD 筛选机制的效率。

### 6. 主要结论与发现
*   **性能领先**：TF-SSD 在所有指标上均显著优于现有的无监督和无需训练方法。在 CoCA 数据集上，比之前的 SOTA 无训练方法 ZS-CoSOD 在 F-measure 上提升了 **13.7%**。
*   **媲美有监督**：在无需任何参数更新的情况下，TF-SSD 的表现甚至可以与部分最新的有监督学习方法相媲美。
*   **协同效应**：实验证明 DINO 的语义引导能有效弥补 SAM 缺乏类别意识的缺陷，而 SAM 的边界精度则提升了 DINO 粗糙的定位能力。

### 7. 优点（亮点）
*   **无需训练**：极大地降低了部署成本，且不受训练集类别偏差的影响，具有极强的通用性。
*   **即插即用**：利用现有的视觉基础模型，通过逻辑过滤实现复杂任务，思路简洁高效。
*   **高质量筛选**：QMG 和 ISF 的设计有效地解决了 SAM 掩码冗余和缺乏显著性感知的问题。

### 8. 不足与局限
*   **小目标检测**：由于 QMG 中存在面积比例过滤阈值，且 DINO 的注意力图对极小目标的响应可能较弱，导致该方法在处理如“月亮”等极小物体时容易失败。
*   **超参数依赖**：QMG 和 ISF 中的多个阈值（如面积范围、重叠率等）可能需要根据不同场景进行微调，虽然论文给出了推荐值，但在极端分布的数据下可能存在鲁棒性挑战。
*   **计算开销**：虽然无需训练，但推理时需要运行 SAM 和 DINO 两个大型模型，对于实时性要求极高的场景可能存在压力。

（完）
