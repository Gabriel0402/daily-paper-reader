---
title: Differentiable Normative Guidance for Nash Bargaining Solution Recovery
title_zh: 用于纳什议价解恢复的可微规范性引导
authors: "Moirangthem Tiken Singh, Surajit Borkotokey, Rajnish Kumar"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29297v1"
tags: ["query:gt-mai"]
score: 9.0
evidence: 多智能体谈判系统中的纳什议价解决方案
tldr: 针对自主AI代理在谈判中难以生成公平且符合纳什议价解（NBS）的效用分配问题，本文提出一种引导式图扩散框架。该方法利用图注意力机制捕捉代理属性，并通过条件扩散模型生成效用向量。核心贡献在于引入可微复合引导损失，在推理时无需帕累托前沿知识即可优化个体理性（IR）和纳什乘积，显著提升了谈判结果的公平性与效率。
selection_source: fresh_fetch
motivation: 现有生成模型常学习到次优的人类行为且缺乏帕累托前沿知识，导致谈判分配方案难以同时满足个体理性和纳什议价解。
method: 提出一种基于图注意力的条件扩散模型，结合可微引导损失函数，在反向扩散过程中动态惩罚违反个体理性和偏离纳什乘积的行为。
result: "在多个数据集上实现了100%的个体理性合规，且纳什效率较无约束基准模型提升了20至60个百分点。"
conclusion: 该研究证明了通过可微规范引导可以在无需完整前沿知识的情况下，有效恢复复杂谈判中的纳什议价解。
---

## 摘要
谈判系统中的自主人工智能代理必须生成公平的效用分配，以满足个人理性（IR）——确保每个代理至少获得其外部选项，并满足最大化联合盈余的纳什议价解（NBS）。现有的生成模型通常学习次优的人类行为，产生的解远离帕累托效率，而经典方法需要完整的帕累托前沿知识，这在真实数据集中是无法获得的。我们提出了一个引导式图扩散框架，该框架在推理时无需前沿知识即可生成满足个人理性的效用向量，同时逼近纳什议价解。谈判被建模为有向图，利用图注意力捕捉非对称代理属性，并由条件扩散模型将这些属性映射到效用向量。在最后的反向扩散步骤中应用可微复合引导损失，对违反个人理性（IR）和纳什乘积差距进行惩罚。我们证明，在足够的惩罚权重下，解能在有限时间内进入个人理性（IR）区域。在多个数据集上，该方法实现了 100% 的个人理性（IR）合规性。纳什效率在合成数据上达到 99.45%（与 Oracle 相比误差在 0.55 个百分点以内），在 CaSiNo 和 Deal or No Deal 数据集上分别达到 54.24% 和 88.67%，比无约束生成基线提高了 20-60 个百分点。

## Abstract
Autonomous artificial intelligence agents in negotiation systems must generate equitable utility allocations satisfying individual rationality (IR), ensuring each agent receives at least its outside option, and the Nash Bargaining Solution (NBS), which maximizes joint surplus. Existing generative models often learn suboptimal human behaviors, producing solutions far from Pareto efficiency, while classical methods require full Pareto frontier knowledge, which is unavailable in real datasets. We propose a guided graph diffusion framework that generates individually rational utility vectors while approximating the NBS without frontier knowledge at inference time. Negotiations are modeled as directed graphs with graph attention capturing asymmetric agent attributes, and a conditional diffusion model maps these to utility vectors. A differentiable composite guidance loss, applied in the final reverse diffusion steps, penalizes IR violations and Nash product gaps. We prove that, under sufficient penalty weighting, solutions enter the IR region in finite time. Across datasets, the method achieves 100% IR compliance. Nash efficiency reaches 99.45% on synthetic data (within 0.55 percentage points of an oracle), and 54.24% (CaSiNo) and 88.67% (Deal or No Deal), improving 20-60 percentage points over unconstrained generative baselines.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为“引导式图扩散”（Guided Graph Diffusion）的框架，旨在解决自主AI代理在谈判任务中难以生成既符合公平性（纳什议价解）又满足个体理性（不低于保底收益）的效用分配方案的问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在复杂的资源分配和谈判场景中，AI代理需要生成公平且高效的方案。然而，现有的生成模型（如基于人类对话训练的模型）往往会继承人类的认知偏差（如锚定效应、风险厌恶），导致结果处于次优状态（远离帕累托前沿）。
*   **研究动机**：传统的博弈论求解器需要预先知道“帕累托前沿”的精确几何形状，但在现实数据集中这是不可知的。因此，需要一种既能利用深度学习的表达能力，又能通过数学公理（个体理性 IR 和纳什议价解 NBS）进行约束的生成机制，以实现“规范性可信度”。

### 2. 方法论：引导式图扩散框架
该框架由三个核心组件构成：
*   **策略图编码（Strategic Graph Encoding）**：
    *   将谈判状态建模为有向图，节点代表代理，边代表关系。
    *   使用 **GATv2（图注意力网络）** 提取代理间的非对称属性（如分歧点、预算、优先级），生成一个对排列不敏感的上下文嵌入向量 $h$。
*   **条件 MLP 扩散过程（Conditional MLP Diffusion）**：
    *   采用 **DDIM（去噪扩散隐式模型）**。
    *   去噪网络（MLP）以噪声效用向量、时间步嵌入和图上下文 $h$ 为输入，预测噪声。
*   **可微规范性引导（Differentiable Normative Guidance）**：
    *   **核心思想**：在反向扩散的最后阶段（由参数 $t_{start}$ 控制），引入一个可微的复合损失函数 $L_{guide}$。
    *   **损失函数组成**：包含三部分：1) **纳什势垒项**（最大化盈余乘积）；2) **严格 IR 惩罚项**（确保每个代理收益超过分歧点）；3) **前沿贴合项**（约束在可行域内）。
    *   **动态调整**：利用自动微分计算梯度，在推理过程中动态修正效用向量的轨迹，将其推向纳什议价解。

### 3. 实验设计
*   **数据集**：
    1.  **Synthetic NTU**：2.5万个合成样本，具有精确可计算的纳什议价解（作为 Oracle 基准）。
    2.  **CaSiNo**：人类关于露营物品交换的谈判对话数据集。
    3.  **Deal or No Deal**：具有私有估值的物品划分谈判数据集。
*   **对比基线（Baselines）**：
    *   监督学习 MLP、CVAE（条件变分自编码器）、Conditional GAN。
    *   消融版本：无引导的 DDIM、后处理投影 DDIM、全过程硬约束 DDIM。
*   **评估指标**：IR 合规率（%）、纳什乘积、纳什效率（相对于 Oracle 的百分比）、帕累托前沿距离。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到训练分为两个阶段（各15个 epoch），总计30个 epoch；使用了 AdamW 优化器和余弦退火学习率调度。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同性质的数据集上进行了测试，涵盖了从解析解到真实人类行为的频谱。
*   **消融与敏感性分析**：
    *   进行了多维超参数网格搜索（针对引导步长 $\lambda$、激活窗口 $t_{start}$ 等）。
    *   通过 Wilcoxon 符号秩检验验证了纳什乘积提升的统计显著性（$p < 0.001$）。
*   **充分性评价**：实验设计较为充分，不仅对比了不同架构，还深入分析了扩散过程中的潜在轨迹动态，证明了“后期引导”比“全过程引导”更有效，具有较强的说服力。

### 6. 主要结论与发现
*   **100% 合规性**：在所有数据集上，该方法均实现了 100% 的个体理性（IR）合规，彻底消除了不公平分配。
*   **效率大幅提升**：在 CaSiNo 数据集上，纳什效率从无约束基线的 5.6% 提升至 **54.2%**；在合成数据上达到 **99.45%**，几乎等同于 Oracle。
*   **克服数据偏差**：证明了即使训练数据（人类谈判）是次优的，通过推理时的规范性引导，模型也能“逃离”数据偏差，找到更优的公平解。

### 7. 优点
*   **无需前沿知识**：在推理时不需要知道帕累托前沿的解析式，这使其能应用于真实世界的黑盒环境。
*   **理论保障**：论文提供了数学证明，确保在足够惩罚权重下，解能在有限时间内进入 IR 区域并收敛至 NBS。
*   **灵活性**：通过调节引导激活窗口（$t_{start}$），平衡了生成的多样性与规范的严谨性。

### 8. 不足与局限
*   **推理延迟**：由于扩散模型需要多步迭代去噪，其推理速度慢于单次前向传播的 GAN 或 MLP，不适用于超高频实时谈判。
*   **信息假设**：目前假设代理的分歧点和优先级是已知的（完全信息），在非对称或隐藏信息的对抗性谈判中，引导机制的有效性尚待验证。
*   **规模扩展**：虽然图编码支持多方，但实验主要集中在双方（Bilateral）谈判，多方谈判中纳什乘积梯度的方差可能会增大，导致优化困难。

（完）
