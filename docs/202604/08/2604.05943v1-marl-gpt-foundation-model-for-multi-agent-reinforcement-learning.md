---
title: "MARL-GPT: Foundation Model for Multi-Agent Reinforcement Learning"
title_zh: MARL-GPT：多智能体强化学习的基础模型
authors: "Maria Nesterova, Mikhail Kolosov, Anton Andreychuk, Egor Cherepanov, Oleg Bulichev, Alexey Kovalev, Konstantin Yakovlev, Aleksandr Panov, Alexey Skrynnik"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05943v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 基于GPT的多智能体强化学习基础模型
tldr: 针对多智能体强化学习（MARL）通常需要为特定任务定制模型的问题，本文提出了MARL-GPT，这是一种基于GPT架构的通用基础模型。该方法利用离线强化学习，在包括SMACv2、GRF和POGEMA在内的多种异构环境的大规模专家轨迹上进行训练。通过统一的Transformer观测编码器，MARL-GPT无需针对特定任务进行微调即可在多个领域达到与专用基准模型相当的性能，为构建通用的多智能体基础模型奠定了基础。
selection_source: fresh_fetch
motivation: 旨在解决当前多智能体强化学习模型缺乏通用性、难以跨不同任务和环境迁移的问题。
method: 采用基于GPT的Transformer架构，结合离线强化学习在大规模跨环境专家数据集上进行统一训练。
result: 在SMACv2、Google足球和POGEMA等多个极具挑战性的环境中，MARL-GPT均表现出与专用模型竞争的性能。
conclusion: 实验证明了构建跨领域通用多智能体基础模型的可行性，为MARL领域的“大模型”化提供了新路径。
---

## 摘要
多智能体强化学习（MARL）的最新进展已在众多具有挑战性的领域和环境中取得了成功，但通常需要为每个任务设计专门的模型。在这项工作中，我们提出了一种连贯的方法论，使得单个基于 GPT 的模型能够在多种不同的 MARL 环境和任务中学习并表现良好，包括星际争霸多智能体挑战赛（SMAC）、Google Research Football 和 POGEMA。我们的方法 MARL-GPT 应用离线强化学习，在专家轨迹（SMACv2 为 4 亿，GRF 为 1 亿，POGEMA 为 10 亿）上进行大规模训练，并结合了一个无需针对特定任务进行微调的单一基于 Transformer 的观测编码器。实验表明，MARL-GPT 在所有测试环境中与专门的基准模型相比，均取得了具有竞争力的性能。因此，我们的研究结果表明，为各种（显著不同的）多智能体问题构建多任务 Transformer 模型确实是可能的，这为开发基础 MARL 模型（类似于自然语言建模中的 ChatGPT、Llama、Mistral 等）铺平了道路。

## Abstract
Recent advances in multi-agent reinforcement learning (MARL) have demonstrated success in numerous challenging domains and environments, but typically require specialized models for each task. In this work, we propose a coherent methodology that makes it possible for a single GPT-based model to learn and perform well across diverse MARL environments and tasks, including StarCraft Multi-Agent Challenge, Google Research Football and POGEMA. Our method, MARL-GPT, applies offline reinforcement learning to train at scale on the expert trajectories (400M for SMACv2, 100M for GRF, and 1B for POGEMA) combined with a single transformer-based observation encoder that requires no task-specific tuning. Experiments show that MARL-GPT achieves competitive performance compared to specialized baselines in all tested environments. Thus, our findings suggest that it is, indeed, possible to build a multi-task transformer-based model for a wide variety of (significantly different) multi-agent problems paving the way to the fundamental MARL model (akin to ChatGPT, Llama, Mistral etc. in natural language modeling).

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MARL-GPT**，旨在构建多智能体强化学习（MARL）领域的基础模型（Foundation Model）。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的 MARL 方法通常是“任务特定型”的，即针对特定环境、特定智能体数量或特定地图进行训练，缺乏跨领域的通用性和泛化能力。
*   **研究动机**：借鉴大语言模型（LLM）在自然语言处理中的成功，作者希望开发一个单一的、基于 Transformer 的模型，能够同时处理多种截然不同的多智能体任务（如战斗、体育、路径规划），并具备零样本（Zero-shot）迁移和快速微调的能力。

### 2. 方法论
MARL-GPT 的核心思想是利用**大规模离线数据集**和**统一的 Transformer 架构**进行多任务学习。
*   **数据采集**：首先使用专门的算法（如 IPPO、RHCR）在不同环境中训练专家策略，收集海量轨迹数据（包含观测、动作、奖励）。
*   **观测编码（关键技术）**：为了让模型处理异构的观测空间，作者设计了一种通用的编码方案。每个观测元素被转化为 Token，并叠加四种嵌入（Embedding）：
    1.  **属性索引（Attribute）**：标识特征类型（如生命值、坐标）。
    2.  **团队索引（Team）**：区分盟友与敌人。
    3.  **智能体索引（Agent）**：标识特定智能体的身份。
    4.  **时间步索引（Timestep）**：提供历史上下文信息。
*   **模型架构与训练**：
    *   采用类 GPT 的 Transformer 架构（基于 NanoGPT）。
    *   **Critic（评论家）**：使用离散分箱（Bins）的分类交叉熵损失，结合保守正则化（Conservative Regularization）以处理离线数据中的分布偏移。
    *   **Actor（执行者）**：结合行为克隆（BC）损失和基于优势函数（Advantage）的策略梯度损失。
    *   **动作空间**：使用统一的输出层配合环境特定的动作掩码（Action Masking）。

### 3. 实验设计
*   **实验场景**：
    1.  **SMACv2**：星际争霸 II 战斗，测试复杂的微操和对抗。
    2.  **Google Research Football (GRF)**：足球比赛，测试随机性环境下的协作。
    3.  **POGEMA**：多智能体路径规划，测试大规模智能体群体的避障。
*   **基准模型（Benchmarks）**：
    *   **单任务专家**：针对每个任务专门训练的 IPPO 或 RHCR。
    *   **离线 RL 基准**：Decision Transformer (DT)、BC、CQL、BC-LSTM、RATE。
*   **对比维度**：跨环境胜率、吞吐量、零样本泛化能力、在线微调速度。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H100 80GB GPU**。
*   **训练时长**：主实验（7M 参数模型，500,000 次迭代）耗时约 **161 小时**。
*   **数据规模**：训练集包含 SMACv2（4 亿样本）、GRF（1 亿样本）和 POGEMA（10 亿样本）。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 5vs5 到 11vs11 的多种规模，并在不同种族（星际争霸）和地图（路径规划）上进行了测试。
*   **消融实验**：作者对模型大小（2M vs 7M）、历史窗口长度、数据集大小、位置编码的必要性以及奖励折扣因子 $\gamma$ 进行了详细的消融研究。
*   **充分性评价**：实验设计非常充分，不仅验证了多任务同时学习的效果，还专门测试了在未见任务上的零样本表现和微调效率，并包含了一个真实的机器人实验（Appendix D），证明了从仿真到现实的迁移潜力。

### 6. 主要结论与发现
*   **单一模型胜过专用模型**：MARL-GPT 在多个任务中的表现优于传统的离线 RL 基准，甚至在某些场景下达到了专家级水平。
*   **强大的泛化性**：模型能够直接迁移到智能体数量更多（如从 5vs5 迁移到 10vs10）或未见过的地图分布中。
*   **微调优势**：相比从零开始训练，使用 MARL-GPT 预训练模型进行在线微调的速度显著加快。
*   **编码的重要性**：实验证明，作者提出的四组位置编码对于模型理解多智能体结构至关重要。

### 7. 优点
*   **通用性强**：打破了 MARL 任务之间的壁垒，实现了真正的多领域统一建模。
*   **架构优雅**：通过巧妙的 Token 编码解决了多智能体系统中智能体数量可变和排列不变性的问题。
*   **开源贡献**：作者承诺开源代码、专家权重和大规模数据集，对社区具有极高的价值。

### 8. 不足与局限
*   **输入限制**：目前仅支持结构化的向量输入，无法直接处理原始图像（像素）观测。
*   **语义对齐缺失**：虽然使用了统一编码，但不同环境间的特征语义并未显式对齐（例如，环境 A 的“生命值”和环境 B 的“能量值”在模型看来只是不同的索引），这限制了更深层次的跨领域知识迁移。
*   **动作空间绑定**：动作索引在不同环境中含义完全不同，导致模型在零样本迁移到全新动作空间时存在困难。
*   **算力门槛**：尽管模型参数量（7M）不算巨大，但大规模离线训练对算力和高质量专家数据的依赖较高。

（完）
