---
title: Hierarchical Reinforcement Learning with Augmented Step-Level Transitions for LLM Agents
title_zh: 面向大语言模型智能体的增强步级转移分层强化学习
authors: "Shuai Zhen, Yanhua Yu, Ruopei Guo, Nan Cheng, Yang Deng"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05808v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 大语言模型智能体的分层强化学习
tldr: 针对LLM智能体在复杂决策任务中因依赖长交互历史导致的高计算成本和扩展性受限问题，本文提出STEP-HRL框架。该框架采用分层强化学习，通过全局子任务进度和局部交互摘要构建增强的步级状态转移，使智能体仅需单步信息即可进行有效学习。实验证明，该方法在ScienceWorld和ALFWorld上显著提升了性能与泛化能力，并大幅降低了Token消耗。
selection_source: fresh_fetch
motivation: 旨在解决LLM智能体因依赖冗长交互历史而导致的计算开销巨大及长序列建模困难的问题。
method: 提出一种分层强化学习框架，通过全局子任务进度和局部摘要模块构建增强的步级转移，实现高效的单步决策学习。
result: 在ScienceWorld和ALFWorld基准测试中，该方法在任务成功率、泛化性能和Token节省方面均显著优于现有基准。
conclusion: STEP-HRL证明了通过分层任务分解和状态增强，可以在减少计算资源消耗的同时提升LLM智能体的复杂决策能力。
---

## 摘要
大语言模型（LLM）智能体在复杂的交互式决策任务中展现了强大的能力。然而，现有的 LLM 智能体通常依赖于日益增长的交互历史，导致了高昂的计算成本和有限的可扩展性。在本文中，我们提出了 STEP-HRL，这是一种分层强化学习（HRL）框架，它通过仅以单步转移而非完整交互历史为条件，实现了步级学习。STEP-HRL 对任务进行分层构建，利用已完成的子任务来表示整个任务的全局进度。通过引入局部进度模块，它还迭代且有选择性地总结每个子任务内的交互历史，以生成局部进度的紧凑摘要。这些组件共同为高层和底层策略生成了增强的步级转移。在 ScienceWorld 和 ALFWorld 基准测试上的实验结果一致表明，STEP-HRL 在性能和泛化能力方面显著优于基准模型，同时减少了 token 的使用量。我们的代码可在 https://github.com/TonyStark042/STEP-HRL 获取。

## Abstract
Large language model (LLM) agents have demonstrated strong capabilities in complex interactive decision-making tasks. However, existing LLM agents typically rely on increasingly long interaction histories, resulting in high computational cost and limited scalability. In this paper, we propose STEP-HRL, a hierarchical reinforcement learning (HRL) framework that enables step-level learning by conditioning only on single-step transitions rather than full interaction histories. STEP-HRL structures tasks hierarchically, using completed subtasks to represent global progress of overall task. By introducing a local progress module, it also iteratively and selectively summarizes interaction history within each subtask to produce a compact summary of local progress. Together, these components yield augmented step-level transitions for both high-level and low-level policies. Experimental results on ScienceWorld and ALFWorld benchmarks consistently demonstrate that STEP-HRL substantially outperforms baselines in terms of performance and generalization while reducing token usage. Our code is available at https://github.com/TonyStark042/STEP-HRL.

---

## 论文详细总结（自动生成）

以下是对论文《Hierarchical Reinforcement Learning with Augmented Step-Level Transitions for LLM Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 智能体通常采用“历史条件化（History-conditioned）”的决策模式，即策略依赖于不断增长的交互历史。
*   **研究动机**：
    *   **计算成本高**：Transformer 的注意力机制随上下文长度呈二次方增长。
    *   **决策干扰**：冗长的历史包含大量冗余或无关信息，会掩盖关键决策信号，降低推理质量。
    *   **可扩展性受限**：长序列建模增加了信用分配（Credit Assignment）的难度。
*   **整体含义**：本文旨在将 LLM 智能体的决策从“依赖全量历史”转变为“依赖增强的单步状态”，通过分层强化学习（HRL）和进度摘要机制，实现高效、可扩展的步级（Step-level）决策。

### 2. 方法论：STEP-HRL 框架
核心思想是将复杂任务分解为分层结构，并引入“局部进度”模块来压缩历史信息。
*   **分层结构**：
    *   **高层策略 ($\pi_h$)**：根据任务指令和已完成子任务序列（全局进度），生成下一个子任务。
    *   **底层策略 ($\pi_l$)**：根据当前子任务、当前观察和局部进度摘要，生成具体的原子动作。
*   **局部进度模块 ($\pi_p$)**：
    *   这是一个关键创新。它在每个子任务内部运行，迭代地将上一步的进度摘要、当前动作和新观察融合，生成一个紧凑的文本摘要。这使得底层策略只需关注“当前步”的增强信息，而无需回溯整个历史。
*   **训练流程（两阶段）**：
    1.  **行为克隆（BC）初始化**：利用专家演示数据（包含子任务和进度标注）对高层、底层和进度策略进行联合微调。
    2.  **步级离线强化学习（Offline RL）**：采用类似 IQL（Implicit Q-Learning）的架构。为三个策略分别配备 Critic 网络，通过分位数回归（Expectile Regression）学习状态价值，并利用优势加权回归（AWR）优化策略，避免了显式的在线探索。

### 3. 实验设计
*   **数据集/场景**：
    *   **ScienceWorld**：复杂的科学实验环境，涉及物理、化学、生物等 30 类任务，具有密集的中间奖励。
    *   **ALFWorld**：基于文本的家庭环境任务（如清洁、加热、放置），具有稀疏奖励（仅任务完成时有奖励）。
*   **Benchmark 设置**：分为 **Seen（见过的任务）** 和 **Unseen（未见过的任务）**，重点考察泛化能力。
*   **对比方法**：
    *   **提示词方法**：ReAct, Reflexion。
    *   **微调/强化学习方法**：Swift-Sage, ETO（基于 DPO）, WKM（世界知识模型）, GLIDER（现有的分层强化学习基准）。
    *   **闭源模型参考**：GPT-3.5, GPT-4。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 张 NVIDIA A100 80G GPU**。
*   **训练细节**：
    *   行为克隆阶段：5 个 Epoch，Batch Size 为 128。
    *   离线 RL 阶段：3 个 Epoch，Batch Size 为 256。
    *   模型底座：Mistral-7B, Gemma-7B, Llama3-8B，以及更小规模的 Llama-1B/3B。
*   **时长**：文中未明确给出总训练小时数，但提到了参数高效微调（LoRA）的使用，暗示了训练过程相对高效。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流基准上进行了跨 3 种主流开源底座（Mistral, Gemma, Llama3）的全面测试。
    *   进行了 **模型规模扩展性实验**（从 1B 到 8B）。
    *   **消融实验**：分别验证了局部进度模块（LP）、分层结构（Hier）和强化学习阶段（RL）的必要性。
    *   **效率分析**：对比了不同 RL 范式下的 Token 使用量随步数的变化。
    *   **超参数敏感性分析**：对 RL 中的温度参数 $\beta$、分位数参数 $\tau$、数据来源和数据量进行了详细讨论。
*   **充分性评价**：实验设计非常充分，涵盖了性能、泛化性、效率和鲁棒性等多个维度，对比基准包含了最新的分层 RL 方法，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能卓越**：STEP-HRL 在 ScienceWorld 和 ALFWorld 上的表现显著优于所有基准模型，尤其在 Unseen 任务上展现了极强的泛化能力。
*   **效率极高**：相比于传统的历史条件化 RL，STEP-HRL 的单步 Token 消耗几乎保持恒定，大幅降低了推理延迟和显存占用。
*   **规模鲁棒性**：即使是较小规模的模型（如 Llama-1B），在 STEP-HRL 框架下也能达到甚至超过某些 7B 规模的基准模型。
*   **局部进度的重要性**：消融实验证明，如果没有局部进度模块对历史进行压缩，智能体在长程任务中极易迷失。

### 7. 优点（亮点）
*   **架构创新**：通过“局部进度”模块巧妙地解决了 LLM 智能体长上下文依赖的痛点，实现了真正的“步级”决策。
*   **训练高效**：采用参数共享设计（高层、底层、进度策略共享底座），减少了训练开销并促进了知识迁移。
*   **推理友好**：恒定的输入长度使得推理成本可预测，适合实际部署。

### 8. 不足与局限
*   **数据依赖**：该方法高度依赖高质量的专家演示数据，特别是需要通过 LLM（如 DeepSeek）预先生成子任务和进度标注，这在缺乏专家轨迹的领域难以应用。
*   **终止判定风险**：底层策略在执行动作的同时需要预测子任务是否结束，如果预测失误（提前结束或延迟结束），会影响高层规划的连贯性。
*   **标注成本**：虽然使用了自动化标注，但针对不同环境设计合适的进度生成 Prompt 仍具有一定的工程复杂度。

（完）
