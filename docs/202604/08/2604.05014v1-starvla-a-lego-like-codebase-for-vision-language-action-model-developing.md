---
title: "StarVLA: A Lego-like Codebase for Vision-Language-Action Model Developing"
title_zh: StarVLA：一种用于视觉-语言-动作模型开发的乐高式代码库
authors: StarVLA Community
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05014v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 视觉-语言-动作模型开发的基准代码库
tldr: StarVLA是一个开源的视觉-语言-动作（VLA）模型开发框架，旨在解决当前VLA研究中架构碎片化和评估不统一的问题。它采用模块化的‘主干-动作头’设计，支持多种VLM和世界模型，并集成了跨具身学习策略与统一的仿真及真机评估接口。该框架通过简化的训练方案在多个基准测试中达到或超过了现有水平，显著降低了具身智能研究的门槛。
selection_source: fresh_fetch
motivation: 针对当前VLA研究中架构不兼容、代码库碎片化以及评估协议不统一导致的复现与对比困难问题。
method: 提出一种模块化的‘主干-动作头’架构，支持独立更换模型主干与动作解码方式，并集成多种训练策略与统一评估接口。
result: 提供的训练方案在LIBERO和SimplerEnv等多个基准测试中，性能达到或超过了现有的先进方法。
conclusion: StarVLA作为一个全面且易用的开源框架，有效降低了VLA模型的开发门槛，促进了具身智能领域的标准化与创新。
---

## 摘要
构建通用具身智能体需要整合感知、语言理解和动作，这些是基于多模态基础模型（包括视觉-语言模型和世界模型的最新进展）的视觉-语言-动作（VLA）方法所解决的核心能力。尽管取得了快速进展，但 VLA 方法在不兼容的架构、代码库和评估协议中仍然处于碎片化状态，阻碍了有原则的比较和可复现性。我们提出了 StarVLA，一个用于 VLA 研究的开源代码库。StarVLA 从三个方面解决了这些挑战。首先，它提供了一种模块化的“主干网络-动作头”架构，支持 VLM 主干（如 Qwen-VL）和世界模型主干（如 Cosmos），以及代表性的动作解码范式，所有这些都在一个共享的抽象层下，其中主干和动作头都可以独立更换。其次，它提供了可重用的训练策略，包括跨具身学习和多模态协同训练，这些策略在支持的范式中一致适用。第三，它通过统一的评估接口集成了主要的基准测试，包括 LIBERO、SimplerEnv、RoboTwin 2.0、RoboCasa-GR1 和 BEHAVIOR-1K，支持仿真和真实机器人部署。StarVLA 还提供了简单、完全可复现的单基准训练方案，尽管数据工程极少，但在使用 VLM 和世界模型主干的多个基准测试上，已经达到或超过了之前的方法。据我们所知，StarVLA 是目前最全面的开源 VLA 框架之一，我们期望它能降低复现现有方法和开发新方法原型的门槛。StarVLA 正在积极维护和扩展；我们将随着项目的发展更新本报告。代码和文档可在 https://github.com/starVLA/starVLA 获取。

## Abstract
Building generalist embodied agents requires integrating perception, language understanding, and action, which are core capabilities addressed by Vision-Language-Action (VLA) approaches based on multimodal foundation models, including recent advances in vision-language models and world models. Despite rapid progress, VLA methods remain fragmented across incompatible architectures, codebases, and evaluation protocols, hindering principled comparison and reproducibility. We present StarVLA, an open-source codebase for VLA research. StarVLA addresses these challenges in three aspects. First, it provides a modular backbone--action-head architecture that supports both VLM backbones (e.g., Qwen-VL) and world-model backbones (e.g., Cosmos) alongside representative action-decoding paradigms, all under a shared abstraction in which backbone and action head can each be swapped independently. Second, it provides reusable training strategies, including cross-embodiment learning and multimodal co-training, that apply consistently across supported paradigms. Third, it integrates major benchmarks, including LIBERO, SimplerEnv, RoboTwin~2.0, RoboCasa-GR1, and BEHAVIOR-1K, through a unified evaluation interface that supports both simulation and real-robot deployment. StarVLA also ships simple, fully reproducible single-benchmark training recipes that, despite minimal data engineering, already match or surpass prior methods on multiple benchmarks with both VLM and world-model backbones. To our best knowledge, StarVLA is one of the most comprehensive open-source VLA frameworks available, and we expect it to lower the barrier for reproducing existing methods and prototyping new ones. StarVLA is being actively maintained and expanded; we will update this report as the project evolves. The code and documentation are available at https://github.com/starVLA/starVLA.

---

## 论文详细总结（自动生成）

以下是对论文《StarVLA: A Lego-like Codebase for Vision-Language-Action Model Developing》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：具身智能（Embodied AI）正向通用智能体演进，视觉-语言-动作（VLA）模型成为核心范式。
*   **核心问题**：当前的 VLA 研究存在严重的**碎片化**问题。不同方法在架构设计（如基于 VLM 或世界模型）、代码实现、数据处理和评估协议上互不兼容。这种“巴别塔”现状导致研究者难以进行公平的基准测试、复现现有成果或组合不同技术的优势。
*   **研究目的**：开发一个像“乐高”一样模块化、统一且开源的开发库，降低 VLA 模型的开发门槛，推动标准化。

### 2. 方法论
StarVLA 的核心思想是**解耦与标准化**，主要包含以下关键技术：
*   **模块化架构（Backbone-Action-Head）**：
    *   将模型拆分为**主干网络（Backbone）**和**动作头（Action Head）**。
    *   **主干网络**：支持 VLM（如 Qwen2.5/3-VL）和世界模型（如 Cosmos-Predict2）作为掉换组件。
    *   **动作头**：实现了四种代表性范式：`FAST`（自回归标记化）、`OFT`（并行回归）、`π`（流匹配去噪）和 `GR00T`（双系统推理）。
*   **统一的 I/O 接口**：
    *   训练和推理均采用原始环境观测（Raw Images, Text），避免了复杂的预处理导致训练/测试分布偏移。
*   **灵活的训练策略**：
    *   支持行为克隆（BC）监督学习、多模态协同训练（保持 VLM 的推理能力）以及跨具身（Cross-embodiment）混合数据训练。
*   **服务器-客户端评估架构**：
    *   通过 WebSocket 实现模型推理与仿真环境的分离，解决了不同基准测试（如 LIBERO 与 SimplerEnv）依赖冲突的问题。

### 3. 实验设计
*   **集成基准测试**：涵盖了 LIBERO、SimplerEnv（包含 WidowX 和 Google Robot 场景）、RoboTwin 2.0（双臂协作）、RoboCasa-GR1（人机交互/家居场景）和 BEHAVIOR-1K。
*   **对比方法**：与 OpenVLA、$\pi_0$、$\pi_{0.5}$、GR00T-N1.5/1.6、RT-1/2、CogACT 等当前最先进的（SOTA）开源或闭源模型进行了对比。
*   **实验场景**：包括单基准测试（Specialist）、多模态协同训练实验以及跨基准/跨具身通用测试（Generalist）。

### 4. 资源与算力
论文详细记录了不同实验的算力消耗：
*   **LIBERO 训练**：使用 8 台 A100 GPU，训练 100K 步。
*   **SimplerEnv 训练**：使用 16 台 A100 GPU，训练 100K 步。
*   **RoboTwin 2.0 训练**：使用 48 台 A100 GPU，训练 150K 步。
*   **效率分析**：测试了从单节点（8x A100/H200）到多节点（最高 256x A100）的扩展性，显示在 64 卡以上时并行效率稳定在 79-80%。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 4 种动作范式在 5 个主流基准上的表现，以及不同主干网络（Qwen vs Cosmos）的对比。
*   **充分性与公平性**：
    *   **充分**：涵盖了从感知退化分析到跨具身泛化能力的全面评估。
    *   **客观**：所有实验均采用官方评估协议，且在单基准测试中不使用额外的数据增强或预训练混合数据，确保了基准线的纯净和可复现性。
    *   **公平**：通过统一框架消除了底层实现差异对算法性能的影响。

### 6. 主要结论与发现
*   **模块化有效性**：证明了单一的“主干-动作头”抽象可以兼容极其多样的 VLA 范式。
*   **性能领先**：StarVLA 提供的简单训练方案在多个基准上达到或超过了现有 SOTA。例如，在 LIBERO 上仅用 1/6 的步数就达到了与 OpenVLA 相当的性能。
*   **协同训练的重要性**：纯动作训练会导致 VLM 的感知能力快速下降，而 StarVLA 的协同训练策略能有效保持空间接地和推理能力。
*   **通用性潜力**：单一模型在跨具身混合训练后，在 RoboCasa 等复杂任务上的表现甚至优于专门训练的专家模型。

### 7. 优点
*   **高度解耦**：像乐高一样灵活，研究者可以独立研究主干网络或动作解码器的改进。
*   **工程质量高**：解决了具身智能研究中常见的环境依赖冲突和部署难题。
*   **复现性强**：提供了详尽的 YAML 配置和训练脚本，且性能基准非常扎实。
*   **全面性**：是目前最完整的开源 VLA 框架之一，同时支持 VLM 和世界模型。

### 8. 不足与局限
*   **强化学习（RL）缺失**：虽然文中提到计划支持 RL 微调，但当前版本主要集中在监督学习和协同训练。
*   **算力门槛**：尽管代码高效，但训练高性能 VLA 模型仍需大量的 A100/H200 算力，对小型研究团队仍有挑战。
*   **应用范围**：目前主要集中在机械臂操纵任务，对于足式机器人或更复杂的全身控制（Whole-body control）涉及较少。

（完）
