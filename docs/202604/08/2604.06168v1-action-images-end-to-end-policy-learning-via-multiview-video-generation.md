---
title: "Action Images: End-to-End Policy Learning via Multiview Video Generation"
title_zh: 动作图像：通过多视图视频生成的端到端策略学习
authors: "Haoyu Zhen, Zixian Gao, Qiao Sun, Yilin Zhao, Yuncong Yang, Yilun Du, Tsun-Hsuan Wang, Yi-Ling Qiao, Chuang Gan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06168v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于机器人策略学习的世界动作模型
tldr: Action Images 提出了一种将机器人策略学习转化为多视图视频生成的统一框架。该方法不使用传统的低维动作向量，而是将 7 自由度动作编码为像素级的“动作图像”，即追踪机械臂运动的多视图视频。这使得预训练视频模型能直接作为零样本策略使用，无需额外策略头。实验证明该方法在 RLBench 和真实场景中表现优异，显著提升了视频与动作的联合生成质量及跨环境迁移能力。
selection_source: fresh_fetch
motivation: 现有模型因动作表示与像素空间脱节，难以充分利用预训练视频模型的先验知识，且在不同视角和环境间的迁移效果较差。
method: 将机器人动作转化为可解释的像素级多视图动作视频，并利用统一的视频生成骨干网络同时处理控制、视频生成和动作标注任务。
result: 在 RLBench 和真实世界测试中，该模型取得了领先的零样本成功率，并显著改善了视频-动作联合生成的保真度。
conclusion: 将动作表示为像素对齐的图像是实现端到端策略学习和多模态世界模型统一的有效路径。
---

## 摘要
世界动作模型 (WAMs) 已成为机器人策略学习的一个极具前景的方向，因为它们可以利用强大的视频骨干网络来建模未来状态。然而，现有方法通常依赖于独立的动作模块，或者使用非像素对齐（pixel-grounded）的动作表示，这使得充分利用视频模型的预训练知识变得困难，并限制了在不同视角和环境之间的迁移。在这项工作中，我们提出了 Action Images，这是一种统一的世界动作模型，将策略学习公式化为多视图视频生成。我们没有将控制编码为低维 token，而是将 7 自由度 (7-DoF) 机器人动作转化为可解释的动作图像：即在 2D 像素中对齐并显式跟踪机械臂运动的多视图动作视频。这种像素对齐的动作表示允许视频骨干网络本身充当零样本策略，而无需独立的策略头或动作模块。除了控制之外，同一个统一模型还支持在共享表示下的视频-动作联合生成、动作条件视频生成以及动作标注。在 RLBench 和真实世界评估中，我们的模型实现了最强的零样本成功率，并比之前的视频空间世界模型提高了视频-动作联合生成的质量，这表明可解释的动作图像是策略学习的一条极具前景的路径。

## Abstract
World action models (WAMs) have emerged as a promising direction for robot policy learning, as they can leverage powerful video backbones to model the future states. However, existing approaches often rely on separate action modules, or use action representations that are not pixel-grounded, making it difficult to fully exploit the pretrained knowledge of video models and limiting transfer across viewpoints and environments. In this work, we present Action Images, a unified world action model that formulates policy learning as multiview video generation. Instead of encoding control as low-dimensional tokens, we translate 7-DoF robot actions into interpretable action images: multi-view action videos that are grounded in 2D pixels and explicitly track robot-arm motion. This pixel-grounded action representation allows the video backbone itself to act as a zero-shot policy, without a separate policy head or action module. Beyond control, the same unified model supports video-action joint generation, action-conditioned video generation, and action labeling under a shared representation. On RLBench and real-world evaluations, our model achieves the strongest zero-shot success rates and improves video-action joint generation quality over prior video-space world models, suggesting that interpretable action images are a promising route to policy learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Action Images: End-to-End Policy Learning via Multiview Video Generation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的机器人世界模型（World Models）虽然在预测未来视频方面表现出色，但在将其转化为可泛化的控制策略（Policy）时存在巨大鸿沟。
*   **研究动机**：作者认为这种鸿沟源于**动作表示（Action Representation）与像素空间的脱节**。目前的模型通常将动作视为低维向量或离散 token，并依赖额外的策略头（Policy Head）来解码控制信号。这导致视频模型的预训练知识无法直接迁移到控制任务中，且在不同视角或机器人形态间难以泛化。

### 2. 方法论
*   **核心思想**：将 7 自由度（7-DoF）的机器人动作转化为**像素对齐的多视图“动作图像”**。通过这种方式，策略学习被简化为一种类似于“视觉跟踪”的视频生成任务，使视频骨干网络能够直接生成可执行的动作。
*   **关键技术细节**：
    1.  **动作图像化（Action as Images）**：将 7-DoF 动作（位置、姿态、夹爪状态）转换为 3 个语义 3D 点（位置点、法向量点、向上向量点）。
    2.  **多视图渲染**：将这些 3D 点投影到不同相机的 2D 像素平面，并渲染为 RGB 高斯热图。
        *   **红色通道**：末端执行器位置。
        *   **绿色通道**：法向矢量。
        *   **蓝色通道**：向上矢量 + 夹爪开合度（在低响应背景区域编码）。
    3.  **统一骨干网络**：基于预训练的视频生成模型（Wan 2.2），将视频 Latent 和动作图像 Latent 在时间维度拼接，通过不同的掩码策略（Masking Strategies）进行统一训练。
    4.  **动作解码（Decoding）**：采用多视图几何方法。先在主视图确定 2D 锚点，通过射线投射（Ray Casting）在 3D 空间采样候选点，再利用侧视图热图进行匹配，从而无损地恢复 3D 动作。

### 3. 实验设计
*   **数据集**：
    *   **RLBench**（仿真）：180k 轨迹，提供精确的相机标定和动作。
    *   **DROID**（真实）：80k 轨迹，用于真实世界学习。
    *   **BridgeV2**（真实）：30k 轨迹，用于增强视频生成能力。
*   **对比方法**：
    *   **VLA 模型**：$\pi_{0.5}$、MolmoAct。
    *   **扩散策略**：MV-Policy（多视图扩散策略）。
    *   **世界模型**：TesserAct、Cosmos-Policy。
*   **评估场景**：
    *   **零样本（Zero-shot）泛化**：在未见过的任务、物体和环境（包括真实世界 xArm 机器人）中测试。
    *   **多任务能力**：视频-动作联合生成、动作条件视频生成、视频动作标注。

### 4. 资源与算力
*   **算力投入**：使用了 **32 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   基于 Wan 2.2 (14B 参数) 视频模型进行微调。
    *   训练步数为 100,000 步。
    *   使用了 DeepSpeed ZeRO 优化技术和混合精度训练（bfloat16）。
    *   推理端通过并行化和缓存技术，在 8 台 H100 上可达到约 71 FPS 的处理速度。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真环境（RLBench 的多个任务）和真实世界（xArm 平台）的对比。
*   **充分性**：实验设计较为全面，不仅对比了成功率，还通过 PSNR、SSIM、FVD 等指标评估了视频生成质量，并对比了 2D/3D 轨迹误差。
*   **客观性**：通过在完全未见过的真实场景中进行零样本测试，验证了模型的泛化能力，而非仅仅在训练集分布内进行评估。

### 6. 主要结论与发现
*   **像素对齐是关键**：将动作表示为图像显著提升了策略的泛化能力，尤其是在跨视角和跨环境任务中。
*   **零样本能力强**：Action Images 在 RLBench 和真实世界任务中均取得了最高的零样本成功率，优于现有的 VLA 和世界模型。
*   **统一性**：同一个模型可以同时胜任预测（世界模型）和执行（策略模型），且在动作标注任务上优于专门的跟踪算法（如 TAPIR）。

### 7. 优点
*   **架构优雅**：消除了独立的策略头，实现了真正的端到端视频-动作统一建模。
*   **可解释性**：动作以热图形式存在，用户可以直接观察模型“计划”的运动轨迹。
*   **利用先验**：极大地释放了大规模预训练视频模型在物理规律理解上的潜力。

### 8. 不足与局限
*   **开环限制**：目前主要展示的是开环（Open-loop）执行结果，尚未完全整合进高频闭环控制系统。
*   **推理延迟**：尽管有优化，但基于扩散模型的生成过程相比于轻量级策略网络仍有较高的计算开销。
*   **数据依赖**：模型性能高度依赖于多视图数据的质量和相机标定的准确性，对于缺乏标定的野外数据利用率有限。

（完）
