---
title: "A1: A Fully Transparent Open-Source, Adaptive and Efficient Truncated Vision-Language-Action Model"
title_zh: A1：一个完全透明、开源、自适应且高效的截断式视觉-语言-动作模型
authors: "Kaidong Zhang, Jian Zhang, Rongtao Xu, Yu Sun, Shuoshuo Xue, Youpeng Wen, Xiaoyu Guo, Minghao Guo, Weijia Liufu, Liu Zihou, Kangyi Ji, Yangsong Zhang, Jiarun Zhu, Jingzhi Liu, Zihang Li, Ruiyi Chen, Meng Cao, Jingming Zhang, Shen Zhao, Xiaojun Chang, Feng Zheng, Ivan Laptev, Xiaodan Liang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05672v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于机器人操作的高效截断式视觉-语言-动作模型
tldr: "A1是一个开源透明的视觉-语言-动作（VLA）模型框架，旨在解决现有模型推理成本高、延迟大的问题。它通过预训练VLM提供先验，并引入预算感知的自适应推理方案，包括中间层动作一致性监测触发的提前终止机制，以及跨层截断流匹配技术。在多个仿真和真实机器人基准测试中，A1在显著降低计算成本（最高减少76.6%）的同时，实现了超越pi0等基准模型的SOTA成功率。"
selection_source: fresh_fetch
motivation: 现有的VLA模型由于庞大的骨干网络和复杂的动作生成头，导致在普通硬件上实时控制成本极高且延迟严重。
method: 提出了一种自适应推理方案，通过监测中间层动作一致性实现提前终止，并利用跨层截断流匹配技术加速去噪过程。
result: "在RoboChallenge等多个基准测试中取得SOTA表现，且在保持性能的同时，推理延迟降低了高达72%，骨干网络计算量减少了76.6%。"
conclusion: A1证明了通过优化推理流程和模型架构，可以在不牺牲操作成功率的前提下，实现高效、低成本且完全透明的开源VLA部署。
---

## 摘要
视觉-语言-动作（VLA）模型已成为开放世界机器人操作的一种强大范式，但其实际部署往往受限于成本：十亿级参数规模的 VLM 主干网络以及基于迭代扩散/流（diffusion/flow-based）的动作头会产生高延迟和高计算量，使得在通用硬件上进行实时控制变得昂贵。我们提出了 A1，这是一个完全开源且透明的 VLA 框架，旨在不牺牲操作成功率的情况下实现低成本、高吞吐量的推理。我们的方法利用预训练的 VLM 为动作生成提供隐式的可操作性（affordance）先验。我们发布了完整的训练栈（包括训练代码、数据/数据处理流水线、中间检查点和评估脚本），以实现端到端的复现。除了单独优化 VLM，A1 还通过引入一种预算感知的自适应推理方案来针对整个推理流水线，该方案能够同时加速主干网络和动作头。具体而言，我们通过监测 VLM 中间层的动作一致性来触发提前终止，并提出了层间截断流匹配（Inter-Layer Truncated Flow Matching），该方法可以在不同层之间热启动去噪过程，从而以显著减少的有效去噪迭代次数实现精确的动作生成。在仿真基准测试（LIBERO、VLABench）和真实机器人（Franka、AgiBot）上，A1 在显著降低推理成本的同时（例如，流匹配推理的每回合延迟降低高达 72%，主干网络计算量减少高达 76.6% 且性能下降极小），实现了最先进的成功率。在 RoboChallenge 上，A1 实现了 29.00% 的平均成功率，优于包括 pi0 (28.33%)、X-VLA (21.33%) 和 RDT-1B (15.00%) 在内的基准模型。

## Abstract
Vision--Language--Action (VLA) models have emerged as a powerful paradigm for open-world robot manipulation, but their practical deployment is often constrained by \emph{cost}: billion-scale VLM backbones and iterative diffusion/flow-based action heads incur high latency and compute, making real-time control expensive on commodity hardware. We present A1, a fully open-source and transparent VLA framework designed for low-cost, high-throughput inference without sacrificing manipulation success; Our approach leverages pretrained VLMs that provide implicit affordance priors for action generation. We release the full training stack (training code, data/data-processing pipeline, intermediate checkpoints, and evaluation scripts) to enable end-to-end reproducibility. Beyond optimizing the VLM alone, A1 targets the full inference pipeline by introducing a budget-aware adaptive inference scheme that jointly accelerates the backbone and the \emph{action head}. Specifically, we monitor action consistency across intermediate VLM layers to trigger early termination, and propose Inter-Layer Truncated Flow Matching that warm-starts denoising across layers, enabling accurate actions with substantially fewer effective denoising iterations. Across simulation benchmarks (LIBERO, VLABench) and real robots (Franka, AgiBot), A1 achieves state-of-the-art success rates while significantly reducing inference cost (e.g., up to 72% lower per-episode latency for flow-matching inference and up to 76.6% backbone computation reduction with minor performance degradation). On RoboChallenge, A1 achieves an average success rate of 29.00%, outperforming baselines including pi0(28.33%), X-VLA (21.33%), and RDT-1B (15.00%).

---

## 论文详细总结（自动生成）

这篇论文介绍了 **A1**，一个旨在解决视觉-语言-动作（VLA）模型推理成本高昂问题的开源框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 VLA 模型（如 OpenVLA, $\pi_0$）虽然在开放世界机器人操作中表现出色，但其部署成本极高。主要瓶颈在于：1) **庞大的 VLM 骨干网络**（通常为数十亿参数）；2) **迭代式的动作生成头**（如扩散模型或流匹配），需要多次去噪步骤。这导致在普通硬件上难以实现低延迟的实时控制。
*   **研究动机**：作者观察到动作生成存在冗余性——流匹配轨迹通常在极少步数内即可锁定模式，且 VLM 的中间层隐藏状态已包含足够的空间特征。因此，A1 的核心理念是“只在动作发生变化时才消耗计算资源”。

### 2. 论文提出的方法论
A1 结合了预训练的视觉语言模型（Molmo）和高效的自适应推理方案：
*   **双重动作头支持**：提供流匹配（A1-FM）和 MLP（A1-MLP）两种动作头。
*   **自适应提前终止（Early-Exit）**：在推理过程中，监测 VLM 不同层输出的动作一致性。如果相邻两层生成的动作差异低于设定阈值 $\eta_i$，则提前停止骨干网络的计算。
*   **层间截断流匹配（Inter-Layer Truncated Flow Matching）**：这是针对迭代式动作头的关键优化。在每个 VLM 层仅执行极少量的去噪步骤（如 $\delta=2$），并将当前层的输出作为下一层去噪的“热启动”初始值，而不是从随机噪声开始。这避免了提前终止机制将计算压力转移到动作头上的问题。
*   **预算感知阈值校准**：通过在训练集上离线计算动作差异分布，根据预设的计算预算自动设定各层的退出阈值。

### 3. 实验设计
*   **仿真环境**：
    *   **LIBERO**：评估长程操作和知识迁移。
    *   **VLABench**：评估视觉语言理解和常识推理。
*   **真实机器人平台**：涵盖 Franka、AgiBot、WuJie-Arm（自研双臂）和 Dobot-Arm。
*   **基准测试（Benchmark）**：在 **RoboChallenge (Table30)** 这一大规模真实机器人基准上进行了全面评估。
*   **对比方法**：包括 $\pi_0$、$\pi_{0.5}$、OpenVLA、RDT-1B、X-VLA、Octo 等当前最先进的开源和闭源模型。

### 4. 资源与算力
*   **训练细节**：预训练阶段使用 AdamW 优化器，全局 Batch Size 为 1024，总步数为 200K。微调阶段根据任务不同调整 Batch Size（32 到 128 不等）。
*   **算力说明**：文中详细分析了 GFLOPs（如 Molmo-7B 推理约 11 TFLOPs），并给出了推理延迟（秒）。**但论文未明确说明具体使用的 GPU 型号（如 A100 或 H100）及具体数量。**

### 5. 实验数量与充分性
*   **实验规模**：
    *   **数据量**：使用了 DROID、AgiBot 等开源数据，并结合了 15,951 条自研真实轨迹。
    *   **测试组数**：在 RoboChallenge 上测试了 30 个任务；在真实机器人上对 7 类任务进行了每项 10 次的重复实验；在仿真中进行了数百次试验。
*   **充分性与公平性**：实验涵盖了从仿真到真实世界、从单臂到双臂的多种场景。消融实验详细探讨了不同退出比例（$c$ 值）对性能与功耗的影响，对比基准均选取了目前社区公认的 SOTA 模型，实验设计较为客观、公平。

### 6. 论文的主要结论与发现
*   **性能领先**：A1 在 RoboChallenge 上以 29.00% 的成功率超越了 $\pi_0$ (28.33%) 和 RDT-1B (15.00%)。
*   **极高的效率提升**：通过自适应推理，A1 可以在减少 **76.6%** 的骨干网络计算量的情况下，仅损失极小的成功率。
*   **推理加速**：引入层间截断流匹配后，流匹配推理的每回合延迟从 37.8s 降至 10.5s（下降 **72%**）。
*   **冗余性证实**：实验证明 VLM 的深层计算在许多简单动作中是冗余的，只有在关键操作（如精准抓取）时才需要进入深层。

### 7. 优点
*   **完全透明开源**：提供了从训练代码、数据处理到中间检查点的全栈资源，极大地促进了社区的复现和改进。
*   **端到端优化**：不仅优化了 VLM 骨干，还针对扩散/流匹配动作头的迭代开销提出了创新的“热启动”截断方案。
*   **跨平台泛化**：在四种不同的机器人硬件上验证了有效性，证明了其强大的多模态泛化能力。

### 8. 不足与局限
*   **阈值校准依赖数据**：提前终止的阈值需要先在训练集上跑一遍来获取分布，这增加了一定的离线准备时间。
*   **网络延迟挑战**：尽管模型推理变快了，但在云端推理与本地执行之间仍存在网络延迟导致的丝滑度下降问题。
*   **Affordance 标注依赖**：预训练仍部分依赖于带标签的先验数据，未来若能实现完全无监督的动作先验学习将更具扩展性。

（完）
