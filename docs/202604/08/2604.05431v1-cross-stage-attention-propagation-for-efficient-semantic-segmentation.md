---
title: Cross-Stage Attention Propagation for Efficient Semantic Segmentation
title_zh: 跨阶段注意力传播用于高效语义分割
authors: Beoungwoo Kang
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05431v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 通过跨阶段注意力传播实现高效语义分割
tldr: 针对轻量级语义分割中多尺度解码器计算冗余的问题，本文提出跨阶段注意力传播（CSAP）框架。该方法仅在最深特征尺度计算注意力，并将其传播至浅层阶段，从而避免了重复的查询-键计算。CSAP在保持多尺度上下文推理的同时显著降低了计算成本，在ADE20K、Cityscapes等数据集上以更低的功耗实现了超越SegNeXt等模型的性能。
selection_source: fresh_fetch
motivation: 现有多尺度解码器在各阶段独立计算注意力，导致高度相关的注意力分布产生大量计算冗余。
method: 提出CSAP框架，通过在最深层计算注意力并将其传播至浅层，完全省去了浅层阶段的查询-键计算。
result: "CSAP-Tiny在ADE20K上达到42.9% mIoU，相比SegNeXt-Tiny性能提升1.8%且计算量减少16.8%。"
conclusion: 跨阶段注意力传播是一种高效的解码方案，能在显著降低计算开销的同时提升语义分割的精度。
---

## 摘要
近期的轻量级语义分割方法通过结合紧凑的主干网络与高效的解码器头取得了显著进展。然而，大多数多尺度解码器在每个特征尺度上独立计算注意力，由于不同尺度间产生的注意力分布具有强相关性，这引入了大量的冗余。我们提出了跨阶段注意力传播（CSAP），这是一种解码器框架，它在最深的特征尺度上计算注意力，并将生成的注意力图传播到较浅的阶段，从而完全跳过这些阶段的查询-键（query-key）计算。该设计在保留多尺度上下文推理的同时，大幅降低了解码器的计算成本。CSAP-Tiny 在 ADE20K 上仅需 5.5 GFLOPs 即可达到 42.9% 的 mIoU，在 Cityscapes 上以 21.5 GFLOPs 达到 80.5%，在 COCO-Stuff 164K 上以 5.5 GFLOPs 达到 40.9%，在 ADE20K 上比 SegNeXt-Tiny 提升了 1.8%，同时浮点运算量减少了 16.8%。

## Abstract
Recent lightweight semantic segmentation methods have made significant progress by combining compact backbones with efficient decoder heads. However, most multi-scale decoders compute attention independently at each feature scale, introducing substantial redundancy since the resulting attention distributions across scales are strongly correlated. We propose Cross-Stage Attention Propagation (CSAP), a decoder framework that computes attention at the deepest feature scale and propagates the resulting attention maps to shallower stages, bypassing query-key computation at those stages entirely. This design preserves multi-scale contextual reasoning while substantially reducing the decoder's computational cost. CSAP-Tiny achieves 42.9% mIoU on ADE20K with only 5.5 GFLOPs, 80.5% on Cityscapes with 21.5 GFLOPs, and 40.9% on COCO-Stuff 164K with 5.5 GFLOPs, surpassing SegNeXt-Tiny by +1.8% on ADE20K while requiring 16.8% fewer floating-point operations.

---

## 论文详细总结（自动生成）

以下是对论文《Cross-Stage Attention Propagation for Efficient Semantic Segmentation》（跨阶段注意力传播用于高效语义分割）的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在轻量级语义分割任务中，多尺度解码器（Multi-scale Decoders）通常在每个特征尺度上独立计算注意力机制（Query-Key 点积）。
*   **研究动机**：作者发现，不同尺度的特征图所生成的注意力分布具有强相关性。在所有阶段重复计算 Q-K 点积会导致严重的计算冗余，尤其是在高分辨率的浅层特征图上，这种冗余极大地限制了模型的推理效率。
*   **背景**：现有的高效分割模型（如 SegFormer, SegNeXt）虽然优化了主干网络，但解码器在处理长程依赖时依然面临计算瓶颈。

### 2. 方法论
*   **核心思想**：提出**跨阶段注意力传播（CSAP）**框架。只在语义信息最丰富、分辨率最低的最深层（Stage 4）计算一次注意力图，然后将其传播并适配到较浅的阶段（Stage 2 和 3），从而完全省去浅层的 Q-K 计算。
*   **关键技术细节**：
    *   **池化交叉注意力（Pooled Cross-Attention）**：在 Stage 4，使用原始特征作为 Query，通过空间平均池化生成的上下文标记作为 Key 和 Value，降低计算复杂度。
    *   **注意力传播与精炼**：
        1.  将 Stage 4 生成的注意力图 $A_4$ 进行重塑。
        2.  通过**自适应平均池化**将其固定为统一的空间尺寸（如 8×8）。
        3.  使用阶段特定的投影层（$Proj_{\to 2}, Proj_{\to 3}$）将共享的注意力转换为适配各阶段空间特性的权重。
    *   **仅 Value 精炼（Value-Only Refinement）**：在 Stage 2 和 3，不再计算 Q 和 K，仅计算 Value 投影，直接与传播过来的注意力权重相乘得到精炼特征。
*   **算法流程**：输入图像 $\to$ 层次化编码器提取特征 $\to$ Stage 4 计算注意力图 $\to$ 注意力图下传至 Stage 2/3 $\to$ 各阶段特征融合 $\to$ 最终预测。

### 3. 实验设计
*   **数据集**：
    *   **ADE20K**：150 个类别的场景解析基准。
    *   **Cityscapes**：城市街道理解，高分辨率图像。
    *   **COCO-Stuff 164K**：包含 171 个类别的复杂场景。
*   **Benchmark 与对比方法**：
    *   以 **SegNeXt-Tiny** 为主要对比基准。
    *   对比了 SegFormer-B0, TopFormer, FeedFormer, MetaSeg-T, EDAFormer-T, VWFormer 等主流轻量级模型。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA RTX 3090 GPU** 进行训练和评估。
*   **软件**：基于 **MMSegmentation** 框架开发。
*   **训练细节**：
    *   优化器：AdamW，初始学习率 $6 \times 10^{-5}$。
    *   迭代次数：所有数据集均训练 160K 次迭代。
    *   批大小：ADE20K/COCO 为 16，Cityscapes 为 8。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个主流数据集的完整对比实验。
*   **消融实验**：
    *   对比了不同解码器设计（标准自注意力 vs. CSAP）。
    *   探究了传播源阶段的影响（证明从最深层 Stage 4 传播效果最好）。
    *   分析了注意力图的可视化一致性。
*   **充分性评价**：实验设计客观、公平，通过在相同主干网络（MSCAN-T）和训练策略下进行对比，有力证明了 CSAP 机制在降低计算量（GFLOPs）的同时保持甚至提升了精度（mIoU）。

### 6. 主要结论与发现
*   **高效性**：CSAP-Tiny 在 ADE20K 上达到 42.9% mIoU，仅需 5.5 GFLOPs。相比 SegNeXt-Tiny，精度提升 **1.8%**，计算量减少 **16.8%**。
*   **冗余验证**：可视化实验证实，传播得到的注意力图与独立计算的注意力图在空间结构上高度相似，证明了浅层 Q-K 计算确实是可省略的冗余。
*   **语义源重要性**：从最深层（Stage 4）提取注意力效果最佳，因为深层特征具有更好的语义抽象能力，生成的注意力模式更具通用性。

### 7. 优点
*   **极低冗余**：通过注意力共享机制，显著降低了多尺度解码器的矩阵乘法开销。
*   **即插即用**：该方法与具体的主干网络设计无关，可作为通用组件集成到其他层次化架构中。
*   **性能平衡**：在大幅减少算力的前提下，性能损失极小（相比全量计算仅下降 0.2% mIoU），甚至因减少过拟合在某些指标上有所超越。

### 8. 不足与局限
*   **模型规模覆盖**：论文主要集中在 Tiny/Small 规模的模型，尚未深入探讨该机制在大模型（Large/Huge）上的表现。
*   **固定池化限制**：注意力传播中使用了固定的自适应池化尺寸（8×8），对于极度复杂的场景，这种压缩可能会丢失部分精细的局部关联信息。
*   **硬件实测**：虽然 GFLOPs 显著降低，但论文未提供在移动端 SoC 或 NPU 上的实际推理延迟（Latency）数据。

（完）
