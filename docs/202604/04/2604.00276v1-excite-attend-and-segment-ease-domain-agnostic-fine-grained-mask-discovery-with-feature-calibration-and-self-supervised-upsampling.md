---
title: "Excite, Attend and Segment (EASe): Domain-Agnostic Fine-Grained Mask Discovery with Feature Calibration and Self-Supervised Upsampling"
title_zh: EASe：结合特征校准与自监督上采样的领域无关细粒度掩码发现
authors: "Deepank Singh, Anurag Nihal, Vedhus Hoskere"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00276v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用基础模型进行无监督语义分割
tldr: 针对无监督分割在复杂场景下因基础模型粗粒度特征导致细粒度细节丢失的问题，本文提出EASe框架。该框架通过SAUCE模块对特征进行通道激励与选择性校准，并结合空间编码实现全分辨率语义恢复。利用无需训练的CAFE聚合器，EASe能直接在像素级生成多粒度掩码，在多个标准基准和复杂形态数据集上显著超越了现有SOTA方法。
selection_source: fresh_fetch
motivation: 现有的无监督分割方法多依赖粗粒度的补丁级表示，难以捕捉复杂形态场景中的细粒度结构细节。
method: 提出SAUCE模块进行语义感知上采样与通道激励，并结合CAFE聚合器在像素级实现多粒度特征聚合与掩码生成。
result: 实验结果表明，EASe在各大标准基准测试及具有复杂形态的多元数据集上均取得了优于现有最先进方法的性能。
conclusion: EASe通过像素级特征校准与自监督上采样，为跨领域的细粒度无监督语义掩码发现提供了一种高效且通用的解决方案。
---

## 摘要
无监督分割方法越来越多地利用基础模型（FM）来改进显著物体发现。然而，这些方法在具有复杂、多组件形态的场景中往往表现不佳，而这些场景中细粒度的结构细节是必不可少的。许多最先进的无监督分割流水线依赖于利用粗粒度、补丁级（patch-level）表示的掩码发现方法。这些粗糙的表示本质上抑制了解决此类复杂形态所需的细粒度细节。为了克服这一局限性，我们提出了 EASe（Excite, Attend and Segment），这是一个无监督且领域无关的语义分割框架，旨在挑战性的真实场景中实现便捷的细粒度掩码发现。EASe 利用一种新颖的带通道激励的语义感知上采样（SAUCE）来激励低分辨率基础模型特征通道以进行选择性校准，并在空间编码的图像和基础模型特征之间进行注意力计算，以恢复全分辨率语义表示。最后，EASe 使用一种新颖的无需训练的提示注意力特征聚合器（CAFE），将聚合后的特征分割成多粒度掩码，该聚合器利用 SAUCE 注意力分数作为语义分组信号。EASe 结合 SAUCE 和 CAFE，直接在像素级特征表示上运行，从而实现准确的细粒度密集语义掩码发现。我们的评估表明，在主要标准基准测试和具有复杂形态的多样化数据集上，EASe 的性能优于之前的最先进技术（SOTA）。代码可在 https://ease-project.github.io 获取。

## Abstract
Unsupervised segmentation approaches have increasingly leveraged foundation models (FM) to improve salient object discovery. However, these methods often falter in scenes with complex, multi-component morphologies, where fine-grained structural detail is indispensable. Many state-of-the-art unsupervised segmentation pipelines rely on mask discovery approaches that utilize coarse, patch-level representations. These coarse representations inherently suppress the fine-grained detail required to resolve such complex morphologies. To overcome this limitation, we propose Excite, Attend and Segment (EASe), an unsupervised domain-agnostic semantic segmentation framework for easy fine-grained mask discovery across challenging real-world scenes. EASe utilizes novel Semantic-Aware Upsampling with Channel Excitation (SAUCE) to excite low-resolution FM feature channels for selective calibration and attends across spatially-encoded image and FM features to recover full-resolution semantic representations. Finally, EASe segments the aggregated features into multi-granularity masks using a novel training-free Cue-Attentive Feature Aggregator (CAFE) which leverages SAUCE attention scores as a semantic grouping signal. EASe, together with SAUCE and CAFE, operate directly at pixel-level feature representations to enable accurate fine-grained dense semantic mask discovery. Our evaluation demonstrates superior performance of EASe over previous state-of-the-arts (SOTAs) across major standard benchmarks and diverse datasets with complex morphologies. Code is available at https://ease-project.github.io

---

## 论文详细总结（自动生成）

这是一份关于论文《Excite, Attend and Segment (EASe): Domain-Agnostic Fine-Grained Mask Discovery with Feature Calibration and Self-Supervised Upsampling》的结构化技术总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的无监督语义分割方法主要依赖基础模型（如 DINO, CLIP）提取的特征。然而，这些特征通常处于粗糙的“补丁级”（patch-level）分辨率，导致在处理具有复杂形态、细长结构或多组件特征的场景（如裂缝检测、基础设施缺陷、遥感影像）时，细粒度细节严重丢失。
*   **核心问题**：如何克服基础模型的粗粒度特征瓶颈，在无需人工标注的情况下，实现跨领域的像素级细粒度掩码发现？

### 2. 论文提出的方法论
EASe 框架由两个协同组件组成，旨在将粗糙的语义标记提升为高分辨率的分割掩码：

*   **SAUCE（带通道激励的语义感知上采样）**：
    *   **核心思想**：一种自监督的上采样模块，通过通道校准增强语义判别力。
    *   **技术细节**：利用 **Squeeze-and-Excitation (SE)** 块对低分辨率特征通道进行重加权，放大对目标领域重要的维度。
    *   **流程**：使用轻量级编码器提取图像的高分辨率查询（Query），与经过 SE 校准的低分辨率键（Key）和值（Value）进行交叉注意力计算，将语义信息插值到每个像素。
*   **CAFE（提示注意力特征聚合器）**：
    *   **核心思想**：一种**无需训练**的聚合模块，直接复用 SAUCE 的注意力分数作为语义分组信号。
    *   **算法流程**：
        1.  **提示发现（CRS）**：通过跨分辨率播种初始化原型字典，并迭代精炼。
        2.  **注意力引导分组（AGG）**：结合特征相似度和空间注意力亲和力，将像素分配给最相关的原型。
        3.  **层次合并（HM）**：通过凝聚聚类生成从细到粗的多粒度掩码序列，并利用校准的粒度分数自动选择目标层级。

### 3. 实验设计
*   **数据集**：涵盖了 9 个不同领域的基准测试：
    *   **标准基准**：Pascal VOC, COCO-Object, COCO-Stuff, Cityscapes, ADE20K, PartImageNet, KITTI。
    *   **领域特定（复杂形态）**：OmniCrack30K（裂缝检测）、RSDD（屋顶损伤检测）。
*   **对比方法**：与 MaskCut, DiffSeg, DiffCut, STEGO, ReCO 等多种 SOTA 无监督分割方法进行对比。
*   **评估指标**：主要使用平均交并比（mIoU），在裂缝数据集上额外使用了中心线 IoU（clIoU）。

### 4. 资源与算力
*   **训练细节**：SAUCE 模块在 ImageNet 上进行了自监督预训练，共计 127,500 个步长（steps）。
*   **优化器**：使用 AdamW，学习率为 $2 \times 10^{-4}$。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但提到其架构具有可扩展性，且 CAFE 聚合阶段是无需训练的（training-free），这降低了推理时的计算负担。

### 5. 实验数量与充分性
*   **实验规模**：在 9 个数据集上进行了广泛测试，实验覆盖了从物体中心到场景级、从零件级到专业工业检测的多种任务。
*   **消融实验**：非常充分。作者分别对 SAUCE 的通道激励（SE）、查询-键维度（$d_{qk}$）、缩减率（$r$）以及 CAFE 的各个阶段（CRS, AGG, HM）进行了详尽的消融研究。
*   **客观性**：通过在不同骨干网络（DINOv3-S/B/L）上验证，证明了方法的通用性；使用了统一的超参数设置跨数据集运行，体现了实验的公平性。

### 6. 主要结论与发现
*   **性能领先**：EASe 在 9 个基准中的 8 个上达到了 SOTA。在标准数据集上平均提升约 +3.7 mIoU，在复杂形态数据集（如 RSDD）上提升高达 **+30.3 mIoU**。
*   **细粒度优势**：相比于依赖图像空间细化（如 CRF）的方法，EASe 在处理断裂、细长或碎片化目标时表现出极强的鲁棒性。
*   **通道校准的重要性**：实验证明，随着骨干网络维度增加（如从 ViT-S 到 ViT-L），SAUCE 的通道激励模块对性能的提升愈发显著。

### 7. 优点
*   **像素级精度**：摆脱了补丁级表示的限制，直接在全分辨率特征空间操作。
*   **领域无关性**：无需针对特定数据集进行微调，即可在自然图像和专业工业图像间无缝切换。
*   **多粒度输出**：CAFE 能够一次性生成不同层级的分割结果，适应不同的标注需求。
*   **高效性**：取代了昂贵的谱图切割（Graph Cuts）和复杂的迭代细化过程。

### 8. 不足与局限
*   **粒度选择依赖**：虽然是无监督方法，但在评估时仍需利用极少量（约 200 张）标注图像来校准目标层级的粒度分数（$G_{scale}, G_{sem}$）。
*   **极细结构挑战**：在 OmniCrack30K 的某些子集中，对于极细的“发丝级”裂缝，虽然优于前人，但绝对得分（clIoU）仍有提升空间。
*   **预训练依赖**：SAUCE 的性能高度依赖于其自监督预训练的质量，且需要针对不同的骨干网络进行适配训练。

（完）
