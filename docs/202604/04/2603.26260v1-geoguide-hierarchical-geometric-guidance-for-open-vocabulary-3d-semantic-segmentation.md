---
title: "GeoGuide: Hierarchical Geometric Guidance for Open-Vocabulary 3D Semantic Segmentation"
title_zh: GeoGuide：面向开放词汇 3D 语义分割的层级几何引导
authors: "Xujing Tao, Chuxin Wang, Yubo Ai, Zhixin Cheng, Zhuoyuan Li, Liangsheng Liu, Yujia Chen, Xinjun Li, Qiao Li, Wenfei Yang, Tianzhu Zhang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26260v1"
tags: ["query:mmseg"]
score: 9.0
evidence: 利用预训练模型的开放词汇3D语义分割
tldr: 针对开放词汇3D语义分割中过度依赖2D蒸馏导致几何特征缺失和误差继承的问题，本文提出GeoGuide框架。该框架利用预训练3D模型，通过不确定性超点蒸馏、实例级掩码重建和实例间关系一致性三大模块，在不同层级上融合几何与语义信息。GeoGuide有效抑制了2D噪声，增强了局部与全局的语义一致性，在ScanNet v2等多个主流数据集上取得了领先性能。
selection_source: fresh_fetch
motivation: 现有方法过度依赖2D模型蒸馏，限制了3D几何特征的学习并容易继承2D预测的噪声和误差。
method: 提出包含超点蒸馏、实例掩码重建和实例间关系一致性的分层几何引导框架，利用3D几何先验校准语义特征。
result: 在ScanNet v2、Matterport3D和nuScenes等多个数据集上的实验结果证明了该方法优于现有的开放词汇3D分割模型。
conclusion: GeoGuide通过多层级几何-语义一致性建模，显著提升了开放词汇3D语义分割的准确性和鲁棒性。
---

## 摘要
开放词汇 3D 语义分割旨在分割训练集之外的任意类别。现有方法主要依赖于从 2D 开放词汇模型中蒸馏知识。然而，将 3D 特征对齐到 2D 表示空间限制了内在的 3D 几何学习，并继承了 2D 预测中的误差。为了解决这些局限性，我们提出了 GeoGuide，这是一个利用预训练 3D 模型来整合层级几何-语义一致性的新型开放词汇 3D 分割框架。具体而言，我们引入了一个基于不确定性的超点蒸馏模块，用于融合几何和语义特征以估计逐点不确定性，自适应地加权超点内的 2D 特征，在抑制噪声的同时保留判别性信息，从而增强局部语义一致性。此外，我们的实例级掩码重建模块利用几何先验，通过重建完整的实例掩码来强制执行实例内的语义一致性。此外，我们的实例间关系一致性模块对齐几何和语义相似性矩阵，以校准同类别对象之间的跨实例一致性，从而减轻视点诱导的语义漂移。在 ScanNet v2、Matterport3D 和 nuScenes 上的广泛实验证明了 GeoGuide 的卓越性能。

## Abstract
Open-vocabulary 3D semantic segmentation aims to segment arbitrary categories beyond the training set. Existing methods predominantly rely on distilling knowledge from 2D open-vocabulary models. However, aligning 3D features to the 2D representation space restricts intrinsic 3D geometric learning and inherits errors from 2D predictions. To address these limitations, we propose GeoGuide, a novel framework that leverages pretrained 3D models to integrate hierarchical geometry-semantic consistency for open-vocabulary 3D segmentation. Specifically, we introduce an Uncertainty-based Superpoint Distillation module to fuse geometric and semantic features for estimating per-point uncertainty, adaptively weighting 2D features within superpoints to suppress noise while preserving discriminative information to enhance local semantic consistency. Furthermore, our Instance-level Mask Reconstruction module leverages geometric priors to enforce semantic consistency within instances by reconstructing complete instance masks. Additionally, our Inter-Instance Relation Consistency module aligns geometric and semantic similarity matrices to calibrate cross-instance consistency for same-category objects, mitigating viewpoint-induced semantic drift. Extensive experiments on ScanNet v2, Matterport3D, and nuScenes demonstrate the superior performance of GeoGuide.

---

## 论文详细总结（自动生成）

这是一份关于论文《GeoGuide: Hierarchical Geometric Guidance for Open-Vocabulary 3D Semantic Segmentation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的开放词汇 3D 语义分割方法过度依赖从 2D 模型（如 CLIP、OpenSeg）向 3D 模型的知识蒸馏。
*   **研究背景与动机**：
    *   **几何缺失**：简单的 2D-3D 对齐忽略了 3D 空间固有的几何结构。
    *   **误差继承**：2D 模型常因遮挡、光照或视角变化产生错误预测，3D 模型在蒸馏过程中会“继承”这些噪声。
    *   **不一致性**：同一物体在不同视角下的 2D 特征存在偏差，导致 3D 空间中语义表示不连续。
*   **整体含义**：提出 **GeoGuide** 框架，旨在利用预训练 3D 模型的几何先验，通过层级化的约束来校准和引导 2D 知识的蒸馏过程。

### 2. 方法论与关键技术
GeoGuide 核心思想是构建**层级几何-语义一致性**，包含三个关键模块：
*   **基于不确定性的超点蒸馏 (USD)**：
    *   **局部一致性**：利用超点（几何同质区域）应具有相同语义的特性。
    *   **技术细节**：融合 3D 几何特征与 2D 语义特征，通过 MLP 预测逐点“可靠性权重”。利用该权重对超点内的 2D 特征进行加权聚合，从而抑制 2D 预测中的边界噪声。
*   **实例级掩码重建 (IMR)**：
    *   **实例内一致性**：利用类无关的 3D 实例分割结果，通过“掩码重建”任务强制模型学习完整的实例表示。
    *   **流程**：随机遮蔽实例的一部分，利用剩余部分的特征重建完整掩码，确保实例内部语义特征的统一性。
*   **实例间关系一致性 (IIRC)**：
    *   **全局一致性**：利用预训练 3D 模型对同类物体具有相似几何表示的特性。
    *   **技术细节**：计算实例间（及超点间）的几何相似性矩阵和语义相似性矩阵，通过 MSE 损失对齐两者。这能有效缓解因视角变化导致的同类物体语义漂移。

### 3. 实验设计
*   **数据集**：
    *   **ScanNet v2**（室内 RGB-D 场景，20 类）。
    *   **Matterport3D**（大规模室内建筑，21 类基准，最高扩展至 160 类）。
    *   **nuScenes**（室外自动驾驶 LiDAR 序列，23 类）。
*   **Benchmark 与对比方法**：
    *   对比了 **OpenScene**、**GGSD**、**PLA**、**SAS** 等最先进的零样本（Zero-shot）方法。
    *   同时对比了 MinkowskiNet、PTv3 等全监督方法以衡量差距。
*   **评价指标**：平均交并比 (mIoU) 和平均准确率 (mAcc)。

### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA A6000 GPU** 上完成。
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 4。室内场景体素大小设为 2cm，室外设为 5cm。
*   **说明**：文中强调该方法仅需纯 3D 模型进行推理，无需 SAS 或 GGSD 中耗时的自蒸馏策略。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流数据集上进行了标准性能测试。
    *   **长尾场景评估**：在 Matterport3D 上测试了从 21 类到 160 类的泛化能力。
    *   **跨域泛化实验**：进行了 ScanNet $\leftrightarrow$ Matterport3D 的双向迁移测试（不进行微调）。
    *   **消融实验**：详细验证了 USD、IMR、IIRC 三个模块以及预训练权重对结果的影响。
*   **充分性评价**：实验设计非常充分，涵盖了室内/室外、闭集/长尾、同域/跨域等多个维度，对比基准统一，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：GeoGuide 在所有数据集上均达到 SOTA。在 ScanNet v2 上达到 **64.8 mIoU**，比强基线 SAS 提升了 5.6%。
*   **几何校准有效**：实验证明，直接引入 3D 预训练特征若无显式几何约束，性能提升有限甚至会下降；而 GeoGuide 的层级约束能有效释放 3D 预训练模型的潜力。
*   **鲁棒性强**：在类别增加（长尾）和跨数据集测试中，GeoGuide 的性能下降幅度远小于其他方法，证明其捕获的是领域无关的几何结构特征。

### 7. 优点
*   **层级化设计**：从局部超点到单个实例，再到全局实例间关系，构建了完整的几何引导体系。
*   **噪声抑制**：通过不确定性权重机制，巧妙解决了 2D 蒸馏中固有的预测噪声问题。
*   **泛化能力出色**：不依赖特定 2D 提取器，在 LSeg 和 OpenSeg 特征下均有显著提升。

### 8. 不足与局限
*   **依赖外部组件**：IMR 模块依赖于预先生成的类无关 3D 实例分割结果，若实例分割质量较差，可能会影响重建效果。
*   **计算开销**：虽然推理时丢弃了引导模块，但训练阶段需要计算多个相似性矩阵（尤其是实例间和超点间），对于超大规模场景可能会增加显存压力。
*   **实时性未提及**：论文未详细讨论训练耗时与推理延迟的具体数值。

（完）
