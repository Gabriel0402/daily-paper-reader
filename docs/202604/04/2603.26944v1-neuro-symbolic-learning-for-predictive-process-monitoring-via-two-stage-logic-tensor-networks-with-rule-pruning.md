---
title: Neuro-Symbolic Learning for Predictive Process Monitoring via Two-Stage Logic Tensor Networks with Rule Pruning
title_zh: 基于带规则剪枝的两阶段逻辑张量网络的预测性过程监控神经符号学习
authors: "Fabrizio De Santis, Gyunam Park, Francesco Zanichelli"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26944v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 在神经符号学习中使用线性时态逻辑形式化时间知识
tldr: 本研究针对预测性流程监控中数据驱动方法忽视领域约束的问题，提出一种基于逻辑张量网络（LTN）的神经符号学习方法。通过将控制流、时间和负载知识形式化为逻辑约束，并引入两阶段优化策略（加权公理损失预训练与规则剪枝），有效解决了逻辑约束与预测准确性之间的冲突。在医疗和金融等四个真实数据集上的实验表明，该方法在提升预测性能的同时确保了合规性，尤其在受限样本场景下表现优异。
selection_source: fresh_fetch
motivation: 传统数据驱动方法难以整合领域特定的顺序约束和逻辑规则，导致在医疗、金融等严监管领域的预测准确性和合规性不足。
method: 提出一种集成线性时序逻辑（LTL）的LTN框架，并采用两阶段优化策略，通过加权公理损失预训练和基于满足度动态的规则剪枝来平衡数据学习与知识注入。
result: 在四个真实世界事件日志上的评估显示，该方法显著提升了预测性能，且两阶段优化策略在防止知识注入导致性能下降方面起到了关键作用。
conclusion: 神经符号方法能有效结合领域知识与深度学习，在确保业务流程合规性的同时，显著增强了模型在复杂约束和有限数据环境下的预测能力。
---

## 摘要
对序列事件数据的预测建模对于欺诈检测和医疗监控至关重要。现有的数据驱动方法从历史数据中学习相关性，但未能纳入特定领域的序列约束以及管理事件关系的逻辑规则，从而限制了准确性和监管合规性。例如，医疗程序必须遵循特定的序列，而金融交易必须遵守合规规则。我们提出了一种神经符号方法，利用逻辑张量网络（LTNs）将领域知识集成为可微逻辑约束。我们使用线性时序逻辑和一阶逻辑对控制流、时序和有效载荷知识进行形式化。我们的核心贡献是一种两阶段优化策略，旨在解决 LTNs 倾向于以牺牲预测准确性为代价来满足逻辑公式的问题。该方法在预训练期间使用加权公理损失来优先进行数据学习，随后根据满足度动态进行规则剪枝，仅保留一致且有贡献的公理。对四个真实世界事件日志的评估表明，领域知识注入显著提高了预测性能，而两阶段优化被证明是至关重要的（如果没有它，知识注入可能会严重降低性能）。该方法在合规样本有限的合规受限场景中表现尤为出色，在确保遵守领域约束的同时，实现了优于纯数据驱动基准模型的性能。

## Abstract
Predictive modeling on sequential event data is critical for fraud detection and healthcare monitoring. Existing data-driven approaches learn correlations from historical data but fail to incorporate domain-specific sequential constraints and logical rules governing event relationships, limiting accuracy and regulatory compliance. For example, healthcare procedures must follow specific sequences, and financial transactions must adhere to compliance rules. We present a neuro-symbolic approach integrating domain knowledge as differentiable logical constraints using Logic Networks (LTNs). We formalize control-flow, temporal, and payload knowledge using Linear Temporal Logic and first-order logic. Our key contribution is a two-stage optimization strategy addressing LTNs' tendency to satisfy logical formulas at the expense of predictive accuracy. The approach uses weighted axiom loss during pretraining to prioritize data learning, followed by rule pruning that retains only consistent, contributive axioms based on satisfaction dynamics. Evaluation on four real-world event logs shows that domain knowledge injection significantly improves predictive performance, with the two-stage optimization proving essential knowledge (without it, knowledge can severely degrade performance). The approach excels particularly in compliance-constrained scenarios with limited compliant training examples, achieving superior performance compared to purely data-driven baselines while ensuring adherence to domain constraints.

---

## 论文详细总结（自动生成）

以下是对论文《Neuro-Symbolic Learning for Predictive Process Monitoring via Two-Stage Logic Tensor Networks with Rule Pruning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：现代组织（医疗、金融、制造）产生大量带时间戳的事件日志。预测性过程监控（PPM）旨在预测流程结果或异常。
*   **核心问题**：现有的深度学习方法（如 LSTM、Transformer）是纯数据驱动的，虽然能捕捉统计相关性，但**无法整合显式的领域约束和业务规则**（如“手术前必须进行体检”）。
*   **技术挑战**：直接使用逻辑张量网络（LTN）等神经符号框架时，模型往往会为了追求逻辑公式的“满足度”而牺牲预测准确性，导致模型学习到“逻辑捷径”而非真实的预测模式。

### 2. 方法论：核心思想与关键技术
该论文提出了一种集成领域知识的神经符号框架，核心在于**两阶段优化策略与规则剪枝机制**：
*   **知识形式化**：
    *   **控制流知识**：使用线性时序逻辑（LTL）定义活动顺序，并转换为一阶逻辑（FOL）。
    *   **时间知识**：定义活动间的等待时间或持续时间约束。
    *   **负载知识**：定义属性（如年龄、指标）与结果之间的逻辑关系。
*   **两阶段优化流程**：
    1.  **阶段一：加权预训练与规则评估**。使用加权损失函数 $L = 1 - (\alpha \cdot Sat(K_D) + \beta \cdot Sat(K_P))$，其中数据公理权重 $\alpha$ 高于知识公理权重 $\beta$（如 0.8 vs 0.2）。这确保了模型先建立准确的分类基础，避免被不可靠的规则误导。
    2.  **规则剪枝（Rule Pruning）**：计算每个规则的门控分数 $g_i$。该分数结合了规则在验证集上的平均满足度（Mean）和稳定性（方差的指数衰减）。只有得分高于阈值 $\tau$ 的规则才会被保留。
    3.  **阶段二：微调**。在保留的精炼知识库上进行最终训练，使模型在保持高准确率的同时严格遵守核心领域规则。

### 3. 实验设计
*   **数据集**：使用了四个真实的公开事件日志：**Sepsis**（医疗败血症）、**BPIC2012**、**BPIC2017**（贷款申请流程）和 **Traffic Fines**（交通罚单管理）。
*   **评估场景**：
    *   **时间切分（Temporal Split）**：按时间顺序划分训练/测试集，评估常规预测性能。
    *   **合规感知切分（Compliance-aware Split）**：测试集富含符合逻辑规则的样本，评估模型在受限/稀疏数据下的泛化能力。
*   **对比基准（Benchmarks）**：
    *   纯数据驱动：LSTM、Transformer (TRF)。
    *   神经符号变体：LTN-Data（仅含数据公理）、LTN-NoP（含知识但不进行剪枝）。
    *   本文方法：Two-Stage-L (基于LSTM)、Two-Stage-T (基于Transformer)。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 Adam 优化器，学习率为 0.001，Batch Size 为 32。模型结构相对轻量（2层 LSTM 或 2头 Transformer，隐藏层维度 128），表明该方法对算力需求并不苛刻，重点在于逻辑约束的集成。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同领域的真实数据集上进行了测试，每组实验均运行了 **5 个随机种子**并报告了均值和标准差。
*   **消融实验**：专门设计了 RQ2 来验证“两阶段优化”和“剪枝”的必要性，对比了不加控制的知识注入效果。
*   **充分性评价**：实验设计较为全面，涵盖了不同架构（RNN vs Transformer）和不同数据分布（常规 vs 合规受限），客观地展示了知识注入在不同数据规模下的收益递减效应，具有较高的公信力。

### 6. 主要结论与发现
*   **知识注入的有效性**：在所有数据集上，加入领域知识的模型均优于纯数据驱动模型，尤其在小样本数据集（如 Sepsis）上 F1 分数提升显著（最高达 5.23%）。
*   **两阶段优化的必要性**：**未经处理的知识注入（LTN-NoP）会导致性能灾难性下降**（如在 Sepsis 上 F1 从 70% 跌至 18%），证明了规则剪枝和加权损失是神经符号学习成功的关键。
*   **合规场景的优越性**：在合规样本稀缺（仅占 4%）的场景下，该方法展现了极强的归纳偏置能力，性能远超无法学习到稀有规则的纯深度学习模型。

### 7. 优点
*   **系统化的知识建模**：将复杂的流程挖掘约束（LTL）无缝集成到可微的张量运算中。
*   **解决“空虚满足”问题**：通过两阶段策略解决了 LTN 优化中逻辑约束与预测准确性之间的冲突。
*   **架构无关性**：该框架可以作为插件应用于 LSTM、Transformer 或任何其他神经网络骨干。

### 8. 不足与局限
*   **规则来源依赖**：规则的质量高度依赖于外部的流程挖掘工具（如 Declare）或领域专家的先验知识，若初始规则库质量极差，剪枝压力会增大。
*   **超参数敏感性**：阈值 $\tau$ 和权重 $\alpha, \beta$ 的选择对结果有影响，可能需要针对不同数据集进行手动调优。
*   **任务局限**：目前主要集中在二分类任务（结果预测），尚未扩展到多分类或更复杂的后缀预测（Suffix Prediction）任务。

（完）
